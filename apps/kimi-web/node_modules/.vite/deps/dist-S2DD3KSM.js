import {
  __export,
  __privateAdd,
  __privateGet,
  __privateMethod,
  __privateSet,
  __publicField,
  __superGet
} from "./chunk-B4Q33VKO.js";

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/constants.js
var DIFFS_TAG_NAME = "diffs-container";
var DIFFS_DEVELOPMENT_BUILD = (() => {
  try {
    return false;
  } catch {
    return false;
  }
})();
var COMMIT_METADATA_SPLIT = /(?=^From [a-f0-9]+ .+$)/m;
var GIT_DIFF_FILE_BREAK_REGEX = /(?=^diff --git)/gm;
var UNIFIED_DIFF_FILE_BREAK_REGEX = /(?=^---\s+\S)/gm;
var FILE_CONTEXT_BLOB = /(?=^@@ )/gm;
var HUNK_HEADER = /^@@ -(\d+)(?:,(\d+))? \+(\d+)(?:,(\d+))? @@(?: (.*))?/m;
var SPLIT_WITH_NEWLINES = new RegExp("(?<=\\n)");
var FILENAME_HEADER_REGEX = /^(---|\+\+\+)\s+([^\t\r\n]+)/;
var FILENAME_HEADER_REGEX_GIT = /^(---|\+\+\+)\s+[ab]\/([^\t\r\n]+)/;
var ALTERNATE_FILE_NAMES_GIT = /^diff --git (?:"a\/(.+?)"|a\/(.+?)) (?:"b\/(.+?)"|b\/(.+?))$/;
var INDEX_LINE_METADATA = /^index ([0-9a-f]+)\.\.([0-9a-f]+)(?: (\d+))?$/i;
var MERGE_CONFLICT_START_MARKER_REGEX = /^<{7,}(?:\s.*)?$/;
var MERGE_CONFLICT_BASE_MARKER_REGEX = /^\|{7,}(?:\s.*)?$/;
var MERGE_CONFLICT_SEPARATOR_MARKER_REGEX = /^={7,}$/;
var MERGE_CONFLICT_END_MARKER_REGEX = /^>{7,}(?:\s.*)?$/;
var HEADER_PREFIX_SLOT_ID = "header-prefix";
var HEADER_FILENAME_SUFFIX_SLOT_ID = "header-filename-suffix";
var HEADER_METADATA_SLOT_ID = "header-metadata";
var CUSTOM_HEADER_SLOT_ID = "header-custom";
var DEFAULT_THEMES = {
  dark: "pierre-dark",
  light: "pierre-light"
};
var THEME_CSS_ATTRIBUTE = "data-theme-css";
var UNSAFE_CSS_ATTRIBUTE = "data-unsafe-css";
var CORE_CSS_ATTRIBUTE = "data-core-css";
var DIFFS_SCROLLBAR_MEASURE_ATTRIBUTE = "data-diffs-scrollbar-measure";
var CODE_VIEW_HEADER_ATTRIBUTE = "data-diffs-code-view-header";
var CODE_VIEW_FOOTER_ATTRIBUTE = "data-diffs-code-view-footer";
var DIFFS_SCROLLBAR_GUTTER_MEASURED_PROPERTY = "--diffs-scrollbar-gutter-measured";
var DEFAULT_COLLAPSED_CONTEXT_THRESHOLD = 1;
var DEFAULT_TOKENIZE_MAX_LENGTH = 1e5;
var DEFAULT_VIRTUAL_FILE_METRICS = {
  hunkLineCount: 50,
  lineHeight: 20,
  diffHeaderHeight: 44,
  spacing: 8
};
var DEFAULT_CODE_VIEW_FILE_METRICS = {
  ...DEFAULT_VIRTUAL_FILE_METRICS,
  hunkLineCount: 1
};
var DEFAULT_CODE_VIEW_LAYOUT = {
  paddingTop: 8,
  paddingBottom: 8,
  gap: 8
};
var DEFAULT_SMOOTH_SCROLL_SETTINGS = {
  omega: 0.015,
  positionEpsilon: 0.5,
  velocityEpsilon: 0.05
};
var DEFAULT_EXPANDED_REGION = Object.freeze({
  fromStart: 0,
  fromEnd: 0
});
var DEFAULT_RENDER_RANGE = {
  startingLine: 0,
  totalLines: Infinity,
  bufferBefore: 0,
  bufferAfter: 0
};
var EMPTY_RENDER_RANGE = {
  startingLine: 0,
  totalLines: 0,
  bufferBefore: 0,
  bufferAfter: 0
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/managers/UniversalRenderingManager.js
var callbacks = /* @__PURE__ */ new Set();
var frameId = null;
function queueRender(callback) {
  callbacks.add(callback);
  frameId ?? (frameId = requestAnimationFrame(render));
}
function dequeueRender(callback) {
  if (callbacks.delete(callback) && callbacks.size === 0 && frameId != null) {
    cancelAnimationFrame(frameId);
    frameId = null;
  }
}
function clearRenderQueue() {
  callbacks.clear();
  if (frameId != null && typeof cancelAnimationFrame === "function") cancelAnimationFrame(frameId);
  frameId = null;
}
function render(time) {
  const toIterate = new Set(callbacks);
  callbacks.clear();
  for (const callback of toIterate) try {
    callback(time);
  } catch (error) {
    console.error(error);
  }
  if (callbacks.size > 0) frameId = requestAnimationFrame(render);
  else frameId = null;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areObjectsEqual.js
function areObjectsEqual(objA, objB, omitKeys) {
  if (objA === objB || objA == null || objB == null) return objA === objB;
  const omitSet = new Set(omitKeys);
  const keysA = Object.keys(objA);
  const keysBSet = new Set(Object.keys(objB));
  for (const key2 of keysA) {
    keysBSet.delete(key2);
    if (omitSet.has(key2)) continue;
    if (!(key2 in objB) || objA[key2] !== objB[key2]) return false;
  }
  for (const key2 of Array.from(keysBSet)) if (!omitSet.has(key2)) return false;
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areThemesEqual.js
function areThemesEqual(themeA, themeB) {
  if (themeA == null || themeB == null || typeof themeA === "string" || typeof themeB === "string") return themeA === themeB;
  return themeA.dark === themeB.dark && themeA.light === themeB.light;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areOptionsEqual.js
function areOptionsEqual(optionsA, optionsB) {
  const themeA = (optionsA == null ? void 0 : optionsA.theme) ?? DEFAULT_THEMES;
  const themeB = (optionsB == null ? void 0 : optionsB.theme) ?? DEFAULT_THEMES;
  const diffOptsA = getParseDiffOptions(optionsA);
  const diffOptsB = getParseDiffOptions(optionsB);
  return areThemesEqual(themeA, themeB) && areObjectsEqual(optionsA, optionsB, ["theme", "parseDiffOptions"]) && areObjectsEqual(diffOptsA, diffOptsB);
}
function getParseDiffOptions(options) {
  if (options != null && "parseDiffOptions" in options) return options.parseDiffOptions;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areSelectionsEqual.js
function areSelectionsEqual(selectionA, selectionB) {
  return (selectionA == null ? void 0 : selectionA.start) === (selectionB == null ? void 0 : selectionB.start) && (selectionA == null ? void 0 : selectionA.end) === (selectionB == null ? void 0 : selectionB.end) && (selectionA == null ? void 0 : selectionA.side) === (selectionB == null ? void 0 : selectionB.side) && (selectionA == null ? void 0 : selectionA.endSide) === (selectionB == null ? void 0 : selectionB.endSide);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createWindowFromScrollPosition.js
function createWindowFromScrollPosition({ scrollTop, scrollHeight, height, fitPerfectly = false, fitPerfectlyOverscroll = 0, overscrollSize }) {
  const windowHeight = height + overscrollSize * 2;
  const effectiveHeight = fitPerfectly ? height + fitPerfectlyOverscroll * 2 : windowHeight;
  scrollHeight = Math.max(scrollHeight, effectiveHeight);
  if (windowHeight >= scrollHeight || fitPerfectly) {
    const top2 = Math.max(scrollTop - fitPerfectlyOverscroll, 0);
    const bottom2 = Math.min(scrollTop + effectiveHeight, scrollHeight);
    return {
      top: top2,
      bottom: Math.max(bottom2, top2)
    };
  }
  let top = scrollTop + height / 2 - windowHeight / 2;
  let bottom = top + windowHeight;
  if (top < 0) top = 0;
  if (bottom > scrollHeight) bottom = scrollHeight;
  top = Math.floor(Math.max(top, 0));
  return {
    top,
    bottom: Math.ceil(Math.max(Math.min(bottom, scrollHeight), top))
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/cleanLastNewline.js
function cleanLastNewline(contents) {
  let end = contents.length;
  if (contents.charCodeAt(end - 1) === 10) {
    end--;
    if (contents.charCodeAt(end - 1) === 13) end--;
  }
  return contents.slice(0, end);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/detachString.js
var stringDetachEncoder = new TextEncoder();
var stringDetachDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
var SURROGATE_CODE_UNIT_PATTERN = /[\uD800-\uDFFF]/;
var STRING_DETACH_INITIAL_BUFFER_SIZE = 1024;
var stringDetachBuffer = new Uint8Array(STRING_DETACH_INITIAL_BUFFER_SIZE);
function releaseStringDetachBuffer() {
  if (stringDetachBuffer.length !== STRING_DETACH_INITIAL_BUFFER_SIZE) stringDetachBuffer = new Uint8Array(STRING_DETACH_INITIAL_BUFFER_SIZE);
}
function detachString(value) {
  if (value.length === 0) return value;
  if (SURROGATE_CODE_UNIT_PATTERN.test(value)) return JSON.parse(JSON.stringify(value));
  const requiredByteLength = value.length * 3;
  if (stringDetachBuffer.length < requiredByteLength) stringDetachBuffer = new Uint8Array(requiredByteLength);
  const { written } = stringDetachEncoder.encodeInto(value, stringDetachBuffer);
  return stringDetachDecoder.decode(stringDetachBuffer.subarray(0, written));
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getHunkSideBoundaries.js
function getHunkSideStartBoundary(start, count) {
  return start - (count === 0 ? 0 : 1);
}
function getHunkSideEndBoundary(start, count) {
  return getHunkSideStartBoundary(start, count) + count;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/realignChangeContent.js
var MAX_ALIGNMENT_COMPARISONS = 4096;
var MIN_IMPROVEMENT_PER_PAIR = 0.5;
function realignChangeContentBySimilarity(diff) {
  for (const hunk of diff.hunks) {
    for (let index = 0; index < hunk.hunkContent.length; index++) {
      const content = hunk.hunkContent[index];
      if (content.type !== "change") continue;
      const replacement = realignChangeBlock(diff, content);
      if (replacement != null) {
        hunk.hunkContent.splice(index, 1, ...replacement);
        index += replacement.length - 1;
      }
    }
    slideBlankBoundaryBlocksUp(hunk, diff);
  }
}
function slideBlankBoundaryBlocksUp(hunk, diff) {
  const { hunkContent } = hunk;
  for (let index = 1; index < hunkContent.length; index++) {
    const block = hunkContent[index];
    const previous = hunkContent[index - 1];
    if (block.type !== "change" || block.additions > 0 && block.deletions > 0 || previous.type !== "context") continue;
    const isInsert = block.additions > 0;
    const lines = isInsert ? diff.additionLines : diff.deletionLines;
    const blockStart = isInsert ? block.additionLineIndex : block.deletionLineIndex;
    const blockLength = isInsert ? block.additions : block.deletions;
    const unit = lines[blockStart] ?? "";
    if (unit.trim() !== "") continue;
    let uniform = true;
    for (let offset = 1; offset < blockLength; offset++) if (lines[blockStart + offset] !== unit) {
      uniform = false;
      break;
    }
    if (!uniform) continue;
    let slide = 0;
    while (slide < previous.lines && diff.additionLines[previous.additionLineIndex + previous.lines - 1 - slide] === unit) slide++;
    if (slide === 0) continue;
    if (index === 1 && slide === previous.lines) continue;
    block.additionLineIndex -= slide;
    block.deletionLineIndex -= slide;
    const blockAdditionEnd = block.additionLineIndex + block.additions;
    const blockDeletionEnd = block.deletionLineIndex + block.deletions;
    const next = hunkContent[index + 1];
    if ((next == null ? void 0 : next.type) === "context") {
      next.lines += slide;
      next.additionLineIndex = blockAdditionEnd;
      next.deletionLineIndex = blockDeletionEnd;
    } else hunkContent.splice(index + 1, 0, {
      type: "context",
      lines: slide,
      additionLineIndex: blockAdditionEnd,
      deletionLineIndex: blockDeletionEnd
    });
    previous.lines -= slide;
    if (previous.lines === 0) {
      hunkContent.splice(index - 1, 1);
      index--;
    }
  }
}
function realignChangeBlock(diff, content) {
  const { deletions, additions, deletionLineIndex, additionLineIndex } = content;
  const pairCount = Math.min(deletions, additions);
  const surplus = Math.abs(additions - deletions);
  if (pairCount === 0 || surplus === 0 || pairCount * (surplus + 1) > MAX_ALIGNMENT_COMPARISONS) return null;
  const strippedDeletions = [];
  for (let line = 0; line < deletions; line++) strippedDeletions.push(stripWhitespace(diff.deletionLines[deletionLineIndex + line] ?? ""));
  const strippedAdditions = [];
  for (let line = 0; line < additions; line++) strippedAdditions.push(stripWhitespace(diff.additionLines[additionLineIndex + line] ?? ""));
  const additionsAreLonger = additions > deletions;
  let bestOffset = 0;
  let bestScore = -1;
  for (let offset = 0; offset <= surplus; offset++) {
    let score = 0;
    for (let pair = 0; pair < pairCount; pair++) score += lineSimilarity(strippedDeletions[pair + (additionsAreLonger ? 0 : offset)], strippedAdditions[pair + (additionsAreLonger ? offset : 0)]);
    if (offset === 0) bestScore = score + pairCount * MIN_IMPROVEMENT_PER_PAIR;
    else if (score > bestScore) {
      bestScore = score;
      bestOffset = offset;
    }
  }
  if (bestOffset === 0) return null;
  const blocks = [];
  const pushBlock = (blockDeletions, blockAdditions, blockDeletionIndex, blockAdditionIndex) => {
    if (blockDeletions > 0 || blockAdditions > 0) blocks.push({
      type: "change",
      deletions: blockDeletions,
      additions: blockAdditions,
      deletionLineIndex: blockDeletionIndex,
      additionLineIndex: blockAdditionIndex
    });
  };
  if (additionsAreLonger) {
    pushBlock(0, bestOffset, deletionLineIndex, additionLineIndex);
    pushBlock(pairCount, pairCount, deletionLineIndex, additionLineIndex + bestOffset);
    pushBlock(0, additions - pairCount - bestOffset, deletionLineIndex + pairCount, additionLineIndex + bestOffset + pairCount);
  } else {
    pushBlock(bestOffset, 0, deletionLineIndex, additionLineIndex);
    pushBlock(pairCount, pairCount, deletionLineIndex + bestOffset, additionLineIndex);
    pushBlock(deletions - pairCount - bestOffset, 0, deletionLineIndex + bestOffset + pairCount, additionLineIndex + pairCount);
  }
  return blocks;
}
var WHITESPACE = /\s+/g;
function stripWhitespace(line) {
  return line.replace(WHITESPACE, "");
}
function lineSimilarity(a2, b3) {
  if (a2 === b3) return 1;
  const maxLength = Math.max(a2.length, b3.length);
  const minLength = Math.min(a2.length, b3.length);
  if (minLength === 0) return 0;
  let prefix = 0;
  while (prefix < minLength && a2[prefix] === b3[prefix]) prefix++;
  let suffix = 0;
  while (suffix < minLength - prefix && a2[a2.length - 1 - suffix] === b3[b3.length - 1 - suffix]) suffix++;
  return (prefix + suffix) / maxLength;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/parsePatchFiles.js
function processPatch(data, cacheKeyPrefix, throwOnError) {
  try {
    return _processPatch(data, cacheKeyPrefix, throwOnError);
  } finally {
    releaseStringDetachBuffer();
  }
}
function _processPatch(data, cacheKeyPrefix, throwOnError = false) {
  const isGitDiff = isGitDiffPatch(data);
  const rawFiles = isGitDiff ? splitGitDiffFiles(data) : splitUnifiedDiffFiles(data);
  let patchMetadata;
  const files = [];
  for (const fileOrPatchMetadata of rawFiles) {
    if (isGitDiff && !GIT_DIFF_FILE_BREAK_REGEX.test(fileOrPatchMetadata)) {
      if (patchMetadata == null) patchMetadata = detachString(fileOrPatchMetadata);
      else if (throwOnError) throw Error("parsePatchContent: unknown file blob");
      else console.error("parsePatchContent: unknown file blob:", fileOrPatchMetadata);
      continue;
    } else if (!isGitDiff && !startsWithUnifiedDiffFileHeader(fileOrPatchMetadata)) {
      if (patchMetadata == null) patchMetadata = detachString(fileOrPatchMetadata);
      else if (throwOnError) throw Error("parsePatchContent: unknown file blob");
      else console.error("parsePatchContent: unknown file blob:", fileOrPatchMetadata);
      continue;
    }
    const currentFile = _processFile(fileOrPatchMetadata, {
      cacheKey: cacheKeyPrefix != null ? `${cacheKeyPrefix}-${files.length}` : void 0,
      isGitDiff,
      throwOnError
    });
    if (currentFile != null) files.push(currentFile);
  }
  return {
    patchMetadata,
    files
  };
}
function processFile(fileDiffString, options) {
  try {
    return _processFile(fileDiffString, options);
  } finally {
    releaseStringDetachBuffer();
  }
}
function _processFile(fileDiffString, { cacheKey, isGitDiff = GIT_DIFF_FILE_BREAK_REGEX.test(fileDiffString), oldFile, newFile, throwOnError = false } = {}) {
  let lastHunkEnd = 0;
  const hunks = splitAtLinePrefix(fileDiffString, "@@ ");
  let currentFile;
  const isPartial = oldFile == null || newFile == null;
  let deletionLineIndex = 0;
  let additionLineIndex = 0;
  for (const hunk of hunks) {
    const lines = splitWithNewlines(hunk);
    const firstLine = lines[0];
    if (firstLine == null) {
      if (throwOnError) throw Error("parsePatchContent: invalid hunk");
      else console.error("parsePatchContent: invalid hunk", hunk);
      continue;
    }
    const fileHeader = parseHunkHeader(firstLine);
    let additionLines = 0;
    let deletionLines = 0;
    if (fileHeader == null || currentFile == null) {
      if (currentFile != null) {
        if (throwOnError) throw Error("parsePatchContent: Invalid hunk");
        else console.error("parsePatchContent: Invalid hunk", hunk);
        continue;
      }
      currentFile = {
        name: "",
        type: "change",
        hunks: [],
        splitLineCount: 0,
        unifiedLineCount: 0,
        isPartial,
        additionLines: !isPartial && oldFile != null && newFile != null ? splitFileContents(newFile.contents) : [],
        deletionLines: !isPartial && oldFile != null && newFile != null ? splitFileContents(oldFile.contents) : [],
        cacheKey: maybeDetachOptionalString(cacheKey)
      };
      if (currentFile.additionLines.length === 1 && (newFile == null ? void 0 : newFile.contents) === "") currentFile.additionLines.length = 0;
      if (currentFile.deletionLines.length === 1 && (oldFile == null ? void 0 : oldFile.contents) === "") currentFile.deletionLines.length = 0;
      for (const line of lines) {
        if (line.startsWith("diff --git")) {
          const filenameMatch2 = line.trim().match(ALTERNATE_FILE_NAMES_GIT);
          const prevName = (filenameMatch2 == null ? void 0 : filenameMatch2[1]) ?? (filenameMatch2 == null ? void 0 : filenameMatch2[2]);
          const name = (filenameMatch2 == null ? void 0 : filenameMatch2[3]) ?? (filenameMatch2 == null ? void 0 : filenameMatch2[4]);
          if (prevName == null || name == null) {
            if (throwOnError) throw Error("parsePatchContent: invalid git diff header");
            else console.error("parsePatchContent: invalid git diff header", line);
            continue;
          }
          currentFile.name = detachString(name.trim());
          if (prevName !== name) currentFile.prevName = detachString(prevName.trim());
          continue;
        }
        const filenameMatch = line.startsWith("---") || line.startsWith("+++") ? line.match(isGitDiff ? FILENAME_HEADER_REGEX_GIT : FILENAME_HEADER_REGEX) : null;
        if (filenameMatch != null) {
          const [, type, fileName] = filenameMatch;
          if (type === "---" && fileName !== "/dev/null") {
            const detachedFileName = detachString(fileName.trim());
            currentFile.prevName = detachedFileName;
            currentFile.name = detachedFileName;
          } else if (type === "+++" && fileName !== "/dev/null") currentFile.name = detachString(fileName.trim());
        } else if (isGitDiff) {
          if (line.startsWith("new mode ")) currentFile.mode = detachString(line.slice(8).trim());
          if (line.startsWith("old mode ")) currentFile.prevMode = detachString(line.slice(8).trim());
          if (line.startsWith("new file mode")) {
            currentFile.type = "new";
            currentFile.mode = detachString(line.slice(13).trim());
          }
          if (line.startsWith("deleted file mode")) {
            currentFile.type = "deleted";
            currentFile.mode = detachString(line.slice(17).trim());
          }
          if (line.startsWith("similarity index")) if (line.startsWith("similarity index 100%")) currentFile.type = "rename-pure";
          else currentFile.type = "rename-changed";
          if (line.startsWith("index ")) {
            const [, prevObjectId, newObjectId, mode] = line.trim().match(INDEX_LINE_METADATA) ?? [];
            if (prevObjectId != null) currentFile.prevObjectId = detachString(prevObjectId);
            if (newObjectId != null) currentFile.newObjectId = detachString(newObjectId);
            if (mode != null) currentFile.mode = detachString(mode);
          }
          if (line.startsWith("rename from ")) currentFile.prevName = detachString(line.slice(12).trim());
          if (line.startsWith("rename to ")) currentFile.name = detachString(line.slice(10).trim());
        }
      }
      continue;
    }
    let currentContent;
    let lastLineType;
    while (lines.length > 0 && (lines[lines.length - 1] === "\n" || lines[lines.length - 1] === "\r" || lines[lines.length - 1] === "\r\n" || lines[lines.length - 1] === "")) lines.pop();
    const { additionStart, deletionStart } = fileHeader;
    deletionLineIndex = isPartial ? deletionLineIndex : deletionStart - 1;
    additionLineIndex = isPartial ? additionLineIndex : additionStart - 1;
    const hunkData = {
      collapsedBefore: 0,
      splitLineCount: 0,
      splitLineStart: 0,
      unifiedLineCount: 0,
      unifiedLineStart: 0,
      additionCount: fileHeader.additionCount,
      additionStart,
      additionLines,
      deletionCount: fileHeader.deletionCount,
      deletionStart,
      deletionLines,
      deletionLineIndex,
      additionLineIndex,
      hunkContent: [],
      hunkContext: maybeDetachOptionalString(fileHeader.hunkContext),
      hunkSpecs: detachString(firstLine),
      noEOFCRAdditions: false,
      noEOFCRDeletions: false
    };
    let parsedAdditionLines = 0;
    let parsedDeletionLines = 0;
    for (let lineIndex = 1; lineIndex < lines.length; lineIndex++) {
      const rawLine = lines[lineIndex];
      if (parsedAdditionLines >= hunkData.additionCount && parsedDeletionLines >= hunkData.deletionCount && !rawLine.startsWith("\\")) {
        if (throwOnError && isHunkBodyLine(rawLine) && !isFormatPatchVersionSeparator(rawLine)) throw Error("parsePatchContent: hunk has more lines than expected");
        break;
      }
      const firstChar = rawLine[0];
      if (firstChar !== "+" && firstChar !== "-" && firstChar !== " " && firstChar !== "\\") {
        if (throwOnError) throw Error("parsePatchContent: invalid hunk line");
        console.error(`parseLineType: Invalid firstChar: "${firstChar}", full line: "${rawLine}"`);
        console.error("processFile: invalid rawLine:", rawLine);
        continue;
      }
      const type = parseRawLineType(firstChar);
      if (type === "addition") {
        if (throwOnError && parsedAdditionLines >= hunkData.additionCount) throw Error("parsePatchContent: hunk has too many addition lines");
        const line = getParsedLineContent(rawLine);
        if (currentContent == null || currentContent.type !== "change") {
          currentContent = createContentGroup("change", deletionLineIndex, additionLineIndex);
          hunkData.hunkContent.push(currentContent);
        }
        additionLineIndex++;
        parsedAdditionLines++;
        if (isPartial) currentFile.additionLines.push(line);
        currentContent.additions++;
        additionLines++;
        lastLineType = "addition";
      } else if (type === "deletion") {
        if (throwOnError && parsedDeletionLines >= hunkData.deletionCount) throw Error("parsePatchContent: hunk has too many deletion lines");
        const line = getParsedLineContent(rawLine);
        if (currentContent == null || currentContent.type !== "change") {
          currentContent = createContentGroup("change", deletionLineIndex, additionLineIndex);
          hunkData.hunkContent.push(currentContent);
        }
        deletionLineIndex++;
        parsedDeletionLines++;
        if (isPartial) currentFile.deletionLines.push(line);
        currentContent.deletions++;
        deletionLines++;
        lastLineType = "deletion";
      } else if (type === "context") {
        if (throwOnError && (parsedDeletionLines >= hunkData.deletionCount || parsedAdditionLines >= hunkData.additionCount)) throw Error("parsePatchContent: hunk has too many context lines");
        const line = getParsedLineContent(rawLine);
        if (currentContent == null || currentContent.type !== "context") {
          currentContent = createContentGroup("context", deletionLineIndex, additionLineIndex);
          hunkData.hunkContent.push(currentContent);
        }
        additionLineIndex++;
        deletionLineIndex++;
        parsedAdditionLines++;
        parsedDeletionLines++;
        if (isPartial) {
          currentFile.deletionLines.push(line);
          currentFile.additionLines.push(line);
        }
        currentContent.lines++;
        lastLineType = "context";
      } else if (type === "metadata" && currentContent != null) {
        if (currentContent.type === "context") {
          hunkData.noEOFCRAdditions = true;
          hunkData.noEOFCRDeletions = true;
        } else if (lastLineType === "deletion") hunkData.noEOFCRDeletions = true;
        else if (lastLineType === "addition") hunkData.noEOFCRAdditions = true;
        if (isPartial && (lastLineType === "addition" || lastLineType === "context")) {
          const lastIndex = currentFile.additionLines.length - 1;
          if (lastIndex >= 0) currentFile.additionLines[lastIndex] = cleanLastNewline(currentFile.additionLines[lastIndex]);
        }
        if (isPartial && (lastLineType === "deletion" || lastLineType === "context")) {
          const lastIndex = currentFile.deletionLines.length - 1;
          if (lastIndex >= 0) currentFile.deletionLines[lastIndex] = cleanLastNewline(currentFile.deletionLines[lastIndex]);
        }
      }
    }
    if (throwOnError && (parsedAdditionLines !== hunkData.additionCount || parsedDeletionLines !== hunkData.deletionCount)) throw Error("parsePatchContent: hunk line count mismatch");
    hunkData.additionLines = additionLines;
    hunkData.deletionLines = deletionLines;
    hunkData.collapsedBefore = Math.max(getHunkSideStartBoundary(hunkData.additionStart, hunkData.additionCount) - lastHunkEnd, 0);
    currentFile.hunks.push(hunkData);
    lastHunkEnd = getHunkSideEndBoundary(hunkData.additionStart, hunkData.additionCount);
    for (const content of hunkData.hunkContent) if (content.type === "context") {
      hunkData.splitLineCount += content.lines;
      hunkData.unifiedLineCount += content.lines;
    } else {
      hunkData.splitLineCount += Math.max(content.additions, content.deletions);
      hunkData.unifiedLineCount += content.deletions + content.additions;
    }
    hunkData.splitLineStart = currentFile.splitLineCount + hunkData.collapsedBefore;
    hunkData.unifiedLineStart = currentFile.unifiedLineCount + hunkData.collapsedBefore;
    currentFile.splitLineCount += hunkData.collapsedBefore + hunkData.splitLineCount;
    currentFile.unifiedLineCount += hunkData.collapsedBefore + hunkData.unifiedLineCount;
  }
  if (currentFile == null) return;
  if (throwOnError && isPartial && !isGitDiff && currentFile.hunks.length === 0) throw Error("parsePatchContent: unified file has no hunks");
  if (currentFile.hunks.length > 0 && !isPartial && currentFile.additionLines.length > 0 && currentFile.deletionLines.length > 0) {
    const lastHunk = currentFile.hunks[currentFile.hunks.length - 1];
    const lastHunkEnd2 = getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount);
    const totalFileLines = currentFile.additionLines.length;
    const collapsedAfter = Math.max(totalFileLines - lastHunkEnd2, 0);
    currentFile.splitLineCount += collapsedAfter;
    currentFile.unifiedLineCount += collapsedAfter;
  }
  if (!isGitDiff) {
    if (currentFile.prevName != null && currentFile.name !== currentFile.prevName) if (currentFile.hunks.length > 0) currentFile.type = "rename-changed";
    else currentFile.type = "rename-pure";
    else if ((oldFile == null || oldFile.contents === "") && newFile != null && newFile.contents !== "") currentFile.type = "new";
    else if (oldFile != null && oldFile.contents !== "" && (newFile == null || newFile.contents === "")) currentFile.type = "deleted";
  }
  if (currentFile.type !== "rename-pure" && currentFile.type !== "rename-changed") currentFile.prevName = void 0;
  realignChangeContentBySimilarity(currentFile);
  return currentFile;
}
function parsePatchFiles(data, cacheKeyPrefix, throwOnError = false) {
  const patches = [];
  const rawPatches = hasCommitMetadataBoundary(data) ? data.split(COMMIT_METADATA_SPLIT) : [data];
  for (const patch of rawPatches) try {
    patches.push(processPatch(patch, cacheKeyPrefix != null ? `${cacheKeyPrefix}-${patches.length}` : void 0, throwOnError));
  } catch (error) {
    if (throwOnError) throw error;
    else console.error(error);
  }
  return patches;
}
function hasCommitMetadataBoundary(data) {
  return data.startsWith("From ") || data.includes("\nFrom ");
}
function splitFileContents(contents) {
  const lines = splitWithNewlines(contents);
  for (let index = 0; index < lines.length; index++) lines[index] = detachString(lines[index]);
  return lines;
}
function splitWithNewlines(contents) {
  if (contents.length === 0) return [""];
  const lines = [];
  let startIndex = 0;
  for (; ; ) {
    const newlineIndex = contents.indexOf("\n", startIndex);
    if (newlineIndex === -1) break;
    lines.push(contents.slice(startIndex, newlineIndex + 1));
    startIndex = newlineIndex + 1;
  }
  if (startIndex < contents.length) lines.push(contents.slice(startIndex));
  return lines;
}
function splitGitDiffFiles(contents) {
  return splitAtLinePrefix(contents, "diff --git");
}
function splitUnifiedDiffFiles(contents) {
  if (contents.length === 0) return [""];
  const parts = [];
  let partStartIndex = 0;
  let lineStartIndex = 0;
  let remainingDeletionLines = 0;
  let remainingAdditionLines = 0;
  let hasOpenedUnifiedFile = false;
  while (lineStartIndex < contents.length) {
    const nextLineStartIndex = getNextLineStartIndex(contents, lineStartIndex);
    if (remainingDeletionLines <= 0 && remainingAdditionLines <= 0) {
      if (isUnifiedDiffFileHeaderAt(contents, lineStartIndex)) {
        if (lineStartIndex > partStartIndex) parts.push(contents.slice(partStartIndex, lineStartIndex));
        partStartIndex = lineStartIndex;
        hasOpenedUnifiedFile = true;
        lineStartIndex = getNextLineStartIndex(contents, nextLineStartIndex);
        continue;
      }
      if (hasOpenedUnifiedFile && contents.startsWith("@@ -", lineStartIndex)) {
        const fileHeader = parseHunkHeader(contents.slice(lineStartIndex, nextLineStartIndex));
        if (fileHeader != null) {
          remainingDeletionLines = fileHeader.deletionCount;
          remainingAdditionLines = fileHeader.additionCount;
        }
      }
      lineStartIndex = nextLineStartIndex;
      continue;
    }
    const firstChar = contents[lineStartIndex];
    if (firstChar === "\\") {
      lineStartIndex = nextLineStartIndex;
      continue;
    }
    if (firstChar === " ") {
      remainingDeletionLines = Math.max(remainingDeletionLines - 1, 0);
      remainingAdditionLines = Math.max(remainingAdditionLines - 1, 0);
    } else if (firstChar === "-") remainingDeletionLines = Math.max(remainingDeletionLines - 1, 0);
    else if (firstChar === "+") remainingAdditionLines = Math.max(remainingAdditionLines - 1, 0);
    lineStartIndex = nextLineStartIndex;
  }
  parts.push(contents.slice(partStartIndex));
  return parts;
}
function startsWithUnifiedDiffFileHeader(contents) {
  return isUnifiedDiffFileHeaderAt(contents, 0);
}
function isUnifiedDiffFileHeaderAt(contents, lineStartIndex) {
  const nextLineStartIndex = getNextLineStartIndex(contents, lineStartIndex);
  return isUnifiedDiffHeaderLineAt(contents, lineStartIndex, "---") && isUnifiedDiffHeaderLineAt(contents, nextLineStartIndex, "+++");
}
function isUnifiedDiffHeaderLineAt(contents, lineStartIndex, prefix) {
  if (!contents.startsWith(prefix, lineStartIndex)) return false;
  const separator = contents[lineStartIndex + prefix.length];
  if (separator !== " " && separator !== "	") return false;
  for (let index = lineStartIndex + prefix.length + 1; index < contents.length; index++) {
    const char = contents[index];
    if (char === "\n" || char === "\r") break;
    if (char !== " " && char !== "	") return true;
  }
  return false;
}
function getNextLineStartIndex(contents, lineStartIndex) {
  const newlineIndex = contents.indexOf("\n", lineStartIndex);
  return newlineIndex === -1 ? contents.length : newlineIndex + 1;
}
function isHunkBodyLine(line) {
  const firstChar = line[0];
  return firstChar === "+" || firstChar === "-" || firstChar === " ";
}
function isFormatPatchVersionSeparator(line) {
  if (!line.startsWith("--")) return false;
  for (let index = 2; index < line.length; index++) {
    const char = line[index];
    if (char !== " " && char !== "	" && char !== "\n" && char !== "\r") return false;
  }
  return true;
}
function parseHunkHeader(line) {
  if (!line.startsWith("@@ -")) return;
  let index = 4;
  const deletionStartResult = readPositiveInteger(line, index);
  if (deletionStartResult == null) return;
  const deletionStart = deletionStartResult.value;
  index = deletionStartResult.endIndex;
  let deletionCount = 1;
  if (line[index] === ",") {
    const deletionCountResult = readPositiveInteger(line, index + 1);
    if (deletionCountResult == null) return;
    deletionCount = deletionCountResult.value;
    index = deletionCountResult.endIndex;
  }
  if (line[index] !== " " || line[index + 1] !== "+") return;
  index += 2;
  const additionStartResult = readPositiveInteger(line, index);
  if (additionStartResult == null) return;
  const additionStart = additionStartResult.value;
  index = additionStartResult.endIndex;
  let additionCount = 1;
  if (line[index] === ",") {
    const additionCountResult = readPositiveInteger(line, index + 1);
    if (additionCountResult == null) return;
    additionCount = additionCountResult.value;
    index = additionCountResult.endIndex;
  }
  if (line[index] !== " " || line[index + 1] !== "@" || line[index + 2] !== "@") return;
  let hunkContext;
  const contextStartIndex = index + 3;
  if (line[contextStartIndex] === " ") hunkContext = trimLineEnd(line.slice(contextStartIndex + 1));
  return {
    additionCount,
    additionStart,
    deletionCount,
    deletionStart,
    hunkContext
  };
}
function readPositiveInteger(value, startIndex) {
  let index = startIndex;
  let parsedValue = 0;
  for (; index < value.length; index++) {
    const digit = value.charCodeAt(index) - 48;
    if (digit < 0 || digit > 9) break;
    parsedValue = parsedValue * 10 + digit;
  }
  if (index === startIndex) return;
  return {
    value: parsedValue,
    endIndex: index
  };
}
function trimLineEnd(value) {
  if (value.endsWith("\r\n")) return value.slice(0, -2);
  if (value.endsWith("\n")) return value.slice(0, -1);
  return value;
}
function isGitDiffPatch(data) {
  return data.startsWith("diff --git") || data.includes("\ndiff --git");
}
function splitAtLinePrefix(contents, prefix) {
  if (contents.length === 0) return [""];
  const newlinePrefix = `
${prefix}`;
  const firstBoundaryIndex = contents.startsWith(prefix) ? 0 : findLinePrefixIndex(contents, newlinePrefix, 0);
  if (firstBoundaryIndex === -1) return [contents];
  const parts = [];
  if (firstBoundaryIndex > 0) parts.push(contents.slice(0, firstBoundaryIndex));
  let startIndex = firstBoundaryIndex;
  for (; ; ) {
    const nextBoundaryIndex = findLinePrefixIndex(contents, newlinePrefix, startIndex + 1);
    if (nextBoundaryIndex === -1) break;
    parts.push(contents.slice(startIndex, nextBoundaryIndex));
    startIndex = nextBoundaryIndex;
  }
  parts.push(contents.slice(startIndex));
  return parts;
}
function findLinePrefixIndex(contents, newlinePrefix, fromIndex) {
  const index = contents.indexOf(newlinePrefix, fromIndex);
  return index === -1 ? -1 : index + 1;
}
function maybeDetachOptionalString(value) {
  return value == null ? value : detachString(value);
}
function parseRawLineType(firstChar) {
  return firstChar === " " ? "context" : firstChar === "\\" ? "metadata" : firstChar === "+" ? "addition" : "deletion";
}
function getParsedLineContent(rawLine) {
  const processedLine = rawLine.slice(1);
  return detachString(processedLine === "" ? "\n" : processedLine);
}
function createContentGroup(type, deletionLineIndex, additionLineIndex) {
  if (type === "change") return {
    type: "change",
    additions: 0,
    deletions: 0,
    additionLineIndex,
    deletionLineIndex
  };
  return {
    type: "context",
    lines: 0,
    additionLineIndex,
    deletionLineIndex
  };
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/base.js
var Diff = class {
  diff(oldStr, newStr, options = {}) {
    let callback;
    if (typeof options === "function") {
      callback = options;
      options = {};
    } else if ("callback" in options) {
      callback = options.callback;
    }
    const oldString = this.castInput(oldStr, options);
    const newString = this.castInput(newStr, options);
    const oldTokens = this.removeEmpty(this.tokenize(oldString, options));
    const newTokens = this.removeEmpty(this.tokenize(newString, options));
    return this.diffWithOptionsObj(oldTokens, newTokens, options, callback);
  }
  diffWithOptionsObj(oldTokens, newTokens, options, callback) {
    var _a9;
    const done = (value) => {
      value = this.postProcess(value, options);
      if (callback) {
        setTimeout(function() {
          callback(value);
        }, 0);
        return void 0;
      } else {
        return value;
      }
    };
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let editLength = 1;
    let maxEditLength = newLen + oldLen;
    if (options.maxEditLength != null) {
      maxEditLength = Math.min(maxEditLength, options.maxEditLength);
    }
    const maxExecutionTime = (_a9 = options.timeout) !== null && _a9 !== void 0 ? _a9 : Infinity;
    const abortAfterTimestamp = Date.now() + maxExecutionTime;
    const bestPath = [{ oldPos: -1, lastComponent: void 0 }];
    let newPos = this.extractCommon(bestPath[0], newTokens, oldTokens, 0, options);
    if (bestPath[0].oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
      return done(this.buildValues(bestPath[0].lastComponent, newTokens, oldTokens));
    }
    let minDiagonalToConsider = -Infinity, maxDiagonalToConsider = Infinity;
    const execEditLength = () => {
      for (let diagonalPath = Math.max(minDiagonalToConsider, -editLength); diagonalPath <= Math.min(maxDiagonalToConsider, editLength); diagonalPath += 2) {
        let basePath;
        const removePath = bestPath[diagonalPath - 1], addPath = bestPath[diagonalPath + 1];
        if (removePath) {
          bestPath[diagonalPath - 1] = void 0;
        }
        let canAdd = false;
        if (addPath) {
          const addPathNewPos = addPath.oldPos - diagonalPath;
          canAdd = addPath && 0 <= addPathNewPos && addPathNewPos < newLen;
        }
        const canRemove = removePath && removePath.oldPos + 1 < oldLen;
        if (!canAdd && !canRemove) {
          bestPath[diagonalPath] = void 0;
          continue;
        }
        if (!canRemove || canAdd && removePath.oldPos < addPath.oldPos) {
          basePath = this.addToPath(addPath, true, false, 0, options);
        } else {
          basePath = this.addToPath(removePath, false, true, 1, options);
        }
        newPos = this.extractCommon(basePath, newTokens, oldTokens, diagonalPath, options);
        if (basePath.oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
          return done(this.buildValues(basePath.lastComponent, newTokens, oldTokens)) || true;
        } else {
          bestPath[diagonalPath] = basePath;
          if (basePath.oldPos + 1 >= oldLen) {
            maxDiagonalToConsider = Math.min(maxDiagonalToConsider, diagonalPath - 1);
          }
          if (newPos + 1 >= newLen) {
            minDiagonalToConsider = Math.max(minDiagonalToConsider, diagonalPath + 1);
          }
        }
      }
      editLength++;
    };
    if (callback) {
      (function exec() {
        setTimeout(function() {
          if (editLength > maxEditLength || Date.now() > abortAfterTimestamp) {
            return callback(void 0);
          }
          if (!execEditLength()) {
            exec();
          }
        }, 0);
      })();
    } else {
      while (editLength <= maxEditLength && Date.now() <= abortAfterTimestamp) {
        const ret = execEditLength();
        if (ret) {
          return ret;
        }
      }
    }
  }
  addToPath(path, added, removed, oldPosInc, options) {
    const last = path.lastComponent;
    if (last && !options.oneChangePerToken && last.added === added && last.removed === removed) {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: { count: last.count + 1, added, removed, previousComponent: last.previousComponent }
      };
    } else {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: { count: 1, added, removed, previousComponent: last }
      };
    }
  }
  extractCommon(basePath, newTokens, oldTokens, diagonalPath, options) {
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let oldPos = basePath.oldPos, newPos = oldPos - diagonalPath, commonCount = 0;
    while (newPos + 1 < newLen && oldPos + 1 < oldLen && this.equals(oldTokens[oldPos + 1], newTokens[newPos + 1], options)) {
      newPos++;
      oldPos++;
      commonCount++;
      if (options.oneChangePerToken) {
        basePath.lastComponent = { count: 1, previousComponent: basePath.lastComponent, added: false, removed: false };
      }
    }
    if (commonCount && !options.oneChangePerToken) {
      basePath.lastComponent = { count: commonCount, previousComponent: basePath.lastComponent, added: false, removed: false };
    }
    basePath.oldPos = oldPos;
    return newPos;
  }
  equals(left, right, options) {
    if (options.comparator) {
      return options.comparator(left, right);
    } else {
      return left === right || !!options.ignoreCase && left.toLowerCase() === right.toLowerCase();
    }
  }
  removeEmpty(array) {
    const ret = [];
    for (let i2 = 0; i2 < array.length; i2++) {
      if (array[i2]) {
        ret.push(array[i2]);
      }
    }
    return ret;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  castInput(value, options) {
    return value;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  tokenize(value, options) {
    return Array.from(value);
  }
  join(chars) {
    return chars.join("");
  }
  postProcess(changeObjects, options) {
    return changeObjects;
  }
  get useLongestToken() {
    return false;
  }
  buildValues(lastComponent, newTokens, oldTokens) {
    const components = [];
    let nextComponent;
    while (lastComponent) {
      components.push(lastComponent);
      nextComponent = lastComponent.previousComponent;
      delete lastComponent.previousComponent;
      lastComponent = nextComponent;
    }
    components.reverse();
    const componentLen = components.length;
    let componentPos = 0, newPos = 0, oldPos = 0;
    for (; componentPos < componentLen; componentPos++) {
      const component = components[componentPos];
      if (!component.removed) {
        if (!component.added && this.useLongestToken) {
          let value = newTokens.slice(newPos, newPos + component.count);
          value = value.map(function(value2, i2) {
            const oldValue = oldTokens[oldPos + i2];
            return oldValue.length > value2.length ? oldValue : value2;
          });
          component.value = this.join(value);
        } else {
          component.value = this.join(newTokens.slice(newPos, newPos + component.count));
        }
        newPos += component.count;
        if (!component.added) {
          oldPos += component.count;
        }
      } else {
        component.value = this.join(oldTokens.slice(oldPos, oldPos + component.count));
        oldPos += component.count;
      }
    }
    return components;
  }
};

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/character.js
var CharacterDiff = class extends Diff {
};
var characterDiff = new CharacterDiff();
function diffChars(oldStr, newStr, options) {
  return characterDiff.diff(oldStr, newStr, options);
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/util/string.js
function longestCommonPrefix(str1, str2) {
  let i2;
  for (i2 = 0; i2 < str1.length && i2 < str2.length; i2++) {
    if (str1[i2] != str2[i2]) {
      return str1.slice(0, i2);
    }
  }
  return str1.slice(0, i2);
}
function longestCommonSuffix(str1, str2) {
  let i2;
  if (!str1 || !str2 || str1[str1.length - 1] != str2[str2.length - 1]) {
    return "";
  }
  for (i2 = 0; i2 < str1.length && i2 < str2.length; i2++) {
    if (str1[str1.length - (i2 + 1)] != str2[str2.length - (i2 + 1)]) {
      return str1.slice(-i2);
    }
  }
  return str1.slice(-i2);
}
function replacePrefix(string, oldPrefix, newPrefix) {
  if (string.slice(0, oldPrefix.length) != oldPrefix) {
    throw Error(`string ${JSON.stringify(string)} doesn't start with prefix ${JSON.stringify(oldPrefix)}; this is a bug`);
  }
  return newPrefix + string.slice(oldPrefix.length);
}
function replaceSuffix(string, oldSuffix, newSuffix) {
  if (!oldSuffix) {
    return string + newSuffix;
  }
  if (string.slice(-oldSuffix.length) != oldSuffix) {
    throw Error(`string ${JSON.stringify(string)} doesn't end with suffix ${JSON.stringify(oldSuffix)}; this is a bug`);
  }
  return string.slice(0, -oldSuffix.length) + newSuffix;
}
function removePrefix(string, oldPrefix) {
  return replacePrefix(string, oldPrefix, "");
}
function removeSuffix(string, oldSuffix) {
  return replaceSuffix(string, oldSuffix, "");
}
function maximumOverlap(string1, string2) {
  return string2.slice(0, overlapCount(string1, string2));
}
function overlapCount(a2, b3) {
  let startA = 0;
  if (a2.length > b3.length) {
    startA = a2.length - b3.length;
  }
  let endB = b3.length;
  if (a2.length < b3.length) {
    endB = a2.length;
  }
  const map = Array(endB);
  let k3 = 0;
  map[0] = 0;
  for (let j2 = 1; j2 < endB; j2++) {
    if (b3[j2] == b3[k3]) {
      map[j2] = map[k3];
    } else {
      map[j2] = k3;
    }
    while (k3 > 0 && b3[j2] != b3[k3]) {
      k3 = map[k3];
    }
    if (b3[j2] == b3[k3]) {
      k3++;
    }
  }
  k3 = 0;
  for (let i2 = startA; i2 < a2.length; i2++) {
    while (k3 > 0 && a2[i2] != b3[k3]) {
      k3 = map[k3];
    }
    if (a2[i2] == b3[k3]) {
      k3++;
    }
  }
  return k3;
}
function segment(string, segmenter) {
  const parts = [];
  for (const segmentObj of Array.from(segmenter.segment(string))) {
    const segment2 = segmentObj.segment;
    if (parts.length && /\s/.test(parts[parts.length - 1]) && /\s/.test(segment2)) {
      parts[parts.length - 1] += segment2;
    } else {
      parts.push(segment2);
    }
  }
  return parts;
}
function trailingWs(string, segmenter) {
  if (segmenter) {
    return leadingAndTrailingWs(string, segmenter)[1];
  }
  let i2;
  for (i2 = string.length - 1; i2 >= 0; i2--) {
    if (!string[i2].match(/\s/)) {
      break;
    }
  }
  return string.substring(i2 + 1);
}
function leadingWs(string, segmenter) {
  if (segmenter) {
    return leadingAndTrailingWs(string, segmenter)[0];
  }
  const match = string.match(/^\s*/);
  return match ? match[0] : "";
}
function leadingAndTrailingWs(string, segmenter) {
  if (!segmenter) {
    return [leadingWs(string), trailingWs(string)];
  }
  if (segmenter.resolvedOptions().granularity != "word") {
    throw new Error('The segmenter passed must have a granularity of "word"');
  }
  const segments = segment(string, segmenter);
  const firstSeg = segments[0];
  const lastSeg = segments[segments.length - 1];
  const head2 = /\s/.test(firstSeg) ? firstSeg : "";
  const tail = /\s/.test(lastSeg) ? lastSeg : "";
  return [head2, tail];
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/word.js
var extendedWordChars = "a-zA-Z0-9_\\u{AD}\\u{C0}-\\u{D6}\\u{D8}-\\u{F6}\\u{F8}-\\u{2C6}\\u{2C8}-\\u{2D7}\\u{2DE}-\\u{2FF}\\u{1E00}-\\u{1EFF}";
var tokenizeIncludingWhitespace = new RegExp(`[${extendedWordChars}]+|\\s+|[^${extendedWordChars}]`, "ug");
var WordDiff = class extends Diff {
  equals(left, right, options) {
    if (options.ignoreCase) {
      left = left.toLowerCase();
      right = right.toLowerCase();
    }
    return left.trim() === right.trim();
  }
  tokenize(value, options = {}) {
    let parts;
    if (options.intlSegmenter) {
      const segmenter = options.intlSegmenter;
      if (segmenter.resolvedOptions().granularity != "word") {
        throw new Error('The segmenter passed must have a granularity of "word"');
      }
      parts = segment(value, segmenter);
    } else {
      parts = value.match(tokenizeIncludingWhitespace) || [];
    }
    const tokens = [];
    let prevPart = null;
    parts.forEach((part) => {
      if (/\s/.test(part)) {
        if (prevPart == null) {
          tokens.push(part);
        } else {
          tokens.push(tokens.pop() + part);
        }
      } else if (prevPart != null && /\s/.test(prevPart)) {
        if (tokens[tokens.length - 1] == prevPart) {
          tokens.push(tokens.pop() + part);
        } else {
          tokens.push(prevPart + part);
        }
      } else {
        tokens.push(part);
      }
      prevPart = part;
    });
    return tokens;
  }
  join(tokens) {
    return tokens.map((token2, i2) => {
      if (i2 == 0) {
        return token2;
      } else {
        return token2.replace(/^\s+/, "");
      }
    }).join("");
  }
  postProcess(changes, options) {
    if (!changes || options.oneChangePerToken) {
      return changes;
    }
    let lastKeep = null;
    let insertion = null;
    let deletion = null;
    changes.forEach((change) => {
      if (change.added) {
        insertion = change;
      } else if (change.removed) {
        deletion = change;
      } else {
        if (insertion || deletion) {
          dedupeWhitespaceInChangeObjects(lastKeep, deletion, insertion, change, options.intlSegmenter);
        }
        lastKeep = change;
        insertion = null;
        deletion = null;
      }
    });
    if (insertion || deletion) {
      dedupeWhitespaceInChangeObjects(lastKeep, deletion, insertion, null, options.intlSegmenter);
    }
    return changes;
  }
};
var wordDiff = new WordDiff();
function dedupeWhitespaceInChangeObjects(startKeep, deletion, insertion, endKeep, segmenter) {
  if (deletion && insertion) {
    const [oldWsPrefix, oldWsSuffix] = leadingAndTrailingWs(deletion.value, segmenter);
    const [newWsPrefix, newWsSuffix] = leadingAndTrailingWs(insertion.value, segmenter);
    if (startKeep) {
      const commonWsPrefix = longestCommonPrefix(oldWsPrefix, newWsPrefix);
      startKeep.value = replaceSuffix(startKeep.value, newWsPrefix, commonWsPrefix);
      deletion.value = removePrefix(deletion.value, commonWsPrefix);
      insertion.value = removePrefix(insertion.value, commonWsPrefix);
    }
    if (endKeep) {
      const commonWsSuffix = longestCommonSuffix(oldWsSuffix, newWsSuffix);
      endKeep.value = replacePrefix(endKeep.value, newWsSuffix, commonWsSuffix);
      deletion.value = removeSuffix(deletion.value, commonWsSuffix);
      insertion.value = removeSuffix(insertion.value, commonWsSuffix);
    }
  } else if (insertion) {
    if (startKeep) {
      const ws = leadingWs(insertion.value, segmenter);
      insertion.value = insertion.value.substring(ws.length);
    }
    if (endKeep) {
      const ws = leadingWs(endKeep.value, segmenter);
      endKeep.value = endKeep.value.substring(ws.length);
    }
  } else if (startKeep && endKeep) {
    const newWsFull = leadingWs(endKeep.value, segmenter), [delWsStart, delWsEnd] = leadingAndTrailingWs(deletion.value, segmenter);
    const newWsStart = longestCommonPrefix(newWsFull, delWsStart);
    deletion.value = removePrefix(deletion.value, newWsStart);
    const newWsEnd = longestCommonSuffix(removePrefix(newWsFull, newWsStart), delWsEnd);
    deletion.value = removeSuffix(deletion.value, newWsEnd);
    endKeep.value = replacePrefix(endKeep.value, newWsFull, newWsEnd);
    startKeep.value = replaceSuffix(startKeep.value, newWsFull, newWsFull.slice(0, newWsFull.length - newWsEnd.length));
  } else if (endKeep) {
    const endKeepWsPrefix = leadingWs(endKeep.value, segmenter);
    const deletionWsSuffix = trailingWs(deletion.value, segmenter);
    const overlap = maximumOverlap(deletionWsSuffix, endKeepWsPrefix);
    deletion.value = removeSuffix(deletion.value, overlap);
  } else if (startKeep) {
    const startKeepWsSuffix = trailingWs(startKeep.value, segmenter);
    const deletionWsPrefix = leadingWs(deletion.value, segmenter);
    const overlap = maximumOverlap(startKeepWsSuffix, deletionWsPrefix);
    deletion.value = removePrefix(deletion.value, overlap);
  }
}
var WordsWithSpaceDiff = class extends Diff {
  tokenize(value) {
    const regex = new RegExp(`(\\r?\\n)|[${extendedWordChars}]+|[^\\S\\n\\r]+|[^${extendedWordChars}]`, "ug");
    return value.match(regex) || [];
  }
};
var wordsWithSpaceDiff = new WordsWithSpaceDiff();
function diffWordsWithSpace(oldStr, newStr, options) {
  return wordsWithSpaceDiff.diff(oldStr, newStr, options);
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/line.js
var LineDiff = class extends Diff {
  constructor() {
    super(...arguments);
    this.tokenize = tokenize;
  }
  equals(left, right, options) {
    if (options.ignoreWhitespace) {
      if (!options.newlineIsToken || !left.includes("\n")) {
        left = left.trim();
      }
      if (!options.newlineIsToken || !right.includes("\n")) {
        right = right.trim();
      }
    } else if (options.ignoreNewlineAtEof && !options.newlineIsToken) {
      if (left.endsWith("\n")) {
        left = left.slice(0, -1);
      }
      if (right.endsWith("\n")) {
        right = right.slice(0, -1);
      }
    }
    return super.equals(left, right, options);
  }
};
var lineDiff = new LineDiff();
function diffLines(oldStr, newStr, options) {
  return lineDiff.diff(oldStr, newStr, options);
}
function tokenize(value, options) {
  if (options.stripTrailingCr) {
    value = value.replace(/\r\n/g, "\n");
  }
  const retLines = [], linesAndNewlines = value.split(/(\n|\r\n)/);
  if (!linesAndNewlines[linesAndNewlines.length - 1]) {
    linesAndNewlines.pop();
  }
  for (let i2 = 0; i2 < linesAndNewlines.length; i2++) {
    const line = linesAndNewlines[i2];
    if (i2 % 2 && !options.newlineIsToken) {
      retLines[retLines.length - 1] += line;
    } else {
      retLines.push(line);
    }
  }
  return retLines;
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/sentence.js
function isSentenceEndPunct(char) {
  return char == "." || char == "!" || char == "?";
}
var SentenceDiff = class extends Diff {
  tokenize(value) {
    var _a9;
    const result = [];
    let tokenStartI = 0;
    for (let i2 = 0; i2 < value.length; i2++) {
      if (i2 == value.length - 1) {
        result.push(value.slice(tokenStartI));
        break;
      }
      if (isSentenceEndPunct(value[i2]) && value[i2 + 1].match(/\s/)) {
        result.push(value.slice(tokenStartI, i2 + 1));
        i2 = tokenStartI = i2 + 1;
        while ((_a9 = value[i2 + 1]) === null || _a9 === void 0 ? void 0 : _a9.match(/\s/)) {
          i2++;
        }
        result.push(value.slice(tokenStartI, i2 + 1));
        tokenStartI = i2 + 1;
      }
    }
    return result;
  }
};
var sentenceDiff = new SentenceDiff();

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/css.js
var CssDiff = class extends Diff {
  tokenize(value) {
    return value.split(/([{}:;,]|\s+)/);
  }
};
var cssDiff = new CssDiff();

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/json.js
var JsonDiff = class extends Diff {
  constructor() {
    super(...arguments);
    this.tokenize = tokenize;
  }
  get useLongestToken() {
    return true;
  }
  castInput(value, options) {
    const { undefinedReplacement, stringifyReplacer = (k3, v2) => typeof v2 === "undefined" ? undefinedReplacement : v2 } = options;
    return typeof value === "string" ? value : JSON.stringify(canonicalize(value, null, null, stringifyReplacer), null, "  ");
  }
  equals(left, right, options) {
    return super.equals(left.replace(/,([\r\n])/g, "$1"), right.replace(/,([\r\n])/g, "$1"), options);
  }
};
var jsonDiff = new JsonDiff();
function canonicalize(obj, stack, replacementStack, replacer, key2) {
  stack = stack || [];
  replacementStack = replacementStack || [];
  if (replacer) {
    obj = replacer(key2 === void 0 ? "" : key2, obj);
  }
  let i2;
  for (i2 = 0; i2 < stack.length; i2 += 1) {
    if (stack[i2] === obj) {
      return replacementStack[i2];
    }
  }
  let canonicalizedObj;
  if ("[object Array]" === Object.prototype.toString.call(obj)) {
    stack.push(obj);
    canonicalizedObj = new Array(obj.length);
    replacementStack.push(canonicalizedObj);
    for (i2 = 0; i2 < obj.length; i2 += 1) {
      canonicalizedObj[i2] = canonicalize(obj[i2], stack, replacementStack, replacer, String(i2));
    }
    stack.pop();
    replacementStack.pop();
    return canonicalizedObj;
  }
  if (obj && obj.toJSON) {
    obj = obj.toJSON();
  }
  if (typeof obj === "object" && obj !== null) {
    stack.push(obj);
    canonicalizedObj = {};
    replacementStack.push(canonicalizedObj);
    const sortedKeys = [];
    let key3;
    for (key3 in obj) {
      if (Object.prototype.hasOwnProperty.call(obj, key3)) {
        sortedKeys.push(key3);
      }
    }
    sortedKeys.sort();
    for (i2 = 0; i2 < sortedKeys.length; i2 += 1) {
      key3 = sortedKeys[i2];
      canonicalizedObj[key3] = canonicalize(obj[key3], stack, replacementStack, replacer, key3);
    }
    stack.pop();
    replacementStack.pop();
  } else {
    canonicalizedObj = obj;
  }
  return canonicalizedObj;
}

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/diff/array.js
var ArrayDiff = class extends Diff {
  tokenize(value) {
    return value.slice();
  }
  join(value) {
    return value;
  }
  removeEmpty(value) {
    return value;
  }
};
var arrayDiff = new ArrayDiff();

// ../../node_modules/.pnpm/diff@9.0.0/node_modules/diff/libesm/patch/create.js
function needsQuoting(s2) {
  for (let i2 = 0; i2 < s2.length; i2++) {
    if (s2[i2] < " " || s2[i2] > "~" || s2[i2] === '"' || s2[i2] === "\\") {
      return true;
    }
  }
  return false;
}
function quoteFileNameIfNeeded(s2) {
  if (!needsQuoting(s2)) {
    return s2;
  }
  let result = '"';
  const bytes = new TextEncoder().encode(s2);
  let i2 = 0;
  while (i2 < bytes.length) {
    const b3 = bytes[i2];
    if (b3 === 7) {
      result += "\\a";
    } else if (b3 === 8) {
      result += "\\b";
    } else if (b3 === 9) {
      result += "\\t";
    } else if (b3 === 10) {
      result += "\\n";
    } else if (b3 === 11) {
      result += "\\v";
    } else if (b3 === 12) {
      result += "\\f";
    } else if (b3 === 13) {
      result += "\\r";
    } else if (b3 === 34) {
      result += '\\"';
    } else if (b3 === 92) {
      result += "\\\\";
    } else if (b3 >= 32 && b3 <= 126) {
      result += String.fromCharCode(b3);
    } else {
      result += "\\" + b3.toString(8).padStart(3, "0");
    }
    i2++;
  }
  result += '"';
  return result;
}
var INCLUDE_HEADERS = {
  includeIndex: true,
  includeUnderline: true,
  includeFileHeaders: true
};
function structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  let optionsObj;
  if (!options) {
    optionsObj = {};
  } else if (typeof options === "function") {
    optionsObj = { callback: options };
  } else {
    optionsObj = options;
  }
  if (typeof optionsObj.context === "undefined") {
    optionsObj.context = 4;
  }
  const context = optionsObj.context;
  if (optionsObj.newlineIsToken) {
    throw new Error("newlineIsToken may not be used with patch-generation functions, only with diffing functions");
  }
  if (!optionsObj.callback) {
    return diffLinesResultToPatch(diffLines(oldStr, newStr, optionsObj));
  } else {
    const { callback } = optionsObj;
    diffLines(oldStr, newStr, Object.assign(Object.assign({}, optionsObj), { callback: (diff) => {
      const patch = diffLinesResultToPatch(diff);
      callback(patch);
    } }));
  }
  function diffLinesResultToPatch(diff) {
    if (!diff) {
      return;
    }
    diff.push({ value: "", lines: [] });
    function contextLines(lines) {
      return lines.map(function(entry) {
        return " " + entry;
      });
    }
    const hunks = [];
    let oldRangeStart = 0, newRangeStart = 0, curRange = [], oldLine = 1, newLine = 1;
    for (let i2 = 0; i2 < diff.length; i2++) {
      const current = diff[i2], lines = current.lines || splitLines(current.value);
      current.lines = lines;
      if (current.added || current.removed) {
        if (!oldRangeStart) {
          const prev = diff[i2 - 1];
          oldRangeStart = oldLine;
          newRangeStart = newLine;
          if (prev) {
            curRange = context > 0 ? contextLines(prev.lines.slice(-context)) : [];
            oldRangeStart -= curRange.length;
            newRangeStart -= curRange.length;
          }
        }
        for (const line of lines) {
          curRange.push((current.added ? "+" : "-") + line);
        }
        if (current.added) {
          newLine += lines.length;
        } else {
          oldLine += lines.length;
        }
      } else {
        if (oldRangeStart) {
          if (lines.length <= context * 2 && i2 < diff.length - 2) {
            for (const line of contextLines(lines)) {
              curRange.push(line);
            }
          } else {
            const contextSize = Math.min(lines.length, context);
            for (const line of contextLines(lines.slice(0, contextSize))) {
              curRange.push(line);
            }
            const hunk = {
              oldStart: oldRangeStart,
              oldLines: oldLine - oldRangeStart + contextSize,
              newStart: newRangeStart,
              newLines: newLine - newRangeStart + contextSize,
              lines: curRange
            };
            hunks.push(hunk);
            oldRangeStart = 0;
            newRangeStart = 0;
            curRange = [];
          }
        }
        oldLine += lines.length;
        newLine += lines.length;
      }
    }
    for (const hunk of hunks) {
      for (let i2 = 0; i2 < hunk.lines.length; i2++) {
        if (hunk.lines[i2].endsWith("\n")) {
          hunk.lines[i2] = hunk.lines[i2].slice(0, -1);
        } else {
          hunk.lines.splice(i2 + 1, 0, "\\ No newline at end of file");
          i2++;
        }
      }
    }
    return {
      oldFileName,
      newFileName,
      oldHeader,
      newHeader,
      hunks
    };
  }
}
function formatPatch(patch, headerOptions) {
  var _a9, _b, _c, _d, _e, _f;
  if (!headerOptions) {
    headerOptions = INCLUDE_HEADERS;
  }
  if (Array.isArray(patch)) {
    if (patch.length > 1 && !headerOptions.includeFileHeaders && !patch.every((p2) => p2.isGit)) {
      throw new Error("Cannot omit file headers on a multi-file patch. (The result would be unparseable; how would a tool trying to apply the patch know which changes are to which file?)");
    }
    return patch.map((p2) => formatPatch(p2, headerOptions)).join("\n");
  }
  const ret = [];
  if (patch.isGit) {
    headerOptions = INCLUDE_HEADERS;
    if (!patch.oldFileName) {
      throw new Error("oldFileName must be specified for Git patches");
    }
    if (!patch.newFileName) {
      throw new Error("newFileName must be specified for Git patches");
    }
    let gitOldName = patch.oldFileName;
    let gitNewName = patch.newFileName;
    if (patch.isCreate && gitOldName === "/dev/null") {
      gitOldName = gitNewName.replace(/^b\//, "a/");
    } else if (patch.isDelete && gitNewName === "/dev/null") {
      gitNewName = gitOldName.replace(/^a\//, "b/");
    }
    ret.push("diff --git " + quoteFileNameIfNeeded(gitOldName) + " " + quoteFileNameIfNeeded(gitNewName));
    if (patch.isDelete) {
      ret.push("deleted file mode " + ((_a9 = patch.oldMode) !== null && _a9 !== void 0 ? _a9 : "100644"));
    }
    if (patch.isCreate) {
      ret.push("new file mode " + ((_b = patch.newMode) !== null && _b !== void 0 ? _b : "100644"));
    }
    if (patch.oldMode && patch.newMode && !patch.isDelete && !patch.isCreate) {
      ret.push("old mode " + patch.oldMode);
      ret.push("new mode " + patch.newMode);
    }
    if (patch.isRename) {
      ret.push("rename from " + quoteFileNameIfNeeded(((_c = patch.oldFileName) !== null && _c !== void 0 ? _c : "").replace(/^a\//, "")));
      ret.push("rename to " + quoteFileNameIfNeeded(((_d = patch.newFileName) !== null && _d !== void 0 ? _d : "").replace(/^b\//, "")));
    }
    if (patch.isCopy) {
      ret.push("copy from " + quoteFileNameIfNeeded(((_e = patch.oldFileName) !== null && _e !== void 0 ? _e : "").replace(/^a\//, "")));
      ret.push("copy to " + quoteFileNameIfNeeded(((_f = patch.newFileName) !== null && _f !== void 0 ? _f : "").replace(/^b\//, "")));
    }
  } else {
    if (headerOptions.includeIndex && patch.oldFileName == patch.newFileName && patch.oldFileName !== void 0) {
      ret.push("Index: " + patch.oldFileName);
    }
    if (headerOptions.includeUnderline) {
      ret.push("===================================================================");
    }
  }
  const hasHunks = patch.hunks.length > 0;
  if (headerOptions.includeFileHeaders && patch.oldFileName !== void 0 && patch.newFileName !== void 0 && (!patch.isGit || hasHunks)) {
    ret.push("--- " + quoteFileNameIfNeeded(patch.oldFileName) + (patch.oldHeader ? "	" + patch.oldHeader : ""));
    ret.push("+++ " + quoteFileNameIfNeeded(patch.newFileName) + (patch.newHeader ? "	" + patch.newHeader : ""));
  }
  for (let i2 = 0; i2 < patch.hunks.length; i2++) {
    const hunk = patch.hunks[i2];
    const oldStart = hunk.oldLines === 0 ? hunk.oldStart - 1 : hunk.oldStart;
    const newStart = hunk.newLines === 0 ? hunk.newStart - 1 : hunk.newStart;
    ret.push("@@ -" + oldStart + "," + hunk.oldLines + " +" + newStart + "," + hunk.newLines + " @@");
    for (const line of hunk.lines) {
      ret.push(line);
    }
  }
  return ret.join("\n") + "\n";
}
function createTwoFilesPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  if (typeof options === "function") {
    options = { callback: options };
  }
  if (!(options === null || options === void 0 ? void 0 : options.callback)) {
    const patchObj = structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options);
    if (!patchObj) {
      return;
    }
    return formatPatch(patchObj, options === null || options === void 0 ? void 0 : options.headerOptions);
  } else {
    const { callback } = options;
    structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, Object.assign(Object.assign({}, options), { callback: (patchObj) => {
      if (!patchObj) {
        callback(void 0);
      } else {
        callback(formatPatch(patchObj, options.headerOptions));
      }
    } }));
  }
}
function splitLines(text2) {
  const hasTrailingNl = text2.endsWith("\n");
  const result = text2.split("\n").map((line) => line + "\n");
  if (hasTrailingNl) {
    result.pop();
  } else {
    result.push(result.pop().slice(0, -1));
  }
  return result;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/parseDiffFromFile.js
var MISSING_FILE_NAME = "/dev/null";
function parseDiffFromFile(oldFile, newFile, options, throwOnError = false) {
  if (oldFile === null && newFile === null) throw new Error("parseDiffFromFile: You must pass oldFile, newFile, or both");
  const resolvedOldFile = oldFile ?? createMissingFile();
  const resolvedNewFile = newFile ?? createMissingFile();
  const fileData = processFile(createTwoFilesPatch(resolvedOldFile.name, resolvedNewFile.name, resolvedOldFile.contents, resolvedNewFile.contents, resolvedOldFile.header, resolvedNewFile.header, options), {
    cacheKey: (() => {
      const oldCacheKey = (oldFile == null ? void 0 : oldFile.cacheKey) ?? (oldFile == null ? void 0 : oldFile.name);
      const newCacheKey = (newFile == null ? void 0 : newFile.cacheKey) ?? (newFile == null ? void 0 : newFile.name);
      if (oldCacheKey != null && newCacheKey != null) return oldCacheKey + ":" + newCacheKey;
      return oldCacheKey ?? newCacheKey;
    })(),
    oldFile: resolvedOldFile,
    newFile: resolvedNewFile,
    throwOnError
  });
  if (fileData == null) throw new Error("parseDiffFrom: FileInvalid diff -- probably need to fix something -- if the files are the same maybe?");
  if (oldFile === null) {
    fileData.type = "new";
    fileData.prevName = void 0;
  } else if (newFile === null) {
    fileData.type = "deleted";
    fileData.prevName = void 0;
  }
  const language = (newFile == null ? void 0 : newFile.lang) ?? (newFile === null ? oldFile == null ? void 0 : oldFile.lang : void 0);
  if (language != null) fileData.lang = language;
  return fileData;
}
function createMissingFile() {
  return {
    name: MISSING_FILE_NAME,
    contents: ""
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/prefersReducedMotion.js
function prefersReducedMotion() {
  if (typeof window === "undefined" || typeof window.matchMedia !== "function") return false;
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areFilesEqual.js
function areFilesEqual(fileA, fileB) {
  return (fileA == null ? void 0 : fileA.cacheKey) === (fileB == null ? void 0 : fileB.cacheKey) && (fileA == null ? void 0 : fileA.contents) === (fileB == null ? void 0 : fileB.contents) && (fileA == null ? void 0 : fileA.name) === (fileB == null ? void 0 : fileB.name) && (fileA == null ? void 0 : fileA.lang) === (fileB == null ? void 0 : fileB.lang);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/hast_utils.js
function createTextNodeElement(value) {
  return {
    type: "text",
    value
  };
}
function createHastElement({ tagName, children = [], properties = {} }) {
  return {
    type: "element",
    tagName,
    properties,
    children
  };
}
function createIconElement({ name, width = 16, height = 16, properties }) {
  return createHastElement({
    tagName: "svg",
    properties: {
      width,
      height,
      viewBox: "0 0 16 16",
      ...properties
    },
    children: [createHastElement({
      tagName: "use",
      properties: { href: `#${name.replace(/^#/, "")}` }
    })]
  });
}
function findCodeElement(nodes) {
  let firstChild = nodes.children[0];
  while (firstChild != null) {
    if (firstChild.type === "element" && firstChild.tagName === "code") return firstChild;
    if ("children" in firstChild) firstChild = firstChild.children[0];
    else firstChild = null;
  }
}
function createGutterWrapper(children) {
  return createHastElement({
    tagName: "div",
    properties: { "data-gutter": "" },
    children
  });
}
function createGutterItem(lineType, lineNumber, lineIndex, properties = {}) {
  return createHastElement({
    tagName: "div",
    properties: {
      "data-line-type": lineType,
      "data-column-number": lineNumber,
      "data-line-index": lineIndex,
      ...properties
    },
    children: lineNumber != null ? [createHastElement({
      tagName: "span",
      properties: { "data-line-number-content": "" },
      children: [createTextNodeElement(`${lineNumber}`)]
    })] : void 0
  });
}
function createGutterGap(type, bufferType, size) {
  return createHastElement({
    tagName: "div",
    properties: {
      "data-gutter-buffer": bufferType,
      "data-buffer-size": size,
      "data-line-type": bufferType === "annotation" ? void 0 : type,
      style: bufferType === "annotation" ? `grid-row: span ${size};` : `grid-row: span ${size};min-height:calc(${size} * 1lh);`
    }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createGutterUtilityElement.js
function createGutterUtilityElement() {
  return createHastElement({
    tagName: "button",
    properties: {
      "data-utility-button": "",
      type: "button"
    },
    children: [createIconElement({
      name: "diffs-icon-plus",
      properties: { "data-icon": "" }
    })]
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areSelectionPointsEqual.js
function areSelectionPointsEqual(a2, b3) {
  return a2.lineNumber === b3.lineNumber && a2.side === b3.side;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isGutterUtilityPath.js
function isGutterUtilityPath(path) {
  for (const element2 of path) {
    if (!(element2 instanceof HTMLElement)) continue;
    if (element2.hasAttribute("data-utility-button") || element2.hasAttribute("data-gutter-utility-slot") || element2.getAttribute("slot") === "gutter-utility-slot" || element2.getAttribute("name") === "gutter-utility-slot") return true;
  }
  return false;
}

// ../../node_modules/.pnpm/html-void-elements@3.0.0/node_modules/html-void-elements/index.js
var htmlVoidElements = [
  "area",
  "base",
  "basefont",
  "bgsound",
  "br",
  "col",
  "command",
  "embed",
  "frame",
  "hr",
  "image",
  "img",
  "input",
  "keygen",
  "link",
  "meta",
  "param",
  "source",
  "track",
  "wbr"
];

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/schema.js
var Schema = class {
  /**
   * @param {SchemaType['property']} property
   *   Property.
   * @param {SchemaType['normal']} normal
   *   Normal.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Schema.
   */
  constructor(property, normal, space) {
    this.normal = normal;
    this.property = property;
    if (space) {
      this.space = space;
    }
  }
};
Schema.prototype.normal = {};
Schema.prototype.property = {};
Schema.prototype.space = void 0;

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/merge.js
function merge(definitions, space) {
  const property = {};
  const normal = {};
  for (const definition of definitions) {
    Object.assign(property, definition.property);
    Object.assign(normal, definition.normal);
  }
  return new Schema(property, normal, space);
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/normalize.js
function normalize(value) {
  return value.toLowerCase();
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/info.js
var Info = class {
  /**
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @returns
   *   Info.
   */
  constructor(property, attribute) {
    this.attribute = attribute;
    this.property = property;
  }
};
Info.prototype.attribute = "";
Info.prototype.booleanish = false;
Info.prototype.boolean = false;
Info.prototype.commaOrSpaceSeparated = false;
Info.prototype.commaSeparated = false;
Info.prototype.defined = false;
Info.prototype.mustUseProperty = false;
Info.prototype.number = false;
Info.prototype.overloadedBoolean = false;
Info.prototype.property = "";
Info.prototype.spaceSeparated = false;
Info.prototype.space = void 0;

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/types.js
var types_exports = {};
__export(types_exports, {
  boolean: () => boolean,
  booleanish: () => booleanish,
  commaOrSpaceSeparated: () => commaOrSpaceSeparated,
  commaSeparated: () => commaSeparated,
  number: () => number,
  overloadedBoolean: () => overloadedBoolean,
  spaceSeparated: () => spaceSeparated
});
var powers = 0;
var boolean = increment();
var booleanish = increment();
var overloadedBoolean = increment();
var number = increment();
var spaceSeparated = increment();
var commaSeparated = increment();
var commaOrSpaceSeparated = increment();
function increment() {
  return 2 ** ++powers;
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/defined-info.js
var checks = (
  /** @type {ReadonlyArray<keyof typeof types>} */
  Object.keys(types_exports)
);
var DefinedInfo = class extends Info {
  /**
   * @constructor
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @param {number | null | undefined} [mask]
   *   Mask.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Info.
   */
  constructor(property, attribute, mask, space) {
    let index = -1;
    super(property, attribute);
    mark(this, "space", space);
    if (typeof mask === "number") {
      while (++index < checks.length) {
        const check = checks[index];
        mark(this, checks[index], (mask & types_exports[check]) === types_exports[check]);
      }
    }
  }
};
DefinedInfo.prototype.defined = true;
function mark(values, key2, value) {
  if (value) {
    values[key2] = value;
  }
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/create.js
function create(definition) {
  const properties = {};
  const normals = {};
  for (const [property, value] of Object.entries(definition.properties)) {
    const info = new DefinedInfo(
      property,
      definition.transform(definition.attributes || {}, property),
      value,
      definition.space
    );
    if (definition.mustUseProperty && definition.mustUseProperty.includes(property)) {
      info.mustUseProperty = true;
    }
    properties[property] = info;
    normals[normalize(property)] = property;
    normals[normalize(info.attribute)] = property;
  }
  return new Schema(properties, normals, definition.space);
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/aria.js
var aria = create({
  properties: {
    ariaActiveDescendant: null,
    ariaAtomic: booleanish,
    ariaAutoComplete: null,
    ariaBusy: booleanish,
    ariaChecked: booleanish,
    ariaColCount: number,
    ariaColIndex: number,
    ariaColSpan: number,
    ariaControls: spaceSeparated,
    ariaCurrent: null,
    ariaDescribedBy: spaceSeparated,
    ariaDetails: null,
    ariaDisabled: booleanish,
    ariaDropEffect: spaceSeparated,
    ariaErrorMessage: null,
    ariaExpanded: booleanish,
    ariaFlowTo: spaceSeparated,
    ariaGrabbed: booleanish,
    ariaHasPopup: null,
    ariaHidden: booleanish,
    ariaInvalid: null,
    ariaKeyShortcuts: null,
    ariaLabel: null,
    ariaLabelledBy: spaceSeparated,
    ariaLevel: number,
    ariaLive: null,
    ariaModal: booleanish,
    ariaMultiLine: booleanish,
    ariaMultiSelectable: booleanish,
    ariaOrientation: null,
    ariaOwns: spaceSeparated,
    ariaPlaceholder: null,
    ariaPosInSet: number,
    ariaPressed: booleanish,
    ariaReadOnly: booleanish,
    ariaRelevant: null,
    ariaRequired: booleanish,
    ariaRoleDescription: spaceSeparated,
    ariaRowCount: number,
    ariaRowIndex: number,
    ariaRowSpan: number,
    ariaSelected: booleanish,
    ariaSetSize: number,
    ariaSort: null,
    ariaValueMax: number,
    ariaValueMin: number,
    ariaValueNow: number,
    ariaValueText: null,
    role: null
  },
  transform(_3, property) {
    return property === "role" ? property : "aria-" + property.slice(4).toLowerCase();
  }
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/case-sensitive-transform.js
function caseSensitiveTransform(attributes, attribute) {
  return attribute in attributes ? attributes[attribute] : attribute;
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/util/case-insensitive-transform.js
function caseInsensitiveTransform(attributes, property) {
  return caseSensitiveTransform(attributes, property.toLowerCase());
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/html.js
var html = create({
  attributes: {
    acceptcharset: "accept-charset",
    classname: "class",
    htmlfor: "for",
    httpequiv: "http-equiv"
  },
  mustUseProperty: ["checked", "multiple", "muted", "selected"],
  properties: {
    // Standard Properties.
    abbr: null,
    accept: commaSeparated,
    acceptCharset: spaceSeparated,
    accessKey: spaceSeparated,
    action: null,
    allow: null,
    allowFullScreen: boolean,
    allowPaymentRequest: boolean,
    allowUserMedia: boolean,
    alt: null,
    as: null,
    async: boolean,
    autoCapitalize: null,
    autoComplete: spaceSeparated,
    autoFocus: boolean,
    autoPlay: boolean,
    blocking: spaceSeparated,
    capture: null,
    charSet: null,
    checked: boolean,
    cite: null,
    className: spaceSeparated,
    cols: number,
    colSpan: null,
    content: null,
    contentEditable: booleanish,
    controls: boolean,
    controlsList: spaceSeparated,
    coords: number | commaSeparated,
    crossOrigin: null,
    data: null,
    dateTime: null,
    decoding: null,
    default: boolean,
    defer: boolean,
    dir: null,
    dirName: null,
    disabled: boolean,
    download: overloadedBoolean,
    draggable: booleanish,
    encType: null,
    enterKeyHint: null,
    fetchPriority: null,
    form: null,
    formAction: null,
    formEncType: null,
    formMethod: null,
    formNoValidate: boolean,
    formTarget: null,
    headers: spaceSeparated,
    height: number,
    hidden: overloadedBoolean,
    high: number,
    href: null,
    hrefLang: null,
    htmlFor: spaceSeparated,
    httpEquiv: spaceSeparated,
    id: null,
    imageSizes: null,
    imageSrcSet: null,
    inert: boolean,
    inputMode: null,
    integrity: null,
    is: null,
    isMap: boolean,
    itemId: null,
    itemProp: spaceSeparated,
    itemRef: spaceSeparated,
    itemScope: boolean,
    itemType: spaceSeparated,
    kind: null,
    label: null,
    lang: null,
    language: null,
    list: null,
    loading: null,
    loop: boolean,
    low: number,
    manifest: null,
    max: null,
    maxLength: number,
    media: null,
    method: null,
    min: null,
    minLength: number,
    multiple: boolean,
    muted: boolean,
    name: null,
    nonce: null,
    noModule: boolean,
    noValidate: boolean,
    onAbort: null,
    onAfterPrint: null,
    onAuxClick: null,
    onBeforeMatch: null,
    onBeforePrint: null,
    onBeforeToggle: null,
    onBeforeUnload: null,
    onBlur: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onContextLost: null,
    onContextMenu: null,
    onContextRestored: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFormData: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLanguageChange: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadEnd: null,
    onLoadStart: null,
    onMessage: null,
    onMessageError: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRejectionHandled: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onScrollEnd: null,
    onSecurityPolicyViolation: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onSlotChange: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnhandledRejection: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onWheel: null,
    open: boolean,
    optimum: number,
    pattern: null,
    ping: spaceSeparated,
    placeholder: null,
    playsInline: boolean,
    popover: null,
    popoverTarget: null,
    popoverTargetAction: null,
    poster: null,
    preload: null,
    readOnly: boolean,
    referrerPolicy: null,
    rel: spaceSeparated,
    required: boolean,
    reversed: boolean,
    rows: number,
    rowSpan: number,
    sandbox: spaceSeparated,
    scope: null,
    scoped: boolean,
    seamless: boolean,
    selected: boolean,
    shadowRootClonable: boolean,
    shadowRootDelegatesFocus: boolean,
    shadowRootMode: null,
    shape: null,
    size: number,
    sizes: null,
    slot: null,
    span: number,
    spellCheck: booleanish,
    src: null,
    srcDoc: null,
    srcLang: null,
    srcSet: null,
    start: number,
    step: null,
    style: null,
    tabIndex: number,
    target: null,
    title: null,
    translate: null,
    type: null,
    typeMustMatch: boolean,
    useMap: null,
    value: booleanish,
    width: number,
    wrap: null,
    writingSuggestions: null,
    // Legacy.
    // See: https://html.spec.whatwg.org/#other-elements,-attributes-and-apis
    align: null,
    // Several. Use CSS `text-align` instead,
    aLink: null,
    // `<body>`. Use CSS `a:active {color}` instead
    archive: spaceSeparated,
    // `<object>`. List of URIs to archives
    axis: null,
    // `<td>` and `<th>`. Use `scope` on `<th>`
    background: null,
    // `<body>`. Use CSS `background-image` instead
    bgColor: null,
    // `<body>` and table elements. Use CSS `background-color` instead
    border: number,
    // `<table>`. Use CSS `border-width` instead,
    borderColor: null,
    // `<table>`. Use CSS `border-color` instead,
    bottomMargin: number,
    // `<body>`
    cellPadding: null,
    // `<table>`
    cellSpacing: null,
    // `<table>`
    char: null,
    // Several table elements. When `align=char`, sets the character to align on
    charOff: null,
    // Several table elements. When `char`, offsets the alignment
    classId: null,
    // `<object>`
    clear: null,
    // `<br>`. Use CSS `clear` instead
    code: null,
    // `<object>`
    codeBase: null,
    // `<object>`
    codeType: null,
    // `<object>`
    color: null,
    // `<font>` and `<hr>`. Use CSS instead
    compact: boolean,
    // Lists. Use CSS to reduce space between items instead
    declare: boolean,
    // `<object>`
    event: null,
    // `<script>`
    face: null,
    // `<font>`. Use CSS instead
    frame: null,
    // `<table>`
    frameBorder: null,
    // `<iframe>`. Use CSS `border` instead
    hSpace: number,
    // `<img>` and `<object>`
    leftMargin: number,
    // `<body>`
    link: null,
    // `<body>`. Use CSS `a:link {color: *}` instead
    longDesc: null,
    // `<frame>`, `<iframe>`, and `<img>`. Use an `<a>`
    lowSrc: null,
    // `<img>`. Use a `<picture>`
    marginHeight: number,
    // `<body>`
    marginWidth: number,
    // `<body>`
    noResize: boolean,
    // `<frame>`
    noHref: boolean,
    // `<area>`. Use no href instead of an explicit `nohref`
    noShade: boolean,
    // `<hr>`. Use background-color and height instead of borders
    noWrap: boolean,
    // `<td>` and `<th>`
    object: null,
    // `<applet>`
    profile: null,
    // `<head>`
    prompt: null,
    // `<isindex>`
    rev: null,
    // `<link>`
    rightMargin: number,
    // `<body>`
    rules: null,
    // `<table>`
    scheme: null,
    // `<meta>`
    scrolling: booleanish,
    // `<frame>`. Use overflow in the child context
    standby: null,
    // `<object>`
    summary: null,
    // `<table>`
    text: null,
    // `<body>`. Use CSS `color` instead
    topMargin: number,
    // `<body>`
    valueType: null,
    // `<param>`
    version: null,
    // `<html>`. Use a doctype.
    vAlign: null,
    // Several. Use CSS `vertical-align` instead
    vLink: null,
    // `<body>`. Use CSS `a:visited {color}` instead
    vSpace: number,
    // `<img>` and `<object>`
    // Non-standard Properties.
    allowTransparency: null,
    autoCorrect: null,
    autoSave: null,
    disablePictureInPicture: boolean,
    disableRemotePlayback: boolean,
    prefix: null,
    property: null,
    results: number,
    security: null,
    unselectable: null
  },
  space: "html",
  transform: caseInsensitiveTransform
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/svg.js
var svg = create({
  attributes: {
    accentHeight: "accent-height",
    alignmentBaseline: "alignment-baseline",
    arabicForm: "arabic-form",
    baselineShift: "baseline-shift",
    capHeight: "cap-height",
    className: "class",
    clipPath: "clip-path",
    clipRule: "clip-rule",
    colorInterpolation: "color-interpolation",
    colorInterpolationFilters: "color-interpolation-filters",
    colorProfile: "color-profile",
    colorRendering: "color-rendering",
    crossOrigin: "crossorigin",
    dataType: "datatype",
    dominantBaseline: "dominant-baseline",
    enableBackground: "enable-background",
    fillOpacity: "fill-opacity",
    fillRule: "fill-rule",
    floodColor: "flood-color",
    floodOpacity: "flood-opacity",
    fontFamily: "font-family",
    fontSize: "font-size",
    fontSizeAdjust: "font-size-adjust",
    fontStretch: "font-stretch",
    fontStyle: "font-style",
    fontVariant: "font-variant",
    fontWeight: "font-weight",
    glyphName: "glyph-name",
    glyphOrientationHorizontal: "glyph-orientation-horizontal",
    glyphOrientationVertical: "glyph-orientation-vertical",
    hrefLang: "hreflang",
    horizAdvX: "horiz-adv-x",
    horizOriginX: "horiz-origin-x",
    horizOriginY: "horiz-origin-y",
    imageRendering: "image-rendering",
    letterSpacing: "letter-spacing",
    lightingColor: "lighting-color",
    markerEnd: "marker-end",
    markerMid: "marker-mid",
    markerStart: "marker-start",
    navDown: "nav-down",
    navDownLeft: "nav-down-left",
    navDownRight: "nav-down-right",
    navLeft: "nav-left",
    navNext: "nav-next",
    navPrev: "nav-prev",
    navRight: "nav-right",
    navUp: "nav-up",
    navUpLeft: "nav-up-left",
    navUpRight: "nav-up-right",
    onAbort: "onabort",
    onActivate: "onactivate",
    onAfterPrint: "onafterprint",
    onBeforePrint: "onbeforeprint",
    onBegin: "onbegin",
    onCancel: "oncancel",
    onCanPlay: "oncanplay",
    onCanPlayThrough: "oncanplaythrough",
    onChange: "onchange",
    onClick: "onclick",
    onClose: "onclose",
    onCopy: "oncopy",
    onCueChange: "oncuechange",
    onCut: "oncut",
    onDblClick: "ondblclick",
    onDrag: "ondrag",
    onDragEnd: "ondragend",
    onDragEnter: "ondragenter",
    onDragExit: "ondragexit",
    onDragLeave: "ondragleave",
    onDragOver: "ondragover",
    onDragStart: "ondragstart",
    onDrop: "ondrop",
    onDurationChange: "ondurationchange",
    onEmptied: "onemptied",
    onEnd: "onend",
    onEnded: "onended",
    onError: "onerror",
    onFocus: "onfocus",
    onFocusIn: "onfocusin",
    onFocusOut: "onfocusout",
    onHashChange: "onhashchange",
    onInput: "oninput",
    onInvalid: "oninvalid",
    onKeyDown: "onkeydown",
    onKeyPress: "onkeypress",
    onKeyUp: "onkeyup",
    onLoad: "onload",
    onLoadedData: "onloadeddata",
    onLoadedMetadata: "onloadedmetadata",
    onLoadStart: "onloadstart",
    onMessage: "onmessage",
    onMouseDown: "onmousedown",
    onMouseEnter: "onmouseenter",
    onMouseLeave: "onmouseleave",
    onMouseMove: "onmousemove",
    onMouseOut: "onmouseout",
    onMouseOver: "onmouseover",
    onMouseUp: "onmouseup",
    onMouseWheel: "onmousewheel",
    onOffline: "onoffline",
    onOnline: "ononline",
    onPageHide: "onpagehide",
    onPageShow: "onpageshow",
    onPaste: "onpaste",
    onPause: "onpause",
    onPlay: "onplay",
    onPlaying: "onplaying",
    onPopState: "onpopstate",
    onProgress: "onprogress",
    onRateChange: "onratechange",
    onRepeat: "onrepeat",
    onReset: "onreset",
    onResize: "onresize",
    onScroll: "onscroll",
    onSeeked: "onseeked",
    onSeeking: "onseeking",
    onSelect: "onselect",
    onShow: "onshow",
    onStalled: "onstalled",
    onStorage: "onstorage",
    onSubmit: "onsubmit",
    onSuspend: "onsuspend",
    onTimeUpdate: "ontimeupdate",
    onToggle: "ontoggle",
    onUnload: "onunload",
    onVolumeChange: "onvolumechange",
    onWaiting: "onwaiting",
    onZoom: "onzoom",
    overlinePosition: "overline-position",
    overlineThickness: "overline-thickness",
    paintOrder: "paint-order",
    panose1: "panose-1",
    pointerEvents: "pointer-events",
    referrerPolicy: "referrerpolicy",
    renderingIntent: "rendering-intent",
    shapeRendering: "shape-rendering",
    stopColor: "stop-color",
    stopOpacity: "stop-opacity",
    strikethroughPosition: "strikethrough-position",
    strikethroughThickness: "strikethrough-thickness",
    strokeDashArray: "stroke-dasharray",
    strokeDashOffset: "stroke-dashoffset",
    strokeLineCap: "stroke-linecap",
    strokeLineJoin: "stroke-linejoin",
    strokeMiterLimit: "stroke-miterlimit",
    strokeOpacity: "stroke-opacity",
    strokeWidth: "stroke-width",
    tabIndex: "tabindex",
    textAnchor: "text-anchor",
    textDecoration: "text-decoration",
    textRendering: "text-rendering",
    transformOrigin: "transform-origin",
    typeOf: "typeof",
    underlinePosition: "underline-position",
    underlineThickness: "underline-thickness",
    unicodeBidi: "unicode-bidi",
    unicodeRange: "unicode-range",
    unitsPerEm: "units-per-em",
    vAlphabetic: "v-alphabetic",
    vHanging: "v-hanging",
    vIdeographic: "v-ideographic",
    vMathematical: "v-mathematical",
    vectorEffect: "vector-effect",
    vertAdvY: "vert-adv-y",
    vertOriginX: "vert-origin-x",
    vertOriginY: "vert-origin-y",
    wordSpacing: "word-spacing",
    writingMode: "writing-mode",
    xHeight: "x-height",
    // These were camelcased in Tiny. Now lowercased in SVG 2
    playbackOrder: "playbackorder",
    timelineBegin: "timelinebegin"
  },
  properties: {
    about: commaOrSpaceSeparated,
    accentHeight: number,
    accumulate: null,
    additive: null,
    alignmentBaseline: null,
    alphabetic: number,
    amplitude: number,
    arabicForm: null,
    ascent: number,
    attributeName: null,
    attributeType: null,
    azimuth: number,
    bandwidth: null,
    baselineShift: null,
    baseFrequency: null,
    baseProfile: null,
    bbox: null,
    begin: null,
    bias: number,
    by: null,
    calcMode: null,
    capHeight: number,
    className: spaceSeparated,
    clip: null,
    clipPath: null,
    clipPathUnits: null,
    clipRule: null,
    color: null,
    colorInterpolation: null,
    colorInterpolationFilters: null,
    colorProfile: null,
    colorRendering: null,
    content: null,
    contentScriptType: null,
    contentStyleType: null,
    crossOrigin: null,
    cursor: null,
    cx: null,
    cy: null,
    d: null,
    dataType: null,
    defaultAction: null,
    descent: number,
    diffuseConstant: number,
    direction: null,
    display: null,
    dur: null,
    divisor: number,
    dominantBaseline: null,
    download: boolean,
    dx: null,
    dy: null,
    edgeMode: null,
    editable: null,
    elevation: number,
    enableBackground: null,
    end: null,
    event: null,
    exponent: number,
    externalResourcesRequired: null,
    fill: null,
    fillOpacity: number,
    fillRule: null,
    filter: null,
    filterRes: null,
    filterUnits: null,
    floodColor: null,
    floodOpacity: null,
    focusable: null,
    focusHighlight: null,
    fontFamily: null,
    fontSize: null,
    fontSizeAdjust: null,
    fontStretch: null,
    fontStyle: null,
    fontVariant: null,
    fontWeight: null,
    format: null,
    fr: null,
    from: null,
    fx: null,
    fy: null,
    g1: commaSeparated,
    g2: commaSeparated,
    glyphName: commaSeparated,
    glyphOrientationHorizontal: null,
    glyphOrientationVertical: null,
    glyphRef: null,
    gradientTransform: null,
    gradientUnits: null,
    handler: null,
    hanging: number,
    hatchContentUnits: null,
    hatchUnits: null,
    height: null,
    href: null,
    hrefLang: null,
    horizAdvX: number,
    horizOriginX: number,
    horizOriginY: number,
    id: null,
    ideographic: number,
    imageRendering: null,
    initialVisibility: null,
    in: null,
    in2: null,
    intercept: number,
    k: number,
    k1: number,
    k2: number,
    k3: number,
    k4: number,
    kernelMatrix: commaOrSpaceSeparated,
    kernelUnitLength: null,
    keyPoints: null,
    // SEMI_COLON_SEPARATED
    keySplines: null,
    // SEMI_COLON_SEPARATED
    keyTimes: null,
    // SEMI_COLON_SEPARATED
    kerning: null,
    lang: null,
    lengthAdjust: null,
    letterSpacing: null,
    lightingColor: null,
    limitingConeAngle: number,
    local: null,
    markerEnd: null,
    markerMid: null,
    markerStart: null,
    markerHeight: null,
    markerUnits: null,
    markerWidth: null,
    mask: null,
    maskContentUnits: null,
    maskUnits: null,
    mathematical: null,
    max: null,
    media: null,
    mediaCharacterEncoding: null,
    mediaContentEncodings: null,
    mediaSize: number,
    mediaTime: null,
    method: null,
    min: null,
    mode: null,
    name: null,
    navDown: null,
    navDownLeft: null,
    navDownRight: null,
    navLeft: null,
    navNext: null,
    navPrev: null,
    navRight: null,
    navUp: null,
    navUpLeft: null,
    navUpRight: null,
    numOctaves: null,
    observer: null,
    offset: null,
    onAbort: null,
    onActivate: null,
    onAfterPrint: null,
    onBeforePrint: null,
    onBegin: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnd: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFocusIn: null,
    onFocusOut: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadStart: null,
    onMessage: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onMouseWheel: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRepeat: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onShow: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onZoom: null,
    opacity: null,
    operator: null,
    order: null,
    orient: null,
    orientation: null,
    origin: null,
    overflow: null,
    overlay: null,
    overlinePosition: number,
    overlineThickness: number,
    paintOrder: null,
    panose1: null,
    path: null,
    pathLength: number,
    patternContentUnits: null,
    patternTransform: null,
    patternUnits: null,
    phase: null,
    ping: spaceSeparated,
    pitch: null,
    playbackOrder: null,
    pointerEvents: null,
    points: null,
    pointsAtX: number,
    pointsAtY: number,
    pointsAtZ: number,
    preserveAlpha: null,
    preserveAspectRatio: null,
    primitiveUnits: null,
    propagate: null,
    property: commaOrSpaceSeparated,
    r: null,
    radius: null,
    referrerPolicy: null,
    refX: null,
    refY: null,
    rel: commaOrSpaceSeparated,
    rev: commaOrSpaceSeparated,
    renderingIntent: null,
    repeatCount: null,
    repeatDur: null,
    requiredExtensions: commaOrSpaceSeparated,
    requiredFeatures: commaOrSpaceSeparated,
    requiredFonts: commaOrSpaceSeparated,
    requiredFormats: commaOrSpaceSeparated,
    resource: null,
    restart: null,
    result: null,
    rotate: null,
    rx: null,
    ry: null,
    scale: null,
    seed: null,
    shapeRendering: null,
    side: null,
    slope: null,
    snapshotTime: null,
    specularConstant: number,
    specularExponent: number,
    spreadMethod: null,
    spacing: null,
    startOffset: null,
    stdDeviation: null,
    stemh: null,
    stemv: null,
    stitchTiles: null,
    stopColor: null,
    stopOpacity: null,
    strikethroughPosition: number,
    strikethroughThickness: number,
    string: null,
    stroke: null,
    strokeDashArray: commaOrSpaceSeparated,
    strokeDashOffset: null,
    strokeLineCap: null,
    strokeLineJoin: null,
    strokeMiterLimit: number,
    strokeOpacity: number,
    strokeWidth: null,
    style: null,
    surfaceScale: number,
    syncBehavior: null,
    syncBehaviorDefault: null,
    syncMaster: null,
    syncTolerance: null,
    syncToleranceDefault: null,
    systemLanguage: commaOrSpaceSeparated,
    tabIndex: number,
    tableValues: null,
    target: null,
    targetX: number,
    targetY: number,
    textAnchor: null,
    textDecoration: null,
    textRendering: null,
    textLength: null,
    timelineBegin: null,
    title: null,
    transformBehavior: null,
    type: null,
    typeOf: commaOrSpaceSeparated,
    to: null,
    transform: null,
    transformOrigin: null,
    u1: null,
    u2: null,
    underlinePosition: number,
    underlineThickness: number,
    unicode: null,
    unicodeBidi: null,
    unicodeRange: null,
    unitsPerEm: number,
    values: null,
    vAlphabetic: number,
    vMathematical: number,
    vectorEffect: null,
    vHanging: number,
    vIdeographic: number,
    version: null,
    vertAdvY: number,
    vertOriginX: number,
    vertOriginY: number,
    viewBox: null,
    viewTarget: null,
    visibility: null,
    width: null,
    widths: null,
    wordSpacing: null,
    writingMode: null,
    x: null,
    x1: null,
    x2: null,
    xChannelSelector: null,
    xHeight: number,
    y: null,
    y1: null,
    y2: null,
    yChannelSelector: null,
    z: null,
    zoomAndPan: null
  },
  space: "svg",
  transform: caseSensitiveTransform
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/xlink.js
var xlink = create({
  properties: {
    xLinkActuate: null,
    xLinkArcRole: null,
    xLinkHref: null,
    xLinkRole: null,
    xLinkShow: null,
    xLinkTitle: null,
    xLinkType: null
  },
  space: "xlink",
  transform(_3, property) {
    return "xlink:" + property.slice(5).toLowerCase();
  }
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/xmlns.js
var xmlns = create({
  attributes: { xmlnsxlink: "xmlns:xlink" },
  properties: { xmlnsXLink: null, xmlns: null },
  space: "xmlns",
  transform: caseInsensitiveTransform
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/xml.js
var xml = create({
  properties: { xmlBase: null, xmlLang: null, xmlSpace: null },
  space: "xml",
  transform(_3, property) {
    return "xml:" + property.slice(3).toLowerCase();
  }
});

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/lib/find.js
var cap = /[A-Z]/g;
var dash = /-[a-z]/g;
var valid = /^data[-\w.:]+$/i;
function find(schema, value) {
  const normal = normalize(value);
  let property = value;
  let Type = Info;
  if (normal in schema.normal) {
    return schema.property[schema.normal[normal]];
  }
  if (normal.length > 4 && normal.slice(0, 4) === "data" && valid.test(value)) {
    if (value.charAt(4) === "-") {
      const rest = value.slice(5).replace(dash, camelcase);
      property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
    } else {
      const rest = value.slice(4);
      if (!dash.test(rest)) {
        let dashes = rest.replace(cap, kebab);
        if (dashes.charAt(0) !== "-") {
          dashes = "-" + dashes;
        }
        value = "data" + dashes;
      }
    }
    Type = DefinedInfo;
  }
  return new Type(property, value);
}
function kebab($0) {
  return "-" + $0.toLowerCase();
}
function camelcase($0) {
  return $0.charAt(1).toUpperCase();
}

// ../../node_modules/.pnpm/property-information@7.1.0/node_modules/property-information/index.js
var html2 = merge([aria, html, xlink, xmlns, xml], "html");
var svg2 = merge([aria, svg, xlink, xmlns, xml], "svg");

// ../../node_modules/.pnpm/zwitch@2.0.4/node_modules/zwitch/index.js
var own = {}.hasOwnProperty;
function zwitch(key2, options) {
  const settings = options || {};
  function one2(value, ...parameters) {
    let fn = one2.invalid;
    const handlers = one2.handlers;
    if (value && own.call(value, key2)) {
      const id = String(value[key2]);
      fn = own.call(handlers, id) ? handlers[id] : one2.unknown;
    }
    if (fn) {
      return fn.call(this, value, ...parameters);
    }
  }
  one2.handlers = settings.handlers || {};
  one2.invalid = settings.invalid;
  one2.unknown = settings.unknown;
  return one2;
}

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/core.js
var defaultSubsetRegex = /["&'<>`]/g;
var surrogatePairsRegex = /[\uD800-\uDBFF][\uDC00-\uDFFF]/g;
var controlCharactersRegex = (
  // eslint-disable-next-line no-control-regex, unicorn/no-hex-escape
  /[\x01-\t\v\f\x0E-\x1F\x7F\x81\x8D\x8F\x90\x9D\xA0-\uFFFF]/g
);
var regexEscapeRegex = /[|\\{}()[\]^$+*?.]/g;
var subsetToRegexCache = /* @__PURE__ */ new WeakMap();
function core(value, options) {
  value = value.replace(
    options.subset ? charactersToExpressionCached(options.subset) : defaultSubsetRegex,
    basic
  );
  if (options.subset || options.escapeOnly) {
    return value;
  }
  return value.replace(surrogatePairsRegex, surrogate).replace(controlCharactersRegex, basic);
  function surrogate(pair, index, all2) {
    return options.format(
      (pair.charCodeAt(0) - 55296) * 1024 + pair.charCodeAt(1) - 56320 + 65536,
      all2.charCodeAt(index + 2),
      options
    );
  }
  function basic(character, index, all2) {
    return options.format(
      character.charCodeAt(0),
      all2.charCodeAt(index + 1),
      options
    );
  }
}
function charactersToExpressionCached(subset) {
  let cached = subsetToRegexCache.get(subset);
  if (!cached) {
    cached = charactersToExpression(subset);
    subsetToRegexCache.set(subset, cached);
  }
  return cached;
}
function charactersToExpression(subset) {
  const groups = [];
  let index = -1;
  while (++index < subset.length) {
    groups.push(subset[index].replace(regexEscapeRegex, "\\$&"));
  }
  return new RegExp("(?:" + groups.join("|") + ")", "g");
}

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/util/to-hexadecimal.js
var hexadecimalRegex = /[\dA-Fa-f]/;
function toHexadecimal(code, next, omit) {
  const value = "&#x" + code.toString(16).toUpperCase();
  return omit && next && !hexadecimalRegex.test(String.fromCharCode(next)) ? value : value + ";";
}

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/util/to-decimal.js
var decimalRegex = /\d/;
function toDecimal(code, next, omit) {
  const value = "&#" + String(code);
  return omit && next && !decimalRegex.test(String.fromCharCode(next)) ? value : value + ";";
}

// ../../node_modules/.pnpm/character-entities-legacy@3.0.0/node_modules/character-entities-legacy/index.js
var characterEntitiesLegacy = [
  "AElig",
  "AMP",
  "Aacute",
  "Acirc",
  "Agrave",
  "Aring",
  "Atilde",
  "Auml",
  "COPY",
  "Ccedil",
  "ETH",
  "Eacute",
  "Ecirc",
  "Egrave",
  "Euml",
  "GT",
  "Iacute",
  "Icirc",
  "Igrave",
  "Iuml",
  "LT",
  "Ntilde",
  "Oacute",
  "Ocirc",
  "Ograve",
  "Oslash",
  "Otilde",
  "Ouml",
  "QUOT",
  "REG",
  "THORN",
  "Uacute",
  "Ucirc",
  "Ugrave",
  "Uuml",
  "Yacute",
  "aacute",
  "acirc",
  "acute",
  "aelig",
  "agrave",
  "amp",
  "aring",
  "atilde",
  "auml",
  "brvbar",
  "ccedil",
  "cedil",
  "cent",
  "copy",
  "curren",
  "deg",
  "divide",
  "eacute",
  "ecirc",
  "egrave",
  "eth",
  "euml",
  "frac12",
  "frac14",
  "frac34",
  "gt",
  "iacute",
  "icirc",
  "iexcl",
  "igrave",
  "iquest",
  "iuml",
  "laquo",
  "lt",
  "macr",
  "micro",
  "middot",
  "nbsp",
  "not",
  "ntilde",
  "oacute",
  "ocirc",
  "ograve",
  "ordf",
  "ordm",
  "oslash",
  "otilde",
  "ouml",
  "para",
  "plusmn",
  "pound",
  "quot",
  "raquo",
  "reg",
  "sect",
  "shy",
  "sup1",
  "sup2",
  "sup3",
  "szlig",
  "thorn",
  "times",
  "uacute",
  "ucirc",
  "ugrave",
  "uml",
  "uuml",
  "yacute",
  "yen",
  "yuml"
];

// ../../node_modules/.pnpm/character-entities-html4@2.1.0/node_modules/character-entities-html4/index.js
var characterEntitiesHtml4 = {
  nbsp: " ",
  iexcl: "¡",
  cent: "¢",
  pound: "£",
  curren: "¤",
  yen: "¥",
  brvbar: "¦",
  sect: "§",
  uml: "¨",
  copy: "©",
  ordf: "ª",
  laquo: "«",
  not: "¬",
  shy: "­",
  reg: "®",
  macr: "¯",
  deg: "°",
  plusmn: "±",
  sup2: "²",
  sup3: "³",
  acute: "´",
  micro: "µ",
  para: "¶",
  middot: "·",
  cedil: "¸",
  sup1: "¹",
  ordm: "º",
  raquo: "»",
  frac14: "¼",
  frac12: "½",
  frac34: "¾",
  iquest: "¿",
  Agrave: "À",
  Aacute: "Á",
  Acirc: "Â",
  Atilde: "Ã",
  Auml: "Ä",
  Aring: "Å",
  AElig: "Æ",
  Ccedil: "Ç",
  Egrave: "È",
  Eacute: "É",
  Ecirc: "Ê",
  Euml: "Ë",
  Igrave: "Ì",
  Iacute: "Í",
  Icirc: "Î",
  Iuml: "Ï",
  ETH: "Ð",
  Ntilde: "Ñ",
  Ograve: "Ò",
  Oacute: "Ó",
  Ocirc: "Ô",
  Otilde: "Õ",
  Ouml: "Ö",
  times: "×",
  Oslash: "Ø",
  Ugrave: "Ù",
  Uacute: "Ú",
  Ucirc: "Û",
  Uuml: "Ü",
  Yacute: "Ý",
  THORN: "Þ",
  szlig: "ß",
  agrave: "à",
  aacute: "á",
  acirc: "â",
  atilde: "ã",
  auml: "ä",
  aring: "å",
  aelig: "æ",
  ccedil: "ç",
  egrave: "è",
  eacute: "é",
  ecirc: "ê",
  euml: "ë",
  igrave: "ì",
  iacute: "í",
  icirc: "î",
  iuml: "ï",
  eth: "ð",
  ntilde: "ñ",
  ograve: "ò",
  oacute: "ó",
  ocirc: "ô",
  otilde: "õ",
  ouml: "ö",
  divide: "÷",
  oslash: "ø",
  ugrave: "ù",
  uacute: "ú",
  ucirc: "û",
  uuml: "ü",
  yacute: "ý",
  thorn: "þ",
  yuml: "ÿ",
  fnof: "ƒ",
  Alpha: "Α",
  Beta: "Β",
  Gamma: "Γ",
  Delta: "Δ",
  Epsilon: "Ε",
  Zeta: "Ζ",
  Eta: "Η",
  Theta: "Θ",
  Iota: "Ι",
  Kappa: "Κ",
  Lambda: "Λ",
  Mu: "Μ",
  Nu: "Ν",
  Xi: "Ξ",
  Omicron: "Ο",
  Pi: "Π",
  Rho: "Ρ",
  Sigma: "Σ",
  Tau: "Τ",
  Upsilon: "Υ",
  Phi: "Φ",
  Chi: "Χ",
  Psi: "Ψ",
  Omega: "Ω",
  alpha: "α",
  beta: "β",
  gamma: "γ",
  delta: "δ",
  epsilon: "ε",
  zeta: "ζ",
  eta: "η",
  theta: "θ",
  iota: "ι",
  kappa: "κ",
  lambda: "λ",
  mu: "μ",
  nu: "ν",
  xi: "ξ",
  omicron: "ο",
  pi: "π",
  rho: "ρ",
  sigmaf: "ς",
  sigma: "σ",
  tau: "τ",
  upsilon: "υ",
  phi: "φ",
  chi: "χ",
  psi: "ψ",
  omega: "ω",
  thetasym: "ϑ",
  upsih: "ϒ",
  piv: "ϖ",
  bull: "•",
  hellip: "…",
  prime: "′",
  Prime: "″",
  oline: "‾",
  frasl: "⁄",
  weierp: "℘",
  image: "ℑ",
  real: "ℜ",
  trade: "™",
  alefsym: "ℵ",
  larr: "←",
  uarr: "↑",
  rarr: "→",
  darr: "↓",
  harr: "↔",
  crarr: "↵",
  lArr: "⇐",
  uArr: "⇑",
  rArr: "⇒",
  dArr: "⇓",
  hArr: "⇔",
  forall: "∀",
  part: "∂",
  exist: "∃",
  empty: "∅",
  nabla: "∇",
  isin: "∈",
  notin: "∉",
  ni: "∋",
  prod: "∏",
  sum: "∑",
  minus: "−",
  lowast: "∗",
  radic: "√",
  prop: "∝",
  infin: "∞",
  ang: "∠",
  and: "∧",
  or: "∨",
  cap: "∩",
  cup: "∪",
  int: "∫",
  there4: "∴",
  sim: "∼",
  cong: "≅",
  asymp: "≈",
  ne: "≠",
  equiv: "≡",
  le: "≤",
  ge: "≥",
  sub: "⊂",
  sup: "⊃",
  nsub: "⊄",
  sube: "⊆",
  supe: "⊇",
  oplus: "⊕",
  otimes: "⊗",
  perp: "⊥",
  sdot: "⋅",
  lceil: "⌈",
  rceil: "⌉",
  lfloor: "⌊",
  rfloor: "⌋",
  lang: "〈",
  rang: "〉",
  loz: "◊",
  spades: "♠",
  clubs: "♣",
  hearts: "♥",
  diams: "♦",
  quot: '"',
  amp: "&",
  lt: "<",
  gt: ">",
  OElig: "Œ",
  oelig: "œ",
  Scaron: "Š",
  scaron: "š",
  Yuml: "Ÿ",
  circ: "ˆ",
  tilde: "˜",
  ensp: " ",
  emsp: " ",
  thinsp: " ",
  zwnj: "‌",
  zwj: "‍",
  lrm: "‎",
  rlm: "‏",
  ndash: "–",
  mdash: "—",
  lsquo: "‘",
  rsquo: "’",
  sbquo: "‚",
  ldquo: "“",
  rdquo: "”",
  bdquo: "„",
  dagger: "†",
  Dagger: "‡",
  permil: "‰",
  lsaquo: "‹",
  rsaquo: "›",
  euro: "€"
};

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/constant/dangerous.js
var dangerous = [
  "cent",
  "copy",
  "divide",
  "gt",
  "lt",
  "not",
  "para",
  "times"
];

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/util/to-named.js
var own2 = {}.hasOwnProperty;
var characters = {};
var key;
for (key in characterEntitiesHtml4) {
  if (own2.call(characterEntitiesHtml4, key)) {
    characters[characterEntitiesHtml4[key]] = key;
  }
}
var notAlphanumericRegex = /[^\dA-Za-z]/;
function toNamed(code, next, omit, attribute) {
  const character = String.fromCharCode(code);
  if (own2.call(characters, character)) {
    const name = characters[character];
    const value = "&" + name;
    if (omit && characterEntitiesLegacy.includes(name) && !dangerous.includes(name) && (!attribute || next && next !== 61 && notAlphanumericRegex.test(String.fromCharCode(next)))) {
      return value;
    }
    return value + ";";
  }
  return "";
}

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/util/format-smart.js
function formatSmart(code, next, options) {
  let numeric = toHexadecimal(code, next, options.omitOptionalSemicolons);
  let named;
  if (options.useNamedReferences || options.useShortestReferences) {
    named = toNamed(
      code,
      next,
      options.omitOptionalSemicolons,
      options.attribute
    );
  }
  if ((options.useShortestReferences || !named) && options.useShortestReferences) {
    const decimal = toDecimal(code, next, options.omitOptionalSemicolons);
    if (decimal.length < numeric.length) {
      numeric = decimal;
    }
  }
  return named && (!options.useShortestReferences || named.length < numeric.length) ? named : numeric;
}

// ../../node_modules/.pnpm/stringify-entities@4.0.4/node_modules/stringify-entities/lib/index.js
function stringifyEntities(value, options) {
  return core(value, Object.assign({ format: formatSmart }, options));
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/comment.js
var htmlCommentRegex = /^>|^->|<!--|-->|--!>|<!-$/g;
var bogusCommentEntitySubset = [">"];
var commentEntitySubset = ["<", ">"];
function comment(node, _1, _22, state) {
  return state.settings.bogusComments ? "<?" + stringifyEntities(
    node.value,
    Object.assign({}, state.settings.characterReferences, {
      subset: bogusCommentEntitySubset
    })
  ) + ">" : "<!--" + node.value.replace(htmlCommentRegex, encode) + "-->";
  function encode($0) {
    return stringifyEntities(
      $0,
      Object.assign({}, state.settings.characterReferences, {
        subset: commentEntitySubset
      })
    );
  }
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/doctype.js
function doctype(_1, _22, _3, state) {
  return "<!" + (state.settings.upperDoctype ? "DOCTYPE" : "doctype") + (state.settings.tightDoctype ? "" : " ") + "html>";
}

// ../../node_modules/.pnpm/ccount@2.0.1/node_modules/ccount/index.js
function ccount(value, character) {
  const source = String(value);
  if (typeof character !== "string") {
    throw new TypeError("Expected character");
  }
  let count = 0;
  let index = source.indexOf(character);
  while (index !== -1) {
    count++;
    index = source.indexOf(character, index + character.length);
  }
  return count;
}

// ../../node_modules/.pnpm/comma-separated-tokens@2.0.3/node_modules/comma-separated-tokens/index.js
function stringify(values, options) {
  const settings = options || {};
  const input = values[values.length - 1] === "" ? [...values, ""] : values;
  return input.join(
    (settings.padRight ? " " : "") + "," + (settings.padLeft === false ? "" : " ")
  ).trim();
}

// ../../node_modules/.pnpm/space-separated-tokens@2.0.2/node_modules/space-separated-tokens/index.js
function stringify2(values) {
  return values.join(" ").trim();
}

// ../../node_modules/.pnpm/hast-util-whitespace@3.0.0/node_modules/hast-util-whitespace/lib/index.js
var re = /[ \t\n\f\r]/g;
function whitespace(thing) {
  return typeof thing === "object" ? thing.type === "text" ? empty(thing.value) : false : empty(thing);
}
function empty(value) {
  return value.replace(re, "") === "";
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/omission/util/siblings.js
var siblingAfter = siblings(1);
var siblingBefore = siblings(-1);
var emptyChildren = [];
function siblings(increment2) {
  return sibling;
  function sibling(parent, index, includeWhitespace) {
    const siblings2 = parent ? parent.children : emptyChildren;
    let offset = (index || 0) + increment2;
    let next = siblings2[offset];
    if (!includeWhitespace) {
      while (next && whitespace(next)) {
        offset += increment2;
        next = siblings2[offset];
      }
    }
    return next;
  }
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/omission/omission.js
var own3 = {}.hasOwnProperty;
function omission(handlers) {
  return omit;
  function omit(node, index, parent) {
    return own3.call(handlers, node.tagName) && handlers[node.tagName](node, index, parent);
  }
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/omission/closing.js
var closing = omission({
  body,
  caption: headOrColgroupOrCaption,
  colgroup: headOrColgroupOrCaption,
  dd,
  dt,
  head: headOrColgroupOrCaption,
  html: html3,
  li,
  optgroup,
  option,
  p,
  rp: rubyElement,
  rt: rubyElement,
  tbody,
  td: cells,
  tfoot,
  th: cells,
  thead,
  tr
});
function headOrColgroupOrCaption(_3, index, parent) {
  const next = siblingAfter(parent, index, true);
  return !next || next.type !== "comment" && !(next.type === "text" && whitespace(next.value.charAt(0)));
}
function html3(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type !== "comment";
}
function body(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type !== "comment";
}
function p(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return next ? next.type === "element" && (next.tagName === "address" || next.tagName === "article" || next.tagName === "aside" || next.tagName === "blockquote" || next.tagName === "details" || next.tagName === "div" || next.tagName === "dl" || next.tagName === "fieldset" || next.tagName === "figcaption" || next.tagName === "figure" || next.tagName === "footer" || next.tagName === "form" || next.tagName === "h1" || next.tagName === "h2" || next.tagName === "h3" || next.tagName === "h4" || next.tagName === "h5" || next.tagName === "h6" || next.tagName === "header" || next.tagName === "hgroup" || next.tagName === "hr" || next.tagName === "main" || next.tagName === "menu" || next.tagName === "nav" || next.tagName === "ol" || next.tagName === "p" || next.tagName === "pre" || next.tagName === "section" || next.tagName === "table" || next.tagName === "ul") : !parent || // Confusing parent.
  !(parent.type === "element" && (parent.tagName === "a" || parent.tagName === "audio" || parent.tagName === "del" || parent.tagName === "ins" || parent.tagName === "map" || parent.tagName === "noscript" || parent.tagName === "video"));
}
function li(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && next.tagName === "li";
}
function dt(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return Boolean(
    next && next.type === "element" && (next.tagName === "dt" || next.tagName === "dd")
  );
}
function dd(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && (next.tagName === "dt" || next.tagName === "dd");
}
function rubyElement(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && (next.tagName === "rp" || next.tagName === "rt");
}
function optgroup(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && next.tagName === "optgroup";
}
function option(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && (next.tagName === "option" || next.tagName === "optgroup");
}
function thead(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return Boolean(
    next && next.type === "element" && (next.tagName === "tbody" || next.tagName === "tfoot")
  );
}
function tbody(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && (next.tagName === "tbody" || next.tagName === "tfoot");
}
function tfoot(_3, index, parent) {
  return !siblingAfter(parent, index);
}
function tr(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && next.tagName === "tr";
}
function cells(_3, index, parent) {
  const next = siblingAfter(parent, index);
  return !next || next.type === "element" && (next.tagName === "td" || next.tagName === "th");
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/omission/opening.js
var opening = omission({
  body: body2,
  colgroup,
  head,
  html: html4,
  tbody: tbody2
});
function html4(node) {
  const head2 = siblingAfter(node, -1);
  return !head2 || head2.type !== "comment";
}
function head(node) {
  const seen = /* @__PURE__ */ new Set();
  for (const child2 of node.children) {
    if (child2.type === "element" && (child2.tagName === "base" || child2.tagName === "title")) {
      if (seen.has(child2.tagName)) return false;
      seen.add(child2.tagName);
    }
  }
  const child = node.children[0];
  return !child || child.type === "element";
}
function body2(node) {
  const head2 = siblingAfter(node, -1, true);
  return !head2 || head2.type !== "comment" && !(head2.type === "text" && whitespace(head2.value.charAt(0))) && !(head2.type === "element" && (head2.tagName === "meta" || head2.tagName === "link" || head2.tagName === "script" || head2.tagName === "style" || head2.tagName === "template"));
}
function colgroup(node, index, parent) {
  const previous = siblingBefore(parent, index);
  const head2 = siblingAfter(node, -1, true);
  if (parent && previous && previous.type === "element" && previous.tagName === "colgroup" && closing(previous, parent.children.indexOf(previous), parent)) {
    return false;
  }
  return Boolean(head2 && head2.type === "element" && head2.tagName === "col");
}
function tbody2(node, index, parent) {
  const previous = siblingBefore(parent, index);
  const head2 = siblingAfter(node, -1);
  if (parent && previous && previous.type === "element" && (previous.tagName === "thead" || previous.tagName === "tbody") && closing(previous, parent.children.indexOf(previous), parent)) {
    return false;
  }
  return Boolean(head2 && head2.type === "element" && head2.tagName === "tr");
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/element.js
var constants = {
  // See: <https://html.spec.whatwg.org/#attribute-name-state>.
  name: [
    ["	\n\f\r &/=>".split(""), "	\n\f\r \"&'/=>`".split("")],
    [`\0	
\f\r "&'/<=>`.split(""), "\0	\n\f\r \"&'/<=>`".split("")]
  ],
  // See: <https://html.spec.whatwg.org/#attribute-value-(unquoted)-state>.
  unquoted: [
    ["	\n\f\r &>".split(""), "\0	\n\f\r \"&'<=>`".split("")],
    ["\0	\n\f\r \"&'<=>`".split(""), "\0	\n\f\r \"&'<=>`".split("")]
  ],
  // See: <https://html.spec.whatwg.org/#attribute-value-(single-quoted)-state>.
  single: [
    ["&'".split(""), "\"&'`".split("")],
    ["\0&'".split(""), "\0\"&'`".split("")]
  ],
  // See: <https://html.spec.whatwg.org/#attribute-value-(double-quoted)-state>.
  double: [
    ['"&'.split(""), "\"&'`".split("")],
    ['\0"&'.split(""), "\0\"&'`".split("")]
  ]
};
function element(node, index, parent, state) {
  const schema = state.schema;
  const omit = schema.space === "svg" ? false : state.settings.omitOptionalTags;
  let selfClosing = schema.space === "svg" ? state.settings.closeEmptyElements : state.settings.voids.includes(node.tagName.toLowerCase());
  const parts = [];
  let last;
  if (schema.space === "html" && node.tagName === "svg") {
    state.schema = svg2;
  }
  const attributes = serializeAttributes(state, node.properties);
  const content = state.all(
    schema.space === "html" && node.tagName === "template" ? node.content : node
  );
  state.schema = schema;
  if (content) selfClosing = false;
  if (attributes || !omit || !opening(node, index, parent)) {
    parts.push("<", node.tagName, attributes ? " " + attributes : "");
    if (selfClosing && (schema.space === "svg" || state.settings.closeSelfClosing)) {
      last = attributes.charAt(attributes.length - 1);
      if (!state.settings.tightSelfClosing || last === "/" || last && last !== '"' && last !== "'") {
        parts.push(" ");
      }
      parts.push("/");
    }
    parts.push(">");
  }
  parts.push(content);
  if (!selfClosing && (!omit || !closing(node, index, parent))) {
    parts.push("</" + node.tagName + ">");
  }
  return parts.join("");
}
function serializeAttributes(state, properties) {
  const values = [];
  let index = -1;
  let key2;
  if (properties) {
    for (key2 in properties) {
      if (properties[key2] !== null && properties[key2] !== void 0) {
        const value = serializeAttribute(state, key2, properties[key2]);
        if (value) values.push(value);
      }
    }
  }
  while (++index < values.length) {
    const last = state.settings.tightAttributes ? values[index].charAt(values[index].length - 1) : void 0;
    if (index !== values.length - 1 && last !== '"' && last !== "'") {
      values[index] += " ";
    }
  }
  return values.join("");
}
function serializeAttribute(state, key2, value) {
  const info = find(state.schema, key2);
  const x3 = state.settings.allowParseErrors && state.schema.space === "html" ? 0 : 1;
  const y3 = state.settings.allowDangerousCharacters ? 0 : 1;
  let quote = state.quote;
  let result;
  if (info.overloadedBoolean && (value === info.attribute || value === "")) {
    value = true;
  } else if ((info.boolean || info.overloadedBoolean) && (typeof value !== "string" || value === info.attribute || value === "")) {
    value = Boolean(value);
  }
  if (value === null || value === void 0 || value === false || typeof value === "number" && Number.isNaN(value)) {
    return "";
  }
  const name = stringifyEntities(
    info.attribute,
    Object.assign({}, state.settings.characterReferences, {
      // Always encode without parse errors in non-HTML.
      subset: constants.name[x3][y3]
    })
  );
  if (value === true) return name;
  value = Array.isArray(value) ? (info.commaSeparated ? stringify : stringify2)(value, {
    padLeft: !state.settings.tightCommaSeparatedLists
  }) : String(value);
  if (state.settings.collapseEmptyAttributes && !value) return name;
  if (state.settings.preferUnquoted) {
    result = stringifyEntities(
      value,
      Object.assign({}, state.settings.characterReferences, {
        attribute: true,
        subset: constants.unquoted[x3][y3]
      })
    );
  }
  if (result !== value) {
    if (state.settings.quoteSmart && ccount(value, quote) > ccount(value, state.alternative)) {
      quote = state.alternative;
    }
    result = quote + stringifyEntities(
      value,
      Object.assign({}, state.settings.characterReferences, {
        // Always encode without parse errors in non-HTML.
        subset: (quote === "'" ? constants.single : constants.double)[x3][y3],
        attribute: true
      })
    ) + quote;
  }
  return name + (result ? "=" + result : result);
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/text.js
var textEntitySubset = ["<", "&"];
function text(node, _3, parent, state) {
  return parent && parent.type === "element" && (parent.tagName === "script" || parent.tagName === "style") ? node.value : stringifyEntities(
    node.value,
    Object.assign({}, state.settings.characterReferences, {
      subset: textEntitySubset
    })
  );
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/raw.js
function raw(node, index, parent, state) {
  return state.settings.allowDangerousHtml ? node.value : text(node, index, parent, state);
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/root.js
function root(node, _1, _22, state) {
  return state.all(node);
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/handle/index.js
var handle = zwitch("type", {
  invalid,
  unknown,
  handlers: { comment, doctype, element, raw, root, text }
});
function invalid(node) {
  throw new Error("Expected node, not `" + node + "`");
}
function unknown(node_) {
  const node = (
    /** @type {Nodes} */
    node_
  );
  throw new Error("Cannot compile unknown node `" + node.type + "`");
}

// ../../node_modules/.pnpm/hast-util-to-html@9.0.5/node_modules/hast-util-to-html/lib/index.js
var emptyOptions = {};
var emptyCharacterReferences = {};
var emptyChildren2 = [];
function toHtml(tree, options) {
  const options_ = options || emptyOptions;
  const quote = options_.quote || '"';
  const alternative = quote === '"' ? "'" : '"';
  if (quote !== '"' && quote !== "'") {
    throw new Error("Invalid quote `" + quote + "`, expected `'` or `\"`");
  }
  const state = {
    one,
    all,
    settings: {
      omitOptionalTags: options_.omitOptionalTags || false,
      allowParseErrors: options_.allowParseErrors || false,
      allowDangerousCharacters: options_.allowDangerousCharacters || false,
      quoteSmart: options_.quoteSmart || false,
      preferUnquoted: options_.preferUnquoted || false,
      tightAttributes: options_.tightAttributes || false,
      upperDoctype: options_.upperDoctype || false,
      tightDoctype: options_.tightDoctype || false,
      bogusComments: options_.bogusComments || false,
      tightCommaSeparatedLists: options_.tightCommaSeparatedLists || false,
      tightSelfClosing: options_.tightSelfClosing || false,
      collapseEmptyAttributes: options_.collapseEmptyAttributes || false,
      allowDangerousHtml: options_.allowDangerousHtml || false,
      voids: options_.voids || htmlVoidElements,
      characterReferences: options_.characterReferences || emptyCharacterReferences,
      closeSelfClosing: options_.closeSelfClosing || false,
      closeEmptyElements: options_.closeEmptyElements || false
    },
    schema: options_.space === "svg" ? svg2 : html2,
    quote,
    alternative
  };
  return state.one(
    Array.isArray(tree) ? { type: "root", children: tree } : tree,
    void 0,
    void 0
  );
}
function one(node, index, parent) {
  return handle(node, index, parent, this);
}
function all(parent) {
  const results = [];
  const children = parent && parent.children || emptyChildren2;
  let index = -1;
  while (++index < children.length) {
    results[index] = this.one(children[index], index, parent);
  }
  return results.join("");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/managers/InteractionManager.js
var InteractionManager = class {
  constructor(mode, options) {
    __publicField(this, "mode");
    __publicField(this, "options");
    __publicField(this, "hoveredLine");
    __publicField(this, "hoveredToken");
    __publicField(this, "pre");
    __publicField(this, "gutterUtilityLine");
    __publicField(this, "gutterUtilityContainer");
    __publicField(this, "gutterUtilityButton");
    __publicField(this, "gutterUtilitySlot");
    __publicField(this, "interactiveLinesAttr", false);
    __publicField(this, "interactiveLineNumbersAttr", false);
    __publicField(this, "hasPointerListeners", false);
    __publicField(this, "hasDocumentPointerListeners", false);
    __publicField(this, "selectedRange", null);
    __publicField(this, "selectedRangeHighlightSide");
    __publicField(this, "selectedRangeLineNumberOnly", false);
    __publicField(this, "editorActiveLine", null);
    __publicField(this, "editorActiveLineSide");
    __publicField(this, "editorLineNumberOnly", false);
    __publicField(this, "proposedSelectedRange");
    __publicField(this, "renderedSelectedLinesState");
    __publicField(this, "renderedEditorActiveLineState");
    __publicField(this, "selectionAnchor");
    __publicField(this, "pointerSession", { mode: "idle" });
    __publicField(this, "getHoveredLine", () => {
      const gutterUtilityLine = this.gutterUtilityLine ?? this.hoveredLine;
      if (gutterUtilityLine != null) {
        if (this.mode === "diff" && gutterUtilityLine.type === "diff-line") return {
          lineNumber: gutterUtilityLine.lineNumber,
          side: gutterUtilityLine.annotationSide
        };
        if (this.mode === "file" && gutterUtilityLine.type === "line") return { lineNumber: gutterUtilityLine.lineNumber };
      }
    });
    __publicField(this, "handlePointerClick", (event) => {
      const { onHunkExpand, onLineClick, onLineNumberClick, onTokenClick, onMergeConflictActionClick } = this.options;
      if (onHunkExpand == null && onLineClick == null && onLineNumberClick == null && onMergeConflictActionClick == null && onTokenClick == null) return;
      if (this.options.onGutterUtilityClick != null && isGutterUtilityPath(event.composedPath())) return;
      debugLogIfEnabled(this.options.__debugPointerEvents, "click", "FileDiff.DEBUG.handlePointerClick:", event);
      this.handlePointerEvent({
        eventType: "click",
        event
      });
    });
    __publicField(this, "handlePointerMove", (event) => {
      if (event.pointerType !== "mouse") return;
      const { lineHoverHighlight = "disabled", onLineEnter, onLineLeave, onTokenEnter, onTokenLeave, enableGutterUtility = false } = this.options;
      if (lineHoverHighlight === "disabled" && !enableGutterUtility && onLineEnter == null && onLineLeave == null && onTokenEnter == null && onTokenLeave == null) return;
      debugLogIfEnabled(this.options.__debugPointerEvents, "move", "FileDiff.DEBUG.handlePointerMove:", event);
      this.handlePointerEvent({
        eventType: "move",
        event
      });
    });
    __publicField(this, "handlePointerLeave", (event) => {
      var _a9, _b, _c, _d;
      const { __debugPointerEvents } = this.options;
      debugLogIfEnabled(__debugPointerEvents, "move", "FileDiff.DEBUG.handlePointerLeave: no event");
      if (this.hoveredLine == null && this.hoveredToken == null) {
        debugLogIfEnabled(__debugPointerEvents, "move", "FileDiff.DEBUG.handlePointerLeave: returned early, no hovered line or token");
        return;
      }
      if (this.hoveredToken != null) {
        (_b = (_a9 = this.options).onTokenLeave) == null ? void 0 : _b.call(_a9, this.hoveredToken, event);
        this.clearHoveredToken();
      }
      if (this.hoveredLine != null) {
        (_d = (_c = this.options).onLineLeave) == null ? void 0 : _d.call(_c, {
          ...this.hoveredLine,
          event
        });
        this.clearHoveredLine();
      }
      this.placeUtility();
    });
    __publicField(this, "handlePointerDown", (event) => {
      if (event.pointerType === "mouse" && event.button !== 0 || this.pre == null || this.pointerSession.mode !== "idle") return;
      const path = event.composedPath();
      if (isGutterUtilityPath(path) && this.options.onGutterUtilityClick != null) this.startGutterSelectionFromPointerDown(event);
      else {
        if (event.pointerType !== "mouse") this.revealUtilityFromGutterPath(path);
        this.startLineSelectionFromPointerDown(event);
      }
    });
    __publicField(this, "handleDocumentPointerMove", (event) => {
      switch (this.pointerSession.mode) {
        case "idle":
          return;
        case "gutterSelecting": {
          if (event.pointerId !== this.pointerSession.pointerId) return;
          event.preventDefault();
          const point = this.resolveSelectionPoint(event, { source: "coordinates-first" });
          if (point == null) return;
          this.pointerSession.current = point;
          this.updateSelection(point.lineNumber, point.side);
          return;
        }
        case "selecting": {
          if (event.pointerId !== this.pointerSession.pointerId) return;
          event.preventDefault();
          const pointerInfo = this.resolveSelectionInfo(event, {
            source: "coordinates-first",
            requireNumberColumn: false
          });
          if (pointerInfo == null || this.selectionAnchor == null) return;
          this.updateSelection(pointerInfo.lineNumber, pointerInfo.eventSide);
          return;
        }
        case "pendingSingleLineUnselect": {
          if (event.pointerId !== this.pointerSession.pointerId) return;
          event.preventDefault();
          const pointerInfo = this.resolveSelectionInfo(event, {
            source: "coordinates-first",
            requireNumberColumn: false
          });
          if (pointerInfo == null || this.selectionAnchor == null) return;
          const point = {
            lineNumber: pointerInfo.lineNumber,
            side: pointerInfo.eventSide
          };
          if (areSelectionPointsEqual(this.pointerSession.pending, point)) return;
          this.updateSelection(pointerInfo.lineNumber, pointerInfo.eventSide, false);
          this.notifySelectionStart(this.getCurrentSelectionRange());
          this.notifySelectionChangeDelta();
          this.pointerSession = {
            mode: "selecting",
            pointerId: event.pointerId
          };
          return;
        }
      }
    });
    __publicField(this, "handleDocumentPointerUp", (event) => {
      const { onGutterUtilityClick } = this.options;
      switch (this.pointerSession.mode) {
        case "idle":
          return;
        case "gutterSelecting": {
          const { pointerSession: session } = this;
          if (event.pointerId !== session.pointerId) return;
          event.preventDefault();
          const point = this.resolveSelectionPoint(event, { source: "coordinates-first" });
          if (point != null) {
            session.current = point;
            this.updateSelection(point.lineNumber, point.side);
          }
          const completedRange = this.buildSelectedLineRange(session.anchor, session.current);
          onGutterUtilityClick == null ? void 0 : onGutterUtilityClick({ ...completedRange });
          this.selectionAnchor = void 0;
          this.notifySelectionEnd(completedRange);
          this.notifySelectionCommitted(completedRange);
          this.clearProposedSelection();
          this.clearPointerSession();
          this.detachDocumentPointerListeners();
          return;
        }
        case "pendingSingleLineUnselect":
          if (event.pointerId !== this.pointerSession.pointerId) return;
          event.preventDefault();
          this.updateSelection(null, void 0, false);
          this.selectionAnchor = void 0;
          this.clearPendingSingleLineState();
          this.detachDocumentPointerListeners();
          this.notifySelectionEnd(this.getCurrentSelectionRange());
          this.notifySelectionCommitted(this.getCurrentSelectionRange());
          this.clearProposedSelection();
          return;
        case "selecting":
          if (event.pointerId !== this.pointerSession.pointerId) return;
          event.preventDefault();
          this.selectionAnchor = void 0;
          this.detachDocumentPointerListeners();
          this.clearPointerSession();
          this.notifySelectionEnd(this.getCurrentSelectionRange());
          this.notifySelectionCommitted(this.getCurrentSelectionRange());
          this.clearProposedSelection();
      }
    });
    __publicField(this, "handleDocumentPointerCancel", (event) => {
      switch (this.pointerSession.mode) {
        case "idle":
          return;
        case "gutterSelecting":
        case "selecting":
        case "pendingSingleLineUnselect":
          if ("pointerId" in this.pointerSession) {
            if (event.pointerId !== this.pointerSession.pointerId) return;
          }
          this.selectionAnchor = void 0;
          this.clearProposedSelection();
          this.clearPendingSingleLineState();
          this.clearPointerSession();
          this.detachDocumentPointerListeners();
      }
    });
    __publicField(this, "renderSelection", () => {
      dequeueRender(this.renderSelection);
      const renderState = this.getSelectionRenderState();
      if (this.pre == null) return;
      const { editorActiveLine, selectedLines } = renderState;
      const selectedLinesChanged = !areLineRenderStatesEqual(this.renderedSelectedLinesState, selectedLines);
      const editorActiveLineChanged = !areLineRenderStatesEqual(this.renderedEditorActiveLineState, editorActiveLine);
      if (!selectedLinesChanged && !editorActiveLineChanged) return;
      if (selectedLinesChanged) this.renderedSelectedLinesState = void 0;
      if (editorActiveLineChanged) this.renderedEditorActiveLineState = void 0;
      const split = this.pre.getAttribute("data-diff-type") === "split";
      const selectedRowRange = !selectedLinesChanged ? void 0 : this.resolveLineRenderRange(selectedLines, split, "selected-lines");
      const editorRowRange = !editorActiveLineChanged ? void 0 : this.resolveLineRenderRange(editorActiveLine, split, "editor-active-line");
      const columns = this.getLineRenderColumns(selectedLinesChanged ? selectedLines : void 0, selectedRowRange, editorActiveLineChanged ? editorActiveLine : void 0, editorRowRange);
      const selectedLinesComplete = selectedLines.range == null || columns.some((column) => column.renderSelectedLines);
      const editorActiveLineComplete = editorActiveLine.range == null || columns.some((column) => column.renderEditorActiveLine);
      if (selectedLinesChanged) for (const element2 of this.pre.querySelectorAll("[data-selected-line]")) element2.removeAttribute("data-selected-line");
      if (editorActiveLineChanged) for (const element2 of this.pre.querySelectorAll("[data-editor-active-line]")) element2.removeAttribute("data-editor-active-line");
      const selectedFirst = selectedRowRange == null ? void 0 : Math.min(selectedRowRange.start, selectedRowRange.end);
      const selectedLast = selectedRowRange == null ? void 0 : Math.max(selectedRowRange.start, selectedRowRange.end);
      const selectedIsSingle = selectedFirst === selectedLast;
      const editorLineIndex = editorRowRange == null ? void 0 : editorRowRange.start;
      for (const column of columns) {
        const { content, gutter, renderEditorActiveLine, renderSelectedLines } = column;
        const finalLineIndex = Math.max(renderSelectedLines ? selectedLast ?? -Infinity : -Infinity, renderEditorActiveLine ? editorLineIndex ?? -Infinity : -Infinity);
        const len = content.children.length;
        for (let i2 = 0; i2 < len; i2++) {
          const contentElement = content.children[i2];
          const gutterElement = gutter.children[i2];
          if (!(contentElement instanceof HTMLElement) || !(gutterElement instanceof HTMLElement)) continue;
          const lineIndex = this.parseLineIndex(contentElement, split);
          if ((lineIndex ?? 0) > finalLineIndex) break;
          if (lineIndex == null) continue;
          if (renderSelectedLines && selectedFirst != null && selectedLast != null && lineIndex >= selectedFirst && lineIndex <= selectedLast) {
            let attributeValue = selectedIsSingle ? "single" : lineIndex === selectedFirst ? "first" : lineIndex === selectedLast ? "last" : "";
            gutterElement.setAttribute("data-selected-line", attributeValue);
            if (!selectedLines.lineNumberOnly) {
              contentElement.setAttribute("data-selected-line", attributeValue);
              if (gutterElement.nextSibling instanceof HTMLElement && contentElement.nextSibling instanceof HTMLElement && (contentElement.nextSibling.hasAttribute("data-line-annotation") || contentElement.nextSibling.hasAttribute("data-merge-conflict-actions"))) {
                if (selectedIsSingle) {
                  attributeValue = "last";
                  contentElement.setAttribute("data-selected-line", "first");
                } else if (lineIndex === selectedFirst) attributeValue = "";
                else if (lineIndex === selectedLast) contentElement.setAttribute("data-selected-line", "");
                contentElement.nextSibling.setAttribute("data-selected-line", attributeValue);
                gutterElement.nextSibling.setAttribute("data-selected-line", attributeValue);
              }
            }
          }
          if (renderEditorActiveLine && lineIndex === editorLineIndex) {
            gutterElement.setAttribute("data-editor-active-line", "");
            if (!editorActiveLine.lineNumberOnly) contentElement.setAttribute("data-editor-active-line", "");
          }
        }
      }
      if (selectedLinesChanged && selectedLinesComplete) this.renderedSelectedLinesState = selectedLines;
      if (editorActiveLineChanged && editorActiveLineComplete) this.renderedEditorActiveLineState = editorActiveLine;
    });
    this.mode = mode;
    this.options = options;
  }
  setOptions(options) {
    this.options = options;
  }
  cleanUp() {
    var _a9, _b, _c, _d, _e, _f, _g;
    (_a9 = this.pre) == null ? void 0 : _a9.removeEventListener("click", this.handlePointerClick);
    (_b = this.pre) == null ? void 0 : _b.removeEventListener("pointerdown", this.handlePointerDown);
    (_c = this.pre) == null ? void 0 : _c.removeEventListener("pointermove", this.handlePointerMove);
    (_d = this.pre) == null ? void 0 : _d.removeEventListener("pointerleave", this.handlePointerLeave);
    (_e = this.pre) == null ? void 0 : _e.removeAttribute("data-interactive-lines");
    (_f = this.pre) == null ? void 0 : _f.removeAttribute("data-interactive-line-numbers");
    this.pre = void 0;
    (_g = this.gutterUtilityContainer) == null ? void 0 : _g.remove();
    this.gutterUtilityLine = void 0;
    this.gutterUtilityContainer = void 0;
    this.gutterUtilityButton = void 0;
    this.gutterUtilitySlot = void 0;
    this.clearHoveredLine();
    this.clearHoveredToken();
    this.detachDocumentPointerListeners();
    this.clearPointerSession();
    dequeueRender(this.renderSelection);
    this.interactiveLinesAttr = false;
    this.interactiveLineNumbersAttr = false;
    this.hasPointerListeners = false;
    this.setSelectionDirty();
  }
  setup(pre) {
    this.setSelectionDirty();
    const { usesCustomGutterUtility = false, enableGutterUtility = false } = this.options;
    if (this.pre !== pre) {
      this.cleanUp();
      this.pre = pre;
    }
    if (enableGutterUtility) this.ensureGutterUtilityNode(usesCustomGutterUtility);
    else if (this.gutterUtilityContainer != null) {
      this.gutterUtilityContainer.remove();
      this.gutterUtilityLine = void 0;
      this.gutterUtilityContainer = void 0;
      this.gutterUtilityButton = void 0;
      this.gutterUtilitySlot = void 0;
      if (this.pointerSession.mode === "gutterSelecting") {
        this.clearPointerSession();
        this.detachDocumentPointerListeners();
      }
    }
    this.syncPointerListeners(pre);
    this.updateInteractiveLineAttributes();
    this.renderSelection();
    this.placeUtility();
  }
  setSelectionDirty() {
    this.renderedSelectedLinesState = void 0;
    this.renderedEditorActiveLineState = void 0;
  }
  isSelectionDirty() {
    return this.renderedSelectedLinesState === void 0 || this.renderedEditorActiveLineState === void 0;
  }
  setSelection(range2, options) {
    const isRangeChange = !(range2 === this.selectedRange || areSelectionsEqual(range2 ?? void 0, this.selectedRange ?? void 0));
    const lineNumberOnly = (options == null ? void 0 : options.lineNumberOnly) ?? false;
    const isStyleChange = (options == null ? void 0 : options.activeLineSide) !== this.selectedRangeHighlightSide || lineNumberOnly !== this.selectedRangeLineNumberOnly;
    if (!this.isSelectionDirty() && !isRangeChange && !isStyleChange) return;
    this.proposedSelectedRange = void 0;
    this.selectedRange = range2;
    this.selectedRangeHighlightSide = options == null ? void 0 : options.activeLineSide;
    this.selectedRangeLineNumberOnly = lineNumberOnly;
    this.renderSelection();
    this.placeUtility();
    if (isRangeChange && (options == null ? void 0 : options.notify) !== false) this.notifySelectionCommitted(range2);
  }
  setEditorActiveLine(lineNumber, { lineNumberOnly = false, side } = {}) {
    const isLineChange = lineNumber !== this.editorActiveLine;
    const isStyleChange = side !== this.editorActiveLineSide || lineNumberOnly !== this.editorLineNumberOnly;
    if (!this.isSelectionDirty() && !isLineChange && !isStyleChange) return;
    this.editorActiveLine = lineNumber;
    this.editorActiveLineSide = side;
    this.editorLineNumberOnly = lineNumberOnly;
    this.renderSelection();
  }
  getSelection() {
    return this.selectedRange;
  }
  handlePointerEvent({ eventType, event }) {
    var _a9, _b;
    const { __debugPointerEvents } = this.options;
    const composedPath = event.composedPath();
    debugLogIfEnabled(__debugPointerEvents, eventType, "FileDiff.DEBUG.handlePointerEvent:", {
      eventType,
      composedPath
    });
    const target = this.resolvePointerTarget(composedPath);
    debugLogIfEnabled(__debugPointerEvents, eventType, "FileDiff.DEBUG.handlePointerEvent: resolvePointerTarget result:", target);
    const { onLineClick, onLineNumberClick, onLineEnter, onLineLeave, onTokenClick, onTokenEnter, onTokenLeave, onHunkExpand, onMergeConflictActionClick } = this.options;
    switch (eventType) {
      case "move": {
        const sameLine = isHoverableLinePointerTarget(target) && ((_a9 = this.hoveredLine) == null ? void 0 : _a9.lineElement) === target.lineElement;
        if (!(isTokenPointerTarget(target) && ((_b = this.hoveredToken) == null ? void 0 : _b.tokenElement) === target.tokenElement)) {
          if (this.hoveredToken != null) {
            onTokenLeave == null ? void 0 : onTokenLeave(this.hoveredToken, event);
            this.clearHoveredToken();
          }
          if (isTokenPointerTarget(target)) {
            this.setHoveredToken(this.toTokenEventBaseProps(target));
            onTokenEnter == null ? void 0 : onTokenEnter(this.hoveredToken, event);
          }
        }
        if (!sameLine) {
          if (this.hoveredLine != null) {
            onLineLeave == null ? void 0 : onLineLeave({
              ...this.hoveredLine,
              event
            });
            this.clearHoveredLine();
          }
          if (isHoverableLinePointerTarget(target)) {
            this.setHoveredLine(this.toEventBaseProps(target));
            this.placeUtility();
            onLineEnter == null ? void 0 : onLineEnter({
              ...this.hoveredLine,
              event
            });
          } else this.placeUtility();
        }
        break;
      }
      case "click": {
        if (target == null) break;
        if (isMergeConflictActionPointerTarget(target) && onMergeConflictActionClick != null) {
          onMergeConflictActionClick(target);
          break;
        }
        if (isExpandoPointerTarget(target) && onHunkExpand != null) {
          onHunkExpand(target.hunkIndex, target.all || event.shiftKey ? "both" : target.direction, target.all || event.shiftKey ? Number.POSITIVE_INFINITY : void 0);
          break;
        }
        if (!isHoverableLinePointerTarget(target)) break;
        if (isTokenPointerTarget(target) && onTokenClick != null) onTokenClick(this.toTokenEventBaseProps(target), event);
        const eventBase = this.toEventBaseProps(target);
        if (onLineNumberClick != null && target.numberColumn) onLineNumberClick({
          ...eventBase,
          event
        });
        else if (onLineClick != null) onLineClick({
          ...eventBase,
          event
        });
        break;
      }
    }
  }
  syncPointerListeners(pre) {
    const { __debugPointerEvents, lineHoverHighlight = "disabled", onLineClick, onLineNumberClick, onLineEnter, onLineLeave, onTokenClick, onTokenEnter, onTokenLeave, onHunkExpand, onMergeConflictActionClick, enableGutterUtility = false, enableLineSelection = false, onGutterUtilityClick } = this.options;
    const enableGutterSelection = onGutterUtilityClick != null;
    const shouldAttachPointerListeners = lineHoverHighlight !== "disabled" || onLineClick != null || onLineNumberClick != null || onLineEnter != null || onLineLeave != null || onTokenClick != null || onTokenEnter != null || onTokenLeave != null || onHunkExpand != null || onMergeConflictActionClick != null || enableGutterUtility || enableLineSelection || enableGutterSelection;
    if (shouldAttachPointerListeners && !this.hasPointerListeners) {
      pre.addEventListener("click", this.handlePointerClick);
      pre.addEventListener("pointerdown", this.handlePointerDown);
      pre.addEventListener("pointermove", this.handlePointerMove);
      pre.addEventListener("pointerleave", this.handlePointerLeave);
      this.hasPointerListeners = true;
      debugLogIfEnabled(__debugPointerEvents, "click", "FileDiff.DEBUG.attachEventListeners: Attaching click events for:", (() => {
        const reasons = [];
        if (__debugPointerEvents === "both" || __debugPointerEvents === "click") {
          if (onLineClick != null) reasons.push("onLineClick");
          if (onLineNumberClick != null) reasons.push("onLineNumberClick");
          if (onHunkExpand != null) reasons.push("expandable hunk separators");
          if (onMergeConflictActionClick != null) reasons.push("merge conflict actions");
        }
        return reasons;
      })());
      debugLogIfEnabled(__debugPointerEvents, "move", "FileDiff.DEBUG.attachEventListeners: Attaching pointer move event");
      debugLogIfEnabled(__debugPointerEvents, "move", "FileDiff.DEBUG.attachEventListeners: Attaching pointer leave event");
    } else if (!shouldAttachPointerListeners && this.hasPointerListeners) {
      pre.removeEventListener("click", this.handlePointerClick);
      pre.removeEventListener("pointerdown", this.handlePointerDown);
      pre.removeEventListener("pointermove", this.handlePointerMove);
      pre.removeEventListener("pointerleave", this.handlePointerLeave);
      this.hasPointerListeners = false;
    }
    const hasActiveLineSelectionSession = this.pointerSession.mode === "selecting" || this.pointerSession.mode === "pendingSingleLineUnselect";
    const hasActiveGutterSelectionSession = this.pointerSession.mode === "gutterSelecting";
    if (!enableLineSelection && hasActiveLineSelectionSession || !enableGutterSelection && hasActiveGutterSelectionSession) {
      this.clearPointerSession();
      this.detachDocumentPointerListeners();
      this.selectionAnchor = void 0;
      this.clearPendingSingleLineState();
    }
  }
  updateInteractiveLineAttributes() {
    if (this.pre == null) return;
    const { onLineClick, onLineNumberClick, enableLineSelection = false } = this.options;
    const shouldHaveInteractiveLines = onLineClick != null;
    const shouldHaveInteractiveLineNumbers = onLineNumberClick != null || enableLineSelection;
    if (shouldHaveInteractiveLines && !this.interactiveLinesAttr) {
      this.pre.setAttribute("data-interactive-lines", "");
      this.interactiveLinesAttr = true;
    } else if (!shouldHaveInteractiveLines && this.interactiveLinesAttr) {
      this.pre.removeAttribute("data-interactive-lines");
      this.interactiveLinesAttr = false;
    }
    if (shouldHaveInteractiveLineNumbers && !this.interactiveLineNumbersAttr) {
      this.pre.setAttribute("data-interactive-line-numbers", "");
      this.interactiveLineNumbersAttr = true;
    } else if (!shouldHaveInteractiveLineNumbers && this.interactiveLineNumbersAttr) {
      this.pre.removeAttribute("data-interactive-line-numbers");
      this.interactiveLineNumbersAttr = false;
    }
  }
  startLineSelectionFromPointerDown(event) {
    var _a9, _b;
    const { enableLineSelection = false } = this.options;
    if (!enableLineSelection) return;
    const pointerInfo = this.resolveSelectionInfo(event, {
      source: "event-path",
      requireNumberColumn: true
    });
    if (pointerInfo == null) return;
    const { pre } = this;
    if (pre == null) return;
    const { lineNumber, eventSide, lineIndex } = pointerInfo;
    if (event.shiftKey && this.selectedRange != null) {
      const rowRange = this.getIndexesFromSelection(this.selectedRange, pre.getAttribute("data-diff-type") === "split");
      if (rowRange == null) return;
      const useStart = rowRange.start <= rowRange.end ? lineIndex >= rowRange.start : lineIndex <= rowRange.end;
      this.selectionAnchor = {
        lineNumber: useStart ? this.selectedRange.start : this.selectedRange.end,
        side: useStart ? this.selectedRange.side : this.selectedRange.endSide ?? this.selectedRange.side
      };
      this.updateSelection(lineNumber, eventSide, false);
      this.notifySelectionStart(this.getCurrentSelectionRange());
      this.pointerSession = {
        mode: "selecting",
        pointerId: event.pointerId
      };
      this.attachDocumentPointerListeners();
      return;
    }
    if (((_a9 = this.selectedRange) == null ? void 0 : _a9.start) === lineNumber && ((_b = this.selectedRange) == null ? void 0 : _b.end) === lineNumber) {
      const point = {
        lineNumber,
        side: eventSide
      };
      this.selectionAnchor = point;
      this.pointerSession = {
        mode: "pendingSingleLineUnselect",
        pointerId: event.pointerId,
        anchor: point,
        pending: point
      };
      this.attachDocumentPointerListeners();
      return;
    }
    if (this.options.controlledSelection === true) this.proposedSelectedRange = null;
    else this.selectedRange = null;
    this.placeUtility();
    this.selectionAnchor = {
      lineNumber,
      side: eventSide
    };
    this.updateSelection(lineNumber, eventSide, false);
    this.notifySelectionStart(this.getCurrentSelectionRange());
    this.pointerSession = {
      mode: "selecting",
      pointerId: event.pointerId
    };
    this.attachDocumentPointerListeners();
  }
  startGutterSelectionFromPointerDown(event) {
    const { onGutterUtilityClick } = this.options;
    if (onGutterUtilityClick == null) return;
    const selectedEndpoints = this.currentSelectionEnds();
    const point = (selectedEndpoints == null ? void 0 : selectedEndpoints.bottom) ?? this.resolveSelectionPoint(event, {
      source: "event-path",
      excludeUtility: false
    });
    const anchor = (selectedEndpoints == null ? void 0 : selectedEndpoints.top) ?? point;
    if (point == null || anchor == null) return;
    event.preventDefault();
    event.stopPropagation();
    this.pointerSession = {
      mode: "gutterSelecting",
      pointerId: event.pointerId,
      anchor,
      current: point
    };
    this.selectionAnchor = {
      lineNumber: anchor.lineNumber,
      side: anchor.side
    };
    this.updateSelection(point.lineNumber, point.side, false);
    this.notifySelectionStart(this.getCurrentSelectionRange());
    this.attachDocumentPointerListeners();
  }
  clearHoveredLine() {
    if (this.hoveredLine == null) return;
    this.hoveredLine.lineElement.removeAttribute("data-hovered");
    this.hoveredLine.numberElement.removeAttribute("data-hovered");
    this.hoveredLine = void 0;
  }
  setHoveredLine(hoveredLine) {
    const { lineHoverHighlight = "disabled" } = this.options;
    if (this.hoveredLine != null) this.clearHoveredLine();
    this.hoveredLine = hoveredLine;
    if (lineHoverHighlight !== "disabled") {
      if (lineHoverHighlight === "both" || lineHoverHighlight === "line") this.hoveredLine.lineElement.setAttribute("data-hovered", "");
      if (lineHoverHighlight === "both" || lineHoverHighlight === "number") this.hoveredLine.numberElement.setAttribute("data-hovered", "");
    }
  }
  clearHoveredToken() {
    if (this.hoveredToken == null) return;
    this.hoveredToken = void 0;
  }
  setHoveredToken(hoveredToken) {
    if (this.hoveredToken != null) this.clearHoveredToken();
    this.hoveredToken = hoveredToken;
  }
  ensureGutterUtilityNode(useCustomGutterUtility) {
    var _a9;
    if (this.gutterUtilityContainer == null) {
      this.gutterUtilityContainer = document.createElement("div");
      this.gutterUtilityContainer.setAttribute("data-gutter-utility-slot", "");
    }
    if (useCustomGutterUtility) {
      if (this.gutterUtilityButton != null) {
        this.gutterUtilityButton.remove();
        this.gutterUtilityButton = void 0;
      }
      if (this.gutterUtilitySlot == null) {
        this.gutterUtilitySlot = document.createElement("slot");
        this.gutterUtilitySlot.name = "gutter-utility-slot";
      }
      if (this.gutterUtilitySlot.parentNode !== this.gutterUtilityContainer) this.gutterUtilityContainer.replaceChildren(this.gutterUtilitySlot);
    } else {
      (_a9 = this.gutterUtilitySlot) == null ? void 0 : _a9.remove();
      this.gutterUtilitySlot = void 0;
      if (this.gutterUtilityButton == null) {
        const tempDiv = document.createElement("div");
        tempDiv.innerHTML = toHtml(createGutterUtilityElement());
        const utilityButton = tempDiv.firstElementChild;
        if (!(utilityButton instanceof HTMLButtonElement)) throw new Error("InteractionManager.ensureGutterUtilityNode: Node element should be a button");
        utilityButton.remove();
        this.gutterUtilityButton = utilityButton;
      }
      if (this.gutterUtilityButton.parentNode !== this.gutterUtilityContainer) this.gutterUtilityContainer.replaceChildren(this.gutterUtilityButton);
    }
  }
  revealUtilityFromGutterPath(path) {
    if (this.placeUtilityFromSelection()) return;
    const target = this.resolvePointerTarget(path);
    if (isLinePointerTarget(target) && target.numberColumn) this.showUtilityOnLine(this.toEventBaseProps(target));
  }
  placeUtility() {
    if (this.placeUtilityFromSelection()) return;
    if (this.hoveredLine != null) {
      this.showUtilityOnLine(this.hoveredLine);
      return;
    }
    this.hideUtility();
  }
  placeUtilityFromSelection() {
    const endpoints = this.currentSelectionEnds();
    if (endpoints == null) return false;
    const target = this.targetForSelectionPoint(endpoints.bottom);
    if (target == null) this.hideUtility();
    else this.showUtilityOnLine(this.toEventBaseProps(target));
    return true;
  }
  showUtilityOnLine(line) {
    if (this.gutterUtilityContainer == null) return;
    this.gutterUtilityLine = line;
    line.numberElement.appendChild(this.gutterUtilityContainer);
  }
  hideUtility() {
    var _a9;
    (_a9 = this.gutterUtilityContainer) == null ? void 0 : _a9.remove();
    this.gutterUtilityLine = void 0;
  }
  currentSelectionEnds() {
    const range2 = this.getCurrentSelectionRange();
    return range2 == null ? void 0 : this.selectionEnds(range2);
  }
  selectionEnds(range2) {
    const start = {
      lineNumber: range2.start,
      side: range2.side
    };
    const end = {
      lineNumber: range2.end,
      side: range2.endSide ?? range2.side
    };
    const startIndex = this.selectionPointRowIndex(start);
    const endIndex = this.selectionPointRowIndex(end);
    if (startIndex == null || endIndex == null) return;
    return startIndex > endIndex ? {
      top: end,
      bottom: start
    } : {
      top: start,
      bottom: end
    };
  }
  selectionPointRowIndex(point) {
    const indexes = this.getLineIndex(point.lineNumber, point.side);
    if (indexes == null) return;
    return this.isSplitDiff() ? indexes[1] : indexes[0];
  }
  targetForSelectionPoint(point) {
    if (this.pre == null) return;
    const indexes = this.getLineIndex(point.lineNumber, point.side);
    if (indexes == null) return;
    const lineIndex = this.mode === "diff" ? `${indexes[0]},${indexes[1]}` : `${indexes[0]}`;
    const candidates = this.pre.querySelectorAll(`[data-column-number="${point.lineNumber}"][data-line-index="${lineIndex}"]`);
    for (const element2 of candidates) {
      if (!(element2 instanceof HTMLElement)) continue;
      const target = this.resolvePointerTarget(getElementPath(element2));
      if (!isLinePointerTarget(target)) continue;
      if (this.mode === "diff" && point.side != null && target.side !== point.side) continue;
      return target;
    }
  }
  attachDocumentPointerListeners() {
    if (this.hasDocumentPointerListeners) return;
    document.addEventListener("pointermove", this.handleDocumentPointerMove);
    document.addEventListener("pointerup", this.handleDocumentPointerUp);
    document.addEventListener("pointercancel", this.handleDocumentPointerCancel);
    this.hasDocumentPointerListeners = true;
  }
  detachDocumentPointerListeners() {
    if (!this.hasDocumentPointerListeners) return;
    document.removeEventListener("pointermove", this.handleDocumentPointerMove);
    document.removeEventListener("pointerup", this.handleDocumentPointerUp);
    document.removeEventListener("pointercancel", this.handleDocumentPointerCancel);
    this.hasDocumentPointerListeners = false;
  }
  clearPointerSession() {
    this.pointerSession = { mode: "idle" };
  }
  clearPendingSingleLineState() {
    if (this.pointerSession.mode === "pendingSingleLineUnselect") this.pointerSession = { mode: "idle" };
  }
  selectionInfoFromPath(path, requireNumberColumn) {
    const target = this.resolvePointerTarget(path);
    if (!isLinePointerTarget(target)) return;
    if (requireNumberColumn && !target.numberColumn) return;
    if (target.splitLineIndex == null) return;
    return {
      lineIndex: target.splitLineIndex,
      lineNumber: target.lineNumber,
      eventSide: this.mode === "diff" ? target.side : void 0
    };
  }
  resolveSelectionInfo(event, options) {
    const path = this.resolveSelectionPath(event, options);
    return path != null ? this.selectionInfoFromPath(path, options.requireNumberColumn) : void 0;
  }
  selectionPointFromPath(path) {
    const target = this.resolvePointerTarget(path);
    if (!isLinePointerTarget(target)) return;
    return {
      lineNumber: target.lineNumber,
      side: this.mode === "diff" ? target.side : void 0
    };
  }
  resolveSelectionPoint(event, options) {
    const path = this.resolveSelectionPath(event, options);
    return path != null ? this.selectionPointFromPath(path) : void 0;
  }
  resolveSelectionPath(event, options) {
    const excludeUtility = options.excludeUtility !== false;
    switch (options.source) {
      case "event-path":
        return this.pathFromEventPath(event.composedPath(), excludeUtility);
      case "coordinates-first": {
        const coordinatePath = this.pathFromCoordinates(event, excludeUtility);
        if (coordinatePath !== void 0) return coordinatePath ?? void 0;
        return this.pathFromEventPath(event.composedPath(), excludeUtility);
      }
    }
  }
  pathFromCoordinates(event, excludeUtility) {
    const coordinateTarget = this.hitTest(event);
    if (coordinateTarget === void 0) return;
    if (coordinateTarget === null) return null;
    return this.pathFromElement(coordinateTarget, excludeUtility) ?? null;
  }
  pathFromEventPath(path, excludeUtility) {
    if (excludeUtility && isGutterUtilityPath(path)) return;
    for (const element2 of path) if (element2 instanceof Element) return this.pathFromElement(element2, excludeUtility);
  }
  pathFromElement(element2, excludeUtility) {
    const path = getElementPath(element2);
    if (excludeUtility && isGutterUtilityPath(path)) return;
    const row = closestSelectableRow(element2);
    if (row != null) return getElementPath(row);
    return this.pathFromAnnotationSlot(element2);
  }
  pathFromAnnotationSlot(element2) {
    const point = selectionPointFromAnnotationSlotName(getAnnotationSlotName(element2));
    if (point == null) return;
    const target = this.targetForSelectionPoint(point);
    return target != null ? getElementPath(target.lineElement) : void 0;
  }
  hitTest(event) {
    var _a9;
    if (!Number.isFinite(event.clientX) || !Number.isFinite(event.clientY)) return;
    const root2 = (_a9 = this.pre) == null ? void 0 : _a9.getRootNode();
    const elementFromPointRoot = hasElementFromPoint(root2) ? root2 : hasElementFromPoint(document) ? document : void 0;
    if (elementFromPointRoot == null) return;
    return elementFromPointRoot.elementFromPoint(event.clientX, event.clientY);
  }
  getLineIndex(lineNumber, side) {
    const { getLineIndex } = this.options;
    return getLineIndex != null ? getLineIndex(lineNumber, side) : [lineNumber - 1, lineNumber - 1];
  }
  getCurrentSelectionRange() {
    return this.proposedSelectedRange !== void 0 ? this.proposedSelectedRange : this.selectedRange;
  }
  clearProposedSelection() {
    this.proposedSelectedRange = void 0;
  }
  updateSelection(currentLine, side, emitChange = true) {
    var _a9, _b;
    const previousRange = this.getCurrentSelectionRange();
    let nextRange;
    if (currentLine == null) nextRange = null;
    else {
      const anchorSide = ((_a9 = this.selectionAnchor) == null ? void 0 : _a9.side) ?? side;
      const anchorLine = ((_b = this.selectionAnchor) == null ? void 0 : _b.lineNumber) ?? currentLine;
      nextRange = this.buildSelectionRange(anchorLine, currentLine, anchorSide, side);
    }
    if (areSelectionsEqual(previousRange ?? void 0, nextRange ?? void 0)) return;
    this.selectedRangeHighlightSide = void 0;
    this.selectedRangeLineNumberOnly = false;
    if (this.options.controlledSelection === true) this.proposedSelectedRange = nextRange;
    else {
      this.selectedRange = nextRange;
      queueRender(this.renderSelection);
    }
    this.placeUtility();
    if (emitChange) this.notifySelectionChangeDelta();
  }
  getIndexesFromSelection(selectedRange, split) {
    if (this.pre == null) return;
    const startIndexes = this.getLineIndex(selectedRange.start, selectedRange.side);
    const finalIndexes = this.getLineIndex(selectedRange.end, selectedRange.endSide ?? selectedRange.side);
    return startIndexes != null && finalIndexes != null ? {
      start: split ? startIndexes[1] : startIndexes[0],
      end: split ? finalIndexes[1] : finalIndexes[0]
    } : void 0;
  }
  getSelectionRenderState() {
    return {
      selectedLines: {
        range: this.selectedRange,
        highlightSide: this.selectedRangeHighlightSide,
        lineNumberOnly: this.selectedRangeLineNumberOnly
      },
      editorActiveLine: {
        range: this.editorActiveLine == null ? null : {
          start: this.editorActiveLine,
          end: this.editorActiveLine,
          side: this.editorActiveLineSide
        },
        highlightSide: this.editorActiveLineSide,
        lineNumberOnly: this.editorLineNumberOnly
      }
    };
  }
  resolveLineRenderRange(state, split, source) {
    if (state.range == null) return;
    const rowRange = this.getIndexesFromSelection(state.range, split);
    if (rowRange == null) {
      console.error({
        rowRange,
        range: state.range
      });
      throw new Error(`InteractionManager.renderSelection: No valid ${source} rowRange`);
    }
    return rowRange;
  }
  getLineRenderColumns(selectedLines, selectedRowRange, editorActiveLine, editorRowRange) {
    if (this.pre == null || selectedRowRange == null && editorRowRange == null) return [];
    const { children: codeElements } = this.pre;
    if (codeElements.length > 2) {
      console.error(codeElements);
      throw new Error("InteractionManager.renderSelection: Somehow there are more than 2 code elements...");
    }
    const columns = [];
    for (const code of codeElements) {
      const side = code.hasAttribute("data-deletions") ? "deletions" : code.hasAttribute("data-additions") ? "additions" : void 0;
      const renderSelectedLines = selectedLines != null && selectedRowRange != null && (selectedLines.highlightSide == null || side == null || selectedLines.highlightSide === side);
      const renderEditorActiveLine = editorActiveLine != null && editorRowRange != null && (editorActiveLine.highlightSide == null || side == null || editorActiveLine.highlightSide === side);
      if (!renderSelectedLines && !renderEditorActiveLine) continue;
      const [gutter, content] = code.children;
      if (!(gutter instanceof HTMLElement) || !(content instanceof HTMLElement)) throw new Error("InteractionManager.renderSelection: missing gutter or content element");
      if (content.children.length !== gutter.children.length) throw new Error("InteractionManager.renderSelection: gutter and content children dont match, something is wrong");
      columns.push({
        content,
        gutter,
        renderEditorActiveLine,
        renderSelectedLines
      });
    }
    return columns;
  }
  notifySelectionCommitted(range2) {
    var _a9, _b;
    (_b = (_a9 = this.options).onLineSelected) == null ? void 0 : _b.call(_a9, range2);
  }
  notifySelectionChangeDelta() {
    var _a9, _b;
    (_b = (_a9 = this.options).onLineSelectionChange) == null ? void 0 : _b.call(_a9, this.getCurrentSelectionRange() ?? null);
  }
  notifySelectionStart(range2) {
    var _a9, _b;
    (_b = (_a9 = this.options).onLineSelectionStart) == null ? void 0 : _b.call(_a9, range2);
  }
  notifySelectionEnd(range2) {
    var _a9, _b;
    (_b = (_a9 = this.options).onLineSelectionEnd) == null ? void 0 : _b.call(_a9, range2);
  }
  toEventBaseProps(target) {
    if (this.mode === "file") return {
      type: "line",
      lineElement: target.lineElement,
      lineNumber: target.lineNumber,
      numberColumn: target.numberColumn,
      numberElement: target.numberElement
    };
    return {
      type: "diff-line",
      annotationSide: target.side,
      lineType: target.lineType,
      lineElement: target.lineElement,
      numberElement: target.numberElement,
      lineNumber: target.lineNumber,
      numberColumn: target.numberColumn
    };
  }
  toTokenEventBaseProps({ lineCharEnd, lineCharStart, lineNumber, side, tokenElement, tokenText }) {
    if (this.mode === "file") return {
      type: "token",
      lineCharEnd,
      lineCharStart,
      lineNumber,
      tokenElement,
      tokenText
    };
    return {
      type: "token",
      lineCharEnd,
      lineCharStart,
      lineNumber,
      side,
      tokenElement,
      tokenText
    };
  }
  buildSelectedLineRange(anchor, current) {
    return this.buildSelectionRange(anchor.lineNumber, current.lineNumber, anchor.side, current.side);
  }
  buildSelectionRange(start, end, side, endSide) {
    return {
      start,
      end,
      ...side != null ? { side } : {},
      ...side !== endSide && endSide != null ? { endSide } : {}
    };
  }
  resolvePointerTarget(path) {
    let numberColumn = false;
    let lineType;
    let codeElement;
    let lineElement;
    let lineIndexValue;
    let numberElement;
    let tokenElement;
    let tokenInfo;
    let expandInfo;
    let lineNumber;
    let mergeConflictActionTarget;
    for (const element2 of path) {
      if (!(element2 instanceof HTMLElement)) continue;
      if (mergeConflictActionTarget == null && element2.hasAttribute("data-merge-conflict-action")) {
        const resolutionValue = element2.getAttribute("data-merge-conflict-action") ?? void 0;
        const conflictIndexValue = element2.getAttribute("data-merge-conflict-conflict-index") ?? void 0;
        const conflictIndex = conflictIndexValue != null ? Number.parseInt(conflictIndexValue, 10) : NaN;
        if (isMergeConflictResolution(resolutionValue) && Number.isFinite(conflictIndex)) mergeConflictActionTarget = {
          kind: "merge-conflict-action",
          resolution: resolutionValue,
          conflictIndex
        };
      }
      if (tokenElement == null && element2.hasAttribute("data-char")) {
        tokenElement = element2;
        const startAttr = element2.getAttribute("data-char");
        if (startAttr != null) {
          const lineCharStart = Number.parseInt(startAttr, 10);
          if (!Number.isNaN(lineCharStart)) {
            const tokenText = element2.textContent ?? "";
            const lineCharEnd = lineCharStart + tokenText.length;
            if (tokenText.trim() !== "" || this.options.enableTokenInteractionsOnWhitespace === true) tokenInfo = {
              tokenElement,
              lineCharStart,
              lineCharEnd,
              tokenText
            };
            continue;
          }
        }
      }
      const columnNumber = numberElement == null ? element2.getAttribute("data-column-number") ?? void 0 : void 0;
      if (columnNumber != null) {
        numberElement = element2;
        lineNumber = Number.parseInt(columnNumber, 10);
        numberColumn = true;
        lineType = getLineTypeFromElement(element2);
        lineIndexValue = element2.getAttribute("data-line-index") ?? void 0;
        continue;
      }
      const lineAttr = lineElement == null ? element2.getAttribute("data-line") ?? void 0 : void 0;
      if (lineAttr != null) {
        lineElement = element2;
        lineNumber = Number.parseInt(lineAttr, 10);
        lineType = getLineTypeFromElement(element2);
        lineIndexValue = element2.getAttribute("data-line-index") ?? void 0;
        continue;
      }
      if (expandInfo == null && (element2.hasAttribute("data-expand-button") || element2.hasAttribute("data-unmodified-lines"))) {
        expandInfo = {
          hunkIndex: void 0,
          direction: (() => {
            if (element2.hasAttribute("data-expand-up")) return "up";
            if (element2.hasAttribute("data-expand-down")) return "down";
            return "both";
          })(),
          all: element2.hasAttribute("data-expand-all-button")
        };
        continue;
      }
      const expandIndexValue = expandInfo != null ? element2.getAttribute("data-expand-index") ?? void 0 : void 0;
      if (expandInfo != null && expandIndexValue != null) {
        const expandIndex = Number.parseInt(expandIndexValue, 10);
        if (!Number.isNaN(expandIndex)) expandInfo.hunkIndex = expandIndex;
        continue;
      }
      if (codeElement == null && element2.hasAttribute("data-code")) {
        codeElement = element2;
        break;
      }
    }
    if (mergeConflictActionTarget != null) return mergeConflictActionTarget;
    if ((expandInfo == null ? void 0 : expandInfo.hunkIndex) != null) return {
      type: "line-info",
      hunkIndex: expandInfo.hunkIndex,
      direction: expandInfo.direction,
      all: expandInfo.all
    };
    lineElement ?? (lineElement = lineIndexValue != null ? queryHTMLElement(codeElement, `[data-line][data-line-index="${lineIndexValue}"]`) : void 0);
    numberElement ?? (numberElement = lineIndexValue != null ? queryHTMLElement(codeElement, `[data-column-number][data-line-index="${lineIndexValue}"]`) : void 0);
    if (codeElement == null || lineElement == null || numberElement == null || lineType == null || lineNumber == null || Number.isNaN(lineNumber)) return;
    const splitLineIndex = this.parseLineIndex(lineElement, this.isSplitDiff());
    if (tokenInfo != null) {
      if (this.mode === "file") return {
        kind: "token",
        lineType,
        lineElement,
        lineNumber,
        numberColumn,
        numberElement,
        side: void 0,
        splitLineIndex,
        ...tokenInfo
      };
      return {
        kind: "token",
        lineType,
        lineElement,
        lineNumber,
        numberColumn,
        numberElement,
        side: getAnnotationSide(lineType, codeElement),
        splitLineIndex,
        ...tokenInfo
      };
    }
    if (this.mode === "file") return {
      kind: "line",
      lineType,
      lineElement,
      lineNumber,
      numberColumn,
      numberElement,
      side: void 0,
      splitLineIndex
    };
    return {
      kind: "line",
      lineType,
      lineElement,
      lineNumber,
      numberColumn,
      numberElement,
      side: getAnnotationSide(lineType, codeElement),
      splitLineIndex
    };
  }
  isSplitDiff() {
    var _a9;
    return ((_a9 = this.pre) == null ? void 0 : _a9.getAttribute("data-diff-type")) === "split";
  }
  parseLineIndex(element2, split) {
    const lineIndexes = (element2.getAttribute("data-line-index") ?? "").split(",").map((value) => Number.parseInt(value, 10)).filter((value) => !Number.isNaN(value));
    if (split && lineIndexes.length === 2) return lineIndexes[1];
    if (!split) return lineIndexes[0];
  }
};
function pluckInteractionOptions({ enableTokenInteractionsOnWhitespace, enableGutterUtility, lineHoverHighlight, onGutterUtilityClick, onLineClick, onLineEnter, onLineLeave, onLineNumberClick, onTokenClick, onTokenEnter, onTokenLeave, renderGutterUtility, __debugPointerEvents, enableLineSelection, controlledSelection, onLineSelected, onLineSelectionStart, onLineSelectionChange, onLineSelectionEnd }, onHunkExpand, getLineIndex, onMergeConflictActionClick) {
  return {
    enableTokenInteractionsOnWhitespace,
    enableGutterUtility: resolveEnableGutterUtilityOption({
      enableGutterUtility,
      renderGutterUtility,
      onGutterUtilityClick
    }),
    usesCustomGutterUtility: renderGutterUtility != null,
    lineHoverHighlight,
    onGutterUtilityClick,
    onHunkExpand,
    onMergeConflictActionClick,
    onLineClick,
    onLineEnter,
    onLineLeave,
    onLineNumberClick,
    onTokenClick,
    onTokenEnter,
    onTokenLeave,
    __debugPointerEvents,
    enableLineSelection,
    controlledSelection,
    onLineSelected,
    onLineSelectionStart,
    onLineSelectionChange,
    onLineSelectionEnd,
    getLineIndex
  };
}
function resolveEnableGutterUtilityOption({ enableGutterUtility, renderGutterUtility, onGutterUtilityClick }) {
  if (onGutterUtilityClick != null && renderGutterUtility != null) throw new Error("Cannot use both 'onGutterUtilityClick' and 'renderGutterUtility'. Use only one gutter utility API.");
  return enableGutterUtility ?? false;
}
function isLinePointerTarget(target) {
  return target != null && "kind" in target && target.kind === "line";
}
function isTokenPointerTarget(target) {
  return target != null && "kind" in target && target.kind === "token";
}
function isHoverableLinePointerTarget(target) {
  return isLinePointerTarget(target) || isTokenPointerTarget(target);
}
function isExpandoPointerTarget(target) {
  return "type" in target && target.type === "line-info";
}
function isMergeConflictActionPointerTarget(target) {
  return "kind" in target && target.kind === "merge-conflict-action";
}
function isMergeConflictResolution(value) {
  return value === "current" || value === "incoming" || value === "both";
}
function queryHTMLElement(parent, query) {
  const element2 = parent == null ? void 0 : parent.querySelector(query);
  return element2 instanceof HTMLElement ? element2 : void 0;
}
function getElementPath(element2) {
  const path = [];
  let current = element2;
  while (current != null) {
    path.push(current);
    current = current.parentNode;
  }
  return path;
}
function closestSelectableRow(element2) {
  const row = element2.closest("[data-line], [data-column-number]");
  if (row instanceof HTMLElement) return row;
  const annotationRow = element2.closest('[data-line-annotation], [data-gutter-buffer="annotation"]');
  if (!(annotationRow instanceof HTMLElement)) return;
  const previousRow = annotationRow.previousElementSibling;
  return previousRow instanceof HTMLElement && (previousRow.hasAttribute("data-line") || previousRow.hasAttribute("data-column-number")) ? previousRow : void 0;
}
function getAnnotationSlotName(element2) {
  const slottedElement = element2.closest('[slot^="annotation-"]');
  if (slottedElement instanceof HTMLElement) return slottedElement.getAttribute("slot") ?? void 0;
  if (element2 instanceof HTMLElement) {
    const slotName = element2.getAttribute("name") ?? void 0;
    return slotName != null && slotName.startsWith("annotation-") ? slotName : void 0;
  }
}
function selectionPointFromAnnotationSlotName(slotName) {
  if (slotName == null) return;
  const match = /^annotation-(?:(additions|deletions)-)?(\d+)$/.exec(slotName);
  if (match == null) return;
  const lineNumber = Number.parseInt(match[2], 10);
  if (!Number.isFinite(lineNumber) || lineNumber <= 0) return;
  return {
    lineNumber,
    side: match[1]
  };
}
function hasElementFromPoint(value) {
  return value != null && typeof value.elementFromPoint === "function";
}
function areSelectionRangesEqual(first, second) {
  return first === second || areSelectionsEqual(first ?? void 0, second ?? void 0);
}
function areLineRenderStatesEqual(first, second) {
  return (first == null ? void 0 : first.highlightSide) === second.highlightSide && (first == null ? void 0 : first.lineNumberOnly) === second.lineNumberOnly && areSelectionRangesEqual((first == null ? void 0 : first.range) ?? null, second.range);
}
function getAnnotationSide(lineType, codeElement) {
  switch (lineType) {
    case "change-deletion":
      return "deletions";
    case "change-addition":
      return "additions";
    default:
      return codeElement.hasAttribute("data-deletions") ? "deletions" : "additions";
  }
}
function getLineTypeFromElement(element2) {
  const lineType = element2.getAttribute("data-line-type");
  if (lineType == null) return;
  switch (lineType) {
    case "change-deletion":
    case "change-addition":
    case "context":
    case "context-expanded":
      return lineType;
    default:
      return;
  }
}
function debugLogIfEnabled(debugLogType = "none", logIfType, ...args) {
  switch (debugLogType) {
    case "none":
      return;
    case "both":
      break;
    case "click":
      if (logIfType !== "click") return;
      break;
    case "move":
      if (logIfType !== "move") return;
      break;
  }
  console.log(...args);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/managers/ResizeManager.js
var _a;
var ResizeManager = (_a = class {
  constructor() {
    __publicField(this, "observedNodes", /* @__PURE__ */ new Map());
    __publicField(this, "applyColumnUpdates", (queuedUpdates) => {
      for (const [item, update] of queuedUpdates) {
        const nextCodeWidth = update.codeInlineSize != null ? resolveCodeWidth(update.codeInlineSize) : item.codeWidth;
        const nextNumberWidth = update.numberInlineSize != null ? resolveNumberWidth(update.numberInlineSize) : item.numberWidth;
        const codeWidthChanged = nextCodeWidth !== item.codeWidth;
        const numberWidthChanged = nextNumberWidth !== item.numberWidth;
        if (!codeWidthChanged && !numberWidthChanged) continue;
        item.codeWidth = nextCodeWidth;
        item.numberWidth = nextNumberWidth;
        if (item.applyColumnVariables) applyCodeColumnVariables(item, {
          codeWidthChanged,
          numberWidthChanged
        });
      }
    });
  }
  static getResizeObserver() {
    const resizeObserver = _a.resizeObserver ?? new ResizeObserver(_a.handleSharedResizeEntries);
    _a.resizeObserver = resizeObserver;
    return resizeObserver;
  }
  static handleSharedResizeEntries(entries) {
    const entriesByManager = /* @__PURE__ */ new Map();
    for (const entry of entries) {
      const manager = _a.managersByElement.get(entry.target);
      if (manager == null) continue;
      const managerEntries = entriesByManager.get(manager);
      if (managerEntries == null) entriesByManager.set(manager, [entry]);
      else managerEntries.push(entry);
    }
    for (const [manager, managerEntries] of entriesByManager) manager.handleResizeEntries(managerEntries);
  }
  setup(pre, { disableAnnotations, columnVariables = "apply" }) {
    const annotationUpdates = /* @__PURE__ */ new Set();
    const applyColumnVariables = columnVariables === "apply";
    let columnCount = 0;
    const observedNodes = new Map(this.observedNodes);
    this.observedNodes.clear();
    for (const element2 of pre.children) {
      if (columnCount === 2) break;
      const codeElement = (() => {
        if (element2 instanceof HTMLElement && element2.tagName === "CODE") return element2;
      })();
      if (codeElement == null) continue;
      columnCount++;
      let item = observedNodes.get(codeElement);
      if (item != null && item.type !== "code") throw new Error("ResizeManager.setup: somehow a code node is being used for an annotation, should be impossible");
      let numberElement = codeElement.firstElementChild;
      if (!(numberElement instanceof HTMLElement)) numberElement = null;
      if (item != null) {
        this.observedNodes.set(codeElement, item);
        observedNodes.delete(codeElement);
        if (item.numberElement !== numberElement) {
          if (item.numberElement != null) {
            this.unobserve(item.numberElement);
            observedNodes.delete(item.numberElement);
          }
          if (numberElement != null) {
            this.observe(numberElement);
            observedNodes.delete(numberElement);
            this.observedNodes.set(numberElement, item);
          }
          item.numberElement = numberElement;
          item.numberWidth = 0;
        } else if (item.numberElement != null) {
          observedNodes.delete(item.numberElement);
          this.observedNodes.set(item.numberElement, item);
        } else item.numberWidth = 0;
        syncColumnVariableMode(item, applyColumnVariables);
      } else {
        item = {
          type: "code",
          codeElement,
          numberElement,
          codeWidth: "auto",
          numberWidth: 0,
          applyColumnVariables
        };
        this.observedNodes.set(codeElement, item);
        this.observe(codeElement);
        if (numberElement != null) {
          this.observedNodes.set(numberElement, item);
          this.observe(numberElement);
        }
      }
    }
    if (columnCount > 1 && !disableAnnotations) {
      const annotationElements = pre.querySelectorAll('[data-line-annotation*=","]');
      const elementMap = /* @__PURE__ */ new Map();
      for (const element2 of annotationElements) {
        if (!(element2 instanceof HTMLElement)) continue;
        const lineAnnotation = element2.getAttribute("data-line-annotation") ?? "";
        if (!/^-?\d+,-?\d+$/.test(lineAnnotation)) {
          console.error("DiffFileRenderer.setupResizeObserver: Invalid element or annotation", {
            lineAnnotation,
            element: element2
          });
          continue;
        }
        let pairs = elementMap.get(lineAnnotation);
        if (pairs == null) {
          pairs = [];
          elementMap.set(lineAnnotation, pairs);
        }
        pairs.push(element2);
      }
      for (const [key2, pair] of elementMap) {
        if (pair.length !== 2) {
          console.error("DiffFileRenderer.setupResizeObserver: Bad Pair", key2, pair);
          continue;
        }
        const [container1, container2] = pair;
        const child1 = container1.firstElementChild;
        const child2 = container2.firstElementChild;
        if (!(container1 instanceof HTMLElement) || !(container2 instanceof HTMLElement) || !(child1 instanceof HTMLElement) || !(child2 instanceof HTMLElement)) continue;
        let item = observedNodes.get(child1);
        if (item != null) {
          this.observedNodes.set(child1, item);
          this.observedNodes.set(child2, item);
          observedNodes.delete(child1);
          observedNodes.delete(child2);
          continue;
        }
        const child1Height = child1.getBoundingClientRect().height;
        const child2Height = child2.getBoundingClientRect().height;
        item = {
          type: "annotations",
          column1: {
            container: container1,
            child: child1,
            childHeight: child1Height
          },
          column2: {
            container: container2,
            child: child2,
            childHeight: child2Height
          },
          currentHeight: "auto"
        };
        annotationUpdates.add({
          child1,
          child2,
          item,
          newHeight: Math.max(child1Height, child2Height)
        });
      }
      for (const pendingUpdate of annotationUpdates) {
        this.applyNewHeight(pendingUpdate.item, pendingUpdate.newHeight);
        this.observedNodes.set(pendingUpdate.child1, pendingUpdate.item);
        this.observedNodes.set(pendingUpdate.child2, pendingUpdate.item);
        this.observe(pendingUpdate.child1);
        this.observe(pendingUpdate.child2);
      }
      annotationUpdates.clear();
    }
    for (const [element2, item] of observedNodes) {
      this.unobserve(element2);
      if (item.type === "code") cleanupStaleCodeItem(item);
      else cleanupStaleAnnotationItem(item);
    }
    observedNodes.clear();
  }
  cleanUp() {
    for (const element2 of this.observedNodes.keys()) this.unobserve(element2);
    this.observedNodes.clear();
  }
  observe(element2) {
    const { managersByElement } = _a;
    const owner = managersByElement.get(element2);
    if (owner === this) return;
    else if (owner != null && owner !== this) throw new Error("ResizeManager.observe: element is already owned by another ResizeManager");
    managersByElement.set(element2, this);
    _a.getResizeObserver().observe(element2);
  }
  unobserve(element2) {
    const { managersByElement, resizeObserver } = _a;
    const owner = managersByElement.get(element2);
    if (owner == null) return;
    else if (owner !== this) throw new Error("ResizeManager.unobserve: element is owned by another ResizeManager");
    managersByElement.delete(element2);
    resizeObserver == null ? void 0 : resizeObserver.unobserve(element2);
    if (resizeObserver != null && managersByElement.size === 0) {
      resizeObserver.disconnect();
      _a.resizeObserver = void 0;
    }
  }
  handleResizeEntries(entries) {
    const codeUpdates = /* @__PURE__ */ new Map();
    const annotationUpdates = /* @__PURE__ */ new Set();
    for (const entry of entries) {
      const { target, borderBoxSize, contentBoxSize } = entry;
      if (!(target instanceof HTMLElement)) {
        console.error("ResizeManager.handleResizeEntries: Invalid element for ResizeObserver", entry);
        continue;
      }
      const item = this.observedNodes.get(target);
      if (item == null) {
        console.error("ResizeManager.handleResizeEntries: Not a valid observed node", entry);
        continue;
      }
      if (item.type === "annotations") {
        const column = (() => {
          if (target === item.column1.child) return item.column1;
          if (target === item.column2.child) return item.column2;
        })();
        if (column == null) {
          console.error(`ResizeManager.handleResizeEntries: Couldn't find a column for`, {
            item,
            target
          });
          continue;
        }
        column.childHeight = borderBoxSize[0].blockSize;
        annotationUpdates.add(item);
      } else if (item.type === "code") {
        const update = codeUpdates.get(item) ?? {};
        const inlineSize = contentBoxSize[0].inlineSize;
        if (target === item.codeElement) update.codeInlineSize = inlineSize;
        else if (target === item.numberElement) update.numberInlineSize = inlineSize;
        codeUpdates.set(item, update);
      }
    }
    this.applyAnnotationUpdates(annotationUpdates);
    annotationUpdates.clear();
    this.applyColumnUpdates(codeUpdates);
    codeUpdates.clear();
  }
  applyAnnotationUpdates(annotationUpdates) {
    for (const item of annotationUpdates) this.applyNewHeight(item, Math.max(item.column1.childHeight, item.column2.childHeight));
  }
  applyNewHeight(item, newHeight) {
    if (newHeight !== item.currentHeight) {
      item.currentHeight = Math.max(newHeight, 0);
      item.column1.container.style.setProperty("--diffs-annotation-min-height", `${item.currentHeight}px`);
      item.column2.container.style.setProperty("--diffs-annotation-min-height", `${item.currentHeight}px`);
    }
  }
}, __publicField(_a, "resizeObserver"), __publicField(_a, "managersByElement", /* @__PURE__ */ new Map()), _a);
function resolveCodeWidth(inlineSize) {
  const width = Math.max(Math.floor(inlineSize), 0);
  return width === 0 ? "auto" : width;
}
function resolveNumberWidth(inlineSize) {
  return Math.max(Math.ceil(inlineSize), 0);
}
function syncColumnVariableMode(item, applyColumnVariables) {
  if (item.applyColumnVariables === applyColumnVariables) return;
  item.applyColumnVariables = applyColumnVariables;
  if (applyColumnVariables) applyCodeColumnVariables(item, {
    codeWidthChanged: true,
    numberWidthChanged: true
  });
  else removeCodeColumnVariables(item);
}
function applyCodeColumnVariables(item, { codeWidthChanged, numberWidthChanged }) {
  const { codeElement, codeWidth, numberWidth } = item;
  if (codeWidthChanged) codeElement.style.setProperty("--diffs-column-width", `${typeof codeWidth === "number" ? `${codeWidth}px` : "auto"}`);
  if (numberWidthChanged) codeElement.style.setProperty("--diffs-column-number-width", `${numberWidth === 0 ? "auto" : `${numberWidth}px`}`);
  if (codeWidthChanged || numberWidthChanged && codeWidth !== "auto") {
    const targetWidth = typeof codeWidth === "number" ? Math.max(codeWidth - numberWidth, 0) : 0;
    codeElement.style.setProperty("--diffs-column-content-width", `${targetWidth > 0 ? `${targetWidth}px` : "auto"}`);
  }
}
function removeCodeColumnVariables(item) {
  item.codeElement.style.removeProperty("--diffs-column-content-width");
  item.codeElement.style.removeProperty("--diffs-column-number-width");
  item.codeElement.style.removeProperty("--diffs-column-width");
}
function cleanupStaleCodeItem(item) {
  if (item.codeElement.isConnected) removeCodeColumnVariables(item);
}
function cleanupStaleAnnotationItem(item) {
  if (item.column1.container.isConnected) item.column1.container.style.removeProperty("--diffs-annotation-min-height");
  if (item.column2.container.isConnected) item.column2.container.style.removeProperty("--diffs-annotation-min-height");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/constants.js
var ResolvedLanguages = /* @__PURE__ */ new Map();
var ResolvingLanguages = /* @__PURE__ */ new Map();
var RegisteredCustomLanguages = /* @__PURE__ */ new Map();
var AttachedLanguages = /* @__PURE__ */ new Set();

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/areLanguagesAttached.js
function areLanguagesAttached(languages) {
  for (const language of Array.isArray(languages) ? languages : [languages]) {
    if (language === "text" || language === "ansi") continue;
    if (!AttachedLanguages.has(language)) return false;
  }
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/attachResolvedLanguages.js
function attachResolvedLanguages(resolvedLanguages, highlighter2) {
  resolvedLanguages = Array.isArray(resolvedLanguages) ? resolvedLanguages : [resolvedLanguages];
  for (const resolvedLang of resolvedLanguages) {
    if (AttachedLanguages.has(resolvedLang.name)) continue;
    let lang = ResolvedLanguages.get(resolvedLang.name);
    if (lang == null) {
      lang = resolvedLang;
      ResolvedLanguages.set(resolvedLang.name, lang);
    }
    AttachedLanguages.add(lang.name);
    highlighter2.loadLanguageSync(lang.data);
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/cleanUpResolvedLanguages.js
function cleanUpResolvedLanguages() {
  ResolvedLanguages.clear();
  AttachedLanguages.clear();
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isWorkerContext.js
function isWorkerContext() {
  return typeof WorkerGlobalScope !== "undefined" && typeof self !== "undefined" && self instanceof WorkerGlobalScope;
}

// ../../node_modules/.pnpm/shiki@4.4.3/node_modules/shiki/dist/rolldown-runtime-BBjsoOtd.mjs
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __exportAll = (all2, no_symbols) => {
  let target = {};
  for (var name in all2) __defProp(target, name, {
    get: all2[name],
    enumerable: true
  });
  if (!no_symbols) __defProp(target, Symbol.toStringTag, { value: "Module" });
  return target;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i2 = 0, n = keys.length, key2; i2 < n; i2++) {
    key2 = keys[i2];
    if (!__hasOwnProp.call(to, key2) && key2 !== except) __defProp(to, key2, {
      get: ((k3) => from[k3]).bind(null, key2),
      enumerable: !(desc = __getOwnPropDesc(from, key2)) || desc.enumerable
    });
  }
  return to;
};
var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));

// ../../node_modules/.pnpm/shiki@4.4.3/node_modules/shiki/dist/langs-bundle-full-xQVO1Cek.mjs
var bundledLanguagesInfo = [
  {
    "id": "abap",
    "name": "ABAP",
    "import": (() => import("./abap-TMI3OR5S.js"))
  },
  {
    "id": "actionscript-3",
    "name": "ActionScript",
    "aliases": ["actionscript", "as3"],
    "import": (() => import("./actionscript-3-Q2ZADZK5.js"))
  },
  {
    "id": "ada",
    "name": "Ada",
    "import": (() => import("./ada-SA5QJHHX.js"))
  },
  {
    "id": "ahk",
    "name": "AutoHotkey",
    "aliases": ["ahk1"],
    "import": (() => import("./ahk-PNAW4YGG.js"))
  },
  {
    "id": "ahk2",
    "name": "AutoHotkey2",
    "import": (() => import("./ahk2-4MWU4WIW.js"))
  },
  {
    "id": "angular-html",
    "name": "Angular HTML",
    "import": (() => import("./angular-html-LBJA2NGV.js"))
  },
  {
    "id": "angular-ts",
    "name": "Angular TypeScript",
    "import": (() => import("./angular-ts-ZOWNOQD5.js"))
  },
  {
    "id": "apache",
    "name": "Apache Conf",
    "import": (() => import("./apache-4FIVMS2X.js"))
  },
  {
    "id": "apex",
    "name": "Apex",
    "import": (() => import("./apex-Y2JBLDCV.js"))
  },
  {
    "id": "apl",
    "name": "APL",
    "import": (() => import("./apl-MDU5GJ2T.js"))
  },
  {
    "id": "applescript",
    "name": "AppleScript",
    "import": (() => import("./applescript-RZO2RF6T.js"))
  },
  {
    "id": "ara",
    "name": "Ara",
    "import": (() => import("./ara-WKO7U3VB.js"))
  },
  {
    "id": "asciidoc",
    "name": "AsciiDoc",
    "aliases": ["adoc"],
    "import": (() => import("./asciidoc-ZQ3GLYKE.js"))
  },
  {
    "id": "asm",
    "name": "Assembly",
    "import": (() => import("./asm-MGZQ7NJH.js"))
  },
  {
    "id": "astro",
    "name": "Astro",
    "import": (() => import("./astro-P7XXJ7DI.js"))
  },
  {
    "id": "awk",
    "name": "AWK",
    "import": (() => import("./awk-DY4SWJO5.js"))
  },
  {
    "id": "ballerina",
    "name": "Ballerina",
    "import": (() => import("./ballerina-6N4HO4W6.js"))
  },
  {
    "id": "bat",
    "name": "Batch File",
    "aliases": ["batch", "cmd"],
    "import": (() => import("./bat-IDFNLM5T.js"))
  },
  {
    "id": "beancount",
    "name": "Beancount",
    "import": (() => import("./beancount-2ZRZ4OIP.js"))
  },
  {
    "id": "berry",
    "name": "Berry",
    "aliases": ["be"],
    "import": (() => import("./berry-LLR22EXX.js"))
  },
  {
    "id": "bibtex",
    "name": "BibTeX",
    "import": (() => import("./bibtex-ZZBXLHK5.js"))
  },
  {
    "id": "bicep",
    "name": "Bicep",
    "import": (() => import("./bicep-3QZDFGE2.js"))
  },
  {
    "id": "bird2",
    "name": "BIRD2 Configuration",
    "aliases": ["bird"],
    "import": (() => import("./bird2-MDKFNDIF.js"))
  },
  {
    "id": "blade",
    "name": "Blade",
    "import": (() => import("./blade-OQYMZX3I.js"))
  },
  {
    "id": "bsl",
    "name": "1C (Enterprise)",
    "aliases": ["1c"],
    "import": (() => import("./bsl-JPS7XEFV.js"))
  },
  {
    "id": "c",
    "name": "C",
    "import": (() => import("./c-YLZIJPXT.js"))
  },
  {
    "id": "c3",
    "name": "C3",
    "import": (() => import("./c3-G2QUOGDE.js"))
  },
  {
    "id": "cadence",
    "name": "Cadence",
    "aliases": ["cdc"],
    "import": (() => import("./cadence-P3VNNU6K.js"))
  },
  {
    "id": "cairo",
    "name": "Cairo",
    "import": (() => import("./cairo-FQ42HMLM.js"))
  },
  {
    "id": "chapel",
    "name": "Chapel",
    "aliases": ["chpl"],
    "import": (() => import("./chapel-E2I6CMQC.js"))
  },
  {
    "id": "clarity",
    "name": "Clarity",
    "import": (() => import("./clarity-2FKGYEHP.js"))
  },
  {
    "id": "clojure",
    "name": "Clojure",
    "aliases": ["clj"],
    "import": (() => import("./clojure-AICJ5XYW.js"))
  },
  {
    "id": "cmake",
    "name": "CMake",
    "import": (() => import("./cmake-ZZZHK2TV.js"))
  },
  {
    "id": "cobol",
    "name": "COBOL",
    "import": (() => import("./cobol-HW3RPSNO.js"))
  },
  {
    "id": "codeowners",
    "name": "CODEOWNERS",
    "import": (() => import("./codeowners-LWR4XOSR.js"))
  },
  {
    "id": "codeql",
    "name": "CodeQL",
    "aliases": ["ql"],
    "import": (() => import("./codeql-D6I6SR6E.js"))
  },
  {
    "id": "coffee",
    "name": "CoffeeScript",
    "aliases": ["coffeescript"],
    "import": (() => import("./coffee-AFGUIBPI.js"))
  },
  {
    "id": "common-lisp",
    "name": "Common Lisp",
    "aliases": ["lisp"],
    "import": (() => import("./common-lisp-QK65Q3FH.js"))
  },
  {
    "id": "coq",
    "name": "Rocq",
    "import": (() => import("./coq-GLOZXHGY.js"))
  },
  {
    "id": "cpp",
    "name": "C++",
    "aliases": ["c++"],
    "import": (() => import("./cpp-246YHS4Y.js"))
  },
  {
    "id": "crystal",
    "name": "Crystal",
    "import": (() => import("./crystal-3OH2WJWZ.js"))
  },
  {
    "id": "csharp",
    "name": "C#",
    "aliases": ["c#", "cs"],
    "import": (() => import("./csharp-SI6GQHOP.js"))
  },
  {
    "id": "css",
    "name": "CSS",
    "import": (() => import("./css-ORKVHNCC.js"))
  },
  {
    "id": "csv",
    "name": "CSV",
    "import": (() => import("./csv-IBG4M2NR.js"))
  },
  {
    "id": "cue",
    "name": "CUE",
    "import": (() => import("./cue-UD3EWUFY.js"))
  },
  {
    "id": "cypher",
    "name": "Cypher",
    "aliases": ["cql"],
    "import": (() => import("./cypher-FFJVKGJU.js"))
  },
  {
    "id": "d",
    "name": "D",
    "import": (() => import("./d-LOT6CNBA.js"))
  },
  {
    "id": "dart",
    "name": "Dart",
    "import": (() => import("./dart-3VJWT4LW.js"))
  },
  {
    "id": "dax",
    "name": "DAX",
    "import": (() => import("./dax-4XZTI4BR.js"))
  },
  {
    "id": "desktop",
    "name": "Desktop",
    "import": (() => import("./desktop-FVUITPLV.js"))
  },
  {
    "id": "diff",
    "name": "Diff",
    "import": (() => import("./diff-NGI2S5P5.js"))
  },
  {
    "id": "docker",
    "name": "Dockerfile",
    "aliases": ["dockerfile"],
    "import": (() => import("./docker-VOAWM2PP.js"))
  },
  {
    "id": "dotenv",
    "name": "dotEnv",
    "import": (() => import("./dotenv-FBIQJ6D6.js"))
  },
  {
    "id": "dream-maker",
    "name": "Dream Maker",
    "import": (() => import("./dream-maker-AAEVPYDT.js"))
  },
  {
    "id": "edge",
    "name": "Edge",
    "import": (() => import("./edge-XJQRYNDO.js"))
  },
  {
    "id": "elixir",
    "name": "Elixir",
    "import": (() => import("./elixir-KIU4T7SB.js"))
  },
  {
    "id": "elm",
    "name": "Elm",
    "import": (() => import("./elm-V73EZJF7.js"))
  },
  {
    "id": "emacs-lisp",
    "name": "Emacs Lisp",
    "aliases": ["elisp"],
    "import": (() => import("./emacs-lisp-NX36RSQC.js"))
  },
  {
    "id": "erb",
    "name": "ERB",
    "import": (() => import("./erb-SHTKMXAS.js"))
  },
  {
    "id": "erlang",
    "name": "Erlang",
    "aliases": ["erl"],
    "import": (() => import("./erlang-IDOT2AHL.js"))
  },
  {
    "id": "fennel",
    "name": "Fennel",
    "import": (() => import("./fennel-YW6AC6PZ.js"))
  },
  {
    "id": "fish",
    "name": "Fish",
    "import": (() => import("./fish-LBRSHKG2.js"))
  },
  {
    "id": "fluent",
    "name": "Fluent",
    "aliases": ["ftl"],
    "import": (() => import("./fluent-XZFBTSJ7.js"))
  },
  {
    "id": "fortran-fixed-form",
    "name": "Fortran (Fixed Form)",
    "aliases": [
      "f",
      "for",
      "f77"
    ],
    "import": (() => import("./fortran-fixed-form-7IJGWRUM.js"))
  },
  {
    "id": "fortran-free-form",
    "name": "Fortran (Free Form)",
    "aliases": [
      "f90",
      "f95",
      "f03",
      "f08",
      "f18"
    ],
    "import": (() => import("./fortran-free-form-KPWBJRHW.js"))
  },
  {
    "id": "fsharp",
    "name": "F#",
    "aliases": ["f#", "fs"],
    "import": (() => import("./fsharp-TZDM5G77.js"))
  },
  {
    "id": "gdresource",
    "name": "GDResource",
    "aliases": ["tscn", "tres"],
    "import": (() => import("./gdresource-ENIZQD2H.js"))
  },
  {
    "id": "gdscript",
    "name": "GDScript",
    "aliases": ["gd"],
    "import": (() => import("./gdscript-UXAIFSRK.js"))
  },
  {
    "id": "gdshader",
    "name": "GDShader",
    "import": (() => import("./gdshader-XNSGKZM2.js"))
  },
  {
    "id": "genie",
    "name": "Genie",
    "import": (() => import("./genie-ROW6FZNF.js"))
  },
  {
    "id": "gherkin",
    "name": "Gherkin",
    "import": (() => import("./gherkin-BDZOELC5.js"))
  },
  {
    "id": "git-commit",
    "name": "Git Commit Message",
    "import": (() => import("./git-commit-HQLNGXYA.js"))
  },
  {
    "id": "git-rebase",
    "name": "Git Rebase Message",
    "import": (() => import("./git-rebase-H72SFGXL.js"))
  },
  {
    "id": "gleam",
    "name": "Gleam",
    "import": (() => import("./gleam-2YXMZ2P2.js"))
  },
  {
    "id": "glimmer-js",
    "name": "Glimmer JS",
    "aliases": ["gjs"],
    "import": (() => import("./glimmer-js-3LK5NMHA.js"))
  },
  {
    "id": "glimmer-ts",
    "name": "Glimmer TS",
    "aliases": ["gts"],
    "import": (() => import("./glimmer-ts-VIOR2WUR.js"))
  },
  {
    "id": "glsl",
    "name": "GLSL",
    "import": (() => import("./glsl-5F7JTBCR.js"))
  },
  {
    "id": "gn",
    "name": "GN",
    "import": (() => import("./gn-7OVS6TWX.js"))
  },
  {
    "id": "gnuplot",
    "name": "Gnuplot",
    "import": (() => import("./gnuplot-JVYQK7D3.js"))
  },
  {
    "id": "go",
    "name": "Go",
    "import": (() => import("./go-SZPXKT3M.js"))
  },
  {
    "id": "graphql",
    "name": "GraphQL",
    "aliases": ["gql"],
    "import": (() => import("./graphql-LBWT4ALB.js"))
  },
  {
    "id": "groovy",
    "name": "Groovy",
    "import": (() => import("./groovy-SKCNHNDP.js"))
  },
  {
    "id": "hack",
    "name": "Hack",
    "import": (() => import("./hack-43ZY2GCN.js"))
  },
  {
    "id": "haml",
    "name": "Ruby Haml",
    "import": (() => import("./haml-WXKCVKEC.js"))
  },
  {
    "id": "handlebars",
    "name": "Handlebars",
    "aliases": ["hbs"],
    "import": (() => import("./handlebars-SHBFPCSS.js"))
  },
  {
    "id": "haskell",
    "name": "Haskell",
    "aliases": ["hs"],
    "import": (() => import("./haskell-JZQQ33HO.js"))
  },
  {
    "id": "haxe",
    "name": "Haxe",
    "import": (() => import("./haxe-MENM5WOG.js"))
  },
  {
    "id": "hcl",
    "name": "HashiCorp HCL",
    "import": (() => import("./hcl-YOIKRDYO.js"))
  },
  {
    "id": "hjson",
    "name": "Hjson",
    "import": (() => import("./hjson-TONWI4F2.js"))
  },
  {
    "id": "hlsl",
    "name": "HLSL",
    "import": (() => import("./hlsl-Y646GXEU.js"))
  },
  {
    "id": "html",
    "name": "HTML",
    "import": (() => import("./html-Y5YPYNRI.js"))
  },
  {
    "id": "html-derivative",
    "name": "HTML (Derivative)",
    "import": (() => import("./html-derivative-M4OSEH4E.js"))
  },
  {
    "id": "http",
    "name": "HTTP",
    "import": (() => import("./http-SZ6JCEDZ.js"))
  },
  {
    "id": "hurl",
    "name": "Hurl",
    "import": (() => import("./hurl-L6TC7POZ.js"))
  },
  {
    "id": "hxml",
    "name": "HXML",
    "import": (() => import("./hxml-KR57A2S7.js"))
  },
  {
    "id": "hy",
    "name": "Hy",
    "import": (() => import("./hy-6UFQ36LZ.js"))
  },
  {
    "id": "imba",
    "name": "Imba",
    "import": (() => import("./imba-RTDQ2GJJ.js"))
  },
  {
    "id": "ini",
    "name": "INI",
    "aliases": ["properties"],
    "import": (() => import("./ini-IQ66VMFC.js"))
  },
  {
    "id": "java",
    "name": "Java",
    "import": (() => import("./java-B5U6PC52.js"))
  },
  {
    "id": "javascript",
    "name": "JavaScript",
    "aliases": [
      "js",
      "cjs",
      "mjs"
    ],
    "import": (() => import("./javascript-NE5RX3EH.js"))
  },
  {
    "id": "jinja",
    "name": "Jinja",
    "import": (() => import("./jinja-MP2T7ZJZ.js"))
  },
  {
    "id": "jison",
    "name": "Jison",
    "import": (() => import("./jison-BPHERSQC.js"))
  },
  {
    "id": "json",
    "name": "JSON",
    "import": (() => import("./json-FAIKY6XM.js"))
  },
  {
    "id": "json5",
    "name": "JSON5",
    "import": (() => import("./json5-3XA2YPFW.js"))
  },
  {
    "id": "jsonc",
    "name": "JSON with Comments",
    "import": (() => import("./jsonc-GG57HCSU.js"))
  },
  {
    "id": "jsonl",
    "name": "JSON Lines",
    "import": (() => import("./jsonl-5WUUIICT.js"))
  },
  {
    "id": "jsonnet",
    "name": "Jsonnet",
    "import": (() => import("./jsonnet-JLSJVF3R.js"))
  },
  {
    "id": "jssm",
    "name": "JSSM",
    "aliases": ["fsl"],
    "import": (() => import("./jssm-NRHTYU6Y.js"))
  },
  {
    "id": "jsx",
    "name": "JSX",
    "import": (() => import("./jsx-DTDDMPS5.js"))
  },
  {
    "id": "julia",
    "name": "Julia",
    "aliases": ["jl"],
    "import": (() => import("./julia-LT7LUEBD.js"))
  },
  {
    "id": "just",
    "name": "Just",
    "aliases": ["justfile"],
    "import": (() => import("./just-K5IXM53D.js"))
  },
  {
    "id": "kdl",
    "name": "KDL",
    "import": (() => import("./kdl-J7HIXM5X.js"))
  },
  {
    "id": "kotlin",
    "name": "Kotlin",
    "aliases": ["kt", "kts"],
    "import": (() => import("./kotlin-J4F4VLQF.js"))
  },
  {
    "id": "kusto",
    "name": "Kusto",
    "aliases": ["kql"],
    "import": (() => import("./kusto-RCZEQIDY.js"))
  },
  {
    "id": "latex",
    "name": "LaTeX",
    "import": (() => import("./latex-OWPMY7Z4.js"))
  },
  {
    "id": "lean",
    "name": "Lean 4",
    "aliases": ["lean4"],
    "import": (() => import("./lean-EYD5USMG.js"))
  },
  {
    "id": "less",
    "name": "Less",
    "import": (() => import("./less-T24CNLRY.js"))
  },
  {
    "id": "liquid",
    "name": "Liquid",
    "import": (() => import("./liquid-EUEM3SXO.js"))
  },
  {
    "id": "llvm",
    "name": "LLVM IR",
    "import": (() => import("./llvm-W5Y5EL3R.js"))
  },
  {
    "id": "log",
    "name": "Log file",
    "import": (() => import("./log-VYMLBHRV.js"))
  },
  {
    "id": "logo",
    "name": "Logo",
    "import": (() => import("./logo-VRBH4SYS.js"))
  },
  {
    "id": "lua",
    "name": "Lua",
    "import": (() => import("./lua-GES3LTLB.js"))
  },
  {
    "id": "luau",
    "name": "Luau",
    "import": (() => import("./luau-JRADVSIA.js"))
  },
  {
    "id": "make",
    "name": "Makefile",
    "aliases": ["makefile"],
    "import": (() => import("./make-PBYLBT77.js"))
  },
  {
    "id": "markdown",
    "name": "Markdown",
    "aliases": ["md"],
    "import": (() => import("./markdown-AXGIICJT.js"))
  },
  {
    "id": "marko",
    "name": "Marko",
    "import": (() => import("./marko-O52MYXGU.js"))
  },
  {
    "id": "matlab",
    "name": "MATLAB",
    "import": (() => import("./matlab-SX5ID6KG.js"))
  },
  {
    "id": "mdc",
    "name": "MDC",
    "import": (() => import("./mdc-LTHAPEBE.js"))
  },
  {
    "id": "mdx",
    "name": "MDX",
    "import": (() => import("./mdx-VEBDUDKB.js"))
  },
  {
    "id": "mermaid",
    "name": "Mermaid",
    "aliases": ["mmd"],
    "import": (() => import("./mermaid-ML74LZUY.js"))
  },
  {
    "id": "mipsasm",
    "name": "MIPS Assembly",
    "aliases": ["mips"],
    "import": (() => import("./mipsasm-CRVS7ZWI.js"))
  },
  {
    "id": "mojo",
    "name": "Mojo",
    "import": (() => import("./mojo-OV3E52YM.js"))
  },
  {
    "id": "moonbit",
    "name": "MoonBit",
    "aliases": ["mbt", "mbti"],
    "import": (() => import("./moonbit-WM6PFYQB.js"))
  },
  {
    "id": "move",
    "name": "Move",
    "import": (() => import("./move-CPCYBBFX.js"))
  },
  {
    "id": "narrat",
    "name": "Narrat Language",
    "aliases": ["nar"],
    "import": (() => import("./narrat-BC4UFDVL.js"))
  },
  {
    "id": "nextflow",
    "name": "Nextflow",
    "aliases": ["nf"],
    "import": (() => import("./nextflow-FTUY3FHR.js"))
  },
  {
    "id": "nextflow-groovy",
    "name": "Nextflow Groovy",
    "import": (() => import("./nextflow-groovy-7CXS7FLZ.js"))
  },
  {
    "id": "nginx",
    "name": "Nginx",
    "import": (() => import("./nginx-CYDPDPIY.js"))
  },
  {
    "id": "nim",
    "name": "Nim",
    "import": (() => import("./nim-DMCJSYVO.js"))
  },
  {
    "id": "nix",
    "name": "Nix",
    "import": (() => import("./nix-J652RT4T.js"))
  },
  {
    "id": "nsis",
    "name": "NSIS",
    "import": (() => import("./nsis-GJIBJSEF.js"))
  },
  {
    "id": "nushell",
    "name": "nushell",
    "aliases": ["nu"],
    "import": (() => import("./nushell-SI74MMW4.js"))
  },
  {
    "id": "objective-c",
    "name": "Objective-C",
    "aliases": ["objc"],
    "import": (() => import("./objective-c-Z5LCTRLZ.js"))
  },
  {
    "id": "objective-cpp",
    "name": "Objective-C++",
    "import": (() => import("./objective-cpp-LSUSUX7H.js"))
  },
  {
    "id": "ocaml",
    "name": "OCaml",
    "import": (() => import("./ocaml-2O4F5WLI.js"))
  },
  {
    "id": "odin",
    "name": "Odin",
    "import": (() => import("./odin-BGYMTLQW.js"))
  },
  {
    "id": "openscad",
    "name": "OpenSCAD",
    "aliases": ["scad"],
    "import": (() => import("./openscad-H6ZJWICM.js"))
  },
  {
    "id": "org",
    "name": "Org Markup",
    "import": (() => import("./org-RE44CK46.js"))
  },
  {
    "id": "pascal",
    "name": "Pascal",
    "import": (() => import("./pascal-ZCMK3VZU.js"))
  },
  {
    "id": "perl",
    "name": "Perl",
    "import": (() => import("./perl-GFQMZ56G.js"))
  },
  {
    "id": "php",
    "name": "PHP",
    "import": (() => import("./php-TXEATVCI.js"))
  },
  {
    "id": "pkl",
    "name": "Pkl",
    "import": (() => import("./pkl-MXJHOF5I.js"))
  },
  {
    "id": "plsql",
    "name": "PL/SQL",
    "import": (() => import("./plsql-NTVZLKEF.js"))
  },
  {
    "id": "po",
    "name": "Gettext PO",
    "aliases": ["pot", "potx"],
    "import": (() => import("./po-D6AX75KH.js"))
  },
  {
    "id": "polar",
    "name": "Polar",
    "import": (() => import("./polar-VWALXFZ5.js"))
  },
  {
    "id": "postcss",
    "name": "PostCSS",
    "import": (() => import("./postcss-GQS4FIZL.js"))
  },
  {
    "id": "powerquery",
    "name": "PowerQuery",
    "import": (() => import("./powerquery-TBFDSNYD.js"))
  },
  {
    "id": "powershell",
    "name": "PowerShell",
    "aliases": [
      "ps",
      "ps1",
      "pwsh"
    ],
    "import": (() => import("./powershell-UEHWSCJ4.js"))
  },
  {
    "id": "prisma",
    "name": "Prisma",
    "import": (() => import("./prisma-37JLG5L3.js"))
  },
  {
    "id": "prolog",
    "name": "Prolog",
    "import": (() => import("./prolog-SE7VTX4V.js"))
  },
  {
    "id": "proto",
    "name": "Protocol Buffer 3",
    "aliases": ["protobuf"],
    "import": (() => import("./proto-PK7EJZ3Q.js"))
  },
  {
    "id": "pug",
    "name": "Pug",
    "aliases": ["jade"],
    "import": (() => import("./pug-XSBWLJSR.js"))
  },
  {
    "id": "puppet",
    "name": "Puppet",
    "import": (() => import("./puppet-MGFX45MF.js"))
  },
  {
    "id": "purescript",
    "name": "PureScript",
    "import": (() => import("./purescript-WEMLPVVI.js"))
  },
  {
    "id": "python",
    "name": "Python",
    "aliases": ["py"],
    "import": (() => import("./python-HTOHNSYL.js"))
  },
  {
    "id": "qml",
    "name": "QML",
    "import": (() => import("./qml-GQNSVCQD.js"))
  },
  {
    "id": "qmldir",
    "name": "QML Directory",
    "import": (() => import("./qmldir-GKCNHFTC.js"))
  },
  {
    "id": "qss",
    "name": "Qt Style Sheets",
    "import": (() => import("./qss-FPJIIWF3.js"))
  },
  {
    "id": "r",
    "name": "R",
    "import": (() => import("./r-IRMK4RAV.js"))
  },
  {
    "id": "racket",
    "name": "Racket",
    "import": (() => import("./racket-OZGEG3EG.js"))
  },
  {
    "id": "raku",
    "name": "Raku",
    "aliases": ["perl6"],
    "import": (() => import("./raku-KYYZ4SEA.js"))
  },
  {
    "id": "razor",
    "name": "ASP.NET Razor",
    "import": (() => import("./razor-APAOK6NK.js"))
  },
  {
    "id": "rbs",
    "name": "RBS",
    "aliases": ["ruby-signature"],
    "import": (() => import("./rbs-DHWSBAPV.js"))
  },
  {
    "id": "reg",
    "name": "Windows Registry Script",
    "import": (() => import("./reg-7FJQR4IY.js"))
  },
  {
    "id": "regexp",
    "name": "RegExp",
    "aliases": ["regex"],
    "import": (() => import("./regexp-PNAJAX2T.js"))
  },
  {
    "id": "rel",
    "name": "Rel",
    "import": (() => import("./rel-P6RHQ43O.js"))
  },
  {
    "id": "riscv",
    "name": "RISC-V",
    "import": (() => import("./riscv-ZJADWUJO.js"))
  },
  {
    "id": "ron",
    "name": "RON",
    "import": (() => import("./ron-3Y6SB226.js"))
  },
  {
    "id": "rosmsg",
    "name": "ROS Interface",
    "import": (() => import("./rosmsg-GZRY3N62.js"))
  },
  {
    "id": "rst",
    "name": "reStructuredText",
    "import": (() => import("./rst-CECMCZG4.js"))
  },
  {
    "id": "ruby",
    "name": "Ruby",
    "aliases": ["rb"],
    "import": (() => import("./ruby-DXAW32KY.js"))
  },
  {
    "id": "rust",
    "name": "Rust",
    "aliases": ["rs"],
    "import": (() => import("./rust-7OEPUVLF.js"))
  },
  {
    "id": "sas",
    "name": "SAS",
    "import": (() => import("./sas-LAHHUPCA.js"))
  },
  {
    "id": "sass",
    "name": "Sass",
    "import": (() => import("./sass-54AZYT6H.js"))
  },
  {
    "id": "scala",
    "name": "Scala",
    "import": (() => import("./scala-Q5L3HJ25.js"))
  },
  {
    "id": "scheme",
    "name": "Scheme",
    "import": (() => import("./scheme-2V74MVAZ.js"))
  },
  {
    "id": "scss",
    "name": "SCSS",
    "import": (() => import("./scss-4AIOKSM6.js"))
  },
  {
    "id": "sdbl",
    "name": "1C (Query)",
    "aliases": ["1c-query"],
    "import": (() => import("./sdbl-4ATW43HK.js"))
  },
  {
    "id": "shaderlab",
    "name": "ShaderLab",
    "aliases": ["shader"],
    "import": (() => import("./shaderlab-E4W52ODO.js"))
  },
  {
    "id": "shellscript",
    "name": "Shell",
    "aliases": [
      "bash",
      "sh",
      "shell",
      "zsh"
    ],
    "import": (() => import("./shellscript-E7HUJBWN.js"))
  },
  {
    "id": "shellsession",
    "name": "Shell Session",
    "aliases": ["console"],
    "import": (() => import("./shellsession-E7VLCY3H.js"))
  },
  {
    "id": "smalltalk",
    "name": "GNU Smalltalk",
    "import": (() => import("./smalltalk-V2KLVHUS.js"))
  },
  {
    "id": "smithy",
    "name": "Smithy",
    "import": (() => import("./smithy-KGND5GKF.js"))
  },
  {
    "id": "solidity",
    "name": "Solidity",
    "import": (() => import("./solidity-4SO5HEO7.js"))
  },
  {
    "id": "soy",
    "name": "Closure Templates",
    "aliases": ["closure-templates"],
    "import": (() => import("./soy-DXKBAP6R.js"))
  },
  {
    "id": "sparql",
    "name": "SPARQL",
    "import": (() => import("./sparql-M3QLAHEN.js"))
  },
  {
    "id": "splunk",
    "name": "Splunk Query Language",
    "aliases": ["spl"],
    "import": (() => import("./splunk-R5KKIKXS.js"))
  },
  {
    "id": "sql",
    "name": "SQL",
    "import": (() => import("./sql-J2QTNY3H.js"))
  },
  {
    "id": "ssh-config",
    "name": "SSH Config",
    "import": (() => import("./ssh-config-CLBDEEUD.js"))
  },
  {
    "id": "stata",
    "name": "Stata",
    "import": (() => import("./stata-ADGBGNWW.js"))
  },
  {
    "id": "stylus",
    "name": "Stylus",
    "aliases": ["styl"],
    "import": (() => import("./stylus-VZTGAWAS.js"))
  },
  {
    "id": "surrealql",
    "name": "SurrealQL",
    "aliases": ["surql"],
    "import": (() => import("./surrealql-EURQVQBG.js"))
  },
  {
    "id": "svelte",
    "name": "Svelte",
    "import": (() => import("./svelte-QUPBXSR7.js"))
  },
  {
    "id": "swift",
    "name": "Swift",
    "import": (() => import("./swift-TEIAMNTQ.js"))
  },
  {
    "id": "system-verilog",
    "name": "SystemVerilog",
    "import": (() => import("./system-verilog-UA4N47FB.js"))
  },
  {
    "id": "systemd",
    "name": "Systemd Units",
    "import": (() => import("./systemd-WLYYURMK.js"))
  },
  {
    "id": "talonscript",
    "name": "TalonScript",
    "aliases": ["talon"],
    "import": (() => import("./talonscript-I4OFSUKM.js"))
  },
  {
    "id": "tasl",
    "name": "Tasl",
    "import": (() => import("./tasl-QL3L5X4E.js"))
  },
  {
    "id": "tcl",
    "name": "Tcl",
    "import": (() => import("./tcl-ESMFIL7J.js"))
  },
  {
    "id": "templ",
    "name": "Templ",
    "import": (() => import("./templ-HESDSJZL.js"))
  },
  {
    "id": "terraform",
    "name": "Terraform",
    "aliases": ["tf", "tfvars"],
    "import": (() => import("./terraform-G46Z4GM7.js"))
  },
  {
    "id": "tex",
    "name": "TeX",
    "import": (() => import("./tex-O6YWYEZW.js"))
  },
  {
    "id": "toml",
    "name": "TOML",
    "import": (() => import("./toml-VZAKJMW7.js"))
  },
  {
    "id": "ts-tags",
    "name": "TypeScript with Tags",
    "aliases": ["lit"],
    "import": (() => import("./ts-tags-UYY42KBG.js"))
  },
  {
    "id": "tsv",
    "name": "TSV",
    "import": (() => import("./tsv-MSB4O6B4.js"))
  },
  {
    "id": "tsx",
    "name": "TSX",
    "import": (() => import("./tsx-5FW72OFI.js"))
  },
  {
    "id": "turtle",
    "name": "Turtle",
    "import": (() => import("./turtle-RS4QMVPI.js"))
  },
  {
    "id": "twig",
    "name": "Twig",
    "import": (() => import("./twig-RI27KLMQ.js"))
  },
  {
    "id": "typescript",
    "name": "TypeScript",
    "aliases": [
      "ts",
      "cts",
      "mts"
    ],
    "import": (() => import("./typescript-ENHKZY3G.js"))
  },
  {
    "id": "typespec",
    "name": "TypeSpec",
    "aliases": ["tsp"],
    "import": (() => import("./typespec-BKAV4K4P.js"))
  },
  {
    "id": "typst",
    "name": "Typst",
    "aliases": ["typ"],
    "import": (() => import("./typst-V6W6OIQW.js"))
  },
  {
    "id": "v",
    "name": "V",
    "import": (() => import("./v-RHJLTOWE.js"))
  },
  {
    "id": "vala",
    "name": "Vala",
    "import": (() => import("./vala-56PBHVC6.js"))
  },
  {
    "id": "vb",
    "name": "Visual Basic",
    "import": (() => import("./vb-7B4CH3HQ.js"))
  },
  {
    "id": "verilog",
    "name": "Verilog",
    "import": (() => import("./verilog-GDWE2A57.js"))
  },
  {
    "id": "vhdl",
    "name": "VHDL",
    "import": (() => import("./vhdl-YRJBXXPL.js"))
  },
  {
    "id": "viml",
    "name": "Vim Script",
    "aliases": ["vim", "vimscript"],
    "import": (() => import("./viml-R4SVOX7J.js"))
  },
  {
    "id": "vue",
    "name": "Vue",
    "import": (() => import("./vue-FXDALQAO.js"))
  },
  {
    "id": "vue-html",
    "name": "Vue HTML",
    "import": (() => import("./vue-html-5YFFWW4L.js"))
  },
  {
    "id": "vue-vine",
    "name": "Vue Vine",
    "import": (() => import("./vue-vine-5U5BA6WW.js"))
  },
  {
    "id": "vyper",
    "name": "Vyper",
    "aliases": ["vy"],
    "import": (() => import("./vyper-VSTONE44.js"))
  },
  {
    "id": "wasm",
    "name": "WebAssembly",
    "import": (() => import("./wasm-3Q2N3AQ7.js"))
  },
  {
    "id": "wenyan",
    "name": "Wenyan",
    "aliases": ["文言"],
    "import": (() => import("./wenyan-KFG52BFC.js"))
  },
  {
    "id": "wgsl",
    "name": "WGSL",
    "import": (() => import("./wgsl-YDZHZSSZ.js"))
  },
  {
    "id": "wikitext",
    "name": "Wikitext",
    "aliases": ["mediawiki", "wiki"],
    "import": (() => import("./wikitext-Q2IJQTAB.js"))
  },
  {
    "id": "wit",
    "name": "WebAssembly Interface Types",
    "import": (() => import("./wit-5VRVE52H.js"))
  },
  {
    "id": "wolfram",
    "name": "Wolfram",
    "aliases": ["wl"],
    "import": (() => import("./wolfram-V3XGIGEV.js"))
  },
  {
    "id": "xml",
    "name": "XML",
    "import": (() => import("./xml-XSG44XT4.js"))
  },
  {
    "id": "xsl",
    "name": "XSL",
    "import": (() => import("./xsl-3OKYXRYH.js"))
  },
  {
    "id": "yaml",
    "name": "YAML",
    "aliases": ["yml"],
    "import": (() => import("./yaml-QFY4KAZC.js"))
  },
  {
    "id": "zenscript",
    "name": "ZenScript",
    "import": (() => import("./zenscript-E22XRRB6.js"))
  },
  {
    "id": "zig",
    "name": "Zig",
    "import": (() => import("./zig-GJA4LXCL.js"))
  }
];
var bundledLanguagesBase = Object.fromEntries(bundledLanguagesInfo.map((i2) => [i2.id, i2.import]));
var bundledLanguagesAlias = Object.fromEntries(bundledLanguagesInfo.flatMap((i2) => {
  var _a9;
  return ((_a9 = i2.aliases) == null ? void 0 : _a9.map((a2) => [a2, i2.import])) || [];
}));
var bundledLanguages = {
  ...bundledLanguagesBase,
  ...bundledLanguagesAlias
};

// ../../node_modules/.pnpm/shiki@4.4.3/node_modules/shiki/dist/themes.mjs
var bundledThemesInfo = [
  {
    "id": "andromeeda",
    "displayName": "Andromeeda",
    "type": "dark",
    "import": (() => import("./andromeeda-AOXWUYCD.js"))
  },
  {
    "id": "aurora-x",
    "displayName": "Aurora X",
    "type": "dark",
    "import": (() => import("./aurora-x-Y5I222WX.js"))
  },
  {
    "id": "ayu-dark",
    "displayName": "Ayu Dark",
    "type": "dark",
    "import": (() => import("./ayu-dark-43FKJ2HV.js"))
  },
  {
    "id": "ayu-light",
    "displayName": "Ayu Light",
    "type": "light",
    "import": (() => import("./ayu-light-T6RHLFMG.js"))
  },
  {
    "id": "ayu-mirage",
    "displayName": "Ayu Mirage",
    "type": "dark",
    "import": (() => import("./ayu-mirage-CPLREM2F.js"))
  },
  {
    "id": "catppuccin-frappe",
    "displayName": "Catppuccin Frappé",
    "type": "dark",
    "import": (() => import("./catppuccin-frappe-SPWKBM4C.js"))
  },
  {
    "id": "catppuccin-latte",
    "displayName": "Catppuccin Latte",
    "type": "light",
    "import": (() => import("./catppuccin-latte-2ET5Q4IP.js"))
  },
  {
    "id": "catppuccin-macchiato",
    "displayName": "Catppuccin Macchiato",
    "type": "dark",
    "import": (() => import("./catppuccin-macchiato-56EE4FKY.js"))
  },
  {
    "id": "catppuccin-mocha",
    "displayName": "Catppuccin Mocha",
    "type": "dark",
    "import": (() => import("./catppuccin-mocha-WQTZO6LU.js"))
  },
  {
    "id": "dark-plus",
    "displayName": "Dark Plus",
    "type": "dark",
    "import": (() => import("./dark-plus-7KK7RBIA.js"))
  },
  {
    "id": "dracula",
    "displayName": "Dracula Theme",
    "type": "dark",
    "import": (() => import("./dracula-6V2P5RZO.js"))
  },
  {
    "id": "dracula-soft",
    "displayName": "Dracula Theme Soft",
    "type": "dark",
    "import": (() => import("./dracula-soft-2DACMPVJ.js"))
  },
  {
    "id": "everforest-dark",
    "displayName": "Everforest Dark",
    "type": "dark",
    "import": (() => import("./everforest-dark-PPUNNLOP.js"))
  },
  {
    "id": "everforest-light",
    "displayName": "Everforest Light",
    "type": "light",
    "import": (() => import("./everforest-light-G5TUMNNY.js"))
  },
  {
    "id": "github-dark",
    "displayName": "GitHub Dark",
    "type": "dark",
    "import": (() => import("./github-dark-E2PXRWSB.js"))
  },
  {
    "id": "github-dark-default",
    "displayName": "GitHub Dark Default",
    "type": "dark",
    "import": (() => import("./github-dark-default-JDD7CKOU.js"))
  },
  {
    "id": "github-dark-dimmed",
    "displayName": "GitHub Dark Dimmed",
    "type": "dark",
    "import": (() => import("./github-dark-dimmed-WSMMNXSC.js"))
  },
  {
    "id": "github-dark-high-contrast",
    "displayName": "GitHub Dark High Contrast",
    "type": "dark",
    "import": (() => import("./github-dark-high-contrast-GD6Z2VLO.js"))
  },
  {
    "id": "github-light",
    "displayName": "GitHub Light",
    "type": "light",
    "import": (() => import("./github-light-HDOCFNI6.js"))
  },
  {
    "id": "github-light-default",
    "displayName": "GitHub Light Default",
    "type": "light",
    "import": (() => import("./github-light-default-2YRQGMIP.js"))
  },
  {
    "id": "github-light-high-contrast",
    "displayName": "GitHub Light High Contrast",
    "type": "light",
    "import": (() => import("./github-light-high-contrast-UX2KBXGX.js"))
  },
  {
    "id": "gruvbox-dark-hard",
    "displayName": "Gruvbox Dark Hard",
    "type": "dark",
    "import": (() => import("./gruvbox-dark-hard-KBCDDNGN.js"))
  },
  {
    "id": "gruvbox-dark-medium",
    "displayName": "Gruvbox Dark Medium",
    "type": "dark",
    "import": (() => import("./gruvbox-dark-medium-5YADEZU2.js"))
  },
  {
    "id": "gruvbox-dark-soft",
    "displayName": "Gruvbox Dark Soft",
    "type": "dark",
    "import": (() => import("./gruvbox-dark-soft-LW33YJ2E.js"))
  },
  {
    "id": "gruvbox-light-hard",
    "displayName": "Gruvbox Light Hard",
    "type": "light",
    "import": (() => import("./gruvbox-light-hard-NIEWFKGM.js"))
  },
  {
    "id": "gruvbox-light-medium",
    "displayName": "Gruvbox Light Medium",
    "type": "light",
    "import": (() => import("./gruvbox-light-medium-B7NTGAJ5.js"))
  },
  {
    "id": "gruvbox-light-soft",
    "displayName": "Gruvbox Light Soft",
    "type": "light",
    "import": (() => import("./gruvbox-light-soft-PZGR73FS.js"))
  },
  {
    "id": "horizon",
    "displayName": "Horizon",
    "type": "dark",
    "import": (() => import("./horizon-3DQUVO4E.js"))
  },
  {
    "id": "horizon-bright",
    "displayName": "Horizon Bright",
    "type": "light",
    "import": (() => import("./horizon-bright-N5HYZIQE.js"))
  },
  {
    "id": "houston",
    "displayName": "Houston",
    "type": "dark",
    "import": (() => import("./houston-JIK55JAW.js"))
  },
  {
    "id": "kanagawa-dragon",
    "displayName": "Kanagawa Dragon",
    "type": "dark",
    "import": (() => import("./kanagawa-dragon-5IP7V5UD.js"))
  },
  {
    "id": "kanagawa-lotus",
    "displayName": "Kanagawa Lotus",
    "type": "light",
    "import": (() => import("./kanagawa-lotus-QOJZROII.js"))
  },
  {
    "id": "kanagawa-wave",
    "displayName": "Kanagawa Wave",
    "type": "dark",
    "import": (() => import("./kanagawa-wave-TWUTETAO.js"))
  },
  {
    "id": "laserwave",
    "displayName": "LaserWave",
    "type": "dark",
    "import": (() => import("./laserwave-NCJNREH4.js"))
  },
  {
    "id": "light-plus",
    "displayName": "Light Plus",
    "type": "light",
    "import": (() => import("./light-plus-CSORJ5XF.js"))
  },
  {
    "id": "material-theme",
    "displayName": "Material Theme",
    "type": "dark",
    "import": (() => import("./material-theme-7NAVULTZ.js"))
  },
  {
    "id": "material-theme-darker",
    "displayName": "Material Theme Darker",
    "type": "dark",
    "import": (() => import("./material-theme-darker-5WSXOBL2.js"))
  },
  {
    "id": "material-theme-lighter",
    "displayName": "Material Theme Lighter",
    "type": "light",
    "import": (() => import("./material-theme-lighter-HF4O3LCC.js"))
  },
  {
    "id": "material-theme-ocean",
    "displayName": "Material Theme Ocean",
    "type": "dark",
    "import": (() => import("./material-theme-ocean-QSVERJWA.js"))
  },
  {
    "id": "material-theme-palenight",
    "displayName": "Material Theme Palenight",
    "type": "dark",
    "import": (() => import("./material-theme-palenight-XEH2GOSH.js"))
  },
  {
    "id": "min-dark",
    "displayName": "Min Dark",
    "type": "dark",
    "import": (() => import("./min-dark-FYBZBATQ.js"))
  },
  {
    "id": "min-light",
    "displayName": "Min Light",
    "type": "light",
    "import": (() => import("./min-light-KNUMGRHM.js"))
  },
  {
    "id": "monokai",
    "displayName": "Monokai",
    "type": "dark",
    "import": (() => import("./monokai-PMXI5R32.js"))
  },
  {
    "id": "night-owl",
    "displayName": "Night Owl",
    "type": "dark",
    "import": (() => import("./night-owl-HR37S3NF.js"))
  },
  {
    "id": "night-owl-light",
    "displayName": "Night Owl Light",
    "type": "light",
    "import": (() => import("./night-owl-light-FDJ6OHOB.js"))
  },
  {
    "id": "nord",
    "displayName": "Nord",
    "type": "dark",
    "import": (() => import("./nord-AWABQVCW.js"))
  },
  {
    "id": "one-dark-pro",
    "displayName": "One Dark Pro",
    "type": "dark",
    "import": (() => import("./one-dark-pro-5DBQEU6O.js"))
  },
  {
    "id": "one-light",
    "displayName": "One Light",
    "type": "light",
    "import": (() => import("./one-light-XRA6B6TJ.js"))
  },
  {
    "id": "plastic",
    "displayName": "Plastic",
    "type": "dark",
    "import": (() => import("./plastic-UBQWKD7K.js"))
  },
  {
    "id": "poimandres",
    "displayName": "Poimandres",
    "type": "dark",
    "import": (() => import("./poimandres-A5PSBSBV.js"))
  },
  {
    "id": "red",
    "displayName": "Red",
    "type": "dark",
    "import": (() => import("./red-4OI7RKVI.js"))
  },
  {
    "id": "rose-pine",
    "displayName": "Rosé Pine",
    "type": "dark",
    "import": (() => import("./rose-pine-DCJTFLS6.js"))
  },
  {
    "id": "rose-pine-dawn",
    "displayName": "Rosé Pine Dawn",
    "type": "light",
    "import": (() => import("./rose-pine-dawn-QB3TMHUA.js"))
  },
  {
    "id": "rose-pine-moon",
    "displayName": "Rosé Pine Moon",
    "type": "dark",
    "import": (() => import("./rose-pine-moon-BWXLXPMV.js"))
  },
  {
    "id": "slack-dark",
    "displayName": "Slack Dark",
    "type": "dark",
    "import": (() => import("./slack-dark-4CCKONH2.js"))
  },
  {
    "id": "slack-ochin",
    "displayName": "Slack Ochin",
    "type": "light",
    "import": (() => import("./slack-ochin-36LSQJUW.js"))
  },
  {
    "id": "snazzy-light",
    "displayName": "Snazzy Light",
    "type": "light",
    "import": (() => import("./snazzy-light-IDSSK35S.js"))
  },
  {
    "id": "solarized-dark",
    "displayName": "Solarized Dark",
    "type": "dark",
    "import": (() => import("./solarized-dark-47IKOCXE.js"))
  },
  {
    "id": "solarized-light",
    "displayName": "Solarized Light",
    "type": "light",
    "import": (() => import("./solarized-light-AA4B3GJK.js"))
  },
  {
    "id": "synthwave-84",
    "displayName": "Synthwave '84",
    "type": "dark",
    "import": (() => import("./synthwave-84-ZOVHJQGK.js"))
  },
  {
    "id": "tokyo-night",
    "displayName": "Tokyo Night",
    "type": "dark",
    "import": (() => import("./tokyo-night-XGYRI2VO.js"))
  },
  {
    "id": "vesper",
    "displayName": "Vesper",
    "type": "dark",
    "import": (() => import("./vesper-7LSLTJUJ.js"))
  },
  {
    "id": "vitesse-black",
    "displayName": "Vitesse Black",
    "type": "dark",
    "import": (() => import("./vitesse-black-2HBQYBY2.js"))
  },
  {
    "id": "vitesse-dark",
    "displayName": "Vitesse Dark",
    "type": "dark",
    "import": (() => import("./vitesse-dark-S4HVBICM.js"))
  },
  {
    "id": "vitesse-light",
    "displayName": "Vitesse Light",
    "type": "light",
    "import": (() => import("./vitesse-light-N4DNXMJD.js"))
  }
];
var bundledThemes = Object.fromEntries(bundledThemesInfo.map((i2) => [i2.id, i2.import]));

// ../../node_modules/.pnpm/@shikijs+engine-oniguruma@4.4.3/node_modules/@shikijs/engine-oniguruma/dist/index.mjs
var dist_exports = {};
__export(dist_exports, {
  createOnigurumaEngine: () => createOnigurumaEngine,
  getDefaultWasmLoader: () => getDefaultWasmLoader,
  loadWasm: () => loadWasm,
  setDefaultWasmLoader: () => setDefaultWasmLoader
});
var ShikiError = class extends Error {
  constructor(message) {
    super(message);
    this.name = "ShikiError";
  }
};
function getHeapMax() {
  return 2147483648;
}
function _emscripten_get_now() {
  return typeof performance !== "undefined" ? performance.now() : Date.now();
}
var alignUp = (x3, multiple) => x3 + (multiple - x3 % multiple) % multiple;
async function main(init) {
  let wasmMemory;
  let buffer;
  const binding = {};
  function updateGlobalBufferAndViews(buf) {
    buffer = buf;
    binding.HEAPU8 = new Uint8Array(buf);
    binding.HEAPU32 = new Uint32Array(buf);
  }
  function _emscripten_memcpy_big(dest, src, num) {
    binding.HEAPU8.copyWithin(dest, src, src + num);
  }
  function emscripten_realloc_buffer(size) {
    try {
      wasmMemory.grow(size - buffer.byteLength + 65535 >>> 16);
      updateGlobalBufferAndViews(wasmMemory.buffer);
      return 1;
    } catch {
    }
  }
  function _emscripten_resize_heap(requestedSize) {
    const oldSize = binding.HEAPU8.length;
    requestedSize = requestedSize >>> 0;
    const maxHeapSize = getHeapMax();
    if (requestedSize > maxHeapSize)
      return false;
    for (let cutDown = 1; cutDown <= 4; cutDown *= 2) {
      let overGrownHeapSize = oldSize * (1 + 0.2 / cutDown);
      overGrownHeapSize = Math.min(overGrownHeapSize, requestedSize + 100663296);
      const newSize = Math.min(maxHeapSize, alignUp(Math.max(requestedSize, overGrownHeapSize), 65536));
      const replacement = emscripten_realloc_buffer(newSize);
      if (replacement)
        return true;
    }
    return false;
  }
  const UTF8Decoder = typeof TextDecoder != "undefined" ? new TextDecoder("utf8") : void 0;
  function UTF8ArrayToString(heapOrArray, idx, maxBytesToRead = 1024) {
    const endIdx = idx + maxBytesToRead;
    let endPtr = idx;
    while (heapOrArray[endPtr] && !(endPtr >= endIdx)) ++endPtr;
    if (endPtr - idx > 16 && heapOrArray.buffer && UTF8Decoder) {
      return UTF8Decoder.decode(heapOrArray.subarray(idx, endPtr));
    }
    let str = "";
    while (idx < endPtr) {
      let u0 = heapOrArray[idx++];
      if (!(u0 & 128)) {
        str += String.fromCharCode(u0);
        continue;
      }
      const u1 = heapOrArray[idx++] & 63;
      if ((u0 & 224) === 192) {
        str += String.fromCharCode((u0 & 31) << 6 | u1);
        continue;
      }
      const u2 = heapOrArray[idx++] & 63;
      if ((u0 & 240) === 224) {
        u0 = (u0 & 15) << 12 | u1 << 6 | u2;
      } else {
        u0 = (u0 & 7) << 18 | u1 << 12 | u2 << 6 | heapOrArray[idx++] & 63;
      }
      if (u0 < 65536) {
        str += String.fromCharCode(u0);
      } else {
        const ch = u0 - 65536;
        str += String.fromCharCode(55296 | ch >> 10, 56320 | ch & 1023);
      }
    }
    return str;
  }
  function UTF8ToString(ptr, maxBytesToRead) {
    return ptr ? UTF8ArrayToString(binding.HEAPU8, ptr, maxBytesToRead) : "";
  }
  const asmLibraryArg = {
    emscripten_get_now: _emscripten_get_now,
    emscripten_memcpy_big: _emscripten_memcpy_big,
    emscripten_resize_heap: _emscripten_resize_heap,
    fd_write: () => 0
  };
  async function createWasm() {
    const info = {
      env: asmLibraryArg,
      wasi_snapshot_preview1: asmLibraryArg
    };
    const exports = await init(info);
    wasmMemory = exports.memory;
    updateGlobalBufferAndViews(wasmMemory.buffer);
    Object.assign(binding, exports);
    binding.UTF8ToString = UTF8ToString;
  }
  await createWasm();
  return binding;
}
var __defProp2 = Object.defineProperty;
var __defNormalProp = (obj, key2, value) => key2 in obj ? __defProp2(obj, key2, { enumerable: true, configurable: true, writable: true, value }) : obj[key2] = value;
var __publicField2 = (obj, key2, value) => __defNormalProp(obj, typeof key2 !== "symbol" ? key2 + "" : key2, value);
var onigBinding = null;
function throwLastOnigError(onigBinding2) {
  throw new ShikiError(onigBinding2.UTF8ToString(onigBinding2.getLastOnigError()));
}
var UtfString = class _UtfString {
  constructor(str) {
    __publicField2(this, "utf16Length");
    __publicField2(this, "utf8Length");
    __publicField2(this, "utf16Value");
    __publicField2(this, "utf8Value");
    __publicField2(this, "utf16OffsetToUtf8");
    __publicField2(this, "utf8OffsetToUtf16");
    const utf16Length = str.length;
    const utf8Length = _UtfString._utf8ByteLength(str);
    const computeIndicesMapping = utf8Length !== utf16Length;
    const utf16OffsetToUtf8 = computeIndicesMapping ? new Uint32Array(utf16Length + 1) : null;
    if (computeIndicesMapping)
      utf16OffsetToUtf8[utf16Length] = utf8Length;
    const utf8OffsetToUtf16 = computeIndicesMapping ? new Uint32Array(utf8Length + 1) : null;
    if (computeIndicesMapping)
      utf8OffsetToUtf16[utf8Length] = utf16Length;
    const utf8Value = new Uint8Array(utf8Length);
    let i8 = 0;
    for (let i16 = 0; i16 < utf16Length; i16++) {
      const charCode = str.charCodeAt(i16);
      let codePoint = charCode;
      let wasSurrogatePair = false;
      if (charCode >= 55296 && charCode <= 56319) {
        if (i16 + 1 < utf16Length) {
          const nextCharCode = str.charCodeAt(i16 + 1);
          if (nextCharCode >= 56320 && nextCharCode <= 57343) {
            codePoint = (charCode - 55296 << 10) + 65536 | nextCharCode - 56320;
            wasSurrogatePair = true;
          }
        }
      }
      if (computeIndicesMapping) {
        utf16OffsetToUtf8[i16] = i8;
        if (wasSurrogatePair)
          utf16OffsetToUtf8[i16 + 1] = i8;
        if (codePoint <= 127) {
          utf8OffsetToUtf16[i8 + 0] = i16;
        } else if (codePoint <= 2047) {
          utf8OffsetToUtf16[i8 + 0] = i16;
          utf8OffsetToUtf16[i8 + 1] = i16;
        } else if (codePoint <= 65535) {
          utf8OffsetToUtf16[i8 + 0] = i16;
          utf8OffsetToUtf16[i8 + 1] = i16;
          utf8OffsetToUtf16[i8 + 2] = i16;
        } else {
          utf8OffsetToUtf16[i8 + 0] = i16;
          utf8OffsetToUtf16[i8 + 1] = i16;
          utf8OffsetToUtf16[i8 + 2] = i16;
          utf8OffsetToUtf16[i8 + 3] = i16;
        }
      }
      if (codePoint <= 127) {
        utf8Value[i8++] = codePoint;
      } else if (codePoint <= 2047) {
        utf8Value[i8++] = 192 | (codePoint & 1984) >>> 6;
        utf8Value[i8++] = 128 | (codePoint & 63) >>> 0;
      } else if (codePoint <= 65535) {
        utf8Value[i8++] = 224 | (codePoint & 61440) >>> 12;
        utf8Value[i8++] = 128 | (codePoint & 4032) >>> 6;
        utf8Value[i8++] = 128 | (codePoint & 63) >>> 0;
      } else {
        utf8Value[i8++] = 240 | (codePoint & 1835008) >>> 18;
        utf8Value[i8++] = 128 | (codePoint & 258048) >>> 12;
        utf8Value[i8++] = 128 | (codePoint & 4032) >>> 6;
        utf8Value[i8++] = 128 | (codePoint & 63) >>> 0;
      }
      if (wasSurrogatePair)
        i16++;
    }
    this.utf16Length = utf16Length;
    this.utf8Length = utf8Length;
    this.utf16Value = str;
    this.utf8Value = utf8Value;
    this.utf16OffsetToUtf8 = utf16OffsetToUtf8;
    this.utf8OffsetToUtf16 = utf8OffsetToUtf16;
  }
  static _utf8ByteLength(str) {
    let result = 0;
    for (let i2 = 0, len = str.length; i2 < len; i2++) {
      const charCode = str.charCodeAt(i2);
      let codepoint = charCode;
      let wasSurrogatePair = false;
      if (charCode >= 55296 && charCode <= 56319) {
        if (i2 + 1 < len) {
          const nextCharCode = str.charCodeAt(i2 + 1);
          if (nextCharCode >= 56320 && nextCharCode <= 57343) {
            codepoint = (charCode - 55296 << 10) + 65536 | nextCharCode - 56320;
            wasSurrogatePair = true;
          }
        }
      }
      if (codepoint <= 127)
        result += 1;
      else if (codepoint <= 2047)
        result += 2;
      else if (codepoint <= 65535)
        result += 3;
      else
        result += 4;
      if (wasSurrogatePair)
        i2++;
    }
    return result;
  }
  createString(onigBinding2) {
    const result = onigBinding2.omalloc(this.utf8Length);
    onigBinding2.HEAPU8.set(this.utf8Value, result);
    return result;
  }
};
var _OnigString = class _OnigString2 {
  constructor(str) {
    __publicField2(this, "id", ++_OnigString2.LAST_ID);
    __publicField2(this, "_onigBinding");
    __publicField2(this, "content");
    __publicField2(this, "utf16Length");
    __publicField2(this, "utf8Length");
    __publicField2(this, "utf16OffsetToUtf8");
    __publicField2(this, "utf8OffsetToUtf16");
    __publicField2(this, "ptr");
    if (!onigBinding)
      throw new ShikiError("Must invoke loadWasm first.");
    this._onigBinding = onigBinding;
    this.content = str;
    const utfString = new UtfString(str);
    this.utf16Length = utfString.utf16Length;
    this.utf8Length = utfString.utf8Length;
    this.utf16OffsetToUtf8 = utfString.utf16OffsetToUtf8;
    this.utf8OffsetToUtf16 = utfString.utf8OffsetToUtf16;
    if (this.utf8Length < 1e4 && !_OnigString2._sharedPtrInUse) {
      if (!_OnigString2._sharedPtr)
        _OnigString2._sharedPtr = onigBinding.omalloc(1e4);
      _OnigString2._sharedPtrInUse = true;
      onigBinding.HEAPU8.set(utfString.utf8Value, _OnigString2._sharedPtr);
      this.ptr = _OnigString2._sharedPtr;
    } else {
      this.ptr = utfString.createString(onigBinding);
    }
  }
  convertUtf8OffsetToUtf16(utf8Offset) {
    if (this.utf8OffsetToUtf16) {
      if (utf8Offset < 0)
        return 0;
      if (utf8Offset > this.utf8Length)
        return this.utf16Length;
      return this.utf8OffsetToUtf16[utf8Offset];
    }
    return utf8Offset;
  }
  convertUtf16OffsetToUtf8(utf16Offset) {
    if (this.utf16OffsetToUtf8) {
      if (utf16Offset < 0)
        return 0;
      if (utf16Offset > this.utf16Length)
        return this.utf8Length;
      return this.utf16OffsetToUtf8[utf16Offset];
    }
    return utf16Offset;
  }
  dispose() {
    if (this.ptr === _OnigString2._sharedPtr)
      _OnigString2._sharedPtrInUse = false;
    else
      this._onigBinding.ofree(this.ptr);
  }
};
__publicField2(_OnigString, "LAST_ID", 0);
__publicField2(_OnigString, "_sharedPtr", 0);
__publicField2(_OnigString, "_sharedPtrInUse", false);
var OnigString = _OnigString;
var OnigScanner = class {
  constructor(patterns) {
    __publicField2(this, "_onigBinding");
    __publicField2(this, "_ptr");
    if (!onigBinding)
      throw new ShikiError("Must invoke loadWasm first.");
    const strPtrsArr = [];
    const strLenArr = [];
    for (let i2 = 0, len = patterns.length; i2 < len; i2++) {
      const utfString = new UtfString(patterns[i2]);
      strPtrsArr[i2] = utfString.createString(onigBinding);
      strLenArr[i2] = utfString.utf8Length;
    }
    const strPtrsPtr = onigBinding.omalloc(4 * patterns.length);
    onigBinding.HEAPU32.set(strPtrsArr, strPtrsPtr / 4);
    const strLenPtr = onigBinding.omalloc(4 * patterns.length);
    onigBinding.HEAPU32.set(strLenArr, strLenPtr / 4);
    const scannerPtr = onigBinding.createOnigScanner(strPtrsPtr, strLenPtr, patterns.length);
    for (let i2 = 0, len = patterns.length; i2 < len; i2++)
      onigBinding.ofree(strPtrsArr[i2]);
    onigBinding.ofree(strLenPtr);
    onigBinding.ofree(strPtrsPtr);
    if (scannerPtr === 0)
      throwLastOnigError(onigBinding);
    this._onigBinding = onigBinding;
    this._ptr = scannerPtr;
  }
  dispose() {
    this._onigBinding.freeOnigScanner(this._ptr);
  }
  findNextMatchSync(string, startPosition, arg) {
    let options = 0;
    if (typeof arg === "number") {
      options = arg;
    }
    if (typeof string === "string") {
      string = new OnigString(string);
      const result = this._findNextMatchSync(string, startPosition, false, options);
      string.dispose();
      return result;
    }
    return this._findNextMatchSync(string, startPosition, false, options);
  }
  _findNextMatchSync(string, startPosition, debugCall, options) {
    const onigBinding2 = this._onigBinding;
    const resultPtr = onigBinding2.findNextOnigScannerMatch(this._ptr, string.id, string.ptr, string.utf8Length, string.convertUtf16OffsetToUtf8(startPosition), options);
    if (resultPtr === 0) {
      return null;
    }
    const HEAPU32 = onigBinding2.HEAPU32;
    let offset = resultPtr / 4;
    const index = HEAPU32[offset++];
    const count = HEAPU32[offset++];
    const captureIndices = [];
    for (let i2 = 0; i2 < count; i2++) {
      const beg = string.convertUtf8OffsetToUtf16(HEAPU32[offset++]);
      const end = string.convertUtf8OffsetToUtf16(HEAPU32[offset++]);
      captureIndices[i2] = {
        start: beg,
        end,
        length: end - beg
      };
    }
    return {
      index,
      captureIndices
    };
  }
};
function isInstantiatorOptionsObject(dataOrOptions) {
  return typeof dataOrOptions.instantiator === "function";
}
function isInstantiatorModule(dataOrOptions) {
  return typeof dataOrOptions.default === "function";
}
function isDataOptionsObject(dataOrOptions) {
  return typeof dataOrOptions.data !== "undefined";
}
function isResponse(dataOrOptions) {
  return typeof Response !== "undefined" && dataOrOptions instanceof Response;
}
function isArrayBuffer(data) {
  var _a9;
  return typeof ArrayBuffer !== "undefined" && (data instanceof ArrayBuffer || ArrayBuffer.isView(data)) || typeof Buffer !== "undefined" && ((_a9 = Buffer.isBuffer) == null ? void 0 : _a9.call(Buffer, data)) || typeof SharedArrayBuffer !== "undefined" && data instanceof SharedArrayBuffer || typeof Uint32Array !== "undefined" && data instanceof Uint32Array;
}
var initPromise;
function loadWasm(options) {
  if (initPromise)
    return initPromise;
  async function _load() {
    onigBinding = await main(async (info) => {
      let instance2 = options;
      instance2 = await instance2;
      if (typeof instance2 === "function")
        instance2 = await instance2(info);
      if (typeof instance2 === "function")
        instance2 = await instance2(info);
      if (isInstantiatorOptionsObject(instance2)) {
        instance2 = await instance2.instantiator(info);
      } else if (isInstantiatorModule(instance2)) {
        instance2 = await instance2.default(info);
      } else {
        if (isDataOptionsObject(instance2))
          instance2 = instance2.data;
        if (isResponse(instance2)) {
          if (typeof WebAssembly.instantiateStreaming === "function")
            instance2 = await _makeResponseStreamingLoader(instance2)(info);
          else
            instance2 = await _makeResponseNonStreamingLoader(instance2)(info);
        } else if (isArrayBuffer(instance2)) {
          instance2 = await _makeArrayBufferLoader(instance2)(info);
        } else if (instance2 instanceof WebAssembly.Module) {
          instance2 = await _makeArrayBufferLoader(instance2)(info);
        } else if ("default" in instance2 && instance2.default instanceof WebAssembly.Module) {
          instance2 = await _makeArrayBufferLoader(instance2.default)(info);
        }
      }
      if ("instance" in instance2)
        instance2 = instance2.instance;
      if ("exports" in instance2)
        instance2 = instance2.exports;
      return instance2;
    });
  }
  initPromise = _load();
  return initPromise;
}
function _makeArrayBufferLoader(data) {
  return (importObject) => WebAssembly.instantiate(data, importObject);
}
function _makeResponseStreamingLoader(data) {
  return (importObject) => WebAssembly.instantiateStreaming(data, importObject);
}
function _makeResponseNonStreamingLoader(data) {
  return async (importObject) => {
    const arrayBuffer = await data.arrayBuffer();
    return WebAssembly.instantiate(arrayBuffer, importObject);
  };
}
var _defaultWasmLoader;
function setDefaultWasmLoader(_loader) {
  _defaultWasmLoader = _loader;
}
function getDefaultWasmLoader() {
  return _defaultWasmLoader;
}
async function createOnigurumaEngine(options) {
  if (options)
    await loadWasm(options);
  return {
    createScanner(patterns) {
      return new OnigScanner(patterns.map((p2) => typeof p2 === "string" ? p2 : p2.source));
    },
    createString(s2) {
      return new OnigString(s2);
    }
  };
}

// ../../node_modules/.pnpm/shiki@4.4.3/node_modules/shiki/dist/engine-oniguruma.mjs
var engine_oniguruma_exports = __exportAll({});
__reExport(engine_oniguruma_exports, dist_exports);

// ../../node_modules/.pnpm/@shikijs+types@4.4.3/node_modules/@shikijs/types/dist/index.mjs
var ShikiError2 = class extends Error {
  constructor(message) {
    super(message);
    this.name = "ShikiError";
  }
};

// ../../node_modules/.pnpm/@shikijs+vscode-textmate@10.0.2/node_modules/@shikijs/vscode-textmate/dist/index.js
function clone(something) {
  return doClone(something);
}
function doClone(something) {
  if (Array.isArray(something)) {
    return cloneArray(something);
  }
  if (something instanceof RegExp) {
    return something;
  }
  if (typeof something === "object") {
    return cloneObj(something);
  }
  return something;
}
function cloneArray(arr) {
  let r4 = [];
  for (let i2 = 0, len = arr.length; i2 < len; i2++) {
    r4[i2] = doClone(arr[i2]);
  }
  return r4;
}
function cloneObj(obj) {
  let r4 = {};
  for (let key2 in obj) {
    r4[key2] = doClone(obj[key2]);
  }
  return r4;
}
function mergeObjects(target, ...sources) {
  sources.forEach((source) => {
    for (let key2 in source) {
      target[key2] = source[key2];
    }
  });
  return target;
}
function basename(path) {
  const idx = ~path.lastIndexOf("/") || ~path.lastIndexOf("\\");
  if (idx === 0) {
    return path;
  } else if (~idx === path.length - 1) {
    return basename(path.substring(0, path.length - 1));
  } else {
    return path.substr(~idx + 1);
  }
}
var CAPTURING_REGEX_SOURCE = /\$(\d+)|\${(\d+):\/(downcase|upcase)}/g;
var RegexSource = class {
  static hasCaptures(regexSource) {
    if (regexSource === null) {
      return false;
    }
    CAPTURING_REGEX_SOURCE.lastIndex = 0;
    return CAPTURING_REGEX_SOURCE.test(regexSource);
  }
  static replaceCaptures(regexSource, captureSource, captureIndices) {
    return regexSource.replace(CAPTURING_REGEX_SOURCE, (match, index, commandIndex, command) => {
      let capture = captureIndices[parseInt(index || commandIndex, 10)];
      if (capture) {
        let result = captureSource.substring(capture.start, capture.end);
        while (result[0] === ".") {
          result = result.substring(1);
        }
        switch (command) {
          case "downcase":
            return result.toLowerCase();
          case "upcase":
            return result.toUpperCase();
          default:
            return result;
        }
      } else {
        return match;
      }
    });
  }
};
function strcmp(a2, b3) {
  if (a2 < b3) {
    return -1;
  }
  if (a2 > b3) {
    return 1;
  }
  return 0;
}
function strArrCmp(a2, b3) {
  if (a2 === null && b3 === null) {
    return 0;
  }
  if (!a2) {
    return -1;
  }
  if (!b3) {
    return 1;
  }
  let len1 = a2.length;
  let len2 = b3.length;
  if (len1 === len2) {
    for (let i2 = 0; i2 < len1; i2++) {
      let res = strcmp(a2[i2], b3[i2]);
      if (res !== 0) {
        return res;
      }
    }
    return 0;
  }
  return len1 - len2;
}
function isValidHexColor(hex) {
  if (/^#[0-9a-f]{6}$/i.test(hex)) {
    return true;
  }
  if (/^#[0-9a-f]{8}$/i.test(hex)) {
    return true;
  }
  if (/^#[0-9a-f]{3}$/i.test(hex)) {
    return true;
  }
  if (/^#[0-9a-f]{4}$/i.test(hex)) {
    return true;
  }
  return false;
}
function escapeRegExpCharacters(value) {
  return value.replace(/[\-\\\{\}\*\+\?\|\^\$\.\,\[\]\(\)\#\s]/g, "\\$&");
}
var CachedFn = class {
  constructor(fn) {
    __publicField(this, "cache", /* @__PURE__ */ new Map());
    this.fn = fn;
  }
  get(key2) {
    if (this.cache.has(key2)) {
      return this.cache.get(key2);
    }
    const value = this.fn(key2);
    this.cache.set(key2, value);
    return value;
  }
};
var Theme = class {
  constructor(_colorMap, _defaults, _root) {
    __publicField(this, "_cachedMatchRoot", new CachedFn(
      (scopeName) => this._root.match(scopeName)
    ));
    this._colorMap = _colorMap;
    this._defaults = _defaults;
    this._root = _root;
  }
  static createFromRawTheme(source, colorMap) {
    return this.createFromParsedTheme(parseTheme(source), colorMap);
  }
  static createFromParsedTheme(source, colorMap) {
    return resolveParsedThemeRules(source, colorMap);
  }
  getColorMap() {
    return this._colorMap.getColorMap();
  }
  getDefaults() {
    return this._defaults;
  }
  match(scopePath) {
    if (scopePath === null) {
      return this._defaults;
    }
    const scopeName = scopePath.scopeName;
    const matchingTrieElements = this._cachedMatchRoot.get(scopeName);
    const effectiveRule = matchingTrieElements.find(
      (v2) => _scopePathMatchesParentScopes(scopePath.parent, v2.parentScopes)
    );
    if (!effectiveRule) {
      return null;
    }
    return new StyleAttributes(
      effectiveRule.fontStyle,
      effectiveRule.foreground,
      effectiveRule.background
    );
  }
};
var ScopeStack = class _ScopeStack {
  constructor(parent, scopeName) {
    this.parent = parent;
    this.scopeName = scopeName;
  }
  static push(path, scopeNames) {
    for (const name of scopeNames) {
      path = new _ScopeStack(path, name);
    }
    return path;
  }
  static from(...segments) {
    let result = null;
    for (let i2 = 0; i2 < segments.length; i2++) {
      result = new _ScopeStack(result, segments[i2]);
    }
    return result;
  }
  push(scopeName) {
    return new _ScopeStack(this, scopeName);
  }
  getSegments() {
    let item = this;
    const result = [];
    while (item) {
      result.push(item.scopeName);
      item = item.parent;
    }
    result.reverse();
    return result;
  }
  toString() {
    return this.getSegments().join(" ");
  }
  extends(other) {
    if (this === other) {
      return true;
    }
    if (this.parent === null) {
      return false;
    }
    return this.parent.extends(other);
  }
  getExtensionIfDefined(base) {
    const result = [];
    let item = this;
    while (item && item !== base) {
      result.push(item.scopeName);
      item = item.parent;
    }
    return item === base ? result.reverse() : void 0;
  }
};
function _scopePathMatchesParentScopes(scopePath, parentScopes) {
  if (parentScopes.length === 0) {
    return true;
  }
  for (let index = 0; index < parentScopes.length; index++) {
    let scopePattern = parentScopes[index];
    let scopeMustMatch = false;
    if (scopePattern === ">") {
      if (index === parentScopes.length - 1) {
        return false;
      }
      scopePattern = parentScopes[++index];
      scopeMustMatch = true;
    }
    while (scopePath) {
      if (_matchesScope(scopePath.scopeName, scopePattern)) {
        break;
      }
      if (scopeMustMatch) {
        return false;
      }
      scopePath = scopePath.parent;
    }
    if (!scopePath) {
      return false;
    }
    scopePath = scopePath.parent;
  }
  return true;
}
function _matchesScope(scopeName, scopePattern) {
  return scopePattern === scopeName || scopeName.startsWith(scopePattern) && scopeName[scopePattern.length] === ".";
}
var StyleAttributes = class {
  constructor(fontStyle, foregroundId, backgroundId) {
    this.fontStyle = fontStyle;
    this.foregroundId = foregroundId;
    this.backgroundId = backgroundId;
  }
};
function parseTheme(source) {
  if (!source) {
    return [];
  }
  if (!source.settings || !Array.isArray(source.settings)) {
    return [];
  }
  let settings = source.settings;
  let result = [], resultLen = 0;
  for (let i2 = 0, len = settings.length; i2 < len; i2++) {
    let entry = settings[i2];
    if (!entry.settings) {
      continue;
    }
    let scopes;
    if (typeof entry.scope === "string") {
      let _scope = entry.scope;
      _scope = _scope.replace(/^[,]+/, "");
      _scope = _scope.replace(/[,]+$/, "");
      scopes = _scope.split(",");
    } else if (Array.isArray(entry.scope)) {
      scopes = entry.scope;
    } else {
      scopes = [""];
    }
    let fontStyle = -1;
    if (typeof entry.settings.fontStyle === "string") {
      fontStyle = 0;
      let segments = entry.settings.fontStyle.split(" ");
      for (let j2 = 0, lenJ = segments.length; j2 < lenJ; j2++) {
        let segment2 = segments[j2];
        switch (segment2) {
          case "italic":
            fontStyle = fontStyle | 1;
            break;
          case "bold":
            fontStyle = fontStyle | 2;
            break;
          case "underline":
            fontStyle = fontStyle | 4;
            break;
          case "strikethrough":
            fontStyle = fontStyle | 8;
            break;
        }
      }
    }
    let foreground = null;
    if (typeof entry.settings.foreground === "string" && isValidHexColor(entry.settings.foreground)) {
      foreground = entry.settings.foreground;
    }
    let background = null;
    if (typeof entry.settings.background === "string" && isValidHexColor(entry.settings.background)) {
      background = entry.settings.background;
    }
    for (let j2 = 0, lenJ = scopes.length; j2 < lenJ; j2++) {
      let _scope = scopes[j2].trim();
      let segments = _scope.split(" ");
      let scope = segments[segments.length - 1];
      let parentScopes = null;
      if (segments.length > 1) {
        parentScopes = segments.slice(0, segments.length - 1);
        parentScopes.reverse();
      }
      result[resultLen++] = new ParsedThemeRule(
        scope,
        parentScopes,
        i2,
        fontStyle,
        foreground,
        background
      );
    }
  }
  return result;
}
var ParsedThemeRule = class {
  constructor(scope, parentScopes, index, fontStyle, foreground, background) {
    this.scope = scope;
    this.parentScopes = parentScopes;
    this.index = index;
    this.fontStyle = fontStyle;
    this.foreground = foreground;
    this.background = background;
  }
};
var FontStyle = ((FontStyle2) => {
  FontStyle2[FontStyle2["NotSet"] = -1] = "NotSet";
  FontStyle2[FontStyle2["None"] = 0] = "None";
  FontStyle2[FontStyle2["Italic"] = 1] = "Italic";
  FontStyle2[FontStyle2["Bold"] = 2] = "Bold";
  FontStyle2[FontStyle2["Underline"] = 4] = "Underline";
  FontStyle2[FontStyle2["Strikethrough"] = 8] = "Strikethrough";
  return FontStyle2;
})(FontStyle || {});
function resolveParsedThemeRules(parsedThemeRules, _colorMap) {
  parsedThemeRules.sort((a2, b3) => {
    let r4 = strcmp(a2.scope, b3.scope);
    if (r4 !== 0) {
      return r4;
    }
    r4 = strArrCmp(a2.parentScopes, b3.parentScopes);
    if (r4 !== 0) {
      return r4;
    }
    return a2.index - b3.index;
  });
  let defaultFontStyle = 0;
  let defaultForeground = "#000000";
  let defaultBackground = "#ffffff";
  while (parsedThemeRules.length >= 1 && parsedThemeRules[0].scope === "") {
    let incomingDefaults = parsedThemeRules.shift();
    if (incomingDefaults.fontStyle !== -1) {
      defaultFontStyle = incomingDefaults.fontStyle;
    }
    if (incomingDefaults.foreground !== null) {
      defaultForeground = incomingDefaults.foreground;
    }
    if (incomingDefaults.background !== null) {
      defaultBackground = incomingDefaults.background;
    }
  }
  let colorMap = new ColorMap(_colorMap);
  let defaults = new StyleAttributes(defaultFontStyle, colorMap.getId(defaultForeground), colorMap.getId(defaultBackground));
  let root2 = new ThemeTrieElement(new ThemeTrieElementRule(0, null, -1, 0, 0), []);
  for (let i2 = 0, len = parsedThemeRules.length; i2 < len; i2++) {
    let rule = parsedThemeRules[i2];
    root2.insert(0, rule.scope, rule.parentScopes, rule.fontStyle, colorMap.getId(rule.foreground), colorMap.getId(rule.background));
  }
  return new Theme(colorMap, defaults, root2);
}
var ColorMap = class {
  constructor(_colorMap) {
    __publicField(this, "_isFrozen");
    __publicField(this, "_lastColorId");
    __publicField(this, "_id2color");
    __publicField(this, "_color2id");
    this._lastColorId = 0;
    this._id2color = [];
    this._color2id = /* @__PURE__ */ Object.create(null);
    if (Array.isArray(_colorMap)) {
      this._isFrozen = true;
      for (let i2 = 0, len = _colorMap.length; i2 < len; i2++) {
        this._color2id[_colorMap[i2]] = i2;
        this._id2color[i2] = _colorMap[i2];
      }
    } else {
      this._isFrozen = false;
    }
  }
  getId(color) {
    if (color === null) {
      return 0;
    }
    color = color.toUpperCase();
    let value = this._color2id[color];
    if (value) {
      return value;
    }
    if (this._isFrozen) {
      throw new Error(`Missing color in color map - ${color}`);
    }
    value = ++this._lastColorId;
    this._color2id[color] = value;
    this._id2color[value] = color;
    return value;
  }
  getColorMap() {
    return this._id2color.slice(0);
  }
};
var emptyParentScopes = Object.freeze([]);
var ThemeTrieElementRule = class _ThemeTrieElementRule {
  constructor(scopeDepth, parentScopes, fontStyle, foreground, background) {
    __publicField(this, "scopeDepth");
    __publicField(this, "parentScopes");
    __publicField(this, "fontStyle");
    __publicField(this, "foreground");
    __publicField(this, "background");
    this.scopeDepth = scopeDepth;
    this.parentScopes = parentScopes || emptyParentScopes;
    this.fontStyle = fontStyle;
    this.foreground = foreground;
    this.background = background;
  }
  clone() {
    return new _ThemeTrieElementRule(this.scopeDepth, this.parentScopes, this.fontStyle, this.foreground, this.background);
  }
  static cloneArr(arr) {
    let r4 = [];
    for (let i2 = 0, len = arr.length; i2 < len; i2++) {
      r4[i2] = arr[i2].clone();
    }
    return r4;
  }
  acceptOverwrite(scopeDepth, fontStyle, foreground, background) {
    if (this.scopeDepth > scopeDepth) {
      console.log("how did this happen?");
    } else {
      this.scopeDepth = scopeDepth;
    }
    if (fontStyle !== -1) {
      this.fontStyle = fontStyle;
    }
    if (foreground !== 0) {
      this.foreground = foreground;
    }
    if (background !== 0) {
      this.background = background;
    }
  }
};
var ThemeTrieElement = class _ThemeTrieElement {
  constructor(_mainRule, rulesWithParentScopes = [], _children = {}) {
    __publicField(this, "_rulesWithParentScopes");
    this._mainRule = _mainRule;
    this._children = _children;
    this._rulesWithParentScopes = rulesWithParentScopes;
  }
  static _cmpBySpecificity(a2, b3) {
    if (a2.scopeDepth !== b3.scopeDepth) {
      return b3.scopeDepth - a2.scopeDepth;
    }
    let aParentIndex = 0;
    let bParentIndex = 0;
    while (true) {
      if (a2.parentScopes[aParentIndex] === ">") {
        aParentIndex++;
      }
      if (b3.parentScopes[bParentIndex] === ">") {
        bParentIndex++;
      }
      if (aParentIndex >= a2.parentScopes.length || bParentIndex >= b3.parentScopes.length) {
        break;
      }
      const parentScopeLengthDiff = b3.parentScopes[bParentIndex].length - a2.parentScopes[aParentIndex].length;
      if (parentScopeLengthDiff !== 0) {
        return parentScopeLengthDiff;
      }
      aParentIndex++;
      bParentIndex++;
    }
    return b3.parentScopes.length - a2.parentScopes.length;
  }
  match(scope) {
    if (scope !== "") {
      let dotIndex = scope.indexOf(".");
      let head2;
      let tail;
      if (dotIndex === -1) {
        head2 = scope;
        tail = "";
      } else {
        head2 = scope.substring(0, dotIndex);
        tail = scope.substring(dotIndex + 1);
      }
      if (this._children.hasOwnProperty(head2)) {
        return this._children[head2].match(tail);
      }
    }
    const rules = this._rulesWithParentScopes.concat(this._mainRule);
    rules.sort(_ThemeTrieElement._cmpBySpecificity);
    return rules;
  }
  insert(scopeDepth, scope, parentScopes, fontStyle, foreground, background) {
    if (scope === "") {
      this._doInsertHere(scopeDepth, parentScopes, fontStyle, foreground, background);
      return;
    }
    let dotIndex = scope.indexOf(".");
    let head2;
    let tail;
    if (dotIndex === -1) {
      head2 = scope;
      tail = "";
    } else {
      head2 = scope.substring(0, dotIndex);
      tail = scope.substring(dotIndex + 1);
    }
    let child;
    if (this._children.hasOwnProperty(head2)) {
      child = this._children[head2];
    } else {
      child = new _ThemeTrieElement(this._mainRule.clone(), ThemeTrieElementRule.cloneArr(this._rulesWithParentScopes));
      this._children[head2] = child;
    }
    child.insert(scopeDepth + 1, tail, parentScopes, fontStyle, foreground, background);
  }
  _doInsertHere(scopeDepth, parentScopes, fontStyle, foreground, background) {
    if (parentScopes === null) {
      this._mainRule.acceptOverwrite(scopeDepth, fontStyle, foreground, background);
      return;
    }
    for (let i2 = 0, len = this._rulesWithParentScopes.length; i2 < len; i2++) {
      let rule = this._rulesWithParentScopes[i2];
      if (strArrCmp(rule.parentScopes, parentScopes) === 0) {
        rule.acceptOverwrite(scopeDepth, fontStyle, foreground, background);
        return;
      }
    }
    if (fontStyle === -1) {
      fontStyle = this._mainRule.fontStyle;
    }
    if (foreground === 0) {
      foreground = this._mainRule.foreground;
    }
    if (background === 0) {
      background = this._mainRule.background;
    }
    this._rulesWithParentScopes.push(new ThemeTrieElementRule(scopeDepth, parentScopes, fontStyle, foreground, background));
  }
};
var EncodedTokenMetadata = class _EncodedTokenMetadata {
  static toBinaryStr(encodedTokenAttributes) {
    return encodedTokenAttributes.toString(2).padStart(32, "0");
  }
  static print(encodedTokenAttributes) {
    const languageId = _EncodedTokenMetadata.getLanguageId(encodedTokenAttributes);
    const tokenType = _EncodedTokenMetadata.getTokenType(encodedTokenAttributes);
    const fontStyle = _EncodedTokenMetadata.getFontStyle(encodedTokenAttributes);
    const foreground = _EncodedTokenMetadata.getForeground(encodedTokenAttributes);
    const background = _EncodedTokenMetadata.getBackground(encodedTokenAttributes);
    console.log({
      languageId,
      tokenType,
      fontStyle,
      foreground,
      background
    });
  }
  static getLanguageId(encodedTokenAttributes) {
    return (encodedTokenAttributes & 255) >>> 0;
  }
  static getTokenType(encodedTokenAttributes) {
    return (encodedTokenAttributes & 768) >>> 8;
  }
  static containsBalancedBrackets(encodedTokenAttributes) {
    return (encodedTokenAttributes & 1024) !== 0;
  }
  static getFontStyle(encodedTokenAttributes) {
    return (encodedTokenAttributes & 30720) >>> 11;
  }
  static getForeground(encodedTokenAttributes) {
    return (encodedTokenAttributes & 16744448) >>> 15;
  }
  static getBackground(encodedTokenAttributes) {
    return (encodedTokenAttributes & 4278190080) >>> 24;
  }
  /**
   * Updates the fields in `metadata`.
   * A value of `0`, `NotSet` or `null` indicates that the corresponding field should be left as is.
   */
  static set(encodedTokenAttributes, languageId, tokenType, containsBalancedBrackets, fontStyle, foreground, background) {
    let _languageId = _EncodedTokenMetadata.getLanguageId(encodedTokenAttributes);
    let _tokenType = _EncodedTokenMetadata.getTokenType(encodedTokenAttributes);
    let _containsBalancedBracketsBit = _EncodedTokenMetadata.containsBalancedBrackets(encodedTokenAttributes) ? 1 : 0;
    let _fontStyle = _EncodedTokenMetadata.getFontStyle(encodedTokenAttributes);
    let _foreground = _EncodedTokenMetadata.getForeground(encodedTokenAttributes);
    let _background = _EncodedTokenMetadata.getBackground(encodedTokenAttributes);
    if (languageId !== 0) {
      _languageId = languageId;
    }
    if (tokenType !== 8) {
      _tokenType = fromOptionalTokenType(tokenType);
    }
    if (containsBalancedBrackets !== null) {
      _containsBalancedBracketsBit = containsBalancedBrackets ? 1 : 0;
    }
    if (fontStyle !== -1) {
      _fontStyle = fontStyle;
    }
    if (foreground !== 0) {
      _foreground = foreground;
    }
    if (background !== 0) {
      _background = background;
    }
    return (_languageId << 0 | _tokenType << 8 | _containsBalancedBracketsBit << 10 | _fontStyle << 11 | _foreground << 15 | _background << 24) >>> 0;
  }
};
function toOptionalTokenType(standardType) {
  return standardType;
}
function fromOptionalTokenType(standardType) {
  return standardType;
}
function createMatchers(selector, matchesName) {
  const results = [];
  const tokenizer = newTokenizer(selector);
  let token2 = tokenizer.next();
  while (token2 !== null) {
    let priority = 0;
    if (token2.length === 2 && token2.charAt(1) === ":") {
      switch (token2.charAt(0)) {
        case "R":
          priority = 1;
          break;
        case "L":
          priority = -1;
          break;
        default:
          console.log(`Unknown priority ${token2} in scope selector`);
      }
      token2 = tokenizer.next();
    }
    let matcher = parseConjunction();
    results.push({ matcher, priority });
    if (token2 !== ",") {
      break;
    }
    token2 = tokenizer.next();
  }
  return results;
  function parseOperand() {
    if (token2 === "-") {
      token2 = tokenizer.next();
      const expressionToNegate = parseOperand();
      return (matcherInput) => !!expressionToNegate && !expressionToNegate(matcherInput);
    }
    if (token2 === "(") {
      token2 = tokenizer.next();
      const expressionInParents = parseInnerExpression();
      if (token2 === ")") {
        token2 = tokenizer.next();
      }
      return expressionInParents;
    }
    if (isIdentifier(token2)) {
      const identifiers = [];
      do {
        identifiers.push(token2);
        token2 = tokenizer.next();
      } while (isIdentifier(token2));
      return (matcherInput) => matchesName(identifiers, matcherInput);
    }
    return null;
  }
  function parseConjunction() {
    const matchers = [];
    let matcher = parseOperand();
    while (matcher) {
      matchers.push(matcher);
      matcher = parseOperand();
    }
    return (matcherInput) => matchers.every((matcher2) => matcher2(matcherInput));
  }
  function parseInnerExpression() {
    const matchers = [];
    let matcher = parseConjunction();
    while (matcher) {
      matchers.push(matcher);
      if (token2 === "|" || token2 === ",") {
        do {
          token2 = tokenizer.next();
        } while (token2 === "|" || token2 === ",");
      } else {
        break;
      }
      matcher = parseConjunction();
    }
    return (matcherInput) => matchers.some((matcher2) => matcher2(matcherInput));
  }
}
function isIdentifier(token2) {
  return !!token2 && !!token2.match(/[\w\.:]+/);
}
function newTokenizer(input) {
  let regex = /([LR]:|[\w\.:][\w\.:\-]*|[\,\|\-\(\)])/g;
  let match = regex.exec(input);
  return {
    next: () => {
      if (!match) {
        return null;
      }
      const res = match[0];
      match = regex.exec(input);
      return res;
    }
  };
}
var FindOption = ((FindOption2) => {
  FindOption2[FindOption2["None"] = 0] = "None";
  FindOption2[FindOption2["NotBeginString"] = 1] = "NotBeginString";
  FindOption2[FindOption2["NotEndString"] = 2] = "NotEndString";
  FindOption2[FindOption2["NotBeginPosition"] = 4] = "NotBeginPosition";
  FindOption2[FindOption2["DebugCall"] = 8] = "DebugCall";
  return FindOption2;
})(FindOption || {});
function disposeOnigString(str) {
  if (typeof str.dispose === "function") {
    str.dispose();
  }
}
var TopLevelRuleReference = class {
  constructor(scopeName) {
    this.scopeName = scopeName;
  }
  toKey() {
    return this.scopeName;
  }
};
var TopLevelRepositoryRuleReference = class {
  constructor(scopeName, ruleName) {
    this.scopeName = scopeName;
    this.ruleName = ruleName;
  }
  toKey() {
    return `${this.scopeName}#${this.ruleName}`;
  }
};
var ExternalReferenceCollector = class {
  constructor() {
    __publicField(this, "_references", []);
    __publicField(this, "_seenReferenceKeys", /* @__PURE__ */ new Set());
    __publicField(this, "visitedRule", /* @__PURE__ */ new Set());
  }
  get references() {
    return this._references;
  }
  add(reference) {
    const key2 = reference.toKey();
    if (this._seenReferenceKeys.has(key2)) {
      return;
    }
    this._seenReferenceKeys.add(key2);
    this._references.push(reference);
  }
};
var ScopeDependencyProcessor = class {
  constructor(repo, initialScopeName) {
    __publicField(this, "seenFullScopeRequests", /* @__PURE__ */ new Set());
    __publicField(this, "seenPartialScopeRequests", /* @__PURE__ */ new Set());
    __publicField(this, "Q");
    this.repo = repo;
    this.initialScopeName = initialScopeName;
    this.seenFullScopeRequests.add(this.initialScopeName);
    this.Q = [new TopLevelRuleReference(this.initialScopeName)];
  }
  processQueue() {
    const q2 = this.Q;
    this.Q = [];
    const deps = new ExternalReferenceCollector();
    for (const dep of q2) {
      collectReferencesOfReference(dep, this.initialScopeName, this.repo, deps);
    }
    for (const dep of deps.references) {
      if (dep instanceof TopLevelRuleReference) {
        if (this.seenFullScopeRequests.has(dep.scopeName)) {
          continue;
        }
        this.seenFullScopeRequests.add(dep.scopeName);
        this.Q.push(dep);
      } else {
        if (this.seenFullScopeRequests.has(dep.scopeName)) {
          continue;
        }
        if (this.seenPartialScopeRequests.has(dep.toKey())) {
          continue;
        }
        this.seenPartialScopeRequests.add(dep.toKey());
        this.Q.push(dep);
      }
    }
  }
};
function collectReferencesOfReference(reference, baseGrammarScopeName, repo, result) {
  const selfGrammar = repo.lookup(reference.scopeName);
  if (!selfGrammar) {
    if (reference.scopeName === baseGrammarScopeName) {
      throw new Error(`No grammar provided for <${baseGrammarScopeName}>`);
    }
    return;
  }
  const baseGrammar = repo.lookup(baseGrammarScopeName);
  if (reference instanceof TopLevelRuleReference) {
    collectExternalReferencesInTopLevelRule({ baseGrammar, selfGrammar }, result);
  } else {
    collectExternalReferencesInTopLevelRepositoryRule(
      reference.ruleName,
      { baseGrammar, selfGrammar, repository: selfGrammar.repository },
      result
    );
  }
  const injections = repo.injections(reference.scopeName);
  if (injections) {
    for (const injection of injections) {
      result.add(new TopLevelRuleReference(injection));
    }
  }
}
function collectExternalReferencesInTopLevelRepositoryRule(ruleName, context, result) {
  if (context.repository && context.repository[ruleName]) {
    const rule = context.repository[ruleName];
    collectExternalReferencesInRules([rule], context, result);
  }
}
function collectExternalReferencesInTopLevelRule(context, result) {
  if (context.selfGrammar.patterns && Array.isArray(context.selfGrammar.patterns)) {
    collectExternalReferencesInRules(
      context.selfGrammar.patterns,
      { ...context, repository: context.selfGrammar.repository },
      result
    );
  }
  if (context.selfGrammar.injections) {
    collectExternalReferencesInRules(
      Object.values(context.selfGrammar.injections),
      { ...context, repository: context.selfGrammar.repository },
      result
    );
  }
}
function collectExternalReferencesInRules(rules, context, result) {
  for (const rule of rules) {
    if (result.visitedRule.has(rule)) {
      continue;
    }
    result.visitedRule.add(rule);
    const patternRepository = rule.repository ? mergeObjects({}, context.repository, rule.repository) : context.repository;
    if (Array.isArray(rule.patterns)) {
      collectExternalReferencesInRules(rule.patterns, { ...context, repository: patternRepository }, result);
    }
    const include = rule.include;
    if (!include) {
      continue;
    }
    const reference = parseInclude(include);
    switch (reference.kind) {
      case 0:
        collectExternalReferencesInTopLevelRule({ ...context, selfGrammar: context.baseGrammar }, result);
        break;
      case 1:
        collectExternalReferencesInTopLevelRule(context, result);
        break;
      case 2:
        collectExternalReferencesInTopLevelRepositoryRule(reference.ruleName, { ...context, repository: patternRepository }, result);
        break;
      case 3:
      case 4:
        const selfGrammar = reference.scopeName === context.selfGrammar.scopeName ? context.selfGrammar : reference.scopeName === context.baseGrammar.scopeName ? context.baseGrammar : void 0;
        if (selfGrammar) {
          const newContext = { baseGrammar: context.baseGrammar, selfGrammar, repository: patternRepository };
          if (reference.kind === 4) {
            collectExternalReferencesInTopLevelRepositoryRule(reference.ruleName, newContext, result);
          } else {
            collectExternalReferencesInTopLevelRule(newContext, result);
          }
        } else {
          if (reference.kind === 4) {
            result.add(new TopLevelRepositoryRuleReference(reference.scopeName, reference.ruleName));
          } else {
            result.add(new TopLevelRuleReference(reference.scopeName));
          }
        }
        break;
    }
  }
}
var BaseReference = class {
  constructor() {
    __publicField(this, "kind", 0);
  }
};
var SelfReference = class {
  constructor() {
    __publicField(this, "kind", 1);
  }
};
var RelativeReference = class {
  constructor(ruleName) {
    __publicField(this, "kind", 2);
    this.ruleName = ruleName;
  }
};
var TopLevelReference = class {
  constructor(scopeName) {
    __publicField(this, "kind", 3);
    this.scopeName = scopeName;
  }
};
var TopLevelRepositoryReference = class {
  constructor(scopeName, ruleName) {
    __publicField(this, "kind", 4);
    this.scopeName = scopeName;
    this.ruleName = ruleName;
  }
};
function parseInclude(include) {
  if (include === "$base") {
    return new BaseReference();
  } else if (include === "$self") {
    return new SelfReference();
  }
  const indexOfSharp = include.indexOf("#");
  if (indexOfSharp === -1) {
    return new TopLevelReference(include);
  } else if (indexOfSharp === 0) {
    return new RelativeReference(include.substring(1));
  } else {
    const scopeName = include.substring(0, indexOfSharp);
    const ruleName = include.substring(indexOfSharp + 1);
    return new TopLevelRepositoryReference(scopeName, ruleName);
  }
}
var HAS_BACK_REFERENCES = /\\(\d+)/;
var BACK_REFERENCING_END = /\\(\d+)/g;
var ruleIdSymbol = Symbol("RuleId");
var endRuleId = -1;
var whileRuleId = -2;
function ruleIdFromNumber(id) {
  return id;
}
function ruleIdToNumber(id) {
  return id;
}
var Rule = class {
  constructor($location, id, name, contentName) {
    __publicField(this, "$location");
    __publicField(this, "id");
    __publicField(this, "_nameIsCapturing");
    __publicField(this, "_name");
    __publicField(this, "_contentNameIsCapturing");
    __publicField(this, "_contentName");
    this.$location = $location;
    this.id = id;
    this._name = name || null;
    this._nameIsCapturing = RegexSource.hasCaptures(this._name);
    this._contentName = contentName || null;
    this._contentNameIsCapturing = RegexSource.hasCaptures(this._contentName);
  }
  get debugName() {
    const location = this.$location ? `${basename(this.$location.filename)}:${this.$location.line}` : "unknown";
    return `${this.constructor.name}#${this.id} @ ${location}`;
  }
  getName(lineText, captureIndices) {
    if (!this._nameIsCapturing || this._name === null || lineText === null || captureIndices === null) {
      return this._name;
    }
    return RegexSource.replaceCaptures(this._name, lineText, captureIndices);
  }
  getContentName(lineText, captureIndices) {
    if (!this._contentNameIsCapturing || this._contentName === null) {
      return this._contentName;
    }
    return RegexSource.replaceCaptures(this._contentName, lineText, captureIndices);
  }
};
var CaptureRule = class extends Rule {
  constructor($location, id, name, contentName, retokenizeCapturedWithRuleId) {
    super($location, id, name, contentName);
    __publicField(this, "retokenizeCapturedWithRuleId");
    this.retokenizeCapturedWithRuleId = retokenizeCapturedWithRuleId;
  }
  dispose() {
  }
  collectPatterns(grammar, out) {
    throw new Error("Not supported!");
  }
  compile(grammar, endRegexSource) {
    throw new Error("Not supported!");
  }
  compileAG(grammar, endRegexSource, allowA, allowG) {
    throw new Error("Not supported!");
  }
};
var MatchRule = class extends Rule {
  constructor($location, id, name, match, captures) {
    super($location, id, name, null);
    __publicField(this, "_match");
    __publicField(this, "captures");
    __publicField(this, "_cachedCompiledPatterns");
    this._match = new RegExpSource(match, this.id);
    this.captures = captures;
    this._cachedCompiledPatterns = null;
  }
  dispose() {
    if (this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns.dispose();
      this._cachedCompiledPatterns = null;
    }
  }
  get debugMatchRegExp() {
    return `${this._match.source}`;
  }
  collectPatterns(grammar, out) {
    out.push(this._match);
  }
  compile(grammar, endRegexSource) {
    return this._getCachedCompiledPatterns(grammar).compile(grammar);
  }
  compileAG(grammar, endRegexSource, allowA, allowG) {
    return this._getCachedCompiledPatterns(grammar).compileAG(grammar, allowA, allowG);
  }
  _getCachedCompiledPatterns(grammar) {
    if (!this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns = new RegExpSourceList();
      this.collectPatterns(grammar, this._cachedCompiledPatterns);
    }
    return this._cachedCompiledPatterns;
  }
};
var IncludeOnlyRule = class extends Rule {
  constructor($location, id, name, contentName, patterns) {
    super($location, id, name, contentName);
    __publicField(this, "hasMissingPatterns");
    __publicField(this, "patterns");
    __publicField(this, "_cachedCompiledPatterns");
    this.patterns = patterns.patterns;
    this.hasMissingPatterns = patterns.hasMissingPatterns;
    this._cachedCompiledPatterns = null;
  }
  dispose() {
    if (this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns.dispose();
      this._cachedCompiledPatterns = null;
    }
  }
  collectPatterns(grammar, out) {
    for (const pattern of this.patterns) {
      const rule = grammar.getRule(pattern);
      rule.collectPatterns(grammar, out);
    }
  }
  compile(grammar, endRegexSource) {
    return this._getCachedCompiledPatterns(grammar).compile(grammar);
  }
  compileAG(grammar, endRegexSource, allowA, allowG) {
    return this._getCachedCompiledPatterns(grammar).compileAG(grammar, allowA, allowG);
  }
  _getCachedCompiledPatterns(grammar) {
    if (!this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns = new RegExpSourceList();
      this.collectPatterns(grammar, this._cachedCompiledPatterns);
    }
    return this._cachedCompiledPatterns;
  }
};
var BeginEndRule = class extends Rule {
  constructor($location, id, name, contentName, begin, beginCaptures, end, endCaptures, applyEndPatternLast, patterns) {
    super($location, id, name, contentName);
    __publicField(this, "_begin");
    __publicField(this, "beginCaptures");
    __publicField(this, "_end");
    __publicField(this, "endHasBackReferences");
    __publicField(this, "endCaptures");
    __publicField(this, "applyEndPatternLast");
    __publicField(this, "hasMissingPatterns");
    __publicField(this, "patterns");
    __publicField(this, "_cachedCompiledPatterns");
    this._begin = new RegExpSource(begin, this.id);
    this.beginCaptures = beginCaptures;
    this._end = new RegExpSource(end ? end : "￿", -1);
    this.endHasBackReferences = this._end.hasBackReferences;
    this.endCaptures = endCaptures;
    this.applyEndPatternLast = applyEndPatternLast || false;
    this.patterns = patterns.patterns;
    this.hasMissingPatterns = patterns.hasMissingPatterns;
    this._cachedCompiledPatterns = null;
  }
  dispose() {
    if (this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns.dispose();
      this._cachedCompiledPatterns = null;
    }
  }
  get debugBeginRegExp() {
    return `${this._begin.source}`;
  }
  get debugEndRegExp() {
    return `${this._end.source}`;
  }
  getEndWithResolvedBackReferences(lineText, captureIndices) {
    return this._end.resolveBackReferences(lineText, captureIndices);
  }
  collectPatterns(grammar, out) {
    out.push(this._begin);
  }
  compile(grammar, endRegexSource) {
    return this._getCachedCompiledPatterns(grammar, endRegexSource).compile(grammar);
  }
  compileAG(grammar, endRegexSource, allowA, allowG) {
    return this._getCachedCompiledPatterns(grammar, endRegexSource).compileAG(grammar, allowA, allowG);
  }
  _getCachedCompiledPatterns(grammar, endRegexSource) {
    if (!this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns = new RegExpSourceList();
      for (const pattern of this.patterns) {
        const rule = grammar.getRule(pattern);
        rule.collectPatterns(grammar, this._cachedCompiledPatterns);
      }
      if (this.applyEndPatternLast) {
        this._cachedCompiledPatterns.push(this._end.hasBackReferences ? this._end.clone() : this._end);
      } else {
        this._cachedCompiledPatterns.unshift(this._end.hasBackReferences ? this._end.clone() : this._end);
      }
    }
    if (this._end.hasBackReferences) {
      if (this.applyEndPatternLast) {
        this._cachedCompiledPatterns.setSource(this._cachedCompiledPatterns.length() - 1, endRegexSource);
      } else {
        this._cachedCompiledPatterns.setSource(0, endRegexSource);
      }
    }
    return this._cachedCompiledPatterns;
  }
};
var BeginWhileRule = class extends Rule {
  constructor($location, id, name, contentName, begin, beginCaptures, _while, whileCaptures, patterns) {
    super($location, id, name, contentName);
    __publicField(this, "_begin");
    __publicField(this, "beginCaptures");
    __publicField(this, "whileCaptures");
    __publicField(this, "_while");
    __publicField(this, "whileHasBackReferences");
    __publicField(this, "hasMissingPatterns");
    __publicField(this, "patterns");
    __publicField(this, "_cachedCompiledPatterns");
    __publicField(this, "_cachedCompiledWhilePatterns");
    this._begin = new RegExpSource(begin, this.id);
    this.beginCaptures = beginCaptures;
    this.whileCaptures = whileCaptures;
    this._while = new RegExpSource(_while, whileRuleId);
    this.whileHasBackReferences = this._while.hasBackReferences;
    this.patterns = patterns.patterns;
    this.hasMissingPatterns = patterns.hasMissingPatterns;
    this._cachedCompiledPatterns = null;
    this._cachedCompiledWhilePatterns = null;
  }
  dispose() {
    if (this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns.dispose();
      this._cachedCompiledPatterns = null;
    }
    if (this._cachedCompiledWhilePatterns) {
      this._cachedCompiledWhilePatterns.dispose();
      this._cachedCompiledWhilePatterns = null;
    }
  }
  get debugBeginRegExp() {
    return `${this._begin.source}`;
  }
  get debugWhileRegExp() {
    return `${this._while.source}`;
  }
  getWhileWithResolvedBackReferences(lineText, captureIndices) {
    return this._while.resolveBackReferences(lineText, captureIndices);
  }
  collectPatterns(grammar, out) {
    out.push(this._begin);
  }
  compile(grammar, endRegexSource) {
    return this._getCachedCompiledPatterns(grammar).compile(grammar);
  }
  compileAG(grammar, endRegexSource, allowA, allowG) {
    return this._getCachedCompiledPatterns(grammar).compileAG(grammar, allowA, allowG);
  }
  _getCachedCompiledPatterns(grammar) {
    if (!this._cachedCompiledPatterns) {
      this._cachedCompiledPatterns = new RegExpSourceList();
      for (const pattern of this.patterns) {
        const rule = grammar.getRule(pattern);
        rule.collectPatterns(grammar, this._cachedCompiledPatterns);
      }
    }
    return this._cachedCompiledPatterns;
  }
  compileWhile(grammar, endRegexSource) {
    return this._getCachedCompiledWhilePatterns(grammar, endRegexSource).compile(grammar);
  }
  compileWhileAG(grammar, endRegexSource, allowA, allowG) {
    return this._getCachedCompiledWhilePatterns(grammar, endRegexSource).compileAG(grammar, allowA, allowG);
  }
  _getCachedCompiledWhilePatterns(grammar, endRegexSource) {
    if (!this._cachedCompiledWhilePatterns) {
      this._cachedCompiledWhilePatterns = new RegExpSourceList();
      this._cachedCompiledWhilePatterns.push(this._while.hasBackReferences ? this._while.clone() : this._while);
    }
    if (this._while.hasBackReferences) {
      this._cachedCompiledWhilePatterns.setSource(0, endRegexSource ? endRegexSource : "￿");
    }
    return this._cachedCompiledWhilePatterns;
  }
};
var RuleFactory = class _RuleFactory {
  static createCaptureRule(helper, $location, name, contentName, retokenizeCapturedWithRuleId) {
    return helper.registerRule((id) => {
      return new CaptureRule($location, id, name, contentName, retokenizeCapturedWithRuleId);
    });
  }
  static getCompiledRuleId(desc, helper, repository) {
    if (!desc.id) {
      helper.registerRule((id) => {
        desc.id = id;
        if (desc.match) {
          return new MatchRule(
            desc.$vscodeTextmateLocation,
            desc.id,
            desc.name,
            desc.match,
            _RuleFactory._compileCaptures(desc.captures, helper, repository)
          );
        }
        if (typeof desc.begin === "undefined") {
          if (desc.repository) {
            repository = mergeObjects({}, repository, desc.repository);
          }
          let patterns = desc.patterns;
          if (typeof patterns === "undefined" && desc.include) {
            patterns = [{ include: desc.include }];
          }
          return new IncludeOnlyRule(
            desc.$vscodeTextmateLocation,
            desc.id,
            desc.name,
            desc.contentName,
            _RuleFactory._compilePatterns(patterns, helper, repository)
          );
        }
        if (desc.while) {
          return new BeginWhileRule(
            desc.$vscodeTextmateLocation,
            desc.id,
            desc.name,
            desc.contentName,
            desc.begin,
            _RuleFactory._compileCaptures(desc.beginCaptures || desc.captures, helper, repository),
            desc.while,
            _RuleFactory._compileCaptures(desc.whileCaptures || desc.captures, helper, repository),
            _RuleFactory._compilePatterns(desc.patterns, helper, repository)
          );
        }
        return new BeginEndRule(
          desc.$vscodeTextmateLocation,
          desc.id,
          desc.name,
          desc.contentName,
          desc.begin,
          _RuleFactory._compileCaptures(desc.beginCaptures || desc.captures, helper, repository),
          desc.end,
          _RuleFactory._compileCaptures(desc.endCaptures || desc.captures, helper, repository),
          desc.applyEndPatternLast,
          _RuleFactory._compilePatterns(desc.patterns, helper, repository)
        );
      });
    }
    return desc.id;
  }
  static _compileCaptures(captures, helper, repository) {
    let r4 = [];
    if (captures) {
      let maximumCaptureId = 0;
      for (const captureId in captures) {
        if (captureId === "$vscodeTextmateLocation") {
          continue;
        }
        const numericCaptureId = parseInt(captureId, 10);
        if (numericCaptureId > maximumCaptureId) {
          maximumCaptureId = numericCaptureId;
        }
      }
      for (let i2 = 0; i2 <= maximumCaptureId; i2++) {
        r4[i2] = null;
      }
      for (const captureId in captures) {
        if (captureId === "$vscodeTextmateLocation") {
          continue;
        }
        const numericCaptureId = parseInt(captureId, 10);
        let retokenizeCapturedWithRuleId = 0;
        if (captures[captureId].patterns) {
          retokenizeCapturedWithRuleId = _RuleFactory.getCompiledRuleId(captures[captureId], helper, repository);
        }
        r4[numericCaptureId] = _RuleFactory.createCaptureRule(helper, captures[captureId].$vscodeTextmateLocation, captures[captureId].name, captures[captureId].contentName, retokenizeCapturedWithRuleId);
      }
    }
    return r4;
  }
  static _compilePatterns(patterns, helper, repository) {
    let r4 = [];
    if (patterns) {
      for (let i2 = 0, len = patterns.length; i2 < len; i2++) {
        const pattern = patterns[i2];
        let ruleId = -1;
        if (pattern.include) {
          const reference = parseInclude(pattern.include);
          switch (reference.kind) {
            case 0:
            case 1:
              ruleId = _RuleFactory.getCompiledRuleId(repository[pattern.include], helper, repository);
              break;
            case 2:
              let localIncludedRule = repository[reference.ruleName];
              if (localIncludedRule) {
                ruleId = _RuleFactory.getCompiledRuleId(localIncludedRule, helper, repository);
              } else {
              }
              break;
            case 3:
            case 4:
              const externalGrammarName = reference.scopeName;
              const externalGrammarInclude = reference.kind === 4 ? reference.ruleName : null;
              const externalGrammar = helper.getExternalGrammar(externalGrammarName, repository);
              if (externalGrammar) {
                if (externalGrammarInclude) {
                  let externalIncludedRule = externalGrammar.repository[externalGrammarInclude];
                  if (externalIncludedRule) {
                    ruleId = _RuleFactory.getCompiledRuleId(externalIncludedRule, helper, externalGrammar.repository);
                  } else {
                  }
                } else {
                  ruleId = _RuleFactory.getCompiledRuleId(externalGrammar.repository.$self, helper, externalGrammar.repository);
                }
              } else {
              }
              break;
          }
        } else {
          ruleId = _RuleFactory.getCompiledRuleId(pattern, helper, repository);
        }
        if (ruleId !== -1) {
          const rule = helper.getRule(ruleId);
          let skipRule = false;
          if (rule instanceof IncludeOnlyRule || rule instanceof BeginEndRule || rule instanceof BeginWhileRule) {
            if (rule.hasMissingPatterns && rule.patterns.length === 0) {
              skipRule = true;
            }
          }
          if (skipRule) {
            continue;
          }
          r4.push(ruleId);
        }
      }
    }
    return {
      patterns: r4,
      hasMissingPatterns: (patterns ? patterns.length : 0) !== r4.length
    };
  }
};
var RegExpSource = class _RegExpSource {
  constructor(regExpSource, ruleId) {
    __publicField(this, "source");
    __publicField(this, "ruleId");
    __publicField(this, "hasAnchor");
    __publicField(this, "hasBackReferences");
    __publicField(this, "_anchorCache");
    if (regExpSource && typeof regExpSource === "string") {
      const len = regExpSource.length;
      let lastPushedPos = 0;
      let output = [];
      let hasAnchor = false;
      for (let pos = 0; pos < len; pos++) {
        const ch = regExpSource.charAt(pos);
        if (ch === "\\") {
          if (pos + 1 < len) {
            const nextCh = regExpSource.charAt(pos + 1);
            if (nextCh === "z") {
              output.push(regExpSource.substring(lastPushedPos, pos));
              output.push("$(?!\\n)(?<!\\n)");
              lastPushedPos = pos + 2;
            } else if (nextCh === "A" || nextCh === "G") {
              hasAnchor = true;
            }
            pos++;
          }
        }
      }
      this.hasAnchor = hasAnchor;
      if (lastPushedPos === 0) {
        this.source = regExpSource;
      } else {
        output.push(regExpSource.substring(lastPushedPos, len));
        this.source = output.join("");
      }
    } else {
      this.hasAnchor = false;
      this.source = regExpSource;
    }
    if (this.hasAnchor) {
      this._anchorCache = this._buildAnchorCache();
    } else {
      this._anchorCache = null;
    }
    this.ruleId = ruleId;
    if (typeof this.source === "string") {
      this.hasBackReferences = HAS_BACK_REFERENCES.test(this.source);
    } else {
      this.hasBackReferences = false;
    }
  }
  clone() {
    return new _RegExpSource(this.source, this.ruleId);
  }
  setSource(newSource) {
    if (this.source === newSource) {
      return;
    }
    this.source = newSource;
    if (this.hasAnchor) {
      this._anchorCache = this._buildAnchorCache();
    }
  }
  resolveBackReferences(lineText, captureIndices) {
    if (typeof this.source !== "string") {
      throw new Error("This method should only be called if the source is a string");
    }
    let capturedValues = captureIndices.map((capture) => {
      return lineText.substring(capture.start, capture.end);
    });
    BACK_REFERENCING_END.lastIndex = 0;
    return this.source.replace(BACK_REFERENCING_END, (match, g1) => {
      return escapeRegExpCharacters(capturedValues[parseInt(g1, 10)] || "");
    });
  }
  _buildAnchorCache() {
    if (typeof this.source !== "string") {
      throw new Error("This method should only be called if the source is a string");
    }
    let A0_G0_result = [];
    let A0_G1_result = [];
    let A1_G0_result = [];
    let A1_G1_result = [];
    let pos, len, ch, nextCh;
    for (pos = 0, len = this.source.length; pos < len; pos++) {
      ch = this.source.charAt(pos);
      A0_G0_result[pos] = ch;
      A0_G1_result[pos] = ch;
      A1_G0_result[pos] = ch;
      A1_G1_result[pos] = ch;
      if (ch === "\\") {
        if (pos + 1 < len) {
          nextCh = this.source.charAt(pos + 1);
          if (nextCh === "A") {
            A0_G0_result[pos + 1] = "￿";
            A0_G1_result[pos + 1] = "￿";
            A1_G0_result[pos + 1] = "A";
            A1_G1_result[pos + 1] = "A";
          } else if (nextCh === "G") {
            A0_G0_result[pos + 1] = "￿";
            A0_G1_result[pos + 1] = "G";
            A1_G0_result[pos + 1] = "￿";
            A1_G1_result[pos + 1] = "G";
          } else {
            A0_G0_result[pos + 1] = nextCh;
            A0_G1_result[pos + 1] = nextCh;
            A1_G0_result[pos + 1] = nextCh;
            A1_G1_result[pos + 1] = nextCh;
          }
          pos++;
        }
      }
    }
    return {
      A0_G0: A0_G0_result.join(""),
      A0_G1: A0_G1_result.join(""),
      A1_G0: A1_G0_result.join(""),
      A1_G1: A1_G1_result.join("")
    };
  }
  resolveAnchors(allowA, allowG) {
    if (!this.hasAnchor || !this._anchorCache || typeof this.source !== "string") {
      return this.source;
    }
    if (allowA) {
      if (allowG) {
        return this._anchorCache.A1_G1;
      } else {
        return this._anchorCache.A1_G0;
      }
    } else {
      if (allowG) {
        return this._anchorCache.A0_G1;
      } else {
        return this._anchorCache.A0_G0;
      }
    }
  }
};
var RegExpSourceList = class {
  constructor() {
    __publicField(this, "_items");
    __publicField(this, "_hasAnchors");
    __publicField(this, "_cached");
    __publicField(this, "_anchorCache");
    this._items = [];
    this._hasAnchors = false;
    this._cached = null;
    this._anchorCache = {
      A0_G0: null,
      A0_G1: null,
      A1_G0: null,
      A1_G1: null
    };
  }
  dispose() {
    this._disposeCaches();
  }
  _disposeCaches() {
    if (this._cached) {
      this._cached.dispose();
      this._cached = null;
    }
    if (this._anchorCache.A0_G0) {
      this._anchorCache.A0_G0.dispose();
      this._anchorCache.A0_G0 = null;
    }
    if (this._anchorCache.A0_G1) {
      this._anchorCache.A0_G1.dispose();
      this._anchorCache.A0_G1 = null;
    }
    if (this._anchorCache.A1_G0) {
      this._anchorCache.A1_G0.dispose();
      this._anchorCache.A1_G0 = null;
    }
    if (this._anchorCache.A1_G1) {
      this._anchorCache.A1_G1.dispose();
      this._anchorCache.A1_G1 = null;
    }
  }
  push(item) {
    this._items.push(item);
    this._hasAnchors = this._hasAnchors || item.hasAnchor;
  }
  unshift(item) {
    this._items.unshift(item);
    this._hasAnchors = this._hasAnchors || item.hasAnchor;
  }
  length() {
    return this._items.length;
  }
  setSource(index, newSource) {
    if (this._items[index].source !== newSource) {
      this._disposeCaches();
      this._items[index].setSource(newSource);
    }
  }
  compile(onigLib) {
    if (!this._cached) {
      let regExps = this._items.map((e) => e.source);
      this._cached = new CompiledRule(onigLib, regExps, this._items.map((e) => e.ruleId));
    }
    return this._cached;
  }
  compileAG(onigLib, allowA, allowG) {
    if (!this._hasAnchors) {
      return this.compile(onigLib);
    } else {
      if (allowA) {
        if (allowG) {
          if (!this._anchorCache.A1_G1) {
            this._anchorCache.A1_G1 = this._resolveAnchors(onigLib, allowA, allowG);
          }
          return this._anchorCache.A1_G1;
        } else {
          if (!this._anchorCache.A1_G0) {
            this._anchorCache.A1_G0 = this._resolveAnchors(onigLib, allowA, allowG);
          }
          return this._anchorCache.A1_G0;
        }
      } else {
        if (allowG) {
          if (!this._anchorCache.A0_G1) {
            this._anchorCache.A0_G1 = this._resolveAnchors(onigLib, allowA, allowG);
          }
          return this._anchorCache.A0_G1;
        } else {
          if (!this._anchorCache.A0_G0) {
            this._anchorCache.A0_G0 = this._resolveAnchors(onigLib, allowA, allowG);
          }
          return this._anchorCache.A0_G0;
        }
      }
    }
  }
  _resolveAnchors(onigLib, allowA, allowG) {
    let regExps = this._items.map((e) => e.resolveAnchors(allowA, allowG));
    return new CompiledRule(onigLib, regExps, this._items.map((e) => e.ruleId));
  }
};
var CompiledRule = class {
  constructor(onigLib, regExps, rules) {
    __publicField(this, "scanner");
    this.regExps = regExps;
    this.rules = rules;
    this.scanner = onigLib.createOnigScanner(regExps);
  }
  dispose() {
    if (typeof this.scanner.dispose === "function") {
      this.scanner.dispose();
    }
  }
  toString() {
    const r4 = [];
    for (let i2 = 0, len = this.rules.length; i2 < len; i2++) {
      r4.push("   - " + this.rules[i2] + ": " + this.regExps[i2]);
    }
    return r4.join("\n");
  }
  findNextMatchSync(string, startPosition, options) {
    const result = this.scanner.findNextMatchSync(string, startPosition, options);
    if (!result) {
      return null;
    }
    return {
      ruleId: this.rules[result.index],
      captureIndices: result.captureIndices
    };
  }
};
var BasicScopeAttributes = class {
  constructor(languageId, tokenType) {
    this.languageId = languageId;
    this.tokenType = tokenType;
  }
};
var _a2;
var BasicScopeAttributesProvider = (_a2 = class {
  constructor(initialLanguageId, embeddedLanguages) {
    __publicField(this, "_defaultAttributes");
    __publicField(this, "_embeddedLanguagesMatcher");
    __publicField(this, "_getBasicScopeAttributes", new CachedFn((scopeName) => {
      const languageId = this._scopeToLanguage(scopeName);
      const standardTokenType = this._toStandardTokenType(scopeName);
      return new BasicScopeAttributes(languageId, standardTokenType);
    }));
    this._defaultAttributes = new BasicScopeAttributes(
      initialLanguageId,
      8
      /* NotSet */
    );
    this._embeddedLanguagesMatcher = new ScopeMatcher(Object.entries(embeddedLanguages || {}));
  }
  getDefaultAttributes() {
    return this._defaultAttributes;
  }
  getBasicScopeAttributes(scopeName) {
    if (scopeName === null) {
      return _a2._NULL_SCOPE_METADATA;
    }
    return this._getBasicScopeAttributes.get(scopeName);
  }
  /**
   * Given a produced TM scope, return the language that token describes or null if unknown.
   * e.g. source.html => html, source.css.embedded.html => css, punctuation.definition.tag.html => null
   */
  _scopeToLanguage(scope) {
    return this._embeddedLanguagesMatcher.match(scope) || 0;
  }
  _toStandardTokenType(scopeName) {
    const m3 = scopeName.match(_a2.STANDARD_TOKEN_TYPE_REGEXP);
    if (!m3) {
      return 8;
    }
    switch (m3[1]) {
      case "comment":
        return 1;
      case "string":
        return 2;
      case "regex":
        return 3;
      case "meta.embedded":
        return 0;
    }
    throw new Error("Unexpected match for standard token type!");
  }
}, __publicField(_a2, "_NULL_SCOPE_METADATA", new BasicScopeAttributes(0, 0)), __publicField(_a2, "STANDARD_TOKEN_TYPE_REGEXP", /\b(comment|string|regex|meta\.embedded)\b/), _a2);
var ScopeMatcher = class {
  constructor(values) {
    __publicField(this, "values");
    __publicField(this, "scopesRegExp");
    if (values.length === 0) {
      this.values = null;
      this.scopesRegExp = null;
    } else {
      this.values = new Map(values);
      const escapedScopes = values.map(
        ([scopeName, value]) => escapeRegExpCharacters(scopeName)
      );
      escapedScopes.sort();
      escapedScopes.reverse();
      this.scopesRegExp = new RegExp(
        `^((${escapedScopes.join(")|(")}))($|\\.)`,
        ""
      );
    }
  }
  match(scope) {
    if (!this.scopesRegExp) {
      return void 0;
    }
    const m3 = scope.match(this.scopesRegExp);
    if (!m3) {
      return void 0;
    }
    return this.values.get(m3[1]);
  }
};
var DebugFlags = {
  InDebugMode: typeof process !== "undefined" && !!process.env["VSCODE_TEXTMATE_DEBUG"]
};
var UseOnigurumaFindOptions = false;
var TokenizeStringResult = class {
  constructor(stack, stoppedEarly) {
    this.stack = stack;
    this.stoppedEarly = stoppedEarly;
  }
};
function _tokenizeString(grammar, lineText, isFirstLine, linePos, stack, lineTokens, checkWhileConditions, timeLimit) {
  const lineLength = lineText.content.length;
  let STOP = false;
  let anchorPosition = -1;
  if (checkWhileConditions) {
    const whileCheckResult = _checkWhileConditions(
      grammar,
      lineText,
      isFirstLine,
      linePos,
      stack,
      lineTokens
    );
    stack = whileCheckResult.stack;
    linePos = whileCheckResult.linePos;
    isFirstLine = whileCheckResult.isFirstLine;
    anchorPosition = whileCheckResult.anchorPosition;
  }
  const startTime = Date.now();
  while (!STOP) {
    if (timeLimit !== 0) {
      const elapsedTime = Date.now() - startTime;
      if (elapsedTime > timeLimit) {
        return new TokenizeStringResult(stack, true);
      }
    }
    scanNext();
  }
  return new TokenizeStringResult(stack, false);
  function scanNext() {
    if (false) {
      console.log("");
      console.log(
        `@@scanNext ${linePos}: |${lineText.content.substr(linePos).replace(/\n$/, "\\n")}|`
      );
    }
    const r4 = matchRuleOrInjections(
      grammar,
      lineText,
      isFirstLine,
      linePos,
      stack,
      anchorPosition
    );
    if (!r4) {
      lineTokens.produce(stack, lineLength);
      STOP = true;
      return;
    }
    const captureIndices = r4.captureIndices;
    const matchedRuleId = r4.matchedRuleId;
    const hasAdvanced = captureIndices && captureIndices.length > 0 ? captureIndices[0].end > linePos : false;
    if (matchedRuleId === endRuleId) {
      const poppedRule = stack.getRule(grammar);
      if (false) {
        console.log(
          "  popping " + poppedRule.debugName + " - " + poppedRule.debugEndRegExp
        );
      }
      lineTokens.produce(stack, captureIndices[0].start);
      stack = stack.withContentNameScopesList(stack.nameScopesList);
      handleCaptures(
        grammar,
        lineText,
        isFirstLine,
        stack,
        lineTokens,
        poppedRule.endCaptures,
        captureIndices
      );
      lineTokens.produce(stack, captureIndices[0].end);
      const popped = stack;
      stack = stack.parent;
      anchorPosition = popped.getAnchorPos();
      if (!hasAdvanced && popped.getEnterPos() === linePos) {
        if (false) {
          console.error(
            "[1] - Grammar is in an endless loop - Grammar pushed & popped a rule without advancing"
          );
        }
        stack = popped;
        lineTokens.produce(stack, lineLength);
        STOP = true;
        return;
      }
    } else {
      const _rule = grammar.getRule(matchedRuleId);
      lineTokens.produce(stack, captureIndices[0].start);
      const beforePush = stack;
      const scopeName = _rule.getName(lineText.content, captureIndices);
      const nameScopesList = stack.contentNameScopesList.pushAttributed(
        scopeName,
        grammar
      );
      stack = stack.push(
        matchedRuleId,
        linePos,
        anchorPosition,
        captureIndices[0].end === lineLength,
        null,
        nameScopesList,
        nameScopesList
      );
      if (_rule instanceof BeginEndRule) {
        const pushedRule = _rule;
        if (false) {
          console.log(
            "  pushing " + pushedRule.debugName + " - " + pushedRule.debugBeginRegExp
          );
        }
        handleCaptures(
          grammar,
          lineText,
          isFirstLine,
          stack,
          lineTokens,
          pushedRule.beginCaptures,
          captureIndices
        );
        lineTokens.produce(stack, captureIndices[0].end);
        anchorPosition = captureIndices[0].end;
        const contentName = pushedRule.getContentName(
          lineText.content,
          captureIndices
        );
        const contentNameScopesList = nameScopesList.pushAttributed(
          contentName,
          grammar
        );
        stack = stack.withContentNameScopesList(contentNameScopesList);
        if (pushedRule.endHasBackReferences) {
          stack = stack.withEndRule(
            pushedRule.getEndWithResolvedBackReferences(
              lineText.content,
              captureIndices
            )
          );
        }
        if (!hasAdvanced && beforePush.hasSameRuleAs(stack)) {
          if (false) {
            console.error(
              "[2] - Grammar is in an endless loop - Grammar pushed the same rule without advancing"
            );
          }
          stack = stack.pop();
          lineTokens.produce(stack, lineLength);
          STOP = true;
          return;
        }
      } else if (_rule instanceof BeginWhileRule) {
        const pushedRule = _rule;
        if (false) {
          console.log("  pushing " + pushedRule.debugName);
        }
        handleCaptures(
          grammar,
          lineText,
          isFirstLine,
          stack,
          lineTokens,
          pushedRule.beginCaptures,
          captureIndices
        );
        lineTokens.produce(stack, captureIndices[0].end);
        anchorPosition = captureIndices[0].end;
        const contentName = pushedRule.getContentName(
          lineText.content,
          captureIndices
        );
        const contentNameScopesList = nameScopesList.pushAttributed(
          contentName,
          grammar
        );
        stack = stack.withContentNameScopesList(contentNameScopesList);
        if (pushedRule.whileHasBackReferences) {
          stack = stack.withEndRule(
            pushedRule.getWhileWithResolvedBackReferences(
              lineText.content,
              captureIndices
            )
          );
        }
        if (!hasAdvanced && beforePush.hasSameRuleAs(stack)) {
          if (false) {
            console.error(
              "[3] - Grammar is in an endless loop - Grammar pushed the same rule without advancing"
            );
          }
          stack = stack.pop();
          lineTokens.produce(stack, lineLength);
          STOP = true;
          return;
        }
      } else {
        const matchingRule = _rule;
        if (false) {
          console.log(
            "  matched " + matchingRule.debugName + " - " + matchingRule.debugMatchRegExp
          );
        }
        handleCaptures(
          grammar,
          lineText,
          isFirstLine,
          stack,
          lineTokens,
          matchingRule.captures,
          captureIndices
        );
        lineTokens.produce(stack, captureIndices[0].end);
        stack = stack.pop();
        if (!hasAdvanced) {
          if (false) {
            console.error(
              "[4] - Grammar is in an endless loop - Grammar is not advancing, nor is it pushing/popping"
            );
          }
          stack = stack.safePop();
          lineTokens.produce(stack, lineLength);
          STOP = true;
          return;
        }
      }
    }
    if (captureIndices[0].end > linePos) {
      linePos = captureIndices[0].end;
      isFirstLine = false;
    }
  }
}
function _checkWhileConditions(grammar, lineText, isFirstLine, linePos, stack, lineTokens) {
  let anchorPosition = stack.beginRuleCapturedEOL ? 0 : -1;
  const whileRules = [];
  for (let node = stack; node; node = node.pop()) {
    const nodeRule = node.getRule(grammar);
    if (nodeRule instanceof BeginWhileRule) {
      whileRules.push({
        rule: nodeRule,
        stack: node
      });
    }
  }
  for (let whileRule = whileRules.pop(); whileRule; whileRule = whileRules.pop()) {
    const { ruleScanner, findOptions } = prepareRuleWhileSearch(whileRule.rule, grammar, whileRule.stack.endRule, isFirstLine, linePos === anchorPosition);
    const r4 = ruleScanner.findNextMatchSync(lineText, linePos, findOptions);
    if (false) {
      console.log("  scanning for while rule");
      console.log(ruleScanner.toString());
    }
    if (r4) {
      const matchedRuleId = r4.ruleId;
      if (matchedRuleId !== whileRuleId) {
        stack = whileRule.stack.pop();
        break;
      }
      if (r4.captureIndices && r4.captureIndices.length) {
        lineTokens.produce(whileRule.stack, r4.captureIndices[0].start);
        handleCaptures(grammar, lineText, isFirstLine, whileRule.stack, lineTokens, whileRule.rule.whileCaptures, r4.captureIndices);
        lineTokens.produce(whileRule.stack, r4.captureIndices[0].end);
        anchorPosition = r4.captureIndices[0].end;
        if (r4.captureIndices[0].end > linePos) {
          linePos = r4.captureIndices[0].end;
          isFirstLine = false;
        }
      }
    } else {
      if (false) {
        console.log("  popping " + whileRule.rule.debugName + " - " + whileRule.rule.debugWhileRegExp);
      }
      stack = whileRule.stack.pop();
      break;
    }
  }
  return { stack, linePos, anchorPosition, isFirstLine };
}
function matchRuleOrInjections(grammar, lineText, isFirstLine, linePos, stack, anchorPosition) {
  const matchResult = matchRule(grammar, lineText, isFirstLine, linePos, stack, anchorPosition);
  const injections = grammar.getInjections();
  if (injections.length === 0) {
    return matchResult;
  }
  const injectionResult = matchInjections(injections, grammar, lineText, isFirstLine, linePos, stack, anchorPosition);
  if (!injectionResult) {
    return matchResult;
  }
  if (!matchResult) {
    return injectionResult;
  }
  const matchResultScore = matchResult.captureIndices[0].start;
  const injectionResultScore = injectionResult.captureIndices[0].start;
  if (injectionResultScore < matchResultScore || injectionResult.priorityMatch && injectionResultScore === matchResultScore) {
    return injectionResult;
  }
  return matchResult;
}
function matchRule(grammar, lineText, isFirstLine, linePos, stack, anchorPosition) {
  const rule = stack.getRule(grammar);
  const { ruleScanner, findOptions } = prepareRuleSearch(rule, grammar, stack.endRule, isFirstLine, linePos === anchorPosition);
  const r4 = ruleScanner.findNextMatchSync(lineText, linePos, findOptions);
  if (r4) {
    return {
      captureIndices: r4.captureIndices,
      matchedRuleId: r4.ruleId
    };
  }
  return null;
}
function matchInjections(injections, grammar, lineText, isFirstLine, linePos, stack, anchorPosition) {
  let bestMatchRating = Number.MAX_VALUE;
  let bestMatchCaptureIndices = null;
  let bestMatchRuleId;
  let bestMatchResultPriority = 0;
  const scopes = stack.contentNameScopesList.getScopeNames();
  for (let i2 = 0, len = injections.length; i2 < len; i2++) {
    const injection = injections[i2];
    if (!injection.matcher(scopes)) {
      continue;
    }
    const rule = grammar.getRule(injection.ruleId);
    const { ruleScanner, findOptions } = prepareRuleSearch(rule, grammar, null, isFirstLine, linePos === anchorPosition);
    const matchResult = ruleScanner.findNextMatchSync(lineText, linePos, findOptions);
    if (!matchResult) {
      continue;
    }
    if (false) {
      console.log(`  matched injection: ${injection.debugSelector}`);
      console.log(ruleScanner.toString());
    }
    const matchRating = matchResult.captureIndices[0].start;
    if (matchRating >= bestMatchRating) {
      continue;
    }
    bestMatchRating = matchRating;
    bestMatchCaptureIndices = matchResult.captureIndices;
    bestMatchRuleId = matchResult.ruleId;
    bestMatchResultPriority = injection.priority;
    if (bestMatchRating === linePos) {
      break;
    }
  }
  if (bestMatchCaptureIndices) {
    return {
      priorityMatch: bestMatchResultPriority === -1,
      captureIndices: bestMatchCaptureIndices,
      matchedRuleId: bestMatchRuleId
    };
  }
  return null;
}
function prepareRuleSearch(rule, grammar, endRegexSource, allowA, allowG) {
  if (UseOnigurumaFindOptions) {
    const ruleScanner2 = rule.compile(grammar, endRegexSource);
    const findOptions = getFindOptions(allowA, allowG);
    return { ruleScanner: ruleScanner2, findOptions };
  }
  const ruleScanner = rule.compileAG(grammar, endRegexSource, allowA, allowG);
  return {
    ruleScanner,
    findOptions: 0
    /* None */
  };
}
function prepareRuleWhileSearch(rule, grammar, endRegexSource, allowA, allowG) {
  if (UseOnigurumaFindOptions) {
    const ruleScanner2 = rule.compileWhile(grammar, endRegexSource);
    const findOptions = getFindOptions(allowA, allowG);
    return { ruleScanner: ruleScanner2, findOptions };
  }
  const ruleScanner = rule.compileWhileAG(grammar, endRegexSource, allowA, allowG);
  return {
    ruleScanner,
    findOptions: 0
    /* None */
  };
}
function getFindOptions(allowA, allowG) {
  let options = 0;
  if (!allowA) {
    options |= 1;
  }
  if (!allowG) {
    options |= 4;
  }
  return options;
}
function handleCaptures(grammar, lineText, isFirstLine, stack, lineTokens, captures, captureIndices) {
  if (captures.length === 0) {
    return;
  }
  const lineTextContent = lineText.content;
  const len = Math.min(captures.length, captureIndices.length);
  const localStack = [];
  const maxEnd = captureIndices[0].end;
  for (let i2 = 0; i2 < len; i2++) {
    const captureRule = captures[i2];
    if (captureRule === null) {
      continue;
    }
    const captureIndex = captureIndices[i2];
    if (captureIndex.length === 0) {
      continue;
    }
    if (captureIndex.start > maxEnd) {
      break;
    }
    while (localStack.length > 0 && localStack[localStack.length - 1].endPos <= captureIndex.start) {
      lineTokens.produceFromScopes(localStack[localStack.length - 1].scopes, localStack[localStack.length - 1].endPos);
      localStack.pop();
    }
    if (localStack.length > 0) {
      lineTokens.produceFromScopes(localStack[localStack.length - 1].scopes, captureIndex.start);
    } else {
      lineTokens.produce(stack, captureIndex.start);
    }
    if (captureRule.retokenizeCapturedWithRuleId) {
      const scopeName = captureRule.getName(lineTextContent, captureIndices);
      const nameScopesList = stack.contentNameScopesList.pushAttributed(scopeName, grammar);
      const contentName = captureRule.getContentName(lineTextContent, captureIndices);
      const contentNameScopesList = nameScopesList.pushAttributed(contentName, grammar);
      const stackClone = stack.push(captureRule.retokenizeCapturedWithRuleId, captureIndex.start, -1, false, null, nameScopesList, contentNameScopesList);
      const onigSubStr = grammar.createOnigString(lineTextContent.substring(0, captureIndex.end));
      _tokenizeString(
        grammar,
        onigSubStr,
        isFirstLine && captureIndex.start === 0,
        captureIndex.start,
        stackClone,
        lineTokens,
        false,
        /* no time limit */
        0
      );
      disposeOnigString(onigSubStr);
      continue;
    }
    const captureRuleScopeName = captureRule.getName(lineTextContent, captureIndices);
    if (captureRuleScopeName !== null) {
      const base = localStack.length > 0 ? localStack[localStack.length - 1].scopes : stack.contentNameScopesList;
      const captureRuleScopesList = base.pushAttributed(captureRuleScopeName, grammar);
      localStack.push(new LocalStackElement(captureRuleScopesList, captureIndex.end));
    }
  }
  while (localStack.length > 0) {
    lineTokens.produceFromScopes(localStack[localStack.length - 1].scopes, localStack[localStack.length - 1].endPos);
    localStack.pop();
  }
}
var LocalStackElement = class {
  constructor(scopes, endPos) {
    __publicField(this, "scopes");
    __publicField(this, "endPos");
    this.scopes = scopes;
    this.endPos = endPos;
  }
};
function createGrammar(scopeName, grammar, initialLanguage, embeddedLanguages, tokenTypes, balancedBracketSelectors, grammarRepository, onigLib) {
  return new Grammar(
    scopeName,
    grammar,
    initialLanguage,
    embeddedLanguages,
    tokenTypes,
    balancedBracketSelectors,
    grammarRepository,
    onigLib
  );
}
function collectInjections(result, selector, rule, ruleFactoryHelper, grammar) {
  const matchers = createMatchers(selector, nameMatcher);
  const ruleId = RuleFactory.getCompiledRuleId(rule, ruleFactoryHelper, grammar.repository);
  for (const matcher of matchers) {
    result.push({
      debugSelector: selector,
      matcher: matcher.matcher,
      ruleId,
      grammar,
      priority: matcher.priority
    });
  }
}
function nameMatcher(identifers, scopes) {
  if (scopes.length < identifers.length) {
    return false;
  }
  let lastIndex = 0;
  return identifers.every((identifier) => {
    for (let i2 = lastIndex; i2 < scopes.length; i2++) {
      if (scopesAreMatching(scopes[i2], identifier)) {
        lastIndex = i2 + 1;
        return true;
      }
    }
    return false;
  });
}
function scopesAreMatching(thisScopeName, scopeName) {
  if (!thisScopeName) {
    return false;
  }
  if (thisScopeName === scopeName) {
    return true;
  }
  const len = scopeName.length;
  return thisScopeName.length > len && thisScopeName.substr(0, len) === scopeName && thisScopeName[len] === ".";
}
var Grammar = class {
  constructor(_rootScopeName, grammar, initialLanguage, embeddedLanguages, tokenTypes, balancedBracketSelectors, grammarRepository, _onigLib) {
    __publicField(this, "_rootId");
    __publicField(this, "_lastRuleId");
    __publicField(this, "_ruleId2desc");
    __publicField(this, "_includedGrammars");
    __publicField(this, "_grammarRepository");
    __publicField(this, "_grammar");
    __publicField(this, "_injections");
    __publicField(this, "_basicScopeAttributesProvider");
    __publicField(this, "_tokenTypeMatchers");
    this._rootScopeName = _rootScopeName;
    this.balancedBracketSelectors = balancedBracketSelectors;
    this._onigLib = _onigLib;
    this._basicScopeAttributesProvider = new BasicScopeAttributesProvider(
      initialLanguage,
      embeddedLanguages
    );
    this._rootId = -1;
    this._lastRuleId = 0;
    this._ruleId2desc = [null];
    this._includedGrammars = {};
    this._grammarRepository = grammarRepository;
    this._grammar = initGrammar(grammar, null);
    this._injections = null;
    this._tokenTypeMatchers = [];
    if (tokenTypes) {
      for (const selector of Object.keys(tokenTypes)) {
        const matchers = createMatchers(selector, nameMatcher);
        for (const matcher of matchers) {
          this._tokenTypeMatchers.push({
            matcher: matcher.matcher,
            type: tokenTypes[selector]
          });
        }
      }
    }
  }
  get themeProvider() {
    return this._grammarRepository;
  }
  dispose() {
    for (const rule of this._ruleId2desc) {
      if (rule) {
        rule.dispose();
      }
    }
  }
  createOnigScanner(sources) {
    return this._onigLib.createOnigScanner(sources);
  }
  createOnigString(sources) {
    return this._onigLib.createOnigString(sources);
  }
  getMetadataForScope(scope) {
    return this._basicScopeAttributesProvider.getBasicScopeAttributes(scope);
  }
  _collectInjections() {
    const grammarRepository = {
      lookup: (scopeName2) => {
        if (scopeName2 === this._rootScopeName) {
          return this._grammar;
        }
        return this.getExternalGrammar(scopeName2);
      },
      injections: (scopeName2) => {
        return this._grammarRepository.injections(scopeName2);
      }
    };
    const result = [];
    const scopeName = this._rootScopeName;
    const grammar = grammarRepository.lookup(scopeName);
    if (grammar) {
      const rawInjections = grammar.injections;
      if (rawInjections) {
        for (let expression in rawInjections) {
          collectInjections(
            result,
            expression,
            rawInjections[expression],
            this,
            grammar
          );
        }
      }
      const injectionScopeNames = this._grammarRepository.injections(scopeName);
      if (injectionScopeNames) {
        injectionScopeNames.forEach((injectionScopeName) => {
          const injectionGrammar = this.getExternalGrammar(injectionScopeName);
          if (injectionGrammar) {
            const selector = injectionGrammar.injectionSelector;
            if (selector) {
              collectInjections(
                result,
                selector,
                injectionGrammar,
                this,
                injectionGrammar
              );
            }
          }
        });
      }
    }
    result.sort((i1, i2) => i1.priority - i2.priority);
    return result;
  }
  getInjections() {
    if (this._injections === null) {
      this._injections = this._collectInjections();
    }
    return this._injections;
  }
  registerRule(factory) {
    const id = ++this._lastRuleId;
    const result = factory(ruleIdFromNumber(id));
    this._ruleId2desc[id] = result;
    return result;
  }
  getRule(ruleId) {
    return this._ruleId2desc[ruleIdToNumber(ruleId)];
  }
  getExternalGrammar(scopeName, repository) {
    if (this._includedGrammars[scopeName]) {
      return this._includedGrammars[scopeName];
    } else if (this._grammarRepository) {
      const rawIncludedGrammar = this._grammarRepository.lookup(scopeName);
      if (rawIncludedGrammar) {
        this._includedGrammars[scopeName] = initGrammar(
          rawIncludedGrammar,
          repository && repository.$base
        );
        return this._includedGrammars[scopeName];
      }
    }
    return void 0;
  }
  tokenizeLine(lineText, prevState, timeLimit = 0) {
    const r4 = this._tokenize(lineText, prevState, false, timeLimit);
    return {
      tokens: r4.lineTokens.getResult(r4.ruleStack, r4.lineLength),
      ruleStack: r4.ruleStack,
      stoppedEarly: r4.stoppedEarly
    };
  }
  tokenizeLine2(lineText, prevState, timeLimit = 0) {
    const r4 = this._tokenize(lineText, prevState, true, timeLimit);
    return {
      tokens: r4.lineTokens.getBinaryResult(r4.ruleStack, r4.lineLength),
      ruleStack: r4.ruleStack,
      stoppedEarly: r4.stoppedEarly
    };
  }
  _tokenize(lineText, prevState, emitBinaryTokens, timeLimit) {
    if (this._rootId === -1) {
      this._rootId = RuleFactory.getCompiledRuleId(
        this._grammar.repository.$self,
        this,
        this._grammar.repository
      );
      this.getInjections();
    }
    let isFirstLine;
    if (!prevState || prevState === StateStackImpl.NULL) {
      isFirstLine = true;
      const rawDefaultMetadata = this._basicScopeAttributesProvider.getDefaultAttributes();
      const defaultStyle = this.themeProvider.getDefaults();
      const defaultMetadata = EncodedTokenMetadata.set(
        0,
        rawDefaultMetadata.languageId,
        rawDefaultMetadata.tokenType,
        null,
        defaultStyle.fontStyle,
        defaultStyle.foregroundId,
        defaultStyle.backgroundId
      );
      const rootScopeName = this.getRule(this._rootId).getName(
        null,
        null
      );
      let scopeList;
      if (rootScopeName) {
        scopeList = AttributedScopeStack.createRootAndLookUpScopeName(
          rootScopeName,
          defaultMetadata,
          this
        );
      } else {
        scopeList = AttributedScopeStack.createRoot(
          "unknown",
          defaultMetadata
        );
      }
      prevState = new StateStackImpl(
        null,
        this._rootId,
        -1,
        -1,
        false,
        null,
        scopeList,
        scopeList
      );
    } else {
      isFirstLine = false;
      prevState.reset();
    }
    lineText = lineText + "\n";
    const onigLineText = this.createOnigString(lineText);
    const lineLength = onigLineText.content.length;
    const lineTokens = new LineTokens(
      emitBinaryTokens,
      lineText,
      this._tokenTypeMatchers,
      this.balancedBracketSelectors
    );
    const r4 = _tokenizeString(
      this,
      onigLineText,
      isFirstLine,
      0,
      prevState,
      lineTokens,
      true,
      timeLimit
    );
    disposeOnigString(onigLineText);
    return {
      lineLength,
      lineTokens,
      ruleStack: r4.stack,
      stoppedEarly: r4.stoppedEarly
    };
  }
};
function initGrammar(grammar, base) {
  grammar = clone(grammar);
  grammar.repository = grammar.repository || {};
  grammar.repository.$self = {
    $vscodeTextmateLocation: grammar.$vscodeTextmateLocation,
    patterns: grammar.patterns,
    name: grammar.scopeName
  };
  grammar.repository.$base = base || grammar.repository.$self;
  return grammar;
}
var AttributedScopeStack = class _AttributedScopeStack {
  /**
   * Invariant:
   * ```
   * if (parent && !scopePath.extends(parent.scopePath)) {
   * 	throw new Error();
   * }
   * ```
   */
  constructor(parent, scopePath, tokenAttributes) {
    this.parent = parent;
    this.scopePath = scopePath;
    this.tokenAttributes = tokenAttributes;
  }
  static fromExtension(namesScopeList, contentNameScopesList) {
    let current = namesScopeList;
    let scopeNames = (namesScopeList == null ? void 0 : namesScopeList.scopePath) ?? null;
    for (const frame of contentNameScopesList) {
      scopeNames = ScopeStack.push(scopeNames, frame.scopeNames);
      current = new _AttributedScopeStack(current, scopeNames, frame.encodedTokenAttributes);
    }
    return current;
  }
  static createRoot(scopeName, tokenAttributes) {
    return new _AttributedScopeStack(null, new ScopeStack(null, scopeName), tokenAttributes);
  }
  static createRootAndLookUpScopeName(scopeName, tokenAttributes, grammar) {
    const rawRootMetadata = grammar.getMetadataForScope(scopeName);
    const scopePath = new ScopeStack(null, scopeName);
    const rootStyle = grammar.themeProvider.themeMatch(scopePath);
    const resolvedTokenAttributes = _AttributedScopeStack.mergeAttributes(
      tokenAttributes,
      rawRootMetadata,
      rootStyle
    );
    return new _AttributedScopeStack(null, scopePath, resolvedTokenAttributes);
  }
  get scopeName() {
    return this.scopePath.scopeName;
  }
  toString() {
    return this.getScopeNames().join(" ");
  }
  equals(other) {
    return _AttributedScopeStack.equals(this, other);
  }
  static equals(a2, b3) {
    do {
      if (a2 === b3) {
        return true;
      }
      if (!a2 && !b3) {
        return true;
      }
      if (!a2 || !b3) {
        return false;
      }
      if (a2.scopeName !== b3.scopeName || a2.tokenAttributes !== b3.tokenAttributes) {
        return false;
      }
      a2 = a2.parent;
      b3 = b3.parent;
    } while (true);
  }
  static mergeAttributes(existingTokenAttributes, basicScopeAttributes, styleAttributes) {
    let fontStyle = -1;
    let foreground = 0;
    let background = 0;
    if (styleAttributes !== null) {
      fontStyle = styleAttributes.fontStyle;
      foreground = styleAttributes.foregroundId;
      background = styleAttributes.backgroundId;
    }
    return EncodedTokenMetadata.set(
      existingTokenAttributes,
      basicScopeAttributes.languageId,
      basicScopeAttributes.tokenType,
      null,
      fontStyle,
      foreground,
      background
    );
  }
  pushAttributed(scopePath, grammar) {
    if (scopePath === null) {
      return this;
    }
    if (scopePath.indexOf(" ") === -1) {
      return _AttributedScopeStack._pushAttributed(this, scopePath, grammar);
    }
    const scopes = scopePath.split(/ /g);
    let result = this;
    for (const scope of scopes) {
      result = _AttributedScopeStack._pushAttributed(result, scope, grammar);
    }
    return result;
  }
  static _pushAttributed(target, scopeName, grammar) {
    const rawMetadata = grammar.getMetadataForScope(scopeName);
    const newPath = target.scopePath.push(scopeName);
    const scopeThemeMatchResult = grammar.themeProvider.themeMatch(newPath);
    const metadata = _AttributedScopeStack.mergeAttributes(
      target.tokenAttributes,
      rawMetadata,
      scopeThemeMatchResult
    );
    return new _AttributedScopeStack(target, newPath, metadata);
  }
  getScopeNames() {
    return this.scopePath.getSegments();
  }
  getExtensionIfDefined(base) {
    var _a9;
    const result = [];
    let self2 = this;
    while (self2 && self2 !== base) {
      result.push({
        encodedTokenAttributes: self2.tokenAttributes,
        scopeNames: self2.scopePath.getExtensionIfDefined(((_a9 = self2.parent) == null ? void 0 : _a9.scopePath) ?? null)
      });
      self2 = self2.parent;
    }
    return self2 === base ? result.reverse() : void 0;
  }
};
var _a3;
var StateStackImpl = (_a3 = class {
  /**
   * Invariant:
   * ```
   * if (contentNameScopesList !== nameScopesList && contentNameScopesList?.parent !== nameScopesList) {
   * 	throw new Error();
   * }
   * if (this.parent && !nameScopesList.extends(this.parent.contentNameScopesList)) {
   * 	throw new Error();
   * }
   * ```
   */
  constructor(parent, ruleId, enterPos, anchorPos, beginRuleCapturedEOL, endRule, nameScopesList, contentNameScopesList) {
    __publicField(this, "_stackElementBrand");
    /**
     * The position on the current line where this state was pushed.
     * This is relevant only while tokenizing a line, to detect endless loops.
     * Its value is meaningless across lines.
     */
    __publicField(this, "_enterPos");
    /**
     * The captured anchor position when this stack element was pushed.
     * This is relevant only while tokenizing a line, to restore the anchor position when popping.
     * Its value is meaningless across lines.
     */
    __publicField(this, "_anchorPos");
    /**
     * The depth of the stack.
     */
    __publicField(this, "depth");
    this.parent = parent;
    this.ruleId = ruleId;
    this.beginRuleCapturedEOL = beginRuleCapturedEOL;
    this.endRule = endRule;
    this.nameScopesList = nameScopesList;
    this.contentNameScopesList = contentNameScopesList;
    this.depth = this.parent ? this.parent.depth + 1 : 1;
    this._enterPos = enterPos;
    this._anchorPos = anchorPos;
  }
  equals(other) {
    if (other === null) {
      return false;
    }
    return _a3._equals(this, other);
  }
  static _equals(a2, b3) {
    if (a2 === b3) {
      return true;
    }
    if (!this._structuralEquals(a2, b3)) {
      return false;
    }
    return AttributedScopeStack.equals(a2.contentNameScopesList, b3.contentNameScopesList);
  }
  /**
   * A structural equals check. Does not take into account `scopes`.
   */
  static _structuralEquals(a2, b3) {
    do {
      if (a2 === b3) {
        return true;
      }
      if (!a2 && !b3) {
        return true;
      }
      if (!a2 || !b3) {
        return false;
      }
      if (a2.depth !== b3.depth || a2.ruleId !== b3.ruleId || a2.endRule !== b3.endRule) {
        return false;
      }
      a2 = a2.parent;
      b3 = b3.parent;
    } while (true);
  }
  clone() {
    return this;
  }
  static _reset(el) {
    while (el) {
      el._enterPos = -1;
      el._anchorPos = -1;
      el = el.parent;
    }
  }
  reset() {
    _a3._reset(this);
  }
  pop() {
    return this.parent;
  }
  safePop() {
    if (this.parent) {
      return this.parent;
    }
    return this;
  }
  push(ruleId, enterPos, anchorPos, beginRuleCapturedEOL, endRule, nameScopesList, contentNameScopesList) {
    return new _a3(
      this,
      ruleId,
      enterPos,
      anchorPos,
      beginRuleCapturedEOL,
      endRule,
      nameScopesList,
      contentNameScopesList
    );
  }
  getEnterPos() {
    return this._enterPos;
  }
  getAnchorPos() {
    return this._anchorPos;
  }
  getRule(grammar) {
    return grammar.getRule(this.ruleId);
  }
  toString() {
    const r4 = [];
    this._writeString(r4, 0);
    return "[" + r4.join(",") + "]";
  }
  _writeString(res, outIndex) {
    var _a9, _b;
    if (this.parent) {
      outIndex = this.parent._writeString(res, outIndex);
    }
    res[outIndex++] = `(${this.ruleId}, ${(_a9 = this.nameScopesList) == null ? void 0 : _a9.toString()}, ${(_b = this.contentNameScopesList) == null ? void 0 : _b.toString()})`;
    return outIndex;
  }
  withContentNameScopesList(contentNameScopeStack) {
    if (this.contentNameScopesList === contentNameScopeStack) {
      return this;
    }
    return this.parent.push(
      this.ruleId,
      this._enterPos,
      this._anchorPos,
      this.beginRuleCapturedEOL,
      this.endRule,
      this.nameScopesList,
      contentNameScopeStack
    );
  }
  withEndRule(endRule) {
    if (this.endRule === endRule) {
      return this;
    }
    return new _a3(
      this.parent,
      this.ruleId,
      this._enterPos,
      this._anchorPos,
      this.beginRuleCapturedEOL,
      endRule,
      this.nameScopesList,
      this.contentNameScopesList
    );
  }
  // Used to warn of endless loops
  hasSameRuleAs(other) {
    let el = this;
    while (el && el._enterPos === other._enterPos) {
      if (el.ruleId === other.ruleId) {
        return true;
      }
      el = el.parent;
    }
    return false;
  }
  toStateStackFrame() {
    var _a9, _b, _c;
    return {
      ruleId: ruleIdToNumber(this.ruleId),
      beginRuleCapturedEOL: this.beginRuleCapturedEOL,
      endRule: this.endRule,
      nameScopesList: ((_b = this.nameScopesList) == null ? void 0 : _b.getExtensionIfDefined(((_a9 = this.parent) == null ? void 0 : _a9.nameScopesList) ?? null)) ?? [],
      contentNameScopesList: ((_c = this.contentNameScopesList) == null ? void 0 : _c.getExtensionIfDefined(this.nameScopesList)) ?? []
    };
  }
  static pushFrame(self2, frame) {
    const namesScopeList = AttributedScopeStack.fromExtension((self2 == null ? void 0 : self2.nameScopesList) ?? null, frame.nameScopesList);
    return new _a3(
      self2,
      ruleIdFromNumber(frame.ruleId),
      frame.enterPos ?? -1,
      frame.anchorPos ?? -1,
      frame.beginRuleCapturedEOL,
      frame.endRule,
      namesScopeList,
      AttributedScopeStack.fromExtension(namesScopeList, frame.contentNameScopesList)
    );
  }
}, // TODO remove me
__publicField(_a3, "NULL", new _a3(
  null,
  0,
  0,
  0,
  false,
  null,
  null,
  null
)), _a3);
var BalancedBracketSelectors = class {
  constructor(balancedBracketScopes, unbalancedBracketScopes) {
    __publicField(this, "balancedBracketScopes");
    __publicField(this, "unbalancedBracketScopes");
    __publicField(this, "allowAny", false);
    this.balancedBracketScopes = balancedBracketScopes.flatMap(
      (selector) => {
        if (selector === "*") {
          this.allowAny = true;
          return [];
        }
        return createMatchers(selector, nameMatcher).map((m3) => m3.matcher);
      }
    );
    this.unbalancedBracketScopes = unbalancedBracketScopes.flatMap(
      (selector) => createMatchers(selector, nameMatcher).map((m3) => m3.matcher)
    );
  }
  get matchesAlways() {
    return this.allowAny && this.unbalancedBracketScopes.length === 0;
  }
  get matchesNever() {
    return this.balancedBracketScopes.length === 0 && !this.allowAny;
  }
  match(scopes) {
    for (const excluder of this.unbalancedBracketScopes) {
      if (excluder(scopes)) {
        return false;
      }
    }
    for (const includer of this.balancedBracketScopes) {
      if (includer(scopes)) {
        return true;
      }
    }
    return this.allowAny;
  }
};
var LineTokens = class {
  constructor(emitBinaryTokens, lineText, tokenTypeOverrides, balancedBracketSelectors) {
    __publicField(this, "_emitBinaryTokens");
    /**
     * defined only if `false`.
     */
    __publicField(this, "_lineText");
    /**
     * used only if `_emitBinaryTokens` is false.
     */
    __publicField(this, "_tokens");
    /**
     * used only if `_emitBinaryTokens` is true.
     */
    __publicField(this, "_binaryTokens");
    __publicField(this, "_lastTokenEndIndex");
    __publicField(this, "_tokenTypeOverrides");
    this.balancedBracketSelectors = balancedBracketSelectors;
    this._emitBinaryTokens = emitBinaryTokens;
    this._tokenTypeOverrides = tokenTypeOverrides;
    if (false) {
      this._lineText = lineText;
    } else {
      this._lineText = null;
    }
    this._tokens = [];
    this._binaryTokens = [];
    this._lastTokenEndIndex = 0;
  }
  produce(stack, endIndex) {
    this.produceFromScopes(stack.contentNameScopesList, endIndex);
  }
  produceFromScopes(scopesList, endIndex) {
    var _a9;
    if (this._lastTokenEndIndex >= endIndex) {
      return;
    }
    if (this._emitBinaryTokens) {
      let metadata = (scopesList == null ? void 0 : scopesList.tokenAttributes) ?? 0;
      let containsBalancedBrackets = false;
      if ((_a9 = this.balancedBracketSelectors) == null ? void 0 : _a9.matchesAlways) {
        containsBalancedBrackets = true;
      }
      if (this._tokenTypeOverrides.length > 0 || this.balancedBracketSelectors && !this.balancedBracketSelectors.matchesAlways && !this.balancedBracketSelectors.matchesNever) {
        const scopes2 = (scopesList == null ? void 0 : scopesList.getScopeNames()) ?? [];
        for (const tokenType of this._tokenTypeOverrides) {
          if (tokenType.matcher(scopes2)) {
            metadata = EncodedTokenMetadata.set(
              metadata,
              0,
              toOptionalTokenType(tokenType.type),
              null,
              -1,
              0,
              0
            );
          }
        }
        if (this.balancedBracketSelectors) {
          containsBalancedBrackets = this.balancedBracketSelectors.match(scopes2);
        }
      }
      if (containsBalancedBrackets) {
        metadata = EncodedTokenMetadata.set(
          metadata,
          0,
          8,
          containsBalancedBrackets,
          -1,
          0,
          0
        );
      }
      if (this._binaryTokens.length > 0 && this._binaryTokens[this._binaryTokens.length - 1] === metadata) {
        this._lastTokenEndIndex = endIndex;
        return;
      }
      this._binaryTokens.push(this._lastTokenEndIndex);
      this._binaryTokens.push(metadata);
      this._lastTokenEndIndex = endIndex;
      return;
    }
    const scopes = (scopesList == null ? void 0 : scopesList.getScopeNames()) ?? [];
    this._tokens.push({
      startIndex: this._lastTokenEndIndex,
      endIndex,
      // value: lineText.substring(lastTokenEndIndex, endIndex),
      scopes
    });
    this._lastTokenEndIndex = endIndex;
  }
  getResult(stack, lineLength) {
    if (this._tokens.length > 0 && this._tokens[this._tokens.length - 1].startIndex === lineLength - 1) {
      this._tokens.pop();
    }
    if (this._tokens.length === 0) {
      this._lastTokenEndIndex = -1;
      this.produce(stack, lineLength);
      this._tokens[this._tokens.length - 1].startIndex = 0;
    }
    return this._tokens;
  }
  getBinaryResult(stack, lineLength) {
    if (this._binaryTokens.length > 0 && this._binaryTokens[this._binaryTokens.length - 2] === lineLength - 1) {
      this._binaryTokens.pop();
      this._binaryTokens.pop();
    }
    if (this._binaryTokens.length === 0) {
      this._lastTokenEndIndex = -1;
      this.produce(stack, lineLength);
      this._binaryTokens[this._binaryTokens.length - 2] = 0;
    }
    const result = new Uint32Array(this._binaryTokens.length);
    for (let i2 = 0, len = this._binaryTokens.length; i2 < len; i2++) {
      result[i2] = this._binaryTokens[i2];
    }
    return result;
  }
};
var SyncRegistry = class {
  constructor(theme, _onigLib) {
    __publicField(this, "_grammars", /* @__PURE__ */ new Map());
    __publicField(this, "_rawGrammars", /* @__PURE__ */ new Map());
    __publicField(this, "_injectionGrammars", /* @__PURE__ */ new Map());
    __publicField(this, "_theme");
    this._onigLib = _onigLib;
    this._theme = theme;
  }
  dispose() {
    for (const grammar of this._grammars.values()) {
      grammar.dispose();
    }
  }
  setTheme(theme) {
    this._theme = theme;
  }
  getColorMap() {
    return this._theme.getColorMap();
  }
  /**
   * Add `grammar` to registry and return a list of referenced scope names
   */
  addGrammar(grammar, injectionScopeNames) {
    this._rawGrammars.set(grammar.scopeName, grammar);
    if (injectionScopeNames) {
      this._injectionGrammars.set(grammar.scopeName, injectionScopeNames);
    }
  }
  /**
   * Lookup a raw grammar.
   */
  lookup(scopeName) {
    return this._rawGrammars.get(scopeName);
  }
  /**
   * Returns the injections for the given grammar
   */
  injections(targetScope) {
    return this._injectionGrammars.get(targetScope);
  }
  /**
   * Get the default theme settings
   */
  getDefaults() {
    return this._theme.getDefaults();
  }
  /**
   * Match a scope in the theme.
   */
  themeMatch(scopePath) {
    return this._theme.match(scopePath);
  }
  /**
   * Lookup a grammar.
   */
  grammarForScopeName(scopeName, initialLanguage, embeddedLanguages, tokenTypes, balancedBracketSelectors) {
    if (!this._grammars.has(scopeName)) {
      let rawGrammar = this._rawGrammars.get(scopeName);
      if (!rawGrammar) {
        return null;
      }
      this._grammars.set(scopeName, createGrammar(
        scopeName,
        rawGrammar,
        initialLanguage,
        embeddedLanguages,
        tokenTypes,
        balancedBracketSelectors,
        this,
        this._onigLib
      ));
    }
    return this._grammars.get(scopeName);
  }
};
var Registry = class {
  constructor(options) {
    __publicField(this, "_options");
    __publicField(this, "_syncRegistry");
    __publicField(this, "_ensureGrammarCache");
    this._options = options;
    this._syncRegistry = new SyncRegistry(
      Theme.createFromRawTheme(options.theme, options.colorMap),
      options.onigLib
    );
    this._ensureGrammarCache = /* @__PURE__ */ new Map();
  }
  dispose() {
    this._syncRegistry.dispose();
  }
  /**
   * Change the theme. Once called, no previous `ruleStack` should be used anymore.
   */
  setTheme(theme, colorMap) {
    this._syncRegistry.setTheme(Theme.createFromRawTheme(theme, colorMap));
  }
  /**
   * Returns a lookup array for color ids.
   */
  getColorMap() {
    return this._syncRegistry.getColorMap();
  }
  /**
   * Load the grammar for `scopeName` and all referenced included grammars asynchronously.
   * Please do not use language id 0.
   */
  loadGrammarWithEmbeddedLanguages(initialScopeName, initialLanguage, embeddedLanguages) {
    return this.loadGrammarWithConfiguration(initialScopeName, initialLanguage, { embeddedLanguages });
  }
  /**
   * Load the grammar for `scopeName` and all referenced included grammars asynchronously.
   * Please do not use language id 0.
   */
  loadGrammarWithConfiguration(initialScopeName, initialLanguage, configuration) {
    return this._loadGrammar(
      initialScopeName,
      initialLanguage,
      configuration.embeddedLanguages,
      configuration.tokenTypes,
      new BalancedBracketSelectors(
        configuration.balancedBracketSelectors || [],
        configuration.unbalancedBracketSelectors || []
      )
    );
  }
  /**
   * Load the grammar for `scopeName` and all referenced included grammars asynchronously.
   */
  loadGrammar(initialScopeName) {
    return this._loadGrammar(initialScopeName, 0, null, null, null);
  }
  _loadGrammar(initialScopeName, initialLanguage, embeddedLanguages, tokenTypes, balancedBracketSelectors) {
    const dependencyProcessor = new ScopeDependencyProcessor(this._syncRegistry, initialScopeName);
    while (dependencyProcessor.Q.length > 0) {
      dependencyProcessor.Q.map((request) => this._loadSingleGrammar(request.scopeName));
      dependencyProcessor.processQueue();
    }
    return this._grammarForScopeName(
      initialScopeName,
      initialLanguage,
      embeddedLanguages,
      tokenTypes,
      balancedBracketSelectors
    );
  }
  _loadSingleGrammar(scopeName) {
    if (!this._ensureGrammarCache.has(scopeName)) {
      this._doLoadSingleGrammar(scopeName);
      this._ensureGrammarCache.set(scopeName, true);
    }
  }
  _doLoadSingleGrammar(scopeName) {
    const grammar = this._options.loadGrammar(scopeName);
    if (grammar) {
      const injections = typeof this._options.getInjections === "function" ? this._options.getInjections(scopeName) : void 0;
      this._syncRegistry.addGrammar(grammar, injections);
    }
  }
  /**
   * Adds a rawGrammar.
   */
  addGrammar(rawGrammar, injections = [], initialLanguage = 0, embeddedLanguages = null) {
    this._syncRegistry.addGrammar(rawGrammar, injections);
    return this._grammarForScopeName(rawGrammar.scopeName, initialLanguage, embeddedLanguages);
  }
  /**
   * Get the grammar for `scopeName`. The grammar must first be created via `loadGrammar` or `addGrammar`.
   */
  _grammarForScopeName(scopeName, initialLanguage = 0, embeddedLanguages = null, tokenTypes = null, balancedBracketSelectors = null) {
    return this._syncRegistry.grammarForScopeName(
      scopeName,
      initialLanguage,
      embeddedLanguages,
      tokenTypes,
      balancedBracketSelectors
    );
  }
};
var INITIAL = StateStackImpl.NULL;

// ../../node_modules/.pnpm/@shikijs+primitive@4.4.3/node_modules/@shikijs/primitive/dist/index.mjs
function resolveColorReplacements(theme, options) {
  const replacements = typeof theme === "string" ? {} : { ...theme.colorReplacements };
  const themeName = typeof theme === "string" ? theme : theme.name;
  for (const [key2, value] of Object.entries((options == null ? void 0 : options.colorReplacements) || {})) if (typeof value === "string") replacements[key2] = value;
  else if (key2 === themeName) Object.assign(replacements, value);
  return replacements;
}
function applyColorReplacements(color, replacements) {
  if (!color) return color;
  return (replacements == null ? void 0 : replacements[color == null ? void 0 : color.toLowerCase()]) || color;
}
function toArray(x3) {
  return Array.isArray(x3) ? x3 : [x3];
}
async function normalizeGetter(p2) {
  return Promise.resolve(typeof p2 === "function" ? p2() : p2).then((r4) => r4.default || r4);
}
function isPlainLang(lang) {
  return !lang || [
    "plaintext",
    "txt",
    "text",
    "plain"
  ].includes(lang);
}
function isSpecialLang(lang) {
  return lang === "ansi" || isPlainLang(lang);
}
function isNoneTheme(theme) {
  return theme === "none";
}
function isSpecialTheme(theme) {
  return isNoneTheme(theme);
}
var RE_NEWLINE = /(\r?\n)/g;
function splitLines2(code, preserveEnding = false) {
  var _a9;
  if (code.length === 0) return [["", 0]];
  const parts = code.split(RE_NEWLINE);
  let index = 0;
  const lines = [];
  for (let i2 = 0; i2 < parts.length; i2 += 2) {
    const line = preserveEnding ? parts[i2] + (parts[i2 + 1] || "") : parts[i2];
    lines.push([line, index]);
    index += parts[i2].length;
    index += ((_a9 = parts[i2 + 1]) == null ? void 0 : _a9.length) || 0;
  }
  return lines;
}
var VSCODE_FALLBACK_EDITOR_FG = {
  light: "#333333",
  dark: "#bbbbbb"
};
var VSCODE_FALLBACK_EDITOR_BG = {
  light: "#fffffe",
  dark: "#1e1e1e"
};
var RESOLVED_KEY = "__shiki_resolved";
function normalizeTheme(rawTheme) {
  var _a9, _b, _c, _d, _e;
  if (rawTheme == null ? void 0 : rawTheme[RESOLVED_KEY]) return rawTheme;
  const theme = { ...rawTheme };
  if (theme.tokenColors && !theme.settings) {
    theme.settings = theme.tokenColors;
    delete theme.tokenColors;
  }
  theme.type || (theme.type = "dark");
  theme.colorReplacements = { ...theme.colorReplacements };
  theme.settings || (theme.settings = []);
  let { bg, fg } = theme;
  if (!bg || !fg) {
    const globalSetting = theme.settings ? theme.settings.find((s2) => !s2.name && !s2.scope) : void 0;
    if ((_a9 = globalSetting == null ? void 0 : globalSetting.settings) == null ? void 0 : _a9.foreground) fg = globalSetting.settings.foreground;
    if ((_b = globalSetting == null ? void 0 : globalSetting.settings) == null ? void 0 : _b.background) bg = globalSetting.settings.background;
    if (!fg && ((_c = theme == null ? void 0 : theme.colors) == null ? void 0 : _c["editor.foreground"])) fg = theme.colors["editor.foreground"];
    if (!bg && ((_d = theme == null ? void 0 : theme.colors) == null ? void 0 : _d["editor.background"])) bg = theme.colors["editor.background"];
    if (!fg) fg = theme.type === "light" ? VSCODE_FALLBACK_EDITOR_FG.light : VSCODE_FALLBACK_EDITOR_FG.dark;
    if (!bg) bg = theme.type === "light" ? VSCODE_FALLBACK_EDITOR_BG.light : VSCODE_FALLBACK_EDITOR_BG.dark;
    theme.fg = fg;
    theme.bg = bg;
  }
  if (!(theme.settings[0] && theme.settings[0].settings && !theme.settings[0].scope)) theme.settings.unshift({ settings: {
    foreground: theme.fg,
    background: theme.bg
  } });
  let replacementCount = 0;
  const replacementMap = /* @__PURE__ */ new Map();
  function getReplacementColor(value) {
    var _a10;
    if (replacementMap.has(value)) return replacementMap.get(value);
    replacementCount += 1;
    const hex = `#${replacementCount.toString(16).padStart(8, "0").toLowerCase()}`;
    if ((_a10 = theme.colorReplacements) == null ? void 0 : _a10[`#${hex}`]) return getReplacementColor(value);
    replacementMap.set(value, hex);
    return hex;
  }
  theme.settings = theme.settings.map((setting) => {
    var _a10, _b2;
    const replaceFg = ((_a10 = setting.settings) == null ? void 0 : _a10.foreground) && !setting.settings.foreground.startsWith("#");
    const replaceBg = ((_b2 = setting.settings) == null ? void 0 : _b2.background) && !setting.settings.background.startsWith("#");
    if (!replaceFg && !replaceBg) return setting;
    const clone2 = {
      ...setting,
      settings: { ...setting.settings }
    };
    if (replaceFg) {
      const replacement = getReplacementColor(setting.settings.foreground);
      theme.colorReplacements[replacement] = setting.settings.foreground;
      clone2.settings.foreground = replacement;
    }
    if (replaceBg) {
      const replacement = getReplacementColor(setting.settings.background);
      theme.colorReplacements[replacement] = setting.settings.background;
      clone2.settings.background = replacement;
    }
    return clone2;
  });
  for (const key2 of Object.keys(theme.colors || {})) if (key2 === "editor.foreground" || key2 === "editor.background" || key2.startsWith("terminal.ansi")) {
    if (!((_e = theme.colors[key2]) == null ? void 0 : _e.startsWith("#"))) {
      const replacement = getReplacementColor(theme.colors[key2]);
      theme.colorReplacements[replacement] = theme.colors[key2];
      theme.colors[key2] = replacement;
    }
  }
  Object.defineProperty(theme, RESOLVED_KEY, {
    enumerable: false,
    writable: false,
    value: true
  });
  return theme;
}
async function resolveLangs(langs) {
  return [...new Set((await Promise.all(langs.filter((l3) => !isSpecialLang(l3)).map(async (lang) => await normalizeGetter(lang).then((r4) => Array.isArray(r4) ? r4 : [r4])))).flat())];
}
async function resolveThemes(themes2) {
  return (await Promise.all(themes2.map(async (theme) => isSpecialTheme(theme) ? null : normalizeTheme(await normalizeGetter(theme))))).filter((i2) => !!i2);
}
function resolveLangAlias(name, alias) {
  if (!alias) return name;
  if (alias[name]) {
    const resolved = /* @__PURE__ */ new Set([name]);
    while (alias[name]) {
      name = alias[name];
      if (resolved.has(name)) throw new ShikiError2(`Circular alias \`${[...resolved].join(" -> ")} -> ${name}\``);
      resolved.add(name);
    }
  }
  return name;
}
var Registry2 = class extends Registry {
  constructor(_resolver, _themes, _langs, _alias = {}) {
    super(_resolver);
    __publicField(this, "_resolver");
    __publicField(this, "_themes");
    __publicField(this, "_langs");
    __publicField(this, "_alias");
    __publicField(this, "_resolvedThemes", /* @__PURE__ */ new Map());
    __publicField(this, "_resolvedGrammars", /* @__PURE__ */ new Map());
    __publicField(this, "_langMap", /* @__PURE__ */ new Map());
    __publicField(this, "_langGraph", /* @__PURE__ */ new Map());
    __publicField(this, "_textmateThemeCache", /* @__PURE__ */ new WeakMap());
    __publicField(this, "_loadedThemesCache", null);
    __publicField(this, "_loadedLanguagesCache", null);
    this._resolver = _resolver;
    this._themes = _themes;
    this._langs = _langs;
    this._alias = _alias;
    this._themes.map((t) => this.loadTheme(t));
    this.loadLanguages(this._langs);
  }
  getTheme(theme) {
    if (typeof theme === "string") return this._resolvedThemes.get(theme);
    else return this.loadTheme(theme);
  }
  loadTheme(theme) {
    const _theme = normalizeTheme(theme);
    if (_theme.name) {
      this._resolvedThemes.set(_theme.name, _theme);
      this._loadedThemesCache = null;
    }
    return _theme;
  }
  getLoadedThemes() {
    if (!this._loadedThemesCache) this._loadedThemesCache = [...this._resolvedThemes.keys()];
    return this._loadedThemesCache;
  }
  setTheme(theme) {
    let textmateTheme = this._textmateThemeCache.get(theme);
    if (!textmateTheme) {
      textmateTheme = Theme.createFromRawTheme(theme);
      this._textmateThemeCache.set(theme, textmateTheme);
    }
    this._syncRegistry.setTheme(textmateTheme);
  }
  getGrammar(name) {
    name = resolveLangAlias(name, this._alias);
    return this._resolvedGrammars.get(name);
  }
  loadLanguage(lang) {
    var _a9, _b, _c, _d;
    if (this.getGrammar(lang.name)) return;
    const embeddedLazilyBy = new Set([...this._langMap.values()].filter((i2) => {
      var _a10;
      return (_a10 = i2.embeddedLangsLazy) == null ? void 0 : _a10.includes(lang.name);
    }));
    this._resolver.addLanguage(lang);
    const grammarConfig = {
      balancedBracketSelectors: lang.balancedBracketSelectors || ["*"],
      unbalancedBracketSelectors: lang.unbalancedBracketSelectors || []
    };
    this._syncRegistry._rawGrammars.set(lang.scopeName, lang);
    const g = this.loadGrammarWithConfiguration(lang.scopeName, 1, grammarConfig);
    g.name = lang.name;
    this._resolvedGrammars.set(lang.name, g);
    if (lang.aliases) lang.aliases.forEach((alias) => {
      this._alias[alias] = lang.name;
    });
    this._loadedLanguagesCache = null;
    if (embeddedLazilyBy.size) for (const e of embeddedLazilyBy) {
      this._resolvedGrammars.delete(e.name);
      this._loadedLanguagesCache = null;
      (_b = (_a9 = this._syncRegistry) == null ? void 0 : _a9._injectionGrammars) == null ? void 0 : _b.delete(e.scopeName);
      (_d = (_c = this._syncRegistry) == null ? void 0 : _c._grammars) == null ? void 0 : _d.delete(e.scopeName);
      this.loadLanguage(this._langMap.get(e.name));
    }
  }
  dispose() {
    super.dispose();
    this._resolvedThemes.clear();
    this._resolvedGrammars.clear();
    this._langMap.clear();
    this._langGraph.clear();
    this._loadedThemesCache = null;
  }
  loadLanguages(langs) {
    for (const lang of langs) this.resolveEmbeddedLanguages(lang);
    const langsGraphArray = [...this._langGraph.entries()];
    const missingLangs = langsGraphArray.filter(([_3, lang]) => !lang);
    if (missingLangs.length) {
      const dependents = langsGraphArray.filter(([_3, lang]) => {
        var _a9;
        if (!lang) return false;
        return (_a9 = lang.embeddedLanguages || lang.embeddedLangs) == null ? void 0 : _a9.some((l3) => missingLangs.map(([name]) => name).includes(l3));
      }).filter((lang) => !missingLangs.includes(lang));
      throw new ShikiError2(`Missing languages ${missingLangs.map(([name]) => `\`${name}\``).join(", ")}, required by ${dependents.map(([name]) => `\`${name}\``).join(", ")}`);
    }
    for (const [_3, lang] of langsGraphArray) this._resolver.addLanguage(lang);
    for (const [_3, lang] of langsGraphArray) this.loadLanguage(lang);
  }
  getLoadedLanguages() {
    if (!this._loadedLanguagesCache) this._loadedLanguagesCache = [.../* @__PURE__ */ new Set([...this._resolvedGrammars.keys(), ...Object.keys(this._alias)])];
    return this._loadedLanguagesCache;
  }
  resolveEmbeddedLanguages(lang) {
    this._langMap.set(lang.name, lang);
    this._langGraph.set(lang.name, lang);
    const embedded = lang.embeddedLanguages ?? lang.embeddedLangs;
    if (embedded) for (const embeddedLang of embedded) this._langGraph.set(embeddedLang, this._langMap.get(embeddedLang));
  }
};
var Resolver = class {
  constructor(engine, langs) {
    __publicField(this, "_langs", /* @__PURE__ */ new Map());
    __publicField(this, "_scopeToLang", /* @__PURE__ */ new Map());
    __publicField(this, "_injections", /* @__PURE__ */ new Map());
    __publicField(this, "_onigLib");
    this._onigLib = {
      createOnigScanner: (patterns) => engine.createScanner(patterns),
      createOnigString: (s2) => engine.createString(s2)
    };
    langs.forEach((i2) => this.addLanguage(i2));
  }
  get onigLib() {
    return this._onigLib;
  }
  getLangRegistration(langIdOrAlias) {
    return this._langs.get(langIdOrAlias);
  }
  loadGrammar(scopeName) {
    return this._scopeToLang.get(scopeName);
  }
  addLanguage(l3) {
    this._langs.set(l3.name, l3);
    if (l3.aliases) l3.aliases.forEach((a2) => {
      this._langs.set(a2, l3);
    });
    this._scopeToLang.set(l3.scopeName, l3);
    if (l3.injectTo) l3.injectTo.forEach((i2) => {
      if (!this._injections.get(i2)) this._injections.set(i2, []);
      this._injections.get(i2).push(l3.scopeName);
    });
  }
  getInjections(scopeName) {
    const scopeParts = scopeName.split(".");
    let injections = [];
    for (let i2 = 1; i2 <= scopeParts.length; i2++) {
      const subScopeName = scopeParts.slice(0, i2).join(".");
      injections = [...injections, ...this._injections.get(subScopeName) || []];
    }
    return injections;
  }
};
var instancesCount = 0;
function createShikiPrimitive(options) {
  instancesCount += 1;
  if (options.warnings !== false && instancesCount >= 10 && instancesCount % 10 === 0) console.warn(`[Shiki] ${instancesCount} instances have been created. Shiki is supposed to be used as a singleton, consider refactoring your code to cache your highlighter instance; Or call \`highlighter.dispose()\` to release unused instances.`);
  let isDisposed = false;
  if (!options.engine) throw new ShikiError2("`engine` option is required for synchronous mode");
  const langs = (options.langs || []).flat(1);
  const themes2 = (options.themes || []).flat(1).map(normalizeTheme);
  const _registry = new Registry2(new Resolver(options.engine, langs), themes2, langs, options.langAlias);
  let _lastTheme;
  function resolveLangAlias$1(name) {
    return resolveLangAlias(name, options.langAlias);
  }
  function getLanguage(name) {
    ensureNotDisposed();
    const _lang = _registry.getGrammar(typeof name === "string" ? name : name.name);
    if (!_lang) throw new ShikiError2(`Language \`${name}\` not found, you may need to load it first`);
    return _lang;
  }
  function getTheme(name) {
    if (name === "none") return {
      bg: "",
      fg: "",
      name: "none",
      settings: [],
      type: "dark"
    };
    ensureNotDisposed();
    const _theme = _registry.getTheme(name);
    if (!_theme) throw new ShikiError2(`Theme \`${name}\` not found, you may need to load it first`);
    return _theme;
  }
  function setTheme(name) {
    ensureNotDisposed();
    const theme = getTheme(name);
    if (_lastTheme !== name) {
      _registry.setTheme(theme);
      _lastTheme = name;
    }
    return {
      theme,
      colorMap: _registry.getColorMap()
    };
  }
  function getLoadedThemes() {
    ensureNotDisposed();
    return _registry.getLoadedThemes();
  }
  function getLoadedLanguages() {
    ensureNotDisposed();
    return _registry.getLoadedLanguages();
  }
  function loadLanguageSync(...langs2) {
    ensureNotDisposed();
    _registry.loadLanguages(langs2.flat(1));
  }
  async function loadLanguage(...langs2) {
    return loadLanguageSync(await resolveLangs(langs2));
  }
  function loadThemeSync(...themes3) {
    ensureNotDisposed();
    for (const theme of themes3.flat(1)) _registry.loadTheme(theme);
  }
  async function loadTheme(...themes3) {
    ensureNotDisposed();
    return loadThemeSync(await resolveThemes(themes3));
  }
  function ensureNotDisposed() {
    if (isDisposed) throw new ShikiError2("Shiki instance has been disposed");
  }
  function dispose() {
    if (isDisposed) return;
    isDisposed = true;
    _registry.dispose();
    instancesCount -= 1;
  }
  return {
    setTheme,
    getTheme,
    getLanguage,
    getLoadedThemes,
    getLoadedLanguages,
    resolveLangAlias: resolveLangAlias$1,
    loadLanguage,
    loadLanguageSync,
    loadTheme,
    loadThemeSync,
    dispose,
    [Symbol.dispose]: dispose
  };
}
async function createShikiPrimitiveAsync(options) {
  if (!options.engine) console.warn("`engine` option is required. Use `createOnigurumaEngine` or `createJavaScriptRegexEngine` to create an engine.");
  const [themes2, langs, engine] = await Promise.all([
    resolveThemes(options.themes || []),
    resolveLangs(options.langs || []),
    options.engine
  ]);
  return createShikiPrimitive({
    ...options,
    themes: themes2,
    langs,
    engine
  });
}
var _grammarStateMap = /* @__PURE__ */ new WeakMap();
function setLastGrammarStateToMap(keys, state) {
  _grammarStateMap.set(keys, state);
}
function getLastGrammarStateFromMap(keys) {
  return _grammarStateMap.get(keys);
}
var GrammarState = class GrammarState2 {
  constructor(...args) {
    /**
    * Theme to Stack mapping
    */
    __publicField(this, "_stacks", {});
    __publicField(this, "lang");
    if (args.length === 2) {
      const [stacksMap, lang] = args;
      this.lang = lang;
      this._stacks = stacksMap;
    } else {
      const [stack, lang, theme] = args;
      this.lang = lang;
      this._stacks = { [theme]: stack };
    }
  }
  get themes() {
    return Object.keys(this._stacks);
  }
  get theme() {
    return this.themes[0];
  }
  get _stack() {
    return this._stacks[this.theme];
  }
  /**
  * Static method to create a initial grammar state.
  */
  static initial(lang, themes2) {
    return new GrammarState2(Object.fromEntries(toArray(themes2).map((theme) => [theme, INITIAL])), lang);
  }
  /**
  * Get the internal stack object.
  * @internal
  */
  getInternalStack(theme = this.theme) {
    return this._stacks[theme];
  }
  getScopes(theme = this.theme) {
    return getScopes(this._stacks[theme]);
  }
  toJSON() {
    return {
      lang: this.lang,
      theme: this.theme,
      themes: this.themes,
      scopes: this.getScopes()
    };
  }
};
function getScopes(stack) {
  const scopes = [];
  const visited = /* @__PURE__ */ new Set();
  function pushScope(stack2) {
    var _a9;
    if (visited.has(stack2)) return;
    visited.add(stack2);
    const name = (_a9 = stack2 == null ? void 0 : stack2.nameScopesList) == null ? void 0 : _a9.scopeName;
    if (name) scopes.push(name);
    if (stack2.parent) pushScope(stack2.parent);
  }
  pushScope(stack);
  return scopes;
}
function getGrammarStack(state, theme) {
  if (!(state instanceof GrammarState)) throw new ShikiError2("Invalid grammar state");
  return state.getInternalStack(theme);
}
var RE_COMMA = /,/;
var RE_SPACE = / /;
function codeToTokensBase(primitive, code, options = {}) {
  const { theme: themeName = primitive.getLoadedThemes()[0] } = options;
  if (isPlainLang(primitive.resolveLangAlias(options.lang || "text")) || isNoneTheme(themeName)) return splitLines2(code).map((line) => [{
    content: line[0],
    offset: line[1]
  }]);
  const { theme, colorMap } = primitive.setTheme(themeName);
  const _grammar = primitive.getLanguage(options.lang || "text");
  if (options.grammarState) {
    if (options.grammarState.lang !== _grammar.name) throw new ShikiError2(`Grammar state language "${options.grammarState.lang}" does not match highlight language "${_grammar.name}"`);
    if (!options.grammarState.themes.includes(theme.name)) throw new ShikiError2(`Grammar state themes "${options.grammarState.themes}" do not contain highlight theme "${theme.name}"`);
  }
  return tokenizeWithTheme(code, _grammar, theme, colorMap, options);
}
function getLastGrammarState(...args) {
  if (args.length === 2) return getLastGrammarStateFromMap(args[1]);
  const [primitive, code, options = {}] = args;
  const { lang = "text", theme: themeName = primitive.getLoadedThemes()[0] } = options;
  if (isPlainLang(lang) || isNoneTheme(themeName)) throw new ShikiError2("Plain language does not have grammar state");
  if (lang === "ansi") throw new ShikiError2("ANSI language does not have grammar state");
  const { theme, colorMap } = primitive.setTheme(themeName);
  const _grammar = primitive.getLanguage(lang);
  return new GrammarState(_tokenizeWithTheme(code, _grammar, theme, colorMap, options).stateStack, _grammar.name, theme.name);
}
function tokenizeWithTheme(code, grammar, theme, colorMap, options) {
  const result = _tokenizeWithTheme(code, grammar, theme, colorMap, options);
  const grammarState = new GrammarState(result.stateStack, grammar.name, theme.name);
  setLastGrammarStateToMap(result.tokens, grammarState);
  return result.tokens;
}
function _tokenizeWithTheme(code, grammar, theme, colorMap, options) {
  const colorReplacements = resolveColorReplacements(theme, options);
  const { tokenizeMaxLineLength = 0, tokenizeTimeLimit = 500, includeExplanation = false } = options;
  const lines = splitLines2(code);
  let stateStack = options.grammarState ? getGrammarStack(options.grammarState, theme.name) ?? INITIAL : options.grammarContextCode != null ? _tokenizeWithTheme(options.grammarContextCode, grammar, theme, colorMap, {
    ...options,
    grammarState: void 0,
    grammarContextCode: void 0
  }).stateStack : INITIAL;
  let actual = [];
  const final = [];
  for (let i2 = 0, len = lines.length; i2 < len; i2++) {
    const [line, lineOffset] = lines[i2];
    if (line === "") {
      actual = [];
      final.push([]);
      continue;
    }
    if (tokenizeMaxLineLength > 0 && line.length >= tokenizeMaxLineLength) {
      actual = [];
      final.push([{
        content: line,
        offset: lineOffset,
        color: "",
        fontStyle: 0
      }]);
      continue;
    }
    let resultWithScopes;
    let tokensWithScopes;
    let tokensWithScopesIndex;
    if (includeExplanation && includeExplanation !== "tokenType") {
      resultWithScopes = grammar.tokenizeLine(line, stateStack, tokenizeTimeLimit);
      tokensWithScopes = resultWithScopes.tokens;
      tokensWithScopesIndex = 0;
    }
    const result = grammar.tokenizeLine2(line, stateStack, tokenizeTimeLimit);
    const tokensLength = result.tokens.length / 2;
    for (let j2 = 0; j2 < tokensLength; j2++) {
      const startIndex = result.tokens[2 * j2];
      const nextStartIndex = j2 + 1 < tokensLength ? result.tokens[2 * j2 + 2] : line.length;
      if (startIndex === nextStartIndex) continue;
      const metadata = result.tokens[2 * j2 + 1];
      const color = applyColorReplacements(colorMap[EncodedTokenMetadata.getForeground(metadata)], colorReplacements);
      const fontStyle = EncodedTokenMetadata.getFontStyle(metadata);
      const token2 = {
        content: line.substring(startIndex, nextStartIndex),
        offset: lineOffset + startIndex,
        color,
        fontStyle
      };
      if (includeExplanation === "tokenType") token2.type = EncodedTokenMetadata.getTokenType(metadata);
      else if (includeExplanation) {
        const themeSettingsSelectors = [];
        if (includeExplanation !== "scopeName") for (const setting of theme.settings) {
          let selectors;
          switch (typeof setting.scope) {
            case "string":
              selectors = setting.scope.split(RE_COMMA).map((scope) => scope.trim());
              break;
            case "object":
              selectors = setting.scope;
              break;
            default:
              continue;
          }
          themeSettingsSelectors.push({
            settings: setting,
            selectors: selectors.map((selector) => selector.split(RE_SPACE))
          });
        }
        token2.explanation = [];
        let offset = 0;
        while (startIndex + offset < nextStartIndex) {
          const tokenWithScopes = tokensWithScopes[tokensWithScopesIndex];
          const tokenWithScopesText = line.substring(tokenWithScopes.startIndex, tokenWithScopes.endIndex);
          offset += tokenWithScopesText.length;
          token2.explanation.push({
            content: tokenWithScopesText,
            scopes: includeExplanation === "scopeName" ? explainThemeScopesNameOnly(tokenWithScopes.scopes) : explainThemeScopesFull(themeSettingsSelectors, tokenWithScopes.scopes)
          });
          tokensWithScopesIndex += 1;
        }
      }
      actual.push(token2);
    }
    final.push(actual);
    actual = [];
    stateStack = result.ruleStack;
  }
  return {
    tokens: final,
    stateStack
  };
}
function explainThemeScopesNameOnly(scopes) {
  return scopes.map((scope) => ({ scopeName: scope }));
}
function explainThemeScopesFull(themeSelectors, scopes) {
  const result = [];
  for (let i2 = 0, len = scopes.length; i2 < len; i2++) {
    const scope = scopes[i2];
    result[i2] = {
      scopeName: scope,
      themeMatches: explainThemeScope(themeSelectors, scope, scopes.slice(0, i2))
    };
  }
  return result;
}
function matchesOne(selector, scope) {
  return selector === scope || scope.substring(0, selector.length) === selector && scope[selector.length] === ".";
}
function matches(selectors, scope, parentScopes) {
  if (!matchesOne(selectors.at(-1), scope)) return false;
  let selectorParentIndex = selectors.length - 2;
  let parentIndex = parentScopes.length - 1;
  while (selectorParentIndex >= 0 && parentIndex >= 0) {
    if (matchesOne(selectors[selectorParentIndex], parentScopes[parentIndex])) selectorParentIndex -= 1;
    parentIndex -= 1;
  }
  if (selectorParentIndex === -1) return true;
  return false;
}
function explainThemeScope(themeSettingsSelectors, scope, parentScopes) {
  const result = [];
  for (const { selectors, settings } of themeSettingsSelectors) for (const selectorPieces of selectors) if (matches(selectorPieces, scope, parentScopes)) {
    result.push(settings);
    break;
  }
  return result;
}
function codeToTokensWithThemes(primitive, code, options, codeToTokensBaseFn = codeToTokensBase) {
  const themes2 = Object.entries(options.themes).filter((i2) => i2[1]).map((i2) => ({
    color: i2[0],
    theme: i2[1]
  }));
  const themedTokens = themes2.map((t) => {
    const tokens2 = codeToTokensBaseFn(primitive, code, {
      ...options,
      theme: t.theme
    });
    return {
      tokens: tokens2,
      state: getLastGrammarStateFromMap(tokens2),
      theme: typeof t.theme === "string" ? t.theme : t.theme.name
    };
  });
  const tokens = alignThemesTokenization(...themedTokens.map((i2) => i2.tokens));
  const mergedTokens = tokens[0].map((line, lineIdx) => line.map((_token, tokenIdx) => {
    const mergedToken = {
      content: _token.content,
      variants: {},
      offset: _token.offset
    };
    if ("includeExplanation" in options && options.includeExplanation) mergedToken.explanation = _token.explanation;
    tokens.forEach((t, themeIdx) => {
      const { content: _3, explanation: __, offset: ___, ...styles } = t[lineIdx][tokenIdx];
      mergedToken.variants[themes2[themeIdx].color] = styles;
    });
    return mergedToken;
  }));
  const mergedGrammarState = themedTokens[0].state ? new GrammarState(Object.fromEntries(themedTokens.map((s2) => {
    var _a9;
    return [s2.theme, (_a9 = s2.state) == null ? void 0 : _a9.getInternalStack(s2.theme)];
  })), themedTokens[0].state.lang) : void 0;
  if (mergedGrammarState) setLastGrammarStateToMap(mergedTokens, mergedGrammarState);
  return mergedTokens;
}
function alignThemesTokenization(...themes2) {
  const outThemes = themes2.map(() => []);
  const count = themes2.length;
  for (let i2 = 0; i2 < themes2[0].length; i2++) {
    const lines = themes2.map((t) => t[i2]);
    const outLines = outThemes.map(() => []);
    outThemes.forEach((t, i3) => t.push(outLines[i3]));
    const indexes = lines.map(() => 0);
    const current = lines.map((l3) => l3[0]);
    while (current.every((t) => t)) {
      const minLength = Math.min(...current.map((t) => t.content.length));
      for (let n = 0; n < count; n++) {
        const token2 = current[n];
        if (token2.content.length === minLength) {
          outLines[n].push(token2);
          indexes[n] += 1;
          current[n] = lines[n][indexes[n]];
        } else {
          outLines[n].push({
            ...token2,
            content: token2.content.slice(0, minLength)
          });
          current[n] = {
            ...token2,
            content: token2.content.slice(minLength),
            offset: token2.offset + minLength
          };
        }
      }
    }
  }
  return outThemes;
}

// ../../node_modules/.pnpm/@shikijs+core@4.4.3/node_modules/@shikijs/core/dist/index.mjs
var RE_WHITESPACE = /\s+/g;
function addClassToHast(node, className) {
  var _a9;
  if (!className) return node;
  node.properties || (node.properties = {});
  (_a9 = node.properties).class || (_a9.class = []);
  if (typeof node.properties.class === "string") node.properties.class = node.properties.class.split(RE_WHITESPACE);
  if (!Array.isArray(node.properties.class)) node.properties.class = [];
  const targets = Array.isArray(className) ? className : className.split(RE_WHITESPACE);
  for (const c of targets) if (c && !node.properties.class.includes(c)) node.properties.class.push(c);
  return node;
}
var RE_LANG_ATTR = /:?lang=["']([^"']+)["']/g;
var RE_CODE_FENCE = /(?:```|~~~)([\w-]+)/g;
var RE_LATEX_BEGIN = /\\begin\{([\w-]+)\}/g;
var RE_SCRIPT_LANG = /<script\s+(?:type|lang)=["']([^"']+)["']/gi;
var RE_FRONTMATTER = /^\s*---\r?\n[\s\S]*?\r?\n---(?:\r?\n|\s*$)/;
function createPositionConverter(code) {
  const lines = splitLines2(code, true).map(([line]) => line);
  function indexToPos(index) {
    if (index === code.length) return {
      line: lines.length - 1,
      character: lines.at(-1).length
    };
    let character = index;
    let line = 0;
    for (const lineText of lines) {
      if (character < lineText.length) break;
      character -= lineText.length;
      line++;
    }
    return {
      line,
      character
    };
  }
  function posToIndex(line, character) {
    let index = 0;
    for (let i2 = 0; i2 < line; i2++) index += lines[i2].length;
    index += character;
    return index;
  }
  return {
    lines,
    indexToPos,
    posToIndex
  };
}
function guessEmbeddedLanguages(code, _lang, highlighter2) {
  const langs = /* @__PURE__ */ new Set();
  for (const match of code.matchAll(RE_LANG_ATTR)) {
    const lang = match[1].toLowerCase().trim();
    if (lang) langs.add(lang);
  }
  for (const match of code.matchAll(RE_CODE_FENCE)) {
    const lang = match[1].toLowerCase().trim();
    if (lang) langs.add(lang);
  }
  for (const match of code.matchAll(RE_LATEX_BEGIN)) {
    const lang = match[1].toLowerCase().trim();
    if (lang) langs.add(lang);
  }
  for (const match of code.matchAll(RE_SCRIPT_LANG)) {
    const fullType = match[1].toLowerCase().trim();
    const lang = fullType.includes("/") ? fullType.split("/").pop() : fullType;
    if (lang) langs.add(lang);
  }
  if (RE_FRONTMATTER.test(code)) langs.add("yaml");
  if (!highlighter2) return [...langs];
  const bundle = highlighter2.getBundledLanguages();
  return [...langs].filter((l3) => l3 && bundle[l3]);
}
var COLOR_KEYS = ["color", "background-color"];
function splitToken(token2, offsets) {
  let lastOffset = 0;
  const tokens = [];
  for (const offset of offsets) {
    if (offset > lastOffset) tokens.push({
      ...token2,
      content: token2.content.slice(lastOffset, offset),
      offset: token2.offset + lastOffset
    });
    lastOffset = offset;
  }
  if (lastOffset < token2.content.length) tokens.push({
    ...token2,
    content: token2.content.slice(lastOffset),
    offset: token2.offset + lastOffset
  });
  return tokens;
}
function findFirstBreakpointAfter(breakpoints, offset) {
  let start = 0;
  let end = breakpoints.length;
  while (start < end) {
    const middle = start + (end - start >> 1);
    if (breakpoints[middle] <= offset) start = middle + 1;
    else end = middle;
  }
  return start;
}
function splitTokens(tokens, breakpoints) {
  const sorted = [...breakpoints instanceof Set ? breakpoints : new Set(breakpoints)].sort((a2, b3) => a2 - b3);
  if (!sorted.length) return tokens;
  return tokens.map((line) => {
    return line.flatMap((token2) => {
      const tokenEnd = token2.offset + token2.content.length;
      const start = findFirstBreakpointAfter(sorted, token2.offset);
      let end = start;
      while (end < sorted.length && sorted[end] < tokenEnd) end++;
      if (start === end) return token2;
      const breakpointsInToken = sorted.slice(start, end);
      for (let index = 0; index < breakpointsInToken.length; index++) breakpointsInToken[index] -= token2.offset;
      return splitToken(token2, breakpointsInToken);
    });
  });
}
function flatTokenVariants(merged, variantsOrder, cssVariablePrefix, defaultColor, colorsRendering = "css-vars") {
  const token2 = {
    content: merged.content,
    explanation: merged.explanation,
    offset: merged.offset
  };
  const styles = variantsOrder.map((t) => getTokenStyleObject(merged.variants[t]));
  const styleKeys = new Set(styles.flatMap((t) => Object.keys(t)));
  const mergedStyles = {};
  const varKey = (idx, key2) => {
    const keyName = key2 === "color" ? "" : key2 === "background-color" ? "-bg" : `-${key2}`;
    return cssVariablePrefix + variantsOrder[idx] + (key2 === "color" ? "" : keyName);
  };
  styles.forEach((cur, idx) => {
    for (const key2 of styleKeys) {
      const value = cur[key2] || "inherit";
      if (idx === 0 && defaultColor && COLOR_KEYS.includes(key2)) if (defaultColor === "light-dark()" && styles.length > 1) {
        const lightIndex = variantsOrder.findIndex((t) => t === "light");
        const darkIndex = variantsOrder.findIndex((t) => t === "dark");
        if (lightIndex === -1 || darkIndex === -1) throw new ShikiError2('When using `defaultColor: "light-dark()"`, you must provide both `light` and `dark` themes');
        const lightValue = styles[lightIndex][key2] || "inherit";
        const darkValue = styles[darkIndex][key2] || "inherit";
        mergedStyles[key2] = `light-dark(${lightValue}, ${darkValue})`;
        if (colorsRendering === "css-vars") mergedStyles[varKey(idx, key2)] = value;
      } else mergedStyles[key2] = value;
      else if (colorsRendering === "css-vars") mergedStyles[varKey(idx, key2)] = value;
    }
  });
  token2.htmlStyle = mergedStyles;
  return token2;
}
function getTokenStyleObject(token2) {
  const styles = {};
  if (token2.color) styles.color = token2.color;
  if (token2.bgColor) styles["background-color"] = token2.bgColor;
  if (token2.fontStyle) {
    if (token2.fontStyle & FontStyle.Italic) styles["font-style"] = "italic";
    if (token2.fontStyle & FontStyle.Bold) styles["font-weight"] = "bold";
    const decorations2 = [];
    if (token2.fontStyle & FontStyle.Underline) decorations2.push("underline");
    if (token2.fontStyle & FontStyle.Strikethrough) decorations2.push("line-through");
    if (decorations2.length) styles["text-decoration"] = decorations2.join(" ");
  }
  return styles;
}
function stringifyTokenStyle(token2) {
  if (typeof token2 === "string") return token2;
  return Object.entries(token2).map(([key2, value]) => `${key2}:${value}`).join(";");
}
function transformerDecorations() {
  const map = /* @__PURE__ */ new WeakMap();
  function getContext(shiki) {
    if (!map.has(shiki.meta)) {
      let normalizePosition = function(p2) {
        if (typeof p2 === "number") {
          if (p2 < 0 || p2 > shiki.source.length) throw new ShikiError2(`Invalid decoration offset: ${p2}. Code length: ${shiki.source.length}`);
          return {
            ...converter.indexToPos(p2),
            offset: p2
          };
        } else {
          const line = converter.lines[p2.line];
          if (line === void 0) throw new ShikiError2(`Invalid decoration position ${JSON.stringify(p2)}. Lines length: ${converter.lines.length}`);
          let character = p2.character;
          if (character < 0) character = line.length + character;
          if (character < 0 || character > line.length) throw new ShikiError2(`Invalid decoration position ${JSON.stringify(p2)}. Line ${p2.line} length: ${line.length}`);
          return {
            ...p2,
            character,
            offset: converter.posToIndex(p2.line, character)
          };
        }
      };
      const converter = createPositionConverter(shiki.source);
      const decorations2 = (shiki.options.decorations || []).map((d2) => ({
        ...d2,
        start: normalizePosition(d2.start),
        end: normalizePosition(d2.end)
      }));
      verifyIntersections(decorations2);
      map.set(shiki.meta, {
        decorations: decorations2,
        converter,
        source: shiki.source
      });
    }
    return map.get(shiki.meta);
  }
  return {
    name: "shiki:decorations",
    tokens(tokens) {
      var _a9;
      if (!((_a9 = this.options.decorations) == null ? void 0 : _a9.length)) return;
      return splitTokens(tokens, getContext(this).decorations.flatMap((d2) => [d2.start.offset, d2.end.offset]));
    },
    code(codeEl) {
      var _a9;
      if (!((_a9 = this.options.decorations) == null ? void 0 : _a9.length)) return;
      const ctx = getContext(this);
      const lines = [...codeEl.children].filter((i2) => i2.type === "element" && i2.tagName === "span");
      if (lines.length !== ctx.converter.lines.length) throw new ShikiError2(`Number of lines in code element (${lines.length}) does not match the number of lines in the source (${ctx.converter.lines.length}). Failed to apply decorations.`);
      function applyLineSection(line, start, end, decoration) {
        const lineEl = lines[line];
        let text2 = "";
        let startIndex = -1;
        let endIndex = -1;
        if (start === 0) startIndex = 0;
        if (end === 0) endIndex = 0;
        if (end === Number.POSITIVE_INFINITY) endIndex = lineEl.children.length;
        if (startIndex === -1 || endIndex === -1) for (let i2 = 0; i2 < lineEl.children.length; i2++) {
          text2 += stringify3(lineEl.children[i2]);
          if (startIndex === -1 && text2.length === start) startIndex = i2 + 1;
          if (endIndex === -1 && text2.length === end) endIndex = i2 + 1;
        }
        if (startIndex === -1) throw new ShikiError2(`Failed to find start index for decoration ${JSON.stringify(decoration.start)}`);
        if (endIndex === -1) throw new ShikiError2(`Failed to find end index for decoration ${JSON.stringify(decoration.end)}`);
        const children = lineEl.children.slice(startIndex, endIndex);
        if (!decoration.alwaysWrap && children.length === lineEl.children.length) applyDecoration(lineEl, decoration, "line");
        else if (!decoration.alwaysWrap && children.length === 1 && children[0].type === "element") applyDecoration(children[0], decoration, "token");
        else {
          const wrapper = {
            type: "element",
            tagName: "span",
            properties: {},
            children
          };
          applyDecoration(wrapper, decoration, "wrapper");
          lineEl.children.splice(startIndex, children.length, wrapper);
        }
      }
      function applyLine(line, decoration) {
        lines[line] = applyDecoration(lines[line], decoration, "line");
      }
      function applyDecoration(el, decoration, type) {
        var _a10;
        const properties = decoration.properties || {};
        const transform2 = decoration.transform || ((i2) => i2);
        el.tagName = decoration.tagName || "span";
        el.properties = {
          ...el.properties,
          ...properties,
          class: el.properties.class
        };
        if ((_a10 = decoration.properties) == null ? void 0 : _a10.class) addClassToHast(el, decoration.properties.class);
        el = transform2(el, type) || el;
        return el;
      }
      const lineApplies = [];
      const sorted = ctx.decorations.sort((a2, b3) => b3.start.offset - a2.start.offset || a2.end.offset - b3.end.offset);
      for (const decoration of sorted) {
        const { start, end } = decoration;
        if (start.line === end.line) applyLineSection(start.line, start.character, end.character, decoration);
        else if (start.line < end.line) {
          applyLineSection(start.line, start.character, Number.POSITIVE_INFINITY, decoration);
          for (let i2 = start.line + 1; i2 < end.line; i2++) lineApplies.unshift(() => applyLine(i2, decoration));
          applyLineSection(end.line, 0, end.character, decoration);
        }
      }
      lineApplies.forEach((i2) => i2());
    }
  };
}
function verifyIntersections(items) {
  for (let i2 = 0; i2 < items.length; i2++) {
    const foo = items[i2];
    if (foo.start.offset > foo.end.offset) throw new ShikiError2(`Invalid decoration range: ${JSON.stringify(foo.start)} - ${JSON.stringify(foo.end)}`);
    for (let j2 = i2 + 1; j2 < items.length; j2++) {
      const bar = items[j2];
      const isFooHasBarStart = foo.start.offset <= bar.start.offset && bar.start.offset < foo.end.offset;
      const isFooHasBarEnd = foo.start.offset < bar.end.offset && bar.end.offset <= foo.end.offset;
      const isBarHasFooStart = bar.start.offset <= foo.start.offset && foo.start.offset < bar.end.offset;
      const isBarHasFooEnd = bar.start.offset < foo.end.offset && foo.end.offset <= bar.end.offset;
      if (isFooHasBarStart || isFooHasBarEnd || isBarHasFooStart || isBarHasFooEnd) {
        if (isFooHasBarStart && isFooHasBarEnd) continue;
        if (isBarHasFooStart && isBarHasFooEnd) continue;
        if (isBarHasFooStart && foo.start.offset === foo.end.offset) continue;
        if (isFooHasBarEnd && bar.start.offset === bar.end.offset) continue;
        throw new ShikiError2(`Decorations ${JSON.stringify(foo.start)} and ${JSON.stringify(bar.start)} intersect.`);
      }
    }
  }
}
function stringify3(el) {
  if (el.type === "text") return el.value;
  if (el.type === "element") return el.children.map(stringify3).join("");
  return "";
}
var builtInTransformers = [transformerDecorations()];
function getTransformers(options) {
  const transformers = sortTransformersByEnforcement(options.transformers || []);
  return [
    ...transformers.pre,
    ...transformers.normal,
    ...transformers.post,
    ...builtInTransformers
  ];
}
function sortTransformersByEnforcement(transformers) {
  const pre = [];
  const post = [];
  const normal = [];
  for (const transformer of transformers) switch (transformer.enforce) {
    case "pre":
      pre.push(transformer);
      break;
    case "post":
      post.push(transformer);
      break;
    default:
      normal.push(transformer);
  }
  return {
    pre,
    post,
    normal
  };
}
var namedColors = [
  "black",
  "red",
  "green",
  "yellow",
  "blue",
  "magenta",
  "cyan",
  "white",
  "brightBlack",
  "brightRed",
  "brightGreen",
  "brightYellow",
  "brightBlue",
  "brightMagenta",
  "brightCyan",
  "brightWhite"
];
var decorations = {
  1: "bold",
  2: "dim",
  3: "italic",
  4: "underline",
  7: "reverse",
  8: "hidden",
  9: "strikethrough"
};
function findSequence(value, position) {
  const nextEscape = value.indexOf("\x1B", position);
  if (nextEscape !== -1) {
    if (value[nextEscape + 1] === "[") {
      const nextClose = value.indexOf("m", nextEscape);
      if (nextClose !== -1) return {
        sequence: value.substring(nextEscape + 2, nextClose).split(";"),
        startPosition: nextEscape,
        position: nextClose + 1
      };
    }
  }
  return { position: value.length };
}
function parseColor(sequence) {
  const colorMode = sequence.shift();
  if (colorMode === "2") {
    const rgb = sequence.splice(0, 3).map((x3) => Number.parseInt(x3));
    if (rgb.length !== 3 || rgb.some((x3) => Number.isNaN(x3))) return;
    return {
      type: "rgb",
      rgb
    };
  } else if (colorMode === "5") {
    const index = sequence.shift();
    if (index) return {
      type: "table",
      index: Number(index)
    };
  }
}
function parseSequence(sequence) {
  const commands = [];
  while (sequence.length > 0) {
    const code = sequence.shift();
    if (!code) continue;
    const codeInt = Number.parseInt(code);
    if (Number.isNaN(codeInt)) continue;
    if (codeInt === 0) commands.push({ type: "resetAll" });
    else if (codeInt <= 9) {
      if (decorations[codeInt]) commands.push({
        type: "setDecoration",
        value: decorations[codeInt]
      });
    } else if (codeInt <= 29) {
      const decoration = decorations[codeInt - 20];
      if (decoration) {
        commands.push({
          type: "resetDecoration",
          value: decoration
        });
        if (decoration === "dim") commands.push({
          type: "resetDecoration",
          value: "bold"
        });
      }
    } else if (codeInt <= 37) commands.push({
      type: "setForegroundColor",
      value: {
        type: "named",
        name: namedColors[codeInt - 30]
      }
    });
    else if (codeInt === 38) {
      const color = parseColor(sequence);
      if (color) commands.push({
        type: "setForegroundColor",
        value: color
      });
    } else if (codeInt === 39) commands.push({ type: "resetForegroundColor" });
    else if (codeInt <= 47) commands.push({
      type: "setBackgroundColor",
      value: {
        type: "named",
        name: namedColors[codeInt - 40]
      }
    });
    else if (codeInt === 48) {
      const color = parseColor(sequence);
      if (color) commands.push({
        type: "setBackgroundColor",
        value: color
      });
    } else if (codeInt === 49) commands.push({ type: "resetBackgroundColor" });
    else if (codeInt === 53) commands.push({
      type: "setDecoration",
      value: "overline"
    });
    else if (codeInt === 55) commands.push({
      type: "resetDecoration",
      value: "overline"
    });
    else if (codeInt >= 90 && codeInt <= 97) commands.push({
      type: "setForegroundColor",
      value: {
        type: "named",
        name: namedColors[codeInt - 90 + 8]
      }
    });
    else if (codeInt >= 100 && codeInt <= 107) commands.push({
      type: "setBackgroundColor",
      value: {
        type: "named",
        name: namedColors[codeInt - 100 + 8]
      }
    });
  }
  return commands;
}
function createAnsiSequenceParser() {
  let foreground = null;
  let background = null;
  let decorations2 = /* @__PURE__ */ new Set();
  return { parse(value) {
    const tokens = [];
    let position = 0;
    do {
      const findResult = findSequence(value, position);
      const text2 = findResult.sequence ? value.substring(position, findResult.startPosition) : value.substring(position);
      if (text2.length > 0) tokens.push({
        value: text2,
        foreground,
        background,
        decorations: new Set(decorations2)
      });
      if (findResult.sequence) {
        const commands = parseSequence(findResult.sequence);
        for (const styleToken of commands) if (styleToken.type === "resetAll") {
          foreground = null;
          background = null;
          decorations2.clear();
        } else if (styleToken.type === "resetForegroundColor") foreground = null;
        else if (styleToken.type === "resetBackgroundColor") background = null;
        else if (styleToken.type === "resetDecoration") decorations2.delete(styleToken.value);
        for (const styleToken of commands) if (styleToken.type === "setForegroundColor") foreground = styleToken.value;
        else if (styleToken.type === "setBackgroundColor") background = styleToken.value;
        else if (styleToken.type === "setDecoration") decorations2.add(styleToken.value);
      }
      position = findResult.position;
    } while (position < value.length);
    return tokens;
  } };
}
var defaultNamedColorsMap = {
  black: "#000000",
  red: "#bb0000",
  green: "#00bb00",
  yellow: "#bbbb00",
  blue: "#0000bb",
  magenta: "#ff00ff",
  cyan: "#00bbbb",
  white: "#eeeeee",
  brightBlack: "#555555",
  brightRed: "#ff5555",
  brightGreen: "#00ff00",
  brightYellow: "#ffff55",
  brightBlue: "#5555ff",
  brightMagenta: "#ff55ff",
  brightCyan: "#55ffff",
  brightWhite: "#ffffff"
};
function createColorPalette(namedColorsMap = defaultNamedColorsMap) {
  function namedColor(name) {
    return namedColorsMap[name];
  }
  function rgbColor(rgb) {
    return `#${rgb.map((x3) => Math.max(0, Math.min(x3, 255)).toString(16).padStart(2, "0")).join("")}`;
  }
  let colorTable;
  function getColorTable() {
    if (colorTable) return colorTable;
    colorTable = [];
    for (let i2 = 0; i2 < namedColors.length; i2++) colorTable.push(namedColor(namedColors[i2]));
    let levels = [
      0,
      95,
      135,
      175,
      215,
      255
    ];
    for (let r4 = 0; r4 < 6; r4++) for (let g = 0; g < 6; g++) for (let b3 = 0; b3 < 6; b3++) colorTable.push(rgbColor([
      levels[r4],
      levels[g],
      levels[b3]
    ]));
    let level = 8;
    for (let i2 = 0; i2 < 24; i2++, level += 10) colorTable.push(rgbColor([
      level,
      level,
      level
    ]));
    return colorTable;
  }
  function tableColor(index) {
    return getColorTable()[index];
  }
  function value(color) {
    switch (color.type) {
      case "named":
        return namedColor(color.name);
      case "rgb":
        return rgbColor(color.rgb);
      case "table":
        return tableColor(color.index);
    }
  }
  return { value };
}
var RE_HEX_COLOR = /#([0-9a-f]{3,8})/i;
var RE_CSS_VAR_ANSI = /var\((--[\w-]+-ansi-[\w-]+)\)/;
var defaultAnsiColors = {
  black: "#000000",
  red: "#cd3131",
  green: "#0DBC79",
  yellow: "#E5E510",
  blue: "#2472C8",
  magenta: "#BC3FBC",
  cyan: "#11A8CD",
  white: "#E5E5E5",
  brightBlack: "#666666",
  brightRed: "#F14C4C",
  brightGreen: "#23D18B",
  brightYellow: "#F5F543",
  brightBlue: "#3B8EEA",
  brightMagenta: "#D670D6",
  brightCyan: "#29B8DB",
  brightWhite: "#FFFFFF"
};
function tokenizeAnsiWithTheme(theme, fileContents, options) {
  const colorReplacements = resolveColorReplacements(theme, options);
  const lines = splitLines2(fileContents);
  const colorPalette = createColorPalette(Object.fromEntries(namedColors.map((name) => {
    var _a9;
    const key2 = `terminal.ansi${name[0].toUpperCase()}${name.substring(1)}`;
    return [name, ((_a9 = theme.colors) == null ? void 0 : _a9[key2]) || defaultAnsiColors[name]];
  })));
  const parser = createAnsiSequenceParser();
  return lines.map((line) => parser.parse(line[0]).map((token2) => {
    let color;
    let bgColor;
    if (token2.decorations.has("reverse")) {
      color = token2.background ? colorPalette.value(token2.background) : theme.bg;
      bgColor = token2.foreground ? colorPalette.value(token2.foreground) : theme.fg;
    } else {
      color = token2.foreground ? colorPalette.value(token2.foreground) : theme.fg;
      bgColor = token2.background ? colorPalette.value(token2.background) : void 0;
    }
    color = applyColorReplacements(color, colorReplacements);
    bgColor = applyColorReplacements(bgColor, colorReplacements);
    if (token2.decorations.has("dim")) color = dimColor(color);
    let fontStyle = FontStyle.None;
    if (token2.decorations.has("bold")) fontStyle |= FontStyle.Bold;
    if (token2.decorations.has("italic")) fontStyle |= FontStyle.Italic;
    if (token2.decorations.has("underline")) fontStyle |= FontStyle.Underline;
    if (token2.decorations.has("strikethrough")) fontStyle |= FontStyle.Strikethrough;
    return {
      content: token2.value,
      offset: line[1],
      color,
      bgColor,
      fontStyle
    };
  }));
}
function dimColor(color) {
  const hexMatch = color.match(RE_HEX_COLOR);
  if (hexMatch) {
    const hex = hexMatch[1];
    if (hex.length === 8) {
      const alpha = Math.round(Number.parseInt(hex.slice(6, 8), 16) / 2).toString(16).padStart(2, "0");
      return `#${hex.slice(0, 6)}${alpha}`;
    } else if (hex.length === 6) return `#${hex}80`;
    else if (hex.length === 4) {
      const r4 = hex[0];
      const g = hex[1];
      const b3 = hex[2];
      const a2 = hex[3];
      return `#${r4}${r4}${g}${g}${b3}${b3}${Math.round(Number.parseInt(`${a2}${a2}`, 16) / 2).toString(16).padStart(2, "0")}`;
    } else if (hex.length === 3) {
      const r4 = hex[0];
      const g = hex[1];
      const b3 = hex[2];
      return `#${r4}${r4}${g}${g}${b3}${b3}80`;
    }
  }
  const cssVarMatch = color.match(RE_CSS_VAR_ANSI);
  if (cssVarMatch) return `var(${cssVarMatch[1]}-dim)`;
  return color;
}
function codeToTokensBase2(primitive, code, options = {}) {
  const lang = primitive.resolveLangAlias(options.lang || "text");
  const { theme: themeName = primitive.getLoadedThemes()[0] } = options;
  if (!isPlainLang(lang) && !isNoneTheme(themeName) && lang === "ansi") {
    const { theme } = primitive.setTheme(themeName);
    return tokenizeAnsiWithTheme(theme, code, options);
  }
  return codeToTokensBase(primitive, code, options);
}
function codeToTokens(primitive, code, options) {
  let bg;
  let fg;
  let tokens;
  let themeName;
  let rootStyle;
  let grammarState;
  if ("themes" in options) {
    const { defaultColor = "light", cssVariablePrefix = "--shiki-", colorsRendering = "css-vars" } = options;
    const themes2 = Object.entries(options.themes).filter((i2) => i2[1]).map((i2) => ({
      color: i2[0],
      theme: i2[1]
    })).sort((a2, b3) => a2.color === defaultColor ? -1 : b3.color === defaultColor ? 1 : 0);
    if (themes2.length === 0) throw new ShikiError2("`themes` option must not be empty");
    const themeTokens = codeToTokensWithThemes(primitive, code, options, codeToTokensBase2);
    grammarState = getLastGrammarStateFromMap(themeTokens);
    if (defaultColor && "light-dark()" !== defaultColor && !themes2.some((t) => t.color === defaultColor)) throw new ShikiError2(`\`themes\` option must contain the defaultColor key \`${defaultColor}\``);
    const themeRegs = themes2.map((t) => primitive.getTheme(t.theme));
    const themesOrder = themes2.map((t) => t.color);
    tokens = themeTokens.map((line) => line.map((token2) => flatTokenVariants(token2, themesOrder, cssVariablePrefix, defaultColor, colorsRendering)));
    if (grammarState) setLastGrammarStateToMap(tokens, grammarState);
    const themeColorReplacements = themes2.map((t) => resolveColorReplacements(t.theme, options));
    fg = mapThemeColors(themes2, themeRegs, themeColorReplacements, cssVariablePrefix, defaultColor, "fg", colorsRendering);
    bg = mapThemeColors(themes2, themeRegs, themeColorReplacements, cssVariablePrefix, defaultColor, "bg", colorsRendering);
    themeName = `shiki-themes ${themeRegs.map((t) => t.name).join(" ")}`;
    rootStyle = defaultColor ? void 0 : [fg, bg].join(";");
  } else if ("theme" in options) {
    const colorReplacements = resolveColorReplacements(options.theme, options);
    tokens = codeToTokensBase2(primitive, code, options);
    const _theme = primitive.getTheme(options.theme);
    bg = applyColorReplacements(_theme.bg, colorReplacements);
    fg = applyColorReplacements(_theme.fg, colorReplacements);
    themeName = _theme.name;
    grammarState = getLastGrammarStateFromMap(tokens);
  } else throw new ShikiError2("Invalid options, either `theme` or `themes` must be provided");
  return {
    tokens,
    fg,
    bg,
    themeName,
    rootStyle,
    grammarState
  };
}
function mapThemeColors(themes2, themeRegs, themeColorReplacements, cssVariablePrefix, defaultColor, property, colorsRendering) {
  return themes2.map((t, idx) => {
    const value = applyColorReplacements(themeRegs[idx][property], themeColorReplacements[idx]) || "inherit";
    const cssVar = `${cssVariablePrefix + t.color}${property === "bg" ? "-bg" : ""}:${value}`;
    if (idx === 0 && defaultColor) {
      if (defaultColor === "light-dark()" && themes2.length > 1) {
        const lightIndex = themes2.findIndex((t2) => t2.color === "light");
        const darkIndex = themes2.findIndex((t2) => t2.color === "dark");
        if (lightIndex === -1 || darkIndex === -1) throw new ShikiError2('When using `defaultColor: "light-dark()"`, you must provide both `light` and `dark` themes');
        return `light-dark(${applyColorReplacements(themeRegs[lightIndex][property], themeColorReplacements[lightIndex]) || "inherit"}, ${applyColorReplacements(themeRegs[darkIndex][property], themeColorReplacements[darkIndex]) || "inherit"});${cssVar}`;
      }
      return value;
    }
    if (colorsRendering === "css-vars") return cssVar;
    return null;
  }).filter((i2) => !!i2).join(";");
}
var RE_WHITESPACE_ONLY = /^\s+$/;
var RE_LEADING_TRAILING_WHITESPACE = /^(\s*)(.*?)(\s*)$/;
function codeToHast(primitive, code, options, transformerContext = {
  meta: {},
  options,
  codeToHast: (_code, _options) => codeToHast(primitive, _code, _options),
  codeToTokens: (_code, _options) => codeToTokens(primitive, _code, _options)
}) {
  var _a9, _b;
  let input = code;
  for (const transformer of getTransformers(options)) input = ((_a9 = transformer.preprocess) == null ? void 0 : _a9.call(transformerContext, input, options)) || input;
  let { tokens, fg, bg, themeName, rootStyle, grammarState } = codeToTokens(primitive, input, options);
  const { mergeWhitespaces = true, mergeSameStyleTokens = false } = options;
  if (mergeWhitespaces === true) tokens = mergeWhitespaceTokens(tokens);
  else if (mergeWhitespaces === "never") tokens = splitWhitespaceTokens(tokens);
  if (mergeSameStyleTokens) tokens = mergeAdjacentStyledTokens(tokens);
  const contextSource = {
    ...transformerContext,
    get source() {
      return input;
    }
  };
  for (const transformer of getTransformers(options)) tokens = ((_b = transformer.tokens) == null ? void 0 : _b.call(contextSource, tokens)) || tokens;
  return tokensToHast(tokens, {
    ...options,
    fg,
    bg,
    themeName,
    rootStyle: options.rootStyle === false ? false : options.rootStyle ?? rootStyle
  }, contextSource, grammarState);
}
function tokensToHast(tokens, options, transformerContext, grammarState = getLastGrammarStateFromMap(tokens)) {
  var _a9, _b, _c, _d;
  const transformers = getTransformers(options);
  const lines = [];
  const root2 = {
    type: "root",
    children: []
  };
  const { structure = "classic", tabindex = "0" } = options;
  const properties = { class: `shiki ${options.themeName || ""}` };
  if (options.rootStyle !== false) if (options.rootStyle != null) properties.style = options.rootStyle;
  else properties.style = `background-color:${options.bg};color:${options.fg}`;
  if (tabindex !== false && tabindex != null) properties.tabindex = tabindex.toString();
  for (const [key2, value] of Object.entries(options.meta || {})) if (!key2.startsWith("_")) properties[key2] = value;
  let preNode = {
    type: "element",
    tagName: "pre",
    properties,
    children: [],
    data: options.data
  };
  let codeNode = {
    type: "element",
    tagName: "code",
    properties: {},
    children: lines
  };
  const lineNodes = [];
  const context = {
    ...transformerContext,
    structure,
    addClassToHast,
    get source() {
      return transformerContext.source;
    },
    get tokens() {
      return tokens;
    },
    get options() {
      return options;
    },
    get root() {
      return root2;
    },
    get pre() {
      return preNode;
    },
    get code() {
      return codeNode;
    },
    get lines() {
      return lineNodes;
    }
  };
  tokens.forEach((line, idx) => {
    var _a10, _b2;
    if (idx) {
      if (structure === "inline") root2.children.push({
        type: "element",
        tagName: "br",
        properties: {},
        children: []
      });
      else if (structure === "classic") lines.push({
        type: "text",
        value: "\n"
      });
    }
    let lineNode = {
      type: "element",
      tagName: "span",
      properties: { class: "line" },
      children: []
    };
    let col = 0;
    for (const token2 of line) {
      let tokenNode = {
        type: "element",
        tagName: "span",
        properties: { ...token2.htmlAttrs },
        children: [{
          type: "text",
          value: token2.content
        }]
      };
      const style = stringifyTokenStyle(token2.htmlStyle || getTokenStyleObject(token2));
      if (style) tokenNode.properties.style = style;
      for (const transformer of transformers) tokenNode = ((_a10 = transformer == null ? void 0 : transformer.span) == null ? void 0 : _a10.call(context, tokenNode, idx + 1, col, lineNode, token2)) || tokenNode;
      if (structure === "inline") root2.children.push(tokenNode);
      else if (structure === "classic") lineNode.children.push(tokenNode);
      col += token2.content.length;
    }
    if (structure === "classic") {
      for (const transformer of transformers) lineNode = ((_b2 = transformer == null ? void 0 : transformer.line) == null ? void 0 : _b2.call(context, lineNode, idx + 1)) || lineNode;
      lineNodes.push(lineNode);
      lines.push(lineNode);
    } else if (structure === "inline") lineNodes.push(lineNode);
  });
  if (structure === "classic") {
    for (const transformer of transformers) codeNode = ((_a9 = transformer == null ? void 0 : transformer.code) == null ? void 0 : _a9.call(context, codeNode)) || codeNode;
    preNode.children.push(codeNode);
    for (const transformer of transformers) preNode = ((_b = transformer == null ? void 0 : transformer.pre) == null ? void 0 : _b.call(context, preNode)) || preNode;
    root2.children.push(preNode);
  } else if (structure === "inline") {
    const syntheticLines = [];
    let currentLine = {
      type: "element",
      tagName: "span",
      properties: { class: "line" },
      children: []
    };
    for (const child of root2.children) if (child.type === "element" && child.tagName === "br") {
      syntheticLines.push(currentLine);
      currentLine = {
        type: "element",
        tagName: "span",
        properties: { class: "line" },
        children: []
      };
    } else if (child.type === "element" || child.type === "text") currentLine.children.push(child);
    syntheticLines.push(currentLine);
    let transformedCode = {
      type: "element",
      tagName: "code",
      properties: {},
      children: syntheticLines
    };
    for (const transformer of transformers) transformedCode = ((_c = transformer == null ? void 0 : transformer.code) == null ? void 0 : _c.call(context, transformedCode)) || transformedCode;
    root2.children = [];
    for (let i2 = 0; i2 < transformedCode.children.length; i2++) {
      if (i2 > 0) root2.children.push({
        type: "element",
        tagName: "br",
        properties: {},
        children: []
      });
      const line = transformedCode.children[i2];
      if (line.type === "element") root2.children.push(...line.children);
    }
  }
  let result = root2;
  for (const transformer of transformers) result = ((_d = transformer == null ? void 0 : transformer.root) == null ? void 0 : _d.call(context, result)) || result;
  if (grammarState) setLastGrammarStateToMap(result, grammarState);
  return result;
}
function mergeWhitespaceTokens(tokens) {
  return tokens.map((line) => {
    const newLine = [];
    let carryOnContent = "";
    let firstOffset;
    line.forEach((token2, idx) => {
      const couldMerge = !(token2.fontStyle && (token2.fontStyle & FontStyle.Underline || token2.fontStyle & FontStyle.Strikethrough));
      if (couldMerge && RE_WHITESPACE_ONLY.test(token2.content) && line[idx + 1]) {
        if (firstOffset === void 0) firstOffset = token2.offset;
        carryOnContent += token2.content;
      } else if (carryOnContent) {
        if (couldMerge) newLine.push({
          ...token2,
          offset: firstOffset,
          content: carryOnContent + token2.content
        });
        else newLine.push({
          content: carryOnContent,
          offset: firstOffset
        }, token2);
        firstOffset = void 0;
        carryOnContent = "";
      } else newLine.push(token2);
    });
    return newLine;
  });
}
function splitWhitespaceTokens(tokens) {
  return tokens.map((line) => {
    return line.flatMap((token2) => {
      if (RE_WHITESPACE_ONLY.test(token2.content)) return token2;
      const match = token2.content.match(RE_LEADING_TRAILING_WHITESPACE);
      if (!match) return token2;
      const [, leading, content, trailing] = match;
      if (!leading && !trailing) return token2;
      const expanded = [{
        ...token2,
        offset: token2.offset + leading.length,
        content
      }];
      if (leading) expanded.unshift({
        content: leading,
        offset: token2.offset
      });
      if (trailing) expanded.push({
        content: trailing,
        offset: token2.offset + leading.length + content.length
      });
      return expanded;
    });
  });
}
function mergeAdjacentStyledTokens(tokens) {
  return tokens.map((line) => {
    const newLine = [];
    for (const token2 of line) {
      if (newLine.length === 0) {
        newLine.push({ ...token2 });
        continue;
      }
      const prevToken = newLine.at(-1);
      const prevStyle = stringifyTokenStyle(prevToken.htmlStyle || getTokenStyleObject(prevToken));
      const currentStyle = stringifyTokenStyle(token2.htmlStyle || getTokenStyleObject(token2));
      const isPrevDecorated = prevToken.fontStyle && (prevToken.fontStyle & FontStyle.Underline || prevToken.fontStyle & FontStyle.Strikethrough);
      const isDecorated = token2.fontStyle && (token2.fontStyle & FontStyle.Underline || token2.fontStyle & FontStyle.Strikethrough);
      if (!isPrevDecorated && !isDecorated && prevStyle === currentStyle) prevToken.content += token2.content;
      else newLine.push({ ...token2 });
    }
    return newLine;
  });
}
var hastToHtml = toHtml;
function codeToHtml(primitive, code, options) {
  var _a9;
  const context = {
    meta: {},
    options,
    codeToHast: (_code, _options) => codeToHast(primitive, _code, _options),
    codeToTokens: (_code, _options) => codeToTokens(primitive, _code, _options)
  };
  let result = hastToHtml(codeToHast(primitive, code, options, context));
  for (const transformer of getTransformers(options)) result = ((_a9 = transformer.postprocess) == null ? void 0 : _a9.call(context, result, options)) || result;
  return result;
}
async function createHighlighterCore(options) {
  const primitive = await createShikiPrimitiveAsync(options);
  return {
    getLastGrammarState: (...args) => getLastGrammarState(primitive, ...args),
    codeToTokensBase: (code, options2) => codeToTokensBase2(primitive, code, options2),
    codeToTokensWithThemes: (code, options2) => codeToTokensWithThemes(primitive, code, options2),
    codeToTokens: (code, options2) => codeToTokens(primitive, code, options2),
    codeToHast: (code, options2) => codeToHast(primitive, code, options2),
    codeToHtml: (code, options2) => codeToHtml(primitive, code, options2),
    getBundledLanguages: () => ({}),
    getBundledThemes: () => ({}),
    ...primitive,
    getInternalContext: () => primitive
  };
}
function makeSingletonHighlighterCore(createHighlighter2) {
  let _shiki;
  async function getSingletonHighlighterCore2(options) {
    if (!_shiki) {
      _shiki = createHighlighter2({
        ...options,
        themes: options.themes || [],
        langs: options.langs || []
      });
      return _shiki;
    } else {
      const s2 = await _shiki;
      await Promise.all([s2.loadTheme(...options.themes || []), s2.loadLanguage(...options.langs || [])]);
      return s2;
    }
  }
  return getSingletonHighlighterCore2;
}
var getSingletonHighlighterCore = makeSingletonHighlighterCore(createHighlighterCore);
function createBundledHighlighter(options) {
  const bundledLanguages2 = options.langs;
  const bundledThemes2 = options.themes;
  const engine = options.engine;
  async function createHighlighter2(options2) {
    function resolveLang(lang) {
      var _a9;
      if (typeof lang === "string") {
        lang = ((_a9 = options2.langAlias) == null ? void 0 : _a9[lang]) || lang;
        if (isSpecialLang(lang)) return [];
        const bundle = bundledLanguages2[lang];
        if (!bundle) throw new ShikiError2(`Language \`${lang}\` is not included in this bundle. You may want to load it from external source.`);
        return bundle;
      }
      return lang;
    }
    function resolveTheme2(theme) {
      if (isSpecialTheme(theme)) return "none";
      if (typeof theme === "string") {
        const bundle = bundledThemes2[theme];
        if (!bundle) throw new ShikiError2(`Theme \`${theme}\` is not included in this bundle. You may want to load it from external source.`);
        return bundle;
      }
      return theme;
    }
    const _themes = (options2.themes ?? []).map((i2) => resolveTheme2(i2));
    const langs = (options2.langs ?? []).map((i2) => resolveLang(i2));
    const core2 = await createHighlighterCore({
      engine: options2.engine ?? engine(),
      ...options2,
      themes: _themes,
      langs
    });
    return {
      ...core2,
      loadLanguage(...langs2) {
        return core2.loadLanguage(...langs2.map(resolveLang));
      },
      loadTheme(...themes2) {
        return core2.loadTheme(...themes2.map(resolveTheme2));
      },
      getBundledLanguages() {
        return bundledLanguages2;
      },
      getBundledThemes() {
        return bundledThemes2;
      }
    };
  }
  return createHighlighter2;
}
function makeSingletonHighlighter(createHighlighter2) {
  let _shiki;
  async function getSingletonHighlighter2(options = {}) {
    if (!_shiki) {
      _shiki = createHighlighter2({
        ...options,
        themes: [],
        langs: []
      });
      const s2 = await _shiki;
      await Promise.all([s2.loadTheme(...options.themes || []), s2.loadLanguage(...options.langs || [])]);
      return s2;
    } else {
      const s2 = await _shiki;
      await Promise.all([s2.loadTheme(...options.themes || []), s2.loadLanguage(...options.langs || [])]);
      return s2;
    }
  }
  return getSingletonHighlighter2;
}
function createSingletonShorthands(createHighlighter2, config) {
  const getSingletonHighlighter2 = makeSingletonHighlighter(createHighlighter2);
  async function get(code, options) {
    var _a9;
    const shiki = await getSingletonHighlighter2({
      langs: [options.lang],
      themes: "theme" in options ? [options.theme] : Object.values(options.themes)
    });
    const langs = await ((_a9 = config == null ? void 0 : config.guessEmbeddedLanguages) == null ? void 0 : _a9.call(config, code, options.lang, shiki));
    if (langs) await shiki.loadLanguage(...langs);
    return shiki;
  }
  return {
    getSingletonHighlighter(options) {
      return getSingletonHighlighter2(options);
    },
    async codeToHtml(code, options) {
      return (await get(code, options)).codeToHtml(code, options);
    },
    async codeToHast(code, options) {
      return (await get(code, options)).codeToHast(code, options);
    },
    async codeToTokens(code, options) {
      return (await get(code, options)).codeToTokens(code, options);
    },
    async codeToTokensBase(code, options) {
      return (await get(code, options)).codeToTokensBase(code, options);
    },
    async codeToTokensWithThemes(code, options) {
      return (await get(code, options)).codeToTokensWithThemes(code, options);
    },
    async getLastGrammarState(code, options) {
      return (await getSingletonHighlighter2({
        langs: [options.lang],
        themes: [options.theme]
      })).getLastGrammarState(code, options);
    }
  };
}
function createCssVariablesTheme(options = {}) {
  var _a9;
  const { name = "css-variables", variablePrefix = "--shiki-", fontStyle = true } = options;
  const variable = (name2) => {
    var _a10;
    if ((_a10 = options.variableDefaults) == null ? void 0 : _a10[name2]) return `var(${variablePrefix}${name2}, ${options.variableDefaults[name2]})`;
    return `var(${variablePrefix}${name2})`;
  };
  const theme = {
    name,
    type: "dark",
    colors: {
      "editor.foreground": variable("foreground"),
      "editor.background": variable("background"),
      "terminal.ansiBlack": variable("ansi-black"),
      "terminal.ansiRed": variable("ansi-red"),
      "terminal.ansiGreen": variable("ansi-green"),
      "terminal.ansiYellow": variable("ansi-yellow"),
      "terminal.ansiBlue": variable("ansi-blue"),
      "terminal.ansiMagenta": variable("ansi-magenta"),
      "terminal.ansiCyan": variable("ansi-cyan"),
      "terminal.ansiWhite": variable("ansi-white"),
      "terminal.ansiBrightBlack": variable("ansi-bright-black"),
      "terminal.ansiBrightRed": variable("ansi-bright-red"),
      "terminal.ansiBrightGreen": variable("ansi-bright-green"),
      "terminal.ansiBrightYellow": variable("ansi-bright-yellow"),
      "terminal.ansiBrightBlue": variable("ansi-bright-blue"),
      "terminal.ansiBrightMagenta": variable("ansi-bright-magenta"),
      "terminal.ansiBrightCyan": variable("ansi-bright-cyan"),
      "terminal.ansiBrightWhite": variable("ansi-bright-white")
    },
    tokenColors: [
      {
        scope: [
          "keyword.operator.accessor",
          "meta.group.braces.round.function.arguments",
          "meta.template.expression",
          "markup.fenced_code meta.embedded.block"
        ],
        settings: { foreground: variable("foreground") }
      },
      {
        scope: "emphasis",
        settings: { fontStyle: "italic" }
      },
      {
        scope: [
          "strong",
          "markup.heading.markdown",
          "markup.bold.markdown"
        ],
        settings: { fontStyle: "bold" }
      },
      {
        scope: ["markup.italic.markdown"],
        settings: { fontStyle: "italic" }
      },
      {
        scope: "meta.link.inline.markdown",
        settings: {
          fontStyle: "underline",
          foreground: variable("token-link")
        }
      },
      {
        scope: [
          "string",
          "markup.fenced_code",
          "markup.inline"
        ],
        settings: { foreground: variable("token-string") }
      },
      {
        scope: ["comment", "string.quoted.docstring.multi"],
        settings: { foreground: variable("token-comment") }
      },
      {
        scope: [
          "constant.numeric",
          "constant.language",
          "constant.other.placeholder",
          "constant.character.format.placeholder",
          "variable.language.this",
          "variable.other.object",
          "variable.other.class",
          "variable.other.constant",
          "meta.property-name",
          "meta.property-value",
          "support"
        ],
        settings: { foreground: variable("token-constant") }
      },
      {
        scope: [
          "keyword",
          "storage.modifier",
          "storage.type",
          "storage.control.clojure",
          "entity.name.function.clojure",
          "entity.name.tag.yaml",
          "support.function.node",
          "support.type.property-name.json",
          "punctuation.separator.key-value",
          "punctuation.definition.template-expression"
        ],
        settings: { foreground: variable("token-keyword") }
      },
      {
        scope: "variable.parameter.function",
        settings: { foreground: variable("token-parameter") }
      },
      {
        scope: [
          "support.function",
          "entity.name.type",
          "entity.other.inherited-class",
          "meta.function-call",
          "meta.instance.constructor",
          "entity.other.attribute-name",
          "entity.name.function",
          "constant.keyword.clojure"
        ],
        settings: { foreground: variable("token-function") }
      },
      {
        scope: [
          "entity.name.tag",
          "string.quoted",
          "string.regexp",
          "string.interpolated",
          "string.template",
          "string.unquoted.plain.out.yaml",
          "keyword.other.template"
        ],
        settings: { foreground: variable("token-string-expression") }
      },
      {
        scope: [
          "punctuation.definition.arguments",
          "punctuation.definition.dict",
          "punctuation.separator",
          "meta.function-call.arguments"
        ],
        settings: { foreground: variable("token-punctuation") }
      },
      {
        scope: ["markup.underline.link", "punctuation.definition.metadata.markdown"],
        settings: { foreground: variable("token-link") }
      },
      {
        scope: ["beginning.punctuation.definition.list.markdown"],
        settings: { foreground: variable("token-string") }
      },
      {
        scope: [
          "punctuation.definition.string.begin.markdown",
          "punctuation.definition.string.end.markdown",
          "string.other.link.title.markdown",
          "string.other.link.description.markdown"
        ],
        settings: { foreground: variable("token-keyword") }
      },
      {
        scope: [
          "markup.inserted",
          "meta.diff.header.to-file",
          "punctuation.definition.inserted"
        ],
        settings: { foreground: variable("token-inserted") }
      },
      {
        scope: [
          "markup.deleted",
          "meta.diff.header.from-file",
          "punctuation.definition.deleted"
        ],
        settings: { foreground: variable("token-deleted") }
      },
      {
        scope: ["markup.changed", "punctuation.definition.changed"],
        settings: { foreground: variable("token-changed") }
      }
    ]
  };
  if (!fontStyle) theme.tokenColors = (_a9 = theme.tokenColors) == null ? void 0 : _a9.map((tokenColor) => {
    var _a10;
    if ((_a10 = tokenColor.settings) == null ? void 0 : _a10.fontStyle) delete tokenColor.settings.fontStyle;
    return tokenColor;
  });
  return theme;
}

// ../../node_modules/.pnpm/shiki@4.4.3/node_modules/shiki/dist/bundle-full.mjs
var createHighlighter = createBundledHighlighter({
  langs: bundledLanguages,
  themes: bundledThemes,
  engine: () => (0, engine_oniguruma_exports.createOnigurumaEngine)(import("./wasm-5RJGDYBN.js"))
});
var { codeToHtml: codeToHtml2, codeToHast: codeToHast2, codeToTokens: codeToTokens2, codeToTokensBase: codeToTokensBase3, codeToTokensWithThemes: codeToTokensWithThemes2, getSingletonHighlighter, getLastGrammarState: getLastGrammarState2 } = createSingletonShorthands(createHighlighter, { guessEmbeddedLanguages });

// ../../node_modules/.pnpm/@shikijs+engine-javascript@4.4.3/node_modules/@shikijs/engine-javascript/dist/scanner-DX8LRFGE.mjs
var MAX = 4294967295;
var JavaScriptScanner = class {
  constructor(patterns, options = {}) {
    __publicField(this, "patterns");
    __publicField(this, "options");
    __publicField(this, "regexps");
    this.patterns = patterns;
    this.options = options;
    const { forgiving = false, cache: cache2, regexConstructor } = options;
    if (!regexConstructor) throw new Error("Option `regexConstructor` is not provided");
    this.regexps = patterns.map((p2) => {
      if (typeof p2 !== "string") return p2;
      const cached = cache2 == null ? void 0 : cache2.get(p2);
      if (cached) {
        if (cached instanceof RegExp) return cached;
        if (forgiving) return null;
        throw cached;
      }
      try {
        const regex = regexConstructor(p2);
        cache2 == null ? void 0 : cache2.set(p2, regex);
        return regex;
      } catch (e) {
        cache2 == null ? void 0 : cache2.set(p2, e);
        if (forgiving) return null;
        throw e;
      }
    });
  }
  findNextMatchSync(string, startPosition, _options) {
    const str = typeof string === "string" ? string : string.content;
    const pending = [];
    function toResult(index, match, offset = 0) {
      return {
        index,
        captureIndices: match.indices.map((indice) => {
          if (indice == null) return {
            start: MAX,
            end: MAX,
            length: 0
          };
          return {
            start: indice[0] + offset,
            end: indice[1] + offset,
            length: indice[1] - indice[0]
          };
        })
      };
    }
    for (let i2 = 0; i2 < this.regexps.length; i2++) {
      const regexp = this.regexps[i2];
      if (!regexp) continue;
      try {
        regexp.lastIndex = startPosition;
        const match = regexp.exec(str);
        if (!match) continue;
        if (match.index === startPosition) return toResult(i2, match, 0);
        pending.push([
          i2,
          match,
          0
        ]);
      } catch (e) {
        if (this.options.forgiving) continue;
        throw e;
      }
    }
    if (pending.length) {
      const minIndex = Math.min(...pending.map((m3) => m3[1].index));
      for (const [i2, match, offset] of pending) if (match.index === minIndex) return toResult(i2, match, offset);
    }
    return null;
  }
};

// ../../node_modules/.pnpm/oniguruma-parser@0.12.2/node_modules/oniguruma-parser/dist/utils.js
function r(e) {
  if ([...e].length !== 1) throw new Error(`Expected "${e}" to be a single code point`);
  return e.codePointAt(0);
}
function l(e, t, n) {
  return e.has(t) || e.set(t, n), e.get(t);
}
var i = /* @__PURE__ */ new Set(["alnum", "alpha", "ascii", "blank", "cntrl", "digit", "graph", "lower", "print", "punct", "space", "upper", "word", "xdigit"]);
var o = String.raw;
function u(e, t) {
  if (e == null) throw new Error(t ?? "Value expected");
  return e;
}

// ../../node_modules/.pnpm/oniguruma-parser@0.12.2/node_modules/oniguruma-parser/dist/tokenizer/tokenize.js
var m = o`\[\^?`;
var b = `c.? | C(?:-.?)?|${o`[pP]\{(?:\^?[-\x20_]*[A-Za-z][-\x20\w]*\})?`}|${o`x[89A-Fa-f]\p{AHex}(?:\\x[89A-Fa-f]\p{AHex})*`}|${o`u(?:\p{AHex}{4})? | x\{[^\}]*\}? | x\p{AHex}{0,2}`}|${o`o\{[^\}]*\}?`}|${o`\d{1,3}`}`;
var y = /[?*+][?+]?|\{(?:\d+(?:,\d*)?|,\d+)\}\??/;
var C = new RegExp(o`
  \\ (?:
    ${b}
    | [gk]<[^>]*>?
    | [gk]'[^']*'?
    | .
  )
  | \( (?:
    \? (?:
      [:=!>({]
      | <[=!]
      | <[^>]*>
      | '[^']*'
      | ~\|?
      | #(?:[^)\\]|\\.?)*
      | [^:)]*[:)]
    )?
    | \*[^\)]*\)?
  )?
  | (?:${y.source})+
  | ${m}
  | .
`.replace(/\s+/g, ""), "gsu");
var T = new RegExp(o`
  \\ (?:
    ${b}
    | .
  )
  | \[:(?:\^?\p{Alpha}+|\^):\]
  | ${m}
  | &&
  | .
`.replace(/\s+/g, ""), "gsu");
function M(e, n = {}) {
  const t = { flags: "", ...n, rules: { captureGroup: false, singleline: false, ...n.rules } };
  if (typeof e != "string") throw new Error("String expected as pattern");
  const o3 = Y(t.flags), s2 = [o3.extended], a2 = { captureGroup: t.rules.captureGroup, getCurrentModX() {
    return s2.at(-1);
  }, numOpenGroups: 0, popModX() {
    s2.pop();
  }, pushModX(u2) {
    s2.push(u2);
  }, replaceCurrentModX(u2) {
    s2[s2.length - 1] = u2;
  }, singleline: t.rules.singleline };
  let r4 = [], i2;
  for (C.lastIndex = 0; i2 = C.exec(e); ) {
    const u2 = F(a2, e, i2[0], C.lastIndex);
    u2.tokens ? r4.push(...u2.tokens) : u2.token && r4.push(u2.token), u2.lastIndex !== void 0 && (C.lastIndex = u2.lastIndex);
  }
  const l3 = [];
  let c = 0;
  r4.filter((u2) => u2.type === "GroupOpen").forEach((u2) => {
    u2.kind === "capturing" ? u2.number = ++c : u2.raw === "(" && l3.push(u2);
  }), c || l3.forEach((u2, S2) => {
    u2.kind = "capturing", u2.number = S2 + 1;
  });
  const g = c || l3.length;
  return { tokens: r4.map((u2) => u2.type === "EscapedNumber" ? ee(u2, g) : u2).flat(), flags: o3 };
}
function F(e, n, t, o3) {
  const [s2, a2] = t;
  if (t === "[" || t === "[^") {
    const r4 = K(n, t, o3);
    return { tokens: r4.tokens, lastIndex: r4.lastIndex };
  }
  if (s2 === "\\") {
    if ("AbBGyYzZ".includes(a2)) return { token: w(t, t) };
    if (/^\\g[<']/.test(t)) {
      if (!/^\\g(?:<[^>]+>|'[^']+')$/.test(t)) throw new Error(`Invalid group name "${t}"`);
      return { token: R(t) };
    }
    if (/^\\k[<']/.test(t)) {
      if (!/^\\k(?:<[^>]+>|'[^']+')$/.test(t)) throw new Error(`Invalid group name "${t}"`);
      return { token: A(t) };
    }
    if (a2 === "K") return { token: I("keep", t) };
    if (a2 === "N" || a2 === "R") return { token: k("newline", t, { negate: a2 === "N" }) };
    if (a2 === "O") return { token: k("any", t) };
    if (a2 === "X") return { token: k("text_segment", t) };
    const r4 = x(t, { inCharClass: false });
    return Array.isArray(r4) ? { tokens: r4 } : { token: r4 };
  }
  if (s2 === "(") {
    if (a2 === "*") return { token: j(t) };
    if (t === "(?{") throw new Error(`Unsupported callout "${t}"`);
    if (t.startsWith("(?#")) {
      if (n[o3] !== ")") throw new Error('Unclosed comment group "(?#"');
      return { lastIndex: o3 + 1 };
    }
    if (/^\(\?[-imx]+[:)]$/.test(t)) return { token: L(t, e) };
    if (e.pushModX(e.getCurrentModX()), e.numOpenGroups++, t === "(" && !e.captureGroup || t === "(?:") return { token: f("group", t) };
    if (t === "(?>") return { token: f("atomic", t) };
    if (t === "(?=" || t === "(?!" || t === "(?<=" || t === "(?<!") return { token: f(t[2] === "<" ? "lookbehind" : "lookahead", t, { negate: t.endsWith("!") }) };
    if (t === "(" && e.captureGroup || t.startsWith("(?<") && t.endsWith(">") || t.startsWith("(?'") && t.endsWith("'")) return { token: f("capturing", t, { ...t !== "(" && { name: t.slice(3, -1) } }) };
    if (t.startsWith("(?~")) {
      if (t === "(?~|") throw new Error(`Unsupported absence function kind "${t}"`);
      return { token: f("absence_repeater", t) };
    }
    throw t === "(?(" ? new Error(`Unsupported conditional "${t}"`) : new Error(`Invalid or unsupported group option "${t}"`);
  }
  if (t === ")") {
    if (e.popModX(), e.numOpenGroups--, e.numOpenGroups < 0) throw new Error('Unmatched ")"');
    return { token: Q(t) };
  }
  if (e.getCurrentModX()) {
    if (t === "#") {
      const r4 = n.indexOf(`
`, o3);
      return { lastIndex: r4 === -1 ? n.length : r4 };
    }
    if (/^\s$/.test(t)) {
      const r4 = /\s+/y;
      return r4.lastIndex = o3, { lastIndex: r4.exec(n) ? r4.lastIndex : o3 };
    }
  }
  if (t === ".") return { token: k("dot", t) };
  if (t === "^" || t === "$") {
    const r4 = e.singleline ? { "^": o`\A`, $: o`\Z` }[t] : t;
    return { token: w(r4, t) };
  }
  return t === "|" ? { token: P(t) } : y.test(t) ? { tokens: te(t) } : { token: d(r(t), t) };
}
function K(e, n, t) {
  const o3 = [E(n[1] === "^", n)];
  let s2 = 1, a2;
  for (T.lastIndex = t; a2 = T.exec(e); ) {
    const r4 = a2[0];
    if (r4[0] === "[" && r4[1] !== ":") s2++, o3.push(E(r4[1] === "^", r4));
    else if (r4 === "]") {
      if (o3.at(-1).type === "CharacterClassOpen") o3.push(d(93, r4));
      else if (s2--, o3.push(z(r4)), !s2) break;
    } else {
      const i2 = X(r4);
      Array.isArray(i2) ? o3.push(...i2) : o3.push(i2);
    }
  }
  return { tokens: o3, lastIndex: T.lastIndex || e.length };
}
function X(e) {
  if (e[0] === "\\") return x(e, { inCharClass: true });
  if (e[0] === "[") {
    const n = /\[:(?<negate>\^?)(?<name>[a-z]+):\]/.exec(e);
    if (!n || !i.has(n.groups.name)) throw new Error(`Invalid POSIX class "${e}"`);
    return k("posix", e, { value: n.groups.name, negate: !!n.groups.negate });
  }
  return e === "-" ? U(e) : e === "&&" ? H(e) : d(r(e), e);
}
function x(e, { inCharClass: n }) {
  const t = e[1];
  if (t === "c" || t === "C") return Z(e);
  if ("dDhHsSwW".includes(t)) return q(e);
  if (e.startsWith(o`\o{`)) throw new Error(`Incomplete, invalid, or unsupported octal code point "${e}"`);
  if (/^\\[pP]\{/.test(e)) {
    if (e.length === 3) throw new Error(`Incomplete or invalid Unicode property "${e}"`);
    return V(e);
  }
  if (new RegExp("^\\\\x[89A-Fa-f]\\p{AHex}", "u").test(e)) try {
    const o3 = e.split(/\\x/).slice(1).map((i2) => parseInt(i2, 16)), s2 = new TextDecoder("utf-8", { ignoreBOM: true, fatal: true }).decode(new Uint8Array(o3)), a2 = new TextEncoder();
    return [...s2].map((i2) => {
      const l3 = [...a2.encode(i2)].map((c) => `\\x${c.toString(16)}`).join("");
      return d(r(i2), l3);
    });
  } catch {
    throw new Error(`Multibyte code "${e}" incomplete or invalid in Oniguruma`);
  }
  if (t === "u" || t === "x") return d(J(e), e);
  if ($.has(t)) return d($.get(t), e);
  if (/\d/.test(t)) return W(n, e);
  if (e === "\\") throw new Error(o`Incomplete escape "\"`);
  if (t === "M") throw new Error(`Unsupported meta "${e}"`);
  if ([...e].length === 2) return d(e.codePointAt(1), e);
  throw new Error(`Unexpected escape "${e}"`);
}
function P(e) {
  return { type: "Alternator", raw: e };
}
function w(e, n) {
  return { type: "Assertion", kind: e, raw: n };
}
function A(e) {
  return { type: "Backreference", raw: e };
}
function d(e, n) {
  return { type: "Character", value: e, raw: n };
}
function z(e) {
  return { type: "CharacterClassClose", raw: e };
}
function U(e) {
  return { type: "CharacterClassHyphen", raw: e };
}
function H(e) {
  return { type: "CharacterClassIntersector", raw: e };
}
function E(e, n) {
  return { type: "CharacterClassOpen", negate: e, raw: n };
}
function k(e, n, t = {}) {
  return { type: "CharacterSet", kind: e, ...t, raw: n };
}
function I(e, n, t = {}) {
  return e === "keep" ? { type: "Directive", kind: e, raw: n } : { type: "Directive", kind: e, flags: u(t.flags), raw: n };
}
function W(e, n) {
  return { type: "EscapedNumber", inCharClass: e, raw: n };
}
function Q(e) {
  return { type: "GroupClose", raw: e };
}
function f(e, n, t = {}) {
  return { type: "GroupOpen", kind: e, ...t, raw: n };
}
function D(e, n, t, o3) {
  return { type: "NamedCallout", kind: e, tag: n, arguments: t, raw: o3 };
}
function _(e, n, t, o3) {
  return { type: "Quantifier", kind: e, min: n, max: t, raw: o3 };
}
function R(e) {
  return { type: "Subroutine", raw: e };
}
var B = /* @__PURE__ */ new Set(["COUNT", "CMP", "ERROR", "FAIL", "MAX", "MISMATCH", "SKIP", "TOTAL_COUNT"]);
var $ = /* @__PURE__ */ new Map([["a", 7], ["b", 8], ["e", 27], ["f", 12], ["n", 10], ["r", 13], ["t", 9], ["v", 11]]);
function Z(e) {
  const n = e[1] === "c" ? e[2] : e[3];
  if (!n || !/[A-Za-z]/.test(n)) throw new Error(`Unsupported control character "${e}"`);
  return d(r(n.toUpperCase()) - 64, e);
}
function L(e, n) {
  let { on: t, off: o3 } = /^\(\?(?<on>[imx]*)(?:-(?<off>[-imx]*))?/.exec(e).groups;
  o3 ?? (o3 = "");
  const s2 = (n.getCurrentModX() || t.includes("x")) && !o3.includes("x"), a2 = v(t), r4 = v(o3), i2 = {};
  if (a2 && (i2.enable = a2), r4 && (i2.disable = r4), e.endsWith(")")) return n.replaceCurrentModX(s2), I("flags", e, { flags: i2 });
  if (e.endsWith(":")) return n.pushModX(s2), n.numOpenGroups++, f("group", e, { ...(a2 || r4) && { flags: i2 } });
  throw new Error(`Unexpected flag modifier "${e}"`);
}
function j(e) {
  const n = /\(\*(?<name>[A-Za-z_]\w*)?(?:\[(?<tag>(?:[A-Za-z_]\w*)?)\])?(?:\{(?<args>[^}]*)\})?\)/.exec(e);
  if (!n) throw new Error(`Incomplete or invalid named callout "${e}"`);
  const { name: t, tag: o3, args: s2 } = n.groups;
  if (!t) throw new Error(`Invalid named callout "${e}"`);
  if (o3 === "") throw new Error(`Named callout tag with empty value not allowed "${e}"`);
  const a2 = s2 ? s2.split(",").filter((g) => g !== "").map((g) => /^[+-]?\d+$/.test(g) ? +g : g) : [], [r4, i2, l3] = a2, c = B.has(t) ? t.toLowerCase() : "custom";
  switch (c) {
    case "fail":
    case "mismatch":
    case "skip":
      if (a2.length > 0) throw new Error(`Named callout arguments not allowed "${a2}"`);
      break;
    case "error":
      if (a2.length > 1) throw new Error(`Named callout allows only one argument "${a2}"`);
      if (typeof r4 == "string") throw new Error(`Named callout argument must be a number "${r4}"`);
      break;
    case "max":
      if (!a2.length || a2.length > 2) throw new Error(`Named callout must have one or two arguments "${a2}"`);
      if (typeof r4 == "string" && !/^[A-Za-z_]\w*$/.test(r4)) throw new Error(`Named callout argument one must be a tag or number "${r4}"`);
      if (a2.length === 2 && (typeof i2 == "number" || !/^[<>X]$/.test(i2))) throw new Error(`Named callout optional argument two must be '<', '>', or 'X' "${i2}"`);
      break;
    case "count":
    case "total_count":
      if (a2.length > 1) throw new Error(`Named callout allows only one argument "${a2}"`);
      if (a2.length === 1 && (typeof r4 == "number" || !/^[<>X]$/.test(r4))) throw new Error(`Named callout optional argument must be '<', '>', or 'X' "${r4}"`);
      break;
    case "cmp":
      if (a2.length !== 3) throw new Error(`Named callout must have three arguments "${a2}"`);
      if (typeof r4 == "string" && !/^[A-Za-z_]\w*$/.test(r4)) throw new Error(`Named callout argument one must be a tag or number "${r4}"`);
      if (typeof i2 == "number" || !/^(?:[<>!=]=|[<>])$/.test(i2)) throw new Error(`Named callout argument two must be '==', '!=', '>', '<', '>=', or '<=' "${i2}"`);
      if (typeof l3 == "string" && !/^[A-Za-z_]\w*$/.test(l3)) throw new Error(`Named callout argument three must be a tag or number "${l3}"`);
      break;
    case "custom":
      throw new Error(`Undefined callout name "${t}"`);
    default:
      throw new Error(`Unexpected named callout kind "${c}"`);
  }
  return D(c, o3 ?? null, (s2 == null ? void 0 : s2.split(",")) ?? null, e);
}
function O(e) {
  let n = null, t, o3;
  if (e[0] === "{") {
    const { minStr: s2, maxStr: a2 } = /^\{(?<minStr>\d*)(?:,(?<maxStr>\d*))?/.exec(e).groups, r4 = 1e5;
    if (+s2 > r4 || a2 && +a2 > r4) throw new Error("Quantifier value unsupported in Oniguruma");
    if (t = +s2, o3 = a2 === void 0 ? +s2 : a2 === "" ? 1 / 0 : +a2, t > o3 && (n = "possessive", [t, o3] = [o3, t]), e.endsWith("?")) {
      if (n === "possessive") throw new Error('Unsupported possessive interval quantifier chain with "?"');
      n = "lazy";
    } else n || (n = "greedy");
  } else t = e[0] === "+" ? 1 : 0, o3 = e[0] === "?" ? 1 : 1 / 0, n = e[1] === "+" ? "possessive" : e[1] === "?" ? "lazy" : "greedy";
  return _(n, t, o3, e);
}
function q(e) {
  const n = e[1].toLowerCase();
  return k({ d: "digit", h: "hex", s: "space", w: "word" }[n], e, { negate: e[1] !== n });
}
function V(e) {
  const { p: n, neg: t, value: o3 } = /^\\(?<p>[pP])\{(?<neg>\^?)(?<value>[^}]+)/.exec(e).groups;
  return k("property", e, { value: o3, negate: n === "P" && !t || n === "p" && !!t });
}
function v(e) {
  const n = {};
  return e.includes("i") && (n.ignoreCase = true), e.includes("m") && (n.dotAll = true), e.includes("x") && (n.extended = true), Object.keys(n).length ? n : null;
}
function Y(e) {
  const n = { ignoreCase: false, dotAll: false, extended: false, digitIsAscii: false, posixIsAscii: false, spaceIsAscii: false, wordIsAscii: false, textSegmentMode: null };
  for (let t = 0; t < e.length; t++) {
    const o3 = e[t];
    if (!"imxDPSWy".includes(o3)) throw new Error(`Invalid flag "${o3}"`);
    if (o3 === "y") {
      if (!/^y{[gw]}/.test(e.slice(t))) throw new Error('Invalid or unspecified flag "y" mode');
      n.textSegmentMode = e[t + 2] === "g" ? "grapheme" : "word", t += 3;
      continue;
    }
    n[{ i: "ignoreCase", m: "dotAll", x: "extended", D: "digitIsAscii", P: "posixIsAscii", S: "spaceIsAscii", W: "wordIsAscii" }[o3]] = true;
  }
  return n;
}
function J(e) {
  if (new RegExp("^(?:\\\\u(?!\\p{AHex}{4})|\\\\x(?!\\p{AHex}{1,2}|\\{\\p{AHex}{1,8}\\}))", "u").test(e)) throw new Error(`Incomplete or invalid escape "${e}"`);
  const n = e[2] === "{" ? new RegExp("^\\\\x\\{\\s*(?<hex>\\p{AHex}+)", "u").exec(e).groups.hex : e.slice(2);
  return parseInt(n, 16);
}
function ee(e, n) {
  const { raw: t, inCharClass: o3 } = e, s2 = t.slice(1);
  if (!o3 && (s2 !== "0" && s2.length === 1 || s2[0] !== "0" && +s2 <= n)) return [A(t)];
  const a2 = [], r4 = s2.match(/^[0-7]+|\d/g);
  for (let i2 = 0; i2 < r4.length; i2++) {
    const l3 = r4[i2];
    let c;
    if (i2 === 0 && l3 !== "8" && l3 !== "9") {
      if (c = parseInt(l3, 8), c > 127) throw new Error(o`Octal encoded byte above 177 unsupported "${t}"`);
    } else c = r(l3);
    a2.push(d(c, (i2 === 0 ? "\\" : "") + l3));
  }
  return a2;
}
function te(e) {
  const n = [], t = new RegExp(y, "gy");
  let o3;
  for (; o3 = t.exec(e); ) {
    const s2 = o3[0];
    if (s2[0] === "{") {
      const a2 = /^\{(?<min>\d+),(?<max>\d+)\}\??$/.exec(s2);
      if (a2) {
        const { min: r4, max: i2 } = a2.groups;
        if (+r4 > +i2 && s2.endsWith("?")) {
          t.lastIndex--, n.push(O(s2.slice(0, -1)));
          continue;
        }
      }
    }
    n.push(O(s2));
  }
  return n;
}

// ../../node_modules/.pnpm/oniguruma-parser@0.12.2/node_modules/oniguruma-parser/dist/parser/node-utils.js
function o2(e, t) {
  if (!Array.isArray(e.body)) throw new Error("Expected node with body array");
  if (e.body.length !== 1) return false;
  const r4 = e.body[0];
  return !t || Object.keys(t).every((n) => t[n] === r4[n]);
}
function s(e) {
  return y2.has(e.type);
}
var y2 = /* @__PURE__ */ new Set(["AbsenceFunction", "Backreference", "CapturingGroup", "Character", "CharacterClass", "CharacterSet", "Group", "Quantifier", "Subroutine"]);

// ../../node_modules/.pnpm/oniguruma-parser@0.12.2/node_modules/oniguruma-parser/dist/parser/parse.js
function J2(e, r4 = {}) {
  const n = { flags: "", normalizeUnknownPropertyNames: false, skipBackrefValidation: false, skipLookbehindValidation: false, skipPropertyNameValidation: false, unicodePropertyMap: null, ...r4, rules: { captureGroup: false, singleline: false, ...r4.rules } }, o3 = M(e, { flags: n.flags, rules: { captureGroup: n.rules.captureGroup, singleline: n.rules.singleline } }), i2 = (p2, N) => {
    const u2 = o3.tokens[t.nextIndex];
    switch (t.parent = p2, t.nextIndex++, u2.type) {
      case "Alternator":
        return b2();
      case "Assertion":
        return W2(u2);
      case "Backreference":
        return X2(u2, t);
      case "Character":
        return m2(u2.value, { useLastValid: !!N.isCheckingRangeEnd });
      case "CharacterClassHyphen":
        return ee2(u2, t, N);
      case "CharacterClassOpen":
        return re2(u2, t, N);
      case "CharacterSet":
        return ne(u2, t);
      case "Directive":
        return I2(u2.kind, { flags: u2.flags });
      case "GroupOpen":
        return te2(u2, t, N);
      case "NamedCallout":
        return U2(u2.kind, u2.tag, u2.arguments);
      case "Quantifier":
        return oe(u2, t);
      case "Subroutine":
        return ae(u2, t);
      default:
        throw new Error(`Unexpected token type "${u2.type}"`);
    }
  }, t = { capturingGroups: [], hasNumberedRef: false, namedGroupsByName: /* @__PURE__ */ new Map(), nextIndex: 0, normalizeUnknownPropertyNames: n.normalizeUnknownPropertyNames, parent: null, skipBackrefValidation: n.skipBackrefValidation, skipLookbehindValidation: n.skipLookbehindValidation, skipPropertyNameValidation: n.skipPropertyNameValidation, subroutines: [], tokens: o3.tokens, unicodePropertyMap: n.unicodePropertyMap, walk: i2 }, d2 = B2(T2(o3.flags));
  let s2 = d2.body[0];
  for (; t.nextIndex < o3.tokens.length; ) {
    const p2 = i2(s2, {});
    p2.type === "Alternative" ? (d2.body.push(p2), s2 = p2) : s2.body.push(p2);
  }
  const { capturingGroups: a2, hasNumberedRef: l3, namedGroupsByName: c, subroutines: f2 } = t;
  if (l3 && c.size && !n.rules.captureGroup) throw new Error("Numbered backref/subroutine not allowed when using named capture");
  for (const { ref: p2 } of f2) if (typeof p2 == "number") {
    if (p2 > a2.length) throw new Error("Subroutine uses a group number that's not defined");
    p2 && (a2[p2 - 1].isSubroutined = true);
  } else if (c.has(p2)) {
    if (c.get(p2).length > 1) throw new Error(o`Subroutine uses a duplicate group name "\g<${p2}>"`);
    c.get(p2)[0].isSubroutined = true;
  } else throw new Error(o`Subroutine uses a group name that's not defined "\g<${p2}>"`);
  return d2;
}
function W2({ kind: e }) {
  return F2(u({ "^": "line_start", $: "line_end", "\\A": "string_start", "\\b": "word_boundary", "\\B": "word_boundary", "\\G": "search_start", "\\y": "text_segment_boundary", "\\Y": "text_segment_boundary", "\\z": "string_end", "\\Z": "string_end_newline" }[e], `Unexpected assertion kind "${e}"`), { negate: e === o`\B` || e === o`\Y` });
}
function X2({ raw: e }, r4) {
  const n = /^\\k[<']/.test(e), o3 = n ? e.slice(3, -1) : e.slice(1), i2 = (t, d2 = false) => {
    const s2 = r4.capturingGroups.length;
    let a2 = false;
    if (t > s2) if (r4.skipBackrefValidation) a2 = true;
    else throw new Error(`Not enough capturing groups defined to the left "${e}"`);
    return r4.hasNumberedRef = true, k2(d2 ? s2 + 1 - t : t, { orphan: a2 });
  };
  if (n) {
    const t = /^(?<sign>-?)0*(?<num>[1-9]\d*)$/.exec(o3);
    if (t) return i2(+t.groups.num, !!t.groups.sign);
    if (/[-+]/.test(o3)) throw new Error(`Invalid backref name "${e}"`);
    if (!r4.namedGroupsByName.has(o3)) throw new Error(`Group name not defined to the left "${e}"`);
    return k2(o3);
  }
  return i2(+o3);
}
function ee2(e, r4, n) {
  const { tokens: o3, walk: i2 } = r4, t = r4.parent, d2 = t.body.at(-1), s2 = o3[r4.nextIndex];
  if (!n.isCheckingRangeEnd && d2 && d2.type !== "CharacterClass" && d2.type !== "CharacterClassRange" && s2 && s2.type !== "CharacterClassOpen" && s2.type !== "CharacterClassClose" && s2.type !== "CharacterClassIntersector") {
    const a2 = i2(t, { ...n, isCheckingRangeEnd: true });
    if (d2.type === "Character" && a2.type === "Character") return t.body.pop(), L2(d2, a2);
    throw new Error("Invalid character class range");
  }
  return m2(r("-"));
}
function re2({ negate: e }, r4, n) {
  const { tokens: o3, walk: i2 } = r4, t = [C2()], d2 = o3[r4.nextIndex];
  let s2 = z2(d2);
  for (; s2.type !== "CharacterClassClose"; ) {
    if (s2.type === "CharacterClassIntersector") t.push(C2()), r4.nextIndex++;
    else {
      const l3 = t.at(-1);
      l3.body.push(i2(l3, n));
    }
    s2 = z2(o3[r4.nextIndex], d2);
  }
  const a2 = C2({ negate: e });
  return t.length === 1 ? a2.body = t[0].body : (a2.kind = "intersection", a2.body = t.map((l3) => l3.body.length === 1 ? l3.body[0] : l3)), r4.nextIndex++, a2;
}
function ne({ kind: e, negate: r4, value: n }, o3) {
  const { normalizeUnknownPropertyNames: i2, skipPropertyNameValidation: t, unicodePropertyMap: d2 } = o3;
  if (e === "property") {
    const s2 = w2(n);
    if (i.has(s2) && !(d2 == null ? void 0 : d2.has(s2))) e = "posix", n = s2;
    else return Q2(n, { negate: r4, normalizeUnknownPropertyNames: i2, skipPropertyNameValidation: t, unicodePropertyMap: d2 });
  }
  return e === "posix" ? R2(n, { negate: r4 }) : E2(e, { negate: r4 });
}
function te2(e, r4, n) {
  const { tokens: o3, capturingGroups: i2, namedGroupsByName: t, skipLookbehindValidation: d2, walk: s2 } = r4, a2 = ie(e), l3 = a2.type === "AbsenceFunction", c = $2(a2), f2 = c && a2.negate;
  if (a2.type === "CapturingGroup" && (i2.push(a2), a2.name && l(t, a2.name, []).push(a2)), l3 && n.isInAbsenceFunction) throw new Error("Nested absence function not supported by Oniguruma");
  let p2 = D2(o3[r4.nextIndex]);
  for (; p2.type !== "GroupClose"; ) {
    if (p2.type === "Alternator") a2.body.push(b2()), r4.nextIndex++;
    else {
      const N = a2.body.at(-1), u2 = s2(N, { ...n, isInAbsenceFunction: n.isInAbsenceFunction || l3, isInLookbehind: n.isInLookbehind || c, isInNegLookbehind: n.isInNegLookbehind || f2 });
      if (N.body.push(u2), (c || n.isInLookbehind) && !d2) {
        const v2 = "Lookbehind includes a pattern not allowed by Oniguruma";
        if (f2 || n.isInNegLookbehind) {
          if (M2(u2) || u2.type === "CapturingGroup") throw new Error(v2);
        } else if (M2(u2) || $2(u2) && u2.negate) throw new Error(v2);
      }
    }
    p2 = D2(o3[r4.nextIndex]);
  }
  return r4.nextIndex++, a2;
}
function oe({ kind: e, min: r4, max: n }, o3) {
  const i2 = o3.parent, t = i2.body.at(-1);
  if (!t || !s(t)) throw new Error("Quantifier requires a repeatable token");
  const d2 = _2(e, r4, n, t);
  return i2.body.pop(), d2;
}
function ae({ raw: e }, r4) {
  const { capturingGroups: n, subroutines: o3 } = r4;
  let i2 = e.slice(3, -1);
  const t = /^(?<sign>[-+]?)0*(?<num>[1-9]\d*)$/.exec(i2);
  if (t) {
    const s2 = +t.groups.num, a2 = n.length;
    if (r4.hasNumberedRef = true, i2 = { "": s2, "+": a2 + s2, "-": a2 + 1 - s2 }[t.groups.sign], i2 < 1) throw new Error("Invalid subroutine number");
  } else i2 === "0" && (i2 = 0);
  const d2 = O2(i2);
  return o3.push(d2), d2;
}
function G(e, r4) {
  if (e !== "repeater") throw new Error(`Unexpected absence function kind "${e}"`);
  return { type: "AbsenceFunction", kind: e, body: h(r4 == null ? void 0 : r4.body) };
}
function b2(e) {
  return { type: "Alternative", body: V2(e == null ? void 0 : e.body) };
}
function F2(e, r4) {
  const n = { type: "Assertion", kind: e };
  return (e === "word_boundary" || e === "text_segment_boundary") && (n.negate = !!(r4 == null ? void 0 : r4.negate)), n;
}
function k2(e, r4) {
  const n = !!(r4 == null ? void 0 : r4.orphan);
  return { type: "Backreference", ref: e, ...n && { orphan: n } };
}
function P2(e, r4) {
  const n = { name: void 0, isSubroutined: false, ...r4 };
  if (n.name !== void 0 && !se(n.name)) throw new Error(`Group name "${n.name}" invalid in Oniguruma`);
  return { type: "CapturingGroup", number: e, ...n.name && { name: n.name }, ...n.isSubroutined && { isSubroutined: n.isSubroutined }, body: h(r4 == null ? void 0 : r4.body) };
}
function m2(e, r4) {
  const n = { useLastValid: false, ...r4 };
  if (e > 1114111) {
    const o3 = e.toString(16);
    if (n.useLastValid) e = 1114111;
    else throw e > 1310719 ? new Error(`Invalid code point out of range "\\x{${o3}}"`) : new Error(`Invalid code point out of range in JS "\\x{${o3}}"`);
  }
  return { type: "Character", value: e };
}
function C2(e) {
  const r4 = { kind: "union", negate: false, ...e };
  return { type: "CharacterClass", kind: r4.kind, negate: r4.negate, body: V2(e == null ? void 0 : e.body) };
}
function L2(e, r4) {
  if (r4.value < e.value) throw new Error("Character class range out of order");
  return { type: "CharacterClassRange", min: e, max: r4 };
}
function E2(e, r4) {
  const n = !!(r4 == null ? void 0 : r4.negate), o3 = { type: "CharacterSet", kind: e };
  return (e === "digit" || e === "hex" || e === "newline" || e === "space" || e === "word") && (o3.negate = n), (e === "text_segment" || e === "newline" && !n) && (o3.variableLength = true), o3;
}
function I2(e, r4 = {}) {
  if (e === "keep") return { type: "Directive", kind: e };
  if (e === "flags") return { type: "Directive", kind: e, flags: u(r4.flags) };
  throw new Error(`Unexpected directive kind "${e}"`);
}
function T2(e) {
  return { type: "Flags", ...e };
}
function A2(e) {
  const r4 = e == null ? void 0 : e.atomic, n = e == null ? void 0 : e.flags;
  if (r4 && n) throw new Error("Atomic group cannot have flags");
  return { type: "Group", ...r4 && { atomic: r4 }, ...n && { flags: n }, body: h(e == null ? void 0 : e.body) };
}
function K2(e) {
  const r4 = { behind: false, negate: false, ...e };
  return { type: "LookaroundAssertion", kind: r4.behind ? "lookbehind" : "lookahead", negate: r4.negate, body: h(e == null ? void 0 : e.body) };
}
function U2(e, r4, n) {
  return { type: "NamedCallout", kind: e, tag: r4, arguments: n };
}
function R2(e, r4) {
  const n = !!(r4 == null ? void 0 : r4.negate);
  if (!i.has(e)) throw new Error(`Invalid POSIX class "${e}"`);
  return { type: "CharacterSet", kind: "posix", value: e, negate: n };
}
function _2(e, r4, n, o3) {
  if (r4 > n) throw new Error("Invalid reversed quantifier range");
  return { type: "Quantifier", kind: e, min: r4, max: n, body: o3 };
}
function B2(e, r4) {
  return { type: "Regex", body: h(r4 == null ? void 0 : r4.body), flags: e };
}
function O2(e) {
  return { type: "Subroutine", ref: e };
}
function Q2(e, r4) {
  var _a9;
  const n = { negate: false, normalizeUnknownPropertyNames: false, skipPropertyNameValidation: false, unicodePropertyMap: null, ...r4 };
  let o3 = (_a9 = n.unicodePropertyMap) == null ? void 0 : _a9.get(w2(e));
  if (!o3) {
    if (n.normalizeUnknownPropertyNames) o3 = de(e);
    else if (n.unicodePropertyMap && !n.skipPropertyNameValidation) throw new Error(o`Invalid Unicode property "\p{${e}}"`);
  }
  return { type: "CharacterSet", kind: "property", value: o3 ?? e, negate: n.negate };
}
function ie({ flags: e, kind: r4, name: n, negate: o3, number: i2 }) {
  switch (r4) {
    case "absence_repeater":
      return G("repeater");
    case "atomic":
      return A2({ atomic: true });
    case "capturing":
      return P2(i2, { name: n });
    case "group":
      return A2({ flags: e });
    case "lookahead":
    case "lookbehind":
      return K2({ behind: r4 === "lookbehind", negate: o3 });
    default:
      throw new Error(`Unexpected group kind "${r4}"`);
  }
}
function h(e) {
  if (e === void 0) e = [b2()];
  else if (!Array.isArray(e) || !e.length || !e.every((r4) => r4.type === "Alternative")) throw new Error("Invalid body; expected array of one or more Alternative nodes");
  return e;
}
function V2(e) {
  if (e === void 0) e = [];
  else if (!Array.isArray(e) || !e.every((r4) => !!r4.type)) throw new Error("Invalid body; expected array of nodes");
  return e;
}
function M2(e) {
  return e.type === "LookaroundAssertion" && e.kind === "lookahead";
}
function $2(e) {
  return e.type === "LookaroundAssertion" && e.kind === "lookbehind";
}
function se(e) {
  return /^[\p{Alpha}\p{Pc}][^)]*$/u.test(e);
}
function de(e) {
  return e.trim().replace(/[- _]+/g, "_").replace(/[A-Z][a-z]+(?=[A-Z])/g, "$&_").replace(/[A-Za-z]+/g, (r4) => r4[0].toUpperCase() + r4.slice(1).toLowerCase());
}
function w2(e) {
  return e.replace(/[- _]+/g, "").toLowerCase();
}
function z2(e, r4) {
  const n = r4;
  return u(e, `Unclosed character class${(n == null ? void 0 : n.type) === "Character" && n.value === 93 && n.raw === "]" ? ' (started with "]")' : ""}`);
}
function D2(e) {
  return u(e, "Unclosed group");
}

// ../../node_modules/.pnpm/oniguruma-parser@0.12.2/node_modules/oniguruma-parser/dist/traverser/traverse.js
function S(a2, v2, N = null) {
  function b3(e, s2) {
    for (let t = 0; t < e.length; t++) {
      const r4 = n(e[t], s2, t, e);
      t = Math.max(-1, t + r4);
    }
  }
  function n(e, s2 = null, t = null, r4 = null) {
    var _a9, _b;
    let i2 = 0, c = false;
    const d2 = { node: e, parent: s2, key: t, container: r4, root: a2, remove() {
      x2(r4).splice(Math.max(0, l2(t) + i2), 1), i2--, c = true;
    }, removeAllNextSiblings() {
      return x2(r4).splice(l2(t) + 1);
    }, removeAllPrevSiblings() {
      const o3 = l2(t) + i2;
      return i2 -= o3, x2(r4).splice(0, Math.max(0, o3));
    }, replaceWith(o3, m3 = {}) {
      const y3 = !!m3.traverse;
      r4 ? r4[Math.max(0, l2(t) + i2)] = o3 : u(s2, "Can't replace root node")[t] = o3, y3 && n(o3, s2, t, r4), c = true;
    }, replaceWithMultiple(o3, m3 = {}) {
      const y3 = !!m3.traverse;
      if (x2(r4).splice(Math.max(0, l2(t) + i2), 1, ...o3), i2 += o3.length - 1, y3) {
        let g = 0;
        for (let p2 = 0; p2 < o3.length; p2++) g += n(o3[p2], s2, l2(t) + p2 + g, r4);
      }
      c = true;
    }, skip() {
      c = true;
    } }, { type: f2 } = e, u2 = v2["*"], h2 = v2[f2], R3 = typeof u2 == "function" ? u2 : u2 == null ? void 0 : u2.enter, P3 = typeof h2 == "function" ? h2 : h2 == null ? void 0 : h2.enter;
    if (R3 == null ? void 0 : R3(d2, N), P3 == null ? void 0 : P3(d2, N), !c) switch (f2) {
      case "AbsenceFunction":
      case "Alternative":
      case "CapturingGroup":
      case "CharacterClass":
      case "Group":
      case "LookaroundAssertion":
        b3(e.body, e);
        break;
      case "Assertion":
      case "Backreference":
      case "Character":
      case "CharacterSet":
      case "Directive":
      case "Flags":
      case "NamedCallout":
      case "Subroutine":
        break;
      case "CharacterClassRange":
        n(e.min, e, "min"), n(e.max, e, "max");
        break;
      case "Quantifier":
        n(e.body, e, "body");
        break;
      case "Regex":
        b3(e.body, e), n(e.flags, e, "flags");
        break;
      default:
        throw new Error(`Unexpected node type "${f2}"`);
    }
    return (_a9 = h2 == null ? void 0 : h2.exit) == null ? void 0 : _a9.call(h2, d2, N), (_b = u2 == null ? void 0 : u2.exit) == null ? void 0 : _b.call(u2, d2, N), i2;
  }
  return n(a2), a2;
}
function x2(a2) {
  if (!Array.isArray(a2)) throw new Error("Container expected");
  return a2;
}
function l2(a2) {
  if (typeof a2 != "number") throw new Error("Numeric key expected");
  return a2;
}

// ../../node_modules/.pnpm/regex@6.1.0/node_modules/regex/src/utils-internals.js
var noncapturingDelim = String.raw`\(\?(?:[:=!>A-Za-z\-]|<[=!]|\(DEFINE\))`;
function incrementIfAtLeast(arr, threshold) {
  for (let i2 = 0; i2 < arr.length; i2++) {
    if (arr[i2] >= threshold) {
      arr[i2]++;
    }
  }
}
function spliceStr(str, pos, oldValue, newValue) {
  return str.slice(0, pos) + newValue + str.slice(pos + oldValue.length);
}

// ../../node_modules/.pnpm/regex-utilities@2.3.0/node_modules/regex-utilities/src/index.js
var Context = Object.freeze({
  DEFAULT: "DEFAULT",
  CHAR_CLASS: "CHAR_CLASS"
});
function replaceUnescaped(expression, needle, replacement, context) {
  const re3 = new RegExp(String.raw`${needle}|(?<$skip>\[\^?|\\?.)`, "gsu");
  const negated = [false];
  let numCharClassesOpen = 0;
  let result = "";
  for (const match of expression.matchAll(re3)) {
    const { 0: m3, groups: { $skip } } = match;
    if (!$skip && (!context || context === Context.DEFAULT === !numCharClassesOpen)) {
      if (replacement instanceof Function) {
        result += replacement(match, {
          context: numCharClassesOpen ? Context.CHAR_CLASS : Context.DEFAULT,
          negated: negated[negated.length - 1]
        });
      } else {
        result += replacement;
      }
      continue;
    }
    if (m3[0] === "[") {
      numCharClassesOpen++;
      negated.push(m3[1] === "^");
    } else if (m3 === "]" && numCharClassesOpen) {
      numCharClassesOpen--;
      negated.pop();
    }
    result += m3;
  }
  return result;
}
function forEachUnescaped(expression, needle, callback, context) {
  replaceUnescaped(expression, needle, callback, context);
}
function execUnescaped(expression, needle, pos = 0, context) {
  if (!new RegExp(needle, "su").test(expression)) {
    return null;
  }
  const re3 = new RegExp(`${needle}|(?<$skip>\\\\?.)`, "gsu");
  re3.lastIndex = pos;
  let numCharClassesOpen = 0;
  let match;
  while (match = re3.exec(expression)) {
    const { 0: m3, groups: { $skip } } = match;
    if (!$skip && (!context || context === Context.DEFAULT === !numCharClassesOpen)) {
      return match;
    }
    if (m3 === "[") {
      numCharClassesOpen++;
    } else if (m3 === "]" && numCharClassesOpen) {
      numCharClassesOpen--;
    }
    if (re3.lastIndex == match.index) {
      re3.lastIndex++;
    }
  }
  return null;
}
function hasUnescaped(expression, needle, context) {
  return !!execUnescaped(expression, needle, 0, context);
}
function getGroupContents(expression, contentsStartPos) {
  const token2 = /\\?./gsu;
  token2.lastIndex = contentsStartPos;
  let contentsEndPos = expression.length;
  let numCharClassesOpen = 0;
  let numGroupsOpen = 1;
  let match;
  while (match = token2.exec(expression)) {
    const [m3] = match;
    if (m3 === "[") {
      numCharClassesOpen++;
    } else if (!numCharClassesOpen) {
      if (m3 === "(") {
        numGroupsOpen++;
      } else if (m3 === ")") {
        numGroupsOpen--;
        if (!numGroupsOpen) {
          contentsEndPos = match.index;
          break;
        }
      }
    } else if (m3 === "]") {
      numCharClassesOpen--;
    }
  }
  return expression.slice(contentsStartPos, contentsEndPos);
}

// ../../node_modules/.pnpm/regex@6.1.0/node_modules/regex/src/atomic.js
var atomicPluginToken = new RegExp(String.raw`(?<noncapturingStart>${noncapturingDelim})|(?<capturingStart>\((?:\?<[^>]+>)?)|\\?.`, "gsu");
function atomic(expression, data) {
  const hiddenCaptures = (data == null ? void 0 : data.hiddenCaptures) ?? [];
  let captureTransfers = (data == null ? void 0 : data.captureTransfers) ?? /* @__PURE__ */ new Map();
  if (!/\(\?>/.test(expression)) {
    return {
      pattern: expression,
      captureTransfers,
      hiddenCaptures
    };
  }
  const aGDelim = "(?>";
  const emulatedAGDelim = "(?:(?=(";
  const captureNumMap = [0];
  const addedHiddenCaptures = [];
  let numCapturesBeforeAG = 0;
  let numAGs = 0;
  let aGPos = NaN;
  let hasProcessedAG;
  do {
    hasProcessedAG = false;
    let numCharClassesOpen = 0;
    let numGroupsOpenInAG = 0;
    let inAG = false;
    let match;
    atomicPluginToken.lastIndex = Number.isNaN(aGPos) ? 0 : aGPos + emulatedAGDelim.length;
    while (match = atomicPluginToken.exec(expression)) {
      const { 0: m3, index, groups: { capturingStart, noncapturingStart } } = match;
      if (m3 === "[") {
        numCharClassesOpen++;
      } else if (!numCharClassesOpen) {
        if (m3 === aGDelim && !inAG) {
          aGPos = index;
          inAG = true;
        } else if (inAG && noncapturingStart) {
          numGroupsOpenInAG++;
        } else if (capturingStart) {
          if (inAG) {
            numGroupsOpenInAG++;
          } else {
            numCapturesBeforeAG++;
            captureNumMap.push(numCapturesBeforeAG + numAGs);
          }
        } else if (m3 === ")" && inAG) {
          if (!numGroupsOpenInAG) {
            numAGs++;
            const addedCaptureNum = numCapturesBeforeAG + numAGs;
            expression = `${expression.slice(0, aGPos)}${emulatedAGDelim}${expression.slice(aGPos + aGDelim.length, index)}))<$$${addedCaptureNum}>)${expression.slice(index + 1)}`;
            hasProcessedAG = true;
            addedHiddenCaptures.push(addedCaptureNum);
            incrementIfAtLeast(hiddenCaptures, addedCaptureNum);
            if (captureTransfers.size) {
              const newCaptureTransfers = /* @__PURE__ */ new Map();
              captureTransfers.forEach((from, to) => {
                newCaptureTransfers.set(
                  to >= addedCaptureNum ? to + 1 : to,
                  from.map((f2) => f2 >= addedCaptureNum ? f2 + 1 : f2)
                );
              });
              captureTransfers = newCaptureTransfers;
            }
            break;
          }
          numGroupsOpenInAG--;
        }
      } else if (m3 === "]") {
        numCharClassesOpen--;
      }
    }
  } while (hasProcessedAG);
  hiddenCaptures.push(...addedHiddenCaptures);
  expression = replaceUnescaped(
    expression,
    String.raw`\\(?<backrefNum>[1-9]\d*)|<\$\$(?<wrappedBackrefNum>\d+)>`,
    ({ 0: m3, groups: { backrefNum, wrappedBackrefNum } }) => {
      if (backrefNum) {
        const bNum = +backrefNum;
        if (bNum > captureNumMap.length - 1) {
          throw new Error(`Backref "${m3}" greater than number of captures`);
        }
        return `\\${captureNumMap[bNum]}`;
      }
      return `\\${wrappedBackrefNum}`;
    },
    Context.DEFAULT
  );
  return {
    pattern: expression,
    captureTransfers,
    hiddenCaptures
  };
}
var baseQuantifier = String.raw`(?:[?*+]|\{\d+(?:,\d*)?\})`;
var possessivePluginToken = new RegExp(String.raw`
\\(?: \d+
  | c[A-Za-z]
  | [gk]<[^>]+>
  | [pPu]\{[^\}]+\}
  | u[A-Fa-f\d]{4}
  | x[A-Fa-f\d]{2}
  )
| \((?: \? (?: [:=!>]
  | <(?:[=!]|[^>]+>)
  | [A-Za-z\-]+:
  | \(DEFINE\)
  ))?
| (?<qBase>${baseQuantifier})(?<qMod>[?+]?)(?<invalidQ>[?*+\{]?)
| \\?.
`.replace(/\s+/g, ""), "gsu");
function possessive(expression) {
  if (!new RegExp(`${baseQuantifier}\\+`).test(expression)) {
    return {
      pattern: expression
    };
  }
  const openGroupIndices = [];
  let lastGroupIndex = null;
  let lastCharClassIndex = null;
  let lastToken = "";
  let numCharClassesOpen = 0;
  let match;
  possessivePluginToken.lastIndex = 0;
  while (match = possessivePluginToken.exec(expression)) {
    const { 0: m3, index, groups: { qBase, qMod, invalidQ } } = match;
    if (m3 === "[") {
      if (!numCharClassesOpen) {
        lastCharClassIndex = index;
      }
      numCharClassesOpen++;
    } else if (m3 === "]") {
      if (numCharClassesOpen) {
        numCharClassesOpen--;
      } else {
        lastCharClassIndex = null;
      }
    } else if (!numCharClassesOpen) {
      if (qMod === "+" && lastToken && !lastToken.startsWith("(")) {
        if (invalidQ) {
          throw new Error(`Invalid quantifier "${m3}"`);
        }
        let charsAdded = -1;
        if (/^\{\d+\}$/.test(qBase)) {
          expression = spliceStr(expression, index + qBase.length, qMod, "");
        } else {
          if (lastToken === ")" || lastToken === "]") {
            const nodeIndex = lastToken === ")" ? lastGroupIndex : lastCharClassIndex;
            if (nodeIndex === null) {
              throw new Error(`Invalid unmatched "${lastToken}"`);
            }
            expression = `${expression.slice(0, nodeIndex)}(?>${expression.slice(nodeIndex, index)}${qBase})${expression.slice(index + m3.length)}`;
          } else {
            expression = `${expression.slice(0, index - lastToken.length)}(?>${lastToken}${qBase})${expression.slice(index + m3.length)}`;
          }
          charsAdded += 4;
        }
        possessivePluginToken.lastIndex += charsAdded;
      } else if (m3[0] === "(") {
        openGroupIndices.push(index);
      } else if (m3 === ")") {
        lastGroupIndex = openGroupIndices.length ? openGroupIndices.pop() : null;
      }
    }
    lastToken = m3;
  }
  return {
    pattern: expression
  };
}

// ../../node_modules/.pnpm/regex-recursion@6.0.2/node_modules/regex-recursion/src/index.js
var r2 = String.raw;
var gRToken = r2`\\g<(?<gRNameOrNum>[^>&]+)&R=(?<gRDepth>[^>]+)>`;
var recursiveToken = r2`\(\?R=(?<rDepth>[^\)]+)\)|${gRToken}`;
var namedCaptureDelim = r2`\(\?<(?![=!])(?<captureName>[^>]+)>`;
var captureDelim = r2`${namedCaptureDelim}|(?<unnamed>\()(?!\?)`;
var token = new RegExp(r2`${namedCaptureDelim}|${recursiveToken}|\(\?|\\?.`, "gsu");
var overlappingRecursionMsg = "Cannot use multiple overlapping recursions";
function recursion(pattern, data) {
  const { hiddenCaptures, mode } = {
    hiddenCaptures: [],
    mode: "plugin",
    ...data
  };
  let captureTransfers = (data == null ? void 0 : data.captureTransfers) ?? /* @__PURE__ */ new Map();
  if (!new RegExp(recursiveToken, "su").test(pattern)) {
    return {
      pattern,
      captureTransfers,
      hiddenCaptures
    };
  }
  if (mode === "plugin" && hasUnescaped(pattern, r2`\(\?\(DEFINE\)`, Context.DEFAULT)) {
    throw new Error("DEFINE groups cannot be used with recursion");
  }
  const addedHiddenCaptures = [];
  const hasNumberedBackref = hasUnescaped(pattern, r2`\\[1-9]`, Context.DEFAULT);
  const groupContentsStartPos = /* @__PURE__ */ new Map();
  const openGroups = [];
  let hasRecursed = false;
  let numCharClassesOpen = 0;
  let numCapturesPassed = 0;
  let match;
  token.lastIndex = 0;
  while (match = token.exec(pattern)) {
    const { 0: m3, groups: { captureName, rDepth, gRNameOrNum, gRDepth } } = match;
    if (m3 === "[") {
      numCharClassesOpen++;
    } else if (!numCharClassesOpen) {
      if (rDepth) {
        assertMaxInBounds(rDepth);
        if (hasRecursed) {
          throw new Error(overlappingRecursionMsg);
        }
        if (hasNumberedBackref) {
          throw new Error(
            // When used in `external` mode by transpilers other than Regex+, backrefs might have
            // gone through conversion from named to numbered, so avoid a misleading error
            `${mode === "external" ? "Backrefs" : "Numbered backrefs"} cannot be used with global recursion`
          );
        }
        const left = pattern.slice(0, match.index);
        const right = pattern.slice(token.lastIndex);
        if (hasUnescaped(right, recursiveToken, Context.DEFAULT)) {
          throw new Error(overlappingRecursionMsg);
        }
        const reps = +rDepth - 1;
        pattern = makeRecursive(
          left,
          right,
          reps,
          false,
          hiddenCaptures,
          addedHiddenCaptures,
          numCapturesPassed
        );
        captureTransfers = mapCaptureTransfers(
          captureTransfers,
          left,
          reps,
          addedHiddenCaptures.length,
          0,
          numCapturesPassed
        );
        break;
      } else if (gRNameOrNum) {
        assertMaxInBounds(gRDepth);
        let isWithinReffedGroup = false;
        for (const g of openGroups) {
          if (g.name === gRNameOrNum || g.num === +gRNameOrNum) {
            isWithinReffedGroup = true;
            if (g.hasRecursedWithin) {
              throw new Error(overlappingRecursionMsg);
            }
            break;
          }
        }
        if (!isWithinReffedGroup) {
          throw new Error(r2`Recursive \g cannot be used outside the referenced group "${mode === "external" ? gRNameOrNum : r2`\g<${gRNameOrNum}&R=${gRDepth}>`}"`);
        }
        const startPos = groupContentsStartPos.get(gRNameOrNum);
        const groupContents = getGroupContents(pattern, startPos);
        if (hasNumberedBackref && hasUnescaped(groupContents, r2`${namedCaptureDelim}|\((?!\?)`, Context.DEFAULT)) {
          throw new Error(
            // When used in `external` mode by transpilers other than Regex+, backrefs might have
            // gone through conversion from named to numbered, so avoid a misleading error
            `${mode === "external" ? "Backrefs" : "Numbered backrefs"} cannot be used with recursion of capturing groups`
          );
        }
        const groupContentsLeft = pattern.slice(startPos, match.index);
        const groupContentsRight = groupContents.slice(groupContentsLeft.length + m3.length);
        const numAddedHiddenCapturesPreExpansion = addedHiddenCaptures.length;
        const reps = +gRDepth - 1;
        const expansion = makeRecursive(
          groupContentsLeft,
          groupContentsRight,
          reps,
          true,
          hiddenCaptures,
          addedHiddenCaptures,
          numCapturesPassed
        );
        captureTransfers = mapCaptureTransfers(
          captureTransfers,
          groupContentsLeft,
          reps,
          addedHiddenCaptures.length - numAddedHiddenCapturesPreExpansion,
          numAddedHiddenCapturesPreExpansion,
          numCapturesPassed
        );
        const pre = pattern.slice(0, startPos);
        const post = pattern.slice(startPos + groupContents.length);
        pattern = `${pre}${expansion}${post}`;
        token.lastIndex += expansion.length - m3.length - groupContentsLeft.length - groupContentsRight.length;
        openGroups.forEach((g) => g.hasRecursedWithin = true);
        hasRecursed = true;
      } else if (captureName) {
        numCapturesPassed++;
        groupContentsStartPos.set(String(numCapturesPassed), token.lastIndex);
        groupContentsStartPos.set(captureName, token.lastIndex);
        openGroups.push({
          num: numCapturesPassed,
          name: captureName
        });
      } else if (m3[0] === "(") {
        const isUnnamedCapture = m3 === "(";
        if (isUnnamedCapture) {
          numCapturesPassed++;
          groupContentsStartPos.set(String(numCapturesPassed), token.lastIndex);
        }
        openGroups.push(isUnnamedCapture ? { num: numCapturesPassed } : {});
      } else if (m3 === ")") {
        openGroups.pop();
      }
    } else if (m3 === "]") {
      numCharClassesOpen--;
    }
  }
  hiddenCaptures.push(...addedHiddenCaptures);
  return {
    pattern,
    captureTransfers,
    hiddenCaptures
  };
}
function assertMaxInBounds(max) {
  const errMsg = `Max depth must be integer between 2 and 100; used ${max}`;
  if (!/^[1-9]\d*$/.test(max)) {
    throw new Error(errMsg);
  }
  max = +max;
  if (max < 2 || max > 100) {
    throw new Error(errMsg);
  }
}
function makeRecursive(left, right, reps, isSubpattern, hiddenCaptures, addedHiddenCaptures, numCapturesPassed) {
  const namesInRecursed = /* @__PURE__ */ new Set();
  if (isSubpattern) {
    forEachUnescaped(left + right, namedCaptureDelim, ({ groups: { captureName } }) => {
      namesInRecursed.add(captureName);
    }, Context.DEFAULT);
  }
  const rest = [
    reps,
    isSubpattern ? namesInRecursed : null,
    hiddenCaptures,
    addedHiddenCaptures,
    numCapturesPassed
  ];
  return `${left}${repeatWithDepth(`(?:${left}`, "forward", ...rest)}(?:)${repeatWithDepth(`${right})`, "backward", ...rest)}${right}`;
}
function repeatWithDepth(pattern, direction, reps, namesInRecursed, hiddenCaptures, addedHiddenCaptures, numCapturesPassed) {
  const startNum = 2;
  const getDepthNum = (i2) => direction === "forward" ? i2 + startNum : reps - i2 + startNum - 1;
  let result = "";
  for (let i2 = 0; i2 < reps; i2++) {
    const depthNum = getDepthNum(i2);
    result += replaceUnescaped(
      pattern,
      r2`${captureDelim}|\\k<(?<backref>[^>]+)>`,
      ({ 0: m3, groups: { captureName, unnamed, backref } }) => {
        if (backref && namesInRecursed && !namesInRecursed.has(backref)) {
          return m3;
        }
        const suffix = `_$${depthNum}`;
        if (unnamed || captureName) {
          const addedCaptureNum = numCapturesPassed + addedHiddenCaptures.length + 1;
          addedHiddenCaptures.push(addedCaptureNum);
          incrementIfAtLeast2(hiddenCaptures, addedCaptureNum);
          return unnamed ? m3 : `(?<${captureName}${suffix}>`;
        }
        return r2`\k<${backref}${suffix}>`;
      },
      Context.DEFAULT
    );
  }
  return result;
}
function incrementIfAtLeast2(arr, threshold) {
  for (let i2 = 0; i2 < arr.length; i2++) {
    if (arr[i2] >= threshold) {
      arr[i2]++;
    }
  }
}
function mapCaptureTransfers(captureTransfers, left, reps, numCapturesAddedInExpansion, numAddedHiddenCapturesPreExpansion, numCapturesPassed) {
  if (captureTransfers.size && numCapturesAddedInExpansion) {
    let numCapturesInLeft = 0;
    forEachUnescaped(left, captureDelim, () => numCapturesInLeft++, Context.DEFAULT);
    const recursionDelimCaptureNum = numCapturesPassed - numCapturesInLeft + numAddedHiddenCapturesPreExpansion;
    const newCaptureTransfers = /* @__PURE__ */ new Map();
    captureTransfers.forEach((from, to) => {
      const numCapturesInRight = (numCapturesAddedInExpansion - numCapturesInLeft * reps) / reps;
      const numCapturesAddedInLeft = numCapturesInLeft * reps;
      const newTo = to > recursionDelimCaptureNum + numCapturesInLeft ? to + numCapturesAddedInExpansion : to;
      const newFrom = [];
      for (const f2 of from) {
        if (f2 <= recursionDelimCaptureNum) {
          newFrom.push(f2);
        } else if (f2 > recursionDelimCaptureNum + numCapturesInLeft + numCapturesInRight) {
          newFrom.push(f2 + numCapturesAddedInExpansion);
        } else if (f2 <= recursionDelimCaptureNum + numCapturesInLeft) {
          for (let i2 = 0; i2 <= reps; i2++) {
            newFrom.push(f2 + numCapturesInLeft * i2);
          }
        } else {
          for (let i2 = 0; i2 <= reps; i2++) {
            newFrom.push(f2 + numCapturesAddedInLeft + numCapturesInRight * i2);
          }
        }
      }
      newCaptureTransfers.set(newTo, newFrom);
    });
    return newCaptureTransfers;
  }
  return captureTransfers;
}

// ../../node_modules/.pnpm/oniguruma-to-es@4.3.6/node_modules/oniguruma-to-es/dist/esm/index.js
var cp = String.fromCodePoint;
var r3 = String.raw;
var envFlags = {};
var globalRegExp = globalThis.RegExp;
envFlags.flagGroups = (() => {
  try {
    new globalRegExp("(?i:)");
  } catch {
    return false;
  }
  return true;
})();
envFlags.unicodeSets = (() => {
  try {
    new globalRegExp("[[]]", "v");
  } catch {
    return false;
  }
  return true;
})();
envFlags.bugFlagVLiteralHyphenIsRange = envFlags.unicodeSets ? (() => {
  try {
    new globalRegExp(r3`[\d\-a]`, "v");
  } catch {
    return true;
  }
  return false;
})() : false;
envFlags.bugNestedClassIgnoresNegation = envFlags.unicodeSets && new globalRegExp("[[^a]]", "v").test("a");
function getNewCurrentFlags(current, { enable, disable }) {
  return {
    dotAll: !(disable == null ? void 0 : disable.dotAll) && !!((enable == null ? void 0 : enable.dotAll) || current.dotAll),
    ignoreCase: !(disable == null ? void 0 : disable.ignoreCase) && !!((enable == null ? void 0 : enable.ignoreCase) || current.ignoreCase)
  };
}
function getOrInsert(map, key2, defaultValue) {
  if (!map.has(key2)) {
    map.set(key2, defaultValue);
  }
  return map.get(key2);
}
function isMinTarget(target, min) {
  return EsVersion[target] >= EsVersion[min];
}
function throwIfNullish(value, msg) {
  if (value == null) {
    throw new Error(msg ?? "Value expected");
  }
  return value;
}
var EsVersion = {
  ES2025: 2025,
  ES2024: 2024,
  ES2018: 2018
};
var Target = (
  /** @type {const} */
  {
    auto: "auto",
    ES2025: "ES2025",
    ES2024: "ES2024",
    ES2018: "ES2018"
  }
);
function getOptions(options = {}) {
  if ({}.toString.call(options) !== "[object Object]") {
    throw new Error("Unexpected options");
  }
  if (options.target !== void 0 && !Target[options.target]) {
    throw new Error(`Unexpected target "${options.target}"`);
  }
  const opts = {
    // Sets the level of emulation rigor/strictness.
    accuracy: "default",
    // Disables advanced emulation that relies on returning a `RegExp` subclass, resulting in
    // certain patterns not being emulatable.
    avoidSubclass: false,
    // Oniguruma flags; a string with `i`, `m`, `x`, `D`, `S`, `W`, `y{g}` in any order (all
    // optional). Oniguruma's `m` is equivalent to JavaScript's `s` (`dotAll`).
    flags: "",
    // Include JavaScript flag `g` (`global`) in the result.
    global: false,
    // Include JavaScript flag `d` (`hasIndices`) in the result.
    hasIndices: false,
    // Delay regex construction until first use if the transpiled pattern is at least this length.
    lazyCompileLength: Infinity,
    // JavaScript version used for generated regexes. Using `auto` detects the best value based on
    // your environment. Later targets allow faster processing, simpler generated source, and
    // support for additional features.
    target: "auto",
    // Disables minifications that simplify the pattern without changing the meaning.
    verbose: false,
    ...options,
    // Advanced options that override standard behavior, error checking, and flags when enabled.
    rules: {
      // Useful with TextMate grammars that merge backreferences across patterns.
      allowOrphanBackrefs: false,
      // Use ASCII `\b` and `\B`, which increases search performance of generated regexes.
      asciiWordBoundaries: false,
      // Allow unnamed captures and numbered calls (backreferences and subroutines) when using
      // named capture. This is Oniguruma option `ONIG_OPTION_CAPTURE_GROUP`; on by default in
      // `vscode-oniguruma`.
      captureGroup: false,
      // Change the recursion depth limit from Oniguruma's `20` to an integer `2`–`20`.
      recursionLimit: 20,
      // `^` as `\A`; `$` as`\Z`. Improves search performance of generated regexes without changing
      // the meaning if searching line by line. This is Oniguruma option `ONIG_OPTION_SINGLELINE`.
      singleline: false,
      ...options.rules
    }
  };
  if (opts.target === "auto") {
    opts.target = envFlags.flagGroups ? "ES2025" : envFlags.unicodeSets ? "ES2024" : "ES2018";
  }
  return opts;
}
var asciiSpaceChar = "[	-\r ]";
var CharsWithoutIgnoreCaseExpansion = /* @__PURE__ */ new Set([
  cp(304),
  // İ
  cp(305)
  // ı
]);
var defaultWordChar = r3`[\p{L}\p{M}\p{N}\p{Pc}]`;
function getIgnoreCaseMatchChars(char) {
  if (CharsWithoutIgnoreCaseExpansion.has(char)) {
    return [char];
  }
  const set = /* @__PURE__ */ new Set();
  const lower = char.toLowerCase();
  const upper = lower.toUpperCase();
  const title = LowerToTitleCaseMap.get(lower);
  const altLower = LowerToAlternativeLowerCaseMap.get(lower);
  const altUpper = LowerToAlternativeUpperCaseMap.get(lower);
  if ([...upper].length === 1) {
    set.add(upper);
  }
  altUpper && set.add(altUpper);
  title && set.add(title);
  set.add(lower);
  altLower && set.add(altLower);
  return [...set];
}
var JsUnicodePropertyMap = new Map(
  `C Other
Cc Control cntrl
Cf Format
Cn Unassigned
Co Private_Use
Cs Surrogate
L Letter
LC Cased_Letter
Ll Lowercase_Letter
Lm Modifier_Letter
Lo Other_Letter
Lt Titlecase_Letter
Lu Uppercase_Letter
M Mark Combining_Mark
Mc Spacing_Mark
Me Enclosing_Mark
Mn Nonspacing_Mark
N Number
Nd Decimal_Number digit
Nl Letter_Number
No Other_Number
P Punctuation punct
Pc Connector_Punctuation
Pd Dash_Punctuation
Pe Close_Punctuation
Pf Final_Punctuation
Pi Initial_Punctuation
Po Other_Punctuation
Ps Open_Punctuation
S Symbol
Sc Currency_Symbol
Sk Modifier_Symbol
Sm Math_Symbol
So Other_Symbol
Z Separator
Zl Line_Separator
Zp Paragraph_Separator
Zs Space_Separator
ASCII
ASCII_Hex_Digit AHex
Alphabetic Alpha
Any
Assigned
Bidi_Control Bidi_C
Bidi_Mirrored Bidi_M
Case_Ignorable CI
Cased
Changes_When_Casefolded CWCF
Changes_When_Casemapped CWCM
Changes_When_Lowercased CWL
Changes_When_NFKC_Casefolded CWKCF
Changes_When_Titlecased CWT
Changes_When_Uppercased CWU
Dash
Default_Ignorable_Code_Point DI
Deprecated Dep
Diacritic Dia
Emoji
Emoji_Component EComp
Emoji_Modifier EMod
Emoji_Modifier_Base EBase
Emoji_Presentation EPres
Extended_Pictographic ExtPict
Extender Ext
Grapheme_Base Gr_Base
Grapheme_Extend Gr_Ext
Hex_Digit Hex
IDS_Binary_Operator IDSB
IDS_Trinary_Operator IDST
ID_Continue IDC
ID_Start IDS
Ideographic Ideo
Join_Control Join_C
Logical_Order_Exception LOE
Lowercase Lower
Math
Noncharacter_Code_Point NChar
Pattern_Syntax Pat_Syn
Pattern_White_Space Pat_WS
Quotation_Mark QMark
Radical
Regional_Indicator RI
Sentence_Terminal STerm
Soft_Dotted SD
Terminal_Punctuation Term
Unified_Ideograph UIdeo
Uppercase Upper
Variation_Selector VS
White_Space space
XID_Continue XIDC
XID_Start XIDS`.split(/\s/).map((p2) => [w2(p2), p2])
);
var LowerToAlternativeLowerCaseMap = /* @__PURE__ */ new Map([
  ["s", cp(383)],
  // s, ſ
  [cp(383), "s"]
  // ſ, s
]);
var LowerToAlternativeUpperCaseMap = /* @__PURE__ */ new Map([
  [cp(223), cp(7838)],
  // ß, ẞ
  [cp(107), cp(8490)],
  // k, K (Kelvin)
  [cp(229), cp(8491)],
  // å, Å (Angstrom)
  [cp(969), cp(8486)]
  // ω, Ω (Ohm)
]);
var LowerToTitleCaseMap = new Map([
  titleEntry(453),
  titleEntry(456),
  titleEntry(459),
  titleEntry(498),
  ...titleRange(8072, 8079),
  ...titleRange(8088, 8095),
  ...titleRange(8104, 8111),
  titleEntry(8124),
  titleEntry(8140),
  titleEntry(8188)
]);
var PosixClassMap = /* @__PURE__ */ new Map([
  ["alnum", r3`[\p{Alpha}\p{Nd}]`],
  ["alpha", r3`\p{Alpha}`],
  ["ascii", r3`\p{ASCII}`],
  ["blank", r3`[\p{Zs}\t]`],
  ["cntrl", r3`\p{Cc}`],
  ["digit", r3`\p{Nd}`],
  ["graph", r3`[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]`],
  ["lower", r3`\p{Lower}`],
  ["print", r3`[[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]\p{Zs}]`],
  ["punct", r3`[\p{P}\p{S}]`],
  // Updated value from Onig 6.9.9; changed from Unicode `\p{punct}`
  ["space", r3`\p{space}`],
  ["upper", r3`\p{Upper}`],
  ["word", r3`[\p{Alpha}\p{M}\p{Nd}\p{Pc}]`],
  ["xdigit", r3`\p{AHex}`]
]);
function range(start, end) {
  const range2 = [];
  for (let i2 = start; i2 <= end; i2++) {
    range2.push(i2);
  }
  return range2;
}
function titleEntry(codePoint) {
  const char = cp(codePoint);
  return [char.toLowerCase(), char];
}
function titleRange(start, end) {
  return range(start, end).map((codePoint) => titleEntry(codePoint));
}
var UnicodePropertiesWithSpecificCase = /* @__PURE__ */ new Set([
  "Lower",
  "Lowercase",
  "Upper",
  "Uppercase",
  "Ll",
  "Lowercase_Letter",
  "Lt",
  "Titlecase_Letter",
  "Lu",
  "Uppercase_Letter"
  // The `Changes_When_*` properties (and their aliases) could be included, but they're very rare.
  // Some other properties include a handful of chars with specific cases only, but these chars are
  // generally extreme edge cases and using such properties case insensitively generally produces
  // undesired behavior anyway
]);
function transform(ast, options) {
  const opts = {
    // A couple edge cases exist where options `accuracy` and `bestEffortTarget` are used:
    // - `CharacterSet` kind `text_segment` (`\X`): An exact representation would require heavy
    //   Unicode data; a best-effort approximation requires knowing the target.
    // - `CharacterSet` kind `posix` with values `graph` and `print`: Their complex Unicode
    //   representations would be hard to change to ASCII versions after the fact in the generator
    //   based on `target`/`accuracy`, so produce the appropriate structure here.
    accuracy: "default",
    asciiWordBoundaries: false,
    avoidSubclass: false,
    bestEffortTarget: "ES2025",
    ...options
  };
  addParentProperties(ast);
  const firstPassState = {
    accuracy: opts.accuracy,
    asciiWordBoundaries: opts.asciiWordBoundaries,
    avoidSubclass: opts.avoidSubclass,
    flagDirectivesByAlt: /* @__PURE__ */ new Map(),
    jsGroupNameMap: /* @__PURE__ */ new Map(),
    minTargetEs2024: isMinTarget(opts.bestEffortTarget, "ES2024"),
    passedLookbehind: false,
    strategy: null,
    // Subroutines can appear before the groups they ref, so collect reffed nodes for a second pass 
    subroutineRefMap: /* @__PURE__ */ new Map(),
    supportedGNodes: /* @__PURE__ */ new Set(),
    digitIsAscii: ast.flags.digitIsAscii,
    spaceIsAscii: ast.flags.spaceIsAscii,
    wordIsAscii: ast.flags.wordIsAscii
  };
  S(ast, FirstPassVisitor, firstPassState);
  const globalFlags = {
    dotAll: ast.flags.dotAll,
    ignoreCase: ast.flags.ignoreCase
  };
  const secondPassState = {
    currentFlags: globalFlags,
    prevFlags: null,
    globalFlags,
    groupOriginByCopy: /* @__PURE__ */ new Map(),
    groupsByName: /* @__PURE__ */ new Map(),
    multiplexCapturesToLeftByRef: /* @__PURE__ */ new Map(),
    openRefs: /* @__PURE__ */ new Map(),
    reffedNodesByReferencer: /* @__PURE__ */ new Map(),
    subroutineRefMap: firstPassState.subroutineRefMap
  };
  S(ast, SecondPassVisitor, secondPassState);
  const thirdPassState = {
    groupsByName: secondPassState.groupsByName,
    highestOrphanBackref: 0,
    numCapturesToLeft: 0,
    reffedNodesByReferencer: secondPassState.reffedNodesByReferencer
  };
  S(ast, ThirdPassVisitor, thirdPassState);
  ast._originMap = secondPassState.groupOriginByCopy;
  ast._strategy = firstPassState.strategy;
  return ast;
}
var FirstPassVisitor = {
  AbsenceFunction({ node, parent, replaceWith }) {
    const { body: body3, kind } = node;
    if (kind === "repeater") {
      const innerGroup = A2();
      innerGroup.body[0].body.push(
        // Insert own alts as `body`
        K2({ negate: true, body: body3 }),
        Q2("Any")
      );
      const outerGroup = A2();
      outerGroup.body[0].body.push(
        _2("greedy", 0, Infinity, innerGroup)
      );
      replaceWith(setParentDeep(outerGroup, parent), { traverse: true });
    } else {
      throw new Error(`Unsupported absence function "(?~|"`);
    }
  },
  Alternative: {
    enter({ node, parent, key: key2 }, { flagDirectivesByAlt }) {
      const flagDirectives = node.body.filter((el) => el.kind === "flags");
      for (let i2 = key2 + 1; i2 < parent.body.length; i2++) {
        const forwardSiblingAlt = parent.body[i2];
        getOrInsert(flagDirectivesByAlt, forwardSiblingAlt, []).push(...flagDirectives);
      }
    },
    exit({ node }, { flagDirectivesByAlt }) {
      var _a9;
      if ((_a9 = flagDirectivesByAlt.get(node)) == null ? void 0 : _a9.length) {
        const flags = getCombinedFlagModsFromFlagNodes(flagDirectivesByAlt.get(node));
        if (flags) {
          const flagGroup = A2({ flags });
          flagGroup.body[0].body = node.body;
          node.body = [setParentDeep(flagGroup, node)];
        }
      }
    }
  },
  Assertion({ node, parent, key: key2, container, root: root2, remove, replaceWith }, state) {
    const { kind, negate } = node;
    const { asciiWordBoundaries, avoidSubclass, supportedGNodes, wordIsAscii } = state;
    if (kind === "text_segment_boundary") {
      throw new Error(`Unsupported text segment boundary "\\${negate ? "Y" : "y"}"`);
    } else if (kind === "line_end") {
      replaceWith(setParentDeep(K2({ body: [
        b2({ body: [F2("string_end")] }),
        b2({ body: [m2(10)] })
        // `\n`
      ] }), parent));
    } else if (kind === "line_start") {
      replaceWith(setParentDeep(parseFragment(r3`(?<=\A|\n(?!\z))`, { skipLookbehindValidation: true }), parent));
    } else if (kind === "search_start") {
      if (supportedGNodes.has(node)) {
        root2.flags.sticky = true;
        remove();
      } else {
        const prev = container[key2 - 1];
        if (prev && isAlwaysNonZeroLength(prev)) {
          replaceWith(setParentDeep(K2({ negate: true }), parent));
        } else if (avoidSubclass) {
          throw new Error(r3`Uses "\G" in a way that requires a subclass`);
        } else {
          replaceWith(setParent(F2("string_start"), parent));
          state.strategy = "clip_search";
        }
      }
    } else if (kind === "string_end" || kind === "string_start") {
    } else if (kind === "string_end_newline") {
      replaceWith(setParentDeep(parseFragment(r3`(?=\n?\z)`), parent));
    } else if (kind === "word_boundary") {
      if (!wordIsAscii && !asciiWordBoundaries) {
        const b3 = `(?:(?<=${defaultWordChar})(?!${defaultWordChar})|(?<!${defaultWordChar})(?=${defaultWordChar}))`;
        const B3 = `(?:(?<=${defaultWordChar})(?=${defaultWordChar})|(?<!${defaultWordChar})(?!${defaultWordChar}))`;
        replaceWith(setParentDeep(parseFragment(negate ? B3 : b3), parent));
      }
    } else {
      throw new Error(`Unexpected assertion kind "${kind}"`);
    }
  },
  Backreference({ node }, { jsGroupNameMap }) {
    let { ref } = node;
    if (typeof ref === "string" && !isValidJsGroupName(ref)) {
      ref = getAndStoreJsGroupName(ref, jsGroupNameMap);
      node.ref = ref;
    }
  },
  CapturingGroup({ node }, { jsGroupNameMap, subroutineRefMap }) {
    let { name } = node;
    if (name && !isValidJsGroupName(name)) {
      name = getAndStoreJsGroupName(name, jsGroupNameMap);
      node.name = name;
    }
    subroutineRefMap.set(node.number, node);
    if (name) {
      subroutineRefMap.set(name, node);
    }
  },
  CharacterClassRange({ node, parent, replaceWith }) {
    if (parent.kind === "intersection") {
      const cc = C2({ body: [node] });
      replaceWith(setParentDeep(cc, parent), { traverse: true });
    }
  },
  CharacterSet({ node, parent, replaceWith }, { accuracy, minTargetEs2024, digitIsAscii, spaceIsAscii, wordIsAscii }) {
    const { kind, negate, value } = node;
    if (digitIsAscii && (kind === "digit" || value === "digit")) {
      replaceWith(setParent(E2("digit", { negate }), parent));
      return;
    }
    if (spaceIsAscii && (kind === "space" || value === "space")) {
      replaceWith(setParentDeep(setNegate(parseFragment(asciiSpaceChar), negate), parent));
      return;
    }
    if (wordIsAscii && (kind === "word" || value === "word")) {
      replaceWith(setParent(E2("word", { negate }), parent));
      return;
    }
    if (kind === "any") {
      replaceWith(setParent(Q2("Any"), parent));
    } else if (kind === "digit") {
      replaceWith(setParent(Q2("Nd", { negate }), parent));
    } else if (kind === "dot") {
    } else if (kind === "text_segment") {
      if (accuracy === "strict") {
        throw new Error(r3`Use of "\X" requires non-strict accuracy`);
      }
      const eBase = "\\p{Emoji}(?:\\p{EMod}|\\uFE0F\\u20E3?|[\\x{E0020}-\\x{E007E}]+\\x{E007F})?";
      const emoji = r3`\p{RI}{2}|${eBase}(?:\u200D${eBase})*`;
      replaceWith(setParentDeep(parseFragment(
        // Close approximation of an extended grapheme cluster; see <unicode.org/reports/tr29/>
        r3`(?>\r\n|${minTargetEs2024 ? r3`\p{RGI_Emoji}` : emoji}|\P{M}\p{M}*)`,
        // Allow JS property `RGI_Emoji` through
        { skipPropertyNameValidation: true }
      ), parent));
    } else if (kind === "hex") {
      replaceWith(setParent(Q2("AHex", { negate }), parent));
    } else if (kind === "newline") {
      replaceWith(setParentDeep(parseFragment(negate ? "[^\n]" : "(?>\r\n?|[\n\v\f\u2028\u2029])"), parent));
    } else if (kind === "posix") {
      if (!minTargetEs2024 && (value === "graph" || value === "print")) {
        if (accuracy === "strict") {
          throw new Error(`POSIX class "${value}" requires min target ES2024 or non-strict accuracy`);
        }
        let ascii = {
          graph: "!-~",
          print: " -~"
        }[value];
        if (negate) {
          ascii = `\0-${cp(ascii.codePointAt(0) - 1)}${cp(ascii.codePointAt(2) + 1)}-􏿿`;
        }
        replaceWith(setParentDeep(parseFragment(`[${ascii}]`), parent));
      } else {
        replaceWith(setParentDeep(setNegate(parseFragment(PosixClassMap.get(value)), negate), parent));
      }
    } else if (kind === "property") {
      if (!JsUnicodePropertyMap.has(w2(value))) {
        node.key = "sc";
      }
    } else if (kind === "space") {
      replaceWith(setParent(Q2("space", { negate }), parent));
    } else if (kind === "word") {
      replaceWith(setParentDeep(setNegate(parseFragment(defaultWordChar), negate), parent));
    } else {
      throw new Error(`Unexpected character set kind "${kind}"`);
    }
  },
  Directive({ node, parent, root: root2, remove, replaceWith, removeAllPrevSiblings, removeAllNextSiblings }) {
    const { kind, flags } = node;
    if (kind === "flags") {
      if (!flags.enable && !flags.disable) {
        remove();
      } else {
        const flagGroup = A2({ flags });
        flagGroup.body[0].body = removeAllNextSiblings();
        replaceWith(setParentDeep(flagGroup, parent), { traverse: true });
      }
    } else if (kind === "keep") {
      const firstAlt = root2.body[0];
      const hasWrapperGroup = root2.body.length === 1 && // Not emulatable if within a `CapturingGroup`
      o2(firstAlt, { type: "Group" }) && firstAlt.body[0].body.length === 1;
      const topLevel = hasWrapperGroup ? firstAlt.body[0] : root2;
      if (parent.parent !== topLevel || topLevel.body.length > 1) {
        throw new Error(r3`Uses "\K" in a way that's unsupported`);
      }
      const lookbehind = K2({ behind: true });
      lookbehind.body[0].body = removeAllPrevSiblings();
      replaceWith(setParentDeep(lookbehind, parent));
    } else {
      throw new Error(`Unexpected directive kind "${kind}"`);
    }
  },
  Flags({ node, parent }) {
    if (node.posixIsAscii) {
      throw new Error('Unsupported flag "P"');
    }
    if (node.textSegmentMode === "word") {
      throw new Error('Unsupported flag "y{w}"');
    }
    [
      "digitIsAscii",
      // Flag D
      "extended",
      // Flag x
      "posixIsAscii",
      // Flag P
      "spaceIsAscii",
      // Flag S
      "wordIsAscii",
      // Flag W
      "textSegmentMode"
      // Flag y{g} or y{w}
    ].forEach((f2) => delete node[f2]);
    Object.assign(node, {
      // JS flag g; no Onig equiv
      global: false,
      // JS flag d; no Onig equiv
      hasIndices: false,
      // JS flag m; no Onig equiv but its behavior is always on in Onig. Onig's only line break
      // char is line feed, unlike JS, so this flag isn't used since it would produce inaccurate
      // results (also allows `^` and `$` to be used in the generator for string start and end)
      multiline: false,
      // JS flag y; no Onig equiv, but used for `\G` emulation
      sticky: node.sticky ?? false
      // Note: Regex+ doesn't allow explicitly adding flags it handles implicitly, so leave out
      // properties `unicode` (JS flag u) and `unicodeSets` (JS flag v). Keep the existing values
      // for `ignoreCase` (flag i) and `dotAll` (JS flag s, but Onig flag m)
    });
    parent.options = {
      disable: {
        // Onig uses different rules for flag x than Regex+, so disable the implicit flag
        x: true,
        // Onig has no flag to control "named capture only" mode but contextually applies its
        // behavior when named capturing is used, so disable Regex+'s implicit flag for it
        n: true
      },
      force: {
        // Always add flag v because we're generating an AST that relies on it (it enables JS
        // support for Onig features nested classes, intersection, Unicode properties, etc.).
        // However, the generator might disable flag v based on its `target` option
        v: true
      }
    };
  },
  Group({ node }) {
    if (!node.flags) {
      return;
    }
    const { enable, disable } = node.flags;
    (enable == null ? void 0 : enable.extended) && delete enable.extended;
    (disable == null ? void 0 : disable.extended) && delete disable.extended;
    (enable == null ? void 0 : enable.dotAll) && (disable == null ? void 0 : disable.dotAll) && delete enable.dotAll;
    (enable == null ? void 0 : enable.ignoreCase) && (disable == null ? void 0 : disable.ignoreCase) && delete enable.ignoreCase;
    enable && !Object.keys(enable).length && delete node.flags.enable;
    disable && !Object.keys(disable).length && delete node.flags.disable;
    !node.flags.enable && !node.flags.disable && delete node.flags;
  },
  LookaroundAssertion({ node }, state) {
    const { kind } = node;
    if (kind === "lookbehind") {
      state.passedLookbehind = true;
    }
  },
  NamedCallout({ node, parent, replaceWith }) {
    const { kind } = node;
    if (kind === "fail") {
      replaceWith(setParentDeep(K2({ negate: true }), parent));
    } else {
      throw new Error(`Unsupported named callout "(*${kind.toUpperCase()}"`);
    }
  },
  Quantifier({ node }) {
    if (node.body.type === "Quantifier") {
      const group = A2();
      group.body[0].body.push(node.body);
      node.body = setParentDeep(group, node);
    }
  },
  Regex: {
    enter({ node }, { supportedGNodes }) {
      const leadingGs = [];
      let hasAltWithLeadG = false;
      let hasAltWithoutLeadG = false;
      for (const alt of node.body) {
        if (alt.body.length === 1 && alt.body[0].kind === "search_start") {
          alt.body.pop();
        } else {
          const leadingG = getLeadingG(alt.body);
          if (leadingG) {
            hasAltWithLeadG = true;
            Array.isArray(leadingG) ? leadingGs.push(...leadingG) : leadingGs.push(leadingG);
          } else {
            hasAltWithoutLeadG = true;
          }
        }
      }
      if (hasAltWithLeadG && !hasAltWithoutLeadG) {
        leadingGs.forEach((g) => supportedGNodes.add(g));
      }
    },
    exit(_3, { accuracy, passedLookbehind, strategy }) {
      if (accuracy === "strict" && passedLookbehind && strategy) {
        throw new Error(r3`Uses "\G" in a way that requires non-strict accuracy`);
      }
    }
  },
  Subroutine({ node }, { jsGroupNameMap }) {
    let { ref } = node;
    if (typeof ref === "string" && !isValidJsGroupName(ref)) {
      ref = getAndStoreJsGroupName(ref, jsGroupNameMap);
      node.ref = ref;
    }
  }
};
var SecondPassVisitor = {
  Backreference({ node }, { multiplexCapturesToLeftByRef, reffedNodesByReferencer }) {
    const { orphan, ref } = node;
    if (!orphan) {
      reffedNodesByReferencer.set(node, [...multiplexCapturesToLeftByRef.get(ref).map(({ node: node2 }) => node2)]);
    }
  },
  CapturingGroup: {
    enter({
      node,
      parent,
      replaceWith,
      skip
    }, {
      groupOriginByCopy,
      groupsByName,
      multiplexCapturesToLeftByRef,
      openRefs,
      reffedNodesByReferencer
    }) {
      const origin = groupOriginByCopy.get(node);
      if (origin && openRefs.has(node.number)) {
        const recursion2 = setParent(createRecursion(node.number), parent);
        reffedNodesByReferencer.set(recursion2, openRefs.get(node.number));
        replaceWith(recursion2);
        return;
      }
      openRefs.set(node.number, node);
      multiplexCapturesToLeftByRef.set(node.number, []);
      if (node.name) {
        getOrInsert(multiplexCapturesToLeftByRef, node.name, []);
      }
      const multiplexNodes = multiplexCapturesToLeftByRef.get(node.name ?? node.number);
      for (let i2 = 0; i2 < multiplexNodes.length; i2++) {
        const multiplex = multiplexNodes[i2];
        if (
          // This group is from subroutine expansion, and there's a multiplex value from either the
          // origin node or a prior subroutine expansion group with the same origin
          origin === multiplex.node || origin && origin === multiplex.origin || // This group is not from subroutine expansion, and it comes after a subroutine expansion
          // group that refers to this group
          node === multiplex.origin
        ) {
          multiplexNodes.splice(i2, 1);
          break;
        }
      }
      multiplexCapturesToLeftByRef.get(node.number).push({ node, origin });
      if (node.name) {
        multiplexCapturesToLeftByRef.get(node.name).push({ node, origin });
      }
      if (node.name) {
        const groupsWithSameName = getOrInsert(groupsByName, node.name, /* @__PURE__ */ new Map());
        let hasDuplicateNameToRemove = false;
        if (origin) {
          hasDuplicateNameToRemove = true;
        } else {
          for (const groupInfo of groupsWithSameName.values()) {
            if (!groupInfo.hasDuplicateNameToRemove) {
              hasDuplicateNameToRemove = true;
              break;
            }
          }
        }
        groupsByName.get(node.name).set(node, { node, hasDuplicateNameToRemove });
      }
    },
    exit({ node }, { openRefs }) {
      if (openRefs.get(node.number) === node) {
        openRefs.delete(node.number);
      }
    }
  },
  Group: {
    enter({ node }, state) {
      state.prevFlags = state.currentFlags;
      if (node.flags) {
        state.currentFlags = getNewCurrentFlags(state.currentFlags, node.flags);
      }
    },
    exit(_3, state) {
      state.currentFlags = state.prevFlags;
    }
  },
  Subroutine({ node, parent, replaceWith }, state) {
    const { isRecursive, ref } = node;
    if (isRecursive) {
      let reffed = parent;
      while (reffed = reffed.parent) {
        if (reffed.type === "CapturingGroup" && (reffed.name === ref || reffed.number === ref)) {
          break;
        }
      }
      state.reffedNodesByReferencer.set(node, reffed);
      return;
    }
    const reffedGroupNode = state.subroutineRefMap.get(ref);
    const isGlobalRecursion = ref === 0;
    const expandedSubroutine = isGlobalRecursion ? createRecursion(0) : (
      // The reffed group might itself contain subroutines, which are expanded during sub-traversal
      cloneCapturingGroup(reffedGroupNode, state.groupOriginByCopy, null)
    );
    let replacement = expandedSubroutine;
    if (!isGlobalRecursion) {
      const reffedGroupFlagMods = getCombinedFlagModsFromFlagNodes(getAllParents(
        reffedGroupNode,
        (p2) => p2.type === "Group" && !!p2.flags
      ));
      const reffedGroupFlags = reffedGroupFlagMods ? getNewCurrentFlags(state.globalFlags, reffedGroupFlagMods) : state.globalFlags;
      if (!areFlagsEqual(reffedGroupFlags, state.currentFlags)) {
        replacement = A2({
          flags: getFlagModsFromFlags(reffedGroupFlags)
        });
        replacement.body[0].body.push(expandedSubroutine);
      }
    }
    replaceWith(setParentDeep(replacement, parent), { traverse: !isGlobalRecursion });
  }
};
var ThirdPassVisitor = {
  Backreference({ node, parent, replaceWith }, state) {
    if (node.orphan) {
      state.highestOrphanBackref = Math.max(state.highestOrphanBackref, node.ref);
      return;
    }
    const reffedNodes = state.reffedNodesByReferencer.get(node);
    const participants = reffedNodes.filter((reffed) => canParticipateWithNode(reffed, node));
    if (!participants.length) {
      replaceWith(setParentDeep(K2({ negate: true }), parent));
    } else if (participants.length > 1) {
      const group = A2({
        atomic: true,
        body: participants.reverse().map((reffed) => b2({
          body: [k2(reffed.number)]
        }))
      });
      replaceWith(setParentDeep(group, parent));
    } else {
      node.ref = participants[0].number;
    }
  },
  CapturingGroup({ node }, state) {
    node.number = ++state.numCapturesToLeft;
    if (node.name) {
      if (state.groupsByName.get(node.name).get(node).hasDuplicateNameToRemove) {
        delete node.name;
      }
    }
  },
  Regex: {
    exit({ node }, state) {
      const numCapsNeeded = Math.max(state.highestOrphanBackref - state.numCapturesToLeft, 0);
      for (let i2 = 0; i2 < numCapsNeeded; i2++) {
        const emptyCapture = P2();
        node.body.at(-1).body.push(emptyCapture);
      }
    }
  },
  Subroutine({ node }, state) {
    if (!node.isRecursive || node.ref === 0) {
      return;
    }
    node.ref = state.reffedNodesByReferencer.get(node).number;
  }
};
function addParentProperties(root2) {
  S(root2, {
    "*"({ node, parent }) {
      node.parent = parent;
    }
  });
}
function areFlagsEqual(a2, b3) {
  return a2.dotAll === b3.dotAll && a2.ignoreCase === b3.ignoreCase;
}
function canParticipateWithNode(capture, node) {
  let rightmostPoint = node;
  do {
    if (rightmostPoint.type === "Regex") {
      return false;
    }
    if (rightmostPoint.type === "Alternative") {
      continue;
    }
    if (rightmostPoint === capture) {
      return false;
    }
    const kidsOfParent = getKids(rightmostPoint.parent);
    for (const kid of kidsOfParent) {
      if (kid === rightmostPoint) {
        break;
      }
      if (kid === capture || isAncestorOf(kid, capture)) {
        return true;
      }
    }
  } while (rightmostPoint = rightmostPoint.parent);
  throw new Error("Unexpected path");
}
function cloneCapturingGroup(obj, originMap, up, up2) {
  const store = Array.isArray(obj) ? [] : {};
  for (const [key2, value] of Object.entries(obj)) {
    if (key2 === "parent") {
      store.parent = Array.isArray(up) ? up2 : up;
    } else if (value && typeof value === "object") {
      store[key2] = cloneCapturingGroup(value, originMap, store, up);
    } else {
      if (key2 === "type" && value === "CapturingGroup") {
        originMap.set(store, originMap.get(obj) ?? obj);
      }
      store[key2] = value;
    }
  }
  return store;
}
function createRecursion(ref) {
  const node = O2(ref);
  node.isRecursive = true;
  return node;
}
function getAllParents(node, filterFn) {
  const results = [];
  while (node = node.parent) {
    if (!filterFn || filterFn(node)) {
      results.push(node);
    }
  }
  return results;
}
function getAndStoreJsGroupName(name, map) {
  if (map.has(name)) {
    return map.get(name);
  }
  const jsName = `$${map.size}_${name.replace(/^[^$_\p{IDS}]|[^$\u200C\u200D\p{IDC}]/ug, "_")}`;
  map.set(name, jsName);
  return jsName;
}
function getCombinedFlagModsFromFlagNodes(flagNodes) {
  const flagProps = ["dotAll", "ignoreCase"];
  const combinedFlags = { enable: {}, disable: {} };
  flagNodes.forEach(({ flags }) => {
    flagProps.forEach((prop) => {
      var _a9, _b;
      if ((_a9 = flags.enable) == null ? void 0 : _a9[prop]) {
        delete combinedFlags.disable[prop];
        combinedFlags.enable[prop] = true;
      }
      if ((_b = flags.disable) == null ? void 0 : _b[prop]) {
        combinedFlags.disable[prop] = true;
      }
    });
  });
  if (!Object.keys(combinedFlags.enable).length) {
    delete combinedFlags.enable;
  }
  if (!Object.keys(combinedFlags.disable).length) {
    delete combinedFlags.disable;
  }
  if (combinedFlags.enable || combinedFlags.disable) {
    return combinedFlags;
  }
  return null;
}
function getFlagModsFromFlags({ dotAll, ignoreCase }) {
  const mods = {};
  if (dotAll || ignoreCase) {
    mods.enable = {};
    dotAll && (mods.enable.dotAll = true);
    ignoreCase && (mods.enable.ignoreCase = true);
  }
  if (!dotAll || !ignoreCase) {
    mods.disable = {};
    !dotAll && (mods.disable.dotAll = true);
    !ignoreCase && (mods.disable.ignoreCase = true);
  }
  return mods;
}
function getKids(node) {
  if (!node) {
    throw new Error("Node expected");
  }
  const { body: body3 } = node;
  return Array.isArray(body3) ? body3 : body3 ? [body3] : null;
}
function getLeadingG(els) {
  const firstToConsider = els.find((el) => el.kind === "search_start" || isLoneGLookaround(el, { negate: false }) || !isAlwaysZeroLength(el));
  if (!firstToConsider) {
    return null;
  }
  if (firstToConsider.kind === "search_start") {
    return firstToConsider;
  }
  if (firstToConsider.type === "LookaroundAssertion") {
    return firstToConsider.body[0].body[0];
  }
  if (firstToConsider.type === "CapturingGroup" || firstToConsider.type === "Group") {
    const gNodesForGroup = [];
    for (const alt of firstToConsider.body) {
      const leadingG = getLeadingG(alt.body);
      if (!leadingG) {
        return null;
      }
      Array.isArray(leadingG) ? gNodesForGroup.push(...leadingG) : gNodesForGroup.push(leadingG);
    }
    return gNodesForGroup;
  }
  return null;
}
function isAncestorOf(node, descendant) {
  const kids = getKids(node) ?? [];
  for (const kid of kids) {
    if (kid === descendant || isAncestorOf(kid, descendant)) {
      return true;
    }
  }
  return false;
}
function isAlwaysZeroLength({ type }) {
  return type === "Assertion" || type === "Directive" || type === "LookaroundAssertion";
}
function isAlwaysNonZeroLength(node) {
  const types = [
    "Character",
    "CharacterClass",
    "CharacterSet"
  ];
  return types.includes(node.type) || node.type === "Quantifier" && node.min && types.includes(node.body.type);
}
function isLoneGLookaround(node, options) {
  const opts = {
    negate: null,
    ...options
  };
  return node.type === "LookaroundAssertion" && (opts.negate === null || node.negate === opts.negate) && node.body.length === 1 && o2(node.body[0], {
    type: "Assertion",
    kind: "search_start"
  });
}
function isValidJsGroupName(name) {
  return /^[$_\p{IDS}][$\u200C\u200D\p{IDC}]*$/u.test(name);
}
function parseFragment(pattern, options) {
  const ast = J2(pattern, {
    ...options,
    // Providing a custom set of Unicode property names avoids converting some JS Unicode
    // properties (ex: `\p{Alpha}`) to Onig POSIX classes
    unicodePropertyMap: JsUnicodePropertyMap
  });
  const alts = ast.body;
  if (alts.length > 1 || alts[0].body.length > 1) {
    return A2({ body: alts });
  }
  return alts[0].body[0];
}
function setNegate(node, negate) {
  node.negate = negate;
  return node;
}
function setParent(node, parent) {
  node.parent = parent;
  return node;
}
function setParentDeep(node, parent) {
  addParentProperties(node);
  node.parent = parent;
  return node;
}
function generate(ast, options) {
  const opts = getOptions(options);
  const minTargetEs2024 = isMinTarget(opts.target, "ES2024");
  const minTargetEs2025 = isMinTarget(opts.target, "ES2025");
  const recursionLimit = opts.rules.recursionLimit;
  if (!Number.isInteger(recursionLimit) || recursionLimit < 2 || recursionLimit > 20) {
    throw new Error("Invalid recursionLimit; use 2-20");
  }
  let hasCaseInsensitiveNode = null;
  let hasCaseSensitiveNode = null;
  if (!minTargetEs2025) {
    const iStack = [ast.flags.ignoreCase];
    S(ast, FlagModifierVisitor, {
      getCurrentModI: () => iStack.at(-1),
      popModI() {
        iStack.pop();
      },
      pushModI(isIOn) {
        iStack.push(isIOn);
      },
      setHasCasedChar() {
        if (iStack.at(-1)) {
          hasCaseInsensitiveNode = true;
        } else {
          hasCaseSensitiveNode = true;
        }
      }
    });
  }
  const appliedGlobalFlags = {
    dotAll: ast.flags.dotAll,
    // - Turn global flag i on if a case insensitive node was used and no case sensitive nodes were
    //   used (to avoid unnecessary node expansion).
    // - Turn global flag i off if a case sensitive node was used (since case sensitivity can't be
    //   forced without the use of ES2025 flag groups)
    ignoreCase: !!((ast.flags.ignoreCase || hasCaseInsensitiveNode) && !hasCaseSensitiveNode)
  };
  let lastNode = ast;
  const state = {
    accuracy: opts.accuracy,
    appliedGlobalFlags,
    captureMap: /* @__PURE__ */ new Map(),
    currentFlags: {
      dotAll: ast.flags.dotAll,
      ignoreCase: ast.flags.ignoreCase
    },
    inCharClass: false,
    lastNode,
    originMap: ast._originMap,
    recursionLimit,
    useAppliedIgnoreCase: !!(!minTargetEs2025 && hasCaseInsensitiveNode && hasCaseSensitiveNode),
    useFlagMods: minTargetEs2025,
    useFlagV: minTargetEs2024,
    verbose: opts.verbose
  };
  function gen(node) {
    state.lastNode = lastNode;
    lastNode = node;
    const fn = throwIfNullish(generator[node.type], `Unexpected node type "${node.type}"`);
    return fn(node, state, gen);
  }
  const result = {
    pattern: ast.body.map(gen).join("|"),
    // Could reset `lastNode` at this point via `lastNode = ast`, but it isn't needed by flags
    flags: gen(ast.flags),
    options: { ...ast.options }
  };
  if (!minTargetEs2024) {
    delete result.options.force.v;
    result.options.disable.v = true;
    result.options.unicodeSetsPlugin = null;
  }
  result._captureTransfers = /* @__PURE__ */ new Map();
  result._hiddenCaptures = [];
  state.captureMap.forEach((value, key2) => {
    if (value.hidden) {
      result._hiddenCaptures.push(key2);
    }
    if (value.transferTo) {
      getOrInsert(result._captureTransfers, value.transferTo, []).push(key2);
    }
  });
  return result;
}
var FlagModifierVisitor = {
  "*": {
    enter({ node }, state) {
      if (isAnyGroup(node)) {
        const currentModI = state.getCurrentModI();
        state.pushModI(
          node.flags ? getNewCurrentFlags({ ignoreCase: currentModI }, node.flags).ignoreCase : currentModI
        );
      }
    },
    exit({ node }, state) {
      if (isAnyGroup(node)) {
        state.popModI();
      }
    }
  },
  Backreference(_3, state) {
    state.setHasCasedChar();
  },
  Character({ node }, state) {
    if (charHasCase(cp(node.value))) {
      state.setHasCasedChar();
    }
  },
  CharacterClassRange({ node, skip }, state) {
    skip();
    if (getCasesOutsideCharClassRange(node, { firstOnly: true }).length) {
      state.setHasCasedChar();
    }
  },
  CharacterSet({ node }, state) {
    if (node.kind === "property" && UnicodePropertiesWithSpecificCase.has(node.value)) {
      state.setHasCasedChar();
    }
  }
};
var generator = {
  /**
  @param {AlternativeNode} node
  */
  Alternative({ body: body3 }, _3, gen) {
    return body3.map(gen).join("");
  },
  /**
  @param {AssertionNode} node
  */
  Assertion({ kind, negate }) {
    if (kind === "string_end") {
      return "$";
    }
    if (kind === "string_start") {
      return "^";
    }
    if (kind === "word_boundary") {
      return negate ? r3`\B` : r3`\b`;
    }
    throw new Error(`Unexpected assertion kind "${kind}"`);
  },
  /**
  @param {BackreferenceNode} node
  */
  Backreference({ ref }, state) {
    if (typeof ref !== "number") {
      throw new Error("Unexpected named backref in transformed AST");
    }
    if (!state.useFlagMods && state.accuracy === "strict" && state.currentFlags.ignoreCase && !state.captureMap.get(ref).ignoreCase) {
      throw new Error("Use of case-insensitive backref to case-sensitive group requires target ES2025 or non-strict accuracy");
    }
    return "\\" + ref;
  },
  /**
  @param {CapturingGroupNode} node
  */
  CapturingGroup(node, state, gen) {
    const { body: body3, name, number: number2 } = node;
    const data = { ignoreCase: state.currentFlags.ignoreCase };
    const origin = state.originMap.get(node);
    if (origin) {
      data.hidden = true;
      if (number2 > origin.number) {
        data.transferTo = origin.number;
      }
    }
    state.captureMap.set(number2, data);
    return `(${name ? `?<${name}>` : ""}${body3.map(gen).join("|")})`;
  },
  /**
  @param {CharacterNode} node
  */
  Character({ value }, state) {
    const char = cp(value);
    const escaped = getCharEscape(value, {
      escDigit: state.lastNode.type === "Backreference",
      inCharClass: state.inCharClass,
      useFlagV: state.useFlagV
    });
    if (escaped !== char) {
      return escaped;
    }
    if (state.useAppliedIgnoreCase && state.currentFlags.ignoreCase && charHasCase(char)) {
      const cases = getIgnoreCaseMatchChars(char);
      return state.inCharClass ? cases.join("") : cases.length > 1 ? `[${cases.join("")}]` : cases[0];
    }
    return char;
  },
  /**
  @param {CharacterClassNode} node
  */
  CharacterClass(node, state, gen) {
    const { kind, negate, parent } = node;
    let { body: body3 } = node;
    if (kind === "intersection" && !state.useFlagV) {
      throw new Error("Use of character class intersection requires min target ES2024");
    }
    if (envFlags.bugFlagVLiteralHyphenIsRange && state.useFlagV && body3.some(isLiteralHyphen)) {
      body3 = [m2(45), ...body3.filter((kid) => !isLiteralHyphen(kid))];
    }
    const genClass = () => `[${negate ? "^" : ""}${body3.map(gen).join(kind === "intersection" ? "&&" : "")}]`;
    if (!state.inCharClass) {
      if (
        // Already established `kind !== 'intersection'` if `!state.useFlagV`; don't check again
        (!state.useFlagV || envFlags.bugNestedClassIgnoresNegation) && !negate
      ) {
        const negatedChildClasses = body3.filter(
          (kid) => kid.type === "CharacterClass" && kid.kind === "union" && kid.negate
        );
        if (negatedChildClasses.length) {
          const group = A2();
          const groupFirstAlt = group.body[0];
          group.parent = parent;
          groupFirstAlt.parent = group;
          body3 = body3.filter((kid) => !negatedChildClasses.includes(kid));
          node.body = body3;
          if (body3.length) {
            node.parent = groupFirstAlt;
            groupFirstAlt.body.push(node);
          } else {
            group.body.pop();
          }
          negatedChildClasses.forEach((cc) => {
            const newAlt = b2({ body: [cc] });
            cc.parent = newAlt;
            newAlt.parent = group;
            group.body.push(newAlt);
          });
          return gen(group);
        }
      }
      state.inCharClass = true;
      const result = genClass();
      state.inCharClass = false;
      return result;
    }
    const firstEl = body3[0];
    if (
      // Already established that the parent is a char class via `inCharClass`; don't check again
      kind === "union" && !negate && firstEl && // Allows many nested classes to work with `target` ES2018 which doesn't support nesting
      ((!state.useFlagV || !state.verbose) && parent.kind === "union" && !(envFlags.bugFlagVLiteralHyphenIsRange && state.useFlagV) || !state.verbose && parent.kind === "intersection" && // JS doesn't allow intersection with union or ranges
      body3.length === 1 && firstEl.type !== "CharacterClassRange")
    ) {
      return body3.map(gen).join("");
    }
    if (!state.useFlagV && parent.type === "CharacterClass") {
      throw new Error("Uses nested character class in a way that requires min target ES2024");
    }
    return genClass();
  },
  /**
  @param {CharacterClassRangeNode} node
  */
  CharacterClassRange(node, state) {
    const min = node.min.value;
    const max = node.max.value;
    const escOpts = {
      escDigit: false,
      inCharClass: true,
      useFlagV: state.useFlagV
    };
    const minStr = getCharEscape(min, escOpts);
    const maxStr = getCharEscape(max, escOpts);
    const extraChars = /* @__PURE__ */ new Set();
    if (state.useAppliedIgnoreCase && state.currentFlags.ignoreCase) {
      const charsOutsideRange = getCasesOutsideCharClassRange(node);
      const ranges = getCodePointRangesFromChars(charsOutsideRange);
      ranges.forEach((value) => {
        extraChars.add(
          Array.isArray(value) ? `${getCharEscape(value[0], escOpts)}-${getCharEscape(value[1], escOpts)}` : getCharEscape(value, escOpts)
        );
      });
    }
    return `${minStr}-${maxStr}${[...extraChars].join("")}`;
  },
  /**
  @param {CharacterSetNode} node
  */
  CharacterSet({ kind, negate, value, key: key2 }, state) {
    if (kind === "dot") {
      return state.currentFlags.dotAll ? state.appliedGlobalFlags.dotAll || state.useFlagMods ? "." : "[^]" : (
        // Onig's only line break char is line feed, unlike JS
        r3`[^\n]`
      );
    }
    if (kind === "digit") {
      return negate ? r3`\D` : r3`\d`;
    }
    if (kind === "property") {
      if (state.useAppliedIgnoreCase && state.currentFlags.ignoreCase && UnicodePropertiesWithSpecificCase.has(value)) {
        throw new Error(`Unicode property "${value}" can't be case-insensitive when other chars have specific case`);
      }
      return `${negate ? r3`\P` : r3`\p`}{${key2 ? `${key2}=` : ""}${value}}`;
    }
    if (kind === "word") {
      return negate ? r3`\W` : r3`\w`;
    }
    throw new Error(`Unexpected character set kind "${kind}"`);
  },
  /**
  @param {FlagsNode} node
  */
  Flags(node, state) {
    return (
      // The transformer should never turn on the properties for flags d, g, m since Onig doesn't
      // have equivs. Flag m is never used since Onig uses different line break chars than JS
      // (node.hasIndices ? 'd' : '') +
      // (node.global ? 'g' : '') +
      // (node.multiline ? 'm' : '') +
      (state.appliedGlobalFlags.ignoreCase ? "i" : "") + (node.dotAll ? "s" : "") + (node.sticky ? "y" : "")
    );
  },
  /**
  @param {GroupNode} node
  */
  Group({ atomic: atomic2, body: body3, flags, parent }, state, gen) {
    const currentFlags = state.currentFlags;
    if (flags) {
      state.currentFlags = getNewCurrentFlags(currentFlags, flags);
    }
    const contents = body3.map(gen).join("|");
    const result = !state.verbose && body3.length === 1 && // Single alt
    parent.type !== "Quantifier" && !atomic2 && (!state.useFlagMods || !flags) ? contents : `(?${getGroupPrefix(atomic2, flags, state.useFlagMods)}${contents})`;
    state.currentFlags = currentFlags;
    return result;
  },
  /**
  @param {LookaroundAssertionNode} node
  */
  LookaroundAssertion({ body: body3, kind, negate }, _3, gen) {
    const prefix = `${kind === "lookahead" ? "" : "<"}${negate ? "!" : "="}`;
    return `(?${prefix}${body3.map(gen).join("|")})`;
  },
  /**
  @param {QuantifierNode} node
  */
  Quantifier(node, _3, gen) {
    return gen(node.body) + getQuantifierStr(node);
  },
  /**
  @param {SubroutineNode & {isRecursive: true}} node
  */
  Subroutine({ isRecursive, ref }, state) {
    if (!isRecursive) {
      throw new Error("Unexpected non-recursive subroutine in transformed AST");
    }
    const limit = state.recursionLimit;
    return ref === 0 ? `(?R=${limit})` : r3`\g<${ref}&R=${limit}>`;
  }
};
var BaseEscapeChars = /* @__PURE__ */ new Set([
  "$",
  "(",
  ")",
  "*",
  "+",
  ".",
  "?",
  "[",
  "\\",
  "]",
  "^",
  "{",
  "|",
  "}"
]);
var CharClassEscapeChars = /* @__PURE__ */ new Set([
  "-",
  "\\",
  "]",
  "^",
  // Literal `[` doesn't require escaping with flag u, but this can help work around regex source
  // linters and regex syntax processors that expect unescaped `[` to create a nested class
  "["
]);
var CharClassEscapeCharsFlagV = /* @__PURE__ */ new Set([
  "(",
  ")",
  "-",
  "/",
  "[",
  "\\",
  "]",
  "^",
  "{",
  "|",
  "}",
  // Double punctuators; also includes already-listed `-` and `^`
  "!",
  "#",
  "$",
  "%",
  "&",
  "*",
  "+",
  ",",
  ".",
  ":",
  ";",
  "<",
  "=",
  ">",
  "?",
  "@",
  "`",
  "~"
]);
var CharCodeEscapeMap = /* @__PURE__ */ new Map([
  [9, r3`\t`],
  // horizontal tab
  [10, r3`\n`],
  // line feed
  [11, r3`\v`],
  // vertical tab
  [12, r3`\f`],
  // form feed
  [13, r3`\r`],
  // carriage return
  [8232, r3`\u2028`],
  // line separator
  [8233, r3`\u2029`],
  // paragraph separator
  [65279, r3`\uFEFF`]
  // ZWNBSP/BOM
]);
var casedRe = new RegExp("^\\p{Cased}$", "u");
function charHasCase(char) {
  return casedRe.test(char);
}
function getCasesOutsideCharClassRange(node, options) {
  const firstOnly = !!(options == null ? void 0 : options.firstOnly);
  const min = node.min.value;
  const max = node.max.value;
  const found = [];
  if (min < 65 && (max === 65535 || max >= 131071) || min === 65536 && max >= 131071) {
    return found;
  }
  for (let i2 = min; i2 <= max; i2++) {
    const char = cp(i2);
    if (!charHasCase(char)) {
      continue;
    }
    const charsOutsideRange = getIgnoreCaseMatchChars(char).filter((caseOfChar) => {
      const num = caseOfChar.codePointAt(0);
      return num < min || num > max;
    });
    if (charsOutsideRange.length) {
      found.push(...charsOutsideRange);
      if (firstOnly) {
        break;
      }
    }
  }
  return found;
}
function getCharEscape(codePoint, { escDigit, inCharClass, useFlagV }) {
  if (CharCodeEscapeMap.has(codePoint)) {
    return CharCodeEscapeMap.get(codePoint);
  }
  if (
    // Control chars, etc.; condition modeled on the Chrome developer console's display for strings
    codePoint < 32 || codePoint > 126 && codePoint < 160 || // Unicode planes 4-16; unassigned, special purpose, and private use area
    codePoint > 262143 || // Avoid corrupting a preceding backref by immediately following it with a literal digit
    escDigit && isDigitCharCode(codePoint)
  ) {
    return codePoint > 255 ? `\\u{${codePoint.toString(16).toUpperCase()}}` : `\\x${codePoint.toString(16).toUpperCase().padStart(2, "0")}`;
  }
  const escapeChars = inCharClass ? useFlagV ? CharClassEscapeCharsFlagV : CharClassEscapeChars : BaseEscapeChars;
  const char = cp(codePoint);
  return (escapeChars.has(char) ? "\\" : "") + char;
}
function getCodePointRangesFromChars(chars) {
  const codePoints = chars.map((char) => char.codePointAt(0)).sort((a2, b3) => a2 - b3);
  const values = [];
  let start = null;
  for (let i2 = 0; i2 < codePoints.length; i2++) {
    if (codePoints[i2 + 1] === codePoints[i2] + 1) {
      start ?? (start = codePoints[i2]);
    } else if (start === null) {
      values.push(codePoints[i2]);
    } else {
      values.push([start, codePoints[i2]]);
      start = null;
    }
  }
  return values;
}
function getGroupPrefix(atomic2, flagMods, useFlagMods) {
  if (atomic2) {
    return ">";
  }
  let mods = "";
  if (flagMods && useFlagMods) {
    const { enable, disable } = flagMods;
    mods = ((enable == null ? void 0 : enable.ignoreCase) ? "i" : "") + ((enable == null ? void 0 : enable.dotAll) ? "s" : "") + (disable ? "-" : "") + ((disable == null ? void 0 : disable.ignoreCase) ? "i" : "") + ((disable == null ? void 0 : disable.dotAll) ? "s" : "");
  }
  return `${mods}:`;
}
function getQuantifierStr({ kind, max, min }) {
  let base;
  if (!min && max === 1) {
    base = "?";
  } else if (!min && max === Infinity) {
    base = "*";
  } else if (min === 1 && max === Infinity) {
    base = "+";
  } else if (min === max) {
    base = `{${min}}`;
  } else {
    base = `{${min},${max === Infinity ? "" : max}}`;
  }
  return base + {
    greedy: "",
    lazy: "?",
    possessive: "+"
  }[kind];
}
function isAnyGroup({ type }) {
  return type === "CapturingGroup" || type === "Group" || type === "LookaroundAssertion";
}
function isDigitCharCode(value) {
  return value > 47 && value < 58;
}
function isLiteralHyphen({ type, value }) {
  return type === "Character" && value === 45;
}
var _captureMap, _compiled, _pattern, _nameMap, _strategy, __EmulatedRegExp_instances, execCore_fn, _a4;
var EmulatedRegExp = (_a4 = class extends RegExp {
  /**
  @overload
  @param {string} pattern
  @param {string} [flags]
  @param {EmulatedRegExpOptions} [options]
  */
  /**
  @overload
  @param {EmulatedRegExp} pattern
  @param {string} [flags]
  */
  constructor(pattern, flags, options) {
    var __super = (...args) => {
      super(...args);
      __privateAdd(this, __EmulatedRegExp_instances);
      /**
      @type {Map<number, {
        hidden?: true;
        transferTo?: number;
      }>}
      */
      __privateAdd(this, _captureMap, /* @__PURE__ */ new Map());
      /**
      @type {RegExp | EmulatedRegExp | null}
      */
      __privateAdd(this, _compiled, null);
      /**
      @type {string}
      */
      __privateAdd(this, _pattern);
      /**
      @type {Map<number, string>?}
      */
      __privateAdd(this, _nameMap, null);
      /**
      @type {string?}
      */
      __privateAdd(this, _strategy, null);
      /**
      Can be used to serialize the instance.
      @type {EmulatedRegExpOptions}
      */
      __publicField(this, "rawOptions", {});
      return this;
    };
    const lazyCompile = !!(options == null ? void 0 : options.lazyCompile);
    if (pattern instanceof RegExp) {
      if (options) {
        throw new Error("Cannot provide options when copying a regexp");
      }
      const re3 = pattern;
      __super(re3, flags);
      __privateSet(this, _pattern, re3.source);
      if (re3 instanceof _a4) {
        __privateSet(this, _captureMap, __privateGet(re3, _captureMap));
        __privateSet(this, _nameMap, __privateGet(re3, _nameMap));
        __privateSet(this, _strategy, __privateGet(re3, _strategy));
        this.rawOptions = re3.rawOptions;
      }
    } else {
      const opts = {
        hiddenCaptures: [],
        strategy: null,
        transfers: [],
        ...options
      };
      __super(lazyCompile ? "" : pattern, flags);
      __privateSet(this, _pattern, pattern);
      __privateSet(this, _captureMap, createCaptureMap(opts.hiddenCaptures, opts.transfers));
      __privateSet(this, _strategy, opts.strategy);
      this.rawOptions = options ?? {};
    }
    if (!lazyCompile) {
      __privateSet(this, _compiled, this);
    }
  }
  // Override the getter with one that works with lazy-compiled regexes
  get source() {
    return __privateGet(this, _pattern) || "(?:)";
  }
  /**
  Called internally by all String/RegExp methods that use regexes.
  @override
  @param {string} str
  @returns {RegExpExecArray?}
  */
  exec(str) {
    if (!__privateGet(this, _compiled)) {
      const { lazyCompile, ...rest } = this.rawOptions;
      __privateSet(this, _compiled, new _a4(__privateGet(this, _pattern), this.flags, rest));
    }
    const useLastIndex = this.global || this.sticky;
    const pos = this.lastIndex;
    if (__privateGet(this, _strategy) === "clip_search" && useLastIndex && pos) {
      this.lastIndex = 0;
      const match = __privateMethod(this, __EmulatedRegExp_instances, execCore_fn).call(this, str.slice(pos));
      if (match) {
        adjustMatchDetailsForOffset(match, pos, str, this.hasIndices);
        this.lastIndex += pos;
      }
      return match;
    }
    return __privateMethod(this, __EmulatedRegExp_instances, execCore_fn).call(this, str);
  }
}, _captureMap = new WeakMap(), _compiled = new WeakMap(), _pattern = new WeakMap(), _nameMap = new WeakMap(), _strategy = new WeakMap(), __EmulatedRegExp_instances = new WeakSet(), /**
Adds support for hidden and transfer captures.
@param {string} str
@returns
*/
execCore_fn = function(str) {
  __privateGet(this, _compiled).lastIndex = this.lastIndex;
  const match = __superGet(_a4.prototype, this, "exec").call(__privateGet(this, _compiled), str);
  this.lastIndex = __privateGet(this, _compiled).lastIndex;
  if (!match || !__privateGet(this, _captureMap).size) {
    return match;
  }
  const matchCopy = [...match];
  match.length = 1;
  let indicesCopy;
  if (this.hasIndices) {
    indicesCopy = [...match.indices];
    match.indices.length = 1;
  }
  const mappedNums = [0];
  for (let i2 = 1; i2 < matchCopy.length; i2++) {
    const { hidden, transferTo } = __privateGet(this, _captureMap).get(i2) ?? {};
    if (hidden) {
      mappedNums.push(null);
    } else {
      mappedNums.push(match.length);
      match.push(matchCopy[i2]);
      if (this.hasIndices) {
        match.indices.push(indicesCopy[i2]);
      }
    }
    if (transferTo && matchCopy[i2] !== void 0) {
      const to = mappedNums[transferTo];
      if (!to) {
        throw new Error(`Invalid capture transfer to "${to}"`);
      }
      match[to] = matchCopy[i2];
      if (this.hasIndices) {
        match.indices[to] = indicesCopy[i2];
      }
      if (match.groups) {
        if (!__privateGet(this, _nameMap)) {
          __privateSet(this, _nameMap, createNameMap(this.source));
        }
        const name = __privateGet(this, _nameMap).get(transferTo);
        if (name) {
          match.groups[name] = matchCopy[i2];
          if (this.hasIndices) {
            match.indices.groups[name] = indicesCopy[i2];
          }
        }
      }
    }
  }
  return match;
}, _a4);
function adjustMatchDetailsForOffset(match, offset, input, hasIndices) {
  match.index += offset;
  match.input = input;
  if (hasIndices) {
    const indices = match.indices;
    for (let i2 = 0; i2 < indices.length; i2++) {
      const arr = indices[i2];
      if (arr) {
        indices[i2] = [arr[0] + offset, arr[1] + offset];
      }
    }
    const groupIndices = indices.groups;
    if (groupIndices) {
      Object.keys(groupIndices).forEach((key2) => {
        const arr = groupIndices[key2];
        if (arr) {
          groupIndices[key2] = [arr[0] + offset, arr[1] + offset];
        }
      });
    }
  }
}
function createCaptureMap(hiddenCaptures, transfers) {
  const captureMap = /* @__PURE__ */ new Map();
  for (const num of hiddenCaptures) {
    captureMap.set(num, {
      hidden: true
    });
  }
  for (const [to, from] of transfers) {
    for (const num of from) {
      getOrInsert(captureMap, num, {}).transferTo = to;
    }
  }
  return captureMap;
}
function createNameMap(pattern) {
  const re3 = /(?<capture>\((?:\?<(?![=!])(?<name>[^>]+)>|(?!\?)))|\\?./gsu;
  const map = /* @__PURE__ */ new Map();
  let numCharClassesOpen = 0;
  let numCaptures = 0;
  let match;
  while (match = re3.exec(pattern)) {
    const { 0: m3, groups: { capture, name } } = match;
    if (m3 === "[") {
      numCharClassesOpen++;
    } else if (!numCharClassesOpen) {
      if (capture) {
        numCaptures++;
        if (name) {
          map.set(numCaptures, name);
        }
      }
    } else if (m3 === "]") {
      numCharClassesOpen--;
    }
  }
  return map;
}
function toRegExp(pattern, options) {
  const d2 = toRegExpDetails(pattern, options);
  if (d2.options) {
    return new EmulatedRegExp(d2.pattern, d2.flags, d2.options);
  }
  return new RegExp(d2.pattern, d2.flags);
}
function toRegExpDetails(pattern, options) {
  const opts = getOptions(options);
  const onigurumaAst = J2(pattern, {
    flags: opts.flags,
    normalizeUnknownPropertyNames: true,
    rules: {
      captureGroup: opts.rules.captureGroup,
      singleline: opts.rules.singleline
    },
    skipBackrefValidation: opts.rules.allowOrphanBackrefs,
    unicodePropertyMap: JsUnicodePropertyMap
  });
  const regexPlusAst = transform(onigurumaAst, {
    accuracy: opts.accuracy,
    asciiWordBoundaries: opts.rules.asciiWordBoundaries,
    avoidSubclass: opts.avoidSubclass,
    bestEffortTarget: opts.target
  });
  const generated = generate(regexPlusAst, opts);
  const recursionResult = recursion(generated.pattern, {
    captureTransfers: generated._captureTransfers,
    hiddenCaptures: generated._hiddenCaptures,
    mode: "external"
  });
  const possessiveResult = possessive(recursionResult.pattern);
  const atomicResult = atomic(possessiveResult.pattern, {
    captureTransfers: recursionResult.captureTransfers,
    hiddenCaptures: recursionResult.hiddenCaptures
  });
  const details = {
    pattern: atomicResult.pattern,
    flags: `${opts.hasIndices ? "d" : ""}${opts.global ? "g" : ""}${generated.flags}${generated.options.disable.v ? "u" : "v"}`
  };
  if (opts.avoidSubclass) {
    if (opts.lazyCompileLength !== Infinity) {
      throw new Error("Lazy compilation requires subclass");
    }
  } else {
    const hiddenCaptures = atomicResult.hiddenCaptures.sort((a2, b3) => a2 - b3);
    const transfers = Array.from(atomicResult.captureTransfers);
    const strategy = regexPlusAst._strategy;
    const lazyCompile = details.pattern.length >= opts.lazyCompileLength;
    if (hiddenCaptures.length || transfers.length || strategy || lazyCompile) {
      details.options = {
        ...hiddenCaptures.length && { hiddenCaptures },
        ...transfers.length && { transfers },
        ...strategy && { strategy },
        ...lazyCompile && { lazyCompile }
      };
    }
  }
  return details;
}

// ../../node_modules/.pnpm/@shikijs+engine-javascript@4.4.3/node_modules/@shikijs/engine-javascript/dist/engine-compile.mjs
function defaultJavaScriptRegexConstructor(pattern, options) {
  return toRegExp(pattern, {
    global: true,
    hasIndices: true,
    lazyCompileLength: 3e3,
    rules: {
      allowOrphanBackrefs: true,
      asciiWordBoundaries: true,
      captureGroup: true,
      recursionLimit: 5,
      singleline: true
    },
    ...options
  });
}
function createJavaScriptRegexEngine(options = {}) {
  const _options = {
    target: "auto",
    cache: /* @__PURE__ */ new Map(),
    ...options
  };
  _options.regexConstructor || (_options.regexConstructor = (pattern) => defaultJavaScriptRegexConstructor(pattern, { target: _options.target }));
  return {
    createScanner(patterns) {
      return new JavaScriptScanner(patterns, _options);
    },
    createString(s2) {
      return { content: s2 };
    }
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/resolveLanguage.js
async function resolveLanguage(lang) {
  if (isWorkerContext()) throw new Error(`resolveLanguage("${lang}") cannot be called from a worker context. Languages must be pre-resolved on the main thread and passed to the worker via the resolvedLanguages parameter.`);
  const resolver = ResolvingLanguages.get(lang);
  if (resolver != null) return resolver;
  try {
    let loader = RegisteredCustomLanguages.get(lang);
    if (loader == null && Object.prototype.hasOwnProperty.call(bundledLanguages, lang)) loader = bundledLanguages[lang];
    if (loader == null) throw new Error(`resolveLanguage: "${lang}" not found in bundled or custom languages`);
    const resolver2 = loader().then(({ default: data }) => {
      const resolvedLang = {
        name: lang,
        data
      };
      if (!ResolvedLanguages.has(lang)) ResolvedLanguages.set(lang, resolvedLang);
      return resolvedLang;
    });
    ResolvingLanguages.set(lang, resolver2);
    return await resolver2;
  } finally {
    ResolvingLanguages.delete(lang);
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/getResolvedOrResolveLanguage.js
function getResolvedOrResolveLanguage(language) {
  return ResolvedLanguages.get(language) ?? resolveLanguage(language);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/constants.js
var AttachedThemes = /* @__PURE__ */ new Set();

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/createThemeCollection.js
function createThemeCollection(options) {
  const descriptors = [];
  const seen = /* @__PURE__ */ new Set();
  for (const entry of getCollectionEntries(options.themes)) {
    const themes2 = isThemeCollectionSource(entry) ? entry.getThemes() : [entry];
    for (const descriptor of themes2) {
      if (seen.has(descriptor.name)) throw new Error(`Theme collection already contains theme "${descriptor.name}"`);
      seen.add(descriptor.name);
      descriptors.push(descriptor);
    }
  }
  const allThemes = Object.freeze([...descriptors]);
  const lightThemes = Object.freeze(allThemes.filter((descriptor) => descriptor.colorScheme === "light"));
  const darkThemes = Object.freeze(allThemes.filter((descriptor) => descriptor.colorScheme === "dark"));
  const themesByName = new Map(allThemes.map((descriptor) => [descriptor.name, descriptor]));
  const allNames = Object.freeze(allThemes.map((descriptor) => descriptor.name));
  const lightNames = Object.freeze(lightThemes.map((descriptor) => descriptor.name));
  const darkNames = Object.freeze(darkThemes.map((descriptor) => descriptor.name));
  function filteredThemes(filterOptions) {
    if (filterOptions == null) return allThemes;
    const { colorScheme, collection } = filterOptions;
    if (collection == null) {
      if (colorScheme === "light") return lightThemes;
      if (colorScheme === "dark") return darkThemes;
      return allThemes;
    }
    return allThemes.filter((descriptor) => {
      if (descriptor.collection !== collection) return false;
      return colorScheme == null || descriptor.colorScheme === colorScheme;
    });
  }
  return {
    getTheme(name) {
      return themesByName.get(name);
    },
    getThemes(themeOptions) {
      return filteredThemes(themeOptions);
    },
    getThemeNames(namesOptions) {
      if ((namesOptions == null ? void 0 : namesOptions.collection) == null) {
        if ((namesOptions == null ? void 0 : namesOptions.colorScheme) === "light") return lightNames;
        if ((namesOptions == null ? void 0 : namesOptions.colorScheme) === "dark") return darkNames;
        return allNames;
      }
      return filteredThemes(namesOptions).map((descriptor) => descriptor.name);
    },
    hasTheme(name) {
      return themesByName.has(name);
    },
    orderBy(compare) {
      return createThemeCollection({ themes: allThemes.map((descriptor, index) => ({
        descriptor,
        index
      })).sort((a2, b3) => {
        const result = compare(a2.descriptor, b3.descriptor);
        if (result !== 0) return result;
        return a2.index - b3.index;
      }).map((entry) => entry.descriptor) });
    },
    pick(names) {
      const picked = [];
      const pickedNames = /* @__PURE__ */ new Set();
      for (const name of names) {
        if (pickedNames.has(name)) throw new Error(`Theme collection pick already includes theme "${name}"`);
        pickedNames.add(name);
        const descriptor = themesByName.get(name);
        if (descriptor == null) throw new Error(`Theme collection does not contain theme "${name}"`);
        picked.push(descriptor);
      }
      return createThemeCollection({ themes: picked });
    },
    registerInto(resolver) {
      for (const descriptor of allThemes) resolver.registerThemeIfAbsent(descriptor.name, descriptor.load);
    }
  };
}
function getCollectionEntries(input) {
  if (isThemeCollectionEntry(input)) return [input];
  return input;
}
function isThemeCollectionEntry(input) {
  return isThemeCollectionSource(input) || isThemeDescriptor(input);
}
function isThemeDescriptor(input) {
  return typeof input.name === "string" && typeof input.load === "function";
}
function isThemeCollectionSource(entry) {
  return typeof entry.getThemes === "function";
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/unwrapDefault.js
function unwrapDefault(value) {
  return value !== null && typeof value === "object" && "default" in value ? value.default : value;
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/createThemeResolver.js
var DuplicateThemeError = class extends Error {
  constructor(name) {
    super(`Theme "${name}" is already registered`);
    this.name = "DuplicateThemeError";
  }
};
var UnregisteredThemeError = class extends Error {
  constructor(name) {
    super(`No loader registered for theme "${name}"`);
    this.name = "UnregisteredThemeError";
  }
};
var UnresolvedThemeError = class extends Error {
  constructor(name) {
    super(`Theme "${name}" has not been resolved`);
    this.name = "UnresolvedThemeError";
  }
};
function createThemeResolver() {
  const loaders = /* @__PURE__ */ new Map();
  const resolved = /* @__PURE__ */ new Map();
  const inflight = /* @__PURE__ */ new Map();
  let cacheGeneration = 0;
  function registerTheme(name, loader) {
    if (loaders.has(name)) throw new DuplicateThemeError(name);
    loaders.set(name, loader);
  }
  function registerThemeIfAbsent(name, loader) {
    if (loaders.has(name)) return false;
    loaders.set(name, loader);
    return true;
  }
  function hasRegisteredTheme(name) {
    return loaders.has(name);
  }
  function resolveTheme2(name) {
    const cached = resolved.get(name);
    if (cached !== void 0) return Promise.resolve(cached);
    const existing = inflight.get(name);
    if (existing !== void 0) return existing;
    const loader = loaders.get(name);
    if (loader === void 0) return Promise.reject(new UnregisteredThemeError(name));
    const generation = cacheGeneration;
    const promise = loader().then((result) => {
      const theme = unwrapDefault(result);
      if (generation === cacheGeneration) resolved.set(name, theme);
      if (inflight.get(name) === promise) inflight.delete(name);
      return theme;
    }).catch((err) => {
      if (inflight.get(name) === promise) inflight.delete(name);
      throw err;
    });
    inflight.set(name, promise);
    return promise;
  }
  function resolveThemes3(names) {
    return Promise.all(names.map((name) => resolveTheme2(name)));
  }
  function seedResolvedTheme(name, theme) {
    resolved.set(name, theme);
  }
  function seedResolvedThemes(entries) {
    for (const [name, theme] of entries) seedResolvedTheme(name, theme);
  }
  function getResolvedTheme(name) {
    return resolved.get(name);
  }
  function getResolvedThemes2(names) {
    const themes2 = [];
    for (const name of names) {
      const theme = resolved.get(name);
      if (theme === void 0) throw new UnresolvedThemeError(name);
      themes2.push(theme);
    }
    return themes2;
  }
  function hasResolvedTheme(name) {
    return resolved.has(name);
  }
  function hasResolvedThemes2(names) {
    for (const name of names) if (!resolved.has(name)) return false;
    return true;
  }
  function getResolvedOrResolveTheme2(name) {
    const cached = resolved.get(name);
    if (cached !== void 0) return cached;
    return resolveTheme2(name);
  }
  function clearResolvedThemes() {
    cacheGeneration++;
    resolved.clear();
    inflight.clear();
  }
  return {
    clearResolvedThemes,
    getResolvedOrResolveTheme: getResolvedOrResolveTheme2,
    getResolvedTheme,
    getResolvedThemes: getResolvedThemes2,
    hasRegisteredTheme,
    hasResolvedTheme,
    hasResolvedThemes: hasResolvedThemes2,
    registerTheme,
    registerThemeIfAbsent,
    resolveTheme: resolveTheme2,
    resolveThemes: resolveThemes3,
    seedResolvedTheme,
    seedResolvedThemes
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/themeResolver.js
var themeResolver = createThemeResolver();

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/attachResolvedThemes.js
function attachResolvedThemes(themes2, highlighter2) {
  themes2 = Array.isArray(themes2) ? themes2 : [themes2];
  for (let themeRef of themes2) {
    let resolvedTheme;
    if (typeof themeRef === "string") {
      resolvedTheme = themeResolver.getResolvedTheme(themeRef);
      if (resolvedTheme == null) throw new Error(`loadResolvedThemes: ${themeRef} is not resolved, you must resolve it before calling loadResolvedThemes`);
    } else {
      resolvedTheme = themeRef;
      themeRef = themeRef.name;
      if (themeResolver.getResolvedTheme(themeRef) == null) themeResolver.seedResolvedTheme(themeRef, resolvedTheme);
    }
    if (AttachedThemes.has(themeRef)) continue;
    AttachedThemes.add(themeRef);
    highlighter2.loadThemeSync(resolvedTheme);
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/cleanUpResolvedThemes.js
function cleanUpResolvedThemes() {
  themeResolver.clearResolvedThemes();
  AttachedThemes.clear();
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/createTheme.js
function createTheme({ name, load, colorScheme, collection, displayName }) {
  return {
    name,
    colorScheme,
    collection,
    displayName,
    load: normalizingLoader(load)
  };
}
function normalizingLoader(loader) {
  return async () => {
    return normalizeTheme(unwrapDefault(await loader()));
  };
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/collections/pierre.js
var PIERRE_COLLECTION = "pierre";
var DARK_PIERRE_THEMES = [
  "pierre-dark",
  "pierre-dark-soft",
  "pierre-dark-vibrant",
  "pierre-dark-protanopia-deuteranopia",
  "pierre-dark-tritanopia"
];
var LIGHT_PIERRE_THEMES = [
  "pierre-light",
  "pierre-light-soft",
  "pierre-light-vibrant",
  "pierre-light-protanopia-deuteranopia",
  "pierre-light-tritanopia"
];
var PIERRE_THEMES = [...LIGHT_PIERRE_THEMES, ...DARK_PIERRE_THEMES];
var LIGHT_PIERRE_THEME_NAMES = new Set(LIGHT_PIERRE_THEMES);
function pierreColorScheme(name) {
  if (LIGHT_PIERRE_THEME_NAMES.has(name)) return "light";
  return "dark";
}
var PIERRE_THEME_DISPLAY_NAMES = {
  "pierre-dark": "Pierre Dark",
  "pierre-dark-soft": "Pierre Dark Soft",
  "pierre-dark-vibrant": "Pierre Dark Vibrant",
  "pierre-dark-protanopia-deuteranopia": "Pierre Dark Protanopia & Deuteranopia",
  "pierre-dark-tritanopia": "Pierre Dark Tritanopia",
  "pierre-light": "Pierre Light",
  "pierre-light-soft": "Pierre Light Soft",
  "pierre-light-vibrant": "Pierre Light Vibrant",
  "pierre-light-protanopia-deuteranopia": "Pierre Light Protanopia & Deuteranopia",
  "pierre-light-tritanopia": "Pierre Light Tritanopia"
};
var PIERRE_THEME_IMPORTS = {
  "pierre-dark": () => import("./pierre-dark-SDO4HLHN.js"),
  "pierre-dark-soft": () => import("./pierre-dark-soft-45AVTE5X.js"),
  "pierre-dark-vibrant": () => import("./pierre-dark-vibrant-22M3HHE6.js"),
  "pierre-dark-protanopia-deuteranopia": () => import("./pierre-dark-protanopia-deuteranopia-6W5S2RIC.js"),
  "pierre-dark-tritanopia": () => import("./pierre-dark-tritanopia-3POVQ24R.js"),
  "pierre-light": () => import("./pierre-light-KHEB3YAQ.js"),
  "pierre-light-soft": () => import("./pierre-light-soft-32GSNRPG.js"),
  "pierre-light-vibrant": () => import("./pierre-light-vibrant-YCXG5OU7.js"),
  "pierre-light-protanopia-deuteranopia": () => import("./pierre-light-protanopia-deuteranopia-552CGNJZ.js"),
  "pierre-light-tritanopia": () => import("./pierre-light-tritanopia-B5XGUVCB.js")
};
function createPierreTheme(name) {
  return createTheme({
    name,
    collection: PIERRE_COLLECTION,
    colorScheme: pierreColorScheme(name),
    displayName: PIERRE_THEME_DISPLAY_NAMES[name],
    load: PIERRE_THEME_IMPORTS[name]
  });
}
var pierreThemes = createThemeCollection({ themes: PIERRE_THEMES.map((name) => createPierreTheme(name)) });

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/collections/shiki.js
var SHIKI_COLLECTION = "shiki";
var LIGHT_SHIKI_THEMES = [
  "ayu-light",
  "catppuccin-latte",
  "everforest-light",
  "github-light",
  "github-light-default",
  "github-light-high-contrast",
  "gruvbox-light-hard",
  "gruvbox-light-medium",
  "gruvbox-light-soft",
  "horizon-bright",
  "kanagawa-lotus",
  "light-plus",
  "material-theme-lighter",
  "min-light",
  "night-owl-light",
  "one-light",
  "rose-pine-dawn",
  "slack-ochin",
  "snazzy-light",
  "solarized-light",
  "vitesse-light"
];
var DARK_SHIKI_THEMES = [
  "andromeeda",
  "aurora-x",
  "ayu-dark",
  "ayu-mirage",
  "catppuccin-frappe",
  "catppuccin-macchiato",
  "catppuccin-mocha",
  "dark-plus",
  "dracula",
  "dracula-soft",
  "everforest-dark",
  "github-dark",
  "github-dark-default",
  "github-dark-dimmed",
  "github-dark-high-contrast",
  "gruvbox-dark-hard",
  "gruvbox-dark-medium",
  "gruvbox-dark-soft",
  "horizon",
  "houston",
  "kanagawa-dragon",
  "kanagawa-wave",
  "laserwave",
  "material-theme",
  "material-theme-darker",
  "material-theme-ocean",
  "material-theme-palenight",
  "min-dark",
  "monokai",
  "night-owl",
  "nord",
  "one-dark-pro",
  "plastic",
  "poimandres",
  "red",
  "rose-pine",
  "rose-pine-moon",
  "slack-dark",
  "solarized-dark",
  "synthwave-84",
  "tokyo-night",
  "vesper",
  "vitesse-black",
  "vitesse-dark"
];
var LIGHT_SHIKI_THEME_NAMES = new Set(LIGHT_SHIKI_THEMES);
function shikiColorScheme(name) {
  if (LIGHT_SHIKI_THEME_NAMES.has(name)) return "light";
  return "dark";
}
var SHIKI_THEME_IMPORTS = {
  andromeeda: () => import("./andromeeda-AOXWUYCD.js"),
  "aurora-x": () => import("./aurora-x-Y5I222WX.js"),
  "ayu-dark": () => import("./ayu-dark-43FKJ2HV.js"),
  "ayu-light": () => import("./ayu-light-T6RHLFMG.js"),
  "ayu-mirage": () => import("./ayu-mirage-CPLREM2F.js"),
  "catppuccin-frappe": () => import("./catppuccin-frappe-SPWKBM4C.js"),
  "catppuccin-latte": () => import("./catppuccin-latte-2ET5Q4IP.js"),
  "catppuccin-macchiato": () => import("./catppuccin-macchiato-56EE4FKY.js"),
  "catppuccin-mocha": () => import("./catppuccin-mocha-WQTZO6LU.js"),
  "dark-plus": () => import("./dark-plus-7KK7RBIA.js"),
  dracula: () => import("./dracula-6V2P5RZO.js"),
  "dracula-soft": () => import("./dracula-soft-2DACMPVJ.js"),
  "everforest-dark": () => import("./everforest-dark-PPUNNLOP.js"),
  "everforest-light": () => import("./everforest-light-G5TUMNNY.js"),
  "github-dark": () => import("./github-dark-E2PXRWSB.js"),
  "github-dark-default": () => import("./github-dark-default-JDD7CKOU.js"),
  "github-dark-dimmed": () => import("./github-dark-dimmed-WSMMNXSC.js"),
  "github-dark-high-contrast": () => import("./github-dark-high-contrast-GD6Z2VLO.js"),
  "github-light": () => import("./github-light-HDOCFNI6.js"),
  "github-light-default": () => import("./github-light-default-2YRQGMIP.js"),
  "github-light-high-contrast": () => import("./github-light-high-contrast-UX2KBXGX.js"),
  "gruvbox-dark-hard": () => import("./gruvbox-dark-hard-KBCDDNGN.js"),
  "gruvbox-dark-medium": () => import("./gruvbox-dark-medium-5YADEZU2.js"),
  "gruvbox-dark-soft": () => import("./gruvbox-dark-soft-LW33YJ2E.js"),
  "gruvbox-light-hard": () => import("./gruvbox-light-hard-NIEWFKGM.js"),
  "gruvbox-light-medium": () => import("./gruvbox-light-medium-B7NTGAJ5.js"),
  "gruvbox-light-soft": () => import("./gruvbox-light-soft-PZGR73FS.js"),
  horizon: () => import("./horizon-3DQUVO4E.js"),
  "horizon-bright": () => import("./horizon-bright-N5HYZIQE.js"),
  houston: () => import("./houston-JIK55JAW.js"),
  "kanagawa-dragon": () => import("./kanagawa-dragon-5IP7V5UD.js"),
  "kanagawa-lotus": () => import("./kanagawa-lotus-QOJZROII.js"),
  "kanagawa-wave": () => import("./kanagawa-wave-TWUTETAO.js"),
  laserwave: () => import("./laserwave-NCJNREH4.js"),
  "light-plus": () => import("./light-plus-CSORJ5XF.js"),
  "material-theme": () => import("./material-theme-7NAVULTZ.js"),
  "material-theme-darker": () => import("./material-theme-darker-5WSXOBL2.js"),
  "material-theme-lighter": () => import("./material-theme-lighter-HF4O3LCC.js"),
  "material-theme-ocean": () => import("./material-theme-ocean-QSVERJWA.js"),
  "material-theme-palenight": () => import("./material-theme-palenight-XEH2GOSH.js"),
  "min-dark": () => import("./min-dark-FYBZBATQ.js"),
  "min-light": () => import("./min-light-KNUMGRHM.js"),
  monokai: () => import("./monokai-PMXI5R32.js"),
  "night-owl": () => import("./night-owl-HR37S3NF.js"),
  "night-owl-light": () => import("./night-owl-light-FDJ6OHOB.js"),
  nord: () => import("./nord-AWABQVCW.js"),
  "one-dark-pro": () => import("./one-dark-pro-5DBQEU6O.js"),
  "one-light": () => import("./one-light-XRA6B6TJ.js"),
  plastic: () => import("./plastic-UBQWKD7K.js"),
  poimandres: () => import("./poimandres-A5PSBSBV.js"),
  red: () => import("./red-4OI7RKVI.js"),
  "rose-pine": () => import("./rose-pine-DCJTFLS6.js"),
  "rose-pine-dawn": () => import("./rose-pine-dawn-QB3TMHUA.js"),
  "rose-pine-moon": () => import("./rose-pine-moon-BWXLXPMV.js"),
  "slack-dark": () => import("./slack-dark-4CCKONH2.js"),
  "slack-ochin": () => import("./slack-ochin-36LSQJUW.js"),
  "snazzy-light": () => import("./snazzy-light-IDSSK35S.js"),
  "solarized-dark": () => import("./solarized-dark-47IKOCXE.js"),
  "solarized-light": () => import("./solarized-light-AA4B3GJK.js"),
  "synthwave-84": () => import("./synthwave-84-ZOVHJQGK.js"),
  "tokyo-night": () => import("./tokyo-night-XGYRI2VO.js"),
  vesper: () => import("./vesper-7LSLTJUJ.js"),
  "vitesse-black": () => import("./vitesse-black-2HBQYBY2.js"),
  "vitesse-dark": () => import("./vitesse-dark-S4HVBICM.js"),
  "vitesse-light": () => import("./vitesse-light-N4DNXMJD.js")
};
function createShikiTheme(name) {
  return createTheme({
    name,
    collection: SHIKI_COLLECTION,
    colorScheme: shikiColorScheme(name),
    load: SHIKI_THEME_IMPORTS[name]
  });
}
var shikiThemes = createThemeCollection({ themes: Object.freeze([...LIGHT_SHIKI_THEMES.map((name) => createShikiTheme(name)), ...DARK_SHIKI_THEMES.map((name) => createShikiTheme(name))]) });

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/themes.js
var themes = createThemeCollection({ themes: [pierreThemes, shikiThemes] });

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/themeResolution.js
function prepareThemeResolution(themeName) {
  if (isWorkerContext()) throw new Error(`Theme "${themeName}" cannot be resolved from a worker context. Themes must be pre-resolved on the main thread and passed to the worker via the resolvedLanguages parameter.`);
  if (themeResolver.hasRegisteredTheme(themeName)) return;
  const descriptor = shikiThemes.getTheme(themeName);
  if (descriptor != null) {
    themeResolver.registerThemeIfAbsent(descriptor.name, descriptor.load);
    return;
  }
  throw new Error(`No valid theme loader registered for "${themeName}"`);
}
function validateResolvedThemeName(themeName, theme) {
  if (theme.name !== themeName) throw new Error(`resolvedTheme: themeName: ${themeName} does not match theme.name: ${theme.name}`);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/resolveTheme.js
async function resolveTheme(themeName) {
  prepareThemeResolution(themeName);
  const theme = await themeResolver.resolveTheme(themeName);
  validateResolvedThemeName(themeName, theme);
  return theme;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/getResolvedOrResolveTheme.js
function getResolvedOrResolveTheme(themeName) {
  return themeResolver.getResolvedTheme(themeName) ?? resolveTheme(themeName);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/shared_highlighter.js
var highlighter;
async function getSharedHighlighter({ themes: themes2, langs, preferredHighlighter = "shiki-js" }) {
  highlighter ?? (highlighter = createHighlighter({
    themes: [],
    langs: ["text"],
    engine: preferredHighlighter === "shiki-wasm" ? createOnigurumaEngine(import("./wasm-5RJGDYBN.js")) : createJavaScriptRegexEngine()
  }));
  const instance2 = isHighlighterLoading(highlighter) ? await highlighter : highlighter;
  highlighter = instance2;
  const languageLoaders = [];
  for (const language of langs) {
    if (language === "text" || language === "ansi") continue;
    const maybeResolvedLanguage = getResolvedOrResolveLanguage(language);
    if ("then" in maybeResolvedLanguage) languageLoaders.push(maybeResolvedLanguage);
    else attachResolvedLanguages(maybeResolvedLanguage, instance2);
  }
  const themeLoaders = [];
  for (const themeName of themes2) {
    const maybeResolvedTheme = getResolvedOrResolveTheme(themeName);
    if ("then" in maybeResolvedTheme) themeLoaders.push(maybeResolvedTheme);
    else attachResolvedThemes(maybeResolvedTheme, highlighter);
  }
  if (languageLoaders.length > 0 || themeLoaders.length > 0) await Promise.all([Promise.all(languageLoaders).then((languages) => {
    attachResolvedLanguages(languages, instance2);
  }), Promise.all(themeLoaders).then((themes3) => {
    attachResolvedThemes(themes3, instance2);
  })]);
  return instance2;
}
function isHighlighterLoaded(h2 = highlighter) {
  return h2 != null && !("then" in h2);
}
function getHighlighterIfLoaded() {
  if (highlighter != null && !("then" in highlighter)) return highlighter;
}
function isHighlighterLoading(h2 = highlighter) {
  return h2 != null && "then" in h2;
}
function isHighlighterNull(h2 = highlighter) {
  return h2 == null;
}
async function preloadHighlighter(options) {
  await getSharedHighlighter(options);
}
async function disposeHighlighter() {
  if (highlighter == null) return;
  (await highlighter).dispose();
  cleanUpResolvedLanguages();
  cleanUpResolvedThemes();
  highlighter = void 0;
}
for (const descriptor of pierreThemes.getThemes()) themeResolver.registerThemeIfAbsent(descriptor.name, descriptor.load);

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getThemes.js
function getThemes(theme = DEFAULT_THEMES) {
  const themesArr = [];
  if (typeof theme === "string") themesArr.push(theme);
  else {
    themesArr.push(theme.dark);
    themesArr.push(theme.light);
  }
  return themesArr;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/areThemesAttached.js
function areThemesAttached(themes2) {
  for (const theme of getThemes(themes2)) if (!AttachedThemes.has(theme)) return false;
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/hasResolvedThemes.js
function hasResolvedThemes(themeNames) {
  return themeResolver.hasResolvedThemes(themeNames);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areFileRenderOptionsEqual.js
function areFileRenderOptionsEqual(optionsA, optionsB) {
  return areThemesEqual(optionsA.theme, optionsB.theme) && optionsA.useTokenTransformer === optionsB.useTokenTransformer && optionsA.tokenizeMaxLineLength === optionsB.tokenizeMaxLineLength;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areRenderRangesEqual.js
function areRenderRangesEqual(renderRangeA, renderRangeB) {
  if (renderRangeA == null || renderRangeB == null) return renderRangeA === renderRangeB;
  return renderRangeA.startingLine === renderRangeB.startingLine && renderRangeA.totalLines === renderRangeB.totalLines && renderRangeA.bufferBefore === renderRangeB.bufferBefore && renderRangeA.bufferAfter === renderRangeB.bufferAfter;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createAnnotationElement.js
function createAnnotationElement(span) {
  var _a9;
  return createHastElement({
    tagName: "div",
    children: [createHastElement({
      tagName: "div",
      children: (_a9 = span.annotations) == null ? void 0 : _a9.map((slotId) => createHastElement({
        tagName: "slot",
        properties: { name: slotId }
      })),
      properties: { "data-annotation-content": "" }
    })],
    properties: { "data-line-annotation": `${span.hunkIndex},${span.lineIndex}` }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getIconForType.js
function getIconForType(type) {
  switch (type) {
    case "file":
      return "diffs-icon-file-code";
    case "change":
      return "diffs-icon-symbol-modified";
    case "new":
      return "diffs-icon-symbol-added";
    case "deleted":
      return "diffs-icon-symbol-deleted";
    case "rename-pure":
    case "rename-changed":
      return "diffs-icon-symbol-moved";
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createFileHeaderElement.js
function createFileHeaderElement({ fileOrDiff, mode, stickyHeader }) {
  const fileDiff = "type" in fileOrDiff ? fileOrDiff : void 0;
  const properties = {
    "data-diffs-header": mode,
    "data-change-type": fileDiff == null ? void 0 : fileDiff.type,
    "data-sticky": stickyHeader ? "" : void 0
  };
  return createHastElement({
    tagName: "div",
    children: [mode === "custom" ? createHastElement({
      tagName: "slot",
      properties: { name: CUSTOM_HEADER_SLOT_ID }
    }) : createHeaderElement({
      name: fileOrDiff.name,
      prevName: "prevName" in fileOrDiff ? fileOrDiff.prevName : void 0,
      iconType: (fileDiff == null ? void 0 : fileDiff.type) ?? "file"
    }), ...mode === "custom" ? [] : [createMetadataElement(fileDiff)]],
    properties
  });
}
function createHeaderElement({ name, prevName, iconType }) {
  const children = [createHastElement({
    tagName: "slot",
    properties: { name: HEADER_PREFIX_SLOT_ID }
  }), createIconElement({
    name: getIconForType(iconType),
    properties: { "data-change-icon": iconType }
  })];
  if (prevName != null) {
    children.push(createHastElement({
      tagName: "div",
      children: [createHastElement({
        tagName: "bdi",
        children: [createTextNodeElement(prevName)]
      })],
      properties: { "data-prev-name": "" }
    }));
    children.push(createIconElement({
      name: "diffs-icon-arrow-right-short",
      properties: { "data-rename-icon": "" }
    }));
  }
  children.push(createHastElement({
    tagName: "div",
    children: [createHastElement({
      tagName: "bdi",
      children: [createTextNodeElement(name)]
    })],
    properties: { "data-title": "" }
  }));
  children.push(createHastElement({
    tagName: "slot",
    properties: { name: HEADER_FILENAME_SUFFIX_SLOT_ID }
  }));
  return createHastElement({
    tagName: "div",
    children,
    properties: { "data-header-content": "" }
  });
}
function createMetadataElement(fileDiff) {
  const children = [];
  if (fileDiff != null) {
    let additions = 0;
    let deletions = 0;
    for (const hunk of fileDiff.hunks) {
      additions += hunk.additionLines;
      deletions += hunk.deletionLines;
    }
    if (deletions > 0 || additions === 0) children.push(createHastElement({
      tagName: "span",
      children: [createTextNodeElement(`-${deletions}`)],
      properties: { "data-deletions-count": "" }
    }));
    if (additions > 0 || deletions === 0) children.push(createHastElement({
      tagName: "span",
      children: [createTextNodeElement(`+${additions}`)],
      properties: { "data-additions-count": "" }
    }));
  }
  children.push(createHastElement({
    tagName: "slot",
    properties: { name: HEADER_METADATA_SLOT_ID }
  }));
  return createHastElement({
    tagName: "div",
    children,
    properties: { "data-metadata": "" }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createPreElement.js
function createPreElement(options) {
  return createHastElement({
    tagName: "pre",
    properties: createPreWrapperProperties(options)
  });
}
function createPreWrapperProperties({ diffIndicators, disableBackground, disableLineNumbers, overflow, split, totalLines, type, customProperties }) {
  return {
    ...customProperties,
    "data-diff": type === "diff" ? "" : void 0,
    "data-file": type === "file" ? "" : void 0,
    "data-diff-type": type === "diff" ? split ? "split" : "single" : void 0,
    "data-overflow": overflow,
    "data-disable-line-numbers": disableLineNumbers ? "" : void 0,
    "data-background": !disableBackground ? "" : void 0,
    "data-indicators": diffIndicators === "bars" || diffIndicators === "classic" ? diffIndicators : void 0,
    style: `--diffs-min-number-column-width-default:${`${totalLines}`.length}ch;`
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getFiletypeFromFileName.js
var CUSTOM_EXTENSION_TO_FILE_FORMAT = /* @__PURE__ */ new Map();
var customExtensionsVersion = 0;
var EXTENSION_TO_FILE_FORMAT = {
  "1c": "1c",
  abap: "abap",
  as: "actionscript-3",
  ada: "ada",
  adb: "ada",
  ads: "ada",
  adoc: "asciidoc",
  asciidoc: "asciidoc",
  "component.html": "angular-html",
  "component.ts": "angular-ts",
  conf: "nginx",
  htaccess: "apache",
  cls: "tex",
  trigger: "apex",
  apl: "apl",
  applescript: "applescript",
  scpt: "applescript",
  ara: "ara",
  asm: "asm",
  s: "riscv",
  astro: "astro",
  awk: "awk",
  bal: "ballerina",
  sh: "zsh",
  bash: "zsh",
  bat: "cmd",
  cmd: "cmd",
  be: "berry",
  beancount: "beancount",
  bib: "bibtex",
  bicep: "bicep",
  "blade.php": "blade",
  bsl: "bsl",
  c: "c",
  h: "objective-cpp",
  cs: "csharp",
  cpp: "cpp",
  hpp: "cpp",
  cc: "cpp",
  cxx: "cpp",
  hh: "cpp",
  cdc: "cdc",
  cairo: "cairo",
  clar: "clarity",
  clj: "clojure",
  cljs: "clojure",
  cljc: "clojure",
  soy: "soy",
  cmake: "cmake",
  "CMakeLists.txt": "cmake",
  cob: "cobol",
  cbl: "cobol",
  cobol: "cobol",
  CODEOWNERS: "codeowners",
  ql: "ql",
  coffee: "coffeescript",
  lisp: "lisp",
  cl: "lisp",
  lsp: "lisp",
  log: "log",
  v: "verilog",
  cql: "cql",
  cr: "crystal",
  css: "css",
  csv: "csv",
  cue: "cue",
  cypher: "cypher",
  cyp: "cypher",
  d: "d",
  dart: "dart",
  dax: "dax",
  desktop: "desktop",
  diff: "diff",
  patch: "diff",
  Dockerfile: "dockerfile",
  dockerfile: "dockerfile",
  env: "dotenv",
  dm: "dream-maker",
  edge: "edge",
  el: "emacs-lisp",
  ex: "elixir",
  exs: "elixir",
  elm: "elm",
  erb: "erb",
  erl: "erlang",
  hrl: "erlang",
  f: "fortran-fixed-form",
  for: "fortran-fixed-form",
  fs: "fsharp",
  fsi: "fsharp",
  fsx: "fsharp",
  f03: "f03",
  f08: "f08",
  f18: "f18",
  f77: "f77",
  f90: "fortran-free-form",
  f95: "fortran-free-form",
  fnl: "fennel",
  fish: "fish",
  ftl: "ftl",
  tres: "gdresource",
  res: "gdresource",
  gd: "gdscript",
  gdshader: "gdshader",
  gs: "genie",
  feature: "gherkin",
  COMMIT_EDITMSG: "git-commit",
  "git-rebase-todo": "git-rebase",
  gjs: "glimmer-js",
  gleam: "gleam",
  gts: "glimmer-ts",
  glsl: "glsl",
  vert: "glsl",
  frag: "glsl",
  shader: "shaderlab",
  gp: "gnuplot",
  plt: "gnuplot",
  gnuplot: "gnuplot",
  go: "go",
  graphql: "graphql",
  gql: "graphql",
  groovy: "groovy",
  gvy: "groovy",
  hack: "hack",
  haml: "haml",
  hbs: "handlebars",
  handlebars: "handlebars",
  hs: "haskell",
  lhs: "haskell",
  hx: "haxe",
  hcl: "hcl",
  hjson: "hjson",
  hlsl: "hlsl",
  fx: "hlsl",
  html: "html",
  htm: "html",
  http: "http",
  rest: "http",
  hxml: "hxml",
  hy: "hy",
  imba: "imba",
  ini: "ini",
  cfg: "ini",
  jade: "pug",
  pug: "pug",
  java: "java",
  js: "javascript",
  mjs: "javascript",
  cjs: "javascript",
  jinja: "jinja",
  jinja2: "jinja",
  j2: "jinja",
  jison: "jison",
  jl: "julia",
  json: "json",
  json5: "json5",
  jsonc: "jsonc",
  jsonl: "jsonl",
  jsonnet: "jsonnet",
  libsonnet: "jsonnet",
  jssm: "jssm",
  jsx: "jsx",
  kt: "kotlin",
  kts: "kts",
  kql: "kusto",
  tex: "tex",
  ltx: "tex",
  lean: "lean4",
  less: "less",
  liquid: "liquid",
  lit: "lit",
  ll: "llvm",
  logo: "logo",
  lua: "lua",
  luau: "luau",
  Makefile: "makefile",
  mk: "makefile",
  makefile: "makefile",
  md: "markdown",
  markdown: "markdown",
  marko: "marko",
  m: "wolfram",
  mat: "matlab",
  mdc: "mdc",
  mdx: "mdx",
  wiki: "wikitext",
  mediawiki: "wikitext",
  mmd: "mermaid",
  mermaid: "mermaid",
  mips: "mipsasm",
  mojo: "mojo",
  "🔥": "mojo",
  move: "move",
  nar: "narrat",
  nf: "nextflow",
  nim: "nim",
  nims: "nim",
  nimble: "nim",
  nix: "nix",
  nu: "nushell",
  mm: "objective-cpp",
  ml: "ocaml",
  mli: "ocaml",
  mll: "ocaml",
  mly: "ocaml",
  pas: "pascal",
  p: "pascal",
  pl: "prolog",
  pm: "perl",
  t: "perl",
  raku: "raku",
  p6: "raku",
  pl6: "raku",
  php: "php",
  phtml: "php",
  pls: "plsql",
  sql: "sql",
  po: "po",
  polar: "polar",
  pcss: "postcss",
  pot: "pot",
  potx: "potx",
  pq: "powerquery",
  pqm: "powerquery",
  ps1: "powershell",
  psm1: "powershell",
  psd1: "powershell",
  prisma: "prisma",
  pro: "prolog",
  P: "prolog",
  properties: "properties",
  proto: "protobuf",
  pp: "puppet",
  purs: "purescript",
  py: "python",
  pyw: "python",
  pyi: "python",
  qml: "qml",
  qmldir: "qmldir",
  qss: "qss",
  r: "r",
  R: "r",
  rkt: "racket",
  rktl: "racket",
  razor: "razor",
  cshtml: "razor",
  rb: "ruby",
  rbw: "ruby",
  reg: "reg",
  regex: "regexp",
  rel: "rel",
  rs: "rust",
  rst: "rst",
  rake: "ruby",
  gemspec: "ruby",
  jbuilder: "ruby",
  builder: "ruby",
  rabl: "ruby",
  arb: "ruby",
  ru: "ruby",
  podspec: "ruby",
  Gemfile: "ruby",
  Rakefile: "ruby",
  Guardfile: "ruby",
  Capfile: "ruby",
  Berksfile: "ruby",
  Brewfile: "ruby",
  Vagrantfile: "ruby",
  Thorfile: "ruby",
  Appraisals: "ruby",
  Dangerfile: "ruby",
  sas: "sas",
  sass: "sass",
  scala: "scala",
  sc: "scala",
  scm: "scheme",
  ss: "scheme",
  sld: "scheme",
  scss: "scss",
  sdbl: "sdbl",
  shadergraph: "shader",
  st: "smalltalk",
  sol: "solidity",
  sparql: "sparql",
  rq: "sparql",
  spl: "splunk",
  config: "ssh-config",
  do: "stata",
  ado: "stata",
  dta: "stata",
  styl: "stylus",
  stylus: "stylus",
  svelte: "svelte",
  swift: "swift",
  sv: "system-verilog",
  svh: "system-verilog",
  service: "systemd",
  socket: "systemd",
  device: "systemd",
  timer: "systemd",
  talon: "talonscript",
  tasl: "tasl",
  tcl: "tcl",
  templ: "templ",
  tf: "tf",
  tfvars: "tfvars",
  toml: "toml",
  ts: "typescript",
  mts: "typescript",
  cts: "typescript",
  tsp: "typespec",
  tsv: "tsv",
  tsx: "tsx",
  ttl: "turtle",
  twig: "twig",
  typ: "typst",
  vv: "v",
  vala: "vala",
  vapi: "vala",
  vb: "vb",
  vbs: "vb",
  bas: "vb",
  vh: "verilog",
  vhd: "vhdl",
  vhdl: "vhdl",
  vim: "vimscript",
  vue: "vue",
  "vine.ts": "vue-vine",
  vy: "vyper",
  wasm: "wasm",
  wat: "wasm",
  wy: "文言",
  wgsl: "wgsl",
  wit: "wit",
  wl: "wolfram",
  nb: "wolfram",
  xml: "xml",
  xsl: "xsl",
  xslt: "xsl",
  yaml: "yaml",
  yml: "yml",
  zs: "zenscript",
  zig: "zig",
  zsh: "zsh",
  sty: "tex"
};
function getFiletypeFromFileName(fileName) {
  var _a9;
  if (CUSTOM_EXTENSION_TO_FILE_FORMAT.has(fileName)) return CUSTOM_EXTENSION_TO_FILE_FORMAT.get(fileName) ?? "text";
  if (EXTENSION_TO_FILE_FORMAT[fileName] != null) return EXTENSION_TO_FILE_FORMAT[fileName];
  const compoundMatch = fileName.match(/\.([^/\\]+\.[^/\\]+)$/);
  if (compoundMatch != null) {
    if (CUSTOM_EXTENSION_TO_FILE_FORMAT.has(compoundMatch[1])) return CUSTOM_EXTENSION_TO_FILE_FORMAT.get(compoundMatch[1]) ?? "text";
    if (EXTENSION_TO_FILE_FORMAT[compoundMatch[1]] != null) return EXTENSION_TO_FILE_FORMAT[compoundMatch[1]] ?? "text";
  }
  const simpleMatch = ((_a9 = fileName.match(/\.([^.]+)$/)) == null ? void 0 : _a9[1]) ?? "";
  if (CUSTOM_EXTENSION_TO_FILE_FORMAT.has(simpleMatch)) return CUSTOM_EXTENSION_TO_FILE_FORMAT.get(simpleMatch) ?? "text";
  return EXTENSION_TO_FILE_FORMAT[simpleMatch] ?? "text";
}
function replaceCustomExtensions(version, map) {
  if (version <= customExtensionsVersion) return false;
  CUSTOM_EXTENSION_TO_FILE_FORMAT.clear();
  for (const key2 in map) {
    const lang = map[key2];
    if (lang != null) CUSTOM_EXTENSION_TO_FILE_FORMAT.set(key2, lang);
  }
  customExtensionsVersion = version;
  return true;
}
function getCustomExtensionsVersion() {
  return customExtensionsVersion;
}
function setCustomExtension(key2, lang) {
  const existing = CUSTOM_EXTENSION_TO_FILE_FORMAT.get(key2);
  if (existing === lang) return false;
  if (existing != null) console.warn(`setCustomExtension: overriding custom mapping for "${key2}" from "${existing}" to "${lang}"`);
  CUSTOM_EXTENSION_TO_FILE_FORMAT.set(key2, lang);
  customExtensionsVersion++;
  return true;
}
function getCustomExtensionsMap() {
  return Object.fromEntries(CUSTOM_EXTENSION_TO_FILE_FORMAT);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getHighlighterOptions.js
function getHighlighterOptions(lang, { theme, preferredHighlighter = "shiki-js" }) {
  return {
    langs: [lang ?? "text"],
    themes: getThemes(theme),
    preferredHighlighter
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getLineAnnotationName.js
function getLineAnnotationName(annotation) {
  return `annotation-${"side" in annotation ? `${annotation.side}-` : ""}${annotation.lineNumber}`;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/processLine.js
function processLine(node, line, state) {
  const lineInfo = typeof state.lineInfo === "function" ? state.lineInfo(line) : state.lineInfo[line - 1];
  if (lineInfo == null) {
    const errorMessage = `processLine: line ${line}, contains no state.lineInfo`;
    console.error(errorMessage, {
      node,
      line,
      state
    });
    throw new Error(errorMessage);
  }
  node.tagName = "div";
  node.properties["data-line"] = lineInfo.lineNumber;
  node.properties["data-alt-line"] = lineInfo.altLineNumber;
  node.properties["data-line-type"] = lineInfo.type;
  node.properties["data-line-index"] = lineInfo.lineIndex;
  if (node.children.length === 0) node.children.push(createTextNodeElement("\n"));
  return node;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/wrapTokenFragments.js
var NO_TOKEN = Symbol("no-token");
var MULTIPLE_TOKENS = Symbol("multiple-tokens");
function wrapTokenFragments(container) {
  const ownTokenChar = getTokenChar(container);
  if (ownTokenChar != null) return ownTokenChar;
  let containerTokenState = NO_TOKEN;
  const wrappedChildren = [];
  let currentTokenChildren = [];
  let currentTokenChar;
  const flushTokenChildren = () => {
    if (currentTokenChildren.length === 0 || currentTokenChar == null) {
      currentTokenChildren = [];
      currentTokenChar = void 0;
      return;
    }
    if (currentTokenChildren.length === 1) {
      const child = currentTokenChildren[0];
      if ((child == null ? void 0 : child.type) === "element") {
        setTokenChar(child, currentTokenChar);
        for (const grandChild of child.children) stripTokenChar(grandChild);
      } else stripTokenChar(child);
      wrappedChildren.push(child);
      currentTokenChildren = [];
      currentTokenChar = void 0;
      return;
    }
    for (const child of currentTokenChildren) stripTokenChar(child);
    wrappedChildren.push(createHastElement({
      tagName: "span",
      properties: { "data-char": currentTokenChar },
      children: currentTokenChildren
    }));
    currentTokenChildren = [];
    currentTokenChar = void 0;
  };
  const mergeContainerTokenState = (childTokenState) => {
    if (childTokenState === NO_TOKEN) return;
    if (childTokenState === MULTIPLE_TOKENS) {
      containerTokenState = MULTIPLE_TOKENS;
      return;
    }
    if (containerTokenState === NO_TOKEN) {
      containerTokenState = childTokenState;
      return;
    }
    if (containerTokenState !== childTokenState) containerTokenState = MULTIPLE_TOKENS;
  };
  for (const child of container.children) {
    const childTokenState = child.type === "element" ? wrapTokenFragments(child) : NO_TOKEN;
    mergeContainerTokenState(childTokenState);
    if (typeof childTokenState !== "number") {
      flushTokenChildren();
      wrappedChildren.push(child);
      continue;
    }
    if (currentTokenChar != null && currentTokenChar !== childTokenState) flushTokenChildren();
    currentTokenChar ?? (currentTokenChar = childTokenState);
    currentTokenChildren.push(child);
  }
  flushTokenChildren();
  container.children = wrappedChildren;
  return containerTokenState;
}
function getTokenChar(node) {
  const value = node.properties["data-char"];
  if (typeof value === "number") return value;
}
function stripTokenChar(node) {
  if (node.type !== "element") return;
  node.properties["data-char"] = void 0;
  for (const child of node.children) stripTokenChar(child);
}
function setTokenChar(node, char) {
  node.properties["data-char"] = char;
}

// ../../node_modules/.pnpm/@shikijs+transformers@4.4.3/node_modules/@shikijs/transformers/dist/index.mjs
var symbol = Symbol("highlighted-lines");
function transformerStyleToClass(options = {}) {
  const { classPrefix = "__shiki_", classSuffix = "", classReplacer = (className) => className } = options;
  const classToStyle = /* @__PURE__ */ new Map();
  function stringifyStyle(style) {
    return Object.entries(style).map(([key2, value]) => `${key2}:${value}`).join(";");
  }
  function registerStyle(style) {
    const str = typeof style === "string" ? style : stringifyStyle(style);
    let className = classPrefix + cyrb53(str) + classSuffix;
    className = classReplacer(className);
    if (!classToStyle.has(className)) classToStyle.set(className, typeof style === "string" ? style : { ...style });
    return className;
  }
  return {
    name: "@shikijs/transformers:style-to-class",
    pre(t) {
      if (!t.properties.style) return;
      const className = registerStyle(t.properties.style);
      delete t.properties.style;
      this.addClassToHast(t, className);
    },
    tokens(lines) {
      for (const line of lines) for (const token2 of line) {
        if (!token2.htmlStyle) continue;
        const className = registerStyle(token2.htmlStyle);
        token2.htmlStyle = {};
        token2.htmlAttrs || (token2.htmlAttrs = {});
        if (!token2.htmlAttrs.class) token2.htmlAttrs.class = className;
        else token2.htmlAttrs.class += ` ${className}`;
      }
    },
    getClassRegistry() {
      return classToStyle;
    },
    getCSS() {
      let css = "";
      for (const [className, style] of classToStyle.entries()) css += `.${className}{${typeof style === "string" ? style : stringifyStyle(style)}}`;
      return css;
    },
    clearRegistry() {
      classToStyle.clear();
    }
  };
}
function cyrb53(str, seed = 0) {
  let h1 = 3735928559 ^ seed;
  let h2 = 1103547991 ^ seed;
  for (let i2 = 0, ch; i2 < str.length; i2++) {
    ch = str.charCodeAt(i2);
    h1 = Math.imul(h1 ^ ch, 2654435761);
    h2 = Math.imul(h2 ^ ch, 1597334677);
  }
  h1 = Math.imul(h1 ^ h1 >>> 16, 2246822507);
  h1 ^= Math.imul(h2 ^ h2 >>> 13, 3266489909);
  h2 = Math.imul(h2 ^ h2 >>> 16, 2246822507);
  h2 ^= Math.imul(h1 ^ h1 >>> 13, 3266489909);
  return (4294967296 * (2097151 & h2) + (h1 >>> 0)).toString(36).slice(0, 6);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createTransformerWithState.js
function createTransformerWithState(useTokenTransformer = false, useCSSClasses = false) {
  const state = { lineInfo: [] };
  const transformers = [{
    line(node) {
      delete node.properties.class;
      return node;
    },
    pre(pre) {
      const code = findCodeElement(pre);
      const children = [];
      if (code != null) {
        let index = 1;
        for (const node of code.children) {
          if (node.type !== "element") continue;
          if (useTokenTransformer) wrapTokenFragments(node);
          children.push(processLine(node, index, state));
          index++;
        }
        code.children = children;
      }
      return pre;
    },
    ...useTokenTransformer ? {
      tokens(lines) {
        for (const line of lines) {
          let col = 0;
          for (const token2 of line) {
            const tokenWithOriginalRange = token2;
            tokenWithOriginalRange.__lineChar ?? (tokenWithOriginalRange.__lineChar = col);
            col += token2.content.length;
          }
        }
      },
      preprocess(_code, options) {
        options.mergeWhitespaces = "never";
      },
      span(hast, _line, _char, _lineElement, token2) {
        if ((token2 == null ? void 0 : token2.offset) != null && token2.content != null) {
          const tokenChar = token2.__lineChar;
          if (tokenChar != null) hast.properties["data-char"] = tokenChar;
          return hast;
        }
        return hast;
      }
    } : null
  }];
  if (useCSSClasses) transformers.push(tokenStyleNormalizer, toClass);
  if (useTokenTransformer) transformers.push({ line: (node) => {
    if (node.type === "element" && node.children.length === 0) node.children.push({
      type: "element",
      tagName: "br",
      properties: {},
      children: []
    });
    return node;
  } });
  return {
    state,
    transformers,
    toClass
  };
}
var toClass = transformerStyleToClass({ classPrefix: "hl-" });
var tokenStyleNormalizer = {
  name: "token-style-normalizer",
  tokens(lines) {
    for (const line of lines) for (const token2 of line) {
      if (token2.htmlStyle != null) continue;
      const style = {};
      if (token2.color != null) style.color = token2.color;
      if (token2.bgColor != null) style["background-color"] = token2.bgColor;
      if (token2.fontStyle != null && token2.fontStyle !== 0) {
        if ((token2.fontStyle & 1) !== 0) style["font-style"] = "italic";
        if ((token2.fontStyle & 2) !== 0) style["font-weight"] = "bold";
        if ((token2.fontStyle & 4) !== 0) style["text-decoration"] = "underline";
      }
      if (Object.keys(style).length > 0) token2.htmlStyle = style;
    }
  }
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/formatCSSVariablePrefix.js
function formatCSSVariablePrefix(type) {
  return `--${type === "token" ? "diffs-token" : "diffs"}-`;
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/color.js
var HEX_TRANSPARENT_RE = /^#(?:[0-9a-f]{3}0|[0-9a-f]{6}00)$/i;
var ALPHA_ZERO_RE = /^0(?:\.0+)?%?$/;
function getFunctionalAlpha(color) {
  var _a9;
  const openParen = color.indexOf("(");
  if (openParen <= 0 || !color.endsWith(")")) return;
  const fn = color.slice(0, openParen).trim();
  if (!/^(?:rgb|rgba|hsl|hsla|hwb|lab|lch|oklab|oklch|color)$/i.test(fn)) return;
  const inner = color.slice(openParen + 1, -1).trim();
  if (inner.length === 0) return;
  const slashIndex = inner.lastIndexOf("/");
  if (slashIndex !== -1) return inner.slice(slashIndex + 1).trim();
  if (/^(?:rgba|hsla)$/i.test(fn)) {
    const parts = inner.split(",");
    if (parts.length === 4) return (_a9 = parts[3]) == null ? void 0 : _a9.trim();
  }
}
function parseHexRgba(color) {
  const match = /^#([0-9a-f]{3}|[0-9a-f]{6}|[0-9a-f]{8})\b/i.exec(color.trim());
  if (match == null) return null;
  const hex = match[1];
  let expanded;
  let alpha = 1;
  if (hex.length === 3) expanded = hex.split("").map((c) => c + c).join("");
  else if (hex.length === 6) expanded = hex;
  else {
    expanded = hex.slice(0, 6);
    alpha = parseInt(hex.slice(6, 8), 16) / 255;
  }
  return [
    parseInt(expanded.slice(0, 2), 16),
    parseInt(expanded.slice(2, 4), 16),
    parseInt(expanded.slice(4, 6), 16),
    alpha
  ];
}
function relativeLuminance(color) {
  if (color == null) return null;
  const rgba = parseHexRgba(color);
  if (rgba == null) return null;
  const r4 = rgba[0] / 255;
  const g = rgba[1] / 255;
  const b3 = rgba[2] / 255;
  const channel = (v2) => v2 <= 0.03928 ? v2 / 12.92 : ((v2 + 0.055) / 1.055) ** 2.4;
  return 0.2126 * channel(r4) + 0.7152 * channel(g) + 0.0722 * channel(b3);
}
function isFullyTransparent(color) {
  if (color == null) return false;
  const normalized = color.trim().toLowerCase();
  if (normalized === "transparent") return true;
  if (HEX_TRANSPARENT_RE.test(normalized)) return true;
  const alpha = getFunctionalAlpha(normalized);
  return alpha != null && ALPHA_ZERO_RE.test(alpha);
}
function hoverWouldEraseText(hover, bg, fg) {
  if (bg == null || fg == null) return false;
  const hoverL = relativeLuminance(hover);
  const bgL = relativeLuminance(bg);
  const fgL = relativeLuminance(fg);
  if (hoverL == null || bgL == null || fgL == null) return false;
  return Math.abs(hoverL - fgL) < Math.abs(hoverL - bgL);
}

// ../../node_modules/.pnpm/@pierre+theming@1.0.1_@pierre+theme@2.0.0_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5_shiki@4.4.3/node_modules/@pierre/theming/dist/modules/normalizeThemeColors.js
var cache = /* @__PURE__ */ new WeakMap();
function normalizeThemeColors(theme) {
  const cached = cache.get(theme);
  if (cached != null) return cached;
  const originalColors = theme.colors ?? {};
  const colors = { ...originalColors };
  const editorBackground = originalColors["editor.background"] ?? theme.bg;
  const editorForeground = originalColors["editor.foreground"] ?? theme.fg;
  const sidebarBackground = originalColors["sideBar.background"] ?? editorBackground;
  const sidebarForeground = originalColors["sideBar.foreground"] ?? editorForeground;
  fill(colors, "editor.background", editorBackground);
  fill(colors, "editor.foreground", editorForeground);
  fill(colors, "sideBar.background", sidebarBackground);
  fill(colors, "sideBar.foreground", sidebarForeground);
  fill(colors, "input.background", originalColors["input.background"] ?? sidebarBackground);
  fill(colors, "sideBarSectionHeader.foreground", originalColors["sideBarSectionHeader.foreground"] ?? sidebarForeground);
  fill(colors, "list.activeSelectionForeground", originalColors["list.activeSelectionForeground"] ?? sidebarForeground);
  fill(colors, "gitDecoration.addedResourceForeground", firstColor(originalColors["gitDecoration.addedResourceForeground"], originalColors["terminal.ansiGreen"], originalColors["editorGutter.addedBackground"]));
  fill(colors, "gitDecoration.modifiedResourceForeground", firstColor(originalColors["gitDecoration.modifiedResourceForeground"], originalColors["terminal.ansiBlue"], originalColors["editorGutter.modifiedBackground"]));
  fill(colors, "gitDecoration.deletedResourceForeground", firstColor(originalColors["gitDecoration.deletedResourceForeground"], originalColors["terminal.ansiRed"], originalColors["editorGutter.deletedBackground"]));
  const focusRing = (isFullyTransparent(originalColors["list.focusOutline"]) ? void 0 : originalColors["list.focusOutline"]) ?? (isFullyTransparent(originalColors["focusBorder"]) ? void 0 : originalColors["focusBorder"]);
  if (focusRing != null) colors["list.focusOutline"] = focusRing;
  else delete colors["list.focusOutline"];
  const hover = originalColors["list.hoverBackground"];
  if (hover != null && (matchesSurface(hover, sidebarBackground) || hoverWouldEraseText(hover, sidebarBackground, sidebarForeground))) delete colors["list.hoverBackground"];
  const result = Object.freeze({
    ...theme,
    colors: Object.freeze(colors)
  });
  cache.set(theme, result);
  return result;
}
function fill(colors, key2, value) {
  if (value != null && value !== "") colors[key2] = value;
}
function firstColor(...candidates) {
  for (const candidate of candidates) if (candidate != null && candidate !== "") return candidate;
}
function matchesSurface(color, surface) {
  return surface != null && color.toLowerCase() === surface.toLowerCase();
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getHighlighterThemeStyles.js
function getHighlighterThemeStyles({ theme = DEFAULT_THEMES, highlighter: highlighter2, prefix }) {
  let styles = "";
  if (typeof theme === "string") {
    const themeData = highlighter2.getTheme(theme);
    const normalized = normalizeThemeColors(themeData);
    styles += `color:${normalized.fg};`;
    styles += `background-color:${normalized.bg};`;
    styles += `${formatCSSVariablePrefix("global")}fg:${normalized.fg};`;
    styles += `${formatCSSVariablePrefix("global")}bg:${normalized.bg};`;
    styles += getGitVariables(themeData, prefix);
  } else {
    let themeData = highlighter2.getTheme(theme.dark);
    let normalized = normalizeThemeColors(themeData);
    styles += `${formatCSSVariablePrefix("global")}dark:${normalized.fg};`;
    styles += `${formatCSSVariablePrefix("global")}dark-bg:${normalized.bg};`;
    styles += getGitVariables(themeData, "dark");
    themeData = highlighter2.getTheme(theme.light);
    normalized = normalizeThemeColors(themeData);
    styles += `${formatCSSVariablePrefix("global")}light:${normalized.fg};`;
    styles += `${formatCSSVariablePrefix("global")}light-bg:${normalized.bg};`;
    styles += getGitVariables(themeData, "light");
  }
  return styles;
}
function getGitVariables(themeData, modePrefix) {
  var _a9, _b, _c, _d, _e, _f;
  modePrefix = modePrefix != null ? `${modePrefix}-` : "";
  let styles = "";
  const additionGreen = ((_a9 = themeData.colors) == null ? void 0 : _a9["gitDecoration.addedResourceForeground"]) ?? ((_b = themeData.colors) == null ? void 0 : _b["terminal.ansiGreen"]);
  if (additionGreen != null) styles += `${formatCSSVariablePrefix("global")}${modePrefix}addition-color:${additionGreen};`;
  const deletionRed = ((_c = themeData.colors) == null ? void 0 : _c["gitDecoration.deletedResourceForeground"]) ?? ((_d = themeData.colors) == null ? void 0 : _d["terminal.ansiRed"]);
  if (deletionRed != null) styles += `${formatCSSVariablePrefix("global")}${modePrefix}deletion-color:${deletionRed};`;
  const modifiedBlue = ((_e = themeData.colors) == null ? void 0 : _e["gitDecoration.modifiedResourceForeground"]) ?? ((_f = themeData.colors) == null ? void 0 : _f["terminal.ansiBlue"]);
  if (modifiedBlue != null) styles += `${formatCSSVariablePrefix("global")}${modePrefix}modified-color:${modifiedBlue};`;
  return styles;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getLineNodes.js
function getLineNodes(nodes) {
  let firstChild = nodes.children[0];
  while (firstChild != null) {
    if (firstChild.type === "element" && firstChild.tagName === "code") return firstChild.children;
    if ("children" in firstChild) firstChild = firstChild.children[0];
    else firstChild = null;
  }
  console.error(nodes);
  throw new Error("getLineNodes: Unable to find children");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/computeFileOffsets.js
var LINE_FEED = 10;
var CARRIAGE_RETURN = 13;
var DENSE_LINE_BREAK_BLOCK_SIZE = 4;
var DENSE_LINE_BREAK_BLOCK_SPAN = 40;
function computeLineOffsets(contents) {
  const offsets = [0];
  let carriageReturn = contents.indexOf("\r");
  let lineFeed = contents.indexOf("\n");
  let blockStart = 0;
  let blockBreaks = 0;
  while (carriageReturn !== -1 || lineFeed !== -1) {
    let nextOffset;
    if (carriageReturn !== -1 && (lineFeed === -1 || carriageReturn < lineFeed)) if (lineFeed === carriageReturn + 1) {
      nextOffset = lineFeed + 1;
      carriageReturn = contents.indexOf("\r", nextOffset);
      lineFeed = contents.indexOf("\n", nextOffset);
    } else {
      nextOffset = carriageReturn + 1;
      carriageReturn = contents.indexOf("\r", nextOffset);
    }
    else {
      nextOffset = lineFeed + 1;
      lineFeed = contents.indexOf("\n", nextOffset);
    }
    offsets.push(nextOffset);
    if (++blockBreaks === DENSE_LINE_BREAK_BLOCK_SIZE) {
      if (nextOffset - blockStart <= DENSE_LINE_BREAK_BLOCK_SPAN) {
        for (let i2 = nextOffset; i2 < contents.length; i2++) {
          const char = contents.charCodeAt(i2);
          if (char === LINE_FEED || char === CARRIAGE_RETURN) {
            if (char === CARRIAGE_RETURN && i2 + 1 < contents.length && contents.charCodeAt(i2 + 1) === LINE_FEED) i2++;
            offsets.push(i2 + 1);
          }
        }
        return offsets;
      }
      blockStart = nextOffset;
      blockBreaks = 0;
    }
  }
  return offsets;
}
function linesFromFileContents(contents) {
  const offsets = computeLineOffsets(contents);
  return Array.from({ length: offsets.length }, (_3, i2) => {
    const start = offsets[i2];
    const end = offsets[i2 + 1] ?? contents.length;
    return contents.slice(start, end);
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/renderFileWithHighlighter.js
var DEFAULT_PLAIN_TEXT_OPTIONS = { forcePlainText: false };
function renderFileWithHighlighter(file, highlighter2, { theme = DEFAULT_THEMES, tokenizeMaxLineLength, useTokenTransformer }, { forcePlainText, startingLine, totalLines, lines } = DEFAULT_PLAIN_TEXT_OPTIONS) {
  if (forcePlainText) {
    startingLine ?? (startingLine = 0);
    totalLines ?? (totalLines = Infinity);
  } else {
    startingLine = 0;
    totalLines = Infinity;
  }
  const isWindowedHighlight = startingLine > 0 || totalLines < Infinity;
  const { state, transformers } = createTransformerWithState(useTokenTransformer);
  const lang = forcePlainText ? "text" : file.lang ?? getFiletypeFromFileName(file.name);
  const baseThemeType = typeof theme === "string" ? highlighter2.getTheme(theme).type : void 0;
  const themeStyles = getHighlighterThemeStyles({
    theme,
    highlighter: highlighter2
  });
  state.lineInfo = (shikiLineNumber) => ({
    type: "context",
    lineIndex: shikiLineNumber - 1 + startingLine,
    lineNumber: shikiLineNumber + startingLine
  });
  const hastConfig = (() => {
    if (typeof theme === "string") return {
      lang,
      theme,
      transformers,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeMaxLineLength,
      tokenizeTimeLimit: 0
    };
    return {
      lang,
      themes: theme,
      transformers,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeMaxLineLength,
      tokenizeTimeLimit: 0
    };
  })();
  const highlightedLines = getLineNodes(highlighter2.codeToHast(isWindowedHighlight ? extractWindowedFileContent(lines ?? linesFromFileContents(file.contents), startingLine, totalLines) : file.contents, hastConfig));
  const code = isWindowedHighlight ? new Array(startingLine) : highlightedLines;
  if (isWindowedHighlight) code.push(...highlightedLines);
  return {
    code,
    themeStyles,
    baseThemeType
  };
}
function extractWindowedFileContent(lines, startingLine, totalLines) {
  if (lines.length === 0) return "";
  const endLine = Math.min(startingLine + totalLines, lines.length);
  return lines.slice(startingLine, endLine).join("");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/includesFileAnnotations.js
var FILE_ANNOTATION_DOM_KEY = `-1,-1`;
function includesFileAnnotations(annotations) {
  return (annotations == null ? void 0 : annotations.some((annotation) => annotation.lineNumber === 0)) ?? false;
}
function getFileAnnotations(annotations) {
  const fileAnnotations = annotations[0];
  return fileAnnotations != null && fileAnnotations.length > 0 ? fileAnnotations : void 0;
}
function shouldRenderFileAnnotations(renderRange) {
  return renderRange.startingLine === 0 && renderRange.totalLines > 0;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/applyLineTextWithNewline.js
function applyLineTextWithNewline(line, lineText) {
  if (line.endsWith("\r\n")) return lineText + "\r\n";
  if (line.endsWith("\r")) return lineText + "\r";
  if (line.endsWith("\n")) return lineText + "\n";
  return lineText;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createContentColumn.js
function createContentColumn(children, rowCount) {
  return createHastElement({
    tagName: "div",
    children,
    properties: {
      "data-content": "",
      style: `grid-row: span ${rowCount}`
    }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isFilePlainText.js
function isFilePlainText(file) {
  return (file.lang ?? getFiletypeFromFileName(file.name)) === "text";
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/renderers/FileRenderer.js
function isLineCacheForFile(lineCache, file) {
  return file.cacheKey == null ? lineCache.file === file && lineCache.sourceContents === file.contents : lineCache.cacheKey === file.cacheKey;
}
var instanceId = -1;
var FileRenderer = class {
  constructor(options = { theme: DEFAULT_THEMES }, onRenderUpdate, workerManager) {
    __publicField(this, "options");
    __publicField(this, "onRenderUpdate");
    __publicField(this, "workerManager");
    __publicField(this, "__id", `file-renderer:${++instanceId}`);
    __publicField(this, "highlighter");
    __publicField(this, "renderCache");
    __publicField(this, "computedLang", "text");
    __publicField(this, "lineAnnotations", {});
    __publicField(this, "lineCache");
    __publicField(this, "pendingStructuralRows");
    __publicField(this, "textDocumentCache", /* @__PURE__ */ new WeakMap());
    __publicField(this, "editSessionActive", false);
    this.options = options;
    this.onRenderUpdate = onRenderUpdate;
    this.workerManager = workerManager;
    if ((workerManager == null ? void 0 : workerManager.isWorkingPool()) !== true) this.highlighter = areThemesAttached(options.theme ?? DEFAULT_THEMES) ? getHighlighterIfLoaded() : void 0;
  }
  setOptions(options) {
    this.options = options;
  }
  mergeOptions(options) {
    this.options = {
      ...this.options,
      ...options
    };
  }
  setLineAnnotations(lineAnnotations) {
    this.lineAnnotations = {};
    for (const annotation of lineAnnotations) {
      const arr = this.lineAnnotations[annotation.lineNumber] ?? [];
      this.lineAnnotations[annotation.lineNumber] = arr;
      arr.push(annotation);
    }
  }
  cleanUp() {
    this.recycle();
    this.workerManager = void 0;
    this.onRenderUpdate = void 0;
  }
  /**
  * Enter edit-session mode: rendering happens locally with the token
  * transformer forced on, and worker-pool requests/results are suspended
  * for this renderer. Called on every editor attach, including a re-attach
  * after recycle.
  */
  beginEditSession() {
    this.editSessionActive = true;
  }
  /** Leave edit-session mode. Rendering returns to the pool when one works. */
  endEditSession() {
    this.editSessionActive = false;
  }
  /**
  * Ensures that the DOM is compatible with editor render updates
  */
  editorRenderReady() {
    var _a9;
    return ((_a9 = this.renderCache) == null ? void 0 : _a9.options.useTokenTransformer) === true && this.renderCache.highlighted && this.renderCache.result != null;
  }
  recycle() {
    var _a9;
    this.clearRenderCache();
    this.highlighter = void 0;
    (_a9 = this.workerManager) == null ? void 0 : _a9.cleanUpTasks(this);
    this.lineCache = void 0;
    this.endEditSession();
    this.textDocumentCache = /* @__PURE__ */ new WeakMap();
  }
  syncEditedContentsToFile() {
    const { renderCache, lineCache } = this;
    if ((renderCache == null ? void 0 : renderCache.isDirty) !== true || lineCache == null || !isLineCacheForFile(lineCache, renderCache.file)) return;
    renderCache.file.contents = lineCache.lines.join("");
  }
  hasUnkeyedFileContentsChanged(file) {
    const { lineCache } = this;
    return file.cacheKey == null && lineCache != null && lineCache.file === file && lineCache.sourceContents !== file.contents;
  }
  invalidateChangedUnkeyedFile(file) {
    var _a9;
    if (!this.hasUnkeyedFileContentsChanged(file)) return;
    (_a9 = this.workerManager) == null ? void 0 : _a9.cleanUpTasks(this);
    this.clearRenderCache();
    this.lineCache = void 0;
    this.textDocumentCache = /* @__PURE__ */ new WeakMap();
  }
  clearRenderCache() {
    var _a9;
    this.syncEditedContentsToFile();
    this.pendingStructuralRows = void 0;
    const renderCache = this.renderCache;
    this.renderCache = void 0;
    if (renderCache != null && renderCache.isDirty === true && renderCache.file.cacheKey != null) (_a9 = this.workerManager) == null ? void 0 : _a9.evictFileFromCache(renderCache.file.cacheKey);
  }
  hydrate(file) {
    var _a9, _b;
    const { options } = this.getRenderOptions(file);
    const massiveFile = isFileMassive(this.getOrCreateLineCache(file).length, this.getTokenizeMaxLength());
    let cache2 = (_a9 = this.workerManager) == null ? void 0 : _a9.getFileResultCache(file);
    if (cache2 != null && !areFileRenderOptionsEqual(options, cache2.options)) cache2 = void 0;
    this.renderCache ?? (this.renderCache = {
      file,
      options,
      highlighted: !massiveFile && !isFilePlainText(file),
      result: massiveFile ? void 0 : cache2 == null ? void 0 : cache2.result,
      renderRange: void 0
    });
    if (!this.editSessionActive && ((_b = this.workerManager) == null ? void 0 : _b.isWorkingPool()) === true) {
      if (this.renderCache.result == null && !massiveFile) this.workerManager.highlightFileAST(this, file);
    } else if (this.highlighter == null) {
      this.computedLang = file.lang ?? getFiletypeFromFileName(file.name);
      this.initializeHighlighter();
    }
  }
  getLocalHighlightTheme() {
    var _a9;
    return ((_a9 = this.workerManager) == null ? void 0 : _a9.getFileRenderOptions().theme) ?? this.options.theme ?? DEFAULT_THEMES;
  }
  getEffectiveCodeOptions() {
    var _a9;
    const poolOptions = ((_a9 = this.workerManager) == null ? void 0 : _a9.isWorkingPool()) === true ? this.workerManager.getFileRenderOptions() : void 0;
    return {
      theme: this.getLocalHighlightTheme(),
      tokenizeMaxLineLength: (poolOptions == null ? void 0 : poolOptions.tokenizeMaxLineLength) ?? this.options.tokenizeMaxLineLength
    };
  }
  getRenderOptions(file) {
    const options = (() => {
      var _a9;
      if (((_a9 = this.workerManager) == null ? void 0 : _a9.isWorkingPool()) === true) {
        const poolOptions = this.workerManager.getFileRenderOptions();
        if (this.editSessionActive && poolOptions.useTokenTransformer !== true) return {
          ...poolOptions,
          useTokenTransformer: true
        };
        return poolOptions;
      }
      const { tokenizeMaxLineLength = 1e3 } = this.options;
      return {
        theme: this.getLocalHighlightTheme(),
        useTokenTransformer: this.editSessionActive || this.options.useTokenTransformer === true,
        tokenizeMaxLineLength
      };
    })();
    const { renderCache } = this;
    if ((renderCache == null ? void 0 : renderCache.result) == null) return {
      options,
      forceHighlight: true
    };
    if (!areFilesEqual(file, renderCache.file) || !areFileRenderOptionsEqual(options, renderCache.options)) return {
      options,
      forceHighlight: true
    };
    return {
      options,
      forceHighlight: false
    };
  }
  getOrCreateLineCache(file) {
    this.invalidateChangedUnkeyedFile(file);
    let { lineCache } = this;
    if (lineCache == null || !isLineCacheForFile(lineCache, file)) lineCache = {
      cacheKey: file.cacheKey,
      file,
      sourceContents: file.contents,
      lines: linesFromFileContents(file.contents)
    };
    this.lineCache = lineCache;
    return lineCache.lines;
  }
  getLineCount(file) {
    var _a9;
    const lines = this.getOrCreateLineCache(file);
    return ((_a9 = this.textDocumentCache.get(file)) == null ? void 0 : _a9.lineCount) ?? lines.length;
  }
  updateRenderCache(dirtyLines, themeType, lineCountChangeInFlight = false) {
    this.pendingStructuralRows = void 0;
    if (this.renderCache == null) return;
    const { file, result } = this.renderCache;
    if (result == null) return;
    const pendingStructuralRows = lineCountChangeInFlight ? /* @__PURE__ */ new Map() : void 0;
    this.pendingStructuralRows = pendingStructuralRows;
    const lineCache = this.lineCache != null && isLineCacheForFile(this.lineCache, file) ? this.lineCache : void 0;
    for (const [line, tokens] of dirtyLines) {
      if (pendingStructuralRows === void 0 && lineCache != null && line < lineCache.lines.length) {
        const lineText = tokens.map((token2) => token2[2]).join("");
        lineCache.lines[line] = applyLineTextWithNewline(lineCache.lines[line] ?? "", lineText);
      }
      const row = {
        type: "element",
        tagName: "div",
        properties: {
          "data-line": line + 1,
          "data-line-type": "context",
          "data-line-index": line
        },
        children: tokens.map(([char, fg, text2]) => {
          if (char === 0 && fg === "") {
            if (text2 === "") return {
              type: "element",
              tagName: "br",
              properties: {},
              children: []
            };
            return {
              type: "text",
              value: text2
            };
          }
          return {
            type: "element",
            tagName: "span",
            properties: {
              "data-char": char,
              style: `color:${fg};`
            },
            children: [{
              type: "text",
              value: text2
            }]
          };
        })
      };
      if (pendingStructuralRows !== void 0) pendingStructuralRows.set(line, row);
      else result.code[line] = row;
    }
    result.baseThemeType = themeType;
    this.renderCache.isDirty = true;
  }
  applyDocumentChange(textDocument) {
    var _a9;
    const pendingStructuralRows = this.pendingStructuralRows;
    this.pendingStructuralRows = void 0;
    if (this.renderCache == null) return;
    const { file, result } = this.renderCache;
    if (result == null) return;
    const previousLines = this.lineCache != null && isLineCacheForFile(this.lineCache, file) ? this.lineCache.lines : linesFromFileContents(file.contents);
    const nextLines = linesFromFileContents(textDocument.getText());
    if (previousLines.length !== nextLines.length) {
      const maxShared = Math.min(previousLines.length, nextLines.length);
      let prefix = 0;
      while (prefix < maxShared && previousLines[prefix] === nextLines[prefix]) prefix++;
      let suffix = 0;
      while (suffix < maxShared - prefix && previousLines[previousLines.length - 1 - suffix] === nextLines[nextLines.length - 1 - suffix]) suffix++;
      const previousCode = result.code;
      result.code = new Array(nextLines.length);
      for (let i2 = 0; i2 < prefix; i2++) result.code[i2] = previousCode[i2];
      for (let i2 = 0; i2 < suffix; i2++) result.code[nextLines.length - 1 - i2] = previousCode[previousLines.length - 1 - i2];
      if (pendingStructuralRows !== void 0) {
        for (const [line, row] of pendingStructuralRows) if (line < nextLines.length) result.code[line] = row;
      }
      for (let i2 = prefix; i2 < nextLines.length - suffix; i2++) (_a9 = result.code)[i2] ?? (_a9[i2] = {
        type: "element",
        tagName: "div",
        properties: {
          "data-line": i2 + 1,
          "data-line-type": "context",
          "data-line-index": i2
        },
        children: [{
          type: "element",
          tagName: "span",
          properties: { "data-char": 0 },
          children: [{
            type: "text",
            value: textDocument.getLineText(i2)
          }]
        }]
      });
      for (let i2 = 0; i2 < result.code.length; i2++) {
        const line = result.code[i2];
        if ((line == null ? void 0 : line.type) === "element") {
          line.properties["data-line"] = i2 + 1;
          line.properties["data-line-index"] = i2;
        }
      }
      this.renderCache.isDirty = true;
    }
    this.lineCache = {
      cacheKey: file.cacheKey,
      file,
      sourceContents: file.contents,
      lines: nextLines
    };
    this.textDocumentCache.set(file, textDocument);
  }
  renderFile(file = ((_a9) => (_a9 = this.renderCache) == null ? void 0 : _a9.file)(), renderRange = DEFAULT_RENDER_RANGE) {
    var _a10, _b, _c;
    if (file == null) return;
    this.invalidateChangedUnkeyedFile(file);
    if (((_a10 = this.renderCache) == null ? void 0 : _a10.isDirty) === true && !areFilesEqual(file, this.renderCache.file)) {
      this.clearRenderCache();
      this.lineCache = void 0;
      this.textDocumentCache = /* @__PURE__ */ new WeakMap();
    }
    let { options, forceHighlight } = this.getRenderOptions(file);
    if (((_b = this.renderCache) == null ? void 0 : _b.isDirty) === true && !areFileRenderOptionsEqual(options, this.renderCache.options)) this.clearRenderCache();
    const cache2 = this.getMatchingWorkerResultCache(file, options);
    if (cache2 != null && !this.hasHighlightedRenderCache(file, options)) {
      this.renderCache = {
        file,
        highlighted: true,
        renderRange: void 0,
        ...cache2
      };
      forceHighlight = false;
    }
    this.renderCache ?? (this.renderCache = {
      file,
      highlighted: false,
      options,
      result: void 0,
      renderRange: void 0
    });
    const lines = this.getOrCreateLineCache(file);
    const hasContent = file.contents.length > 0;
    const forcePlainText = !hasContent || isFilePlainText(file) || isFileMassive(lines.length, this.getTokenizeMaxLength());
    const newContent = !areFilesEqual(file, this.renderCache.file);
    const newRenderRange = !areRenderRangesEqual(this.renderCache.renderRange, renderRange);
    if (!this.editSessionActive && ((_c = this.workerManager) == null ? void 0 : _c.isWorkingPool()) === true) {
      if (forcePlainText || this.renderCache.result == null || !this.renderCache.highlighted && (newContent || newRenderRange)) {
        this.renderCache.file = file;
        this.renderCache.options = options;
        this.renderCache.highlighted = false;
        if (this.renderCache.result == null || newContent || newRenderRange || forceHighlight) this.renderCache.result = this.workerManager.getPlainFileAST(file, renderRange.startingLine, renderRange.totalLines, lines);
        this.renderCache.renderRange = renderRange;
      }
      if (!forcePlainText && hasContent && (!this.renderCache.highlighted || forceHighlight)) this.workerManager.highlightFileAST(this, file);
    } else {
      this.computedLang = file.lang ?? getFiletypeFromFileName(file.name);
      const hasThemes = this.highlighter != null && areThemesAttached(options.theme);
      const hasLangs = this.highlighter != null && areLanguagesAttached(this.computedLang);
      const canHighlight = !forcePlainText && hasLangs;
      if (this.highlighter != null && hasThemes && (forceHighlight || forcePlainText || !this.renderCache.highlighted && canHighlight || this.renderCache.result == null)) {
        const { result, options: options2 } = this.renderFileWithHighlighter(file, this.highlighter, forcePlainText || !hasLangs);
        this.renderCache = {
          file,
          options: options2,
          highlighted: canHighlight,
          result,
          renderRange: void 0
        };
      }
      if (!hasThemes || !forcePlainText && !hasLangs) this.asyncHighlight(file).then(({ result, options: options2 }) => {
        if (this.renderCache != null) this.renderCache.highlighted = false;
        this.applyHighlightResult(file, result, options2, !forcePlainText);
      });
    }
    return this.renderCache.result != null ? this.processFileResult(this.renderCache.file, renderRange, this.renderCache.result) : void 0;
  }
  async asyncRender(file, renderRange = DEFAULT_RENDER_RANGE) {
    const { result } = await this.asyncHighlight(file);
    return this.processFileResult(file, renderRange, result);
  }
  async asyncHighlight(file) {
    const forcePlainText = isFileMassive(this.getOrCreateLineCache(file).length, this.getTokenizeMaxLength());
    this.computedLang = forcePlainText ? "text" : file.lang ?? getFiletypeFromFileName(file.name);
    const hasThemes = this.highlighter != null && hasResolvedThemes(getThemes(this.getLocalHighlightTheme()));
    const hasLangs = forcePlainText || this.highlighter != null && areLanguagesAttached(this.computedLang);
    if (this.highlighter == null || !hasThemes || !hasLangs) this.highlighter = await this.initializeHighlighter();
    return this.renderFileWithHighlighter(file, this.highlighter, forcePlainText);
  }
  renderFileWithHighlighter(file, highlighter2, forcePlainText = false) {
    const { options } = this.getRenderOptions(file);
    return {
      result: renderFileWithHighlighter(file, highlighter2, options, { forcePlainText }),
      options
    };
  }
  processFileResult(file, renderRange, { code, themeStyles, baseThemeType }) {
    const totalLines = this.getLineCount(file);
    const { disableFileHeader = false } = this.options;
    const contentArray = [];
    const gutter = createGutterWrapper();
    const endLine = Math.min(renderRange.startingLine + renderRange.totalLines, totalLines);
    let rowCount = 0;
    const fileLevelAnnotations = shouldRenderFileAnnotations(renderRange) ? getFileAnnotations(this.lineAnnotations) : void 0;
    if (fileLevelAnnotations != null) {
      gutter.children.push(createGutterGap("context", "annotation", 1));
      contentArray.push(createAnnotationElement({
        type: "annotation",
        hunkIndex: -1,
        lineIndex: -1,
        annotations: fileLevelAnnotations.map((annotation) => getLineAnnotationName(annotation))
      }));
      rowCount++;
    }
    for (let lineIndex = renderRange.startingLine; lineIndex < endLine; lineIndex++) {
      const lineNumber = lineIndex + 1;
      const line = code[lineIndex];
      if (line == null) {
        const message = "FileRenderer.processFileResult: Line doesnt exist";
        console.error(message, {
          name: file.name,
          lineIndex,
          lineNumber
        });
        throw new Error(message);
      }
      gutter.children.push(createGutterItem("context", lineNumber, `${lineIndex}`));
      contentArray.push(line);
      rowCount++;
      const annotations = this.lineAnnotations[lineNumber];
      if (annotations != null) {
        gutter.children.push(createGutterGap("context", "annotation", 1));
        contentArray.push(createAnnotationElement({
          type: "annotation",
          hunkIndex: 0,
          lineIndex: lineNumber,
          annotations: annotations.map((annotation) => getLineAnnotationName(annotation))
        }));
        rowCount++;
      }
    }
    gutter.properties.style = `grid-row: span ${rowCount}`;
    return {
      gutterAST: gutter.children ?? [],
      contentAST: contentArray,
      preAST: this.createPreElement(totalLines),
      headerAST: !disableFileHeader ? this.renderHeader(file) : void 0,
      totalLines,
      rowCount,
      themeStyles,
      baseThemeType,
      bufferBefore: renderRange.bufferBefore,
      bufferAfter: renderRange.bufferAfter,
      css: ""
    };
  }
  renderHeader(file) {
    const { headerRenderMode = "default", stickyHeader = false } = this.options;
    return createFileHeaderElement({
      fileOrDiff: file,
      mode: headerRenderMode,
      stickyHeader
    });
  }
  renderFullHTML(result) {
    return toHtml(this.renderFullAST(result));
  }
  renderFullAST(result, children = []) {
    children.push(createHastElement({
      tagName: "code",
      children: this.renderCodeAST(result),
      properties: { "data-code": "" }
    }));
    return {
      ...result.preAST,
      children
    };
  }
  renderCodeAST(result) {
    const gutter = createGutterWrapper();
    gutter.children = result.gutterAST;
    gutter.properties.style = `grid-row: span ${result.rowCount}`;
    return [gutter, createContentColumn(result.contentAST, result.rowCount)];
  }
  renderPartialHTML(children, includeCodeNode = false) {
    if (!includeCodeNode) return toHtml(children);
    return toHtml(createHastElement({
      tagName: "code",
      children,
      properties: { "data-code": "" }
    }));
  }
  async initializeHighlighter() {
    var _a9;
    this.highlighter = await getSharedHighlighter(getHighlighterOptions(this.computedLang, {
      theme: this.getLocalHighlightTheme(),
      preferredHighlighter: ((_a9 = this.workerManager) == null ? void 0 : _a9.getPreferredHighlighter()) ?? this.options.preferredHighlighter
    }));
    return this.highlighter;
  }
  onHighlightSuccess(file, result, options, highlighted = true) {
    if (this.editSessionActive) return;
    this.applyHighlightResult(file, result, options, highlighted);
  }
  applyHighlightResult(file, result, options, highlighted = true) {
    var _a9;
    if (this.renderCache == null) return;
    const triggerRenderUpdate = !areFilesEqual(file, this.renderCache.file) || !this.renderCache.highlighted || !areFileRenderOptionsEqual(options, this.renderCache.options);
    this.renderCache = {
      file,
      options,
      highlighted,
      result,
      renderRange: void 0
    };
    if (triggerRenderUpdate) (_a9 = this.onRenderUpdate) == null ? void 0 : _a9.call(this);
  }
  getMatchingWorkerResultCache(file, options) {
    var _a9;
    if (this.editSessionActive) return;
    const cache2 = (_a9 = this.workerManager) == null ? void 0 : _a9.getFileResultCache(file);
    if (cache2 == null || !areFileRenderOptionsEqual(options, cache2.options)) return;
    return cache2;
  }
  hasHighlightedRenderCache(file, options) {
    const { renderCache } = this;
    return (renderCache == null ? void 0 : renderCache.result) != null && renderCache.highlighted && areFilesEqual(file, renderCache.file) && areFileRenderOptionsEqual(options, renderCache.options);
  }
  onHighlightError(error) {
    console.error(error);
  }
  getTokenizeMaxLength() {
    return this.options.tokenizeMaxLength ?? 1e5;
  }
  createPreElement(totalLines) {
    const { disableLineNumbers = false, overflow = "scroll" } = this.options;
    return createPreElement({
      type: "file",
      diffIndicators: "none",
      disableBackground: true,
      disableLineNumbers,
      overflow,
      split: false,
      totalLines
    });
  }
};
function isFileMassive(lineCount, tokenizeMaxLength) {
  return lineCount > tokenizeMaxLength;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/sprite.js
var SVGSpriteSheet = `<svg data-icon-sprite aria-hidden="true" width="0" height="0">
  <symbol id="diffs-icon-arrow-right-short" viewBox="0 0 16 16">
    <path d="M8.47 4.22a.75.75 0 0 0 0 1.06l1.97 1.97H3.75a.75.75 0 0 0 0 1.5h6.69l-1.97 1.97a.75.75 0 1 0 1.06 1.06l3.25-3.25a.75.75 0 0 0 0-1.06L9.53 4.22a.75.75 0 0 0-1.06 0"/>
  </symbol>
  <symbol id="diffs-icon-brand-github" viewBox="0 0 16 16">
    <path d="M8 0c4.42 0 8 3.58 8 8a8.01 8.01 0 0 1-5.45 7.59c-.4.08-.55-.17-.55-.38 0-.27.01-1.13.01-2.2 0-.75-.25-1.23-.54-1.48 1.78-.2 3.65-.88 3.65-3.95 0-.88-.31-1.59-.82-2.15.08-.2.36-1.02-.08-2.12 0 0-.67-.22-2.2.82-.64-.18-1.32-.27-2-.27s-1.36.09-2 .27c-1.53-1.03-2.2-.82-2.2-.82-.44 1.1-.16 1.92-.08 2.12-.51.56-.82 1.28-.82 2.15 0 3.06 1.86 3.75 3.64 3.95-.23.2-.44.55-.51 1.07-.46.21-1.61.55-2.33-.66-.15-.24-.6-.83-1.23-.82-.67.01-.27.38.01.53.34.19.73.9.82 1.13.16.45.68 1.31 2.69.94 0 .67.01 1.3.01 1.49 0 .21-.15.45-.55.38A7.995 7.995 0 0 1 0 8c0-4.42 3.58-8 8-8"/>
  </symbol>
  <symbol id="diffs-icon-chevron" viewBox="0 0 16 16">
    <path d="M1.47 4.47a.75.75 0 0 1 1.06 0L8 9.94l5.47-5.47a.75.75 0 1 1 1.06 1.06l-6 6a.75.75 0 0 1-1.06 0l-6-6a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-chevrons-narrow" viewBox="0 0 10 16">
    <path d="M4.47 2.22a.75.75 0 0 1 1.06 0l3.25 3.25a.75.75 0 0 1-1.06 1.06L5 3.81 2.28 6.53a.75.75 0 0 1-1.06-1.06zM1.22 9.47a.75.75 0 0 1 1.06 0L5 12.19l2.72-2.72a.75.75 0 0 1 1.06 1.06l-3.25 3.25a.75.75 0 0 1-1.06 0l-3.25-3.25a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-diff-split" viewBox="0 0 16 16">
    <path d="M14 0H8.5v16H14a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2m-1.5 6.5v1h1a.5.5 0 0 1 0 1h-1v1a.5.5 0 0 1-1 0v-1h-1a.5.5 0 0 1 0-1h1v-1a.5.5 0 0 1 1 0"/><path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h5.5V0zm.5 7.5h3a.5.5 0 0 1 0 1h-3a.5.5 0 0 1 0-1" opacity=".3"/>
  </symbol>
  <symbol id="diffs-icon-diff-unified" viewBox="0 0 16 16">
    <path fill-rule="evenodd" d="M16 14a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V8.5h16zm-8-4a.5.5 0 0 0-.5.5v1h-1a.5.5 0 0 0 0 1h1v1a.5.5 0 0 0 1 0v-1h1a.5.5 0 0 0 0-1h-1v-1A.5.5 0 0 0 8 10" clip-rule="evenodd"/><path fill-rule="evenodd" d="M14 0a2 2 0 0 1 2 2v5.5H0V2a2 2 0 0 1 2-2zM6.5 3.5a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1z" clip-rule="evenodd" opacity=".4"/>
  </symbol>
  <symbol id="diffs-icon-expand" viewBox="0 0 16 16">
    <path d="M3.47 5.47a.75.75 0 0 1 1.06 0L8 8.94l3.47-3.47a.75.75 0 1 1 1.06 1.06l-4 4a.75.75 0 0 1-1.06 0l-4-4a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-expand-all" viewBox="0 0 16 16">
    <path d="M11.47 9.47a.75.75 0 1 1 1.06 1.06l-4 4a.75.75 0 0 1-1.06 0l-4-4a.75.75 0 1 1 1.06-1.06L8 12.94zM7.526 1.418a.75.75 0 0 1 1.004.052l4 4a.75.75 0 1 1-1.06 1.06L8 3.06 4.53 6.53a.75.75 0 1 1-1.06-1.06l4-4z"/>
  </symbol>
  <symbol id="diffs-icon-file-code" viewBox="0 0 16 16">
    <path d="M10.75 0c.199 0 .39.08.53.22l3.5 3.5c.14.14.22.331.22.53v9A2.75 2.75 0 0 1 12.25 16h-8.5A2.75 2.75 0 0 1 1 13.25V2.75A2.75 2.75 0 0 1 3.75 0zm-7 1.5c-.69 0-1.25.56-1.25 1.25v10.5c0 .69.56 1.25 1.25 1.25h8.5c.69 0 1.25-.56 1.25-1.25V5h-1.25A2.25 2.25 0 0 1 10 2.75V1.5z"/><path d="M7.248 6.19a.75.75 0 0 1 .063 1.058L5.753 9l1.558 1.752a.75.75 0 0 1-1.122.996l-2-2.25a.75.75 0 0 1 0-.996l2-2.25a.75.75 0 0 1 1.06-.063M8.69 7.248a.75.75 0 1 1 1.12-.996l2 2.25a.75.75 0 0 1 0 .996l-2 2.25a.75.75 0 1 1-1.12-.996L10.245 9z"/>
  </symbol>
  <symbol id="diffs-icon-plus" viewBox="0 0 16 16">
    <path d="M8 3a.75.75 0 0 1 .75.75v3.5h3.5a.75.75 0 0 1 0 1.5h-3.5v3.5a.75.75 0 0 1-1.5 0v-3.5h-3.5a.75.75 0 0 1 0-1.5h3.5v-3.5A.75.75 0 0 1 8 3"/>
  </symbol>
  <symbol id="diffs-icon-symbol-added" viewBox="0 0 16 16">
    <path d="M8 4a.75.75 0 0 1 .75.75v2.5h2.5a.75.75 0 0 1 0 1.5h-2.5v2.5a.75.75 0 0 1-1.5 0v-2.5h-2.5a.75.75 0 0 1 0-1.5h2.5v-2.5A.75.75 0 0 1 8 4"/><path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/>
  </symbol>
  <symbol id="diffs-icon-symbol-deleted" viewBox="0 0 16 16">
    <path d="M4 8a.75.75 0 0 1 .75-.75h6.5a.75.75 0 0 1 0 1.5h-6.5A.75.75 0 0 1 4 8"/><path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/>
  </symbol>
  <symbol id="diffs-icon-symbol-diffstat" viewBox="0 0 16 16">
    <path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/><path d="M8.75 4.296a.75.75 0 0 0-1.5 0V6.25h-2a.75.75 0 0 0 0 1.5h2v1.5h1.5v-1.5h2a.75.75 0 0 0 0-1.5h-2zM5.25 10a.75.75 0 0 0 0 1.5h5.5a.75.75 0 0 0 0-1.5z"/>
  </symbol>
  <symbol id="diffs-icon-symbol-ignored" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706s.826.607 1.706.802c.898.2 2.091.288 3.704.288s2.806-.088 3.704-.288c.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5s-2.806.088-3.704.288c-.88.196-1.381.478-1.706.802s-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8M0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m11.53-2.47a.75.75 0 0 0-1.06-1.06l-6 6a.75.75 0 1 0 1.06 1.06z"/>
  </symbol>
  <symbol id="diffs-icon-symbol-modified" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706s.826.607 1.706.802c.898.2 2.091.288 3.704.288s2.806-.088 3.704-.288c.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5s-2.806.088-3.704.288c-.88.196-1.381.478-1.706.802s-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8M0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m8 3a3 3 0 1 0 0-6 3 3 0 0 0 0 6"/>
  </symbol>
  <symbol id="diffs-icon-symbol-moved" viewBox="0 0 16 16">
    <path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/><path d="M8.495 4.695a.75.75 0 0 0-.05 1.06L10.486 8l-2.041 2.246a.75.75 0 0 0 1.11 1.008l2.5-2.75a.75.75 0 0 0 0-1.008l-2.5-2.75a.75.75 0 0 0-1.06-.051m-4 0a.75.75 0 0 0-.05 1.06l2.044 2.248-1.796 1.995a.75.75 0 0 0 1.114 1.004l2.25-2.5a.75.75 0 0 0-.002-1.007l-2.5-2.75a.75.75 0 0 0-1.06-.05"/>
  </symbol>
  <symbol id="diffs-icon-symbol-ref" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706.286.286.71.54 1.41.73V1.86c-.7.19-1.124.444-1.41.73-.324.325-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8m4 6.397c.697.07 1.522.103 2.5.103 1.613 0 2.806-.088 3.704-.288.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5c-.978 0-1.803.033-2.5.103zM0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m7-2a1 1 0 0 1 1-1h3a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H8a1 1 0 0 1-1-1z"/>
  </symbol>
</svg>`;

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areLineAnnotationsEqual.js
function areLineAnnotationsEqual(annotationA, annotationB) {
  return annotationA.lineNumber === annotationB.lineNumber && annotationA.metadata === annotationB.metadata;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/arePrePropertiesEqual.js
function arePrePropertiesEqual(propsA, propsB) {
  if (propsA == null || propsB == null) return propsA === propsB;
  return areCustomPropertiesEqual(propsA.customProperties, propsB.customProperties) && propsA.type === propsB.type && propsA.diffIndicators === propsB.diffIndicators && propsA.disableBackground === propsB.disableBackground && propsA.disableLineNumbers === propsB.disableLineNumbers && propsA.overflow === propsB.overflow && propsA.split === propsB.split && propsA.totalLines === propsB.totalLines;
}
var EMPTY_CUSTOM_PROPERTIES = {};
function areCustomPropertiesEqual(customPropertiesA = EMPTY_CUSTOM_PROPERTIES, customPropertiesB = EMPTY_CUSTOM_PROPERTIES) {
  if (customPropertiesA === customPropertiesB) return true;
  const keysA = Object.keys(customPropertiesA);
  const keysB = Object.keys(customPropertiesB);
  if (keysA.length !== keysB.length) return false;
  for (const key2 of keysA) if (customPropertiesA[key2] !== customPropertiesB[key2]) return false;
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createAnnotationWrapperNode.js
function createAnnotationWrapperNode(slot) {
  const wrapper = document.createElement("div");
  wrapper.dataset.annotationSlot = "";
  wrapper.slot = slot;
  wrapper.style.whiteSpace = "normal";
  return wrapper;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createGutterUtilityContentNode.js
function createGutterUtilityContentNode() {
  const gutterUtilityContent = document.createElement("div");
  gutterUtilityContent.slot = "gutter-utility-slot";
  gutterUtilityContent.style.position = "absolute";
  gutterUtilityContent.style.top = "0";
  gutterUtilityContent.style.bottom = "0";
  gutterUtilityContent.style.textAlign = "center";
  gutterUtilityContent.style.whiteSpace = "normal";
  gutterUtilityContent.style.touchAction = "none";
  return gutterUtilityContent;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createUnsafeCSSStyleNode.js
function createUnsafeCSSStyleNode() {
  const node = document.createElement("style");
  node.setAttribute(UNSAFE_CSS_ATTRIBUTE, "");
  return node;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/style.js
var style_default = '@layer base {\n  :host {\n    --diffs-font-fallback: "SF Mono", Monaco, Consolas, "Ubuntu Mono", "Liberation Mono",\n      "Courier New", monospace;\n    --diffs-header-font-fallback: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue",\n      "Noto Sans", "Liberation Sans", Arial, sans-serif;\n    --diffs-mixer: light-dark(#000, #fff);\n    --diffs-gap-fallback: 8px;\n    --diffs-scrollbar-gutter-fallback: 6px;\n    --diffs-scrollbar-gutter: var(--diffs-scrollbar-gutter-override, var(--diffs-scrollbar-gutter-measured, var(--diffs-scrollbar-gutter-fallback)));\n    --diffs-added-light: #0dbe4e;\n    --diffs-added-dark: #5ecc71;\n    --diffs-modified-light: #009fff;\n    --diffs-modified-dark: #69b1ff;\n    --diffs-deleted-light: #ff2e3f;\n    --diffs-deleted-dark: #ff6762;\n    --diffs-warning-light: #d5a910;\n    --diffs-warning-dark: #ffd452;\n    color-scheme: light dark;\n    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));\n    font-size: var(--diffs-font-size, 13px);\n    line-height: var(--diffs-line-height, 20px);\n    font-feature-settings: var(--diffs-font-features);\n    --diffs-bg: light-dark(var(--diffs-light-bg, #fff), var(--diffs-dark-bg, #000));\n    --diffs-bg-buffer: var(--diffs-bg-buffer-override, light-dark(color-mix(in lab, var(--diffs-bg) 92%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 92%, var(--diffs-mixer))));\n    --diffs-bg-context: var(--diffs-bg-context-override, light-dark(color-mix(in lab, var(--diffs-bg) 98.5%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 92.5%, var(--diffs-mixer))));\n    --diffs-bg-context-gutter: var(--diffs-bg-context-gutter-override, light-dark(color-mix(in lab, var(--diffs-bg-context) 90%, var(--diffs-bg)), color-mix(in lab, var(--diffs-bg-context) 45%, var(--diffs-bg))));\n    --diffs-bg-separator: var(--diffs-bg-separator-override, light-dark(color-mix(in lab, var(--diffs-bg) 96%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 85%, var(--diffs-mixer))));\n    --diffs-fg: light-dark(var(--diffs-light, #000), var(--diffs-dark, #fff));\n    --diffs-fg-number: var(--diffs-fg-number-override, light-dark(color-mix(in lab, var(--diffs-fg) 65%, var(--diffs-bg)), color-mix(in lab, var(--diffs-fg) 65%, var(--diffs-bg))));\n    --diffs-fg-conflict-marker: var(--diffs-fg-conflict-marker-override, var(--diffs-fg-number));\n    --diffs-deletion-base: var(--diffs-deletion-color-override, light-dark(var(--diffs-light-deletion-color, var(--diffs-deletion-color, var(--diffs-deleted-light))), var(--diffs-dark-deletion-color, var(--diffs-deletion-color, var(--diffs-deleted-dark)))));\n    --diffs-addition-base: var(--diffs-addition-color-override, light-dark(var(--diffs-light-addition-color, var(--diffs-addition-color, var(--diffs-added-light))), var(--diffs-dark-addition-color, var(--diffs-addition-color, var(--diffs-added-dark)))));\n    --diffs-modified-base: var(--diffs-modified-color-override, light-dark(var(--diffs-light-modified-color, var(--diffs-modified-color, var(--diffs-modified-light))), var(--diffs-dark-modified-color, var(--diffs-modified-color, var(--diffs-modified-dark)))));\n    --diffs-bg-deletion: var(--diffs-bg-deletion-override, light-dark(color-mix(in lab, var(--diffs-bg) 88%, var(--diffs-deletion-base)), color-mix(in lab, var(--diffs-bg) 80%, var(--diffs-deletion-base))));\n    --diffs-bg-deletion-emphasis: var(--diffs-bg-deletion-emphasis-override, light-dark(rgb(from var(--diffs-deletion-base) r g b / .15), rgb(from var(--diffs-deletion-base) r g b / .2)));\n    --diffs-bg-addition: var(--diffs-bg-addition-override, light-dark(color-mix(in lab, var(--diffs-bg) 88%, var(--diffs-addition-base)), color-mix(in lab, var(--diffs-bg) 80%, var(--diffs-addition-base))));\n    --diffs-bg-addition-emphasis: var(--diffs-bg-addition-emphasis-override, light-dark(rgb(from var(--diffs-addition-base) r g b / .15), rgb(from var(--diffs-addition-base) r g b / .2)));\n    --diffs-selection-base: var(--diffs-modified-base);\n    --diffs-selection-number-fg: light-dark(color-mix(in lab, var(--diffs-selection-base) 65%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-selection-base) 75%, var(--diffs-mixer)));\n    background-color: var(--diffs-bg);\n    color: var(--diffs-fg);\n    display: block;\n  }\n\n  pre, code, [data-error-wrapper] {\n    isolation: isolate;\n    font-family: var(--diffs-font-family, var(--diffs-font-fallback));\n    outline: none;\n    margin: 0;\n    padding: 0;\n    display: block;\n  }\n\n  pre, code {\n    background-color: var(--diffs-bg);\n  }\n\n  code {\n    contain: content;\n  }\n\n  input, button {\n    font-family: inherit;\n    font-size: inherit;\n    line-height: inherit;\n  }\n\n  *, :before, :after {\n    box-sizing: border-box;\n  }\n\n  [data-icon-sprite] {\n    display: none;\n  }\n\n  [data-diffs-header], [data-separator] {\n    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));\n  }\n\n  [data-diffs-header][data-sticky] {\n    z-index: 1;\n    background-color: var(--diffs-bg);\n    position: sticky;\n    top: 0;\n  }\n\n  [data-file-info] {\n    color: var(--fg);\n    background-color: color-mix(in lab, var(--bg) 98%, var(--fg));\n    border-block: 1px solid color-mix(in lab, var(--bg) 95%, var(--fg));\n    padding: 10px;\n    font-weight: 700;\n  }\n\n  [data-diff], [data-file] {\n    --diffs-grid-number-column-width: minmax(min-content, max-content);\n    --diffs-code-grid: var(--diffs-grid-number-column-width) 1fr;\n\n    &[data-dehydrated] {\n      --diffs-code-grid: var(--diffs-grid-number-column-width) minmax(0, 1fr);\n    }\n\n    &:hover [data-code]::-webkit-scrollbar-thumb {\n      background-color: var(--diffs-bg-context);\n    }\n  }\n\n  @supports (-webkit-touch-callout: none) {\n    :host {\n      --diffs-scrollbar-gutter-fallback: 0px;\n    }\n  }\n\n  [data-line] span {\n    color: light-dark(var(--diffs-token-light, var(--diffs-light)), var(--diffs-token-dark, var(--diffs-dark)));\n    background-color: light-dark(var(--diffs-token-light-bg, inherit), var(--diffs-token-dark-bg, inherit));\n    font-weight: light-dark(var(--diffs-token-light-font-weight, inherit), var(--diffs-token-dark-font-weight, inherit));\n    font-style: light-dark(var(--diffs-token-light-font-style, inherit), var(--diffs-token-dark-font-style, inherit));\n    text-decoration: light-dark(var(--diffs-token-light-text-decoration, inherit), var(--diffs-token-dark-text-decoration, inherit));\n  }\n\n  [data-line], [data-gutter-buffer], [data-column-number], [data-line-annotation], [data-no-newline], [data-merge-conflict], [data-merge-conflict-actions], [data-editor-overlay] {\n    --diffs-computed-decoration-bg: var(--diffs-bg);\n    --diffs-computed-diff-line-bg: var(--diffs-computed-decoration-bg);\n    --diffs-computed-selected-line-bg: var(--diffs-computed-diff-line-bg);\n    --diffs-computed-editor-active-line-bg: var(--diffs-computed-selected-line-bg);\n    --diffs-computed-hovered-line-bg: var(--diffs-computed-editor-active-line-bg);\n    --diffs-hover-mix-target: var(--diffs-bg-hover-override, var(--diffs-mixer));\n    --diffs-line-bg: var(--diffs-computed-hovered-line-bg);\n    color: var(--diffs-fg);\n    background-color: var(--diffs-line-bg, var(--diffs-bg));\n  }\n\n  [data-line], [data-no-newline] {\n    &[data-decoration-bg] {\n      --mix-deco-light: 92%;\n      --mix-deco-dark: 85%;\n\n      &[data-decoration-bg-depth="2"] {\n        --mix-deco-light: 88%;\n        --mix-deco-dark: 80%;\n      }\n\n      &[data-decoration-bg-depth="3"] {\n        --mix-deco-light: 85%;\n        --mix-deco-dark: 78%;\n      }\n\n      --diffs-hover-mix-target: var(--diffs-decoration-bg);\n      --diffs-computed-decoration-bg: light-dark(color-mix(in lab,\n          var(--diffs-bg) var(--mix-deco-light),\n          var(--diffs-decoration-bg)), color-mix(in lab,\n          var(--diffs-bg) var(--mix-deco-dark),\n          var(--diffs-decoration-bg)));\n    }\n  }\n\n  [data-line-annotation], [data-gutter-buffer="annotation"] {\n    --diffs-annotation-bg: var(--diffs-bg-context);\n    --diffs-computed-decoration-bg: var(--diffs-annotation-bg);\n    --diffs-hover-mix-target: var(--diffs-computed-editor-active-line-bg);\n  }\n\n  [data-merge-conflict-actions], [data-gutter-buffer="merge-conflict-action"], [data-gutter-buffer="merge-conflict-marker-base"], [data-gutter-buffer="merge-conflict-marker-separator"], [data-merge-conflict="marker-base"], [data-merge-conflict="marker-separator"] {\n    --diffs-computed-decoration-bg: var(--diffs-bg-context);\n    --diffs-hover-mix-target: var(--diffs-computed-editor-active-line-bg);\n  }\n\n  [data-gutter-buffer="merge-conflict-marker-start"], [data-merge-conflict="marker-start"] {\n    --diffs-computed-decoration-bg: light-dark(color-mix(in lab,\n        var(--diffs-bg) 78%,\n        var(--conflict-bg-current-header-override, var(--diffs-addition-base))), color-mix(in lab,\n        var(--diffs-bg) 68%,\n        var(--conflict-bg-current-header-override, var(--diffs-addition-base))));\n    --diffs-hover-mix-target: var(--diffs-computed-editor-active-line-bg);\n  }\n\n  [data-gutter-buffer="merge-conflict-marker-end"], [data-merge-conflict="marker-end"] {\n    --diffs-computed-decoration-bg: light-dark(color-mix(in lab,\n        var(--diffs-bg) 78%,\n        var(--conflict-bg-incoming-header-override, var(--diffs-modified-base))), color-mix(in lab,\n        var(--diffs-bg) 68%,\n        var(--conflict-bg-incoming-header-override, var(--diffs-modified-base))));\n    --diffs-hover-mix-target: var(--diffs-computed-editor-active-line-bg);\n  }\n\n  [data-has-merge-conflict] [data-line-annotation], [data-has-merge-conflict] [data-gutter-buffer="annotation"] {\n    --diffs-computed-decoration-bg: var(--diffs-bg);\n    --diffs-hover-mix-target: var(--diffs-computed-editor-active-line-bg);\n  }\n\n  :where([data-background]) {\n    & [data-gutter-buffer], & [data-column-number] {\n      --mix-light: 91%;\n      --mix-dark: 85%;\n    }\n\n    & [data-line], & [data-no-newline] {\n      --mix-light: 88%;\n      --mix-dark: 80%;\n    }\n\n    & [data-gutter-buffer], & [data-column-number], & [data-line], & [data-no-newline] {\n      --diffs-diff-line-mix-target: var(--diffs-bg);\n\n      &[data-line-type="change-deletion"] {\n        --diffs-diff-line-mix-target: var(--diffs-bg-deletion-override, var(--diffs-deletion-base));\n        --diffs-hover-mix-target: var(--diffs-diff-line-mix-target);\n\n        &:where([data-gutter-buffer], [data-column-number]) {\n          color: var(--diffs-fg-number-deletion-override, var(--diffs-deletion-base));\n          --diffs-diff-line-mix-target: var(--diffs-bg-deletion-number-override, var(--diffs-deletion-base));\n        }\n\n        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-light),\n            var(--diffs-diff-line-mix-target)), color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-dark),\n            var(--diffs-diff-line-mix-target)));\n      }\n\n      &[data-line-type="change-addition"] {\n        --diffs-diff-line-mix-target: var(--diffs-bg-addition-override, var(--diffs-addition-base));\n        --diffs-hover-mix-target: var(--diffs-diff-line-mix-target);\n\n        &:where([data-gutter-buffer], [data-column-number]) {\n          color: var(--diffs-fg-number-addition-override, var(--diffs-addition-base));\n          --diffs-diff-line-mix-target: var(--diffs-bg-addition-number-override, var(--diffs-addition-base));\n        }\n\n        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-light),\n            var(--diffs-diff-line-mix-target)), color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-dark),\n            var(--diffs-diff-line-mix-target)));\n      }\n\n      &[data-merge-conflict="current"] {\n        --diffs-diff-line-mix-target: var(--conflict-bg-current-override, var(--diffs-addition-base));\n        --diffs-hover-mix-target: var(--diffs-diff-line-mix-target);\n\n        &:where([data-gutter-buffer], [data-column-number]) {\n          color: var(--diffs-fg-number-addition-override, var(--diffs-addition-base));\n          --diffs-diff-line-mix-target: var(--conflict-bg-current-number-override, var(--diffs-addition-base));\n        }\n\n        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-light),\n            var(--diffs-diff-line-mix-target)), color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-dark),\n            var(--diffs-diff-line-mix-target)));\n      }\n\n      &[data-merge-conflict="incoming"] {\n        --diffs-diff-line-mix-target: var(--conflict-bg-incoming-override, var(--diffs-modified-base));\n        --diffs-hover-mix-target: var(--diffs-diff-line-mix-target);\n\n        &:where([data-gutter-buffer], [data-column-number]) {\n          color: var(--diffs-modified-base);\n          --diffs-diff-line-mix-target: var(--conflict-bg-incoming-number-override, var(--diffs-modified-base));\n        }\n\n        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-light),\n            var(--diffs-diff-line-mix-target)), color-mix(in lab,\n            var(--diffs-computed-decoration-bg) var(--mix-dark),\n            var(--diffs-diff-line-mix-target)));\n      }\n    }\n  }\n\n  [data-gutter-buffer], [data-column-number], [data-line], [data-line-annotation], [data-merge-conflict], [data-merge-conflict-actions], [data-no-newline], [data-editor-overlay] {\n    --diffs-selection-mix-target: var(--diffs-bg-selection-override, var(--diffs-selection-base));\n    --diffs-selection-emphasis-mix-target: var(--diffs-selection-mix-target);\n\n    &:where([data-editor-overlay]), &:where([data-line], [data-line-annotation], [data-merge-conflict], [data-merge-conflict-actions], [data-no-newline])[data-selected-line] {\n      --mix-selection-light: 82%;\n      --mix-selection-dark: 75%;\n      --diffs-hover-mix-target: var(--diffs-selection-mix-target);\n    }\n\n    &:where([data-gutter-buffer][data-selected-line], [data-column-number]:is([data-selected-line], [data-editor-active-line])) {\n      --mix-selection-light: 75%;\n      --mix-selection-dark: 60%;\n      --diffs-selection-mix-target: var(--diffs-bg-selection-number-override, var(--diffs-selection-base));\n      --diffs-hover-mix-target: var(--diffs-selection-mix-target);\n    }\n\n    &:where([data-line][data-selected-line]):is([data-line-type="change-addition"], [data-line-type="change-deletion"]) {\n      --diffs-selection-emphasis-mix-target: light-dark(color-mix(in lab,\n          var(--diffs-diff-line-mix-target, var(--diffs-selection-mix-target))\n            var(--mix-selection-light),\n          var(--diffs-selection-mix-target)), var(--diffs-selection-mix-target));\n    }\n\n    &:where([data-editor-overlay]), &[data-selected-line] {\n      --diffs-computed-selected-line-bg: light-dark(color-mix(in lab,\n          var(--diffs-computed-diff-line-bg) var(--mix-selection-light),\n          var(--diffs-selection-mix-target)), color-mix(in lab,\n          var(--diffs-computed-diff-line-bg) var(--mix-selection-dark),\n          var(--diffs-selection-mix-target)));\n    }\n  }\n\n  [data-line][data-editor-active-line], [data-column-number][data-editor-active-line] {\n    --diffs-computed-editor-active-line-bg: color-mix(in lab,\n      var(--diffs-computed-selected-line-bg)\n        var(--diffs-editor-active-line-source-mix, 100%),\n      var(--diffs-selection-emphasis-mix-target));\n  }\n\n  @media (pointer: fine) {\n    [data-line][data-hovered], [data-gutter-buffer][data-hovered], [data-column-number][data-hovered], [data-line-annotation][data-hovered], [data-no-newline][data-hovered], [data-merge-conflict][data-hovered], [data-merge-conflict-actions][data-hovered], [data-editor-overlay][data-hovered] {\n      --diffs-computed-hovered-line-bg: light-dark(color-mix(in lab,\n          var(--diffs-computed-editor-active-line-bg) 97%,\n          var(--diffs-hover-mix-target)), color-mix(in lab,\n          var(--diffs-computed-editor-active-line-bg) 91%,\n          var(--diffs-hover-mix-target)));\n    }\n  }\n\n  [data-gutter-buffer][data-selected-line], [data-column-number]:is([data-selected-line], [data-editor-active-line]) {\n    color: var(--diffs-selection-number-fg);\n  }\n\n  [data-no-newline] {\n    user-select: none;\n\n    & span {\n      opacity: .6;\n    }\n  }\n\n  [data-diff-type="split"][data-overflow="scroll"] {\n    grid-template-columns: 1fr 1fr;\n    display: grid;\n\n    & [data-additions] {\n      border-left: 1px solid var(--diffs-bg);\n    }\n\n    & [data-deletions] {\n      border-right: 1px solid var(--diffs-bg);\n    }\n  }\n\n  [data-code] {\n    grid-auto-flow: dense;\n    grid-template-columns: var(--diffs-code-grid);\n    overflow: var(--diffs-overflow-override, scroll) clip;\n    overscroll-behavior-x: none;\n    tab-size: var(--diffs-tab-size, 2);\n    padding-top: var(--diffs-gap-block, var(--diffs-gap-fallback));\n    padding-bottom: max(0px,\n      calc(var(--diffs-gap-block, var(--diffs-gap-fallback)) -\n          var(--diffs-scrollbar-gutter)));\n    scrollbar-gutter: stable;\n    align-self: flex-start;\n    display: grid;\n  }\n\n  [data-diffs-scrollbar-measure] {\n    opacity: 0;\n    pointer-events: none;\n    scrollbar-gutter: auto;\n    grid-template-columns: none;\n    width: 100px;\n    height: 100px;\n    padding: 0;\n    position: absolute;\n    top: -200px;\n    left: -200px;\n  }\n\n  [data-container-size] {\n    container-type: inline-size;\n  }\n\n  [data-code]::-webkit-scrollbar {\n    width: 0;\n    height: var(--diffs-scrollbar-gutter);\n  }\n\n  [data-code]::-webkit-scrollbar-track {\n    background: none;\n  }\n\n  [data-code]::-webkit-scrollbar-thumb {\n    background-color: #0000;\n    background-clip: content-box;\n    border: 1px solid #0000;\n    border-radius: 3px;\n  }\n\n  [data-code]::-webkit-scrollbar-corner {\n    background-color: #0000;\n  }\n\n  @supports ((-moz-appearance: none)) {\n    [data-code] {\n      scrollbar-width: thin;\n      scrollbar-color: var(--diffs-bg-context) transparent;\n      padding-bottom: var(--diffs-gap-block, var(--diffs-gap-fallback));\n    }\n  }\n\n  [data-diffs-header] ~ [data-diff], [data-diffs-header] ~ [data-file] {\n    & [data-code], &[data-overflow="wrap"], &[data-dehydrated][data-diff-type="split"][data-overflow="scroll"] {\n      padding-top: 0;\n    }\n  }\n\n  [data-gutter] {\n    grid-template-rows: subgrid;\n    grid-template-columns: subgrid;\n    z-index: 3;\n    background-color: var(--diffs-bg);\n    grid-column: 1;\n    display: grid;\n    position: relative;\n\n    & [data-gutter-buffer], & [data-column-number] {\n      border-right: var(--diffs-gap-style, 2px solid var(--diffs-bg));\n    }\n  }\n\n  [data-content] {\n    grid-template-rows: subgrid;\n    grid-template-columns: subgrid;\n    background-color: var(--diffs-bg);\n    grid-column: 2;\n    min-width: 0;\n    display: grid;\n  }\n\n  [data-diff-type="split"][data-overflow="wrap"], [data-dehydrated][data-diff-type="split"][data-overflow="scroll"] {\n    grid-auto-flow: dense;\n    grid-template-columns: repeat(2, var(--diffs-code-grid));\n    padding-block: var(--diffs-gap-block, var(--diffs-gap-fallback));\n    display: grid;\n\n    & [data-code] {\n      display: contents;\n    }\n\n    & [data-deletions] {\n      & [data-gutter] {\n        grid-column: 1;\n      }\n\n      & [data-content] {\n        border-right: 1px solid var(--diffs-bg);\n        grid-column: 2;\n      }\n    }\n\n    & [data-additions] {\n      & [data-gutter] {\n        border-left: 1px solid var(--diffs-bg);\n        grid-column: 3;\n      }\n\n      & [data-content] {\n        grid-column: 4;\n      }\n    }\n  }\n\n  [data-dehydrated][data-diff-type="split"][data-overflow="scroll"] [data-content] {\n    overflow: clip;\n  }\n\n  [data-overflow="scroll"] [data-gutter] {\n    position: sticky;\n    left: 0;\n  }\n\n  [data-interactive-lines] [data-line] {\n    cursor: pointer;\n  }\n\n  [data-interactive-line-numbers] [data-column-number] {\n    cursor: pointer;\n    touch-action: none;\n  }\n\n  [data-content-buffer], [data-gutter-buffer] {\n    user-select: none;\n    min-height: 1lh;\n    position: relative;\n  }\n\n  [data-gutter-buffer] {\n    padding-left: 2ch;\n    padding-right: 1ch;\n\n    &:before {\n      content: "";\n      min-width: var(--diffs-min-number-column-width, var(--diffs-min-number-column-width-default, 3ch));\n      display: block;\n    }\n  }\n\n  [data-gutter-buffer="annotation"] {\n    --diffs-annotation-bg: var(--diffs-bg-context-gutter);\n    min-height: 0;\n  }\n\n  [data-gutter-buffer="buffer"] {\n    --diffs-line-bg: var(--diffs-bg-context-gutter);\n  }\n\n  [data-content-buffer] {\n    background-position: 5px 0;\n    background-size: 8px 8px;\n    background-origin: border-box;\n    background-image: repeating-linear-gradient(-45deg,\n      transparent,\n      transparent calc(3px * 1.414),\n      var(--diffs-bg-buffer) calc(3px * 1.414),\n      var(--diffs-bg-buffer) calc(4px * 1.414));\n    grid-column: 1;\n  }\n\n  [data-separator] {\n    box-sizing: content-box;\n    background-color: var(--diffs-bg);\n  }\n\n  [data-separator="simple"] {\n    min-height: 4px;\n  }\n\n  [data-separator="line-info"], [data-separator="line-info-basic"], [data-separator="metadata"], [data-separator="simple"] {\n    background-color: var(--diffs-bg-separator);\n  }\n\n  [data-separator="line-info"], [data-separator="line-info-basic"], [data-separator="metadata"] {\n    height: 32px;\n    position: relative;\n  }\n\n  [data-separator-wrapper] {\n    user-select: none;\n    fill: currentColor;\n    background-color: var(--diffs-bg);\n    align-items: center;\n    height: 100%;\n    display: flex;\n    position: absolute;\n    inset-inline: 0;\n  }\n\n  [data-content] [data-separator-wrapper] {\n    display: none;\n  }\n\n  [data-separator="metadata"] [data-separator-wrapper] {\n    background-color: var(--diffs-bg-separator);\n    height: 100%;\n    color: var(--diffs-fg-number);\n    white-space: nowrap;\n    text-overflow: ellipsis;\n    min-width: min-content;\n    padding-inline: 1ch;\n    inset-inline: 100% auto;\n    overflow: hidden;\n  }\n\n  [data-separator="line-info"] {\n    margin-block: var(--diffs-gap-block, var(--diffs-gap-fallback));\n\n    & [data-separator-wrapper] {\n      min-width: 16px;\n    }\n  }\n\n  [data-separator="line-info-basic"], [data-separator="metadata"] {\n    margin-block: 0;\n  }\n\n  [data-separator="line-info"][data-separator-first] {\n    margin-top: 0;\n  }\n\n  [data-separator="line-info"][data-separator-last] {\n    margin-bottom: 0;\n  }\n\n  [data-expand-index] [data-separator-wrapper] {\n    grid-template-columns: 32px auto;\n    display: grid;\n  }\n\n  [data-expand-index] [data-separator-wrapper][data-separator-multi-button] {\n    grid-template-columns: 32px 32px auto;\n  }\n\n  [data-expand-button], [data-separator-content] {\n    background-color: var(--diffs-bg-separator);\n    flex: none;\n    align-items: center;\n    display: flex;\n  }\n\n  [data-expand-index] [data-separator-content]:hover {\n    cursor: pointer;\n    text-decoration: underline;\n  }\n\n  [data-expand-button] {\n    cursor: pointer;\n    min-width: 32px;\n    color: var(--diffs-fg-number);\n    border-right: 2px solid var(--diffs-bg);\n    flex-shrink: 0;\n    justify-content: center;\n    align-self: stretch;\n\n    &:hover {\n      color: var(--diffs-fg);\n    }\n\n    &[data-expand-all-button] {\n      display: none;\n    }\n  }\n\n  [data-expand-down] [data-icon] {\n    transform: scaleY(-1);\n  }\n\n  [data-separator-content] {\n    height: 100%;\n    color: var(--diffs-fg-number);\n    flex: auto;\n    justify-content: flex-start;\n    padding: 0 1ch;\n    overflow: hidden;\n  }\n\n  [data-separator="line-info"], [data-separator="line-info-basic"] {\n    & [data-separator-content] {\n      user-select: none;\n      height: 100%;\n      overflow: clip;\n    }\n  }\n\n  [data-unmodified-lines] {\n    text-overflow: ellipsis;\n    white-space: nowrap;\n    flex: 0 auto;\n    min-width: 0;\n    display: block;\n    overflow: hidden;\n  }\n\n  @supports (width: 1cqi) {\n    [data-unified] {\n      & [data-separator="line-info"] [data-separator-wrapper] {\n        padding-inline: var(--diffs-gap-inline, var(--diffs-gap-fallback));\n        width: 100cqi;\n\n        & [data-separator-content] {\n          border-radius: 6px;\n        }\n      }\n\n      & [data-separator="line-info"][data-expand-index] [data-separator-wrapper] [data-separator-content] {\n        border-top-left-radius: unset;\n        border-bottom-left-radius: unset;\n      }\n    }\n\n    [data-gutter] {\n      & [data-separator="line-info"] [data-separator-wrapper] {\n        padding-left: var(--diffs-gap-inline, var(--diffs-gap-fallback));\n      }\n\n      & [data-separator="line-info"] [data-separator-content] {\n        border-top-left-radius: 6px;\n        border-bottom-left-radius: 6px;\n      }\n\n      & [data-separator="line-info"][data-expand-index] [data-separator-content] {\n        border-top-left-radius: unset;\n        border-bottom-left-radius: unset;\n      }\n    }\n\n    [data-additions] {\n      & [data-content] [data-separator="line-info"] {\n        background-color: var(--diffs-bg);\n\n        & [data-separator-wrapper] {\n          display: none;\n        }\n      }\n\n      & [data-gutter] [data-separator="line-info"] [data-separator-wrapper] {\n        background-color: var(--diffs-bg-separator);\n        border-top-right-radius: 6px;\n        border-bottom-right-radius: 6px;\n        height: 100%;\n        display: block;\n\n        & [data-separator-content], & [data-expand-button] {\n          display: none;\n        }\n      }\n    }\n\n    [data-overflow="scroll"] [data-additions] [data-gutter] [data-separator="line-info"] [data-separator-wrapper] {\n      width: calc(100cqi - var(--diffs-gap-inline, var(--diffs-gap-fallback)));\n    }\n\n    [data-overflow="wrap"] [data-additions] [data-content] [data-separator="line-info"] [data-separator-wrapper] {\n      background-color: var(--diffs-bg-separator);\n      height: 100%;\n      margin-right: var(--diffs-gap-inline, var(--diffs-gap-fallback));\n      border-top-right-radius: 6px;\n      border-bottom-right-radius: 6px;\n      display: block;\n\n      & [data-separator-content], & [data-expand-button] {\n        display: none;\n      }\n    }\n\n    [data-separator="line-info"] [data-separator-wrapper] {\n      & [data-expand-both], & [data-expand-down], & [data-expand-up] {\n        border-top-left-radius: 6px;\n        border-bottom-left-radius: 6px;\n      }\n    }\n\n    @media (pointer: fine) {\n      [data-separator="line-info"] [data-separator-wrapper] {\n        &[data-separator-multi-button] {\n          & [data-expand-up] {\n            border-top-left-radius: 6px;\n            border-bottom-left-radius: unset;\n          }\n\n          & [data-expand-down] {\n            border-bottom-left-radius: 6px;\n            border-top-left-radius: unset;\n          }\n        }\n      }\n    }\n  }\n\n  @media (pointer: coarse) {\n    [data-separator="line-info-basic"] [data-separator-wrapper][data-separator-multi-button] {\n      grid-template-columns: 34px 34px auto;\n\n      & [data-separator-content] {\n        grid-column: unset;\n        grid-row: unset;\n      }\n    }\n\n    @supports (width: 1cqi) {\n      [data-separator="line-info"] [data-separator-wrapper] {\n        & [data-expand-both], & [data-expand-down], & [data-expand-up] {\n          border-top-left-radius: 6px;\n          border-bottom-left-radius: 6px;\n        }\n\n        &[data-separator-multi-button] {\n          & [data-expand-up] {\n            border-top-left-radius: 6px;\n            border-bottom-left-radius: 6px;\n          }\n\n          & [data-expand-down] {\n            border-bottom-left-radius: unset;\n            border-top-left-radius: unset;\n          }\n        }\n      }\n    }\n  }\n\n  @media (pointer: fine) {\n    [data-separator-wrapper][data-separator-multi-button] {\n      grid-template-rows: 50% 50%;\n      display: grid;\n\n      & [data-separator-content] {\n        grid-area: 1 / 2 / -1;\n        min-width: min-content;\n      }\n\n      & [data-expand-button] {\n        grid-column: 1;\n      }\n    }\n\n    [data-separator="line-info"] [data-separator-wrapper], [data-separator="line-info"] [data-separator-wrapper][data-separator-multi-button] {\n      grid-template-columns: 34px auto;\n    }\n\n    [data-separator="line-info-basic"][data-expand-index] [data-separator-wrapper] {\n      grid-template-columns: 100% auto;\n    }\n\n    [data-separator="line-info"], [data-separator="line-info-basic"] {\n      & [data-separator-multi-button] {\n        & [data-expand-up] {\n          border-bottom: 1px solid var(--diffs-bg);\n          border-right: 2px solid var(--diffs-bg);\n        }\n\n        & [data-expand-down] {\n          border-top: 1px solid var(--diffs-bg);\n          border-right: 2px solid var(--diffs-bg);\n        }\n      }\n    }\n  }\n\n  [data-additions] [data-gutter] [data-separator-wrapper], [data-additions] [data-separator="line-info-basic"] [data-separator-wrapper], [data-content] [data-separator-wrapper] {\n    display: none;\n  }\n\n  [data-line-annotation] {\n    min-height: var(--diffs-annotation-min-height, 0);\n    z-index: 2;\n  }\n\n  [data-merge-conflict-actions] {\n    z-index: 2;\n  }\n\n  [data-separator="custom"] {\n    grid-template-columns: subgrid;\n    display: grid;\n  }\n\n  [data-line], [data-column-number], [data-no-newline] {\n    padding-inline: 1ch;\n    position: relative;\n  }\n\n  [data-indicators="classic"] [data-line] {\n    padding-inline-start: 2ch;\n  }\n\n  [data-indicators="classic"] {\n    & [data-line-type="change-addition"], & [data-line-type="change-deletion"] {\n      &[data-no-newline], &[data-line] {\n        &:before {\n          user-select: none;\n          width: 1ch;\n          height: 1lh;\n          display: inline-block;\n          position: absolute;\n          top: 0;\n          left: 0;\n        }\n      }\n    }\n\n    & [data-line-type="change-addition"] {\n      &[data-line], &[data-no-newline] {\n        &:before {\n          content: "+";\n          color: var(--diffs-addition-base);\n        }\n      }\n    }\n\n    & [data-line-type="change-deletion"] {\n      &[data-line], &[data-no-newline] {\n        &:before {\n          content: "-";\n          color: var(--diffs-deletion-base);\n        }\n      }\n    }\n  }\n\n  [data-indicators="bars"] {\n    & [data-line-type="change-deletion"], & [data-line-type="change-addition"] {\n      &[data-column-number] {\n        &:before {\n          content: "";\n          user-select: none;\n          contain: strict;\n          width: 4px;\n          height: 100%;\n          display: block;\n          position: absolute;\n          top: 0;\n          left: 0;\n        }\n      }\n    }\n\n    & [data-line-type="change-deletion"] {\n      &[data-column-number] {\n        &:before {\n          background-image: linear-gradient(0deg,\n            var(--diffs-bg-deletion) 50%,\n            var(--diffs-deletion-base) 50%);\n          background-repeat: repeat;\n          background-size: 2px 2px;\n          background-size: calc(1lh / round(1lh / 2px))\n            calc(1lh / round(1lh / 2px));\n        }\n      }\n    }\n\n    & [data-line-type="change-addition"] {\n      &[data-column-number] {\n        &:before {\n          background-color: var(--diffs-addition-base);\n        }\n      }\n    }\n  }\n\n  [data-overflow="wrap"] {\n    & [data-line] {\n      white-space: pre-wrap;\n      word-break: break-word;\n    }\n\n    & [data-annotation-content] {\n      word-break: break-word;\n    }\n  }\n\n  [data-overflow="scroll"] [data-line] {\n    white-space: pre;\n    min-height: 1lh;\n  }\n\n  [data-column-number] {\n    box-sizing: content-box;\n    text-align: right;\n    user-select: none;\n    color: var(--diffs-fg-number);\n    padding-left: 2ch;\n  }\n\n  [data-line-number-content] {\n    min-width: var(--diffs-min-number-column-width, var(--diffs-min-number-column-width-default, 3ch));\n    z-index: 1;\n    display: inline-block;\n    position: relative;\n  }\n\n  [data-disable-line-numbers] {\n    & [data-gutter-buffer], & [data-column-number] {\n      min-width: 4px;\n      padding: 0;\n\n      &:before {\n        min-width: 0;\n      }\n    }\n\n    & [data-line-number-content] {\n      display: none;\n    }\n\n    & [data-gutter-utility-slot] {\n      right: unset;\n      justify-content: flex-start;\n      left: 0;\n    }\n\n    &[data-indicators="bars"] [data-gutter-utility-slot] {\n      left: 6px;\n    }\n  }\n\n  [data-file][data-disable-line-numbers] {\n    & [data-gutter-buffer], & [data-column-number] {\n      border-right: 0;\n      min-width: 0;\n    }\n  }\n\n  [data-diff-span] {\n    box-decoration-break: clone;\n    border-radius: 3px;\n  }\n\n  [data-line-type="change-addition"] [data-diff-span] {\n    background-color: var(--diffs-bg-addition-emphasis);\n  }\n\n  [data-line-type="change-deletion"] [data-diff-span] {\n    background-color: var(--diffs-bg-deletion-emphasis);\n  }\n\n  [data-merge-conflict="marker-start"], [data-merge-conflict="marker-base"], [data-merge-conflict="marker-separator"], [data-merge-conflict="marker-end"] {\n    color: var(--diffs-fg);\n    padding-left: 1ch;\n  }\n\n  [data-merge-conflict="marker-start"], [data-merge-conflict="marker-end"] {\n    align-items: center;\n    display: flex;\n\n    &:after {\n      color: var(--diffs-fg-conflict-marker);\n      font-size: .75rem;\n      font-style: normal;\n      line-height: 1.25rem;\n      font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));\n      padding-left: 1ch;\n    }\n  }\n\n  [data-merge-conflict="marker-start"]:after {\n    content: "(Current Change)";\n  }\n\n  [data-merge-conflict="marker-end"]:after {\n    content: "(Incoming Change)";\n  }\n\n  [data-merge-conflict-actions-content] {\n    min-height: 1.75rem;\n    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));\n    color: var(--diffs-fg);\n    align-items: center;\n    gap: .25rem;\n    padding-inline: .5rem;\n    font-size: .75rem;\n    line-height: 1.2;\n    display: flex;\n  }\n\n  [data-merge-conflict-action] {\n    appearance: none;\n    color: var(--diffs-fg-number);\n    font: inherit;\n    cursor: pointer;\n    background: none;\n    border: 0;\n    padding: 0;\n    font-style: normal;\n  }\n\n  [data-merge-conflict-action]:hover {\n    color: var(--diffs-fg);\n  }\n\n  [data-merge-conflict-action="current"]:hover {\n    color: var(--diffs-addition-base);\n  }\n\n  [data-merge-conflict-action="incoming"]:hover {\n    color: var(--diffs-modified-base);\n  }\n\n  [data-merge-conflict-action-separator] {\n    color: var(--diffs-fg-number);\n    opacity: .6;\n    user-select: none;\n  }\n\n  [data-diffs-header="default"] {\n    background-color: var(--diffs-bg);\n    justify-content: space-between;\n    align-items: center;\n    gap: var(--diffs-gap-inline, var(--diffs-gap-fallback));\n    min-height: calc(1lh + (var(--diffs-gap-block, var(--diffs-gap-fallback)) * 3));\n    z-index: 2;\n    flex-direction: row;\n    padding-inline: 16px;\n    display: flex;\n    position: relative;\n    top: 0;\n  }\n\n  [data-header-content] {\n    align-items: center;\n    gap: var(--diffs-gap-inline, var(--diffs-gap-fallback));\n    white-space: nowrap;\n    flex-direction: row;\n    min-width: 0;\n    display: flex;\n  }\n\n  [data-header-content] [data-prev-name], [data-header-content] [data-title] {\n    text-overflow: ellipsis;\n    white-space: nowrap;\n    direction: rtl;\n    min-width: 0;\n    overflow: hidden;\n  }\n\n  [data-prev-name] {\n    opacity: .7;\n  }\n\n  [data-rename-icon] {\n    fill: currentColor;\n    flex-grow: 0;\n    flex-shrink: 0;\n  }\n\n  [data-diffs-header="default"] [data-metadata] {\n    white-space: nowrap;\n    align-items: center;\n    gap: 1ch;\n    display: flex;\n  }\n\n  [data-diffs-header="default"] [data-additions-count] {\n    font-family: var(--diffs-font-family, var(--diffs-font-fallback));\n    color: var(--diffs-addition-base);\n  }\n\n  [data-diffs-header="default"] [data-deletions-count] {\n    font-family: var(--diffs-font-family, var(--diffs-font-fallback));\n    color: var(--diffs-deletion-base);\n  }\n\n  [data-change-icon] {\n    fill: currentColor;\n    flex-shrink: 0;\n  }\n\n  [data-change-icon="change"], [data-change-icon="rename-pure"], [data-change-icon="rename-changed"] {\n    color: var(--diffs-modified-base);\n  }\n\n  [data-change-icon="new"] {\n    color: var(--diffs-addition-base);\n  }\n\n  [data-change-icon="deleted"] {\n    color: var(--diffs-deletion-base);\n  }\n\n  [data-change-icon="file"] {\n    opacity: .6;\n  }\n\n  [data-annotation-content] {\n    z-index: 2;\n    isolation: isolate;\n    white-space: normal;\n    align-self: flex-start;\n    min-width: 0;\n    display: flow-root;\n    position: relative;\n  }\n\n  [data-overflow="scroll"] [data-annotation-content], [data-overflow="scroll"] [data-merge-conflict-actions-content] {\n    width: var(--diffs-column-content-width, auto);\n    left: var(--diffs-column-number-width, 0);\n    position: sticky;\n  }\n\n  [data-annotation-slot] {\n    text-wrap-mode: wrap;\n    word-break: normal;\n    white-space-collapse: collapse;\n  }\n\n  [data-gutter-utility-slot] {\n    touch-action: none;\n    justify-content: flex-end;\n    display: flex;\n    position: absolute;\n    top: 0;\n    bottom: 0;\n    right: 0;\n  }\n\n  [data-utility-button] {\n    appearance: none;\n    cursor: pointer;\n    width: 1lh;\n    height: 1lh;\n    font-size: var(--diffs-font-size, 13px);\n    line-height: var(--diffs-line-height, 20px);\n    background-color: var(--diffs-modified-base);\n    color: var(--diffs-bg);\n    fill: currentColor;\n    z-index: 4;\n    touch-action: none;\n    border: none;\n    border-radius: 4px;\n    justify-content: center;\n    align-items: center;\n    margin-right: calc(-1lh + 1ch);\n    padding: 0;\n    display: flex;\n    position: relative;\n\n    &:before {\n      content: "";\n      display: block;\n      position: absolute;\n      inset: 0 0 0 -4px;\n    }\n  }\n\n  [data-decoration-bar-stack] {\n    pointer-events: none;\n    isolation: isolate;\n    z-index: 1;\n    background-color: var(--diffs-decoration-bar-color, transparent);\n    box-sizing: content-box;\n    border-left: 2px solid var(--diffs-bg);\n    border-right: 2px solid var(--diffs-bg);\n    width: 6px;\n    position: absolute;\n    top: 0;\n    bottom: 0;\n    right: -2px;\n\n    [data-decoration-bar-depth="1"] & {\n      background-color: color-mix(in lab,\n        var(--diffs-bg) 20%,\n        var(--diffs-decoration-bar-color, transparent));\n    }\n\n    [data-decoration-bar-depth="2"] & {\n      background-color: color-mix(in lab,\n        var(--diffs-bg) 45%,\n        var(--diffs-decoration-bar-color, transparent));\n    }\n\n    [data-decoration-bar-depth="3"] & {\n      background-color: color-mix(in lab,\n        var(--diffs-bg) 65%,\n        var(--diffs-decoration-bar-color, transparent));\n    }\n\n    [data-decoration-bar-start] & {\n      border-top-left-radius: 5px;\n      border-top-right-radius: 5px;\n    }\n\n    [data-decoration-bar-end] & {\n      z-index: 3;\n      border-bottom-right-radius: 5px;\n      border-bottom-left-radius: 5px;\n    }\n  }\n\n  [data-placeholder] {\n    contain: strict;\n  }\n\n  [data-error-wrapper] {\n    padding: var(--diffs-gap-block, var(--diffs-gap-fallback))\n      var(--diffs-gap-inline, var(--diffs-gap-fallback));\n    scrollbar-width: none;\n    max-height: 400px;\n    overflow: auto;\n\n    & [data-error-message] {\n      color: var(--diffs-deletion-base);\n      font-size: 18px;\n      font-weight: bold;\n    }\n\n    & [data-error-stack] {\n      color: var(--diffs-fg-number);\n    }\n  }\n}\n\n@layer theme, rendered, unsafe;\n';

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/scrollbarGutter.js
var measuredScrollbarGutter;
function getMeasuredScrollbarGutter(shadowRoot) {
  if (measuredScrollbarGutter != null) return measuredScrollbarGutter;
  const host = shadowRoot.host;
  if (typeof HTMLElement !== "undefined" && host instanceof HTMLElement && !host.isConnected) return;
  const wrapper = document.createElement("div");
  wrapper.setAttribute("data-code", "");
  wrapper.setAttribute(DIFFS_SCROLLBAR_MEASURE_ATTRIBUTE, "true");
  const child = document.createElement("div");
  child.style.position = "relative";
  child.style.width = "200%";
  child.style.height = "200%";
  wrapper.appendChild(child);
  shadowRoot.appendChild(wrapper);
  measuredScrollbarGutter = Math.max(wrapper.offsetHeight - wrapper.clientHeight, 0);
  wrapper.remove();
  return measuredScrollbarGutter;
}
function createMeasuredScrollbarGutterDeclaration(scrollbarGutter) {
  return `${DIFFS_SCROLLBAR_GUTTER_MEASURED_PROPERTY}: ${scrollbarGutter == null ? "var(--diffs-scrollbar-gutter-fallback)" : `${scrollbarGutter}px`};`;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/cssWrappers.js
var LAYER_ORDER = `@layer base, theme, rendered, unsafe;`;
var SCROLLBAR_GUTTER_DECLARATION_PATTERN = new RegExp(`${escapeRegExp(DIFFS_SCROLLBAR_GUTTER_MEASURED_PROPERTY)}\\s*:\\s*[^;]+;`);
function wrapCoreCSS(mainCSS) {
  return `${LAYER_ORDER}
${style_default}
@layer theme {
  ${mainCSS}
}`;
}
function wrapUnsafeCSS(unsafeCSS) {
  return `${LAYER_ORDER}
@layer unsafe {
  ${unsafeCSS}
}`;
}
function wrapThemeCSS(themeCSS, themeType = "system", scrollbarGutter) {
  return `${LAYER_ORDER}
@layer rendered {
  :host {${themeType === "system" ? "" : `
  color-scheme: ${themeType};`}
  ${createMeasuredScrollbarGutterDeclaration(scrollbarGutter)}
  ${themeCSS}
  }
}`;
}
function patchScrollbarGutterSize(themeCSS, scrollbarGutter) {
  const scrollbarGutterRule = createMeasuredScrollbarGutterDeclaration(scrollbarGutter);
  return themeCSS.replace(SCROLLBAR_GUTTER_DECLARATION_PATTERN, scrollbarGutterRule);
}
function escapeRegExp(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getOrCreateCodeNode.js
function getOrCreateCodeNode({ code, pre, columnType, rowSpan, containerSize = false } = {}) {
  if (code == null) {
    code = document.createElement("code");
    code.setAttribute("data-code", "");
    if (columnType != null) code.setAttribute(`data-${columnType}`, "");
    pre == null ? void 0 : pre.appendChild(code);
  }
  if (rowSpan != null) code.style.setProperty("grid-row", `span ${rowSpan}`);
  else code.style.removeProperty("grid-row");
  if (containerSize) code.setAttribute("data-container-size", "");
  else code.removeAttribute("data-container-size");
  return code;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/prerenderHTMLIfNecessary.js
function prerenderHTMLIfNecessary(element2, html5) {
  if (html5 == null) return;
  const shadowRoot = element2.shadowRoot ?? element2.attachShadow({ mode: "open" });
  if (shadowRoot.innerHTML === "") shadowRoot.innerHTML = html5;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/setWrapperNodeProps.js
function setPreNodeProperties(pre, { type, diffIndicators, disableBackground, disableLineNumbers, overflow, split, totalLines, customProperties }) {
  if (customProperties != null) for (const key2 in customProperties) {
    const value = customProperties[key2];
    if (value != null) pre.setAttribute(key2, `${value}`);
  }
  if (type === "diff") {
    pre.setAttribute("data-diff", "");
    pre.removeAttribute("data-file");
  } else {
    pre.setAttribute("data-file", "");
    pre.removeAttribute("data-diff");
  }
  switch (diffIndicators) {
    case "bars":
    case "classic":
      pre.setAttribute("data-indicators", diffIndicators);
      break;
    case "none":
      pre.removeAttribute("data-indicators");
      break;
  }
  if (disableLineNumbers) pre.setAttribute("data-disable-line-numbers", "");
  else pre.removeAttribute("data-disable-line-numbers");
  if (disableBackground) pre.removeAttribute("data-background");
  else pre.setAttribute("data-background", "");
  if (type === "diff") pre.setAttribute("data-diff-type", split ? "split" : "single");
  else pre.removeAttribute("data-diff-type");
  pre.setAttribute("data-overflow", overflow);
  pre.style.setProperty("--diffs-min-number-column-width-default", `${`${totalLines}`.length}ch`);
  return pre;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isStyleNode.js
function isStyleNode(element2) {
  if (typeof HTMLStyleElement !== "undefined" && element2 instanceof HTMLStyleElement) return true;
  const tagName = element2.tagName ?? element2.nodeName;
  return typeof tagName === "string" && tagName.toLowerCase() === "style";
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/shouldUseTokenTransformer.js
function shouldUseTokenTransformer(options) {
  return (options == null ? void 0 : options.useTokenTransformer) === true || (options == null ? void 0 : options.onTokenClick) != null || (options == null ? void 0 : options.onTokenEnter) != null || (options == null ? void 0 : options.onTokenLeave) != null;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getFileRendererOptions.js
function getFileRendererOptions(options) {
  return {
    theme: options == null ? void 0 : options.theme,
    disableLineNumbers: options == null ? void 0 : options.disableLineNumbers,
    overflow: options == null ? void 0 : options.overflow,
    themeType: options == null ? void 0 : options.themeType,
    collapsed: options == null ? void 0 : options.collapsed,
    disableFileHeader: options == null ? void 0 : options.disableFileHeader,
    disableVirtualizationBuffers: options == null ? void 0 : options.disableVirtualizationBuffers,
    stickyHeader: options == null ? void 0 : options.stickyHeader,
    preferredHighlighter: options == null ? void 0 : options.preferredHighlighter,
    useCSSClasses: options == null ? void 0 : options.useCSSClasses,
    useTokenTransformer: shouldUseTokenTransformer(options),
    tokenizeMaxLineLength: options == null ? void 0 : options.tokenizeMaxLineLength,
    tokenizeMaxLength: options == null ? void 0 : options.tokenizeMaxLength,
    unsafeCSS: options == null ? void 0 : options.unsafeCSS,
    headerRenderMode: (options == null ? void 0 : options.renderCustomHeader) != null ? "custom" : "default"
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/guardWebKitScrollDuringRebuild.js
function guardWebKitScrollDuringRebuild(pre, rebuild) {
  const height = (pre == null ? void 0 : pre.offsetHeight) ?? 0;
  if (pre == null || height === 0) {
    rebuild();
    return;
  }
  pre.style.minHeight = `${height}px`;
  try {
    rebuild();
    pre.offsetHeight;
  } finally {
    pre.style.minHeight = "";
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/hostTheme.js
function upsertHostThemeStyle({ shadowRoot, currentNode, themeCSS }) {
  if (themeCSS.trim() === "") {
    currentNode == null ? void 0 : currentNode.remove();
    return;
  }
  currentNode ?? (currentNode = createHostThemeStyleNode());
  currentNode.textContent = themeCSS;
  if (currentNode.parentNode !== shadowRoot) shadowRoot.appendChild(currentNode);
  return currentNode;
}
function createHostThemeStyleNode() {
  const node = document.createElement("style");
  node.setAttribute(THEME_CSS_ATTRIBUTE, "");
  return node;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/platform.js
var _isSafari = void 0;
function isSafari() {
  return _isSafari ?? (_isSafari = "safari" in window && "pushNotification" in window.safari || /^((?!chrome|android).)*safari/i.test(navigator.userAgent));
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/web-components.js
if (typeof HTMLElement !== "undefined" && customElements.get("diffs-container") == null) {
  let sheet;
  class FileDiffContainer extends HTMLElement {
    constructor() {
      super();
      if (this.shadowRoot != null) return;
      const shadowRoot = this.attachShadow({ mode: "open" });
      if (sheet == null) {
        sheet = new CSSStyleSheet();
        sheet.replaceSync(style_default);
      }
      shadowRoot.adoptedStyleSheets = [sheet];
    }
    connectedCallback() {
      getMeasuredScrollbarGutter(this.shadowRoot ?? this.attachShadow({ mode: "open" }));
    }
  }
  customElements.define(DIFFS_TAG_NAME, FileDiffContainer);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/File.js
var EMPTY_STRINGS = [""];
var instanceId2 = -1;
var _a5;
var File = (_a5 = class {
  constructor(options = { theme: DEFAULT_THEMES }, workerManager, isContainerManaged = false) {
    __publicField(this, "options");
    __publicField(this, "workerManager");
    __publicField(this, "isContainerManaged");
    __publicField(this, "__id", `file:${++instanceId2}`);
    __publicField(this, "type", "file");
    __publicField(this, "fileContainer");
    __publicField(this, "spriteSVG");
    __publicField(this, "pre");
    __publicField(this, "code");
    __publicField(this, "bufferBefore");
    __publicField(this, "bufferAfter");
    __publicField(this, "themeCSSStyle");
    __publicField(this, "appliedThemeCSS");
    __publicField(this, "hasAdoptedThemeCSS", false);
    __publicField(this, "unsafeCSSStyle");
    __publicField(this, "appliedUnsafeCSS");
    __publicField(this, "gutterUtilityContent");
    __publicField(this, "errorWrapper");
    __publicField(this, "placeHolder");
    __publicField(this, "lastRenderedHeaderHTML");
    __publicField(this, "cachedHeaderHTML");
    __publicField(this, "appliedPreAttributes");
    __publicField(this, "lastRowCount");
    __publicField(this, "mounted", false);
    __publicField(this, "headerElement");
    __publicField(this, "headerCustom");
    __publicField(this, "headerPrefix");
    __publicField(this, "headerFilenameSuffix");
    __publicField(this, "headerMetadata");
    __publicField(this, "fileRenderer");
    __publicField(this, "resizeManager");
    __publicField(this, "interactionManager");
    __publicField(this, "annotationCache", /* @__PURE__ */ new Map());
    __publicField(this, "lineAnnotations", []);
    __publicField(this, "managersDirty", false);
    __publicField(this, "file");
    __publicField(this, "renderRange");
    __publicField(this, "enabled", true);
    __publicField(this, "editor");
    __publicField(this, "handleHighlightRender", () => {
      this.rerender();
    });
    __publicField(this, "getHoveredLine", () => {
      return this.interactionManager.getHoveredLine();
    });
    var _a9;
    this.options = options;
    this.workerManager = workerManager;
    this.isContainerManaged = isContainerManaged;
    this.fileRenderer = new FileRenderer(options, this.handleHighlightRender, this.workerManager);
    this.resizeManager = new ResizeManager();
    this.interactionManager = new InteractionManager("file", pluckInteractionOptions(options));
    (_a9 = this.workerManager) == null ? void 0 : _a9.subscribeToThemeChanges(this);
  }
  rerender() {
    if (!this.enabled || this.file == null) return;
    this.render({
      file: this.file,
      forceRender: true,
      renderRange: this.renderRange
    });
  }
  __getCurrentFile() {
    return this.file;
  }
  onThemeChange() {
    this.fileRenderer.clearRenderCache();
    this.rerender();
  }
  setOptions(options) {
    if (options == null) return;
    this.options = options;
    this.cachedHeaderHTML = void 0;
    this.syncInteractionOptions();
  }
  syncInteractionOptions() {
    this.interactionManager.setOptions(pluckInteractionOptions(this.options));
  }
  mergeOptions(options) {
    this.options = {
      ...this.options,
      ...options
    };
  }
  setThemeType(themeType) {
    if ((this.options.themeType ?? "system") === themeType) return;
    this.mergeOptions({ themeType });
    this.applyCachedThemeState(themeType);
  }
  applyCachedThemeState(themeType) {
    if (typeof this.options.theme === "string" || this.fileContainer == null || this.appliedThemeCSS == null) return false;
    const effectiveThemeType = this.appliedThemeCSS.baseThemeType ?? themeType;
    if (this.appliedThemeCSS.themeType === effectiveThemeType) return false;
    this.applyThemeState(this.fileContainer, this.appliedThemeCSS.themeStyles, themeType, this.appliedThemeCSS.baseThemeType);
    return true;
  }
  hasThemeChanged() {
    return this.appliedThemeCSS != null && !areThemesEqual(this.appliedThemeCSS.theme, this.options.theme ?? DEFAULT_THEMES);
  }
  setLineAnnotations(lineAnnotations) {
    this.lineAnnotations = lineAnnotations;
  }
  setSelectedLines(range2, options) {
    this.interactionManager.setSelection(range2, options);
  }
  setEditorActiveLine(lineNumber, options) {
    this.interactionManager.setEditorActiveLine(lineNumber, {
      lineNumberOnly: options == null ? void 0 : options.lineNumberOnly,
      side: (options == null ? void 0 : options.side) ?? "additions"
    });
  }
  getCodeScrollLeft() {
    var _a9;
    return ((_a9 = this.code) == null ? void 0 : _a9.scrollLeft) ?? 0;
  }
  setCodeScrollLeft(position) {
    if (this.code != null) this.code.scrollLeft = position;
  }
  __getEffectiveCodeOptions() {
    return {
      ...this.options,
      ...this.fileRenderer.getEffectiveCodeOptions()
    };
  }
  flushManagers() {
    if (!this.managersDirty || this.pre == null) {
      this.managersDirty = false;
      return;
    }
    const { overflow = "scroll" } = this.options;
    this.interactionManager.setup(this.pre);
    this.resizeManager.setup(this.pre, {
      disableAnnotations: overflow === "wrap",
      columnVariables: this.shouldApplyColumnVariables(overflow) ? "apply" : "measure"
    });
    this.managersDirty = false;
  }
  shouldApplyColumnVariables(overflow) {
    return overflow === "scroll" && this.lineAnnotations.length > 0;
  }
  cleanUp(recycle = false) {
    var _a9, _b, _c, _d, _e, _f, _g;
    this.emitPostRender(true);
    (_a9 = this.editor) == null ? void 0 : _a9.cleanUp(recycle);
    this.editor = void 0;
    this.resizeManager.cleanUp();
    this.interactionManager.cleanUp();
    this.managersDirty = false;
    (_b = this.workerManager) == null ? void 0 : _b.unsubscribeToThemeChanges(this);
    this.renderRange = void 0;
    if (!this.isContainerManaged) (_c = this.fileContainer) == null ? void 0 : _c.remove();
    this.fileContainer = void 0;
    this.mounted = false;
    if (!recycle) this.lineAnnotations = [];
    this.clearAuxiliaryNodes();
    this.pre = void 0;
    this.code = void 0;
    (_d = this.bufferBefore) == null ? void 0 : _d.remove();
    this.bufferBefore = void 0;
    (_e = this.bufferAfter) == null ? void 0 : _e.remove();
    this.bufferAfter = void 0;
    this.appliedPreAttributes = void 0;
    this.lastRowCount = void 0;
    this.headerElement = void 0;
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
    this.lastRenderedHeaderHTML = void 0;
    if (!recycle) this.cachedHeaderHTML = void 0;
    (_f = this.errorWrapper) == null ? void 0 : _f.remove();
    this.errorWrapper = void 0;
    this.spriteSVG = void 0;
    this.themeCSSStyle = void 0;
    this.appliedThemeCSS = void 0;
    this.hasAdoptedThemeCSS = false;
    this.unsafeCSSStyle = void 0;
    this.appliedUnsafeCSS = void 0;
    (_g = this.placeHolder) == null ? void 0 : _g.remove();
    this.placeHolder = void 0;
    if (recycle) this.fileRenderer.recycle();
    else {
      this.fileRenderer.cleanUp();
      this.workerManager = void 0;
      this.file = void 0;
    }
    this.enabled = false;
  }
  virtualizedSetup() {
    var _a9;
    this.enabled = true;
    (_a9 = this.workerManager) == null ? void 0 : _a9.subscribeToThemeChanges(this);
  }
  hydrate(props) {
    const { fileContainer, prerenderedHTML, preventEmit = false, file, lineAnnotations } = props;
    if (!this.enabled) throw new Error("File.hydrate: attempting to call hydrate after cleaned up");
    if (this.fileContainer != null) throw new Error("File.hydrate: hydrate can only be called before the instance has rendered or hydrated");
    this.hydrateElements(fileContainer, prerenderedHTML);
    if (shouldRenderCode(this.pre, file, this.options.collapsed) || shouldRenderHeader(this.headerElement, file, this.options.disableFileHeader)) this.render({
      ...props,
      preventEmit: true
    });
    else this.hydrationSetup({
      file,
      lineAnnotations
    });
    if (!preventEmit) this.emitPostRender();
  }
  hydrateElements(fileContainer, prerenderedHTML) {
    var _a9;
    if (this.fileContainer !== fileContainer) this.emitPostRender(true);
    prerenderHTMLIfNecessary(fileContainer, prerenderedHTML);
    for (const element2 of Array.from(((_a9 = fileContainer.shadowRoot) == null ? void 0 : _a9.children) ?? [])) {
      if (element2 instanceof SVGElement) {
        this.spriteSVG = element2;
        continue;
      }
      if (!(element2 instanceof HTMLElement)) continue;
      if (element2 instanceof HTMLPreElement) {
        this.pre = element2;
        this.appliedPreAttributes = void 0;
        continue;
      }
      if (element2 instanceof HTMLStyleElement && element2.hasAttribute("data-theme-css")) {
        this.themeCSSStyle = element2;
        continue;
      }
      if (element2 instanceof HTMLStyleElement && element2.hasAttribute("data-unsafe-css")) {
        this.unsafeCSSStyle = element2;
        this.appliedUnsafeCSS = element2.textContent;
        continue;
      }
      if ("diffsHeader" in element2.dataset) {
        this.headerElement = element2;
        this.lastRenderedHeaderHTML = void 0;
        continue;
      }
    }
    if (this.pre != null) {
      this.syncCodeNodeFromPre(this.pre);
      this.pre.removeAttribute("data-dehydrated");
    }
    this.fileContainer = fileContainer;
    this.hydrateMeasuredScrollbar();
  }
  hydrationSetup({ file, lineAnnotations }) {
    this.lineAnnotations = lineAnnotations ?? this.lineAnnotations;
    this.file = file;
    this.fileRenderer.setOptions(getFileRendererOptions(this.options));
    this.syncInteractionOptions();
    if (this.pre == null) return;
    this.fileRenderer.hydrate(file);
    this.renderAnnotations();
    this.renderGutterUtility();
    this.injectUnsafeCSS();
    this.managersDirty = true;
    this.flushManagers();
  }
  getOrCreateLineCache(file = this.file) {
    return file != null ? this.fileRenderer.getOrCreateLineCache(file) : EMPTY_STRINGS;
  }
  updateBuffers(renderRange) {
    if (this.pre != null) this.applyBuffers(this.pre, renderRange);
  }
  syncRenderViewToEditor() {
    const editor = this.editor;
    const fileContainer = this.fileContainer;
    const file = this.file;
    const lineAnnotations = this.lineAnnotations;
    const renderRange = this.renderRange;
    if (editor != null && fileContainer != null && file != null) this.fileRenderer.initializeHighlighter().then((highlighter2) => {
      if (!this.enabled || this.editor !== editor || this.fileContainer !== fileContainer || this.file !== file) return;
      editor.__syncRenderView(highlighter2, fileContainer, file, lineAnnotations, renderRange);
    });
  }
  attachEditor(editor) {
    var _a9, _b;
    (_a9 = this.editor) == null ? void 0 : _a9.cleanUp();
    this.editor = editor;
    this.fileRenderer.beginEditSession();
    const preparedFile = this.file == null ? void 0 : (_b = editor.__prepareFile) == null ? void 0 : _b.call(editor, this.file);
    if (preparedFile !== void 0 && preparedFile !== this.file) this.renderPreparedFile({
      file: preparedFile,
      forceRender: true,
      preventEmit: true,
      renderRange: this.renderRange
    });
    else if (this.fileRenderer.editorRenderReady()) this.syncRenderViewToEditor();
    else this.rerender();
    return () => {
      this.editor = void 0;
      this.fileRenderer.endEditSession();
    };
  }
  applyDocumentChange(textDocument, newLineAnnotations) {
    this.fileRenderer.applyDocumentChange(textDocument);
    if (newLineAnnotations != null && newLineAnnotations !== this.lineAnnotations && this.file != null) {
      this.setLineAnnotations(newLineAnnotations);
      this.fileRenderer.setLineAnnotations(this.lineAnnotations);
      this.renderAnnotations();
    }
  }
  updateRenderCache(dirtyLines, themeType, options) {
    this.fileRenderer.updateRenderCache(dirtyLines, themeType, options == null ? void 0 : options.lineCountChangeInFlight);
  }
  render(props) {
    var _a9, _b;
    if (!this.enabled) throw new Error("File.render: attempting to call render after cleaned up");
    const file = ((_b = (_a9 = this.editor) == null ? void 0 : _a9.__prepareFile) == null ? void 0 : _b.call(_a9, props.file)) ?? props.file;
    return this.renderPreparedFile(file === props.file ? props : {
      ...props,
      file
    });
  }
  renderPreparedFile({ file, fileContainer, forceRender = false, preventEmit = false, containerWrapper, deferManagers = false, lineAnnotations, renderRange }) {
    var _a9, _b;
    (_a9 = this.editor) == null ? void 0 : _a9.__postponeBgTokenizeToNextFrame();
    const { collapsed = false, themeType = "system" } = this.options;
    const nextRenderRange = collapsed ? void 0 : renderRange;
    const previousRenderRange = this.renderRange;
    const themeChanged = this.hasThemeChanged();
    const annotationsChanged = lineAnnotations != null && (lineAnnotations.length > 0 || this.lineAnnotations.length > 0) ? lineAnnotations !== this.lineAnnotations : false;
    const didFileChange = !areFilesEqual(this.file, file) || this.fileRenderer.hasUnkeyedFileContentsChanged(file);
    if (!collapsed && !forceRender && areRenderRangesEqual(nextRenderRange, this.renderRange) && !didFileChange && !annotationsChanged && !themeChanged) return this.applyCachedThemeState(themeType);
    this.renderRange = nextRenderRange;
    if (didFileChange) this.cachedHeaderHTML = void 0;
    this.file = file;
    this.fileRenderer.setOptions(getFileRendererOptions(this.options));
    this.syncInteractionOptions();
    if (lineAnnotations != null) this.setLineAnnotations(lineAnnotations);
    this.fileRenderer.setLineAnnotations(this.lineAnnotations);
    const { disableErrorHandling = false, disableFileHeader = false } = this.options;
    if (disableFileHeader) {
      if (this.headerElement != null) {
        this.headerElement.remove();
        this.headerElement = void 0;
        this.lastRenderedHeaderHTML = void 0;
      }
      this.clearHeaderSlots();
    }
    fileContainer = this.getOrCreateFileContainerNode(fileContainer, containerWrapper);
    this.applyCachedThemeState(themeType);
    if (collapsed) {
      this.removeRenderedCode();
      this.clearAuxiliaryNodes();
      try {
        const fileResult = this.fileRenderer.renderFile(file, EMPTY_RENDER_RANGE);
        if (fileResult != null) this.applyThemeState(fileContainer, fileResult.themeStyles, themeType, fileResult.baseThemeType);
        if ((fileResult == null ? void 0 : fileResult.headerAST) != null) this.applyHeaderToDOM(fileResult.headerAST, fileContainer);
        this.injectUnsafeCSS();
      } catch (error) {
        if (disableErrorHandling) throw error;
        console.error(error);
        if (error instanceof Error) this.applyErrorToDOM(error, fileContainer);
      }
      if (!preventEmit) this.emitPostRender();
      return true;
    }
    try {
      const pre = this.getOrCreatePreNode(fileContainer);
      if (!this.canPartiallyRender(forceRender, annotationsChanged, didFileChange || themeChanged) || !this.applyPartialRender(previousRenderRange, nextRenderRange)) {
        const fileResult = this.fileRenderer.renderFile(file, nextRenderRange);
        if (fileResult == null) {
          if (((_b = this.workerManager) == null ? void 0 : _b.isInitialized()) === false) this.workerManager.initialize().then(() => this.rerender());
          return false;
        }
        this.applyThemeState(fileContainer, fileResult.themeStyles, themeType, fileResult.baseThemeType);
        if (fileResult.headerAST != null) this.applyHeaderToDOM(fileResult.headerAST, fileContainer);
        this.applyFullRender(fileResult, pre);
      }
      this.applyBuffers(pre, nextRenderRange);
      this.injectUnsafeCSS();
      this.renderAnnotations();
      this.renderGutterUtility();
      this.managersDirty = true;
      if (!deferManagers) this.flushManagers();
      if (this.editor != null) this.syncRenderViewToEditor();
    } catch (error) {
      if (disableErrorHandling) throw error;
      console.error(error);
      if (error instanceof Error) this.applyErrorToDOM(error, fileContainer);
    }
    if (!preventEmit) this.emitPostRender();
    return true;
  }
  emitPostRender(unmount = false) {
    const { fileContainer, options: { onPostRender } } = this;
    if (unmount) {
      if (!this.mounted) return;
      this.mounted = false;
      if (fileContainer == null) return;
      onPostRender == null ? void 0 : onPostRender(fileContainer, this, "unmount");
      return;
    }
    if (fileContainer == null) return;
    const phase = this.mounted ? "update" : "mount";
    this.mounted = true;
    onPostRender == null ? void 0 : onPostRender(fileContainer, this, phase);
  }
  removeRenderedCode() {
    var _a9, _b, _c, _d;
    this.resizeManager.cleanUp();
    this.interactionManager.cleanUp();
    (_a9 = this.bufferBefore) == null ? void 0 : _a9.remove();
    this.bufferBefore = void 0;
    (_b = this.bufferAfter) == null ? void 0 : _b.remove();
    this.bufferAfter = void 0;
    (_c = this.code) == null ? void 0 : _c.remove();
    this.code = void 0;
    (_d = this.pre) == null ? void 0 : _d.remove();
    this.pre = void 0;
    this.appliedPreAttributes = void 0;
    this.lastRowCount = void 0;
  }
  clearAuxiliaryNodes() {
    var _a9;
    for (const { element: element2 } of this.annotationCache.values()) element2.remove();
    this.annotationCache.clear();
    (_a9 = this.gutterUtilityContent) == null ? void 0 : _a9.remove();
    this.gutterUtilityContent = void 0;
  }
  canPartiallyRender(forceRender, annotationsChanged, didContentChange) {
    if (forceRender || annotationsChanged || didContentChange) return false;
    return true;
  }
  renderPlaceholder(height) {
    if (this.fileContainer == null) return false;
    this.emitPostRender(true);
    this.cleanChildNodes();
    if (this.placeHolder == null) {
      const shadowRoot = this.fileContainer.shadowRoot ?? this.fileContainer.attachShadow({ mode: "open" });
      this.placeHolder = document.createElement("div");
      this.placeHolder.dataset.placeholder = "";
      shadowRoot.appendChild(this.placeHolder);
    }
    this.placeHolder.style.setProperty("height", `${height}px`);
    return true;
  }
  async primeHighlightCache(file = this.file) {
    const { workerManager } = this;
    if (file == null || workerManager == null || !workerManager.isWorkingPool() || file.cacheKey == null || isFilePlainText(file)) return;
    const tokenizeMaxLength = this.options.tokenizeMaxLength ?? 1e5;
    if (this.fileRenderer.getOrCreateLineCache(file).length > tokenizeMaxLength) return;
    await workerManager.primeFileHighlightCache(file).catch((error) => {
      console.error(error);
    });
  }
  cleanChildNodes() {
    var _a9, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
    this.resizeManager.cleanUp();
    this.interactionManager.cleanUp();
    this.clearAuxiliaryNodes();
    (_a9 = this.bufferAfter) == null ? void 0 : _a9.remove();
    (_b = this.bufferBefore) == null ? void 0 : _b.remove();
    (_c = this.code) == null ? void 0 : _c.remove();
    (_d = this.errorWrapper) == null ? void 0 : _d.remove();
    (_e = this.headerElement) == null ? void 0 : _e.remove();
    (_f = this.headerPrefix) == null ? void 0 : _f.remove();
    (_g = this.headerFilenameSuffix) == null ? void 0 : _g.remove();
    (_h = this.headerMetadata) == null ? void 0 : _h.remove();
    (_i = this.headerCustom) == null ? void 0 : _i.remove();
    (_j = this.pre) == null ? void 0 : _j.remove();
    (_k = this.spriteSVG) == null ? void 0 : _k.remove();
    (_l = this.themeCSSStyle) == null ? void 0 : _l.remove();
    (_m = this.unsafeCSSStyle) == null ? void 0 : _m.remove();
    this.bufferAfter = void 0;
    this.bufferBefore = void 0;
    this.code = void 0;
    this.errorWrapper = void 0;
    this.headerElement = void 0;
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
    this.pre = void 0;
    this.spriteSVG = void 0;
    this.themeCSSStyle = void 0;
    this.appliedThemeCSS = void 0;
    this.hasAdoptedThemeCSS = false;
    this.unsafeCSSStyle = void 0;
    this.appliedUnsafeCSS = void 0;
    this.lastRenderedHeaderHTML = void 0;
    this.lastRowCount = void 0;
    this.mounted = false;
  }
  renderAnnotations() {
    if (this.isContainerManaged || this.fileContainer == null) {
      for (const { element: element2 } of this.annotationCache.values()) element2.remove();
      this.annotationCache.clear();
      return;
    }
    const staleAnnotations = new Map(this.annotationCache);
    const { renderAnnotation } = this.options;
    if (renderAnnotation != null && this.lineAnnotations.length > 0) for (const [index, annotation] of this.lineAnnotations.entries()) {
      const id = `${index}-${getLineAnnotationName(annotation)}`;
      let cache2 = this.annotationCache.get(id);
      if (cache2 == null || !areLineAnnotationsEqual(annotation, cache2.annotation)) {
        cache2 == null ? void 0 : cache2.element.remove();
        const content = renderAnnotation(annotation);
        if (content == null) continue;
        cache2 = {
          element: createAnnotationWrapperNode(getLineAnnotationName(annotation)),
          annotation
        };
        cache2.element.appendChild(content);
        this.fileContainer.appendChild(cache2.element);
        this.annotationCache.set(id, cache2);
      }
      staleAnnotations.delete(id);
    }
    for (const [id, { element: element2 }] of staleAnnotations.entries()) {
      this.annotationCache.delete(id);
      element2.remove();
    }
  }
  renderGutterUtility() {
    var _a9, _b;
    const { renderGutterUtility } = this.options;
    if (this.fileContainer == null || renderGutterUtility == null) {
      (_a9 = this.gutterUtilityContent) == null ? void 0 : _a9.remove();
      this.gutterUtilityContent = void 0;
      return;
    }
    const element2 = renderGutterUtility(this.interactionManager.getHoveredLine);
    if (element2 != null && this.gutterUtilityContent != null) return;
    else if (element2 == null) {
      (_b = this.gutterUtilityContent) == null ? void 0 : _b.remove();
      this.gutterUtilityContent = void 0;
      return;
    }
    const gutterUtilityContent = createGutterUtilityContentNode();
    gutterUtilityContent.appendChild(element2);
    this.fileContainer.appendChild(gutterUtilityContent);
    this.gutterUtilityContent = gutterUtilityContent;
  }
  injectUnsafeCSS() {
    var _a9, _b;
    const { unsafeCSS } = this.options;
    const shadowRoot = (_a9 = this.fileContainer) == null ? void 0 : _a9.shadowRoot;
    if (shadowRoot == null) return;
    if (unsafeCSS == null || unsafeCSS === "") {
      if (this.unsafeCSSStyle != null) {
        this.unsafeCSSStyle.remove();
        this.unsafeCSSStyle = void 0;
      }
      this.appliedUnsafeCSS = void 0;
      return;
    }
    if (((_b = this.unsafeCSSStyle) == null ? void 0 : _b.parentNode) === shadowRoot && this.appliedUnsafeCSS === unsafeCSS) return;
    this.unsafeCSSStyle ?? (this.unsafeCSSStyle = createUnsafeCSSStyleNode());
    if (this.unsafeCSSStyle.parentNode !== shadowRoot) shadowRoot.appendChild(this.unsafeCSSStyle);
    this.unsafeCSSStyle.textContent = wrapUnsafeCSS(unsafeCSS);
    this.appliedUnsafeCSS = unsafeCSS;
  }
  applyThemeState(container, themeStyles, themeType, baseThemeType) {
    var _a9, _b, _c;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    const effectiveThemeType = baseThemeType ?? themeType;
    const currentTheme = this.options.theme ?? DEFAULT_THEMES;
    const theme = typeof currentTheme === "string" ? currentTheme : { ...currentTheme };
    const scrollbarGutter = getMeasuredScrollbarGutter(shadowRoot);
    if (((_a9 = this.themeCSSStyle) == null ? void 0 : _a9.parentNode) === shadowRoot && ((_b = this.appliedThemeCSS) == null ? void 0 : _b.themeStyles) === themeStyles && this.appliedThemeCSS.themeType === effectiveThemeType && this.appliedThemeCSS.scrollbarGutter === scrollbarGutter) {
      this.appliedThemeCSS.theme = theme;
      return;
    }
    if (this.hasAdoptedThemeCSS && ((_c = this.themeCSSStyle) == null ? void 0 : _c.parentNode) === shadowRoot) {
      this.hasAdoptedThemeCSS = false;
      this.appliedThemeCSS = {
        theme,
        themeStyles,
        themeType: effectiveThemeType,
        baseThemeType,
        scrollbarGutter
      };
      return;
    }
    this.themeCSSStyle = upsertHostThemeStyle({
      shadowRoot,
      currentNode: this.themeCSSStyle,
      themeCSS: wrapThemeCSS(themeStyles, effectiveThemeType, scrollbarGutter)
    });
    this.appliedThemeCSS = this.themeCSSStyle != null ? {
      theme,
      themeStyles,
      themeType: effectiveThemeType,
      baseThemeType,
      scrollbarGutter
    } : void 0;
  }
  hydrateMeasuredScrollbar() {
    var _a9;
    const shadowRoot = (_a9 = this.fileContainer) == null ? void 0 : _a9.shadowRoot;
    if (shadowRoot == null || this.themeCSSStyle == null) return;
    this.themeCSSStyle.textContent = patchScrollbarGutterSize(this.themeCSSStyle.textContent ?? "", getMeasuredScrollbarGutter(shadowRoot));
  }
  shouldGuardRebuildScroll() {
    return this.editor != null && isSafari();
  }
  applyFullRender(result, pre) {
    var _a9;
    this.cleanupErrorWrapper();
    this.applyPreNodeAttributes(pre, result);
    const code = this.code = getOrCreateCodeNode({ code: this.code });
    const codeAst = this.fileRenderer.renderCodeAST(result);
    (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
    const applyColumns = () => {
      if (code.childElementCount >= 2) for (let i2 = 0; i2 < 2; i2++) {
        const domEl = code.children[i2];
        const astEl = codeAst[i2];
        domEl.innerHTML = toHtml(astEl.children);
        domEl.style.cssText = astEl.properties.style;
      }
      else code.innerHTML = toHtml(codeAst);
      if (!pre.contains(code)) pre.replaceChildren(code);
    };
    if (this.shouldGuardRebuildScroll()) guardWebKitScrollDuringRebuild(pre, applyColumns);
    else applyColumns();
    this.lastRowCount = result.rowCount;
  }
  applyPartialRender(previousRenderRange, renderRange) {
    if (previousRenderRange == null || renderRange == null) return false;
    const { file, code } = this;
    const columns = code != null ? this.getColumns(code) : void 0;
    if (file == null || code == null || columns == null) return false;
    const previousStart = previousRenderRange.startingLine;
    const nextStart = renderRange.startingLine;
    const previousEnd = previousRenderRange.totalLines === Infinity ? Number.POSITIVE_INFINITY : previousStart + previousRenderRange.totalLines;
    const nextEnd = renderRange.totalLines === Infinity ? Number.POSITIVE_INFINITY : nextStart + renderRange.totalLines;
    const overlapStart = Math.max(previousStart, nextStart);
    const overlapEnd = Math.min(previousEnd, nextEnd);
    if (overlapEnd <= overlapStart) return false;
    if (!this.trimDOMToOverlap(columns.gutter, overlapStart, overlapEnd) || !this.trimDOMToOverlap(columns.content, overlapStart, overlapEnd)) throw new Error("File.applyPartialRender: failed to trim to overlap");
    let { length: rowCount } = columns.content.children;
    const renderChunk = (startingLine, totalLines) => {
      if (totalLines <= 0) return;
      return this.fileRenderer.renderFile(file, {
        startingLine,
        totalLines,
        bufferBefore: 0,
        bufferAfter: 0
      });
    };
    const prependResult = nextStart < overlapStart ? renderChunk(nextStart, overlapStart - nextStart) : void 0;
    if (prependResult === void 0 && nextStart < overlapStart) return false;
    const appendTotalLines = nextEnd === Number.POSITIVE_INFINITY ? Number.POSITIVE_INFINITY : Math.max(0, nextEnd - overlapEnd);
    const appendResult = nextEnd > overlapEnd ? renderChunk(overlapEnd, appendTotalLines) : void 0;
    if (appendResult === void 0 && nextEnd > overlapEnd) return false;
    this.cleanupErrorWrapper();
    if (prependResult != null) {
      columns.gutter.insertAdjacentHTML("afterbegin", this.fileRenderer.renderPartialHTML(prependResult.gutterAST));
      columns.content.insertAdjacentHTML("afterbegin", this.fileRenderer.renderPartialHTML(prependResult.contentAST));
      rowCount += prependResult.rowCount;
    }
    if (appendResult != null) {
      columns.gutter.insertAdjacentHTML("beforeend", this.fileRenderer.renderPartialHTML(appendResult.gutterAST));
      columns.content.insertAdjacentHTML("beforeend", this.fileRenderer.renderPartialHTML(appendResult.contentAST));
      rowCount += appendResult.rowCount;
    }
    if (this.lastRowCount !== rowCount) {
      columns.gutter.style.setProperty("grid-row", `span ${rowCount}`);
      columns.content.style.setProperty("grid-row", `span ${rowCount}`);
      this.lastRowCount = rowCount;
    }
    return true;
  }
  getColumns(code) {
    const gutter = code.children[0];
    const content = code.children[1];
    if (!(gutter instanceof HTMLElement) || !(content instanceof HTMLElement) || gutter.dataset.gutter == null || content.dataset.content == null) return;
    return {
      gutter,
      content
    };
  }
  trimDOMToOverlap(container, overlapStart, overlapEnd) {
    var _a9, _b;
    const boundaryIndices = this.getDOMBoundaryIndices(container, [overlapStart, overlapEnd]);
    const startIndex = boundaryIndices.get(overlapStart) ?? container.children.length;
    const endIndex = boundaryIndices.get(overlapEnd) ?? container.children.length;
    if (startIndex > endIndex) return false;
    for (let i2 = container.children.length - 1; i2 >= endIndex; i2 -= 1) (_a9 = container.children[i2]) == null ? void 0 : _a9.remove();
    for (let i2 = startIndex - 1; i2 >= 0; i2 -= 1) (_b = container.children[i2]) == null ? void 0 : _b.remove();
    return true;
  }
  getDOMBoundaryIndices(container, boundaries) {
    const sortedBoundaries = [...new Set(boundaries)].sort((a2, b3) => a2 - b3);
    const boundaryIndices = /* @__PURE__ */ new Map();
    if (sortedBoundaries.length === 0) return boundaryIndices;
    let boundaryIndex = 0;
    let nextBoundary = sortedBoundaries[boundaryIndex];
    const { children } = container;
    if (nextBoundary === 0) {
      boundaryIndices.set(0, 0);
      boundaryIndex += 1;
      nextBoundary = sortedBoundaries[boundaryIndex];
    }
    for (let i2 = 0; i2 < children.length; i2 += 1) {
      const child = children[i2];
      if (!(child instanceof HTMLElement)) continue;
      const lineIndex = this.getLineIndexFromDOMNode(child);
      if (lineIndex == null) continue;
      while (nextBoundary != null && lineIndex >= nextBoundary) {
        boundaryIndices.set(nextBoundary, i2);
        boundaryIndex += 1;
        nextBoundary = sortedBoundaries[boundaryIndex];
      }
      if (boundaryIndex >= sortedBoundaries.length) break;
    }
    for (const boundary of sortedBoundaries) if (!boundaryIndices.has(boundary)) boundaryIndices.set(boundary, children.length);
    return boundaryIndices;
  }
  getLineIndexFromDOMNode(node) {
    const lineIndexAttr = node.dataset.lineIndex;
    if (lineIndexAttr == null) return;
    const parsed = Number(lineIndexAttr);
    return Number.isNaN(parsed) ? void 0 : parsed;
  }
  applyBuffers(pre, renderRange) {
    if (renderRange == null || this.shouldDisableVirtualizationBuffers()) {
      if (this.bufferBefore != null) {
        this.bufferBefore.remove();
        this.bufferBefore = void 0;
      }
      if (this.bufferAfter != null) {
        this.bufferAfter.remove();
        this.bufferAfter = void 0;
      }
      return;
    }
    if (renderRange.bufferBefore > 0) {
      if (this.bufferBefore == null) {
        this.bufferBefore = document.createElement("div");
        this.bufferBefore.dataset.virtualizerBuffer = "before";
        pre.before(this.bufferBefore);
      }
      this.bufferBefore.style.setProperty("height", `${renderRange.bufferBefore}px`);
      this.bufferBefore.style.setProperty("contain", "strict");
    } else if (this.bufferBefore != null) {
      this.bufferBefore.remove();
      this.bufferBefore = void 0;
    }
    if (renderRange.bufferAfter > 0) {
      if (this.bufferAfter == null) {
        this.bufferAfter = document.createElement("div");
        this.bufferAfter.dataset.virtualizerBuffer = "after";
        pre.after(this.bufferAfter);
      }
      this.bufferAfter.style.setProperty("height", `${renderRange.bufferAfter}px`);
      this.bufferAfter.style.setProperty("contain", "strict");
    } else if (this.bufferAfter != null) {
      this.bufferAfter.remove();
      this.bufferAfter = void 0;
    }
  }
  shouldDisableVirtualizationBuffers() {
    return this.options.disableVirtualizationBuffers ?? false;
  }
  applyHeaderToDOM(headerAST, container) {
    var _a9, _b, _c, _d, _e, _f, _g;
    const { file } = this;
    if (file == null) return;
    this.cleanupErrorWrapper();
    (_a9 = this.placeHolder) == null ? void 0 : _a9.remove();
    this.placeHolder = void 0;
    const headerHTML = this.cachedHeaderHTML ?? toHtml(headerAST);
    this.cachedHeaderHTML = headerHTML;
    if (headerHTML !== this.lastRenderedHeaderHTML) {
      const tempDiv = document.createElement("div");
      tempDiv.innerHTML = headerHTML;
      const newHeader = tempDiv.firstElementChild;
      if (!(newHeader instanceof HTMLElement)) return;
      if (this.headerElement != null) (_b = container.shadowRoot) == null ? void 0 : _b.replaceChild(newHeader, this.headerElement);
      else (_c = container.shadowRoot) == null ? void 0 : _c.prepend(newHeader);
      this.headerElement = newHeader;
      this.lastRenderedHeaderHTML = headerHTML;
    }
    if (this.isContainerManaged) return;
    const { renderHeaderPrefix, renderHeaderFilenameSuffix, renderCustomHeader, renderHeaderMetadata } = this.options;
    if (renderCustomHeader != null) {
      const content = renderCustomHeader(file) ?? void 0;
      this.headerCustom = this.upsertHeaderSlotElement(container, this.headerCustom, CUSTOM_HEADER_SLOT_ID, content);
      (_d = this.headerPrefix) == null ? void 0 : _d.remove();
      (_e = this.headerFilenameSuffix) == null ? void 0 : _e.remove();
      (_f = this.headerMetadata) == null ? void 0 : _f.remove();
      this.headerPrefix = void 0;
      this.headerFilenameSuffix = void 0;
      this.headerMetadata = void 0;
    } else {
      const prefix = (renderHeaderPrefix == null ? void 0 : renderHeaderPrefix(file)) ?? void 0;
      const suffix = (renderHeaderFilenameSuffix == null ? void 0 : renderHeaderFilenameSuffix(file)) ?? void 0;
      const content = (renderHeaderMetadata == null ? void 0 : renderHeaderMetadata(file)) ?? void 0;
      this.headerPrefix = this.upsertHeaderSlotElement(container, this.headerPrefix, HEADER_PREFIX_SLOT_ID, prefix);
      this.headerFilenameSuffix = this.upsertHeaderSlotElement(container, this.headerFilenameSuffix, HEADER_FILENAME_SUFFIX_SLOT_ID, suffix);
      this.headerMetadata = this.upsertHeaderSlotElement(container, this.headerMetadata, HEADER_METADATA_SLOT_ID, content);
      (_g = this.headerCustom) == null ? void 0 : _g.remove();
      this.headerCustom = void 0;
    }
  }
  clearHeaderSlots() {
    var _a9, _b, _c, _d;
    (_a9 = this.headerPrefix) == null ? void 0 : _a9.remove();
    (_b = this.headerFilenameSuffix) == null ? void 0 : _b.remove();
    (_c = this.headerMetadata) == null ? void 0 : _c.remove();
    (_d = this.headerCustom) == null ? void 0 : _d.remove();
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
  }
  upsertHeaderSlotElement(container, current, slot, content) {
    if (content == null) {
      current == null ? void 0 : current.remove();
      return;
    }
    const element2 = current ?? this.createHeaderSlotElement(slot);
    if (current == null) container.appendChild(element2);
    this.replaceHeaderSlotContent(element2, content);
    return element2;
  }
  replaceHeaderSlotContent(element2, content) {
    element2.replaceChildren();
    if (content instanceof Element) element2.appendChild(content);
    else element2.innerText = `${content}`;
  }
  createHeaderSlotElement(slot) {
    const element2 = document.createElement("div");
    element2.slot = slot;
    return element2;
  }
  getOrCreateFileContainerNode(fileContainer, parentNode) {
    var _a9;
    const { fileContainer: previousContainer } = this;
    const nextContainer = fileContainer ?? previousContainer ?? document.createElement("diffs-container");
    const containerChanged = previousContainer !== nextContainer;
    if (previousContainer != null && containerChanged) (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
    if (containerChanged) this.emitPostRender(true);
    this.fileContainer = nextContainer;
    if (previousContainer != null && containerChanged) {
      this.lastRenderedHeaderHTML = void 0;
      this.headerElement = void 0;
    }
    if (parentNode != null && this.fileContainer.parentNode !== parentNode) parentNode.appendChild(this.fileContainer);
    if (containerChanged) this.adoptReusableShellElements(this.fileContainer);
    this.ensureSpriteSVG(this.fileContainer);
    return this.fileContainer;
  }
  adoptReusableShellElements(fileContainer) {
    const { shadowRoot } = fileContainer;
    if (shadowRoot == null) return;
    for (const element2 of shadowRoot.children) if (element2 instanceof SVGElement) this.spriteSVG ?? (this.spriteSVG = element2);
    else if (isStyleNode(element2) && element2.hasAttribute("data-theme-css")) {
      this.themeCSSStyle ?? (this.themeCSSStyle = element2);
      this.hasAdoptedThemeCSS = true;
    } else if (isStyleNode(element2) && element2.hasAttribute("data-unsafe-css")) {
      this.unsafeCSSStyle ?? (this.unsafeCSSStyle = element2);
      this.appliedUnsafeCSS ?? (this.appliedUnsafeCSS = this.options.unsafeCSS ?? void 0);
    }
  }
  ensureSpriteSVG(fileContainer) {
    const shadowRoot = fileContainer.shadowRoot ?? fileContainer.attachShadow({ mode: "open" });
    if (this.spriteSVG == null) {
      const fragment = document.createElement("div");
      fragment.innerHTML = SVGSpriteSheet;
      const firstChild = fragment.firstChild;
      if (firstChild instanceof SVGElement) this.spriteSVG = firstChild;
    }
    if (this.spriteSVG != null && this.spriteSVG.parentNode !== shadowRoot) shadowRoot.appendChild(this.spriteSVG);
  }
  getOrCreatePreNode(container) {
    var _a9, _b, _c;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    if (this.pre == null) {
      this.pre = document.createElement("pre");
      this.appliedPreAttributes = void 0;
      this.code = void 0;
      shadowRoot.appendChild(this.pre);
    } else if (this.pre.parentNode !== shadowRoot) {
      (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
      (_b = container.shadowRoot) == null ? void 0 : _b.appendChild(this.pre);
      this.appliedPreAttributes = void 0;
    }
    (_c = this.placeHolder) == null ? void 0 : _c.remove();
    this.placeHolder = void 0;
    return this.pre;
  }
  syncCodeNodeFromPre(pre) {
    this.code = void 0;
    for (const child of Array.from(pre.children)) {
      if (!(child instanceof HTMLElement)) continue;
      if (child.hasAttribute("data-code")) {
        this.code = child;
        return;
      }
    }
  }
  applyPreNodeAttributes(pre, { totalLines }) {
    const { overflow = "scroll", disableLineNumbers = false } = this.options;
    const preProperties = {
      type: "file",
      split: false,
      overflow,
      disableLineNumbers,
      diffIndicators: "none",
      disableBackground: true,
      totalLines
    };
    if (arePrePropertiesEqual(preProperties, this.appliedPreAttributes)) return;
    setPreNodeProperties(pre, preProperties);
    this.appliedPreAttributes = preProperties;
  }
  applyErrorToDOM(error, container) {
    var _a9;
    this.cleanupErrorWrapper();
    (_a9 = this.pre) == null ? void 0 : _a9.remove();
    this.pre = void 0;
    this.appliedPreAttributes = void 0;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    this.errorWrapper ?? (this.errorWrapper = document.createElement("div"));
    this.errorWrapper.dataset.errorWrapper = "";
    this.errorWrapper.textContent = "";
    shadowRoot.appendChild(this.errorWrapper);
    const errorMessage = document.createElement("div");
    errorMessage.dataset.errorMessage = "";
    errorMessage.innerText = error.message;
    this.errorWrapper.appendChild(errorMessage);
    const errorStack = document.createElement("pre");
    errorStack.dataset.errorStack = "";
    errorStack.innerText = error.stack ?? "No Error Stack";
    this.errorWrapper.appendChild(errorStack);
  }
  cleanupErrorWrapper() {
    var _a9;
    (_a9 = this.errorWrapper) == null ? void 0 : _a9.remove();
    this.errorWrapper = void 0;
  }
}, __publicField(_a5, "LoadedCustomComponent", true), _a5);
function shouldRenderCode(pre, file, collapsed = false) {
  return !collapsed && pre == null && file != null;
}
function shouldRenderHeader(headerElement, file, disableFileHeader = false) {
  return headerElement == null && file != null && !disableFileHeader;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/computeVirtualFileMetrics.js
function computeVirtualFileMetrics(metrics) {
  return {
    ...DEFAULT_VIRTUAL_FILE_METRICS,
    ...metrics
  };
}
function getVirtualFileHeaderRegion(metrics, disableFileHeader) {
  const paddingTop = getVirtualFilePaddingTop(metrics, disableFileHeader);
  return disableFileHeader ? paddingTop : metrics.diffHeaderHeight + paddingTop;
}
function getVirtualFilePaddingTop(metrics, disableFileHeader) {
  return metrics.paddingTop ?? (disableFileHeader ? metrics.spacing : 0);
}
function getVirtualFilePaddingBottom(metrics) {
  return metrics.paddingBottom ?? metrics.spacing;
}
function getDefaultHunkSeparatorHeight(type) {
  switch (type) {
    case "simple":
      return 4;
    case "metadata":
    case "line-info":
    case "line-info-basic":
    case "custom":
      return 32;
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/VirtualizedFile.js
var LAYOUT_CHECKPOINT_INTERVAL = 5e3;
var instanceId3 = -1;
function hasFileLayoutOptionChanged(previousOptions, nextOptions) {
  return (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.collapsed ?? false) !== (nextOptions.collapsed ?? false) || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS;
}
var VirtualizedFile = class extends File {
  constructor(options, virtualizer, metrics = DEFAULT_VIRTUAL_FILE_METRICS, workerManager, isContainerManaged = false) {
    super(options, workerManager, isContainerManaged);
    __publicField(this, "virtualizer");
    __publicField(this, "metrics");
    __publicField(this, "__id", `virtualized-file:${++instanceId3}`);
    __publicField(this, "top");
    __publicField(this, "height", 0);
    __publicField(this, "cache", {
      heights: /* @__PURE__ */ new Map(),
      checkpoints: [],
      fileAnnotationHeight: 0
    });
    __publicField(this, "isVisible", false);
    __publicField(this, "isSetup", false);
    __publicField(this, "layoutDirty", true);
    __publicField(this, "forceRenderOverride");
    __publicField(this, "currentCollapsed");
    __publicField(this, "onRender", (dirty) => {
      if (this.fileContainer == null || this.file == null) return false;
      if (dirty) this.top = this.getVirtualizedTop();
      return this.render({ file: this.file });
    });
    this.virtualizer = virtualizer;
    this.metrics = metrics;
  }
  setMetrics(metrics, force = false) {
    const nextMetrics = computeVirtualFileMetrics(metrics);
    if (!force && areObjectsEqual(this.metrics, nextMetrics)) return;
    this.metrics = nextMetrics;
    this.resetLayoutCache();
  }
  setLineAnnotations(lineAnnotations) {
    if (this.syncLineAnnotations(lineAnnotations)) this.resetLayoutCache();
  }
  syncLineAnnotations(lineAnnotations) {
    if (lineAnnotations == null || lineAnnotations === this.lineAnnotations) return false;
    if (lineAnnotations.length === 0 && this.lineAnnotations.length === 0) return false;
    super.setLineAnnotations(lineAnnotations);
    return true;
  }
  hasLineAnnotations() {
    return this.lineAnnotations.some((annotation) => annotation.lineNumber > 0);
  }
  getLineHeight(lineIndex, hasMetadataLine = false) {
    const cached = this.cache.heights.get(lineIndex);
    if (cached != null) return cached;
    const multiplier = hasMetadataLine ? 2 : 1;
    return this.metrics.lineHeight * multiplier;
  }
  setOptions(options) {
    if (this.isAdvancedMode()) throw new Error("VirtualizedFile.setOptions cannot be used inside CodeView. Update CodeView options instead.");
    if (options == null) return;
    const { options: previousOptions } = this;
    const optionsChanged = !areOptionsEqual(previousOptions, options);
    const layoutChanged = hasFileLayoutOptionChanged(previousOptions, options);
    super.setOptions(options);
    if (layoutChanged) this.resetLayoutCache(true);
    if (optionsChanged) this.forceRenderOverride = true;
    if (optionsChanged) this.virtualizer.instanceChanged(this, layoutChanged);
  }
  setThemeType(themeType) {
    if (this.isAdvancedMode()) throw new Error("VirtualizedFile.setThemeType cannot be used inside CodeView. Update CodeView options instead.");
    super.setThemeType(themeType);
  }
  resetLayoutCache(recompute = false, resetRenderRange = true) {
    this.layoutDirty = true;
    this.cache.fileAnnotationHeight = 0;
    if (this.cache.heights.size > 0) this.cache.heights.clear();
    if (this.cache.checkpoints.length > 0) this.cache.checkpoints.length = 0;
    if (this.renderRange != null && resetRenderRange) this.renderRange = void 0;
    if (recompute && this.isSimpleMode()) this.computeApproximateSize();
  }
  reconcileHeights() {
    let hasHeightChange = false;
    if (this.fileContainer == null || this.file == null) {
      if (this.height !== 0) hasHeightChange = true;
      this.height = 0;
      return hasHeightChange;
    }
    const { overflow = "scroll" } = this.options;
    this.top = this.getVirtualizedTop();
    if (overflow === "scroll" && this.lineAnnotations.length === 0 && !this.isResizeDebuggingEnabled()) return hasHeightChange;
    if (this.code == null) return hasHeightChange;
    const content = this.code.children[1];
    if (!(content instanceof HTMLElement)) return hasHeightChange;
    const hasFileAnnotations = includesFileAnnotations(this.lineAnnotations);
    if (this.renderRange != null && hasFileAnnotations && shouldRenderFileAnnotations(this.renderRange)) {
      const nextFileAnnotationHeight = measureFileAnnotationHeight(content) ?? 0;
      if (nextFileAnnotationHeight !== this.cache.fileAnnotationHeight) {
        this.cache.fileAnnotationHeight = nextFileAnnotationHeight;
        hasHeightChange = true;
      }
    } else if (!hasFileAnnotations && this.cache.fileAnnotationHeight !== 0) {
      this.cache.fileAnnotationHeight = 0;
      hasHeightChange = true;
    }
    for (const line of content.children) {
      if (!(line instanceof HTMLElement)) continue;
      const lineIndexAttr = line.dataset.lineIndex;
      if (lineIndexAttr == null) continue;
      const lineIndex = Number(lineIndexAttr);
      let measuredHeight = line.getBoundingClientRect().height;
      let hasMetadata = false;
      if (line.nextElementSibling instanceof HTMLElement && ("lineAnnotation" in line.nextElementSibling.dataset || "noNewline" in line.nextElementSibling.dataset)) {
        if ("noNewline" in line.nextElementSibling.dataset) hasMetadata = true;
        measuredHeight += line.nextElementSibling.getBoundingClientRect().height;
      }
      const expectedHeight = this.getLineHeight(lineIndex, hasMetadata);
      if (measuredHeight === expectedHeight) continue;
      hasHeightChange = true;
      if (measuredHeight === this.metrics.lineHeight * (hasMetadata ? 2 : 1)) this.cache.heights.delete(lineIndex);
      else this.cache.heights.set(lineIndex, measuredHeight);
    }
    if (hasHeightChange || this.isResizeDebuggingEnabled()) this.computeApproximateSize(true);
    return hasHeightChange;
  }
  prepareCodeViewItem(file, top, reset, lineAnnotations) {
    const annotationsChanged = this.syncLineAnnotations(lineAnnotations);
    const targetChanged = !areFilesEqual(this.file, file) || this.fileRenderer.hasUnkeyedFileContentsChanged(file);
    let shouldResetLayoutCache = (reset == null ? void 0 : reset.resetFileLayoutCache) === true || targetChanged || annotationsChanged;
    if ((reset == null ? void 0 : reset.metrics) != null) {
      this.metrics = reset.metrics;
      shouldResetLayoutCache = true;
    }
    const { collapsed = false } = this.options;
    if (this.currentCollapsed !== collapsed) {
      this.currentCollapsed = collapsed;
      shouldResetLayoutCache = true;
    }
    if (shouldResetLayoutCache) this.resetLayoutCache();
    if (this.file !== file) this.layoutDirty = true;
    this.file = file;
    this.top = top;
    this.computeApproximateSize();
    return this.height;
  }
  getLinePosition(lineNumber) {
    if (this.file == null || lineNumber < 1) return;
    const { disableFileHeader = false, collapsed = false } = this.options;
    const lastLineIndex = this.fileRenderer.getLineCount(this.file) - 1;
    let top = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    if (collapsed || lastLineIndex < 0) return {
      top,
      height: 0
    };
    const clampedLineIndex = Math.min(Math.max(lineNumber - 1, 0), lastLineIndex);
    const { overflow = "scroll" } = this.options;
    const { lineHeight } = this.metrics;
    top += this.cache.fileAnnotationHeight;
    if (overflow === "scroll" && !this.hasLineAnnotations()) return {
      top: top + clampedLineIndex * lineHeight,
      height: lineHeight
    };
    const checkpoint = this.getLayoutCheckpointBeforeLineIndex(clampedLineIndex);
    top = (checkpoint == null ? void 0 : checkpoint.top) ?? top;
    for (let lineIndex = (checkpoint == null ? void 0 : checkpoint.lineIndex) ?? 0; lineIndex < clampedLineIndex; lineIndex++) top += this.getLineHeight(lineIndex, false);
    return {
      top,
      height: this.getLineHeight(clampedLineIndex, false)
    };
  }
  getEditorViewport() {
    return this.virtualizer.type === "simple" ? this.virtualizer.getRoot() : this.virtualizer.getContainerElement();
  }
  getNumericScrollAnchor(localViewportTop) {
    if (this.file == null || this.renderRange == null) return;
    const { disableFileHeader = false, collapsed = false, overflow = "scroll" } = this.options;
    if (collapsed || this.renderRange.totalLines <= 0) return;
    const lastLineIndex = this.fileRenderer.getLineCount(this.file) - 1;
    if (lastLineIndex < 0) return;
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    const firstRenderedLineIndex = Math.min(this.renderRange.startingLine, lastLineIndex);
    const lastRenderedLineIndex = Math.min(firstRenderedLineIndex + this.renderRange.totalLines - 1, lastLineIndex);
    if (lastRenderedLineIndex < firstRenderedLineIndex) return;
    const { fileAnnotationHeight } = this.cache;
    if (overflow === "scroll" && !this.hasLineAnnotations()) {
      const { lineHeight } = this.metrics;
      const firstRenderedLineTop = headerRegion + (firstRenderedLineIndex === 0 ? fileAnnotationHeight : this.renderRange.bufferBefore);
      const lineIndex = firstRenderedLineIndex + Math.max(Math.ceil((localViewportTop - firstRenderedLineTop) / lineHeight), 0);
      if (lineIndex > lastRenderedLineIndex) return;
      return {
        lineNumber: lineIndex + 1,
        top: headerRegion + fileAnnotationHeight + lineIndex * lineHeight
      };
    }
    let top = headerRegion + (firstRenderedLineIndex === 0 ? fileAnnotationHeight : this.renderRange.bufferBefore);
    for (let lineIndex = firstRenderedLineIndex; lineIndex <= lastRenderedLineIndex; lineIndex++) {
      if (top >= localViewportTop) return {
        lineNumber: lineIndex + 1,
        top
      };
      top += this.getLineHeight(lineIndex);
    }
  }
  getVirtualizedHeight() {
    return this.height;
  }
  getAdvancedStickySpecs(windowSpecs) {
    if (this.top == null || this.file == null) return;
    if (this.options.collapsed === true) return {
      topOffset: this.top,
      height: this.height
    };
    const renderRange = windowSpecs != null ? this.computeRenderRangeFromWindow(this.file, this.top, windowSpecs) : this.renderRange;
    if (renderRange == null) return;
    const { bufferBefore, bufferAfter, totalLines } = renderRange;
    let headerOnlyOffset = 0;
    if (totalLines === 0) {
      const activeWindow = windowSpecs ?? this.virtualizer.getWindowSpecs();
      if (this.top < activeWindow.top) headerOnlyOffset = bufferAfter;
    }
    return {
      topOffset: this.top + bufferBefore + headerOnlyOffset,
      height: this.height - (bufferBefore + bufferAfter)
    };
  }
  cleanUp(recycle = false) {
    var _a9;
    if (this.fileContainer != null && this.isSimpleMode()) (_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.disconnect(this.fileContainer);
    if (!recycle) this.resetLayoutCache();
    this.isSetup = false;
    super.cleanUp(recycle);
  }
  computeApproximateSize(force = false, file = this.file) {
    const shouldValidateSize = this.isResizeDebuggingEnabled();
    if (!force && !this.layoutDirty && !shouldValidateSize) return;
    const isFirstCompute = this.height === 0;
    this.height = 0;
    this.cache.checkpoints = [];
    if (file == null) {
      this.layoutDirty = false;
      return;
    }
    const { disableFileHeader = false, collapsed = false, overflow = "scroll" } = this.options;
    const { lineHeight } = this.metrics;
    const lineCount = this.fileRenderer.getLineCount(file);
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    const paddingBottom = getVirtualFilePaddingBottom(this.metrics);
    this.height += headerRegion;
    if (collapsed) {
      this.layoutDirty = false;
      return;
    }
    this.height += this.cache.fileAnnotationHeight;
    if (overflow === "scroll" && !this.hasLineAnnotations()) this.height += lineCount * lineHeight;
    else for (let lineIndex = 0; lineIndex < lineCount; lineIndex++) {
      this.addLayoutCheckpoint(lineIndex, this.height);
      this.height += this.getLineHeight(lineIndex, false);
    }
    if (lineCount > 0) this.height += paddingBottom;
    if (this.fileContainer != null && shouldValidateSize && !isFirstCompute) {
      const rect = this.fileContainer.getBoundingClientRect();
      if (rect.height !== this.height) console.log("VirtualizedFile.computeApproximateSize: computed height doesnt match", {
        name: file.name,
        elementHeight: rect.height,
        computedHeight: this.height
      });
      else console.log("VirtualizedFile.computeApproximateSize: computed height IS CORRECT");
    }
    this.layoutDirty = false;
  }
  setVisibility(visible) {
    if (this.isAdvancedMode() || this.fileContainer == null) return;
    this.renderRange = void 0;
    if (visible && !this.isVisible) {
      this.top = this.getVirtualizedTop();
      this.isVisible = true;
    } else if (!visible && this.isVisible) {
      this.isVisible = false;
      this.rerender();
    }
  }
  rerender() {
    if (!this.enabled || this.file == null) return;
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, false);
  }
  applyDocumentChange(textDocument, newLineAnnotations, shouldUpdateBuffer = false) {
    var _a9, _b;
    const { renderRange: previousRenderRange } = this;
    (_a9 = this.getAdvancedVirtualizer()) == null ? void 0 : _a9.capturePendingLayoutAnchor();
    super.applyDocumentChange(textDocument, newLineAnnotations);
    (_b = this.getSimpleVirtualizer()) == null ? void 0 : _b.markDOMDirty();
    this.resetLayoutCache(this.isSimpleMode(), false);
    if (!this.isSimpleMode()) this.computeApproximateSize(true);
    else if (shouldUpdateBuffer && previousRenderRange !== void 0 && this.file !== void 0) {
      const windowSpecs = this.virtualizer.getWindowSpecs();
      const renderRange = this.computeRenderRangeFromWindow(this.file, this.top ?? 0, windowSpecs);
      if (renderRange.bufferAfter !== previousRenderRange.bufferAfter) this.updateBuffers(renderRange);
    }
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, true);
  }
  renderPreparedFile({ fileContainer, file, forceRender = false, lineAnnotations, ...props }) {
    var _a9, _b;
    const didFileChange = this.file == null || !areFilesEqual(this.file, file) || this.fileRenderer.hasUnkeyedFileContentsChanged(file);
    const { forceRenderOverride, isSetup } = this;
    this.forceRenderOverride = void 0;
    const annotationsChanged = this.syncLineAnnotations(lineAnnotations);
    if (annotationsChanged) this.resetLayoutCache();
    fileContainer = this.getOrCreateFileContainerNode(fileContainer);
    if (file == null) {
      console.error("VirtualizedFile.render: attempting to virtually render when we dont have file");
      return false;
    }
    if (!isSetup) {
      this.computeApproximateSize(false, file);
      const virtualizer = this.getSimpleVirtualizer();
      this.top ?? (this.top = this.getVirtualizedTop());
      if (this.isAdvancedMode()) this.isVisible = true;
      else {
        if (virtualizer == null) throw new Error("VirtualizedFile.render: simple virtualizer is not available");
        virtualizer.connect(fileContainer, this);
        this.isVisible = virtualizer.isInstanceVisible(this.top ?? 0, this.height);
      }
      this.isSetup = true;
    } else {
      this.top ?? (this.top = this.getVirtualizedTop());
      if (didFileChange && this.isSimpleMode()) {
        (_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.markDOMDirty();
        this.resetLayoutCache(false);
        this.computeApproximateSize(false, file);
      }
    }
    if (!this.isVisible && this.isSimpleMode() && (!didFileChange || !isSetup)) {
      this.file = file;
      if (didFileChange) this.cachedHeaderHTML = void 0;
      return this.renderPlaceholder(this.height);
    }
    const windowSpecs = this.virtualizer.getWindowSpecs();
    const fileTop = this.top ?? 0;
    const renderRange = this.computeRenderRangeFromWindow(file, fileTop, windowSpecs);
    const rendered = super.renderPreparedFile({
      file,
      fileContainer,
      renderRange,
      lineAnnotations,
      forceRender: (forceRenderOverride ?? forceRender) || annotationsChanged || didFileChange,
      ...props
    });
    if (this.isSimpleMode() && rendered) (_b = this.getSimpleVirtualizer()) == null ? void 0 : _b.requestHeightReconcile(this);
    return rendered;
  }
  syncVirtualizedTop() {
    this.top = this.getVirtualizedTop();
  }
  shouldDisableVirtualizationBuffers() {
    return this.isAdvancedMode() || super.shouldDisableVirtualizationBuffers();
  }
  shouldGuardRebuildScroll() {
    return false;
  }
  isSimpleMode() {
    return this.virtualizer.type === "simple";
  }
  isAdvancedMode() {
    return this.virtualizer.type === "advanced";
  }
  addLayoutCheckpoint(lineIndex, top) {
    if (lineIndex % LAYOUT_CHECKPOINT_INTERVAL !== 0) return;
    this.cache.checkpoints.push({
      lineIndex,
      top
    });
  }
  getLayoutCheckpointBeforeLineIndex(lineIndex) {
    if (lineIndex <= 0 || this.cache.checkpoints.length === 0) return;
    let low = 0;
    let high = this.cache.checkpoints.length - 1;
    let result;
    while (low <= high) {
      const mid = low + high >> 1;
      const checkpoint = this.cache.checkpoints[mid];
      if (checkpoint == null) throw new Error("VirtualizedFile: invalid checkpoint index");
      if (checkpoint.lineIndex <= lineIndex) {
        result = checkpoint;
        low = mid + 1;
      } else high = mid - 1;
    }
    return result;
  }
  getLayoutCheckpointBeforeTop(top, hunkLineCount) {
    let low = 0;
    let high = this.cache.checkpoints.length - 1;
    let resultIndex = -1;
    while (low <= high) {
      const mid = low + high >> 1;
      const checkpoint = this.cache.checkpoints[mid];
      if (checkpoint == null) throw new Error("VirtualizedFile: invalid checkpoint index");
      if (checkpoint.top <= top) {
        resultIndex = mid;
        low = mid + 1;
      } else high = mid - 1;
    }
    if (hunkLineCount == null) return resultIndex >= 0 ? this.cache.checkpoints[resultIndex] : void 0;
    for (let index = resultIndex; index >= 0; index--) {
      const checkpoint = this.cache.checkpoints[index];
      if (checkpoint == null) throw new Error("VirtualizedFile: invalid checkpoint index");
      if (checkpoint.lineIndex % hunkLineCount === 0) return checkpoint;
    }
  }
  getVirtualizedTop() {
    if (this.virtualizer.type === "advanced") return this.virtualizer.getLocalTopForInstance(this);
    return this.fileContainer != null ? this.virtualizer.getOffsetInScrollContainer(this.fileContainer) : 0;
  }
  getSimpleVirtualizer() {
    return this.virtualizer.type === "simple" ? this.virtualizer : void 0;
  }
  getAdvancedVirtualizer() {
    return this.virtualizer.type === "advanced" ? this.virtualizer : void 0;
  }
  isResizeDebuggingEnabled() {
    var _a9;
    return ((_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.config.resizeDebugging) ?? false;
  }
  computeRenderRangeFromWindow(file, fileTop, { top, bottom }) {
    const { disableFileHeader = false, overflow = "scroll" } = this.options;
    const { hunkLineCount, lineHeight } = this.metrics;
    const lineCount = this.fileRenderer.getLineCount(file);
    const fileHeight = this.height;
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    const paddingBottom = lineCount > 0 ? getVirtualFilePaddingBottom(this.metrics) : 0;
    const { fileAnnotationHeight } = this.cache;
    const codeRegionTop = headerRegion + fileAnnotationHeight;
    const codeRowsHeight = Math.max(0, fileHeight - headerRegion - fileAnnotationHeight - paddingBottom);
    const hasFileAnnotations = includesFileAnnotations(this.lineAnnotations);
    const fileAnnotationTop = fileTop + headerRegion;
    const measuredFileAnnotationVisible = fileAnnotationHeight > 0 && hasFileAnnotations && fileAnnotationTop < bottom && fileAnnotationTop + fileAnnotationHeight > top;
    if (fileTop < top - fileHeight || fileTop > bottom) return {
      startingLine: 0,
      totalLines: 0,
      bufferBefore: 0,
      bufferAfter: fileHeight - headerRegion - paddingBottom
    };
    if (lineCount <= hunkLineCount) return {
      startingLine: 0,
      totalLines: hunkLineCount,
      bufferBefore: 0,
      bufferAfter: 0
    };
    const estimatedTargetLines = Math.ceil(Math.max(bottom - top, 0) / lineHeight);
    const totalLines = Math.ceil(estimatedTargetLines / hunkLineCount) * hunkLineCount + hunkLineCount;
    const totalHunks = totalLines / hunkLineCount;
    const viewportCenter = (top + bottom) / 2;
    if (overflow === "scroll" && !this.hasLineAnnotations()) {
      const sourceRowsTop = fileTop + codeRegionTop;
      const sourceRowsBottom = sourceRowsTop + codeRowsHeight;
      if (!measuredFileAnnotationVisible && !(sourceRowsTop < bottom && sourceRowsBottom > top)) return {
        startingLine: 0,
        totalLines: 0,
        bufferBefore: 0,
        bufferAfter: fileHeight - headerRegion - paddingBottom
      };
      const centerLine = Math.floor(measuredFileAnnotationVisible && viewportCenter < fileTop + codeRegionTop ? 0 : (viewportCenter - (fileTop + codeRegionTop)) / lineHeight);
      const idealStartHunk2 = Math.floor(centerLine / hunkLineCount) - Math.floor(totalHunks / 2);
      const totalHunksInFile = Math.ceil(lineCount / hunkLineCount);
      const startingLine2 = Math.max(0, Math.min(idealStartHunk2, totalHunksInFile)) * hunkLineCount;
      const clampedTotalLines2 = idealStartHunk2 < 0 ? totalLines + idealStartHunk2 * hunkLineCount : totalLines;
      const bufferBefore2 = startingLine2 === 0 ? 0 : fileAnnotationHeight + startingLine2 * lineHeight;
      const renderedLines = Math.min(clampedTotalLines2, lineCount - startingLine2);
      return {
        startingLine: startingLine2,
        totalLines: clampedTotalLines2,
        bufferBefore: bufferBefore2,
        bufferAfter: Math.max(0, (lineCount - startingLine2 - renderedLines) * lineHeight)
      };
    }
    const overflowHunks = totalHunks;
    const hunkOffsets = [];
    const checkpoint = this.getLayoutCheckpointBeforeTop(Math.max(0, top - fileTop - totalLines * lineHeight * 2), hunkLineCount);
    let absoluteLineTop = fileTop + ((checkpoint == null ? void 0 : checkpoint.top) ?? codeRegionTop);
    let currentLine = (checkpoint == null ? void 0 : checkpoint.lineIndex) ?? 0;
    let firstVisibleHunk;
    let centerHunk;
    let overflowCounter;
    const startingLineIndex = (checkpoint == null ? void 0 : checkpoint.lineIndex) ?? 0;
    for (let lineIndex = startingLineIndex; lineIndex < lineCount; lineIndex++) {
      const isAtHunkBoundary = currentLine % hunkLineCount === 0;
      const currentHunk = Math.floor(currentLine / hunkLineCount);
      if (isAtHunkBoundary) {
        hunkOffsets[currentHunk] = absoluteLineTop - (fileTop + codeRegionTop);
        if (overflowCounter != null) {
          if (overflowCounter <= 0) break;
          overflowCounter--;
        }
      }
      const lineHeight2 = this.getLineHeight(lineIndex, false);
      if (absoluteLineTop > top - lineHeight2 && absoluteLineTop < bottom) firstVisibleHunk ?? (firstVisibleHunk = currentHunk);
      if (absoluteLineTop + lineHeight2 > viewportCenter) centerHunk ?? (centerHunk = currentHunk);
      if (overflowCounter == null && absoluteLineTop >= bottom && isAtHunkBoundary) overflowCounter = overflowHunks;
      currentLine++;
      absoluteLineTop += lineHeight2;
    }
    if (firstVisibleHunk == null) if (measuredFileAnnotationVisible) {
      firstVisibleHunk = 0;
      centerHunk = 0;
    } else return {
      startingLine: 0,
      totalLines: 0,
      bufferBefore: 0,
      bufferAfter: fileHeight - headerRegion - paddingBottom
    };
    centerHunk ?? (centerHunk = firstVisibleHunk);
    const idealStartHunk = Math.round(centerHunk - totalHunks / 2);
    const maxStartHunk = Math.max(0, Math.ceil(lineCount / hunkLineCount) - totalHunks);
    const startHunk = Math.max(0, Math.min(idealStartHunk, maxStartHunk));
    const startingLine = startHunk * hunkLineCount;
    const clampedTotalLines = idealStartHunk < 0 ? totalLines + idealStartHunk * hunkLineCount : totalLines;
    const codeBufferBefore = hunkOffsets[startHunk] ?? 0;
    const bufferBefore = startingLine === 0 ? 0 : fileAnnotationHeight + codeBufferBefore;
    const finalHunkIndex = startHunk + clampedTotalLines / hunkLineCount;
    const bufferAfter = finalHunkIndex < hunkOffsets.length ? codeRowsHeight - hunkOffsets[finalHunkIndex] : codeRowsHeight - (absoluteLineTop - fileTop - codeRegionTop);
    return {
      startingLine,
      totalLines: clampedTotalLines,
      bufferBefore,
      bufferAfter: Math.max(0, bufferAfter)
    };
  }
};
function measureFileAnnotationHeight(content) {
  let height;
  for (const child of content.children) {
    if (!(child instanceof HTMLElement)) continue;
    if (child.dataset.lineAnnotation !== FILE_ANNOTATION_DOM_KEY) continue;
    height = Math.max(height ?? 0, child.getBoundingClientRect().height);
  }
  return height;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areDiffTargetsEqual.js
function areDiffTargetsEqual(diffA, diffB) {
  return diffA === diffB || (diffA == null ? void 0 : diffA.cacheKey) != null && diffA.cacheKey === (diffB == null ? void 0 : diffB.cacheKey);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/cloneFileDiffMetadata.js
function cloneFileDiffMetadata(fileDiff) {
  return {
    ...fileDiff,
    hunks: fileDiff.hunks.map((hunk) => ({
      ...hunk,
      hunkContent: hunk.hunkContent.map((content) => ({ ...content }))
    })),
    deletionLines: [...fileDiff.deletionLines],
    additionLines: [...fileDiff.additionLines]
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/splitFileContents.js
function splitFileContents2(contents) {
  return contents !== "" ? contents.split(SPLIT_WITH_NEWLINES) : [];
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/hydratePartialDiff.js
function hydratePartialDiff(type, fileDiff, files) {
  const targetFileDiff = type === "clone" ? cloneFileDiffMetadata(fileDiff) : fileDiff;
  if (!targetFileDiff.isPartial) throw new Error("hydratePartialDiff: fileDiff must be partial");
  switch (targetFileDiff.type) {
    case "change":
    case "rename-changed":
      return hydrateTwoSidedFileDiff(targetFileDiff, requireOldFile(targetFileDiff, files), requireNewFile(targetFileDiff, files));
    case "rename-pure": {
      const newFile = requireNewFile(targetFileDiff, files);
      requireMissingOldFile(targetFileDiff, files);
      const lines = splitFileContents2(newFile.contents);
      targetFileDiff.isPartial = false;
      targetFileDiff.deletionLines = lines;
      targetFileDiff.additionLines = lines;
      setHydratedCacheKey(targetFileDiff, null, newFile);
      return targetFileDiff;
    }
  }
  throw new Error(`hydratePartialDiff: ${targetFileDiff.type} diffs cannot be hydrated from loaded files`);
}
function hydrateTwoSidedFileDiff(fileDiff, oldFile, newFile) {
  const deletionLines = splitFileContents2(oldFile.contents);
  const additionLines = splitFileContents2(newFile.contents);
  const { hunks, splitLineCount, unifiedLineCount } = hydrateHunks(fileDiff.hunks, additionLines.length);
  fileDiff.hunks = hunks;
  fileDiff.splitLineCount = splitLineCount;
  fileDiff.unifiedLineCount = unifiedLineCount;
  fileDiff.isPartial = false;
  fileDiff.deletionLines = deletionLines;
  fileDiff.additionLines = additionLines;
  setHydratedCacheKey(fileDiff, oldFile, newFile);
  return fileDiff;
}
function hydrateHunks(hunks, totalAdditionLines) {
  let splitLineCount = 0;
  let unifiedLineCount = 0;
  let lastHunkAdditionEnd = 0;
  const hydratedHunks = [];
  for (const hunk of hunks) {
    const additionLineIndex = Math.max(hunk.additionStart - 1, 0);
    const deletionLineIndex = Math.max(hunk.deletionStart - 1, 0);
    let contentAdditionLineIndex = additionLineIndex;
    let contentDeletionLineIndex = deletionLineIndex;
    let hunkAdditionLines = 0;
    let hunkDeletionLines = 0;
    let hunkSplitLineCount = 0;
    let hunkUnifiedLineCount = 0;
    const hunkContent = [];
    for (const content of hunk.hunkContent) {
      if (content.type === "context") {
        hunkContent.push({
          ...content,
          additionLineIndex: contentAdditionLineIndex,
          deletionLineIndex: contentDeletionLineIndex
        });
        contentAdditionLineIndex += content.lines;
        contentDeletionLineIndex += content.lines;
        hunkSplitLineCount += content.lines;
        hunkUnifiedLineCount += content.lines;
        continue;
      }
      hunkContent.push({
        ...content,
        additionLineIndex: contentAdditionLineIndex,
        deletionLineIndex: contentDeletionLineIndex
      });
      contentAdditionLineIndex += content.additions;
      contentDeletionLineIndex += content.deletions;
      hunkAdditionLines += content.additions;
      hunkDeletionLines += content.deletions;
      hunkSplitLineCount += Math.max(content.additions, content.deletions);
      hunkUnifiedLineCount += content.additions + content.deletions;
    }
    const collapsedBefore = Math.max(getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount) - lastHunkAdditionEnd, 0);
    hydratedHunks.push({
      ...hunk,
      collapsedBefore,
      additionLineIndex,
      deletionLineIndex,
      additionLines: hunkAdditionLines,
      deletionLines: hunkDeletionLines,
      hunkContent,
      splitLineStart: splitLineCount + collapsedBefore,
      unifiedLineStart: unifiedLineCount + collapsedBefore,
      splitLineCount: hunkSplitLineCount,
      unifiedLineCount: hunkUnifiedLineCount
    });
    splitLineCount += collapsedBefore + hunkSplitLineCount;
    unifiedLineCount += collapsedBefore + hunkUnifiedLineCount;
    lastHunkAdditionEnd = getHunkSideEndBoundary(hunk.additionStart, hunk.additionCount);
  }
  if (hydratedHunks.length > 0) {
    const lastHunk = hydratedHunks[hydratedHunks.length - 1];
    const lastHunkEnd = getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount);
    const collapsedAfter = Math.max(totalAdditionLines - lastHunkEnd, 0);
    splitLineCount += collapsedAfter;
    unifiedLineCount += collapsedAfter;
  }
  return {
    hunks: hydratedHunks,
    splitLineCount,
    unifiedLineCount
  };
}
function requireOldFile(fileDiff, files) {
  if (files.oldFile == null) throw new Error(`hydratePartialDiff: ${fileDiff.type} diff for ${fileDiff.name} requires oldFile`);
  return files.oldFile;
}
function requireNewFile(fileDiff, files) {
  if (files.newFile == null) throw new Error(`hydratePartialDiff: ${fileDiff.type} diff for ${fileDiff.name} requires newFile`);
  return files.newFile;
}
function requireMissingOldFile(fileDiff, files) {
  if (files.oldFile !== null) throw new Error(`hydratePartialDiff: ${fileDiff.type} diff for ${fileDiff.name} requires oldFile to be null`);
}
function getHydratedCacheKey(fileDiff, oldFile, newFile) {
  if (fileDiff.cacheKey != null) return `${fileDiff.cacheKey}:hydrated`;
  return getLoadedFileCacheKey(oldFile, newFile);
}
function setHydratedCacheKey(fileDiff, oldFile, newFile) {
  const cacheKey = getHydratedCacheKey(fileDiff, oldFile, newFile);
  if (cacheKey == null) {
    delete fileDiff.cacheKey;
    return;
  }
  fileDiff.cacheKey = cacheKey;
}
function getLoadedFileCacheKey(oldFile, newFile) {
  if (oldFile != null && newFile != null) return oldFile.cacheKey != null && newFile.cacheKey != null ? `${oldFile.cacheKey}:${newFile.cacheKey}` : void 0;
  return (oldFile == null ? void 0 : oldFile.cacheKey) ?? (newFile == null ? void 0 : newFile.cacheKey);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/managers/ScrollSyncManager.js
var ScrollSyncManager = class {
  constructor() {
    __publicField(this, "isDeletionsScrolling", false);
    __publicField(this, "isAdditionsScrolling", false);
    __publicField(this, "timeoutId", -1);
    __publicField(this, "codeDeletions");
    __publicField(this, "codeAdditions");
    __publicField(this, "enabled", false);
    __publicField(this, "handleDeletionsScroll", () => {
      var _a9, _b;
      if (this.isAdditionsScrolling) return;
      this.isDeletionsScrolling = true;
      clearTimeout(this.timeoutId);
      this.timeoutId = setTimeout(() => {
        this.isDeletionsScrolling = false;
      }, 300);
      (_b = this.codeAdditions) == null ? void 0 : _b.scrollTo({ left: (_a9 = this.codeDeletions) == null ? void 0 : _a9.scrollLeft });
    });
    __publicField(this, "handleAdditionsScroll", () => {
      var _a9, _b;
      if (this.isDeletionsScrolling) return;
      this.isAdditionsScrolling = true;
      clearTimeout(this.timeoutId);
      this.timeoutId = setTimeout(() => {
        this.isAdditionsScrolling = false;
      }, 300);
      (_b = this.codeDeletions) == null ? void 0 : _b.scrollTo({ left: (_a9 = this.codeAdditions) == null ? void 0 : _a9.scrollLeft });
    });
  }
  cleanUp() {
    var _a9, _b;
    if (!this.enabled) return;
    (_a9 = this.codeDeletions) == null ? void 0 : _a9.removeEventListener("scroll", this.handleDeletionsScroll);
    (_b = this.codeAdditions) == null ? void 0 : _b.removeEventListener("scroll", this.handleAdditionsScroll);
    clearTimeout(this.timeoutId);
    this.codeDeletions = void 0;
    this.codeAdditions = void 0;
    this.enabled = false;
  }
  setup(pre, codeDeletions, codeAdditions) {
    var _a9, _b;
    if (codeDeletions == null || codeAdditions == null) for (const element2 of pre.children ?? []) {
      if (!(element2 instanceof HTMLElement)) continue;
      if ("deletions" in element2.dataset) codeDeletions = element2;
      else if ("additions" in element2.dataset) codeAdditions = element2;
    }
    if (codeAdditions == null || codeDeletions == null) {
      this.cleanUp();
      return;
    }
    if (this.codeDeletions !== codeDeletions) {
      (_a9 = this.codeDeletions) == null ? void 0 : _a9.removeEventListener("scroll", this.handleDeletionsScroll);
      this.codeDeletions = codeDeletions;
      codeDeletions.addEventListener("scroll", this.handleDeletionsScroll, { passive: true });
    }
    if (this.codeAdditions !== codeAdditions) {
      (_b = this.codeAdditions) == null ? void 0 : _b.removeEventListener("scroll", this.handleAdditionsScroll);
      this.codeAdditions = codeAdditions;
      codeAdditions.addEventListener("scroll", this.handleAdditionsScroll, { passive: true });
    }
    this.enabled = true;
  }
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areDiffRenderOptionsEqual.js
function areDiffRenderOptionsEqual(optionsA, optionsB) {
  return areThemesEqual(optionsA.theme, optionsB.theme) && optionsA.useTokenTransformer === optionsB.useTokenTransformer && optionsA.tokenizeMaxLineLength === optionsB.tokenizeMaxLineLength && optionsA.lineDiffType === optionsB.lineDiffType && optionsA.maxLineDiffLength === optionsB.maxLineDiffLength;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createEmptyRowBuffer.js
function createEmptyRowBuffer(size) {
  return createHastElement({
    tagName: "div",
    properties: {
      "data-content-buffer": "",
      "data-buffer-size": size,
      style: `grid-row: span ${size};min-height:calc(${size} * 1lh)`
    }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createNoNewlineElement.js
function createNoNewlineElement(type) {
  return createHastElement({
    tagName: "div",
    children: [createHastElement({
      tagName: "span",
      children: [createTextNodeElement("No newline at end of file")]
    })],
    properties: {
      "data-no-newline": "",
      "data-line-type": type,
      "data-column-content": ""
    }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createSeparator.js
function createExpandButton(type) {
  return createHastElement({
    tagName: "div",
    children: [createIconElement({
      name: type === "both" ? "diffs-icon-expand-all" : "diffs-icon-expand",
      properties: { "data-icon": "" }
    })],
    properties: {
      role: "button",
      "data-expand-button": "",
      "data-expand-both": type === "both" ? "" : void 0,
      "data-expand-up": type === "up" ? "" : void 0,
      "data-expand-down": type === "down" ? "" : void 0
    }
  });
}
function createSeparator({ type, content, expandIndex, chunked = false, slotName, isFirstHunk, isLastHunk }) {
  let buttonCount = 0;
  const children = [];
  if (type === "metadata" && content != null) children.push(createHastElement({
    tagName: "div",
    children: [createTextNodeElement(content)],
    properties: { "data-separator-wrapper": "" }
  }));
  if ((type === "line-info" || type === "line-info-basic") && content != null) {
    const contentChildren = [];
    if (expandIndex != null) if (!chunked) {
      contentChildren.push(createExpandButton(!isFirstHunk && !isLastHunk ? "both" : isFirstHunk ? "down" : "up"));
      buttonCount++;
    } else {
      if (!isFirstHunk) {
        contentChildren.push(createExpandButton("up"));
        buttonCount++;
      }
      if (!isLastHunk) {
        contentChildren.push(createExpandButton("down"));
        buttonCount++;
      }
    }
    contentChildren.push(createHastElement({
      tagName: "div",
      children: [createHastElement({
        tagName: "span",
        children: [createTextNodeElement(content)],
        properties: { "data-unmodified-lines": "" }
      })],
      properties: { "data-separator-content": "" }
    }));
    if (chunked && expandIndex != null) contentChildren.push(createHastElement({
      tagName: "div",
      children: [createTextNodeElement("Expand all")],
      properties: {
        role: "button",
        "data-expand-button": "",
        "data-expand-all-button": ""
      }
    }));
    children.push(createHastElement({
      tagName: "div",
      children: contentChildren,
      properties: {
        "data-separator-wrapper": "",
        "data-separator-multi-button": buttonCount > 1 ? "" : void 0
      }
    }));
  }
  if (type === "custom" && slotName != null) children.push(createHastElement({
    tagName: "slot",
    properties: { name: slotName }
  }));
  return createHastElement({
    tagName: "div",
    children,
    properties: {
      "data-separator": children.length === 0 ? "simple" : type,
      "data-expand-index": expandIndex,
      "data-separator-first": isFirstHunk ? "" : void 0,
      "data-separator-last": isLastHunk ? "" : void 0
    }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getHunkSeparatorSlotName.js
function getHunkSeparatorSlotName(type, hunkIndex) {
  return `hunk-separator-${type}-${hunkIndex}`;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getTotalLineCountFromHunks.js
function getTotalLineCountFromHunks(hunks) {
  const lastHunk = hunks.at(-1);
  if (lastHunk == null) return 0;
  return Math.max(getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount), getHunkSideEndBoundary(lastHunk.deletionStart, lastHunk.deletionCount));
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isDefaultRenderRange.js
function isDefaultRenderRange(renderRange) {
  return renderRange.startingLine === 0 && renderRange.totalLines === Infinity && renderRange.bufferBefore === 0 && renderRange.bufferAfter === 0;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/parseDiffDecorations.js
function createDiffSpanDecoration({ line, spanStart, spanLength }) {
  return {
    start: {
      line,
      character: spanStart
    },
    end: {
      line,
      character: spanStart + spanLength
    },
    properties: { "data-diff-span": "" },
    alwaysWrap: true
  };
}
function pushOrJoinSpan({ item, arr, enableJoin, isNeutral = false, isLastItem = false }) {
  const lastItem = arr[arr.length - 1];
  if (lastItem == null || isLastItem || !enableJoin) {
    arr.push([isNeutral ? 0 : 1, item.value]);
    return;
  }
  const isLastItemNeutral = lastItem[0] === 0;
  if (isNeutral === isLastItemNeutral || isNeutral && item.value.length === 1 && !isLastItemNeutral) {
    lastItem[1] += item.value;
    return;
  }
  arr.push([isNeutral ? 0 : 1, item.value]);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/virtualDiffLayout.js
function getHunkAdditionLineRange(hunk) {
  return [getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount) + 1, getHunkSideEndBoundary(hunk.additionStart, hunk.additionCount) + 1];
}
function getExpandedRegion({ isPartial, rangeSize, expandedHunks, hunkIndex, collapsedContextThreshold }) {
  const normalizedRangeSize = Math.max(rangeSize, 0);
  if (normalizedRangeSize === 0 || isPartial) return {
    fromStart: 0,
    fromEnd: 0,
    rangeSize: normalizedRangeSize,
    collapsedLines: normalizedRangeSize,
    renderAll: false
  };
  if (expandedHunks === true || normalizedRangeSize <= collapsedContextThreshold) return {
    fromStart: normalizedRangeSize,
    fromEnd: 0,
    rangeSize: normalizedRangeSize,
    collapsedLines: 0,
    renderAll: true
  };
  const region = expandedHunks == null ? void 0 : expandedHunks.get(hunkIndex);
  const fromStart = Math.min(Math.max((region == null ? void 0 : region.fromStart) ?? 0, 0), normalizedRangeSize);
  const fromEnd = Math.min(Math.max((region == null ? void 0 : region.fromEnd) ?? 0, 0), normalizedRangeSize);
  const expandedCount = fromStart + fromEnd;
  const renderAll = expandedCount >= normalizedRangeSize;
  return {
    fromStart: renderAll ? normalizedRangeSize : fromStart,
    fromEnd: renderAll ? 0 : fromEnd,
    rangeSize: normalizedRangeSize,
    collapsedLines: Math.max(normalizedRangeSize - expandedCount, 0),
    renderAll
  };
}
function hasTrailingContextMismatch(fileDiff) {
  const lastHunk = fileDiff.hunks[fileDiff.hunks.length - 1];
  if (lastHunk == null || fileDiff.isPartial || fileDiff.additionLines.length === 0 || fileDiff.deletionLines.length === 0) return false;
  const additionRemaining = fileDiff.additionLines.length - getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount);
  const deletionRemaining = fileDiff.deletionLines.length - getHunkSideEndBoundary(lastHunk.deletionStart, lastHunk.deletionCount);
  if (additionRemaining <= 0 && deletionRemaining <= 0) return false;
  return additionRemaining !== deletionRemaining;
}
function getTrailingContextRangeSize({ fileDiff, errorPrefix }) {
  const lastHunk = fileDiff.hunks[fileDiff.hunks.length - 1];
  if (lastHunk == null || fileDiff.isPartial || fileDiff.additionLines.length === 0 || fileDiff.deletionLines.length === 0) return 0;
  const additionRemaining = fileDiff.additionLines.length - getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount);
  const deletionRemaining = fileDiff.deletionLines.length - getHunkSideEndBoundary(lastHunk.deletionStart, lastHunk.deletionCount);
  if (additionRemaining <= 0 && deletionRemaining <= 0) return 0;
  if (additionRemaining !== deletionRemaining) throw new Error(`${errorPrefix}: trailing context mismatch (additions=${additionRemaining}, deletions=${deletionRemaining}) for ${fileDiff.name}`);
  return Math.min(additionRemaining, deletionRemaining);
}
function getTrailingExpandedRegion({ fileDiff, hunkIndex, expandedHunks, collapsedContextThreshold, errorPrefix }) {
  if (hunkIndex !== fileDiff.hunks.length - 1) return;
  const trailingRangeSize = getTrailingContextRangeSize({
    fileDiff,
    errorPrefix
  });
  if (trailingRangeSize <= 0) return;
  if (expandedHunks === true || trailingRangeSize <= collapsedContextThreshold) return {
    fromStart: trailingRangeSize,
    fromEnd: 0,
    rangeSize: trailingRangeSize,
    collapsedLines: 0,
    renderAll: true
  };
  const region = expandedHunks == null ? void 0 : expandedHunks.get(fileDiff.hunks.length);
  const fromStart = Math.min(Math.max((region == null ? void 0 : region.fromStart) ?? 0, 0), trailingRangeSize);
  return {
    fromStart,
    fromEnd: 0,
    rangeSize: trailingRangeSize,
    collapsedLines: trailingRangeSize - fromStart,
    renderAll: fromStart >= trailingRangeSize
  };
}
function isAdditionLineRenderable({ fileDiff, lineNumber, expandedHunks, collapsedContextThreshold }) {
  if (expandedHunks === true || fileDiff.isPartial) return true;
  for (const [hunkIndex, hunk] of fileDiff.hunks.entries()) {
    const [hunkStart, hunkEnd] = getHunkAdditionLineRange(hunk);
    if (lineNumber < hunkStart) {
      const region = getExpandedRegion({
        isPartial: fileDiff.isPartial,
        rangeSize: hunk.collapsedBefore,
        expandedHunks,
        hunkIndex,
        collapsedContextThreshold
      });
      const gapStart = hunkStart - region.rangeSize;
      return region.renderAll || lineNumber < gapStart + region.fromStart || lineNumber >= hunkStart - region.fromEnd;
    }
    if (lineNumber < hunkEnd) return true;
  }
  const trailingRegion = getTrailingExpandedRegion({
    fileDiff,
    hunkIndex: fileDiff.hunks.length - 1,
    expandedHunks,
    collapsedContextThreshold,
    errorPrefix: "isAdditionLineRenderable"
  });
  if (trailingRegion == null || trailingRegion.renderAll) return true;
  const lastHunk = fileDiff.hunks[fileDiff.hunks.length - 1];
  const [, trailingStart] = getHunkAdditionLineRange(lastHunk);
  return lineNumber < trailingStart + trailingRegion.fromStart || lineNumber >= trailingStart + trailingRegion.rangeSize;
}
function getNearestRenderableAdditionLine({ fileDiff, lineNumber, direction, expandedHunks, collapsedContextThreshold }) {
  if (expandedHunks === true || fileDiff.isPartial) return lineNumber;
  const ranges = [];
  let modeledEnd = 1;
  for (const [hunkIndex, hunk] of fileDiff.hunks.entries()) {
    const [hunkStart, hunkEnd] = getHunkAdditionLineRange(hunk);
    const region = getExpandedRegion({
      isPartial: fileDiff.isPartial,
      rangeSize: hunk.collapsedBefore,
      expandedHunks,
      hunkIndex,
      collapsedContextThreshold
    });
    const gapStart = hunkStart - region.rangeSize;
    if (region.renderAll) ranges.push([gapStart, hunkStart]);
    else {
      if (region.fromStart > 0) ranges.push([gapStart, gapStart + region.fromStart]);
      if (region.fromEnd > 0) ranges.push([hunkStart - region.fromEnd, hunkStart]);
    }
    ranges.push([hunkStart, hunkEnd]);
    modeledEnd = hunkEnd;
  }
  const trailingRegion = getTrailingExpandedRegion({
    fileDiff,
    hunkIndex: fileDiff.hunks.length - 1,
    expandedHunks,
    collapsedContextThreshold,
    errorPrefix: "getNearestRenderableAdditionLine"
  });
  if (trailingRegion != null) {
    const trailingStart = modeledEnd;
    modeledEnd = trailingStart + trailingRegion.rangeSize;
    if (trailingRegion.renderAll) ranges.push([trailingStart, modeledEnd]);
    else if (trailingRegion.fromStart > 0) ranges.push([trailingStart, trailingStart + trailingRegion.fromStart]);
  }
  if (lineNumber >= modeledEnd) return lineNumber;
  if (direction === "down") {
    for (const [start, end] of ranges) if (end > lineNumber) return Math.max(start, lineNumber);
    return;
  }
  for (let index = ranges.length - 1; index >= 0; index--) {
    const [start, end] = ranges[index];
    if (start <= lineNumber) return Math.min(end - 1, lineNumber);
  }
}
function getHunkSeparatorHeight({ type, metrics }) {
  return metrics.hunkSeparatorHeight ?? getDefaultHunkSeparatorHeight(type);
}
function getHunkSeparatorGap({ type, metrics }) {
  return type === "simple" || type === "metadata" || type === "line-info-basic" ? 0 : metrics.spacing;
}
function hasLeadingHunkSeparator({ type, hunkIndex, hunkSpecs }) {
  switch (type) {
    case "simple":
      return hunkIndex > 0;
    case "metadata":
      return hunkSpecs != null;
    case "line-info":
    case "line-info-basic":
    case "custom":
      return true;
  }
}
function hasTrailingHunkSeparator(type) {
  return type !== "simple" && type !== "metadata";
}
function getLeadingHunkSeparatorLayout({ type, metrics, hunkIndex, hunkSpecs }) {
  if (!hasLeadingHunkSeparator({
    type,
    hunkIndex,
    hunkSpecs
  })) return;
  const height = getHunkSeparatorHeight({
    type,
    metrics
  });
  const gap = getHunkSeparatorGap({
    type,
    metrics
  });
  const gapBefore = hunkIndex > 0 ? gap : 0;
  const gapAfter = gap;
  return {
    height,
    gapBefore,
    gapAfter,
    totalHeight: gapBefore + height + gapAfter
  };
}
function getTrailingHunkSeparatorLayout({ type, metrics }) {
  if (!hasTrailingHunkSeparator(type)) return;
  const height = getHunkSeparatorHeight({
    type,
    metrics
  });
  const gapBefore = getHunkSeparatorGap({
    type,
    metrics
  });
  return {
    height,
    gapBefore,
    gapAfter: 0,
    totalHeight: gapBefore + height
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/iterateOverDiff.js
function iterateOverDiff({ diff, diffStyle, startingLine = 0, totalLines = Infinity, expandedHunks, collapsedContextThreshold = 1, callback }) {
  var _a9;
  const iterationStart = getIterationStartState({
    diff,
    diffStyle,
    startingLine,
    expandedHunks,
    collapsedContextThreshold
  });
  const state = {
    viewportStart: startingLine,
    viewportEnd: startingLine + totalLines,
    isWindowedHighlight: startingLine > 0 || totalLines < Infinity,
    splitCount: iterationStart.splitCount,
    unifiedCount: iterationStart.unifiedCount,
    finalHunkIndex: diff.hunks.length - 1,
    shouldBreak() {
      if (!state.isWindowedHighlight) return false;
      const breakUnified = state.unifiedCount >= startingLine + totalLines;
      const breakSplit = state.splitCount >= startingLine + totalLines;
      if (diffStyle === "unified") return breakUnified;
      else if (diffStyle === "split") return breakSplit;
      else return breakUnified && breakSplit;
    },
    shouldSkip(unifiedCount, splitCount) {
      if (!state.isWindowedHighlight) return false;
      const skipUnified = unifiedCount > 0 && state.unifiedCount + unifiedCount <= startingLine;
      const skipSplit = splitCount > 0 && state.splitCount + splitCount <= startingLine;
      if (diffStyle === "unified") return skipUnified;
      else if (diffStyle === "split") return skipSplit;
      else return skipUnified && skipSplit;
    },
    incrementCounts(unifiedValue, splitValue) {
      if (diffStyle === "unified" || diffStyle === "both") state.unifiedCount += unifiedValue;
      if (diffStyle === "split" || diffStyle === "both") state.splitCount += splitValue;
    },
    isInWindow(unifiedHeight, splitHeight) {
      if (!state.isWindowedHighlight) return true;
      const unifiedInWindow = state.isInUnifiedWindow(unifiedHeight);
      const splitInWindow = state.isInSplitWindow(splitHeight);
      if (diffStyle === "unified") return unifiedInWindow;
      else if (diffStyle === "split") return splitInWindow;
      else return unifiedInWindow || splitInWindow;
    },
    isInUnifiedWindow(unifiedHeight) {
      return !state.isWindowedHighlight || state.unifiedCount >= startingLine - unifiedHeight && state.unifiedCount < startingLine + totalLines;
    },
    isInSplitWindow(splitHeight) {
      return !state.isWindowedHighlight || state.splitCount >= startingLine - splitHeight && state.splitCount < startingLine + totalLines;
    },
    emit(props, silent = false) {
      if (!silent) if (diffStyle === "unified") state.incrementCounts(1, 0);
      else if (diffStyle === "split") state.incrementCounts(0, 1);
      else state.incrementCounts(1, 1);
      return callback(props) ?? false;
    }
  };
  hunkIterator: for (let hunkIndex = iterationStart.hunkIndex; hunkIndex < diff.hunks.length; hunkIndex++) {
    let getTrailingCollapsedAfter = function(unifiedLineIndex2, splitLineIndex2) {
      if (trailingRegion == null || trailingRegion.collapsedLines <= 0 || trailingRegion.fromStart + trailingRegion.fromEnd > 0) return 0;
      if (diffStyle === "unified") return unifiedLineIndex2 === hunk.unifiedLineStart + hunk.unifiedLineCount - 1 ? trailingRegion.collapsedLines : 0;
      return splitLineIndex2 === hunk.splitLineStart + hunk.splitLineCount - 1 ? trailingRegion.collapsedLines : 0;
    }, consumePendingCollapsed = function() {
      if (consumedCollapsed) return 0;
      consumedCollapsed = true;
      return leadingRegion.collapsedLines;
    };
    const hunk = diff.hunks[hunkIndex];
    if (hunk == null) throw new Error("iterateOverDiff: invalid hunk index");
    if (state.shouldBreak()) break;
    const deletionBoundary = getHunkSideStartBoundary(hunk.deletionStart, hunk.deletionCount);
    const additionBoundary = getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount);
    const deletionStartIndex = !diff.isPartial && hunk.deletionCount === 0 ? deletionBoundary : hunk.deletionLineIndex;
    const additionStartIndex = !diff.isPartial && hunk.additionCount === 0 ? additionBoundary : hunk.additionLineIndex;
    const leadingRegion = getExpandedRegion({
      isPartial: diff.isPartial,
      rangeSize: hunk.collapsedBefore,
      expandedHunks,
      hunkIndex,
      collapsedContextThreshold
    });
    const trailingRegion = hunkIndex === state.finalHunkIndex ? getTrailingExpandedRegion({
      fileDiff: diff,
      hunkIndex,
      expandedHunks,
      collapsedContextThreshold,
      errorPrefix: "iterateOverDiff"
    }) : void 0;
    const expandedLineCount = leadingRegion.fromStart + leadingRegion.fromEnd;
    let consumedCollapsed = leadingRegion.collapsedLines === 0;
    if (!state.shouldSkip(expandedLineCount, expandedLineCount)) {
      let unifiedLineIndex2 = hunk.unifiedLineStart - leadingRegion.rangeSize;
      let splitLineIndex2 = hunk.splitLineStart - leadingRegion.rangeSize;
      let deletionLineIndex2 = deletionStartIndex - leadingRegion.rangeSize;
      let additionLineIndex2 = additionStartIndex - leadingRegion.rangeSize;
      let deletionLineNumber2 = deletionBoundary + 1 - leadingRegion.rangeSize;
      let additionLineNumber2 = additionBoundary + 1 - leadingRegion.rangeSize;
      if (walkContextLines(state, leadingRegion.fromStart, diffStyle, (index) => {
        return state.emit({
          hunkIndex,
          hunk,
          collapsedBefore: 0,
          collapsedAfter: 0,
          type: "context-expanded",
          deletionLine: {
            lineNumber: deletionLineNumber2 + index,
            lineIndex: deletionLineIndex2 + index,
            noEOFCR: false,
            unifiedLineIndex: unifiedLineIndex2 + index,
            splitLineIndex: splitLineIndex2 + index
          },
          additionLine: {
            unifiedLineIndex: unifiedLineIndex2 + index,
            splitLineIndex: splitLineIndex2 + index,
            lineIndex: additionLineIndex2 + index,
            lineNumber: additionLineNumber2 + index,
            noEOFCR: false
          }
        });
      })) break hunkIterator;
      unifiedLineIndex2 = hunk.unifiedLineStart - leadingRegion.fromEnd;
      splitLineIndex2 = hunk.splitLineStart - leadingRegion.fromEnd;
      deletionLineIndex2 = deletionStartIndex - leadingRegion.fromEnd;
      additionLineIndex2 = additionStartIndex - leadingRegion.fromEnd;
      deletionLineNumber2 = deletionBoundary + 1 - leadingRegion.fromEnd;
      additionLineNumber2 = additionBoundary + 1 - leadingRegion.fromEnd;
      if (walkContextLines(state, leadingRegion.fromEnd, diffStyle, (index) => {
        return state.emit({
          hunkIndex,
          hunk,
          collapsedBefore: consumePendingCollapsed(),
          collapsedAfter: 0,
          type: "context-expanded",
          deletionLine: {
            lineNumber: deletionLineNumber2 + index,
            lineIndex: deletionLineIndex2 + index,
            noEOFCR: false,
            unifiedLineIndex: unifiedLineIndex2 + index,
            splitLineIndex: splitLineIndex2 + index
          },
          additionLine: {
            unifiedLineIndex: unifiedLineIndex2 + index,
            splitLineIndex: splitLineIndex2 + index,
            lineIndex: additionLineIndex2 + index,
            lineNumber: additionLineNumber2 + index,
            noEOFCR: false
          }
        });
      }, () => {
        consumePendingCollapsed();
      })) break hunkIterator;
    } else {
      state.incrementCounts(expandedLineCount, expandedLineCount);
      consumePendingCollapsed();
    }
    let unifiedLineIndex = hunk.unifiedLineStart;
    let splitLineIndex = hunk.splitLineStart;
    let deletionLineIndex = deletionStartIndex;
    let additionLineIndex = additionStartIndex;
    let deletionLineNumber = deletionBoundary + 1;
    let additionLineNumber = additionBoundary + 1;
    const lastContent = hunk.hunkContent.at(-1);
    for (const content of hunk.hunkContent) {
      if (state.shouldBreak()) break hunkIterator;
      const isLastContent = content === lastContent;
      if (content.type === "context") {
        if (!state.shouldSkip(content.lines, content.lines)) {
          if (walkContextLines(state, content.lines, diffStyle, (index) => {
            const isLastLine = isLastContent && index === content.lines - 1;
            const unifiedRowIndex = unifiedLineIndex + index;
            const splitRowIndex = splitLineIndex + index;
            return state.emit({
              hunkIndex,
              hunk,
              collapsedBefore: consumePendingCollapsed(),
              collapsedAfter: getTrailingCollapsedAfter(unifiedRowIndex, splitRowIndex),
              type: "context",
              deletionLine: {
                lineNumber: deletionLineNumber + index,
                lineIndex: deletionLineIndex + index,
                noEOFCR: isLastLine && hunk.noEOFCRDeletions,
                unifiedLineIndex: unifiedRowIndex,
                splitLineIndex: splitRowIndex
              },
              additionLine: {
                unifiedLineIndex: unifiedRowIndex,
                splitLineIndex: splitRowIndex,
                lineIndex: additionLineIndex + index,
                lineNumber: additionLineNumber + index,
                noEOFCR: isLastLine && hunk.noEOFCRAdditions
              }
            });
          }, () => {
            consumePendingCollapsed();
          })) break hunkIterator;
        } else {
          state.incrementCounts(content.lines, content.lines);
          consumePendingCollapsed();
        }
        unifiedLineIndex += content.lines;
        splitLineIndex += content.lines;
        deletionLineIndex += content.lines;
        additionLineIndex += content.lines;
        deletionLineNumber += content.lines;
        additionLineNumber += content.lines;
      } else {
        const splitCount = Math.max(content.deletions, content.additions);
        const unifiedCount = content.deletions + content.additions;
        if (!state.shouldSkip(unifiedCount, splitCount)) {
          const iterationRanges = getChangeIterationRanges(state, content, diffStyle);
          if ((((_a9 = iterationRanges[0]) == null ? void 0 : _a9[0]) ?? 0) > 0) consumePendingCollapsed();
          for (const [rangeStart, rangeEnd] of iterationRanges) for (let index = rangeStart; index < rangeEnd; index++) {
            const collapsedAfter = getTrailingCollapsedAfter(unifiedLineIndex + index, diffStyle === "unified" ? splitLineIndex + (index < content.deletions ? index : index - content.deletions) : splitLineIndex + index);
            if (state.emit(getChangeLineData({
              hunkIndex,
              hunk,
              collapsedBefore: consumePendingCollapsed(),
              collapsedAfter,
              diffStyle,
              index,
              unifiedLineIndex,
              splitLineIndex,
              additionLineIndex,
              deletionLineIndex,
              additionLineNumber,
              deletionLineNumber,
              content,
              isLastContent,
              unifiedCount,
              splitCount
            }), true)) break hunkIterator;
          }
        }
        consumePendingCollapsed();
        state.incrementCounts(unifiedCount, splitCount);
        unifiedLineIndex += unifiedCount;
        splitLineIndex += splitCount;
        deletionLineIndex += content.deletions;
        additionLineIndex += content.additions;
        deletionLineNumber += content.deletions;
        additionLineNumber += content.additions;
      }
    }
    if (trailingRegion != null) {
      const { collapsedLines, fromStart, fromEnd } = trailingRegion;
      const len = fromStart + fromEnd;
      if (walkContextLines(state, len, diffStyle, (index) => {
        const isLastLine = index === len - 1;
        return state.emit({
          hunkIndex: diff.hunks.length,
          hunk: void 0,
          collapsedBefore: 0,
          collapsedAfter: isLastLine ? collapsedLines : 0,
          type: "context-expanded",
          deletionLine: {
            lineNumber: deletionLineNumber + index,
            lineIndex: deletionLineIndex + index,
            noEOFCR: false,
            unifiedLineIndex: unifiedLineIndex + index,
            splitLineIndex: splitLineIndex + index
          },
          additionLine: {
            unifiedLineIndex: unifiedLineIndex + index,
            splitLineIndex: splitLineIndex + index,
            lineIndex: additionLineIndex + index,
            lineNumber: additionLineNumber + index,
            noEOFCR: false
          }
        });
      }, void 0, () => state.shouldBreak())) break hunkIterator;
    }
  }
}
function getIterationStartState({ diff, diffStyle, startingLine, expandedHunks, collapsedContextThreshold }) {
  if (startingLine <= 0 || diffStyle === "both") return {
    hunkIndex: 0,
    splitCount: 0,
    unifiedCount: 0
  };
  const prefixCounts = getHunkPrefixCounts({
    diff,
    expandedHunks,
    collapsedContextThreshold
  });
  let low = 0;
  let high = diff.hunks.length - 1;
  let result = diff.hunks.length;
  while (low <= high) {
    const mid = low + high >> 1;
    const counts2 = prefixCounts[mid + 1];
    if (counts2 == null) throw new Error("iterateOverDiff: invalid hunk prefix index");
    if ((diffStyle === "unified" ? counts2.unifiedCount : counts2.splitCount) > startingLine) {
      result = mid;
      high = mid - 1;
    } else low = mid + 1;
  }
  if (result >= diff.hunks.length) {
    const counts2 = prefixCounts[diff.hunks.length];
    if (counts2 == null) throw new Error("iterateOverDiff: invalid terminal hunk prefix index");
    return {
      hunkIndex: diff.hunks.length,
      splitCount: counts2.splitCount,
      unifiedCount: counts2.unifiedCount
    };
  }
  const counts = prefixCounts[result];
  if (counts == null) throw new Error("iterateOverDiff: invalid selected hunk prefix index");
  return {
    hunkIndex: result,
    splitCount: counts.splitCount,
    unifiedCount: counts.unifiedCount
  };
}
function getHunkPrefixCounts({ diff, expandedHunks, collapsedContextThreshold }) {
  let splitCount = 0;
  let unifiedCount = 0;
  const finalHunkIndex = diff.hunks.length - 1;
  const prefixCounts = [{
    splitCount: 0,
    unifiedCount: 0
  }];
  for (let index = 0; index < diff.hunks.length; index++) {
    const hunk = diff.hunks[index];
    if (hunk == null) throw new Error("iterateOverDiff: invalid hunk summary index");
    const leadingRegion = getExpandedRegion({
      isPartial: diff.isPartial,
      rangeSize: hunk.collapsedBefore,
      expandedHunks,
      hunkIndex: index,
      collapsedContextThreshold
    });
    const leadingCount = leadingRegion.fromStart + leadingRegion.fromEnd;
    splitCount += leadingCount + hunk.splitLineCount;
    unifiedCount += leadingCount + hunk.unifiedLineCount;
    const trailingRegion = index === finalHunkIndex ? getTrailingExpandedRegion({
      fileDiff: diff,
      hunkIndex: index,
      expandedHunks,
      collapsedContextThreshold,
      errorPrefix: "iterateOverDiff"
    }) : void 0;
    if (trailingRegion != null) {
      const trailingCount = trailingRegion.fromStart + trailingRegion.fromEnd;
      splitCount += trailingCount;
      unifiedCount += trailingCount;
    }
    prefixCounts.push({
      splitCount,
      unifiedCount
    });
  }
  return prefixCounts;
}
function getContextLineIterationBounds(state, count, diffStyle) {
  if (!state.isWindowedHighlight || count <= 0) return [0, count];
  const ranges = [];
  function pushRange(currentCount) {
    const start2 = Math.max(0, state.viewportStart - currentCount);
    const end2 = Math.min(count, state.viewportEnd - currentCount);
    if (end2 > start2) ranges.push([start2, end2]);
  }
  if (diffStyle !== "split") pushRange(state.unifiedCount);
  if (diffStyle !== "unified") pushRange(state.splitCount);
  if (ranges.length === 0) return [0, 0];
  let start = ranges[0][0];
  let end = ranges[0][1];
  for (let index = 1; index < ranges.length; index++) {
    const range2 = ranges[index];
    start = Math.min(start, range2[0]);
    end = Math.max(end, range2[1]);
  }
  return [start, end];
}
function walkContextLines(state, count, diffStyle, callback, onSkippedStart, shouldBreak) {
  const [startIndex, endIndex] = getContextLineIterationBounds(state, count, diffStyle);
  if (startIndex > 0) {
    state.incrementCounts(startIndex, startIndex);
    onSkippedStart == null ? void 0 : onSkippedStart();
  }
  let index = startIndex;
  while (index < count) {
    if ((shouldBreak == null ? void 0 : shouldBreak()) === true) return true;
    if (index >= endIndex) {
      state.incrementCounts(count - index, count - index);
      break;
    }
    if (state.isInWindow(0, 0)) {
      if (callback(index) === true) return true;
    } else state.incrementCounts(1, 1);
    index++;
  }
  return false;
}
function getChangeIterationRanges(state, content, diffStyle) {
  if (!state.isWindowedHighlight) return [[0, diffStyle === "unified" ? content.deletions + content.additions : Math.max(content.deletions, content.additions)]];
  const useUnified = diffStyle !== "split";
  const useSplit = diffStyle !== "unified";
  const iterationSpace = diffStyle === "unified" ? "unified" : "split";
  const iterationRanges = [];
  function getVisibleRange(start, count) {
    if (start + count <= state.viewportStart || start >= state.viewportEnd) return;
    const visibleStart = Math.max(0, state.viewportStart - start);
    const visibleEnd = Math.min(count, state.viewportEnd - start);
    return visibleEnd > visibleStart ? [visibleStart, visibleEnd] : void 0;
  }
  function mapRangeToIteration(range2, kind) {
    if (iterationSpace === "split") return range2;
    return kind === "additions" ? [range2[0] + content.deletions, range2[1] + content.deletions] : range2;
  }
  function pushRange(range2, kind) {
    if (range2 == null) return;
    const [start, end] = mapRangeToIteration(range2, kind);
    if (end > start) iterationRanges.push([start, end]);
  }
  if (useUnified) {
    pushRange(getVisibleRange(state.unifiedCount, content.deletions), "deletions");
    pushRange(getVisibleRange(state.unifiedCount + content.deletions, content.additions), "additions");
  }
  if (useSplit) {
    pushRange(getVisibleRange(state.splitCount, content.deletions), "deletions");
    pushRange(getVisibleRange(state.splitCount, content.additions), "additions");
  }
  if (iterationRanges.length === 0) return iterationRanges;
  iterationRanges.sort((a2, b3) => a2[0] - b3[0]);
  const merged = [iterationRanges[0]];
  for (const [start, end] of iterationRanges.slice(1)) {
    const last = merged[merged.length - 1];
    if (start <= last[1]) last[1] = Math.max(last[1], end);
    else merged.push([start, end]);
  }
  return merged;
}
function getChangeLineData({ hunkIndex, hunk, collapsedAfter, collapsedBefore, diffStyle, index, unifiedLineIndex, splitLineIndex, additionLineIndex, deletionLineIndex, additionLineNumber, deletionLineNumber, content, isLastContent, unifiedCount, splitCount }) {
  const unifiedDeletionLineIndex = index < content.deletions ? unifiedLineIndex + index : void 0;
  const unifiedAdditionLineIndex = diffStyle === "unified" ? index >= content.deletions ? unifiedLineIndex + index : void 0 : index < content.additions ? unifiedLineIndex + content.deletions + index : void 0;
  const resolvedSplitLineIndex = diffStyle === "unified" ? splitLineIndex + (index < content.deletions ? index : index - content.deletions) : splitLineIndex + index;
  const deletionLineIndexValue = index < content.deletions ? deletionLineIndex + index : void 0;
  const deletionLineNumberValue = index < content.deletions ? deletionLineNumber + index : void 0;
  const additionLineIndexValue = diffStyle === "unified" ? index >= content.deletions ? additionLineIndex + (index - content.deletions) : void 0 : index < content.additions ? additionLineIndex + index : void 0;
  const additionLineNumberValue = diffStyle === "unified" ? index >= content.deletions ? additionLineNumber + (index - content.deletions) : void 0 : index < content.additions ? additionLineNumber + index : void 0;
  const noEOFCRDeletion = diffStyle === "unified" ? isLastContent && index === content.deletions - 1 && hunk.noEOFCRDeletions : isLastContent && index === splitCount - 1 && hunk.noEOFCRDeletions;
  const noEOFCRAddition = diffStyle === "unified" ? isLastContent && index === unifiedCount - 1 && hunk.noEOFCRAdditions : isLastContent && index === splitCount - 1 && hunk.noEOFCRAdditions;
  const deletionLine = deletionLineIndexValue != null && deletionLineNumberValue != null && unifiedDeletionLineIndex != null ? {
    lineNumber: deletionLineNumberValue,
    lineIndex: deletionLineIndexValue,
    noEOFCR: noEOFCRDeletion,
    unifiedLineIndex: unifiedDeletionLineIndex,
    splitLineIndex: resolvedSplitLineIndex
  } : void 0;
  const additionLine = additionLineIndexValue != null && additionLineNumberValue != null && unifiedAdditionLineIndex != null ? {
    unifiedLineIndex: unifiedAdditionLineIndex,
    splitLineIndex: resolvedSplitLineIndex,
    lineIndex: additionLineIndexValue,
    lineNumber: additionLineNumberValue,
    noEOFCR: noEOFCRAddition
  } : void 0;
  if (deletionLine == null && additionLine != null) return {
    type: "change",
    hunkIndex,
    hunk,
    collapsedAfter,
    collapsedBefore,
    deletionLine: void 0,
    additionLine
  };
  else if (deletionLine != null && additionLine == null) return {
    type: "change",
    hunkIndex,
    hunk,
    collapsedAfter,
    collapsedBefore,
    deletionLine,
    additionLine: void 0
  };
  if (deletionLine == null || additionLine == null) throw new Error("iterateOverDiff: missing change line data");
  return {
    type: "change",
    hunkIndex,
    hunk,
    collapsedAfter,
    collapsedBefore,
    deletionLine,
    additionLine
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/renderDiffWithHighlighter.js
var DEFAULT_PLAIN_TEXT_OPTIONS2 = { forcePlainText: false };
function renderDiffWithHighlighter(diff, highlighter2, options, { forcePlainText, startingLine, totalLines, expandedHunks, collapsedContextThreshold = 1 } = DEFAULT_PLAIN_TEXT_OPTIONS2) {
  if (forcePlainText) {
    startingLine ?? (startingLine = 0);
    totalLines ?? (totalLines = Infinity);
  } else {
    startingLine = 0;
    totalLines = Infinity;
  }
  const isWindowedHighlight = startingLine > 0 || totalLines < Infinity;
  const baseThemeType = typeof options.theme === "string" ? highlighter2.getTheme(options.theme).type : void 0;
  const themeStyles = getHighlighterThemeStyles({
    theme: options.theme,
    highlighter: highlighter2
  });
  const lineDiffType = forcePlainText && !isWindowedHighlight && (diff.unifiedLineCount > 1e3 || diff.splitLineCount > 1e3) ? "none" : options.lineDiffType;
  const code = {
    deletionLines: [],
    additionLines: []
  };
  const { maxLineDiffLength } = options;
  const shouldGroupAll = !forcePlainText && !diff.isPartial;
  const expandedHunksForIteration = forcePlainText ? expandedHunks : void 0;
  const buckets = /* @__PURE__ */ new Map();
  function getBucketForHunk(hunkIndex) {
    const index = shouldGroupAll ? 0 : hunkIndex;
    const bucket = buckets.get(index) ?? createBucket();
    buckets.set(index, bucket);
    return bucket;
  }
  function appendContent(lineContent, lineIndex, segments, contentWrapper) {
    if (isWindowedHighlight) {
      let segment2 = segments.at(-1);
      if (segment2 == null || segment2.targetIndex + segment2.count !== lineIndex) {
        segment2 = {
          targetIndex: lineIndex,
          originalOffset: contentWrapper.length,
          count: 0
        };
        segments.push(segment2);
      }
      segment2.count++;
    }
    contentWrapper.push(lineContent);
  }
  iterateOverDiff({
    diff,
    diffStyle: "both",
    startingLine,
    totalLines,
    expandedHunks: isWindowedHighlight ? expandedHunksForIteration : true,
    collapsedContextThreshold,
    callback: ({ hunkIndex, additionLine, deletionLine, type }) => {
      const bucket = getBucketForHunk(hunkIndex);
      const splitLineIndex = additionLine != null ? additionLine.splitLineIndex : deletionLine.splitLineIndex;
      if (type === "change" && additionLine != null && deletionLine != null) computeLineDiffDecorations({
        additionLine: diff.additionLines[additionLine.lineIndex],
        deletionLine: diff.deletionLines[deletionLine.lineIndex],
        deletionLineIndex: bucket.deletionContent.length,
        additionLineIndex: bucket.additionContent.length,
        deletionDecorations: bucket.deletionDecorations,
        additionDecorations: bucket.additionDecorations,
        lineDiffType,
        maxLineDiffLength
      });
      if (deletionLine != null) {
        appendContent(diff.deletionLines[deletionLine.lineIndex], deletionLine.lineIndex, bucket.deletionSegments, bucket.deletionContent);
        bucket.deletionInfo.push({
          type: type === "change" ? "change-deletion" : type,
          lineNumber: deletionLine.lineNumber,
          altLineNumber: type === "change" ? void 0 : additionLine.lineNumber ?? void 0,
          lineIndex: `${deletionLine.unifiedLineIndex},${splitLineIndex}`
        });
      }
      if (additionLine != null) {
        appendContent(diff.additionLines[additionLine.lineIndex], additionLine.lineIndex, bucket.additionSegments, bucket.additionContent);
        bucket.additionInfo.push({
          type: type === "change" ? "change-addition" : type,
          lineNumber: additionLine.lineNumber,
          altLineNumber: type === "change" ? void 0 : deletionLine.lineNumber ?? void 0,
          lineIndex: `${additionLine.unifiedLineIndex},${splitLineIndex}`
        });
      }
    }
  });
  for (const bucket of buckets.values()) {
    if (bucket.deletionContent.length === 0 && bucket.additionContent.length === 0) continue;
    const deletionFile = {
      name: diff.prevName ?? diff.name,
      contents: bucket.deletionContent.value
    };
    const additionFile = {
      name: diff.name,
      contents: bucket.additionContent.value
    };
    const { deletionLines, additionLines } = renderTwoFiles({
      deletionFile,
      deletionInfo: bucket.deletionInfo,
      deletionDecorations: bucket.deletionDecorations,
      additionFile,
      additionInfo: bucket.additionInfo,
      additionDecorations: bucket.additionDecorations,
      highlighter: highlighter2,
      options,
      languageOverride: forcePlainText ? "text" : diff.lang
    });
    if (shouldGroupAll) {
      code.deletionLines = deletionLines;
      code.additionLines = additionLines;
      continue;
    }
    if (bucket.deletionSegments.length > 0) for (const seg of bucket.deletionSegments) for (let i2 = 0; i2 < seg.count; i2++) code.deletionLines[seg.targetIndex + i2] = deletionLines[seg.originalOffset + i2];
    else code.deletionLines.push(...deletionLines);
    if (bucket.additionSegments.length > 0) for (const seg of bucket.additionSegments) for (let i2 = 0; i2 < seg.count; i2++) code.additionLines[seg.targetIndex + i2] = additionLines[seg.originalOffset + i2];
    else code.additionLines.push(...additionLines);
  }
  return {
    code,
    themeStyles,
    baseThemeType
  };
}
function computeLineDiffDecorations({ deletionLine, additionLine, deletionLineIndex, additionLineIndex, deletionDecorations, additionDecorations, lineDiffType, maxLineDiffLength }) {
  if (deletionLine == null || additionLine == null || lineDiffType === "none") return;
  deletionLine = cleanLastNewline(deletionLine);
  additionLine = cleanLastNewline(additionLine);
  if (deletionLine.length > maxLineDiffLength || additionLine.length > maxLineDiffLength) return;
  const lineDiff2 = lineDiffType === "char" ? diffChars(deletionLine, additionLine) : diffWordsWithSpace(deletionLine, additionLine);
  const deletionSpans = [];
  const additionSpans = [];
  const enableJoin = lineDiffType === "word-alt";
  const lastItem = lineDiff2.at(-1);
  for (const item of lineDiff2) {
    const isLastItem = item === lastItem;
    if (!item.added && !item.removed) {
      pushOrJoinSpan({
        item,
        arr: deletionSpans,
        enableJoin,
        isNeutral: true,
        isLastItem
      });
      pushOrJoinSpan({
        item,
        arr: additionSpans,
        enableJoin,
        isNeutral: true,
        isLastItem
      });
    } else if (item.removed) pushOrJoinSpan({
      item,
      arr: deletionSpans,
      enableJoin,
      isLastItem
    });
    else pushOrJoinSpan({
      item,
      arr: additionSpans,
      enableJoin,
      isLastItem
    });
  }
  let spanIndex = 0;
  for (const span of deletionSpans) {
    if (span[0] === 1) deletionDecorations.push(createDiffSpanDecoration({
      line: deletionLineIndex,
      spanStart: spanIndex,
      spanLength: span[1].length
    }));
    spanIndex += span[1].length;
  }
  spanIndex = 0;
  for (const span of additionSpans) {
    if (span[0] === 1) additionDecorations.push(createDiffSpanDecoration({
      line: additionLineIndex,
      spanStart: spanIndex,
      spanLength: span[1].length
    }));
    spanIndex += span[1].length;
  }
}
function createBucket() {
  return {
    deletionContent: {
      push(value) {
        this.value += value;
        this.length++;
      },
      value: "",
      length: 0
    },
    additionContent: {
      push(value) {
        this.value += value;
        this.length++;
      },
      value: "",
      length: 0
    },
    deletionInfo: [],
    additionInfo: [],
    deletionDecorations: [],
    additionDecorations: [],
    deletionSegments: [],
    additionSegments: []
  };
}
function renderTwoFiles({ deletionFile, additionFile, deletionInfo, additionInfo, highlighter: highlighter2, deletionDecorations, additionDecorations, languageOverride, options: { theme: themeOrThemes = DEFAULT_THEMES, ...options } }) {
  const deletionLang = languageOverride ?? getFiletypeFromFileName(deletionFile.name);
  const additionLang = languageOverride ?? getFiletypeFromFileName(additionFile.name);
  const { state, transformers } = createTransformerWithState(options.useTokenTransformer);
  const hastConfig = (() => {
    return typeof themeOrThemes === "string" ? {
      ...options,
      lang: "text",
      theme: themeOrThemes,
      transformers,
      decorations: void 0,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeTimeLimit: 0
    } : {
      ...options,
      lang: "text",
      themes: themeOrThemes,
      transformers,
      decorations: void 0,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeTimeLimit: 0
    };
  })();
  return {
    deletionLines: (() => {
      if (deletionFile.contents === "") return [];
      hastConfig.lang = deletionLang;
      state.lineInfo = deletionInfo;
      hastConfig.decorations = deletionDecorations;
      return getLineNodes(highlighter2.codeToHast(cleanLastNewline(deletionFile.contents), hastConfig));
    })(),
    additionLines: (() => {
      if (additionFile.contents === "") return [];
      hastConfig.lang = additionLang;
      hastConfig.decorations = additionDecorations;
      state.lineInfo = additionInfo;
      return getLineNodes(highlighter2.codeToHast(cleanLastNewline(additionFile.contents), hastConfig));
    })()
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/updateDiffHunks.js
function recomputeDiffHunks(diff, parseDiffOptions) {
  const recomputed = parseDiffFromFile({
    name: diff.prevName ?? diff.name,
    contents: diff.deletionLines.join("")
  }, {
    name: diff.name,
    contents: diff.additionLines.join(""),
    lang: diff.lang
  }, parseDiffOptions);
  return {
    hunks: recomputed.hunks,
    splitLineCount: recomputed.splitLineCount,
    unifiedLineCount: recomputed.unifiedLineCount,
    additionLines: recomputed.additionLines,
    deletionLines: recomputed.deletionLines,
    type: recomputed.type
  };
}
function buildTopAlignedAdditionSentinel(lineCount, deletionContents) {
  const count = Math.max(lineCount, 1);
  let sentinel = Array.from({ length: count }, (_3, index) => `${" ".repeat(index + 1)}
`).join("");
  if (sentinel === deletionContents) sentinel = Array.from({ length: count }, (_3, index) => `\0${" ".repeat(index)}
`).join("");
  return sentinel;
}
function hasOnlyBlankAdditionContents(additionLines) {
  for (const line of additionLines) if (line.trim().length > 0) return false;
  return true;
}
function shouldTopAlignAdditionRecompute(diff, additionLines) {
  return additionLines.length > 0 && additionLines.length < diff.deletionLines.length && hasOnlyBlankAdditionContents(additionLines);
}
function recomputeTopAlignedAdditionDiff(diff, additionLines, parseDiffOptions) {
  const deletionContents = diff.deletionLines.join("");
  const additionSentinel = buildTopAlignedAdditionSentinel(additionLines.length, deletionContents);
  const recomputed = parseDiffFromFile({
    name: diff.prevName ?? diff.name,
    contents: deletionContents
  }, {
    name: diff.name,
    contents: additionSentinel,
    lang: diff.lang
  }, parseDiffOptions);
  return {
    hunks: recomputed.hunks,
    splitLineCount: recomputed.splitLineCount,
    unifiedLineCount: recomputed.unifiedLineCount,
    additionLines,
    deletionLines: recomputed.deletionLines,
    type: recomputed.type
  };
}
function recomputeEmptyDocumentDiff(diff, parseDiffOptions) {
  return recomputeTopAlignedAdditionDiff(diff, [""], parseDiffOptions);
}
function recomputeDiffHunksForEdit(diff, parseDiffOptions) {
  if (diff.additionLines.length === 0) return recomputeEmptyDocumentDiff(diff, parseDiffOptions);
  if (shouldTopAlignAdditionRecompute(diff, diff.additionLines)) return recomputeTopAlignedAdditionDiff(diff, diff.additionLines, parseDiffOptions);
  const additionLines = diff.additionLines;
  const recomputed = recomputeDiffHunks(diff, parseDiffOptions);
  preserveTrailingEditorBlankLine(recomputed, additionLines);
  return recomputed;
}
function hasTrailingEditorBlankLine(additionLines) {
  return additionLines.length > 1 && additionLines.at(-1) === "";
}
function preserveTrailingEditorBlankLine(recomputed, additionLines) {
  if (!hasTrailingEditorBlankLine(additionLines)) return;
  const extraLineCount = additionLines.length - recomputed.additionLines.length;
  if (extraLineCount <= 0) return;
  const extraAdditionLineIndex = recomputed.additionLines.length;
  const lastHunk = recomputed.hunks.at(-1);
  if (lastHunk == null) return;
  if (getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount) !== extraAdditionLineIndex) return;
  for (const content of lastHunk.hunkContent) if (content.type === "change" && content.additions < content.deletions && content.additionLineIndex + content.additions === extraAdditionLineIndex) {
    recomputed.additionLines = additionLines;
    content.additions += extraLineCount;
    lastHunk.additionCount += extraLineCount;
    lastHunk.additionLines += extraLineCount;
    recomputeDiffRenderLineCounts(recomputed);
    return;
  }
}
function updateDiffHunks(diff, changedAdditionLineIndexes, parseDiffOptions) {
  if (diff.isPartial) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  if (diff.deletionLines.length !== diff.additionLines.length) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  const changedLines = Array.from(changedAdditionLineIndexes);
  if (changedLines.length === 0) return applyHunkUpdateResult(diff, {
    hunks: diff.hunks,
    splitLineCount: diff.splitLineCount,
    unifiedLineCount: diff.unifiedLineCount,
    type: diff.type
  });
  for (const line of changedLines) {
    const additionLine = diff.additionLines[line];
    const deletionLine = diff.deletionLines[line];
    if (additionLine == null || deletionLine == null) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
    if (cleanLastNewline(additionLine) === cleanLastNewline(deletionLine)) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  }
  const affectedHunkIndexes = getAffectedHunkIndexes(diff, changedLines);
  if (affectedHunkIndexes.size === 0) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  for (const hunkIndex of affectedHunkIndexes) if (!reparseHunkRegion(diff, hunkIndex, parseDiffOptions)) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  recomputeDiffRenderLineCounts(diff);
  if (hasTrailingContextMismatch(diff)) return applyHunkUpdateResult(diff, recomputeDiffHunks(diff, parseDiffOptions));
  return applyHunkUpdateResult(diff, {
    hunks: diff.hunks,
    splitLineCount: diff.splitLineCount,
    unifiedLineCount: diff.unifiedLineCount,
    type: diff.type
  });
}
function applyHunkUpdateResult(diff, result) {
  Object.assign(diff, result);
  return result;
}
function getAffectedHunkIndexes(diff, changedAdditionLineIndexes) {
  const indexes = /* @__PURE__ */ new Set();
  for (const line of changedAdditionLineIndexes) {
    const hunkIndex = findHunkIndexForAdditionLine(diff, line);
    if (hunkIndex == null) return /* @__PURE__ */ new Set();
    indexes.add(hunkIndex);
  }
  return indexes;
}
function findHunkIndexForAdditionLine(diff, line) {
  for (const [hunkIndex, hunk] of diff.hunks.entries()) {
    const end = hunk.additionLineIndex + hunk.additionCount;
    if (line >= hunk.additionLineIndex && line < end) return hunkIndex;
  }
}
function reparseHunkRegion(diff, hunkIndex, parseDiffOptions) {
  const hunk = diff.hunks[hunkIndex];
  if (hunk == null) return false;
  const deletionSlice = diff.deletionLines.slice(hunk.deletionLineIndex, hunk.deletionLineIndex + hunk.deletionCount);
  const additionSlice = diff.additionLines.slice(hunk.additionLineIndex, hunk.additionLineIndex + hunk.additionCount);
  const reparsed = parseDiffFromFile({
    name: diff.prevName ?? diff.name,
    contents: deletionSlice.join("")
  }, {
    name: diff.name,
    contents: additionSlice.join(""),
    lang: diff.lang
  }, {
    ...parseDiffOptions,
    context: 0
  });
  const reparsedHunk = reparsed.hunks[0];
  if (reparsedHunk == null || reparsed.hunks.length !== 1) return false;
  applyReparsedHunk(hunk, reparsedHunk);
  syncHunkNoEOFCRFromFullFile(diff, hunkIndex);
  return true;
}
function syncHunkNoEOFCRFromFullFile(diff, hunkIndex) {
  const hunk = diff.hunks[hunkIndex];
  if (hunk == null) return;
  if (!(hunkIndex === diff.hunks.length - 1)) {
    hunk.noEOFCRAdditions = false;
    hunk.noEOFCRDeletions = false;
    return;
  }
  const lastAdditionLine = diff.additionLines.at(-1);
  const lastDeletionLine = diff.deletionLines.at(-1);
  hunk.noEOFCRAdditions = lastAdditionLine != null && lastAdditionLine !== "" && !lastAdditionLine.endsWith("\n");
  hunk.noEOFCRDeletions = lastDeletionLine != null && lastDeletionLine !== "" && !lastDeletionLine.endsWith("\n");
}
function applyReparsedHunk(target, parsed) {
  const additionOffset = target.additionLineIndex;
  const deletionOffset = target.deletionLineIndex;
  target.hunkContent = parsed.hunkContent.map((content) => offsetHunkContent(content, additionOffset, deletionOffset));
  target.additionLineIndex = additionOffset + parsed.additionLineIndex;
  target.additionStart = target.additionStart + parsed.additionLineIndex;
  target.additionCount = parsed.additionCount;
  target.additionLines = parsed.additionLines;
  if (parsed.deletionLineIndex >= 0) {
    target.deletionLineIndex = deletionOffset + parsed.deletionLineIndex;
    target.deletionStart = target.deletionStart + parsed.deletionLineIndex;
  }
  target.deletionCount = parsed.deletionCount;
  target.deletionLines = parsed.deletionLines;
  target.noEOFCRAdditions = parsed.noEOFCRAdditions;
  target.noEOFCRDeletions = parsed.noEOFCRDeletions;
  recomputeHunkRenderLineCounts(target);
}
function offsetHunkContent(content, additionOffset, deletionOffset) {
  return {
    ...content,
    additionLineIndex: content.additionLineIndex + additionOffset,
    deletionLineIndex: content.deletionLineIndex + deletionOffset
  };
}
function recomputeHunkRenderLineCounts(hunk) {
  let splitLineCount = 0;
  let unifiedLineCount = 0;
  for (const content of hunk.hunkContent) if (content.type === "context") {
    splitLineCount += content.lines;
    unifiedLineCount += content.lines;
  } else {
    splitLineCount += Math.max(content.additions, content.deletions);
    unifiedLineCount += content.additions + content.deletions;
  }
  hunk.splitLineCount = splitLineCount;
  hunk.unifiedLineCount = unifiedLineCount;
}
function recomputeDiffRenderLineCounts(diff) {
  let splitTotal = 0;
  let unifiedTotal = 0;
  let lastHunkAdditionEnd = 0;
  for (const hunk of diff.hunks) {
    hunk.collapsedBefore = Math.max(getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount) - lastHunkAdditionEnd, 0);
    hunk.splitLineStart = splitTotal + hunk.collapsedBefore;
    hunk.unifiedLineStart = unifiedTotal + hunk.collapsedBefore;
    recomputeHunkRenderLineCounts(hunk);
    splitTotal += hunk.collapsedBefore + hunk.splitLineCount;
    unifiedTotal += hunk.collapsedBefore + hunk.unifiedLineCount;
    lastHunkAdditionEnd = getHunkSideEndBoundary(hunk.additionStart, hunk.additionCount);
  }
  if (diff.hunks.length > 0) {
    const lastHunk = diff.hunks[diff.hunks.length - 1];
    const collapsedAfter = Math.max(diff.additionLines.length - getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount), 0);
    splitTotal += collapsedAfter;
    unifiedTotal += collapsedAfter;
  }
  diff.splitLineCount = splitTotal;
  diff.unifiedLineCount = unifiedTotal;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/editSessionHunks.js
var deletionLineSetCache = /* @__PURE__ */ new WeakMap();
function normalizeEditorLines(lines) {
  if (lines.length > 1 && lines[lines.length - 1] === "") return lines.slice(0, -1);
  return lines;
}
function findDivergenceCore(deletionLines, additionLines) {
  const maxStart = Math.min(deletionLines.length, additionLines.length);
  let start = 0;
  while (start < maxStart && deletionLines[start] === additionLines[start]) start++;
  let deletionEnd = deletionLines.length;
  let additionEnd = additionLines.length;
  while (deletionEnd > start && additionEnd > start && deletionLines[deletionEnd - 1] === additionLines[additionEnd - 1]) {
    deletionEnd--;
    additionEnd--;
  }
  if (start === deletionEnd && start === additionEnd) return;
  return {
    start,
    deletionEnd,
    additionEnd
  };
}
function rebuildSessionHunks(diff, parseDiffOptions) {
  const previousHunks = diff.hunks;
  const editorAdditionLines = diff.additionLines;
  const canonicalAdditionLines = normalizeEditorLines(editorAdditionLines);
  const canonicalDiff = canonicalAdditionLines === editorAdditionLines ? diff : {
    ...diff,
    additionLines: canonicalAdditionLines
  };
  const plans = buildRegionPlans(previousHunks, parseCanonicalChangeBlocks(canonicalDiff, parseDiffOptions), diff.deletionLines.length);
  const nextHunks = buildRegionHunks(canonicalDiff, plans);
  diff.additionLines = canonicalAdditionLines;
  diff.hunks = nextHunks;
  diff.editSessionDirty = true;
  finalizeSessionHunks(diff);
  preserveTrailingEditorBlankLine(diff, editorAdditionLines);
  if (!hasRegionOrSplitLayoutChanged(previousHunks, diff.hunks)) return;
  return { regions: plans.map((plan) => plan.previousSpan) };
}
function applySessionChangedLines(diff, changedAdditionLineIndexes, parseDiffOptions, previousAdditionLines) {
  const lines = Array.from(new Set(changedAdditionLineIndexes)).filter((line) => line >= 0 && line < diff.additionLines.length).sort((a2, b3) => a2 - b3);
  if (lines.length === 0) return;
  const { hunks } = diff;
  let regionIndex;
  let hunkIndex = 0;
  for (const line of lines) {
    while (hunkIndex < hunks.length) {
      const hunk2 = hunks[hunkIndex];
      if (line < getHunkAdditionStart(hunk2) + hunk2.additionCount) break;
      hunkIndex++;
    }
    const hunk = hunks[hunkIndex];
    const start = hunk == null ? void 0 : getHunkAdditionStart(hunk);
    if (start == null || line < start || regionIndex != null && regionIndex !== hunkIndex) return rebuildSessionHunks(diff, parseDiffOptions);
    regionIndex = hunkIndex;
  }
  if (regionIndex == null) return;
  if (canRetainCanonicalBlocks(diff, lines, regionIndex, previousAdditionLines, parseDiffOptions)) {
    diff.editSessionDirty = true;
    return;
  }
  return rebuildSessionHunks(diff, parseDiffOptions);
}
function canRetainCanonicalBlocks(diff, changedLines, regionIndex, previousAdditionLines, parseDiffOptions) {
  if (previousAdditionLines == null || (parseDiffOptions == null ? void 0 : parseDiffOptions.ignoreWhitespace) === true || (parseDiffOptions == null ? void 0 : parseDiffOptions.stripTrailingCr) === true) return false;
  const deletionLineSet = getDeletionLineSet(diff);
  const hunk = diff.hunks[regionIndex];
  if (hunk.hunkContent.some(isPureChange)) return false;
  for (const line of changedLines) {
    const previousLine = previousAdditionLines.get(line);
    const additionLine = diff.additionLines[line];
    if (previousLine == null || additionLine == null || deletionLineSet.has(previousLine) || deletionLineSet.has(additionLine)) return false;
    let insideBalancedChange = false;
    for (const content of hunk.hunkContent) if (content.type === "change" && content.additions === content.deletions && line >= content.additionLineIndex && line < content.additionLineIndex + content.additions) {
      insideBalancedChange = true;
      break;
    }
    if (!insideBalancedChange) return false;
  }
  return true;
}
function getDeletionLineSet(diff) {
  const cached = deletionLineSetCache.get(diff);
  if ((cached == null ? void 0 : cached.lines) === diff.deletionLines) return cached.set;
  const set = new Set(diff.deletionLines);
  deletionLineSetCache.set(diff, {
    lines: diff.deletionLines,
    set
  });
  return set;
}
function isPureChange(content) {
  return (content == null ? void 0 : content.type) === "change" && (content.additions === 0 || content.deletions === 0);
}
function remapExpandedHunksForRegionChange(expandedHunks, change) {
  const remapped = /* @__PURE__ */ new Map();
  const { regions } = change;
  for (let key2 = 0; key2 <= regions.length; key2++) {
    const previous = regions[key2 - 1];
    const next = regions[key2];
    const fromStartSource = key2 === 0 ? expandedHunks.get(0) : previous == null ? void 0 : expandedHunks.get(previous.lastIndex + 1);
    const fromEndSource = next == null ? void 0 : expandedHunks.get(next.firstIndex);
    const fromStart = (fromStartSource == null ? void 0 : fromStartSource.fromStart) ?? 0;
    const fromEnd = (fromEndSource == null ? void 0 : fromEndSource.fromEnd) ?? 0;
    if (fromStart > 0 || fromEnd > 0) remapped.set(key2, {
      fromStart,
      fromEnd
    });
  }
  return remapped;
}
function captureExpansionAnchors(diff, expandedHunks, collapsedContextThreshold) {
  const anchors = [];
  if (diff.isPartial) return anchors;
  for (const [hunkIndex, hunk] of diff.hunks.entries()) {
    const region = getExpandedRegion({
      isPartial: diff.isPartial,
      rangeSize: hunk.collapsedBefore,
      expandedHunks,
      hunkIndex,
      collapsedContextThreshold
    });
    if (region.rangeSize <= collapsedContextThreshold) continue;
    const gapEnd = getHunkDeletionStart(hunk);
    const gapStart = gapEnd - region.rangeSize;
    if (region.fromStart > 0) anchors.push([gapStart, gapStart + region.fromStart]);
    if (region.fromEnd > 0) anchors.push([gapEnd - region.fromEnd, gapEnd]);
  }
  const trailingRegion = getTrailingExpandedRegion({
    fileDiff: diff,
    hunkIndex: diff.hunks.length - 1,
    expandedHunks,
    collapsedContextThreshold,
    errorPrefix: "captureExpansionAnchors"
  });
  if (trailingRegion != null && trailingRegion.fromStart > 0 && trailingRegion.rangeSize > collapsedContextThreshold) {
    const lastHunk = diff.hunks[diff.hunks.length - 1];
    const gapStart = getHunkDeletionStart(lastHunk) + lastHunk.deletionCount;
    anchors.push([gapStart, gapStart + trailingRegion.fromStart]);
  }
  return anchors;
}
function rebuildExpansionFromAnchors(diff, anchors) {
  const rebuilt = /* @__PURE__ */ new Map();
  if (anchors.length === 0) return rebuilt;
  const applyGap = (key2, gapStart, gapEnd) => {
    if (gapEnd <= gapStart) return;
    let fromStart = 0;
    let fromEnd = 0;
    for (const [start, end] of anchors) {
      if (end <= gapStart || start >= gapEnd) continue;
      if (start <= gapStart) fromStart = Math.max(fromStart, Math.min(end, gapEnd) - gapStart);
      if (end >= gapEnd) fromEnd = Math.max(fromEnd, gapEnd - Math.max(start, gapStart));
    }
    if (fromStart > 0 || fromEnd > 0) rebuilt.set(key2, {
      fromStart,
      fromEnd
    });
  };
  for (const [hunkIndex, hunk] of diff.hunks.entries()) {
    const gapEnd = getHunkDeletionStart(hunk);
    applyGap(hunkIndex, gapEnd - Math.max(hunk.collapsedBefore, 0), gapEnd);
  }
  const lastHunk = diff.hunks[diff.hunks.length - 1];
  if (lastHunk != null && !diff.isPartial && diff.deletionLines.length > 0) applyGap(diff.hunks.length, getHunkDeletionStart(lastHunk) + lastHunk.deletionCount, diff.deletionLines.length);
  return rebuilt;
}
function finishEditSessionForDiff(diff, parseDiffOptions) {
  if (diff.editSessionDirty !== true) return false;
  diff.editSessionDirty = void 0;
  Object.assign(diff, diff.additionLines.length <= 1 && diff.additionLines.join("") === "" ? recomputeDiffHunks(diff, parseDiffOptions) : recomputeDiffHunksForEdit(diff, parseDiffOptions));
  return true;
}
function parseCanonicalChangeBlocks(diff, parseDiffOptions) {
  if (findDivergenceCore(diff.deletionLines, diff.additionLines) == null) return [];
  const parsed = parseDiffFromFile({
    name: diff.prevName ?? diff.name,
    contents: diff.deletionLines.join("")
  }, {
    name: diff.name,
    contents: diff.additionLines.join(""),
    lang: diff.lang
  }, parseDiffOptions);
  const blocks = [];
  let coveredAdditions = 0;
  let coveredDeletions = 0;
  for (const hunk of parsed.hunks) {
    const contextLines = hunk.additionCount > 0 ? hunk.additionLineIndex - coveredAdditions : hunk.deletionLineIndex - coveredDeletions;
    coveredAdditions += contextLines;
    coveredDeletions += contextLines;
    for (const content of hunk.hunkContent) {
      if (content.type === "context") {
        coveredAdditions += content.lines;
        coveredDeletions += content.lines;
        continue;
      }
      const block = offsetHunkContent(content, 0, 0);
      if (block.additions === 0) block.additionLineIndex = coveredAdditions;
      if (block.deletions === 0) block.deletionLineIndex = coveredDeletions;
      blocks.push(block);
      coveredAdditions += block.additions;
      coveredDeletions += block.deletions;
    }
  }
  return blocks;
}
function buildRegionPlans(previousHunks, blocks, deletionLineCount) {
  var _a9, _b;
  const previousPlans = previousHunks.map((hunk, index) => {
    const deletionStart = getHunkDeletionStart(hunk);
    return {
      deletionStart,
      deletionEnd: deletionStart + hunk.deletionCount,
      blocks: [],
      previousSpan: {
        firstIndex: index,
        lastIndex: index
      }
    };
  });
  const plans = [];
  let previousIndex = 0;
  for (const block of blocks) {
    const blockStart = block.deletionLineIndex;
    const blockEnd = blockStart + block.deletions;
    while (previousIndex < previousPlans.length && previousPlans[previousIndex].deletionEnd < blockStart) {
      plans.push(previousPlans[previousIndex]);
      previousIndex++;
    }
    let plan = plans.length > 0 && blockTouchesRegion(blockStart, blockEnd, plans[plans.length - 1]) ? plans.pop() : void 0;
    while (previousIndex < previousPlans.length && previousPlans[previousIndex].deletionStart <= blockEnd) {
      plan = mergeRegionPlans(plan, previousPlans[previousIndex]);
      previousIndex++;
    }
    if (plan == null) {
      let deletionStart = blockStart;
      let deletionEnd = blockEnd;
      if ((block.deletions === 0 || block.additions === 0) && deletionLineCount > block.deletions) {
        const previousEnd = ((_a9 = plans[plans.length - 1]) == null ? void 0 : _a9.deletionEnd) ?? 0;
        const nextStart = ((_b = previousPlans[previousIndex]) == null ? void 0 : _b.deletionStart) ?? deletionLineCount;
        if (blockStart > previousEnd) deletionStart--;
        else if (blockEnd < nextStart) deletionEnd++;
      }
      plan = {
        deletionStart,
        deletionEnd,
        blocks: [],
        previousSpan: void 0
      };
    }
    plan.deletionStart = Math.min(plan.deletionStart, blockStart);
    plan.deletionEnd = Math.max(plan.deletionEnd, blockEnd);
    plan.blocks.push(block);
    plans.push(plan);
  }
  while (previousIndex < previousPlans.length) {
    plans.push(previousPlans[previousIndex]);
    previousIndex++;
  }
  return plans;
}
function blockTouchesRegion(blockStart, blockEnd, region) {
  return blockStart <= region.deletionEnd && blockEnd >= region.deletionStart;
}
function mergeRegionPlans(target, source) {
  if (target == null) return source;
  target.deletionStart = Math.min(target.deletionStart, source.deletionStart);
  target.deletionEnd = Math.max(target.deletionEnd, source.deletionEnd);
  target.blocks.push(...source.blocks);
  if (source.previousSpan != null) {
    target.previousSpan ?? (target.previousSpan = { ...source.previousSpan });
    target.previousSpan.firstIndex = Math.min(target.previousSpan.firstIndex, source.previousSpan.firstIndex);
    target.previousSpan.lastIndex = Math.max(target.previousSpan.lastIndex, source.previousSpan.lastIndex);
  }
  return target;
}
function buildRegionHunks(diff, plans) {
  const hunks = [];
  let deletionCursor = 0;
  let additionCursor = 0;
  for (const plan of plans) {
    const contextBefore = plan.deletionStart - deletionCursor;
    if (contextBefore < 0) throw new Error("buildRegionHunks: overlapping old-side regions");
    deletionCursor += contextBefore;
    additionCursor += contextBefore;
    const additionStart = additionCursor;
    const hunkContent = [];
    for (const canonicalBlock of plan.blocks) {
      const deletionContext = canonicalBlock.deletionLineIndex - deletionCursor;
      const additionContext = canonicalBlock.additionLineIndex - additionCursor;
      if (deletionContext < 0 || deletionContext !== additionContext) throw new Error("buildRegionHunks: canonical block context mismatch");
      pushContext(hunkContent, deletionContext, additionCursor, deletionCursor);
      deletionCursor += deletionContext;
      additionCursor += additionContext;
      hunkContent.push({ ...canonicalBlock });
      deletionCursor += canonicalBlock.deletions;
      additionCursor += canonicalBlock.additions;
    }
    const trailingContext = plan.deletionEnd - deletionCursor;
    if (trailingContext < 0) throw new Error("buildRegionHunks: block exceeds its old-side region");
    pushContext(hunkContent, trailingContext, additionCursor, deletionCursor);
    deletionCursor += trailingContext;
    additionCursor += trailingContext;
    hunks.push(createRegionHunk(diff, {
      additionStart,
      additionEnd: additionCursor,
      deletionStart: plan.deletionStart,
      deletionEnd: plan.deletionEnd
    }, hunkContent));
  }
  if (diff.deletionLines.length - deletionCursor !== diff.additionLines.length - additionCursor) throw new Error("buildRegionHunks: trailing context mismatch");
  return hunks;
}
function createRegionHunk(diff, bounds, hunkContent) {
  const additionCount = bounds.additionEnd - bounds.additionStart;
  const deletionCount = bounds.deletionEnd - bounds.deletionStart;
  let additionLines = 0;
  let deletionLines = 0;
  for (const content of hunkContent) if (content.type === "change") {
    additionLines += content.additions;
    deletionLines += content.deletions;
  }
  const hunk = {
    collapsedBefore: 0,
    additionStart: getUnifiedStart(bounds.additionStart, additionCount),
    additionCount,
    additionLines,
    additionLineIndex: getUnifiedLineIndex(bounds.additionStart, additionCount),
    deletionStart: getUnifiedStart(bounds.deletionStart, deletionCount),
    deletionCount,
    deletionLines,
    deletionLineIndex: getUnifiedLineIndex(bounds.deletionStart, deletionCount),
    hunkContent,
    hunkSpecs: `@@ -${getUnifiedStart(bounds.deletionStart, deletionCount)},${deletionCount} +${getUnifiedStart(bounds.additionStart, additionCount)},${additionCount} @@`,
    splitLineStart: 0,
    splitLineCount: 0,
    unifiedLineStart: 0,
    unifiedLineCount: 0,
    noEOFCRAdditions: false,
    noEOFCRDeletions: false
  };
  recomputeHunkRenderLineCounts(hunk);
  return hunk;
}
function pushContext(hunkContent, lines, additionLineIndex, deletionLineIndex) {
  if (lines > 0) hunkContent.push({
    type: "context",
    lines,
    additionLineIndex,
    deletionLineIndex
  });
}
function hasRegionOrSplitLayoutChanged(previous, next) {
  if (previous.length !== next.length) return true;
  for (let index = 0; index < previous.length; index++) {
    const previousHunk = previous[index];
    const nextHunk = next[index];
    if (getHunkDeletionStart(previousHunk) !== getHunkDeletionStart(nextHunk) || previousHunk.deletionCount !== nextHunk.deletionCount || getHunkAdditionStart(previousHunk) !== getHunkAdditionStart(nextHunk) || previousHunk.additionCount !== nextHunk.additionCount || previousHunk.splitLineCount !== nextHunk.splitLineCount || !haveSameSplitRowMapping(previousHunk, nextHunk)) return true;
  }
  return false;
}
function haveSameSplitRowMapping(previous, next) {
  const previousRows = iterateSplitRowMapping(previous);
  const nextRows = iterateSplitRowMapping(next);
  while (true) {
    const previousRow = previousRows.next();
    const nextRow = nextRows.next();
    if (previousRow.done === true || nextRow.done === true) return previousRow.done === nextRow.done;
    if (previousRow.value[0] !== nextRow.value[0] || previousRow.value[1] !== nextRow.value[1]) return false;
  }
}
function* iterateSplitRowMapping(hunk) {
  for (const content of hunk.hunkContent) {
    if (content.type === "context") {
      for (let offset = 0; offset < content.lines; offset++) yield [content.deletionLineIndex + offset, content.additionLineIndex + offset];
      continue;
    }
    const rowCount = Math.max(content.deletions, content.additions);
    for (let offset = 0; offset < rowCount; offset++) yield [offset < content.deletions ? content.deletionLineIndex + offset : void 0, offset < content.additions ? content.additionLineIndex + offset : void 0];
  }
}
function getHunkAdditionStart(hunk) {
  return getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount);
}
function getHunkDeletionStart(hunk) {
  return getHunkSideStartBoundary(hunk.deletionStart, hunk.deletionCount);
}
function getUnifiedStart(lineIndex, count) {
  return count === 0 ? lineIndex : lineIndex + 1;
}
function getUnifiedLineIndex(lineIndex, count) {
  return count === 0 ? lineIndex - 1 : lineIndex;
}
function finalizeSessionHunks(diff) {
  recomputeDiffRenderLineCounts(diff);
  for (let index = 0; index < diff.hunks.length; index++) syncHunkNoEOFCRFromFullFile(diff, index);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/isDiffPlainText.js
function isDiffPlainText(diff) {
  const computedLang = diff.lang ?? getFiletypeFromFileName(diff.name);
  const computedPreviousLang = diff.lang ?? (diff.prevName != null ? getFiletypeFromFileName(diff.prevName) : "text");
  return computedLang === "text" && computedPreviousLang === "text";
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/renderers/DiffHunksRenderer.js
var instanceId4 = -1;
var DiffHunksRenderer = class {
  constructor(options = { theme: DEFAULT_THEMES }, onRenderUpdate, workerManager) {
    __publicField(this, "options");
    __publicField(this, "onRenderUpdate");
    __publicField(this, "workerManager");
    __publicField(this, "__id", `diff-hunks-renderer:${++instanceId4}`);
    __publicField(this, "highlighter");
    __publicField(this, "diff");
    __publicField(this, "expandedHunks", /* @__PURE__ */ new Map());
    __publicField(this, "deletionAnnotations", {});
    __publicField(this, "additionAnnotations", {});
    __publicField(this, "computedLang", "text");
    __publicField(this, "renderCache");
    __publicField(this, "editSessionActive", false);
    __publicField(this, "createAnnotationElement", (span) => {
      return createAnnotationElement(span);
    });
    this.options = options;
    this.onRenderUpdate = onRenderUpdate;
    this.workerManager = workerManager;
    if ((workerManager == null ? void 0 : workerManager.isWorkingPool()) !== true) this.highlighter = areThemesAttached(options.theme ?? DEFAULT_THEMES) ? getHighlighterIfLoaded() : void 0;
  }
  cleanUp() {
    this.recycle();
    this.expandedHunks.clear();
    this.workerManager = void 0;
    this.onRenderUpdate = void 0;
  }
  recycle() {
    var _a9;
    this.highlighter = void 0;
    this.diff = void 0;
    this.clearRenderCache();
    this.additionAnnotations = {};
    this.deletionAnnotations = {};
    (_a9 = this.workerManager) == null ? void 0 : _a9.cleanUpTasks(this);
    this.endEditSession();
  }
  /**
  * Enter edit-session mode: hunk updates preserve the current region
  * skeleton instead of recomputing hunks, and rendering happens locally
  * with the token transformer forced on (worker-pool requests/results are
  * suspended for this renderer). An empty additions document gets one row so
  * the editor has a line for its caret. Called on every editor attach,
  * including a re-attach after recycle.
  */
  beginEditSession() {
    this.editSessionActive = true;
    const diff = this.diffCache;
    if (diff != null && !diff.isPartial && diff.additionLines.length === 0) {
      Object.assign(diff, recomputeEmptyDocumentDiff(diff, this.options.parseDiffOptions));
      this.markEditSessionPass(diff);
      this.clearRenderCache();
    }
  }
  /** Leave edit-session mode. The exit recompute is the host's concern. */
  endEditSession() {
    this.editSessionActive = false;
  }
  /**
  * Ensures that the DOM is compatible with editor render updates
  */
  editorRenderReady() {
    var _a9;
    return ((_a9 = this.renderCache) == null ? void 0 : _a9.options.useTokenTransformer) === true && this.renderCache.highlighted && this.renderCache.result != null;
  }
  /**
  * Re-highlights the current diff in the background and swaps the fresh
  * result in (with a re-render) once it completes. Needed after an edit
  * session's exit recompute: session passes plain-fill shifted lines in the
  * cached result, and the recompute mutates the diff in place (same object,
  * same cacheKey), so identity/cacheKey checks would otherwise treat the
  * stale highlight as current forever. The current result — content-correct,
  * mostly highlighted — keeps rendering until the fresh one lands, so no
  * interim paint drops highlighting.
  */
  refreshHighlightedResult() {
    const { renderCache } = this;
    if (renderCache == null || isDiffPlainText(renderCache.diff) || isDiffMassive(renderCache.diff, this.getTokenizeMaxLength())) return Promise.resolve();
    const { diff } = renderCache;
    const { workerManager } = this;
    if (!this.editSessionActive && (workerManager == null ? void 0 : workerManager.isWorkingPool()) === true && diff.cacheKey != null) {
      workerManager.evictDiffFromCache(diff.cacheKey);
      return workerManager.primeDiffHighlightCache(diff).then(() => {
        this.applyRefreshedResult(diff, workerManager.getDiffResultCache(diff));
      }).catch((error) => this.onHighlightError(error));
    }
    return this.asyncHighlight(diff).then((fresh) => this.applyRefreshedResult(diff, fresh)).catch((error) => this.onHighlightError(error));
  }
  applyRefreshedResult(diff, fresh) {
    var _a9;
    if (fresh == null || this.renderCache == null || this.renderCache.diff !== diff || this.editSessionActive) return;
    const { options } = this.getRenderOptions(diff);
    if (!areDiffRenderOptionsEqual(options, fresh.options)) return;
    this.renderCache = {
      diff,
      options: fresh.options,
      highlighted: true,
      result: fresh.result,
      renderRange: void 0
    };
    (_a9 = this.onRenderUpdate) == null ? void 0 : _a9.call(this);
  }
  get diffCache() {
    var _a9;
    return ((_a9 = this.renderCache) == null ? void 0 : _a9.diff) ?? this.diff;
  }
  clearRenderCache() {
    var _a9;
    const renderCache = this.renderCache;
    this.renderCache = void 0;
    if (renderCache != null && renderCache.isDirty === true && renderCache.diff.cacheKey != null) (_a9 = this.workerManager) == null ? void 0 : _a9.evictDiffFromCache(renderCache.diff.cacheKey);
  }
  setOptions(options) {
    this.options = options;
  }
  mergeOptions(options) {
    this.options = {
      ...this.options,
      ...options
    };
  }
  expandHunk(index, direction, expansionLineCount = this.getOptionsWithDefaults().expansionLineCount) {
    var _a9;
    const region = { ...this.expandedHunks.get(index) ?? {
      fromStart: 0,
      fromEnd: 0
    } };
    if (direction === "up" || direction === "both") region.fromStart += expansionLineCount;
    if (direction === "down" || direction === "both") region.fromEnd += expansionLineCount;
    if (((_a9 = this.renderCache) == null ? void 0 : _a9.highlighted) !== true) this.clearRenderCache();
    this.expandedHunks.set(index, region);
  }
  getExpandedHunk(hunkIndex) {
    return this.expandedHunks.get(hunkIndex) ?? DEFAULT_EXPANDED_REGION;
  }
  getExpandedHunksMap() {
    return this.expandedHunks;
  }
  /** Replace the whole expansion map (session-exit expansion remapping). */
  setExpandedHunksMap(expandedHunks) {
    this.expandedHunks = expandedHunks;
  }
  setLineAnnotations(lineAnnotations) {
    this.additionAnnotations = {};
    this.deletionAnnotations = {};
    for (const annotation of lineAnnotations) {
      const map = (() => {
        switch (annotation.side) {
          case "deletions":
            return this.deletionAnnotations;
          case "additions":
            return this.additionAnnotations;
        }
      })();
      const arr = map[annotation.lineNumber] ?? [];
      map[annotation.lineNumber] = arr;
      arr.push(annotation);
    }
  }
  /**
  * Returns true when a session-mode pass changed the region skeleton itself
  * (a gap edit synthesized or merged regions), which changes the rendered
  * row set without a line-count change — the host must escalate to a full
  * re-render instead of its cheap refresh path.
  */
  updateRenderCache(dirtyLines, themeType, lineCountChangeInFlight = false) {
    var _a9;
    if (this.renderCache == null) return false;
    const { result, diff } = this.renderCache;
    if (result == null) return false;
    if (diff.isPartial) throw new Error("Could not update render cache for partial diff");
    const hastLines = result.code.additionLines;
    const changedAdditionLines = [];
    const previousAdditionLines = /* @__PURE__ */ new Map();
    for (const [line, tokens] of dirtyLines) {
      const prevProps = ((_a9 = hastLines[line]) == null ? void 0 : _a9.properties) ?? {};
      const lineText = tokens.map((a2) => a2[2]).join("");
      const canSyncDiffLine = line < diff.additionLines.length;
      const prevLine = canSyncDiffLine ? diff.additionLines[line] ?? "" : "";
      const prevText = cleanLastNewline(prevLine);
      if (canSyncDiffLine) {
        diff.additionLines[line] = applyLineTextWithNewline(prevLine, lineText);
        if (prevText !== lineText) {
          changedAdditionLines.push(line);
          previousAdditionLines.set(line, prevLine);
        }
      }
      hastLines[line] = {
        type: "element",
        tagName: "div",
        properties: {
          "data-line": prevProps["data-line"] ?? line + 1,
          "data-line-index": prevProps["data-line-index"] ?? line,
          "data-line-type": prevProps["data-line-type"] ?? "context"
        },
        children: tokens.map(([char, fg, text2]) => {
          if (char === 0 && fg === "") {
            if (text2 === "") return {
              type: "element",
              tagName: "br",
              properties: {},
              children: []
            };
            return {
              type: "text",
              value: text2
            };
          }
          return {
            type: "element",
            tagName: "span",
            properties: {
              "data-char": char,
              style: `color:${fg};`
            },
            children: [{
              type: "text",
              value: text2
            }]
          };
        })
      };
    }
    let regionsChanged = false;
    if (changedAdditionLines.length > 0) if (this.editSessionActive && !diff.isPartial) {
      if (!lineCountChangeInFlight) if (diff.additionLines.length <= 1 && diff.additionLines.join("") === "") {
        Object.assign(diff, recomputeEmptyDocumentDiff(diff, this.options.parseDiffOptions));
        this.markEditSessionPass(diff);
        regionsChanged = true;
      } else if (shouldTopAlignAdditionRecompute(diff, diff.additionLines)) {
        Object.assign(diff, recomputeTopAlignedAdditionDiff(diff, diff.additionLines, this.options.parseDiffOptions));
        this.markEditSessionPass(diff);
        regionsChanged = true;
      } else {
        const change = applySessionChangedLines(diff, changedAdditionLines, this.options.parseDiffOptions, previousAdditionLines);
        this.applyExpansionRemap(change);
        regionsChanged = change != null;
      }
    } else Object.assign(diff, updateDiffHunks(diff, changedAdditionLines, this.options.parseDiffOptions));
    result.baseThemeType = themeType;
    this.renderCache.isDirty = true;
    return regionsChanged;
  }
  applyExpansionRemap(change) {
    if (change != null) this.expandedHunks = remapExpandedHunksForRegionChange(this.expandedHunks, change);
  }
  applyDocumentChange(textDocument) {
    if (this.renderCache == null) return;
    const { diff, result } = this.renderCache;
    if (result == null) return;
    if (diff.isPartial) throw new Error("Could not apply document change for partial diff");
    const { additionLines: previousAdditionLines } = diff;
    diff.additionLines = getEditorDocumentLines(textDocument, previousAdditionLines);
    result.code.additionLines = realignAdditionHastLines(previousAdditionLines, diff.additionLines, result.code.additionLines, textDocument);
    if (diff.additionLines.length <= 1 && diff.additionLines.join("") === "") {
      Object.assign(diff, recomputeEmptyDocumentDiff(diff, this.options.parseDiffOptions));
      result.code.additionLines[0] = createPlainAdditionLineElement(0, textDocument.getLineText(0));
      this.markEditSessionPass(diff);
    } else if (this.editSessionActive) this.applySessionDocumentChange(diff);
    else Object.assign(diff, recomputeDiffHunksForEdit(diff, this.options.parseDiffOptions));
    this.renderCache.isDirty = true;
  }
  applySessionDocumentChange(diff) {
    const { parseDiffOptions } = this.options;
    const rawLines = diff.additionLines;
    if (shouldTopAlignAdditionRecompute(diff, rawLines)) {
      Object.assign(diff, recomputeTopAlignedAdditionDiff(diff, rawLines, parseDiffOptions));
      this.markEditSessionPass(diff);
      return;
    }
    this.applyExpansionRemap(rebuildSessionHunks(diff, parseDiffOptions));
  }
  markEditSessionPass(diff) {
    if (!this.editSessionActive) return;
    diff.editSessionDirty = true;
  }
  getUnifiedLineDecoration({ lineType }) {
    return {
      gutterLineType: lineType,
      contentProperties: { "data-line-type": lineType }
    };
  }
  getSplitLineDecoration({ side, type }) {
    const lineType = type === "change" ? side === "deletions" ? "change-deletion" : "change-addition" : type;
    return {
      gutterLineType: lineType,
      contentProperties: { "data-line-type": lineType }
    };
  }
  getOptionsWithDefaults() {
    var _a9;
    const { diffIndicators = "bars", diffStyle = "split", disableBackground = false, disableFileHeader = false, disableLineNumbers = false, disableVirtualizationBuffers = false, collapsed = false, expandUnchanged = false, collapsedContextThreshold = 1, expansionLineCount = 100, hunkSeparators = "line-info", lineDiffType = "word-alt", maxLineDiffLength = 1e3, overflow = "scroll", stickyHeader = false, theme = DEFAULT_THEMES, headerRenderMode = "default", tokenizeMaxLineLength = 1e3, tokenizeMaxLength = DEFAULT_TOKENIZE_MAX_LENGTH, useTokenTransformer = false, useCSSClasses = false } = this.options;
    return {
      diffIndicators,
      diffStyle,
      disableBackground,
      disableFileHeader,
      disableLineNumbers,
      disableVirtualizationBuffers,
      collapsed,
      expandUnchanged,
      collapsedContextThreshold,
      expansionLineCount,
      hunkSeparators,
      lineDiffType,
      maxLineDiffLength,
      overflow,
      stickyHeader,
      theme: ((_a9 = this.workerManager) == null ? void 0 : _a9.getDiffRenderOptions().theme) ?? theme,
      headerRenderMode,
      tokenizeMaxLineLength,
      tokenizeMaxLength,
      useTokenTransformer,
      useCSSClasses
    };
  }
  async initializeHighlighter() {
    var _a9;
    this.highlighter = await getSharedHighlighter(getHighlighterOptions(this.computedLang, {
      theme: this.getLocalHighlightTheme(),
      preferredHighlighter: ((_a9 = this.workerManager) == null ? void 0 : _a9.getPreferredHighlighter()) ?? this.options.preferredHighlighter
    }));
    return this.highlighter;
  }
  hydrate(diff) {
    var _a9, _b;
    if (diff == null) return;
    this.diff = diff;
    const { options } = this.getRenderOptions(diff);
    const massiveDiff = isDiffMassive(diff, this.getTokenizeMaxLength());
    let cache2 = (_a9 = this.workerManager) == null ? void 0 : _a9.getDiffResultCache(diff);
    if (cache2 != null && !areDiffRenderOptionsEqual(options, cache2.options)) cache2 = void 0;
    this.renderCache ?? (this.renderCache = {
      diff,
      highlighted: !massiveDiff && !isDiffPlainText(diff),
      options,
      result: massiveDiff ? void 0 : cache2 == null ? void 0 : cache2.result,
      renderRange: void 0
    });
    if (!this.editSessionActive && ((_b = this.workerManager) == null ? void 0 : _b.isWorkingPool()) === true) {
      if (this.renderCache.result == null && !massiveDiff) this.workerManager.highlightDiffAST(this, this.diff);
    } else if (this.highlighter == null) {
      this.computedLang = diff.lang ?? getFiletypeFromFileName(diff.name);
      this.initializeHighlighter();
    }
  }
  getLocalHighlightTheme() {
    var _a9;
    return ((_a9 = this.workerManager) == null ? void 0 : _a9.getDiffRenderOptions().theme) ?? this.options.theme ?? DEFAULT_THEMES;
  }
  getEffectiveCodeOptions() {
    var _a9;
    const poolOptions = ((_a9 = this.workerManager) == null ? void 0 : _a9.isWorkingPool()) === true ? this.workerManager.getDiffRenderOptions() : void 0;
    return {
      theme: this.getLocalHighlightTheme(),
      tokenizeMaxLineLength: (poolOptions == null ? void 0 : poolOptions.tokenizeMaxLineLength) ?? this.options.tokenizeMaxLineLength
    };
  }
  getRenderOptions(diff) {
    const options = (() => {
      var _a9;
      if (((_a9 = this.workerManager) == null ? void 0 : _a9.isWorkingPool()) === true) {
        const poolOptions = this.workerManager.getDiffRenderOptions();
        if (this.editSessionActive && poolOptions.useTokenTransformer !== true) return {
          ...poolOptions,
          useTokenTransformer: true
        };
        return poolOptions;
      }
      const { theme, tokenizeMaxLineLength, lineDiffType, maxLineDiffLength } = this.getOptionsWithDefaults();
      return {
        theme,
        useTokenTransformer: this.editSessionActive || this.options.useTokenTransformer === true,
        tokenizeMaxLineLength,
        lineDiffType,
        maxLineDiffLength
      };
    })();
    this.getOptionsWithDefaults();
    const { renderCache } = this;
    if ((renderCache == null ? void 0 : renderCache.result) == null) return {
      options,
      forceHighlight: true
    };
    if (!areDiffTargetsEqual(diff, renderCache.diff) || !areDiffRenderOptionsEqual(options, renderCache.options)) return {
      options,
      forceHighlight: true
    };
    return {
      options,
      forceHighlight: false
    };
  }
  renderDiff(diff = ((_a9) => (_a9 = this.renderCache) == null ? void 0 : _a9.diff)(), renderRange = DEFAULT_RENDER_RANGE) {
    var _a10;
    if (diff == null) return;
    const { expandUnchanged, collapsedContextThreshold } = this.getOptionsWithDefaults();
    let { options, forceHighlight } = this.getRenderOptions(diff);
    const cache2 = this.getMatchingWorkerResultCache(diff, options);
    if (cache2 != null && !this.hasHighlightedRenderCache(diff, options)) {
      this.renderCache = {
        diff,
        highlighted: true,
        renderRange: void 0,
        ...cache2
      };
      forceHighlight = false;
    }
    this.renderCache ?? (this.renderCache = {
      diff,
      highlighted: false,
      options,
      result: void 0,
      renderRange: void 0
    });
    const hasContent = diff.additionLines.length > 0 || diff.deletionLines.length > 0;
    const forcePlainText = !hasContent || isDiffPlainText(diff) || isDiffMassive(diff, this.getTokenizeMaxLength());
    const newContent = !areDiffTargetsEqual(diff, this.renderCache.diff);
    const newRenderRange = !areRenderRangesEqual(this.renderCache.renderRange, renderRange);
    if (!this.editSessionActive && ((_a10 = this.workerManager) == null ? void 0 : _a10.isWorkingPool()) === true) {
      const highlightPending = this.renderCache.result == null && this.renderCache.highlighted && !forcePlainText && !newContent && isDefaultRenderRange(renderRange);
      if (highlightPending) this.renderCache.highlightPending = true;
      if (!highlightPending && (forcePlainText || this.renderCache.result == null || !this.renderCache.highlighted && (newContent || newRenderRange))) {
        this.renderCache.diff = diff;
        this.renderCache.options = options;
        this.renderCache.highlighted = false;
        if (this.renderCache.result == null || newContent || newRenderRange || forceHighlight) this.renderCache.result = this.workerManager.getPlainDiffAST(diff, renderRange.startingLine, renderRange.totalLines, isDefaultRenderRange(renderRange) ? true : expandUnchanged ? true : this.expandedHunks, collapsedContextThreshold);
        this.renderCache.renderRange = renderRange;
      }
      if (!forcePlainText && hasContent && (!this.renderCache.highlighted || forceHighlight)) this.workerManager.highlightDiffAST(this, diff);
    } else {
      this.computedLang = diff.lang ?? getFiletypeFromFileName(diff.name);
      const hasThemes = this.highlighter != null && areThemesAttached(options.theme);
      const hasLangs = this.highlighter != null && areLanguagesAttached(this.computedLang);
      const canHighlight = !forcePlainText && hasLangs;
      if (this.highlighter != null && hasThemes && (forceHighlight || forcePlainText || !this.renderCache.highlighted && canHighlight || this.renderCache.result == null)) {
        const { result, options: options2 } = this.renderDiffWithHighlighter(diff, this.highlighter, forcePlainText || !hasLangs);
        this.renderCache = {
          diff,
          options: options2,
          highlighted: canHighlight,
          result,
          renderRange: void 0
        };
      }
      if (!hasThemes || !forcePlainText && !hasLangs) this.asyncHighlight(diff).then(({ result, options: options2 }) => {
        if (this.renderCache != null) this.renderCache.highlighted = false;
        this.applyHighlightResult(diff, result, options2, !forcePlainText);
      });
    }
    return this.renderCache.result != null ? this.processDiffResult(this.renderCache.diff, renderRange, this.renderCache.result) : void 0;
  }
  async asyncRender(diff, renderRange = DEFAULT_RENDER_RANGE) {
    const { result } = await this.asyncHighlight(diff);
    return this.processDiffResult(diff, renderRange, result);
  }
  createPreElement(split, totalLines, customProperties) {
    const { diffIndicators, disableBackground, disableLineNumbers, overflow } = this.getOptionsWithDefaults();
    return createPreElement({
      type: "diff",
      diffIndicators,
      disableBackground,
      disableLineNumbers,
      overflow,
      split,
      totalLines,
      customProperties
    });
  }
  async asyncHighlight(diff) {
    const forcePlainText = isDiffMassive(diff, this.getTokenizeMaxLength());
    this.computedLang = forcePlainText ? "text" : diff.lang ?? getFiletypeFromFileName(diff.name);
    const hasThemes = this.highlighter != null && areThemesAttached(this.getLocalHighlightTheme());
    const hasLangs = forcePlainText || this.highlighter != null && areLanguagesAttached(this.computedLang);
    if (this.highlighter == null || !hasThemes || !hasLangs) this.highlighter = await this.initializeHighlighter();
    return this.renderDiffWithHighlighter(diff, this.highlighter, forcePlainText);
  }
  renderDiffWithHighlighter(diff, highlighter2, forcePlainText = false) {
    const { options } = this.getRenderOptions(diff);
    const { collapsedContextThreshold } = this.getOptionsWithDefaults();
    const result = renderDiffWithHighlighter(diff, highlighter2, options, {
      forcePlainText,
      expandedHunks: forcePlainText ? true : void 0,
      collapsedContextThreshold
    });
    if (this.editSessionActive && diff.additionLines.length === 1 && diff.additionLines[0] === "" && result.code.additionLines[0] == null) {
      let fallbackLine;
      iterateOverDiff({
        diff,
        diffStyle: "both",
        expandedHunks: forcePlainText ? true : void 0,
        collapsedContextThreshold,
        callback: ({ additionLine }) => {
          if ((additionLine == null ? void 0 : additionLine.lineIndex) !== 0) return;
          fallbackLine = additionLine;
          return true;
        }
      });
      if (fallbackLine == null) throw new Error("DiffHunksRenderer: missing empty addition line");
      result.code.additionLines[0] = createPlainAdditionLineElement(0, "", fallbackLine.unifiedLineIndex, fallbackLine.splitLineIndex);
    }
    return {
      result,
      options
    };
  }
  onHighlightSuccess(diff, result, options, highlighted = true) {
    if (this.editSessionActive) return;
    this.applyHighlightResult(diff, result, options, highlighted);
  }
  applyHighlightResult(diff, result, options, highlighted = true) {
    var _a9;
    if (this.renderCache == null) return;
    const triggerRenderUpdate = this.renderCache.highlightPending === true || !this.renderCache.highlighted || !areDiffRenderOptionsEqual(this.renderCache.options, options) || !areDiffTargetsEqual(this.renderCache.diff, diff);
    this.renderCache = {
      diff,
      options,
      highlighted,
      result,
      renderRange: void 0
    };
    if (triggerRenderUpdate) (_a9 = this.onRenderUpdate) == null ? void 0 : _a9.call(this);
  }
  getMatchingWorkerResultCache(diff, options) {
    var _a9;
    if (this.editSessionActive) return;
    const cache2 = (_a9 = this.workerManager) == null ? void 0 : _a9.getDiffResultCache(diff);
    if (cache2 == null || !areDiffRenderOptionsEqual(options, cache2.options)) return;
    return cache2;
  }
  hasHighlightedRenderCache(diff, options) {
    const { renderCache } = this;
    return (renderCache == null ? void 0 : renderCache.result) != null && renderCache.highlighted && areDiffTargetsEqual(diff, renderCache.diff) && areDiffRenderOptionsEqual(options, renderCache.options);
  }
  onHighlightError(error) {
    console.error(error);
  }
  getTokenizeMaxLength() {
    return this.options.tokenizeMaxLength ?? 1e5;
  }
  processDiffResult(fileDiff, renderRange, { code, themeStyles, baseThemeType }) {
    var _a9;
    const { diffStyle, disableFileHeader, expandUnchanged, expansionLineCount, collapsedContextThreshold, hunkSeparators } = this.getOptionsWithDefaults();
    const isRenderCacheDirty = ((_a9 = this.renderCache) == null ? void 0 : _a9.isDirty) ?? false;
    this.diff = fileDiff;
    const unified = diffStyle === "unified";
    const canHydrateContext = canHydrateCollapsedContext(fileDiff, this.options.loadDiffFiles != null);
    const isExpandableDiff = !fileDiff.isPartial || canHydrateContext;
    let additionsContentAST = [];
    let deletionsContentAST = [];
    let unifiedContentAST = [];
    const hunkData = [];
    const { additionLines, deletionLines } = code;
    const context = {
      rowCount: 0,
      hunkSeparators,
      additionsContentAST,
      deletionsContentAST,
      unifiedContentAST,
      unifiedGutterAST: createGutterWrapper(),
      deletionsGutterAST: createGutterWrapper(),
      additionsGutterAST: createGutterWrapper(),
      expansionLineCount,
      hunkData,
      incrementRowCount(count = 1) {
        context.rowCount += count;
      },
      pushToGutter(type, element2) {
        switch (type) {
          case "unified":
            context.unifiedGutterAST.children.push(element2);
            break;
          case "deletions":
            context.deletionsGutterAST.children.push(element2);
            break;
          case "additions":
            context.additionsGutterAST.children.push(element2);
            break;
        }
      }
    };
    const trailingRangeSize = getTrailingContextRangeSize({
      fileDiff,
      errorPrefix: "DiffHunksRenderer.processDiffResult"
    });
    const pendingSplitContext = {
      size: 0,
      side: void 0,
      increment() {
        this.size += 1;
      },
      flush() {
        if (diffStyle === "unified") return;
        if (this.size <= 0 || this.side == null) {
          this.side = void 0;
          this.size = 0;
          return;
        }
        if (this.side === "additions") {
          context.pushToGutter("additions", createGutterGap(void 0, "buffer", this.size));
          additionsContentAST == null ? void 0 : additionsContentAST.push(createEmptyRowBuffer(this.size));
        } else {
          context.pushToGutter("deletions", createGutterGap(void 0, "buffer", this.size));
          deletionsContentAST == null ? void 0 : deletionsContentAST.push(createEmptyRowBuffer(this.size));
        }
        this.size = 0;
        this.side = void 0;
      }
    };
    const pushGutterLineNumber = (type, lineType, lineNumber, lineIndex, gutterProperties) => {
      context.pushToGutter(type, createGutterItem(lineType, lineNumber, lineIndex, gutterProperties));
    };
    function pushSeparators(props) {
      pendingSplitContext.flush();
      if (diffStyle === "unified") pushSeparator("unified", props, context);
      else {
        pushSeparator("deletions", props, context);
        pushSeparator("additions", props, context);
      }
    }
    this.pushFileLevelAnnotations(fileDiff, diffStyle, renderRange, context);
    iterateOverDiff({
      diff: fileDiff,
      diffStyle,
      startingLine: renderRange.startingLine,
      totalLines: renderRange.totalLines,
      expandedHunks: expandUnchanged ? true : this.expandedHunks,
      collapsedContextThreshold,
      callback: ({ hunkIndex, hunk, collapsedBefore, collapsedAfter, additionLine, deletionLine, type }) => {
        var _a10, _b;
        const splitLineIndex = deletionLine != null ? deletionLine.splitLineIndex : additionLine.splitLineIndex;
        const unifiedLineIndex = additionLine != null ? additionLine.unifiedLineIndex : deletionLine.unifiedLineIndex;
        if (diffStyle === "split" && type !== "change") pendingSplitContext.flush();
        if (collapsedBefore > 0) pushSeparators({
          hunkIndex,
          collapsedLines: collapsedBefore,
          rangeSize: Math.max((hunk == null ? void 0 : hunk.collapsedBefore) ?? 0, 0),
          hunkSpecs: hunk == null ? void 0 : hunk.hunkSpecs,
          isFirstHunk: hunkIndex === 0,
          isLastHunk: false,
          isExpandable: isExpandableDiff
        });
        const lineIndex = diffStyle === "unified" ? unifiedLineIndex : splitLineIndex;
        const renderedLineContext = {
          type,
          hunkIndex,
          lineIndex,
          unifiedLineIndex,
          splitLineIndex,
          deletionLine,
          additionLine
        };
        if (diffStyle === "unified") {
          const injectedRows = (_a10 = this.getUnifiedInjectedRowsForLine) == null ? void 0 : _a10.call(this, renderedLineContext);
          if ((injectedRows == null ? void 0 : injectedRows.before) != null) pushUnifiedInjectedRows(injectedRows.before, context);
          let deletionLineContent = deletionLine != null ? deletionLines[deletionLine.lineIndex] : void 0;
          let additionLineContent = additionLine != null ? additionLines[additionLine.lineIndex] : void 0;
          if (deletionLineContent == null && additionLineContent == null) {
            const errorMessage = "DiffHunksRenderer.processDiffResult: deletionLine and additionLine are null, something is wrong";
            console.error(errorMessage, { file: fileDiff.name });
            throw new Error(errorMessage);
          }
          const lineType = type === "change" ? additionLine != null ? "change-addition" : "change-deletion" : type;
          const lineDecoration = this.getUnifiedLineDecoration({
            type,
            lineType,
            additionLineIndex: additionLine == null ? void 0 : additionLine.lineIndex,
            deletionLineIndex: deletionLine == null ? void 0 : deletionLine.lineIndex
          });
          pushGutterLineNumber("unified", lineDecoration.gutterLineType, additionLine != null ? additionLine.lineNumber : deletionLine.lineNumber, `${unifiedLineIndex},${splitLineIndex}`, lineDecoration.gutterProperties);
          if (additionLineContent != null) additionLineContent = withContentProperties(additionLineContent, lineDecoration.contentProperties, isRenderCacheDirty && additionLine != null ? {
            "data-line": additionLine.lineNumber,
            "data-line-index": `${unifiedLineIndex},${splitLineIndex}`
          } : void 0);
          else if (deletionLineContent != null) deletionLineContent = withContentProperties(deletionLineContent, lineDecoration.contentProperties, isRenderCacheDirty && deletionLine != null ? {
            "data-line": deletionLine.lineNumber,
            "data-line-index": `${unifiedLineIndex},${splitLineIndex}`
          } : void 0);
          pushLineWithAnnotation({
            diffStyle: "unified",
            type,
            deletionLine: deletionLineContent,
            additionLine: additionLineContent,
            unifiedSpan: this.getAnnotations("unified", deletionLine == null ? void 0 : deletionLine.lineNumber, additionLine == null ? void 0 : additionLine.lineNumber, hunkIndex, lineIndex),
            createAnnotationElement: (span) => this.createAnnotationElement(span),
            context
          });
          if ((injectedRows == null ? void 0 : injectedRows.after) != null) pushUnifiedInjectedRows(injectedRows.after, context);
        } else {
          const injectedRows = (_b = this.getSplitInjectedRowsForLine) == null ? void 0 : _b.call(this, renderedLineContext);
          if ((injectedRows == null ? void 0 : injectedRows.before) != null) pushSplitInjectedRows(injectedRows.before, context, pendingSplitContext);
          let deletionLineContent = deletionLine != null ? deletionLines[deletionLine.lineIndex] : void 0;
          let additionLineContent = additionLine != null ? additionLines[additionLine.lineIndex] : void 0;
          const deletionLineDecoration = this.getSplitLineDecoration({
            side: "deletions",
            type,
            lineIndex: deletionLine == null ? void 0 : deletionLine.lineIndex
          });
          const additionLineDecoration = this.getSplitLineDecoration({
            side: "additions",
            type,
            lineIndex: additionLine == null ? void 0 : additionLine.lineIndex
          });
          if (deletionLineContent == null && additionLineContent == null) {
            const errorMessage = "DiffHunksRenderer.processDiffResult: deletionLine and additionLine are null, something is wrong";
            console.error(errorMessage, { file: fileDiff.name });
            throw new Error(errorMessage);
          }
          const missingSide = (() => {
            if (type === "change") {
              if (additionLineContent == null) return "additions";
              else if (deletionLineContent == null) return "deletions";
            }
          })();
          if (missingSide != null) {
            if (pendingSplitContext.side != null && pendingSplitContext.side !== missingSide) pendingSplitContext.flush();
            pendingSplitContext.side = missingSide;
            pendingSplitContext.increment();
          } else if (type === "change") pendingSplitContext.flush();
          const annotationSpans = this.getAnnotations("split", deletionLine == null ? void 0 : deletionLine.lineNumber, additionLine == null ? void 0 : additionLine.lineNumber, hunkIndex, lineIndex);
          if (annotationSpans != null && pendingSplitContext.size > 0) pendingSplitContext.flush();
          if (deletionLine != null) {
            const deletionLineDecorated = withContentProperties(deletionLineContent, deletionLineDecoration.contentProperties, isRenderCacheDirty ? {
              "data-line": deletionLine.lineNumber,
              "data-line-index": `${deletionLine.unifiedLineIndex},${splitLineIndex}`
            } : void 0);
            pushGutterLineNumber("deletions", deletionLineDecoration.gutterLineType, deletionLine.lineNumber, `${deletionLine.unifiedLineIndex},${splitLineIndex}`, deletionLineDecoration.gutterProperties);
            if (deletionLineDecorated != null) deletionLineContent = deletionLineDecorated;
          }
          if (additionLine != null) {
            const additionLineDecorated = withContentProperties(additionLineContent, additionLineDecoration.contentProperties, isRenderCacheDirty ? {
              "data-line": additionLine.lineNumber,
              "data-line-index": `${additionLine.unifiedLineIndex},${splitLineIndex}`
            } : void 0);
            pushGutterLineNumber("additions", additionLineDecoration.gutterLineType, additionLine.lineNumber, `${additionLine.unifiedLineIndex},${splitLineIndex}`, additionLineDecoration.gutterProperties);
            if (additionLineDecorated != null) additionLineContent = additionLineDecorated;
          }
          pushLineWithAnnotation({
            diffStyle: "split",
            type,
            additionLine: additionLineContent,
            deletionLine: deletionLineContent,
            ...annotationSpans,
            createAnnotationElement: (span) => this.createAnnotationElement(span),
            context
          });
          if ((injectedRows == null ? void 0 : injectedRows.after) != null) pushSplitInjectedRows(injectedRows.after, context, pendingSplitContext);
        }
        const isFinalSplitHunkRow = diffStyle === "split" && hunk != null && splitLineIndex === hunk.splitLineStart + hunk.splitLineCount - 1;
        const isFinalHunkRow = hunkIndex === fileDiff.hunks.length - 1 && hunk != null && (diffStyle === "split" ? splitLineIndex === hunk.splitLineStart + hunk.splitLineCount - 1 : unifiedLineIndex === hunk.unifiedLineStart + hunk.unifiedLineCount - 1);
        const splitNoEOFCRDeletion = isFinalSplitHunkRow ? hunk.noEOFCRDeletions : false;
        const splitNoEOFCRAddition = isFinalSplitHunkRow ? hunk.noEOFCRAdditions : false;
        const noEOFCRDeletion = ((deletionLine == null ? void 0 : deletionLine.noEOFCR) ?? false) || splitNoEOFCRDeletion;
        const noEOFCRAddition = ((additionLine == null ? void 0 : additionLine.noEOFCR) ?? false) || splitNoEOFCRAddition;
        if (noEOFCRAddition || noEOFCRDeletion) {
          if (diffStyle === "split") pendingSplitContext.flush();
          if (noEOFCRDeletion) {
            const noEOFType = type === "context" || type === "context-expanded" ? type : "change-deletion";
            if (diffStyle === "unified") {
              context.unifiedContentAST.push(createNoNewlineElement(noEOFType));
              context.pushToGutter("unified", createGutterGap(noEOFType, "metadata", 1));
            } else {
              context.deletionsContentAST.push(createNoNewlineElement(noEOFType));
              context.pushToGutter("deletions", createGutterGap(noEOFType, "metadata", 1));
              if (!noEOFCRAddition) {
                context.pushToGutter("additions", createGutterGap(void 0, "buffer", 1));
                context.additionsContentAST.push(createEmptyRowBuffer(1));
              }
            }
          }
          if (noEOFCRAddition) {
            const noEOFType = type === "context" || type === "context-expanded" ? type : "change-addition";
            if (diffStyle === "unified") {
              context.unifiedContentAST.push(createNoNewlineElement(noEOFType));
              context.pushToGutter("unified", createGutterGap(noEOFType, "metadata", 1));
            } else {
              context.additionsContentAST.push(createNoNewlineElement(noEOFType));
              context.pushToGutter("additions", createGutterGap(noEOFType, "metadata", 1));
              if (!noEOFCRDeletion) {
                context.pushToGutter("deletions", createGutterGap(void 0, "buffer", 1));
                context.deletionsContentAST.push(createEmptyRowBuffer(1));
              }
            }
          }
          context.incrementRowCount(1);
        }
        if (hunkSeparators !== "simple" && hunkSeparators !== "metadata" && (collapsedAfter > 0 || isFinalHunkRow && canHydrateContext)) pushSeparators({
          hunkIndex: type === "context-expanded" ? hunkIndex : hunkIndex + 1,
          collapsedLines: isFinalHunkRow && canHydrateContext ? "unknown" : collapsedAfter,
          rangeSize: trailingRangeSize,
          hunkSpecs: void 0,
          isFirstHunk: false,
          isLastHunk: true,
          isExpandable: isExpandableDiff
        });
        context.incrementRowCount(1);
      }
    });
    if (diffStyle === "split") pendingSplitContext.flush();
    const totalLines = Math.max(getTotalLineCountFromHunks(fileDiff.hunks), fileDiff.additionLines.length ?? 0, fileDiff.deletionLines.length ?? 0);
    const hasBuffer = renderRange.bufferBefore > 0 || renderRange.bufferAfter > 0;
    const shouldIncludeAdditions = !unified && fileDiff.type !== "deleted";
    const shouldIncludeDeletions = !unified && fileDiff.type !== "new";
    const hasContent = context.rowCount > 0 || hasBuffer;
    additionsContentAST = shouldIncludeAdditions && hasContent ? additionsContentAST : void 0;
    deletionsContentAST = shouldIncludeDeletions && hasContent ? deletionsContentAST : void 0;
    unifiedContentAST = unified && hasContent ? unifiedContentAST : void 0;
    const preNode = this.createPreElement(deletionsContentAST != null && additionsContentAST != null, totalLines);
    return {
      unifiedGutterAST: unified && hasContent ? context.unifiedGutterAST.children : void 0,
      unifiedContentAST,
      deletionsGutterAST: shouldIncludeDeletions && hasContent ? context.deletionsGutterAST.children : void 0,
      deletionsContentAST,
      additionsGutterAST: shouldIncludeAdditions && hasContent ? context.additionsGutterAST.children : void 0,
      additionsContentAST,
      hunkData,
      preNode,
      themeStyles,
      baseThemeType,
      headerElement: !disableFileHeader ? this.renderHeader(this.diff) : void 0,
      totalLines,
      rowCount: context.rowCount,
      bufferBefore: renderRange.bufferBefore,
      bufferAfter: renderRange.bufferAfter,
      css: ""
    };
  }
  renderCodeAST(type, result) {
    const gutterAST = type === "unified" ? result.unifiedGutterAST : type === "deletions" ? result.deletionsGutterAST : result.additionsGutterAST;
    const contentAST = type === "unified" ? result.unifiedContentAST : type === "deletions" ? result.deletionsContentAST : result.additionsContentAST;
    if (gutterAST == null || contentAST == null) return;
    const gutter = createGutterWrapper(gutterAST);
    gutter.properties.style = `grid-row: span ${result.rowCount}`;
    return [gutter, createContentColumn(contentAST, result.rowCount)];
  }
  renderFullAST(result, children = []) {
    const containerSize = this.getOptionsWithDefaults().hunkSeparators === "line-info";
    const unifiedAST = this.renderCodeAST("unified", result);
    if (unifiedAST != null) {
      children.push(createHastElement({
        tagName: "code",
        children: unifiedAST,
        properties: {
          "data-code": "",
          "data-container-size": containerSize ? "" : void 0,
          "data-unified": ""
        }
      }));
      return {
        ...result.preNode,
        children
      };
    }
    const deletionsAST = this.renderCodeAST("deletions", result);
    if (deletionsAST != null) children.push(createHastElement({
      tagName: "code",
      children: deletionsAST,
      properties: {
        "data-code": "",
        "data-container-size": containerSize ? "" : void 0,
        "data-deletions": ""
      }
    }));
    const additionsAST = this.renderCodeAST("additions", result);
    if (additionsAST != null) children.push(createHastElement({
      tagName: "code",
      children: additionsAST,
      properties: {
        "data-code": "",
        "data-container-size": containerSize ? "" : void 0,
        "data-additions": ""
      }
    }));
    return {
      ...result.preNode,
      children
    };
  }
  renderFullHTML(result, tempChildren = []) {
    return toHtml(this.renderFullAST(result, tempChildren));
  }
  renderPartialHTML(children, columnType) {
    if (columnType == null) return toHtml(children);
    return toHtml(createHastElement({
      tagName: "code",
      children,
      properties: {
        "data-code": "",
        "data-container-size": this.getOptionsWithDefaults().hunkSeparators === "line-info" ? "" : void 0,
        [`data-${columnType}`]: ""
      }
    }));
  }
  pushFileLevelAnnotations(fileDiff, diffStyle, renderRange, context) {
    if (!shouldRenderFileAnnotations(renderRange)) return;
    const deletionAnnotationNames = fileDiff.type !== "new" ? getAnnotationNames(getFileAnnotations(this.deletionAnnotations)) : [];
    const additionAnnotationNames = fileDiff.type !== "deleted" ? getAnnotationNames(getFileAnnotations(this.additionAnnotations)) : [];
    if (deletionAnnotationNames.length === 0 && additionAnnotationNames.length === 0) return;
    const hunkIndex = -1;
    const lineIndex = -1;
    const { createAnnotationElement: createAnnotationElement2 } = this;
    if (diffStyle === "unified") {
      pushLineWithAnnotation({
        diffStyle,
        type: "context",
        unifiedSpan: {
          type: "annotation",
          hunkIndex,
          lineIndex,
          annotations: deletionAnnotationNames.concat(additionAnnotationNames)
        },
        createAnnotationElement: createAnnotationElement2,
        context
      });
      return;
    }
    pushLineWithAnnotation({
      diffStyle,
      type: "context",
      deletionSpan: {
        type: "annotation",
        hunkIndex,
        lineIndex,
        annotations: deletionAnnotationNames
      },
      additionSpan: {
        type: "annotation",
        hunkIndex,
        lineIndex,
        annotations: additionAnnotationNames
      },
      createAnnotationElement: createAnnotationElement2,
      context
    });
  }
  getAnnotations(type, deletionLineNumber, additionLineNumber, hunkIndex, lineIndex) {
    const deletionSpan = {
      type: "annotation",
      hunkIndex,
      lineIndex,
      annotations: []
    };
    if (deletionLineNumber != null) for (const anno of this.deletionAnnotations[deletionLineNumber] ?? []) deletionSpan.annotations.push(getLineAnnotationName(anno));
    const additionSpan = {
      type: "annotation",
      hunkIndex,
      lineIndex,
      annotations: []
    };
    if (additionLineNumber != null) for (const anno of this.additionAnnotations[additionLineNumber] ?? []) (type === "unified" ? deletionSpan : additionSpan).annotations.push(getLineAnnotationName(anno));
    if (type === "unified") {
      if (deletionSpan.annotations.length > 0) return deletionSpan;
      return;
    }
    if (additionSpan.annotations.length === 0 && deletionSpan.annotations.length === 0) return;
    return {
      deletionSpan,
      additionSpan
    };
  }
  renderHeader(diff) {
    const { headerRenderMode, stickyHeader } = this.getOptionsWithDefaults();
    return createFileHeaderElement({
      fileOrDiff: diff,
      mode: headerRenderMode,
      stickyHeader
    });
  }
};
function getAnnotationNames(annotations) {
  return (annotations == null ? void 0 : annotations.map((annotation) => getLineAnnotationName(annotation))) ?? [];
}
var EN_PLURAL_RULES = new Intl.PluralRules("en-US");
function getModifiedLinesString(lines) {
  return `${lines} unmodified line${EN_PLURAL_RULES.select(lines) === "one" ? "" : "s"}`;
}
function pushUnifiedInjectedRows(rows, context) {
  for (const row of rows) {
    context.unifiedContentAST.push(row.content);
    context.pushToGutter("unified", row.gutter);
    context.incrementRowCount(1);
  }
}
function pushSplitInjectedRows(rows, context, pendingSplitContext) {
  for (const { deletion, addition } of rows) {
    if (deletion == null && addition == null) continue;
    const missingSide = deletion != null && addition != null ? void 0 : deletion == null ? "deletions" : "additions";
    if (missingSide == null || pendingSplitContext.side !== missingSide) pendingSplitContext.flush();
    if (deletion != null) {
      context.deletionsContentAST.push(deletion.content);
      context.pushToGutter("deletions", deletion.gutter);
    }
    if (addition != null) {
      context.additionsContentAST.push(addition.content);
      context.pushToGutter("additions", addition.gutter);
    }
    if (missingSide != null) {
      pendingSplitContext.side = missingSide;
      pendingSplitContext.increment();
    }
    context.incrementRowCount(1);
  }
}
function pushLineWithAnnotation({ diffStyle, type, deletionLine, additionLine, unifiedSpan, deletionSpan, additionSpan, createAnnotationElement: createAnnotationElement2, context }) {
  let hasAnnotationRow = false;
  if (diffStyle === "unified") {
    if (additionLine != null) context.unifiedContentAST.push(additionLine);
    else if (deletionLine != null) context.unifiedContentAST.push(deletionLine);
    if (unifiedSpan != null) {
      const lineType = type === "change" ? deletionLine != null ? "change-deletion" : "change-addition" : type;
      context.unifiedContentAST.push(createAnnotationElement2(unifiedSpan));
      context.pushToGutter("unified", createGutterGap(lineType, "annotation", 1));
      hasAnnotationRow = true;
    }
  } else if (diffStyle === "split") {
    if (deletionLine != null) context.deletionsContentAST.push(deletionLine);
    if (additionLine != null) context.additionsContentAST.push(additionLine);
    if (deletionSpan != null) {
      const lineType = type === "change" ? deletionLine != null ? "change-deletion" : "context" : type;
      context.deletionsContentAST.push(createAnnotationElement2(deletionSpan));
      context.pushToGutter("deletions", createGutterGap(lineType, "annotation", 1));
      hasAnnotationRow = true;
    }
    if (additionSpan != null) {
      const lineType = type === "change" ? additionLine != null ? "change-addition" : "context" : type;
      context.additionsContentAST.push(createAnnotationElement2(additionSpan));
      context.pushToGutter("additions", createGutterGap(lineType, "annotation", 1));
      hasAnnotationRow = true;
    }
  }
  if (hasAnnotationRow) context.incrementRowCount(1);
}
function pushSeparator(type, { hunkIndex, collapsedLines, rangeSize, hunkSpecs, isFirstHunk, isLastHunk, isExpandable }, context) {
  if (typeof collapsedLines === "number" && collapsedLines <= 0) return;
  const linesAST = type === "unified" ? context.unifiedContentAST : type === "deletions" ? context.deletionsContentAST : context.additionsContentAST;
  if (context.hunkSeparators === "metadata") {
    if (hunkSpecs != null) {
      context.pushToGutter(type, createSeparator({
        type: "metadata",
        content: hunkSpecs,
        isFirstHunk,
        isLastHunk
      }));
      linesAST.push(createSeparator({
        type: "metadata",
        content: hunkSpecs,
        isFirstHunk,
        isLastHunk
      }));
      if (type !== "additions") context.incrementRowCount(1);
    }
    return;
  }
  if (context.hunkSeparators === "simple") {
    if (hunkIndex > 0) {
      context.pushToGutter(type, createSeparator({
        type: "simple",
        isFirstHunk,
        isLastHunk: false
      }));
      linesAST.push(createSeparator({
        type: "simple",
        isFirstHunk,
        isLastHunk: false
      }));
      if (type !== "additions") context.incrementRowCount(1);
    }
    return;
  }
  const slotName = getHunkSeparatorSlotName(type, hunkIndex);
  const chunked = rangeSize > context.expansionLineCount;
  const expandIndex = isExpandable ? hunkIndex : void 0;
  const content = typeof collapsedLines === "number" ? getModifiedLinesString(collapsedLines) : "More unchanged context may be available";
  context.pushToGutter(type, createSeparator({
    type: context.hunkSeparators,
    content,
    expandIndex,
    chunked,
    slotName,
    isFirstHunk,
    isLastHunk
  }));
  linesAST.push(createSeparator({
    type: context.hunkSeparators,
    content,
    expandIndex,
    chunked,
    slotName,
    isFirstHunk,
    isLastHunk
  }));
  if (type !== "additions") context.incrementRowCount(1);
  context.hunkData.push({
    slotName,
    hunkIndex,
    lines: typeof collapsedLines === "number" ? collapsedLines : 0,
    lineCountKnown: typeof collapsedLines === "number",
    type,
    expandable: isExpandable ? {
      up: !isFirstHunk,
      down: !isLastHunk,
      chunked
    } : void 0
  });
}
function withContentProperties(lineNode, contentProperties, extendProperties) {
  if (lineNode == null || lineNode.type !== "element" || contentProperties == null && extendProperties == null) return lineNode;
  return {
    ...lineNode,
    properties: {
      ...lineNode.properties,
      ...contentProperties,
      ...extendProperties
    }
  };
}
function contentLineCount(lines) {
  return lines.length > 0 && lines[lines.length - 1] === "" ? lines.length - 1 : lines.length;
}
function realignAdditionHastLines(previousLines, nextLines, hastLines, textDocument) {
  const previousContentLength = contentLineCount(previousLines);
  const nextContentLength = contentLineCount(nextLines);
  const maxShared = Math.min(previousContentLength, nextContentLength);
  let prefix = 0;
  while (prefix < maxShared && previousLines[prefix] === nextLines[prefix]) prefix++;
  let suffix = 0;
  while (suffix < maxShared - prefix && previousLines[previousContentLength - 1 - suffix] === nextLines[nextContentLength - 1 - suffix]) suffix++;
  const realigned = new Array(nextLines.length);
  for (let index = 0; index < prefix; index++) realigned[index] = hastLines[index];
  for (let offset = 0; offset < suffix; offset++) realigned[nextContentLength - 1 - offset] = hastLines[previousContentLength - 1 - offset];
  if (previousContentLength < previousLines.length && nextContentLength < nextLines.length) realigned[nextLines.length - 1] = hastLines[previousLines.length - 1];
  for (let index = previousLines.length; index < nextLines.length; index++) realigned[index] ?? (realigned[index] = hastLines[index]);
  for (let index = prefix; index < nextLines.length; index++) realigned[index] ?? (realigned[index] = createPlainAdditionLineElement(index, textDocument.getLineText(index)));
  return realigned;
}
function createPlainAdditionLineElement(lineIndex, lineText, unifiedLineIndex = lineIndex, splitLineIndex = lineIndex) {
  return {
    type: "element",
    tagName: "div",
    properties: {
      "data-line": lineIndex + 1,
      "data-line-index": `${unifiedLineIndex},${splitLineIndex}`,
      "data-line-type": "context"
    },
    children: [{
      type: "element",
      tagName: "span",
      properties: { "data-char": 0 },
      children: [{
        type: "text",
        value: lineText
      }]
    }]
  };
}
function getEditorDocumentLines(textDocument, previousLines) {
  const lines = [];
  const fallbackLineBreak = getFallbackLineBreak(previousLines);
  for (let line = 0; line < textDocument.lineCount; line++) {
    const lineText = textDocument.getLineText(line, true);
    lines.push(line < textDocument.lineCount - 1 && !hasLineBreakSuffix(lineText) ? lineText + fallbackLineBreak : lineText);
  }
  return lines;
}
function hasLineBreakSuffix(line) {
  return line.endsWith("\n") || line.endsWith("\r");
}
function getFallbackLineBreak(lines) {
  for (const line of lines) {
    if (line.endsWith("\r\n")) return "\r\n";
    if (line.endsWith("\n")) return "\n";
    if (line.endsWith("\r")) return "\r";
  }
  return "\n";
}
function isDiffMassive(diff, tokenizeMaxLength) {
  return Math.max(diff.additionLines.length, diff.deletionLines.length) > tokenizeMaxLength;
}
function canHydrateCollapsedContext(fileDiff, hasFileLoader) {
  return fileDiff.isPartial && hasFileLoader && (fileDiff.type === "change" || fileDiff.type === "rename-changed");
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areDiffLineAnnotationsEqual.js
function areDiffLineAnnotationsEqual(annotationA, annotationB) {
  return annotationA.lineNumber === annotationB.lineNumber && annotationA.side === annotationB.side && annotationA.metadata === annotationB.metadata;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areHunkDataEqual.js
function areHunkDataEqual(hunkA, hunkB) {
  var _a9, _b, _c, _d, _e, _f;
  return hunkA.slotName === hunkB.slotName && hunkA.hunkIndex === hunkB.hunkIndex && hunkA.lines === hunkB.lines && hunkA.lineCountKnown === hunkB.lineCountKnown && hunkA.type === hunkB.type && ((_a9 = hunkA.expandable) == null ? void 0 : _a9.chunked) === ((_b = hunkB.expandable) == null ? void 0 : _b.chunked) && ((_c = hunkA.expandable) == null ? void 0 : _c.up) === ((_d = hunkB.expandable) == null ? void 0 : _d.up) && ((_e = hunkA.expandable) == null ? void 0 : _e.down) === ((_f = hunkB.expandable) == null ? void 0 : _f.down);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/awaitWithTimeout.js
async function awaitWithTimeout(callback, timeout = 300) {
  let timeoutId;
  try {
    await Promise.race([callback(), new Promise((resolve) => {
      timeoutId = setTimeout(resolve, timeout);
    })]);
  } finally {
    if (timeoutId != null) clearTimeout(timeoutId);
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getDiffFileInput.js
function getDiffFileInput({ oldFile, newFile }, context) {
  if (oldFile === void 0 && newFile === void 0) return;
  if (oldFile === void 0 || newFile === void 0) throw new Error(`${context}: Pass null for an intentionally missing oldFile or newFile side`);
  if (oldFile === null) {
    if (newFile === null) throw new Error(`${context}: You must pass oldFile, newFile, or both`);
    return {
      oldFile,
      newFile
    };
  }
  if (newFile === null) return {
    oldFile,
    newFile
  };
  return {
    oldFile,
    newFile
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getDiffHunksRendererOptions.js
function getDiffHunksRendererOptions(options) {
  return {
    theme: options == null ? void 0 : options.theme,
    disableLineNumbers: options == null ? void 0 : options.disableLineNumbers,
    overflow: options == null ? void 0 : options.overflow,
    collapsed: options == null ? void 0 : options.collapsed,
    disableFileHeader: options == null ? void 0 : options.disableFileHeader,
    disableVirtualizationBuffers: options == null ? void 0 : options.disableVirtualizationBuffers,
    stickyHeader: options == null ? void 0 : options.stickyHeader,
    preferredHighlighter: options == null ? void 0 : options.preferredHighlighter,
    useCSSClasses: options == null ? void 0 : options.useCSSClasses,
    useTokenTransformer: shouldUseTokenTransformer(options),
    tokenizeMaxLineLength: options == null ? void 0 : options.tokenizeMaxLineLength,
    tokenizeMaxLength: options == null ? void 0 : options.tokenizeMaxLength,
    diffStyle: options == null ? void 0 : options.diffStyle,
    diffIndicators: options == null ? void 0 : options.diffIndicators,
    disableBackground: options == null ? void 0 : options.disableBackground,
    hunkSeparators: typeof (options == null ? void 0 : options.hunkSeparators) === "function" ? "custom" : options == null ? void 0 : options.hunkSeparators,
    expandUnchanged: options == null ? void 0 : options.expandUnchanged,
    loadDiffFiles: options == null ? void 0 : options.loadDiffFiles,
    collapsedContextThreshold: options == null ? void 0 : options.collapsedContextThreshold,
    lineDiffType: options == null ? void 0 : options.lineDiffType,
    maxLineDiffLength: options == null ? void 0 : options.maxLineDiffLength,
    expansionLineCount: options == null ? void 0 : options.expansionLineCount,
    headerRenderMode: (options == null ? void 0 : options.renderCustomHeader) != null ? "custom" : "default"
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/FileDiff.js
function canHydrateDiff(fileDiff) {
  return fileDiff.isPartial && (fileDiff.type === "change" || fileDiff.type === "rename-changed" || fileDiff.type === "rename-pure");
}
var instanceId5 = -1;
var _a6;
var FileDiff = (_a6 = class {
  constructor(options = { theme: DEFAULT_THEMES }, workerManager, isContainerManaged = false) {
    __publicField(this, "options");
    __publicField(this, "workerManager");
    __publicField(this, "isContainerManaged");
    __publicField(this, "__id", `file-diff:${++instanceId5}`);
    __publicField(this, "type", "file-diff");
    __publicField(this, "fileContainer");
    __publicField(this, "spriteSVG");
    __publicField(this, "pre");
    __publicField(this, "codeUnified");
    __publicField(this, "codeDeletions");
    __publicField(this, "codeAdditions");
    __publicField(this, "bufferBefore");
    __publicField(this, "bufferAfter");
    __publicField(this, "themeCSSStyle");
    __publicField(this, "appliedThemeCSS");
    __publicField(this, "hasAdoptedThemeCSS", false);
    __publicField(this, "unsafeCSSStyle");
    __publicField(this, "appliedUnsafeCSS");
    __publicField(this, "gutterUtilityContent");
    __publicField(this, "headerElement");
    __publicField(this, "headerPrefix");
    __publicField(this, "headerFilenameSuffix");
    __publicField(this, "headerMetadata");
    __publicField(this, "headerCustom");
    __publicField(this, "separatorCache", /* @__PURE__ */ new Map());
    __publicField(this, "errorWrapper");
    __publicField(this, "placeHolder");
    __publicField(this, "hunksRenderer");
    __publicField(this, "resizeManager");
    __publicField(this, "scrollSyncManager");
    __publicField(this, "interactionManager");
    __publicField(this, "annotationCache", /* @__PURE__ */ new Map());
    __publicField(this, "lineAnnotations", []);
    __publicField(this, "managersDirty", false);
    __publicField(this, "deletionFile");
    __publicField(this, "additionFile");
    __publicField(this, "fileDiff");
    __publicField(this, "renderRange");
    __publicField(this, "pendingFiles");
    __publicField(this, "appliedPreAttributes");
    __publicField(this, "lastRenderedHeaderHTML");
    __publicField(this, "cachedHeaderHTML");
    __publicField(this, "lastRowCount");
    __publicField(this, "mounted", false);
    __publicField(this, "enabled", true);
    __publicField(this, "editor");
    __publicField(this, "refreshViewTimeout");
    __publicField(this, "lineStateRefreshPending", false);
    __publicField(this, "deferredSelectedLines");
    __publicField(this, "deferredEditorActiveLine");
    __publicField(this, "handleHighlightRender", () => {
      this.rerender();
    });
    __publicField(this, "getLineIndex", (lineNumber, side = "additions") => {
      const fileDiff = this.fileDiffCache;
      if (fileDiff == null) return;
      const lastHunk = fileDiff.hunks.at(-1);
      let targetUnifiedIndex;
      let targetSplitIndex;
      hunkIterator: for (const hunk of fileDiff.hunks) {
        const hunkStart = side === "deletions" ? hunk.deletionStart : hunk.additionStart;
        const hunkCount = side === "deletions" ? hunk.deletionCount : hunk.additionCount;
        let currentLineNumber = getHunkSideStartBoundary(hunkStart, hunkCount) + 1;
        let splitIndex = hunk.splitLineStart;
        let unifiedIndex = hunk.unifiedLineStart;
        if (lineNumber < currentLineNumber) {
          const difference = currentLineNumber - lineNumber;
          targetUnifiedIndex = Math.max(unifiedIndex - difference, 0);
          targetSplitIndex = Math.max(splitIndex - difference, 0);
          break hunkIterator;
        }
        if (lineNumber >= currentLineNumber + hunkCount) {
          if (hunk === lastHunk) {
            const difference = lineNumber - (currentLineNumber + hunkCount);
            targetUnifiedIndex = unifiedIndex + hunk.unifiedLineCount + difference;
            targetSplitIndex = splitIndex + hunk.splitLineCount + difference;
            break hunkIterator;
          }
          continue;
        }
        for (const content of hunk.hunkContent) if (content.type === "context") if (lineNumber < currentLineNumber + content.lines) {
          const difference = lineNumber - currentLineNumber;
          targetSplitIndex = splitIndex + difference;
          targetUnifiedIndex = unifiedIndex + difference;
          break hunkIterator;
        } else {
          currentLineNumber += content.lines;
          splitIndex += content.lines;
          unifiedIndex += content.lines;
        }
        else {
          const sideCount = side === "deletions" ? content.deletions : content.additions;
          if (lineNumber < currentLineNumber + sideCount) {
            const indexDifference = lineNumber - currentLineNumber;
            targetUnifiedIndex = unifiedIndex + (side === "additions" ? content.deletions : 0) + indexDifference;
            targetSplitIndex = splitIndex + indexDifference;
            break hunkIterator;
          } else {
            currentLineNumber += sideCount;
            splitIndex += Math.max(content.deletions, content.additions);
            unifiedIndex += content.deletions + content.additions;
          }
        }
        break hunkIterator;
      }
      if (targetUnifiedIndex == null || targetSplitIndex == null) return;
      return [targetUnifiedIndex, targetSplitIndex];
    });
    __publicField(this, "getHoveredLine", () => {
      return this.interactionManager.getHoveredLine();
    });
    __publicField(this, "handleExpandHunk", (hunkIndex, direction, expansionLineCountOverride) => {
      this.expandHunk(hunkIndex, direction, expansionLineCountOverride);
    });
    __publicField(this, "expandHunk", (hunkIndex, direction, expansionLineCountOverride) => {
      this.hunksRenderer.expandHunk(hunkIndex, direction, expansionLineCountOverride);
      this.loadFilesIfNecessary();
      this.rerender();
    });
    __publicField(this, "handleEditSessionRender", () => {
      this.rerender();
      this.flushDeferredLineState();
    });
    var _a9;
    this.options = options;
    this.workerManager = workerManager;
    this.isContainerManaged = isContainerManaged;
    this.hunksRenderer = this.createHunksRenderer(options);
    this.resizeManager = new ResizeManager();
    this.scrollSyncManager = new ScrollSyncManager();
    this.interactionManager = new InteractionManager("diff", pluckInteractionOptions(options, typeof options.hunkSeparators === "function" || (options.hunkSeparators ?? "line-info") === "line-info" || options.hunkSeparators === "line-info-basic" ? this.handleExpandHunk : void 0, this.getLineIndex));
    (_a9 = this.workerManager) == null ? void 0 : _a9.subscribeToThemeChanges(this);
    this.enabled = true;
  }
  getHunksRendererOptions(options) {
    return getDiffHunksRendererOptions(options);
  }
  createHunksRenderer(options) {
    return new DiffHunksRenderer(this.getHunksRendererOptions(options), this.handleHighlightRender, this.workerManager);
  }
  setOptions(options) {
    if (options == null) return;
    this.options = options;
    this.cachedHeaderHTML = void 0;
    this.hunksRenderer.setOptions(this.getHunksRendererOptions(options));
    this.syncInteractionOptions();
  }
  syncInteractionOptions() {
    this.interactionManager.setOptions(pluckInteractionOptions(this.options, typeof this.options.hunkSeparators === "function" || (this.options.hunkSeparators ?? "line-info") === "line-info" || this.options.hunkSeparators === "line-info-basic" ? this.handleExpandHunk : void 0, this.getLineIndex));
  }
  mergeOptions(options) {
    this.options = {
      ...this.options,
      ...options
    };
  }
  setThemeType(themeType) {
    if ((this.options.themeType ?? "system") === themeType) return;
    this.mergeOptions({ themeType });
    this.applyCachedThemeState(themeType);
  }
  applyCachedThemeState(themeType) {
    if (typeof this.options.theme === "string" || this.fileContainer == null || this.appliedThemeCSS == null) return false;
    const effectiveThemeType = this.appliedThemeCSS.baseThemeType ?? themeType;
    if (this.appliedThemeCSS.themeType === effectiveThemeType) return false;
    this.applyThemeState(this.fileContainer, this.appliedThemeCSS.themeStyles, themeType, this.appliedThemeCSS.baseThemeType);
    return true;
  }
  hasThemeChanged() {
    return this.appliedThemeCSS != null && !areThemesEqual(this.appliedThemeCSS.theme, this.options.theme ?? DEFAULT_THEMES);
  }
  setLineAnnotations(lineAnnotations) {
    this.lineAnnotations = lineAnnotations;
  }
  canPartiallyRender(forceRender, annotationsChanged, didContentChange) {
    if (forceRender || annotationsChanged || didContentChange || typeof this.options.hunkSeparators === "function") return false;
    return true;
  }
  setSelectedLines(range2, options) {
    if (this.lineStateRefreshPending) this.deferredSelectedLines = [range2, options];
    else this.interactionManager.setSelection(range2, options);
  }
  setEditorActiveLine(lineNumber, options) {
    if (this.lineStateRefreshPending) this.deferredEditorActiveLine = [lineNumber, options];
    else this.interactionManager.setEditorActiveLine(lineNumber, {
      lineNumberOnly: options == null ? void 0 : options.lineNumberOnly,
      side: (options == null ? void 0 : options.side) ?? "additions"
    });
  }
  flushDeferredLineState() {
    const { deferredEditorActiveLine: editorActiveLine, deferredSelectedLines: selectedLines } = this;
    this.lineStateRefreshPending = false;
    this.deferredEditorActiveLine = void 0;
    this.deferredSelectedLines = void 0;
    if (editorActiveLine != null) this.setEditorActiveLine(...editorActiveLine);
    if (selectedLines != null) this.interactionManager.setSelection(...selectedLines);
  }
  flushManagers() {
    if (!this.managersDirty || this.pre == null) {
      this.managersDirty = false;
      return;
    }
    const { diffStyle = "split", overflow = "scroll" } = this.options;
    this.interactionManager.setup(this.pre);
    this.resizeManager.setup(this.pre, {
      disableAnnotations: overflow === "wrap",
      columnVariables: this.shouldApplyColumnVariables(overflow) ? "apply" : "measure"
    });
    if (overflow === "scroll" && diffStyle === "split") this.scrollSyncManager.setup(this.pre, this.codeDeletions, this.codeAdditions);
    else this.scrollSyncManager.cleanUp();
    this.managersDirty = false;
  }
  shouldApplyColumnVariables(overflow) {
    var _a9;
    if (typeof this.options.hunkSeparators === "function") return true;
    return overflow === "scroll" && (this.lineAnnotations.length > 0 || ((_a9 = this.pre) == null ? void 0 : _a9.hasAttribute("data-has-merge-conflict")) === true);
  }
  getCodeScrollLeft() {
    var _a9, _b, _c;
    return Math.max(((_a9 = this.codeUnified) == null ? void 0 : _a9.scrollLeft) ?? 0, ((_b = this.codeDeletions) == null ? void 0 : _b.scrollLeft) ?? 0, ((_c = this.codeAdditions) == null ? void 0 : _c.scrollLeft) ?? 0);
  }
  setCodeScrollLeft(position) {
    if (this.codeUnified != null) this.codeUnified.scrollLeft = position;
    if (this.codeAdditions != null) this.codeAdditions.scrollLeft = position;
    if (this.codeDeletions != null) this.codeDeletions.scrollLeft = position;
  }
  __getEffectiveCodeOptions() {
    return {
      ...this.options,
      ...this.hunksRenderer.getEffectiveCodeOptions()
    };
  }
  cleanUp(recycle = false) {
    var _a9, _b, _c, _d, _e, _f, _g;
    dequeueRender(this.handleEditSessionRender);
    this.emitPostRender(true);
    (_a9 = this.editor) == null ? void 0 : _a9.cleanUp(recycle);
    this.editor = void 0;
    this.resizeManager.cleanUp();
    this.interactionManager.cleanUp();
    this.scrollSyncManager.cleanUp();
    this.managersDirty = false;
    (_b = this.workerManager) == null ? void 0 : _b.unsubscribeToThemeChanges(this);
    this.renderRange = void 0;
    this.pendingFiles = void 0;
    if (!this.isContainerManaged) (_c = this.fileContainer) == null ? void 0 : _c.remove();
    this.fileContainer = void 0;
    this.mounted = false;
    if (!recycle) this.lineAnnotations = [];
    this.clearAuxiliaryNodes();
    this.annotationCache.clear();
    this.pre = void 0;
    this.codeUnified = void 0;
    this.codeDeletions = void 0;
    this.codeAdditions = void 0;
    (_d = this.bufferBefore) == null ? void 0 : _d.remove();
    this.bufferBefore = void 0;
    (_e = this.bufferAfter) == null ? void 0 : _e.remove();
    this.bufferAfter = void 0;
    this.appliedPreAttributes = void 0;
    this.headerElement = void 0;
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
    (_f = this.placeHolder) == null ? void 0 : _f.remove();
    this.placeHolder = void 0;
    this.lastRenderedHeaderHTML = void 0;
    if (!recycle) this.cachedHeaderHTML = void 0;
    (_g = this.errorWrapper) == null ? void 0 : _g.remove();
    this.errorWrapper = void 0;
    this.spriteSVG = void 0;
    this.lastRowCount = void 0;
    this.themeCSSStyle = void 0;
    this.appliedThemeCSS = void 0;
    this.hasAdoptedThemeCSS = false;
    this.unsafeCSSStyle = void 0;
    this.appliedUnsafeCSS = void 0;
    if (recycle) this.hunksRenderer.recycle();
    else {
      this.hunksRenderer.cleanUp();
      this.workerManager = void 0;
      this.fileDiff = void 0;
      this.deletionFile = void 0;
      this.additionFile = void 0;
    }
    if (this.refreshViewTimeout != null) {
      clearTimeout(this.refreshViewTimeout);
      this.refreshViewTimeout = void 0;
    }
    this.lineStateRefreshPending = false;
    this.deferredEditorActiveLine = void 0;
    this.deferredSelectedLines = void 0;
    this.enabled = false;
  }
  virtualizedSetup() {
    var _a9;
    this.enabled = true;
    (_a9 = this.workerManager) == null ? void 0 : _a9.subscribeToThemeChanges(this);
  }
  hydrate({ fileContainer, prerenderedHTML, preventEmit = false, lineAnnotations, fileDiff, ...fileInputProps }) {
    if (!this.enabled) throw new Error("FileDiff.hydrate: attempting to call hydrate after cleaned up");
    if (this.fileContainer != null) throw new Error("FileDiff.hydrate: hydrate can only be called before the instance has rendered or hydrated");
    const fileInput = getDiffFileInput(fileInputProps, "FileDiff.hydrate");
    const oldFile = fileInput == null ? void 0 : fileInput.oldFile;
    const newFile = fileInput == null ? void 0 : fileInput.newFile;
    this.hydrateElements(fileContainer, prerenderedHTML);
    if (shouldRenderCode2(this.pre, hasDiffContent({
      fileDiff,
      oldFile,
      newFile
    }), this.options.collapsed) || shouldRenderHeader2(this.headerElement, hasDiffHeaderContent({
      fileDiff,
      oldFile,
      newFile
    }), this.options.disableFileHeader)) this.render({
      ...fileInputProps,
      fileContainer,
      lineAnnotations,
      fileDiff,
      preventEmit: true
    });
    else this.hydrationSetup({
      fileDiff,
      lineAnnotations,
      ...fileInput
    });
    if (!preventEmit) this.emitPostRender();
  }
  hydrateElements(fileContainer, prerenderedHTML) {
    var _a9;
    if (this.fileContainer !== fileContainer) this.emitPostRender(true);
    prerenderHTMLIfNecessary(fileContainer, prerenderedHTML);
    for (const element2 of ((_a9 = fileContainer.shadowRoot) == null ? void 0 : _a9.children) ?? []) {
      if (element2 instanceof SVGElement) {
        this.spriteSVG = element2;
        continue;
      }
      if (!(element2 instanceof HTMLElement)) continue;
      if (element2 instanceof HTMLPreElement) {
        this.pre = element2;
        for (const code of element2.children) {
          if (!(code instanceof HTMLElement) || code.tagName.toLowerCase() !== "code") continue;
          if ("deletions" in code.dataset) this.codeDeletions = code;
          if ("additions" in code.dataset) this.codeAdditions = code;
          if ("unified" in code.dataset) this.codeUnified = code;
        }
        continue;
      }
      if ("diffsHeader" in element2.dataset) {
        this.headerElement = element2;
        continue;
      }
      if (element2 instanceof HTMLStyleElement && element2.hasAttribute("data-theme-css")) {
        this.themeCSSStyle = element2;
        continue;
      }
      if (element2 instanceof HTMLStyleElement && element2.hasAttribute("data-unsafe-css")) {
        this.unsafeCSSStyle = element2;
        this.appliedUnsafeCSS = element2.textContent;
        continue;
      }
    }
    if (this.pre != null) {
      this.syncCodeNodesFromPre(this.pre);
      this.pre.removeAttribute("data-dehydrated");
    }
    this.fileContainer = fileContainer;
    this.hydrateMeasuredScrollbar();
  }
  hydrationSetup({ fileDiff, oldFile, newFile, lineAnnotations }) {
    this.lineAnnotations = lineAnnotations ?? this.lineAnnotations;
    this.additionFile = newFile;
    this.deletionFile = oldFile;
    this.fileDiff = fileDiff ?? (oldFile !== void 0 && newFile !== void 0 ? parseDiffFromFile(oldFile, newFile, this.options.parseDiffOptions) : void 0);
    if (this.pre == null) return;
    this.syncInteractionOptions();
    this.hunksRenderer.hydrate(this.fileDiff);
    this.renderAnnotations();
    this.renderGutterUtility();
    this.injectUnsafeCSS();
    this.managersDirty = true;
    this.flushManagers();
  }
  rerender() {
    if (!this.enabled || this.fileDiff == null && this.additionFile == null && this.deletionFile == null) return;
    this.render({
      forceRender: true,
      renderRange: this.renderRange
    });
  }
  onThemeChange() {
    this.hunksRenderer.clearRenderCache();
    this.rerender();
  }
  loadFilesIfNecessary() {
    var _a9;
    const { fileDiff, options: { loadDiffFiles } } = this;
    if (fileDiff == null || loadDiffFiles == null || !canHydrateDiff(fileDiff) || ((_a9 = this.pendingFiles) == null ? void 0 : _a9.fileDiff) === fileDiff) return;
    this.pendingFiles = {
      fileDiff,
      promise: this.loadFilesForDiff(fileDiff, loadDiffFiles)
    };
  }
  async loadFilesForDiff(fileDiff, loadDiffFiles) {
    var _a9;
    try {
      const files = await loadDiffFiles(fileDiff);
      if (!this.enabled || this.fileDiff !== fileDiff) return;
      await this.handleFilesLoaded(fileDiff, files);
    } catch (error) {
      if (this.options.disableErrorHandling === true) throw error;
      console.error(error);
    } finally {
      if (((_a9 = this.pendingFiles) == null ? void 0 : _a9.fileDiff) === fileDiff) this.pendingFiles = void 0;
    }
  }
  async handleFilesLoaded(expectedDiff, files) {
    if (this.fileDiff !== expectedDiff || !expectedDiff.isPartial) return;
    hydratePartialDiff("merge", expectedDiff, files);
    this.setHydratedState(files);
    await awaitWithTimeout(() => this.primeHighlightCache(expectedDiff));
    if (!this.enabled || this.fileDiff !== expectedDiff) return;
    this.rerender();
  }
  setHydratedState(files) {
    var _a9;
    this.deletionFile = files.oldFile;
    this.additionFile = files.newFile;
    (_a9 = this.workerManager) == null ? void 0 : _a9.cleanUpTasks(this.hunksRenderer);
    this.hunksRenderer.clearRenderCache();
  }
  render({ fileDiff, deferManagers = false, forceRender = false, preventEmit = false, lineAnnotations, fileContainer, containerWrapper, renderRange, ...fileInputProps }) {
    var _a9, _b;
    const fileInput = getDiffFileInput(fileInputProps, "FileDiff.render");
    const oldFile = fileInput == null ? void 0 : fileInput.oldFile;
    const newFile = fileInput == null ? void 0 : fileInput.newFile;
    if (!this.enabled) throw new Error("FileDiff.render: attempting to call render after cleaned up");
    if (fileDiff != null && fileDiff.cacheKey === void 0) fileDiff.cacheKey = fileDiff.prevName != null ? fileDiff.prevName + ":" + fileDiff.name : fileDiff.name;
    (_a9 = this.editor) == null ? void 0 : _a9.__postponeBgTokenizeToNextFrame();
    const { collapsed = false, themeType = "system", expandUnchanged = false } = this.options;
    const nextRenderRange = collapsed ? void 0 : renderRange;
    const themeChanged = this.hasThemeChanged();
    const hasFileInput = fileInput != null;
    const filesDidChange = hasFileInput && (!areOptionalFilesEqual(oldFile, this.deletionFile) || !areOptionalFilesEqual(newFile, this.additionFile));
    let diffDidChange = fileDiff != null && fileDiff !== this.fileDiff;
    const annotationsChanged = lineAnnotations != null && (lineAnnotations.length > 0 || this.lineAnnotations.length > 0) ? lineAnnotations !== this.lineAnnotations : false;
    if (!collapsed && areRenderRangesEqual(nextRenderRange, this.renderRange) && !forceRender && !annotationsChanged && !themeChanged && (fileDiff != null && fileDiff === this.fileDiff || fileDiff == null && !filesDidChange)) return this.applyCachedThemeState(themeType);
    let nextParsedFileDiff;
    if (fileDiff == null && hasFileInput && (filesDidChange || this.fileDiff == null)) nextParsedFileDiff = parseDiffFromFile(fileInput.oldFile, fileInput.newFile, this.options.parseDiffOptions);
    const { renderRange: previousRenderRange } = this;
    this.renderRange = nextRenderRange;
    if (hasFileInput) {
      this.deletionFile = oldFile;
      this.additionFile = newFile;
    } else if (fileDiff != null) {
      this.deletionFile = void 0;
      this.additionFile = void 0;
    }
    if (fileDiff != null) this.fileDiff = fileDiff;
    else if (nextParsedFileDiff != null) {
      diffDidChange = true;
      this.fileDiff = nextParsedFileDiff;
    }
    if (diffDidChange) this.cachedHeaderHTML = void 0;
    if (lineAnnotations != null) this.setLineAnnotations(lineAnnotations);
    if (this.fileDiff == null) return false;
    if (this.fileDiff.editSessionDirty === true && this.shouldSelfHealEditSession()) {
      finishEditSessionForDiff(this.fileDiff, this.options.parseDiffOptions);
      this.hunksRenderer.refreshHighlightedResult();
    }
    if (expandUnchanged) this.loadFilesIfNecessary();
    this.hunksRenderer.setOptions(this.getHunksRendererOptions(this.options));
    this.syncInteractionOptions();
    this.hunksRenderer.setLineAnnotations(this.lineAnnotations);
    const { disableErrorHandling = false, disableFileHeader = false } = this.options;
    if (disableFileHeader) {
      if (this.headerElement != null) {
        this.headerElement.remove();
        this.headerElement = void 0;
        this.lastRenderedHeaderHTML = void 0;
      }
      this.clearHeaderSlots();
    }
    fileContainer = this.getOrCreateFileContainer(fileContainer, containerWrapper);
    this.applyCachedThemeState(themeType);
    if (collapsed) {
      this.removeRenderedCode();
      this.clearAuxiliaryNodes();
      try {
        const hunksResult = this.hunksRenderer.renderDiff(this.fileDiff, EMPTY_RENDER_RANGE);
        if (hunksResult != null) this.applyThemeState(fileContainer, hunksResult.themeStyles, themeType, hunksResult.baseThemeType);
        if ((hunksResult == null ? void 0 : hunksResult.headerElement) != null) this.applyHeaderToDOM(hunksResult.headerElement, fileContainer);
        this.renderSeparators([]);
        this.injectUnsafeCSS();
      } catch (error) {
        if (disableErrorHandling) throw error;
        console.error(error);
        if (error instanceof Error) this.applyErrorToDOM(error, fileContainer);
      }
      if (!preventEmit) this.emitPostRender();
      return true;
    }
    try {
      const pre = this.getOrCreatePreNode(fileContainer);
      if (!(this.canPartiallyRender(forceRender, annotationsChanged, filesDidChange || diffDidChange || themeChanged) && this.applyPartialRender({
        previousRenderRange,
        renderRange: nextRenderRange
      }))) {
        const hunksResult = this.hunksRenderer.renderDiff(this.fileDiff, nextRenderRange);
        if (hunksResult == null) {
          if (((_b = this.workerManager) == null ? void 0 : _b.isInitialized()) === false) this.workerManager.initialize().then(() => this.rerender());
          return false;
        }
        this.applyThemeState(fileContainer, hunksResult.themeStyles, themeType, hunksResult.baseThemeType);
        if (hunksResult.headerElement != null) this.applyHeaderToDOM(hunksResult.headerElement, fileContainer);
        if (hunksResult.additionsContentAST != null || hunksResult.deletionsContentAST != null || hunksResult.unifiedContentAST != null) this.applyHunksToDOM(pre, hunksResult);
        else if (this.pre != null) {
          this.pre.remove();
          this.pre = void 0;
        }
        this.renderSeparators(hunksResult.hunkData);
      }
      this.applyBuffers(pre, nextRenderRange);
      this.injectUnsafeCSS();
      this.renderAnnotations();
      this.renderGutterUtility();
      this.managersDirty = true;
      if (!deferManagers) this.flushManagers();
      if (this.editor != null) this.syncRenderViewToEditor();
    } catch (error) {
      if (disableErrorHandling) throw error;
      console.error(error);
      if (error instanceof Error) this.applyErrorToDOM(error, fileContainer);
    }
    if (!preventEmit) this.emitPostRender();
    return true;
  }
  emitPostRender(unmount = false) {
    var _a9, _b;
    const { fileContainer, options: { onPostRender } } = this;
    if (unmount) {
      if (!this.mounted) return;
      this.mounted = false;
      if (fileContainer == null) return;
      (_b = (_a9 = this.options).onPostRender) == null ? void 0 : _b.call(_a9, fileContainer, this, "unmount");
      return;
    }
    if (fileContainer == null) return;
    const phase = this.mounted ? "update" : "mount";
    this.mounted = true;
    onPostRender == null ? void 0 : onPostRender(fileContainer, this, phase);
  }
  get fileDiffCache() {
    return this.hunksRenderer.diffCache ?? this.fileDiff;
  }
  syncRenderViewToEditor() {
    const editor = this.editor;
    const fileContainer = this.fileContainer;
    const fileDiff = this.fileDiffCache;
    const lineAnnotations = this.lineAnnotations;
    const renderRange = this.computeEditorRenderRange(this.renderRange);
    if (editor != null && fileContainer != null && fileDiff != null && !fileDiff.isPartial) this.hunksRenderer.initializeHighlighter().then((highlighter2) => {
      if (!this.enabled || this.editor !== editor || this.fileContainer !== fileContainer || this.fileDiffCache !== fileDiff) return;
      editor.__syncRenderView(highlighter2, fileContainer, fileDiff, lineAnnotations, renderRange);
    });
  }
  computeEditorRenderRange(renderRange) {
    const fileDiff = this.fileDiffCache;
    if (renderRange == null || fileDiff == null || isDefaultRenderRange(renderRange)) return renderRange;
    const { diffStyle = "split", expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    let firstLineNumber;
    let lastLineNumber;
    iterateOverDiff({
      diff: fileDiff,
      diffStyle,
      startingLine: renderRange.startingLine,
      totalLines: renderRange.totalLines,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold,
      callback: ({ additionLine }) => {
        if (additionLine != null) {
          firstLineNumber ?? (firstLineNumber = additionLine.lineNumber);
          lastLineNumber = additionLine.lineNumber;
        }
      }
    });
    if (firstLineNumber == null || lastLineNumber == null) return {
      ...renderRange,
      startingLine: 0,
      totalLines: 0
    };
    return {
      ...renderRange,
      startingLine: firstLineNumber - 1,
      totalLines: lastLineNumber - firstLineNumber + 1
    };
  }
  attachEditor(editor) {
    var _a9, _b;
    if (this.type !== "file-diff") throw new Error(`FileDiff.attachEditor: cannot attach an editor to a "${this.type}" diff`);
    (_a9 = this.editor) == null ? void 0 : _a9.cleanUp();
    this.editor = editor;
    this.hunksRenderer.beginEditSession();
    if (((_b = this.fileDiff) == null ? void 0 : _b.isPartial) === true) this.loadFilesIfNecessary();
    if (this.hunksRenderer.editorRenderReady()) this.syncRenderViewToEditor();
    else this.rerender();
    return (recycle) => {
      this.editor = void 0;
      if (recycle !== true) this.finishEditSession();
    };
  }
  finishEditSession() {
    this.hunksRenderer.endEditSession();
    this.completeEditSession();
  }
  /**
  * Run the genuine session-end recompute: restore recompute-shaped hunks (a
  * context-only region collapses away, boundaries re-derive), preserve
  * expansion state best-effort via old-side anchors, and repaint through
  * the session render path — which also invalidates virtualized layout,
  * since nothing else does at exit now that editing does not flip
  * expandUnchanged. Marker-guarded and idempotent; CodeView also calls this
  * when reaping a session whose detach closure was consumed by a recycle.
  * Safe on a cleaned-up instance: the recompute is pure metadata work and
  * the deferred rerender is enabled-guarded. Returns true when a recompute
  * ran.
  */
  completeEditSession() {
    const fileDiff = this.fileDiffCache;
    if (fileDiff == null || fileDiff.editSessionDirty !== true) return false;
    const { collapsedContextThreshold = 1 } = this.options;
    const anchors = captureExpansionAnchors(fileDiff, this.hunksRenderer.getExpandedHunksMap(), collapsedContextThreshold);
    finishEditSessionForDiff(fileDiff, this.options.parseDiffOptions);
    this.hunksRenderer.setExpandedHunksMap(rebuildExpansionFromAnchors(fileDiff, anchors));
    this.hunksRenderer.refreshHighlightedResult();
    this.escalateEditSessionRender();
    return true;
  }
  applyDocumentChange(textDocument, newLineAnnotations) {
    var _a9;
    this.hunksRenderer.applyDocumentChange(textDocument);
    const fileDiff = this.hunksRenderer.diffCache;
    if (fileDiff != null) {
      const cacheKey = (_a9 = this.fileDiff) == null ? void 0 : _a9.cacheKey;
      if (cacheKey != null && fileDiff.cacheKey == null) fileDiff.cacheKey = cacheKey;
      this.fileDiff = fileDiff;
    }
    if (newLineAnnotations !== void 0 && newLineAnnotations !== this.lineAnnotations) {
      this.setLineAnnotations(newLineAnnotations);
      this.hunksRenderer.setLineAnnotations(this.lineAnnotations);
      this.renderAnnotations();
    }
    this.rerender();
    this.interactionManager.setSelectionDirty();
  }
  updateRenderCache(dirtyLines, themeType, options = {}) {
    const { shouldRefreshDiffsView, lineCountChangeInFlight } = options;
    if (this.hunksRenderer.updateRenderCache(dirtyLines, themeType, lineCountChangeInFlight)) {
      if (this.refreshViewTimeout != null) {
        clearTimeout(this.refreshViewTimeout);
        this.refreshViewTimeout = void 0;
      }
      this.lineStateRefreshPending = true;
      this.escalateEditSessionRender();
      return;
    }
    if (shouldRefreshDiffsView === true) {
      if (this.refreshViewTimeout != null) clearTimeout(this.refreshViewTimeout);
      this.lineStateRefreshPending = true;
      this.refreshViewTimeout = setTimeout(() => {
        this.refreshViewTimeout = void 0;
        if (this.options.diffStyle === "split") this.refreshSplitDiffView();
        else this.refreshUnifiedDiffView();
        this.flushDeferredLineState();
      }, 150);
    }
  }
  isLineRenderable(lineNumber) {
    const fileDiff = this.fileDiffCache;
    if (fileDiff == null) return true;
    const { expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    return isAdditionLineRenderable({
      fileDiff,
      lineNumber,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold
    });
  }
  getNearestRenderableLine(lineNumber, direction) {
    const fileDiff = this.fileDiffCache;
    if (fileDiff == null) return lineNumber;
    const { expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    return getNearestRenderableAdditionLine({
      fileDiff,
      lineNumber,
      direction,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold
    });
  }
  revealLine(lineNumber) {
    const fileDiff = this.fileDiffCache;
    const { expandUnchanged = false, collapsedContextThreshold = 1, expansionLineCount = 100 } = this.options;
    if (fileDiff == null || fileDiff.isPartial || expandUnchanged) return false;
    const expandedHunks = this.hunksRenderer.getExpandedHunksMap();
    for (const [hunkIndex, hunk] of fileDiff.hunks.entries()) {
      const [hunkStart, hunkEnd] = getHunkAdditionLineRange(hunk);
      if (lineNumber < hunkStart) {
        const region = getExpandedRegion({
          isPartial: fileDiff.isPartial,
          rangeSize: hunk.collapsedBefore,
          expandedHunks,
          hunkIndex,
          collapsedContextThreshold
        });
        const gapStart = hunkStart - region.rangeSize;
        if (region.renderAll || lineNumber < gapStart + region.fromStart || lineNumber >= hunkStart - region.fromEnd) return false;
        const fromStartDistance = lineNumber - (gapStart + region.fromStart) + 1;
        const fromEndDistance = hunkStart - region.fromEnd - lineNumber;
        if (fromStartDistance <= fromEndDistance) this.expandHunk(hunkIndex, "up", fromStartDistance + expansionLineCount);
        else this.expandHunk(hunkIndex, "down", fromEndDistance + expansionLineCount);
        return true;
      }
      if (lineNumber < hunkEnd) return false;
    }
    const trailingRegion = getTrailingExpandedRegion({
      fileDiff,
      hunkIndex: fileDiff.hunks.length - 1,
      expandedHunks,
      collapsedContextThreshold,
      errorPrefix: "FileDiff.revealLine"
    });
    if (trailingRegion == null || trailingRegion.renderAll) return false;
    const lastHunk = fileDiff.hunks[fileDiff.hunks.length - 1];
    const [, trailingStart] = getHunkAdditionLineRange(lastHunk);
    if (lineNumber < trailingStart + trailingRegion.fromStart || lineNumber >= trailingStart + trailingRegion.rangeSize) return false;
    this.expandHunk(fileDiff.hunks.length, "up", lineNumber - (trailingStart + trailingRegion.fromStart) + 1 + expansionLineCount);
    return true;
  }
  shouldSelfHealEditSession() {
    return this.editor == null;
  }
  escalateEditSessionRender() {
    queueRender(this.handleEditSessionRender);
  }
  removeRenderedCode() {
    var _a9, _b, _c, _d, _e, _f;
    this.resizeManager.cleanUp();
    this.scrollSyncManager.cleanUp();
    this.interactionManager.cleanUp();
    (_a9 = this.bufferBefore) == null ? void 0 : _a9.remove();
    this.bufferBefore = void 0;
    (_b = this.bufferAfter) == null ? void 0 : _b.remove();
    this.bufferAfter = void 0;
    (_c = this.codeUnified) == null ? void 0 : _c.remove();
    this.codeUnified = void 0;
    (_d = this.codeDeletions) == null ? void 0 : _d.remove();
    this.codeDeletions = void 0;
    (_e = this.codeAdditions) == null ? void 0 : _e.remove();
    this.codeAdditions = void 0;
    (_f = this.pre) == null ? void 0 : _f.remove();
    this.pre = void 0;
    this.appliedPreAttributes = void 0;
    this.lastRowCount = void 0;
  }
  clearAuxiliaryNodes() {
    var _a9;
    for (const { element: element2 } of this.separatorCache.values()) element2.remove();
    this.separatorCache.clear();
    for (const { element: element2 } of this.annotationCache.values()) element2.remove();
    this.annotationCache.clear();
    (_a9 = this.gutterUtilityContent) == null ? void 0 : _a9.remove();
    this.gutterUtilityContent = void 0;
  }
  renderPlaceholder(height) {
    if (this.fileContainer == null) return false;
    this.emitPostRender(true);
    this.cleanChildNodes();
    if (this.placeHolder == null) {
      const shadowRoot = this.fileContainer.shadowRoot ?? this.fileContainer.attachShadow({ mode: "open" });
      this.placeHolder = document.createElement("div");
      this.placeHolder.dataset.placeholder = "";
      shadowRoot.appendChild(this.placeHolder);
    }
    this.placeHolder.style.setProperty("height", `${height}px`);
    return true;
  }
  async primeHighlightCache(fileDiff = this.fileDiff) {
    const { workerManager } = this;
    if (fileDiff == null || workerManager == null || !workerManager.isWorkingPool() || fileDiff.cacheKey == null || isDiffPlainText(fileDiff)) return;
    const tokenizeMaxLength = this.options.tokenizeMaxLength ?? 1e5;
    if (Math.max(fileDiff.additionLines.length, fileDiff.deletionLines.length) > tokenizeMaxLength) return;
    await workerManager.primeDiffHighlightCache(fileDiff).catch((error) => {
      console.error(error);
    });
  }
  cleanChildNodes() {
    var _a9, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o;
    this.resizeManager.cleanUp();
    this.scrollSyncManager.cleanUp();
    this.interactionManager.cleanUp();
    this.clearAuxiliaryNodes();
    (_a9 = this.bufferAfter) == null ? void 0 : _a9.remove();
    (_b = this.bufferBefore) == null ? void 0 : _b.remove();
    (_c = this.codeAdditions) == null ? void 0 : _c.remove();
    (_d = this.codeDeletions) == null ? void 0 : _d.remove();
    (_e = this.codeUnified) == null ? void 0 : _e.remove();
    (_f = this.errorWrapper) == null ? void 0 : _f.remove();
    (_g = this.headerElement) == null ? void 0 : _g.remove();
    (_h = this.headerPrefix) == null ? void 0 : _h.remove();
    (_i = this.headerFilenameSuffix) == null ? void 0 : _i.remove();
    (_j = this.headerMetadata) == null ? void 0 : _j.remove();
    (_k = this.headerCustom) == null ? void 0 : _k.remove();
    (_l = this.pre) == null ? void 0 : _l.remove();
    (_m = this.spriteSVG) == null ? void 0 : _m.remove();
    (_n = this.themeCSSStyle) == null ? void 0 : _n.remove();
    (_o = this.unsafeCSSStyle) == null ? void 0 : _o.remove();
    this.bufferAfter = void 0;
    this.bufferBefore = void 0;
    this.codeAdditions = void 0;
    this.codeDeletions = void 0;
    this.codeUnified = void 0;
    this.errorWrapper = void 0;
    this.headerElement = void 0;
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
    this.pre = void 0;
    this.spriteSVG = void 0;
    this.themeCSSStyle = void 0;
    this.appliedThemeCSS = void 0;
    this.hasAdoptedThemeCSS = false;
    this.unsafeCSSStyle = void 0;
    this.appliedUnsafeCSS = void 0;
    this.lastRenderedHeaderHTML = void 0;
    this.lastRowCount = void 0;
    this.mounted = false;
  }
  renderSeparators(hunkData) {
    const { hunkSeparators } = this.options;
    if (this.isContainerManaged || this.fileContainer == null || typeof hunkSeparators !== "function") {
      for (const { element: element2 } of this.separatorCache.values()) element2.remove();
      this.separatorCache.clear();
      return;
    }
    const staleSeparators = new Map(this.separatorCache);
    for (const hunk of hunkData) {
      const id = hunk.slotName;
      let cache2 = this.separatorCache.get(id);
      if (cache2 == null || !areHunkDataEqual(hunk, cache2.hunkData)) {
        cache2 == null ? void 0 : cache2.element.remove();
        const element2 = document.createElement("div");
        element2.style.display = "contents";
        element2.slot = hunk.slotName;
        const child = hunkSeparators(hunk, this);
        if (child != null) element2.appendChild(child);
        this.fileContainer.appendChild(element2);
        cache2 = {
          element: element2,
          hunkData: hunk
        };
        this.separatorCache.set(id, cache2);
      }
      staleSeparators.delete(id);
    }
    for (const [id, { element: element2 }] of staleSeparators.entries()) {
      this.separatorCache.delete(id);
      element2.remove();
    }
  }
  renderAnnotations() {
    if (this.isContainerManaged || this.fileContainer == null) {
      for (const { element: element2 } of this.annotationCache.values()) element2.remove();
      this.annotationCache.clear();
      return;
    }
    const staleAnnotations = new Map(this.annotationCache);
    const { renderAnnotation } = this.options;
    if (renderAnnotation != null && this.lineAnnotations.length > 0) for (const [index, annotation] of this.lineAnnotations.entries()) {
      const id = `${index}-${getLineAnnotationName(annotation)}`;
      let cache2 = this.annotationCache.get(id);
      if (cache2 == null || !areDiffLineAnnotationsEqual(annotation, cache2.annotation)) {
        cache2 == null ? void 0 : cache2.element.remove();
        const content = renderAnnotation(annotation);
        if (content == null) continue;
        cache2 = {
          element: createAnnotationWrapperNode(getLineAnnotationName(annotation)),
          annotation
        };
        cache2.element.appendChild(content);
        this.fileContainer.appendChild(cache2.element);
        this.annotationCache.set(id, cache2);
      }
      staleAnnotations.delete(id);
    }
    for (const [id, { element: element2 }] of staleAnnotations.entries()) {
      this.annotationCache.delete(id);
      element2.remove();
    }
  }
  renderGutterUtility() {
    var _a9, _b;
    const { renderGutterUtility } = this.options;
    if (this.fileContainer == null || renderGutterUtility == null) {
      (_a9 = this.gutterUtilityContent) == null ? void 0 : _a9.remove();
      this.gutterUtilityContent = void 0;
      return;
    }
    const element2 = renderGutterUtility(this.interactionManager.getHoveredLine);
    if (element2 != null && this.gutterUtilityContent != null) return;
    else if (element2 == null) {
      (_b = this.gutterUtilityContent) == null ? void 0 : _b.remove();
      this.gutterUtilityContent = void 0;
      return;
    }
    const gutterUtilityContent = createGutterUtilityContentNode();
    gutterUtilityContent.appendChild(element2);
    this.fileContainer.appendChild(gutterUtilityContent);
    this.gutterUtilityContent = gutterUtilityContent;
  }
  getOrCreateFileContainer(fileContainer, parentNode) {
    var _a9;
    const { fileContainer: previousContainer } = this;
    const nextContainer = fileContainer ?? previousContainer ?? document.createElement("diffs-container");
    const containerChanged = previousContainer !== nextContainer;
    if (previousContainer != null && containerChanged) (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
    if (containerChanged) this.emitPostRender(true);
    this.fileContainer = nextContainer;
    if (previousContainer != null && containerChanged) {
      this.lastRenderedHeaderHTML = void 0;
      this.headerElement = void 0;
    }
    if (parentNode != null && this.fileContainer.parentNode !== parentNode) parentNode.appendChild(this.fileContainer);
    if (containerChanged) this.adoptReusableShellElements(this.fileContainer);
    this.ensureSpriteSVG(this.fileContainer);
    return this.fileContainer;
  }
  adoptReusableShellElements(fileContainer) {
    const { shadowRoot } = fileContainer;
    if (shadowRoot == null) return;
    for (const element2 of shadowRoot.children) if (element2 instanceof SVGElement) this.spriteSVG ?? (this.spriteSVG = element2);
    else if (isStyleNode(element2) && element2.hasAttribute("data-theme-css")) {
      this.themeCSSStyle ?? (this.themeCSSStyle = element2);
      this.hasAdoptedThemeCSS = true;
    } else if (isStyleNode(element2) && element2.hasAttribute("data-unsafe-css")) {
      this.unsafeCSSStyle ?? (this.unsafeCSSStyle = element2);
      this.appliedUnsafeCSS ?? (this.appliedUnsafeCSS = this.options.unsafeCSS ?? void 0);
    }
  }
  ensureSpriteSVG(fileContainer) {
    const shadowRoot = fileContainer.shadowRoot ?? fileContainer.attachShadow({ mode: "open" });
    if (this.spriteSVG == null) {
      const fragment = document.createElement("div");
      fragment.innerHTML = SVGSpriteSheet;
      const firstChild = fragment.firstChild;
      if (firstChild instanceof SVGElement) this.spriteSVG = firstChild;
    }
    if (this.spriteSVG != null && this.spriteSVG.parentNode !== shadowRoot) shadowRoot.appendChild(this.spriteSVG);
  }
  getOrCreatePreNode(container) {
    var _a9, _b;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    if (this.pre == null) {
      this.pre = document.createElement("pre");
      this.appliedPreAttributes = void 0;
      this.codeUnified = void 0;
      this.codeDeletions = void 0;
      this.codeAdditions = void 0;
      shadowRoot.appendChild(this.pre);
    } else if (this.pre.parentNode !== shadowRoot) {
      (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
      shadowRoot.appendChild(this.pre);
      this.appliedPreAttributes = void 0;
    }
    (_b = this.placeHolder) == null ? void 0 : _b.remove();
    this.placeHolder = void 0;
    return this.pre;
  }
  syncCodeNodesFromPre(pre) {
    this.codeUnified = void 0;
    this.codeDeletions = void 0;
    this.codeAdditions = void 0;
    for (const child of Array.from(pre.children)) {
      if (!(child instanceof HTMLElement)) continue;
      if (child.hasAttribute("data-unified")) this.codeUnified = child;
      else if (child.hasAttribute("data-deletions")) this.codeDeletions = child;
      else if (child.hasAttribute("data-additions")) this.codeAdditions = child;
    }
  }
  applyHeaderToDOM(headerAST, container) {
    var _a9, _b, _c, _d, _e, _f, _g;
    this.cleanupErrorWrapper();
    (_a9 = this.placeHolder) == null ? void 0 : _a9.remove();
    this.placeHolder = void 0;
    const { fileDiff } = this;
    const headerHTML = this.cachedHeaderHTML ?? toHtml(headerAST);
    this.cachedHeaderHTML = headerHTML;
    if (headerHTML !== this.lastRenderedHeaderHTML) {
      const tempDiv = document.createElement("div");
      tempDiv.innerHTML = headerHTML;
      const newHeader = tempDiv.firstElementChild;
      if (!(newHeader instanceof HTMLElement)) return;
      if (this.headerElement != null) (_b = container.shadowRoot) == null ? void 0 : _b.replaceChild(newHeader, this.headerElement);
      else (_c = container.shadowRoot) == null ? void 0 : _c.prepend(newHeader);
      this.headerElement = newHeader;
      this.lastRenderedHeaderHTML = headerHTML;
    }
    if (this.isContainerManaged || fileDiff == null) return;
    const { renderCustomHeader, renderHeaderPrefix, renderHeaderFilenameSuffix, renderHeaderMetadata } = this.options;
    if (renderCustomHeader != null) {
      const content2 = renderCustomHeader(fileDiff) ?? void 0;
      this.headerCustom = this.upsertHeaderSlotElement(container, this.headerCustom, CUSTOM_HEADER_SLOT_ID, content2);
      (_d = this.headerPrefix) == null ? void 0 : _d.remove();
      (_e = this.headerFilenameSuffix) == null ? void 0 : _e.remove();
      (_f = this.headerMetadata) == null ? void 0 : _f.remove();
      this.headerPrefix = void 0;
      this.headerFilenameSuffix = void 0;
      this.headerMetadata = void 0;
      return;
    }
    const prefix = (renderHeaderPrefix == null ? void 0 : renderHeaderPrefix(fileDiff)) ?? void 0;
    const suffix = (renderHeaderFilenameSuffix == null ? void 0 : renderHeaderFilenameSuffix(fileDiff)) ?? void 0;
    const content = (renderHeaderMetadata == null ? void 0 : renderHeaderMetadata(fileDiff)) ?? void 0;
    this.headerPrefix = this.upsertHeaderSlotElement(container, this.headerPrefix, HEADER_PREFIX_SLOT_ID, prefix);
    this.headerFilenameSuffix = this.upsertHeaderSlotElement(container, this.headerFilenameSuffix, HEADER_FILENAME_SUFFIX_SLOT_ID, suffix);
    this.headerMetadata = this.upsertHeaderSlotElement(container, this.headerMetadata, HEADER_METADATA_SLOT_ID, content);
    (_g = this.headerCustom) == null ? void 0 : _g.remove();
    this.headerCustom = void 0;
  }
  clearHeaderSlots() {
    var _a9, _b, _c, _d;
    (_a9 = this.headerPrefix) == null ? void 0 : _a9.remove();
    (_b = this.headerFilenameSuffix) == null ? void 0 : _b.remove();
    (_c = this.headerMetadata) == null ? void 0 : _c.remove();
    (_d = this.headerCustom) == null ? void 0 : _d.remove();
    this.headerPrefix = void 0;
    this.headerFilenameSuffix = void 0;
    this.headerMetadata = void 0;
    this.headerCustom = void 0;
  }
  upsertHeaderSlotElement(container, current, slot, content) {
    if (content == null) {
      current == null ? void 0 : current.remove();
      return;
    }
    const element2 = current ?? this.createHeaderSlotElement(slot);
    if (current == null) container.appendChild(element2);
    this.replaceHeaderSlotContent(element2, content);
    return element2;
  }
  replaceHeaderSlotContent(element2, content) {
    element2.replaceChildren();
    if (content instanceof Element) element2.appendChild(content);
    else element2.innerText = `${content}`;
  }
  createHeaderSlotElement(slot) {
    const element2 = document.createElement("div");
    element2.slot = slot;
    return element2;
  }
  injectUnsafeCSS() {
    var _a9, _b;
    const { unsafeCSS } = this.options;
    const shadowRoot = (_a9 = this.fileContainer) == null ? void 0 : _a9.shadowRoot;
    if (shadowRoot == null) return;
    if (unsafeCSS == null || unsafeCSS === "") {
      if (this.unsafeCSSStyle != null) {
        this.unsafeCSSStyle.remove();
        this.unsafeCSSStyle = void 0;
      }
      this.appliedUnsafeCSS = void 0;
      return;
    }
    if (((_b = this.unsafeCSSStyle) == null ? void 0 : _b.parentNode) === shadowRoot && this.appliedUnsafeCSS === unsafeCSS) return;
    this.unsafeCSSStyle ?? (this.unsafeCSSStyle = createUnsafeCSSStyleNode());
    if (this.unsafeCSSStyle.parentNode !== shadowRoot) shadowRoot.appendChild(this.unsafeCSSStyle);
    this.unsafeCSSStyle.textContent = wrapUnsafeCSS(unsafeCSS);
    this.appliedUnsafeCSS = unsafeCSS;
  }
  applyThemeState(container, themeStyles, themeType, baseThemeType) {
    var _a9, _b, _c;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    const effectiveThemeType = baseThemeType ?? themeType;
    const currentTheme = this.options.theme ?? DEFAULT_THEMES;
    const theme = typeof currentTheme === "string" ? currentTheme : { ...currentTheme };
    const scrollbarGutter = getMeasuredScrollbarGutter(shadowRoot);
    if (((_a9 = this.themeCSSStyle) == null ? void 0 : _a9.parentNode) === shadowRoot && ((_b = this.appliedThemeCSS) == null ? void 0 : _b.themeStyles) === themeStyles && this.appliedThemeCSS.themeType === effectiveThemeType && this.appliedThemeCSS.scrollbarGutter === scrollbarGutter) {
      this.appliedThemeCSS.theme = theme;
      return;
    }
    if (this.hasAdoptedThemeCSS && ((_c = this.themeCSSStyle) == null ? void 0 : _c.parentNode) === shadowRoot) {
      this.hasAdoptedThemeCSS = false;
      this.appliedThemeCSS = {
        theme,
        themeStyles,
        themeType: effectiveThemeType,
        baseThemeType,
        scrollbarGutter
      };
      return;
    }
    this.themeCSSStyle = upsertHostThemeStyle({
      shadowRoot,
      currentNode: this.themeCSSStyle,
      themeCSS: wrapThemeCSS(themeStyles, effectiveThemeType, scrollbarGutter)
    });
    this.appliedThemeCSS = this.themeCSSStyle != null ? {
      theme,
      themeStyles,
      themeType: effectiveThemeType,
      baseThemeType,
      scrollbarGutter
    } : void 0;
  }
  hydrateMeasuredScrollbar() {
    var _a9;
    const shadowRoot = (_a9 = this.fileContainer) == null ? void 0 : _a9.shadowRoot;
    if (shadowRoot == null || this.themeCSSStyle == null) return;
    this.themeCSSStyle.textContent = patchScrollbarGutterSize(this.themeCSSStyle.textContent ?? "", getMeasuredScrollbarGutter(shadowRoot));
  }
  shouldGuardRebuildScroll() {
    return this.editor != null && isSafari();
  }
  applyHunksToDOM(pre, result) {
    if (this.shouldGuardRebuildScroll()) guardWebKitScrollDuringRebuild(pre, () => this.replaceCodeColumns(pre, result));
    else this.replaceCodeColumns(pre, result);
  }
  applyCodeColumnsInPlace(code, ast, rowCount) {
    const columns = this.getColumnPair(code);
    if (columns == null) return false;
    const gutterChildren = getElementChildren(ast[0]);
    const contentChildren = getElementChildren(ast[1]);
    if (gutterChildren == null || contentChildren == null) return false;
    columns.gutter.innerHTML = toHtml(gutterChildren);
    columns.content.innerHTML = toHtml(contentChildren);
    if (rowCount !== this.lastRowCount) {
      columns.gutter.style.setProperty("grid-row", `span ${rowCount}`);
      columns.content.style.setProperty("grid-row", `span ${rowCount}`);
    }
    return true;
  }
  replaceCodeColumns(pre, result) {
    var _a9, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    const { overflow = "scroll" } = this.options;
    const containerSize = (this.options.hunkSeparators ?? "line-info") === "line-info";
    const rowSpan = overflow === "wrap" ? result.rowCount : void 0;
    this.cleanupErrorWrapper();
    this.applyPreNodeAttributes(pre, result);
    let shouldReplace = false;
    const codeElements = [];
    const unifiedAST = this.hunksRenderer.renderCodeAST("unified", result);
    const deletionsAST = this.hunksRenderer.renderCodeAST("deletions", result);
    const additionsAST = this.hunksRenderer.renderCodeAST("additions", result);
    (_a9 = this.editor) == null ? void 0 : _a9.__captureFocusForDOMReplacement();
    if (unifiedAST != null) {
      shouldReplace = this.codeUnified == null || this.codeAdditions != null || this.codeDeletions != null;
      (_b = this.codeDeletions) == null ? void 0 : _b.remove();
      this.codeDeletions = void 0;
      (_c = this.codeAdditions) == null ? void 0 : _c.remove();
      this.codeAdditions = void 0;
      this.codeUnified = getOrCreateCodeNode({
        code: this.codeUnified,
        columnType: "unified",
        rowSpan,
        containerSize
      });
      if (!this.applyCodeColumnsInPlace(this.codeUnified, unifiedAST, result.rowCount)) this.codeUnified.innerHTML = this.hunksRenderer.renderPartialHTML(unifiedAST);
      codeElements.push(this.codeUnified);
    } else if (deletionsAST != null || additionsAST != null) {
      if (deletionsAST != null) {
        shouldReplace = this.codeDeletions == null || this.codeUnified != null;
        (_d = this.codeUnified) == null ? void 0 : _d.remove();
        this.codeUnified = void 0;
        this.codeDeletions = getOrCreateCodeNode({
          code: this.codeDeletions,
          columnType: "deletions",
          rowSpan,
          containerSize
        });
        if (!this.applyCodeColumnsInPlace(this.codeDeletions, deletionsAST, result.rowCount)) this.codeDeletions.innerHTML = this.hunksRenderer.renderPartialHTML(deletionsAST);
        codeElements.push(this.codeDeletions);
      } else {
        (_e = this.codeDeletions) == null ? void 0 : _e.remove();
        this.codeDeletions = void 0;
      }
      if (additionsAST != null) {
        shouldReplace = shouldReplace || this.codeAdditions == null || this.codeUnified != null;
        (_f = this.codeUnified) == null ? void 0 : _f.remove();
        this.codeUnified = void 0;
        this.codeAdditions = getOrCreateCodeNode({
          code: this.codeAdditions,
          columnType: "additions",
          rowSpan,
          containerSize
        });
        if (!this.applyCodeColumnsInPlace(this.codeAdditions, additionsAST, result.rowCount)) this.codeAdditions.innerHTML = this.hunksRenderer.renderPartialHTML(additionsAST);
        codeElements.push(this.codeAdditions);
      } else {
        (_g = this.codeAdditions) == null ? void 0 : _g.remove();
        this.codeAdditions = void 0;
      }
    } else {
      (_h = this.codeUnified) == null ? void 0 : _h.remove();
      this.codeUnified = void 0;
      (_i = this.codeDeletions) == null ? void 0 : _i.remove();
      this.codeDeletions = void 0;
      (_j = this.codeAdditions) == null ? void 0 : _j.remove();
      this.codeAdditions = void 0;
    }
    if (codeElements.length === 0) pre.textContent = "";
    else if (shouldReplace) pre.replaceChildren(...codeElements);
    this.lastRowCount = result.rowCount;
  }
  applyPartialRender({ previousRenderRange, renderRange }) {
    const { pre, codeUnified, codeAdditions, codeDeletions, options: { diffStyle = "split" } } = this;
    if (pre == null || previousRenderRange == null || renderRange == null || !Number.isFinite(previousRenderRange.totalLines) || !Number.isFinite(renderRange.totalLines) || this.lastRowCount == null) return false;
    const codeElements = this.getCodeColumns(diffStyle, codeUnified, codeDeletions, codeAdditions);
    if (codeElements == null) return false;
    const previousStart = previousRenderRange.startingLine;
    const nextStart = renderRange.startingLine;
    const previousEnd = previousStart + previousRenderRange.totalLines;
    const nextEnd = nextStart + renderRange.totalLines;
    const overlapStart = Math.max(previousStart, nextStart);
    const overlapEnd = Math.min(previousEnd, nextEnd);
    if (overlapEnd <= overlapStart) return false;
    const trimStart = Math.max(0, overlapStart - previousStart);
    const trimEnd = Math.max(0, previousEnd - overlapEnd);
    const trimResult = this.trimColumns({
      columns: codeElements,
      trimStart,
      trimEnd,
      previousStart,
      overlapStart,
      overlapEnd,
      diffStyle
    });
    if (trimResult < 0) throw new Error("FileDiff.applyPartialRender: failed to trim to overlap");
    if (this.lastRowCount < trimResult) throw new Error("FileDiff.applyPartialRender: trimmed beyond DOM row count");
    let rowCount = this.lastRowCount - trimResult;
    const renderChunk = (startingLine, totalLines) => {
      if (totalLines <= 0 || this.fileDiff == null) return;
      return this.hunksRenderer.renderDiff(this.fileDiff, {
        startingLine,
        totalLines,
        bufferBefore: 0,
        bufferAfter: 0
      });
    };
    const prependResult = renderChunk(nextStart, Math.max(overlapStart - nextStart, 0));
    if (prependResult == null && nextStart < overlapStart) return false;
    const appendResult = renderChunk(overlapEnd, Math.max(nextEnd - overlapEnd, 0));
    if (appendResult == null && nextEnd > overlapEnd) return false;
    const applyChunk = (result, insertPosition) => {
      if (result == null) return;
      if (diffStyle === "unified" && !Array.isArray(codeElements)) this.insertPartialHTML(diffStyle, codeElements, result, insertPosition);
      else if (diffStyle === "split" && Array.isArray(codeElements)) this.insertPartialHTML(diffStyle, codeElements, result, insertPosition);
      else throw new Error("FileDiff.applyPartialRender.applyChunk: invalid chunk application");
      rowCount += result.rowCount;
    };
    this.cleanupErrorWrapper();
    applyChunk(prependResult, "afterbegin");
    applyChunk(appendResult, "beforeend");
    if (this.lastRowCount !== rowCount) {
      this.applyRowSpan(diffStyle, codeElements, rowCount);
      this.lastRowCount = rowCount;
    }
    return true;
  }
  insertPartialHTML(diffStyle, columns, result, insertPosition) {
    if (diffStyle === "unified" && !Array.isArray(columns)) {
      const unifiedAST = this.hunksRenderer.renderCodeAST("unified", result);
      this.renderPartialColumn(columns, unifiedAST, insertPosition);
    } else if (diffStyle === "split" && Array.isArray(columns)) {
      const deletionsAST = this.hunksRenderer.renderCodeAST("deletions", result);
      const additionsAST = this.hunksRenderer.renderCodeAST("additions", result);
      this.renderPartialColumn(columns[0], deletionsAST, insertPosition);
      this.renderPartialColumn(columns[1], additionsAST, insertPosition);
    } else throw new Error("FileDiff.insertPartialHTML: Invalid argument composition");
  }
  refreshSplitDiffView() {
    if (this.options.diffStyle !== "split") return;
    const hunksResult = this.hunksRenderer.renderDiff(this.fileDiff, this.renderRange);
    if (hunksResult == null) return;
    const columns = this.getCodeColumns("split", this.codeUnified, this.codeDeletions, this.codeAdditions);
    if (!Array.isArray(columns)) return;
    const applyLineType = (type, column) => {
      if (column == null) return;
      const ast = this.hunksRenderer.renderCodeAST(type, hunksResult);
      const gutterChildren = getElementChildren(ast == null ? void 0 : ast[0]);
      const contentChildren = getElementChildren(ast == null ? void 0 : ast[1]);
      for (const [el, astChildren] of [[column.gutter, gutterChildren], [column.content, contentChildren]]) if (astChildren != null && el.childElementCount === astChildren.length) for (let i2 = 0; i2 < astChildren.length; i2++) {
        const gutterElement = el.children[i2];
        const lineType = astChildren[i2].properties["data-line-type"];
        if (lineType != null && gutterElement.dataset.lineType !== lineType) gutterElement.dataset.lineType = lineType;
      }
    };
    applyLineType("deletions", columns[0]);
    applyLineType("additions", columns[1]);
  }
  refreshUnifiedDiffView() {
    if (this.options.diffStyle !== "unified") return;
    const hunksResult = this.hunksRenderer.renderDiff(this.fileDiff, this.renderRange);
    if (hunksResult == null) return;
    const columns = this.getCodeColumns("unified", this.codeUnified, this.codeDeletions, this.codeAdditions);
    if (columns == null || Array.isArray(columns)) return;
    const ast = this.hunksRenderer.renderCodeAST("unified", hunksResult);
    const gutterChildren = getElementChildren(ast == null ? void 0 : ast[0]);
    const contentChildren = getElementChildren(ast == null ? void 0 : ast[1]);
    const applyColumns = () => {
      for (const [el, astChildren] of [[columns.gutter, gutterChildren], [columns.content, contentChildren]]) if (astChildren != null) el.innerHTML = toHtml(astChildren);
      if (hunksResult.rowCount !== this.lastRowCount) {
        this.applyRowSpan("unified", columns, hunksResult.rowCount);
        this.lastRowCount = hunksResult.rowCount;
      }
    };
    if (this.shouldGuardRebuildScroll()) guardWebKitScrollDuringRebuild(this.pre, applyColumns);
    else applyColumns();
    this.renderSeparators(hunksResult.hunkData);
    this.managersDirty = true;
    this.flushManagers();
    this.syncRenderViewToEditor();
  }
  renderPartialColumn(column, ast, insertPosition) {
    if (column == null || ast == null) return;
    const gutterChildren = getElementChildren(ast[0]);
    const contentChildren = getElementChildren(ast[1]);
    if (gutterChildren == null || contentChildren == null) throw new Error("FileDiff.insertPartialHTML: Unexpected AST structure");
    const firstHASTElement = contentChildren.at(0);
    if (insertPosition === "beforeend" && (firstHASTElement == null ? void 0 : firstHASTElement.type) === "element" && typeof firstHASTElement.properties["data-buffer-size"] === "number") this.mergeBuffersIfNecessary(firstHASTElement.properties["data-buffer-size"], column.content.children[column.content.children.length - 1], column.gutter.children[column.gutter.children.length - 1], gutterChildren, contentChildren, true);
    const lastHASTElement = contentChildren.at(-1);
    if (insertPosition === "afterbegin" && (lastHASTElement == null ? void 0 : lastHASTElement.type) === "element" && typeof lastHASTElement.properties["data-buffer-size"] === "number") this.mergeBuffersIfNecessary(lastHASTElement.properties["data-buffer-size"], column.content.children[0], column.gutter.children[0], gutterChildren, contentChildren, false);
    column.gutter.insertAdjacentHTML(insertPosition, this.hunksRenderer.renderPartialHTML(gutterChildren));
    column.content.insertAdjacentHTML(insertPosition, this.hunksRenderer.renderPartialHTML(contentChildren));
  }
  mergeBuffersIfNecessary(adjustmentSize, contentElement, gutterElement, gutterChildren, contentChildren, fromStart) {
    if (!(contentElement instanceof HTMLElement) || !(gutterElement instanceof HTMLElement)) return;
    const currentSize = this.getBufferSize(contentElement.dataset);
    if (currentSize == null) return;
    if (fromStart) {
      gutterChildren.shift();
      contentChildren.shift();
    } else {
      gutterChildren.pop();
      contentChildren.pop();
    }
    this.updateBufferSize(contentElement, currentSize + adjustmentSize);
    this.updateBufferSize(gutterElement, currentSize + adjustmentSize);
  }
  applyRowSpan(diffStyle, columns, rowCount) {
    const applySpan = (column) => {
      if (column == null) return;
      column.gutter.style.setProperty("grid-row", `span ${rowCount}`);
      column.content.style.setProperty("grid-row", `span ${rowCount}`);
    };
    if (diffStyle === "unified" && !Array.isArray(columns)) applySpan(columns);
    else if (diffStyle === "split" && Array.isArray(columns)) {
      applySpan(columns[0]);
      applySpan(columns[1]);
    } else throw new Error("dun fuuuuked up");
  }
  trimColumnRows(columns, preTrimCount, postTrimStart) {
    let visibleLineIndex = 0;
    let rowCount = 0;
    let rowIndex = 0;
    let pendingMetadataTrim = false;
    const hasPostTrim = postTrimStart >= 0;
    if (columns == null) return 0;
    const contentChildren = Array.from(columns.content.children);
    const gutterChildren = Array.from(columns.gutter.children);
    if (contentChildren.length !== gutterChildren.length) throw new Error("FileDiff.trimColumnRows: columns do not match");
    while (rowIndex < contentChildren.length) {
      if (preTrimCount <= 0 && !hasPostTrim && !pendingMetadataTrim) break;
      const gutterElement = gutterChildren[rowIndex];
      const contentElement = contentChildren[rowIndex];
      rowIndex++;
      if (!(gutterElement instanceof HTMLElement) || !(contentElement instanceof HTMLElement)) {
        console.error({
          gutterElement,
          contentElement
        });
        throw new Error("FileDiff.trimColumnRows: invalid row elements");
      }
      if (pendingMetadataTrim) {
        pendingMetadataTrim = false;
        if (gutterElement.dataset.gutterBuffer === "annotation" && "lineAnnotation" in contentElement.dataset || gutterElement.dataset.gutterBuffer === "metadata" && "noNewline" in contentElement.dataset) {
          gutterElement.remove();
          contentElement.remove();
          rowCount++;
          continue;
        }
      }
      if ("lineIndex" in gutterElement.dataset && "lineIndex" in contentElement.dataset) {
        if (preTrimCount > 0 || hasPostTrim && visibleLineIndex >= postTrimStart) {
          gutterElement.remove();
          contentElement.remove();
          if (preTrimCount > 0) {
            preTrimCount--;
            if (preTrimCount === 0) pendingMetadataTrim = true;
          }
          rowCount++;
        }
        visibleLineIndex++;
        continue;
      }
      if ("separator" in gutterElement.dataset && "separator" in contentElement.dataset) {
        if (preTrimCount > 0 || hasPostTrim && visibleLineIndex >= postTrimStart) {
          gutterElement.remove();
          contentElement.remove();
          rowCount++;
        }
        continue;
      }
      if (gutterElement.dataset.gutterBuffer === "annotation" && "lineAnnotation" in contentElement.dataset) {
        if (preTrimCount > 0 || hasPostTrim && visibleLineIndex >= postTrimStart) {
          gutterElement.remove();
          contentElement.remove();
          rowCount++;
        }
        continue;
      }
      if (gutterElement.dataset.gutterBuffer === "metadata" && "noNewline" in contentElement.dataset) {
        if (preTrimCount > 0 || hasPostTrim && visibleLineIndex >= postTrimStart) {
          gutterElement.remove();
          contentElement.remove();
          rowCount++;
        }
        continue;
      }
      if (gutterElement.dataset.gutterBuffer === "buffer" && "contentBuffer" in contentElement.dataset) {
        const totalRows = this.getBufferSize(contentElement.dataset);
        if (totalRows == null) throw new Error("FileDiff.trimColumnRows: invalid element");
        if (preTrimCount > 0) {
          const rowsToRemove = Math.min(preTrimCount, totalRows);
          const newSize = totalRows - rowsToRemove;
          if (newSize > 0) {
            this.updateBufferSize(gutterElement, newSize);
            this.updateBufferSize(contentElement, newSize);
            rowCount += rowsToRemove;
          } else {
            gutterElement.remove();
            contentElement.remove();
            rowCount += totalRows;
          }
          preTrimCount -= rowsToRemove;
          if (preTrimCount === 0 && newSize === 0) pendingMetadataTrim = true;
        } else if (hasPostTrim) {
          const bufferStart = visibleLineIndex;
          const bufferEnd = visibleLineIndex + totalRows - 1;
          if (postTrimStart <= bufferStart) {
            gutterElement.remove();
            contentElement.remove();
            rowCount += totalRows;
          } else if (postTrimStart <= bufferEnd) {
            const rowsToRemove = bufferEnd - postTrimStart + 1;
            const newSize = totalRows - rowsToRemove;
            this.updateBufferSize(gutterElement, newSize);
            this.updateBufferSize(contentElement, newSize);
            rowCount += rowsToRemove;
          }
        }
        visibleLineIndex += totalRows;
        continue;
      }
      console.error({
        gutterElement,
        contentElement
      });
      throw new Error("FileDiff.trimColumnRows: unknown row elements");
    }
    return rowCount;
  }
  trimColumns({ columns, diffStyle, overlapEnd, overlapStart, previousStart, trimEnd, trimStart }) {
    const preTrimCount = Math.max(0, overlapStart - previousStart);
    const postTrimStart = overlapEnd - previousStart;
    if (postTrimStart < 0) throw new Error("FileDiff.trimColumns: overlap ends before previous");
    const shouldTrimStart = trimStart > 0;
    const shouldTrimEnd = trimEnd > 0;
    if (!shouldTrimStart && !shouldTrimEnd) return 0;
    const effectivePreTrimCount = shouldTrimStart ? preTrimCount : 0;
    const effectivePostTrimStart = shouldTrimEnd ? postTrimStart : -1;
    if (diffStyle === "unified" && !Array.isArray(columns)) return this.trimColumnRows(columns, effectivePreTrimCount, effectivePostTrimStart);
    else if (diffStyle === "split" && Array.isArray(columns)) {
      const deletionsTrim = this.trimColumnRows(columns[0], effectivePreTrimCount, effectivePostTrimStart);
      const additionsTrim = this.trimColumnRows(columns[1], effectivePreTrimCount, effectivePostTrimStart);
      if (columns[0] != null && columns[1] != null && deletionsTrim !== additionsTrim) throw new Error("FileDiff.trimColumns: split columns out of sync");
      return columns[0] != null ? deletionsTrim : additionsTrim;
    } else {
      console.error({
        diffStyle,
        columns
      });
      throw new Error("FileDiff.trimColumns: Invalid columns for diffType");
    }
  }
  getBufferSize(properties) {
    const parsed = Number.parseInt((properties == null ? void 0 : properties.bufferSize) ?? "", 10);
    return Number.isNaN(parsed) ? void 0 : parsed;
  }
  updateBufferSize(element2, size) {
    element2.dataset.bufferSize = `${size}`;
    element2.style.setProperty("grid-row", `span ${size}`);
    element2.style.setProperty("min-height", `calc(${size} * 1lh)`);
  }
  getColumnPair(code) {
    if (code == null) return;
    const gutter = code.children[0];
    const content = code.children[1];
    if (!(gutter instanceof HTMLElement) || !(content instanceof HTMLElement) || gutter.dataset.gutter == null || content.dataset.content == null) return;
    return {
      gutter,
      content
    };
  }
  getCodeColumns(diffStyle, codeUnified, codeDeletions, codeAdditions) {
    if (diffStyle === "unified") return this.getColumnPair(codeUnified);
    else {
      const deletions = this.getColumnPair(codeDeletions);
      const additions = this.getColumnPair(codeAdditions);
      return deletions != null || additions != null ? [deletions, additions] : void 0;
    }
  }
  updateBuffers(renderRange) {
    if (this.pre != null) this.applyBuffers(this.pre, renderRange);
  }
  applyBuffers(pre, renderRange) {
    if (renderRange == null || this.shouldDisableVirtualizationBuffers()) {
      if (this.bufferBefore != null) {
        this.bufferBefore.remove();
        this.bufferBefore = void 0;
      }
      if (this.bufferAfter != null) {
        this.bufferAfter.remove();
        this.bufferAfter = void 0;
      }
      return;
    }
    if (renderRange.bufferBefore > 0) {
      if (this.bufferBefore == null) {
        this.bufferBefore = document.createElement("div");
        this.bufferBefore.dataset.virtualizerBuffer = "before";
        pre.before(this.bufferBefore);
      }
      this.bufferBefore.style.setProperty("height", `${renderRange.bufferBefore}px`);
      this.bufferBefore.style.setProperty("contain", "strict");
    } else if (this.bufferBefore != null) {
      this.bufferBefore.remove();
      this.bufferBefore = void 0;
    }
    if (renderRange.bufferAfter > 0) {
      if (this.bufferAfter == null) {
        this.bufferAfter = document.createElement("div");
        this.bufferAfter.dataset.virtualizerBuffer = "after";
        pre.after(this.bufferAfter);
      }
      this.bufferAfter.style.setProperty("height", `${renderRange.bufferAfter}px`);
      this.bufferAfter.style.setProperty("contain", "strict");
    } else if (this.bufferAfter != null) {
      this.bufferAfter.remove();
      this.bufferAfter = void 0;
    }
  }
  shouldDisableVirtualizationBuffers() {
    return this.options.disableVirtualizationBuffers ?? false;
  }
  applyPreNodeAttributes(pre, { additionsContentAST, deletionsContentAST, totalLines }, customProperties) {
    const { diffIndicators = "bars", disableBackground = false, disableLineNumbers = false, overflow = "scroll", diffStyle = "split" } = this.options;
    const preProperties = {
      type: "diff",
      diffIndicators,
      disableBackground,
      disableLineNumbers,
      overflow,
      split: diffStyle === "unified" ? false : additionsContentAST != null && deletionsContentAST != null,
      totalLines,
      customProperties
    };
    if (arePrePropertiesEqual(preProperties, this.appliedPreAttributes)) return;
    setPreNodeProperties(pre, preProperties);
    this.appliedPreAttributes = preProperties;
  }
  applyErrorToDOM(error, container) {
    var _a9;
    this.cleanupErrorWrapper();
    (_a9 = this.pre) == null ? void 0 : _a9.remove();
    this.pre = void 0;
    this.appliedPreAttributes = void 0;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    this.errorWrapper ?? (this.errorWrapper = document.createElement("div"));
    this.errorWrapper.dataset.errorWrapper = "";
    this.errorWrapper.textContent = "";
    shadowRoot.appendChild(this.errorWrapper);
    const errorMessage = document.createElement("div");
    errorMessage.dataset.errorMessage = "";
    errorMessage.innerText = error.message;
    this.errorWrapper.appendChild(errorMessage);
    const errorStack = document.createElement("pre");
    errorStack.dataset.errorStack = "";
    errorStack.innerText = error.stack ?? "No Error Stack";
    this.errorWrapper.appendChild(errorStack);
  }
  cleanupErrorWrapper() {
    var _a9;
    (_a9 = this.errorWrapper) == null ? void 0 : _a9.remove();
    this.errorWrapper = void 0;
  }
}, __publicField(_a6, "LoadedCustomComponent", true), _a6);
function areOptionalFilesEqual(fileA, fileB) {
  if (fileA == null || fileB == null) return fileA == null && fileB == null;
  return areFilesEqual(fileA, fileB);
}
function hasDiffContent({ fileDiff, oldFile, newFile }) {
  return fileDiff != null && fileDiff.hunks.length > 0 || oldFile != null || newFile != null;
}
function hasDiffHeaderContent({ fileDiff, oldFile, newFile }) {
  return fileDiff != null || oldFile != null || newFile != null;
}
function shouldRenderCode2(pre, hasContent, collapsed = false) {
  return !collapsed && pre == null && hasContent;
}
function shouldRenderHeader2(headerElement, hasContent, disableFileHeader = false) {
  return headerElement == null && hasContent && !disableFileHeader;
}
function getElementChildren(node) {
  if (node == null || node.type !== "element") return;
  return node.children ?? [];
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/computeEstimatedDiffHeights.js
function computeEstimatedDiffHeights({ fileDiff, metrics, disableFileHeader, hunkSeparators, expandUnchanged, expandedHunks: configuredExpandedHunks, collapsedContextThreshold, canHydratePartialDiff }) {
  var _a9, _b, _c;
  let splitHeight = getVirtualFileHeaderRegion(metrics, disableFileHeader);
  let unifiedHeight = splitHeight;
  const expandedHunks = expandUnchanged ? true : configuredExpandedHunks;
  const finalHunkIndex = fileDiff.hunks.length - 1;
  for (let hunkIndex = 0; hunkIndex < fileDiff.hunks.length; hunkIndex++) {
    const hunk = fileDiff.hunks[hunkIndex];
    if (hunk == null) throw new Error("computeEstimatedDiffHeights: invalid hunk index");
    const leadingRegion = getExpandedRegion({
      isPartial: fileDiff.isPartial,
      rangeSize: hunk.collapsedBefore,
      expandedHunks,
      hunkIndex,
      collapsedContextThreshold
    });
    const leadingExpandedHeight = (leadingRegion.fromStart + leadingRegion.fromEnd) * metrics.lineHeight;
    splitHeight += leadingExpandedHeight;
    unifiedHeight += leadingExpandedHeight;
    if (leadingRegion.collapsedLines > 0) {
      const separatorHeight = ((_a9 = getLeadingHunkSeparatorLayout({
        type: hunkSeparators,
        metrics,
        hunkIndex,
        hunkSpecs: hunk.hunkSpecs
      })) == null ? void 0 : _a9.totalHeight) ?? 0;
      splitHeight += separatorHeight;
      unifiedHeight += separatorHeight;
    }
    splitHeight += hunk.splitLineCount * metrics.lineHeight;
    unifiedHeight += hunk.unifiedLineCount * metrics.lineHeight;
    const metadataLineCounts = getNoNewlineMetadataLineCounts(hunk);
    splitHeight += metadataLineCounts.split * metrics.lineHeight;
    unifiedHeight += metadataLineCounts.unified * metrics.lineHeight;
    const trailingRegion = hunkIndex === finalHunkIndex ? getTrailingExpandedRegion({
      fileDiff,
      hunkIndex,
      expandedHunks,
      collapsedContextThreshold,
      errorPrefix: "computeEstimatedDiffHeights"
    }) : void 0;
    if (trailingRegion != null) {
      const trailingExpandedHeight = (trailingRegion.fromStart + trailingRegion.fromEnd) * metrics.lineHeight;
      splitHeight += trailingExpandedHeight;
      unifiedHeight += trailingExpandedHeight;
      if (trailingRegion.collapsedLines > 0) {
        const separatorHeight = ((_b = getTrailingHunkSeparatorLayout({
          type: hunkSeparators,
          metrics
        })) == null ? void 0 : _b.totalHeight) ?? 0;
        splitHeight += separatorHeight;
        unifiedHeight += separatorHeight;
      }
    } else if (hunkIndex === finalHunkIndex && fileDiff.isPartial && canHydratePartialDiff) {
      const separatorHeight = ((_c = getTrailingHunkSeparatorLayout({
        type: hunkSeparators,
        metrics
      })) == null ? void 0 : _c.totalHeight) ?? 0;
      splitHeight += separatorHeight;
      unifiedHeight += separatorHeight;
    }
  }
  if (fileDiff.hunks.length > 0) {
    const paddingBottom = getVirtualFilePaddingBottom(metrics);
    splitHeight += paddingBottom;
    unifiedHeight += paddingBottom;
  }
  return {
    splitHeight,
    unifiedHeight
  };
}
function getNoNewlineMetadataLineCounts(hunk) {
  if (!hunk.noEOFCRAdditions && !hunk.noEOFCRDeletions) return {
    split: 0,
    unified: 0
  };
  const lastContent = hunk.hunkContent.at(-1);
  if (lastContent == null) return {
    split: 0,
    unified: 0
  };
  if (lastContent.type === "context") {
    const metadataRows = lastContent.lines > 0 ? 1 : 0;
    return {
      split: metadataRows,
      unified: metadataRows
    };
  }
  return getChangeNoNewlineMetadataLineCounts(hunk, lastContent);
}
function getChangeNoNewlineMetadataLineCounts(hunk, content) {
  const unified = (content.deletions > 0 && hunk.noEOFCRDeletions ? 1 : 0) + (content.additions > 0 && hunk.noEOFCRAdditions ? 1 : 0);
  const splitDeletionHasMetadata = content.deletions > 0 && hunk.noEOFCRDeletions;
  const splitAdditionHasMetadata = content.additions > 0 && hunk.noEOFCRAdditions;
  return {
    split: splitDeletionHasMetadata || splitAdditionHasMetadata ? 1 : 0,
    unified
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/VirtualizedFileDiff.js
var VIRTUALIZED_FILE_DIFF_LAYOUT_CHECKPOINT_INTERVAL = 3e3;
var instanceId6 = -1;
var VirtualizedFileDiff = class extends FileDiff {
  constructor(options, virtualizer, metrics, workerManager, isContainerManaged = false) {
    super(options, workerManager, isContainerManaged);
    __publicField(this, "__id", `little-virtualized-file-diff:${++instanceId6}`);
    __publicField(this, "top");
    __publicField(this, "height", 0);
    __publicField(this, "metrics");
    __publicField(this, "cache", {
      heightDeltas: /* @__PURE__ */ new Map(),
      measuredHeightDeltaTotal: 0,
      estimatedSplitHeight: void 0,
      estimatedUnifiedHeight: void 0,
      checkpoints: [],
      totalLines: 0,
      fileAnnotationHeight: 0
    });
    __publicField(this, "isVisible", false);
    __publicField(this, "isSetup", false);
    __publicField(this, "virtualizer");
    __publicField(this, "layoutDirty", true);
    __publicField(this, "forceRenderOverride");
    __publicField(this, "currentCollapsed");
    __publicField(this, "currentExpandUnchanged");
    __publicField(this, "pendingHydratedDiff");
    __publicField(this, "pendingExpansions");
    __publicField(this, "onRender", (dirty) => {
      if (this.fileContainer == null) return false;
      if (dirty) this.top = this.getVirtualizedTop();
      return this.render();
    });
    __publicField(this, "expandHunk", (hunkIndex, direction, expansionLineCountOverride) => {
      if (this.fileDiff == null) return;
      if (this.isAdvancedMode()) {
        this.pendingExpansions ?? (this.pendingExpansions = []);
        this.pendingExpansions.push({
          hunkIndex,
          direction,
          expansionLineCountOverride
        });
      } else {
        this.hunksRenderer.expandHunk(hunkIndex, direction, expansionLineCountOverride);
        this.resetLayoutCache({ includeEstimatedHeights: true });
        this.computeApproximateSize();
      }
      this.loadFilesIfNecessary();
      this.forceRenderOverride = true;
      this.virtualizer.instanceChanged(this, true);
    });
    this.virtualizer = virtualizer;
    this.metrics = computeVirtualFileMetrics(metrics);
  }
  setMetrics(metrics, force = false) {
    const nextMetrics = computeVirtualFileMetrics(metrics);
    if (!force && areObjectsEqual(this.metrics, nextMetrics)) return;
    this.metrics = nextMetrics;
    this.resetLayoutCache({ includeEstimatedHeights: true });
  }
  setLineAnnotations(lineAnnotations) {
    if (this.syncLineAnnotations(lineAnnotations)) this.resetLayoutCache({ includeEstimatedHeights: false });
  }
  syncLineAnnotations(lineAnnotations) {
    if (lineAnnotations == null || lineAnnotations === this.lineAnnotations || lineAnnotations.length === 0 && this.lineAnnotations.length === 0) return false;
    super.setLineAnnotations(lineAnnotations);
    return true;
  }
  setFileAnnotationHeight(nextHeight) {
    const previousHeight = this.cache.fileAnnotationHeight;
    if (nextHeight === previousHeight) return false;
    this.cache.fileAnnotationHeight = nextHeight;
    this.cache.measuredHeightDeltaTotal += nextHeight - previousHeight;
    return true;
  }
  hasFileAnnotations(fileDiff = this.fileDiff) {
    if (fileDiff == null || !includesFileAnnotations(this.lineAnnotations)) return false;
    return this.lineAnnotations.some((annotation) => {
      if (annotation.lineNumber !== 0) return false;
      if (fileDiff.type === "new") return annotation.side === "additions";
      if (fileDiff.type === "deleted") return annotation.side === "deletions";
      return true;
    });
  }
  getLineHeight(lineIndex, hasMetadataLine = false) {
    return this.getEstimatedLineHeight(hasMetadataLine) + (this.cache.heightDeltas.get(lineIndex) ?? 0);
  }
  getEstimatedLineHeight(hasMetadataLine = false) {
    const multiplier = hasMetadataLine ? 2 : 1;
    return this.metrics.lineHeight * multiplier;
  }
  setOptions(options) {
    if (this.isAdvancedMode()) throw new Error("VirtualizedFileDiff.setOptions cannot be used inside CodeView. Update CodeView options instead.");
    if (options == null) return;
    const { options: previousOptions } = this;
    const optionsChanged = !areOptionsEqual(previousOptions, options);
    const layoutChanged = optionsChanged && hasDiffLayoutOptionChanged(previousOptions, options);
    super.setOptions(options);
    if (layoutChanged) this.resetLayoutCache({
      forceSimpleRecompute: true,
      includeEstimatedHeights: hasDiffEstimateOptionChanged(previousOptions, options)
    });
    if (optionsChanged) this.forceRenderOverride = true;
    if (optionsChanged && this.isSimpleMode()) this.virtualizer.instanceChanged(this, layoutChanged);
  }
  setThemeType(themeType) {
    if (this.isAdvancedMode()) throw new Error("VirtualizedFileDiff.setThemeType cannot be used inside CodeView. Update CodeView options instead.");
    super.setThemeType(themeType);
  }
  resetLayoutCache({ forceSimpleRecompute = false, includeEstimatedHeights = false, resetRenderRange = true } = {}) {
    this.layoutDirty = true;
    this.cache.fileAnnotationHeight = 0;
    if (this.cache.heightDeltas.size > 0) this.cache.heightDeltas.clear();
    if (this.cache.measuredHeightDeltaTotal !== 0) this.cache.measuredHeightDeltaTotal = 0;
    this.invalidateDerivedLayoutCache(includeEstimatedHeights, resetRenderRange);
    if (forceSimpleRecompute && this.isSimpleMode()) this.computeApproximateSize();
  }
  invalidateDerivedLayoutCache(includeEstimatedHeights, resetRenderRange = true) {
    this.layoutDirty = true;
    if (this.cache.checkpoints.length > 0) this.cache.checkpoints.length = 0;
    if (this.cache.totalLines !== 0) this.cache.totalLines = 0;
    if (includeEstimatedHeights) {
      this.cache.estimatedSplitHeight = void 0;
      this.cache.estimatedUnifiedHeight = void 0;
    }
    if (this.renderRange != null && resetRenderRange) this.renderRange = void 0;
  }
  reconcileHeights() {
    let hasHeightChange = false;
    const { overflow = "scroll" } = this.options;
    if (this.fileContainer == null || this.fileDiff == null) {
      if (this.height !== 0) hasHeightChange = true;
      this.height = 0;
      return hasHeightChange;
    }
    this.top = this.getVirtualizedTop();
    if (overflow === "scroll" && this.lineAnnotations.length === 0 && !this.isResizeDebuggingEnabled()) return hasHeightChange;
    const diffStyle = this.getDiffStyle();
    const codeGroups = diffStyle === "split" ? [this.codeDeletions, this.codeAdditions] : [this.codeUnified];
    const hasFileAnnotations = this.hasFileAnnotations(this.fileDiff);
    if (this.renderRange != null && hasFileAnnotations && shouldRenderFileAnnotations(this.renderRange)) {
      const nextFileAnnotationHeight = measureFileAnnotationHeight2(codeGroups) ?? 0;
      if (this.setFileAnnotationHeight(nextFileAnnotationHeight)) hasHeightChange = true;
    } else if (!hasFileAnnotations && this.setFileAnnotationHeight(0)) hasHeightChange = true;
    for (const codeGroup of codeGroups) {
      if (codeGroup == null) continue;
      const content = codeGroup.children[1];
      if (!(content instanceof HTMLElement)) continue;
      for (const line of content.children) {
        if (!(line instanceof HTMLElement)) continue;
        const lineIndexAttr = line.dataset.lineIndex;
        if (lineIndexAttr == null) continue;
        const lineIndex = parseLineIndex(lineIndexAttr, diffStyle);
        let measuredHeight = line.getBoundingClientRect().height;
        let hasMetadata = false;
        if (line.nextElementSibling instanceof HTMLElement && ("lineAnnotation" in line.nextElementSibling.dataset || "noNewline" in line.nextElementSibling.dataset)) {
          if ("noNewline" in line.nextElementSibling.dataset) hasMetadata = true;
          measuredHeight += line.nextElementSibling.getBoundingClientRect().height;
        }
        const estimatedHeight = this.getEstimatedLineHeight(hasMetadata);
        const previousDelta = this.cache.heightDeltas.get(lineIndex) ?? 0;
        const nextDelta = measuredHeight - estimatedHeight;
        if (nextDelta === previousDelta) continue;
        hasHeightChange = true;
        this.cache.measuredHeightDeltaTotal += nextDelta - previousDelta;
        if (nextDelta === 0) this.cache.heightDeltas.delete(lineIndex);
        else this.cache.heightDeltas.set(lineIndex, nextDelta);
      }
    }
    if (hasHeightChange || this.isResizeDebuggingEnabled()) this.computeApproximateSize(true);
    return hasHeightChange;
  }
  flushManagers() {
    super.flushManagers();
    if (this.lineStateRefreshPending) this.flushDeferredLineState();
  }
  prepareCodeViewItem(fileDiff, top, reset, lineAnnotations) {
    const targetChanged = !areDiffTargetsEqual(this.fileDiff, fileDiff);
    const annotationsChanged = this.syncLineAnnotations(lineAnnotations);
    let shouldResetLayoutCache = (reset == null ? void 0 : reset.resetDiffLayoutCache) === true || targetChanged || annotationsChanged;
    let includeEstimatedHeights = targetChanged || (reset == null ? void 0 : reset.resetDiffLayoutCache) === true && reset.includeEstimatedDiffHeights;
    if ((reset == null ? void 0 : reset.metrics) != null) {
      this.metrics = computeVirtualFileMetrics(reset.metrics);
      shouldResetLayoutCache = true;
      includeEstimatedHeights = true;
    }
    const { collapsed = false, expandUnchanged = false } = this.options;
    if (this.currentCollapsed !== collapsed) {
      this.currentCollapsed = collapsed;
      shouldResetLayoutCache = true;
    }
    if (this.currentExpandUnchanged !== expandUnchanged) {
      this.currentExpandUnchanged = expandUnchanged;
      shouldResetLayoutCache = true;
      includeEstimatedHeights = true;
    }
    if (shouldResetLayoutCache) this.resetLayoutCache({ includeEstimatedHeights });
    this.fileDiff = fileDiff;
    this.top = top;
    this.computeApproximateSize();
    return this.height;
  }
  getLinePosition(lineNumber, side = "additions") {
    if (this.fileDiff == null || lineNumber < 1) return;
    const targetLineIndexes = this.getLineIndex(lineNumber, side);
    if (targetLineIndexes == null) return;
    const { disableFileHeader = false, expandUnchanged = false, collapsed = false, collapsedContextThreshold = 1 } = this.options;
    const diffStyle = this.getDiffStyle();
    const hunkSeparators = this.getHunkSeparatorType();
    const targetLineIndex = diffStyle === "split" ? targetLineIndexes[1] : targetLineIndexes[0];
    this.approximateLayoutCheckpoints();
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    const checkpoint = this.getLayoutCheckpointBeforeLineIndex(targetLineIndex);
    let top = (checkpoint == null ? void 0 : checkpoint.top) ?? headerRegion + this.cache.fileAnnotationHeight;
    if (collapsed) return {
      top: headerRegion,
      height: 0
    };
    let position;
    iterateOverDiff({
      diff: this.fileDiff,
      diffStyle,
      startingLine: (checkpoint == null ? void 0 : checkpoint.renderedLineIndex) ?? 0,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold,
      callback: ({ hunkIndex, hunk, collapsedBefore, collapsedAfter, deletionLine, additionLine }) => {
        const lineIndex = diffStyle === "split" ? (additionLine == null ? void 0 : additionLine.splitLineIndex) ?? (deletionLine == null ? void 0 : deletionLine.splitLineIndex) : (additionLine == null ? void 0 : additionLine.unifiedLineIndex) ?? (deletionLine == null ? void 0 : deletionLine.unifiedLineIndex);
        if (lineIndex == null) throw new Error("VirtualizedFileDiff.getLinePosition: missing line index data");
        if (collapsedBefore > 0) {
          const separator = getLeadingHunkSeparatorLayout({
            type: hunkSeparators,
            metrics: this.metrics,
            hunkIndex,
            hunkSpecs: hunk == null ? void 0 : hunk.hunkSpecs
          });
          if (separator != null) {
            top += separator.gapBefore;
            if (targetLineIndex >= lineIndex - collapsedBefore && targetLineIndex < lineIndex) {
              position = {
                top,
                height: separator.height
              };
              return true;
            }
            top += separator.height + separator.gapAfter;
          }
        }
        const lineHeight = this.getLineHeight(lineIndex, ((additionLine == null ? void 0 : additionLine.noEOFCR) ?? false) || ((deletionLine == null ? void 0 : deletionLine.noEOFCR) ?? false));
        if (lineIndex === targetLineIndex) {
          position = {
            top,
            height: lineHeight
          };
          return true;
        }
        top += lineHeight;
        if (collapsedAfter > 0) {
          const separator = getTrailingHunkSeparatorLayout({
            type: hunkSeparators,
            metrics: this.metrics
          });
          if (separator != null) {
            if (targetLineIndex > lineIndex && targetLineIndex <= lineIndex + collapsedAfter) {
              position = {
                top: top + separator.gapBefore,
                height: separator.height
              };
              return true;
            }
            top += separator.totalHeight;
          }
        }
        return false;
      }
    });
    return position;
  }
  getEditorViewport() {
    return this.virtualizer.type === "simple" ? this.virtualizer.getRoot() : this.virtualizer.getContainerElement();
  }
  getNumericScrollAnchor(localViewportTop) {
    if (this.fileDiff == null) return;
    const { disableFileHeader = false, expandUnchanged = false, collapsed = false, collapsedContextThreshold = 1 } = this.options;
    if (collapsed) return;
    const diffStyle = this.getDiffStyle();
    const hunkSeparators = this.getHunkSeparatorType();
    this.approximateLayoutCheckpoints();
    const checkpoint = this.getLayoutCheckpointBeforeTop(localViewportTop);
    let top = (checkpoint == null ? void 0 : checkpoint.top) ?? getVirtualFileHeaderRegion(this.metrics, disableFileHeader) + this.cache.fileAnnotationHeight;
    let anchor;
    iterateOverDiff({
      diff: this.fileDiff,
      diffStyle,
      startingLine: (checkpoint == null ? void 0 : checkpoint.renderedLineIndex) ?? 0,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold,
      callback: ({ hunkIndex, hunk, collapsedBefore, collapsedAfter, deletionLine, additionLine }) => {
        const lineIndex = diffStyle === "split" ? (additionLine == null ? void 0 : additionLine.splitLineIndex) ?? (deletionLine == null ? void 0 : deletionLine.splitLineIndex) : (additionLine == null ? void 0 : additionLine.unifiedLineIndex) ?? (deletionLine == null ? void 0 : deletionLine.unifiedLineIndex);
        if (lineIndex == null) throw new Error("VirtualizedFileDiff.getNumericScrollAnchor: missing line index data");
        if (collapsedBefore > 0) {
          const separator = getLeadingHunkSeparatorLayout({
            type: hunkSeparators,
            metrics: this.metrics,
            hunkIndex,
            hunkSpecs: hunk == null ? void 0 : hunk.hunkSpecs
          });
          if (separator != null) top += separator.totalHeight;
        }
        if (top >= localViewportTop) {
          if (deletionLine != null) anchor = {
            lineNumber: deletionLine.lineNumber,
            side: "deletions",
            top
          };
          else if (additionLine != null) anchor = {
            lineNumber: additionLine.lineNumber,
            side: "additions",
            top
          };
          if (anchor != null) return true;
        }
        const lineHeight = this.getLineHeight(lineIndex, ((additionLine == null ? void 0 : additionLine.noEOFCR) ?? false) || ((deletionLine == null ? void 0 : deletionLine.noEOFCR) ?? false));
        top += lineHeight;
        if (collapsedAfter > 0) {
          const separator = getTrailingHunkSeparatorLayout({
            type: hunkSeparators,
            metrics: this.metrics
          });
          if (separator != null) top += separator.totalHeight;
        }
        return false;
      }
    });
    return anchor;
  }
  getVirtualizedHeight() {
    return this.height;
  }
  getAdvancedStickySpecs(windowSpecs) {
    if (this.top == null || this.fileDiff == null) return;
    if (this.options.collapsed === true) return {
      topOffset: this.top,
      height: this.height
    };
    const renderRange = windowSpecs != null ? this.computeRenderRangeFromWindow(this.fileDiff, this.top, windowSpecs) : this.renderRange;
    if (renderRange == null) return;
    const { bufferBefore, bufferAfter, totalLines } = renderRange;
    let headerOnlyOffset = 0;
    if (totalLines === 0) {
      const activeWindow = windowSpecs ?? this.virtualizer.getWindowSpecs();
      if (this.top < activeWindow.top) headerOnlyOffset = bufferAfter;
    }
    return {
      topOffset: this.top + bufferBefore + headerOnlyOffset,
      height: this.height - (bufferBefore + bufferAfter)
    };
  }
  cleanUp(recycle = false) {
    var _a9;
    if (this.fileContainer != null && this.isSimpleMode()) (_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.disconnect(this.fileContainer);
    if (!recycle) {
      this.resetLayoutCache({ includeEstimatedHeights: true });
      this.pendingExpansions = void 0;
      this.pendingHydratedDiff = void 0;
    }
    this.isSetup = false;
    super.cleanUp(recycle);
  }
  async handleFilesLoaded(expectedDiff, files) {
    if (this.fileDiff !== expectedDiff || !expectedDiff.isPartial) return;
    if (this.isAdvancedMode()) {
      const nextDiff = hydratePartialDiff("clone", expectedDiff, files);
      await awaitWithTimeout(() => this.primeHighlightCache(nextDiff));
      if (!this.enabled || this.fileDiff !== expectedDiff) return;
      this.pendingHydratedDiff = {
        expectedDiff,
        nextDiff,
        files
      };
    } else {
      hydratePartialDiff("merge", expectedDiff, files);
      this.setHydratedState(files);
      await awaitWithTimeout(() => this.primeHighlightCache(expectedDiff));
      if (!this.enabled || this.fileDiff !== expectedDiff) return;
      this.resetLayoutCache({ includeEstimatedHeights: true });
      this.computeApproximateSize();
    }
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, true);
  }
  consumeCodeViewLayoutChanges(expectedFileDiff) {
    let hasLayoutChange = false;
    let nextDiff;
    const { pendingExpansions, pendingHydratedDiff } = this;
    if (pendingExpansions != null) {
      this.pendingExpansions = void 0;
      for (const pendingExpansion of pendingExpansions) {
        this.hunksRenderer.expandHunk(pendingExpansion.hunkIndex, pendingExpansion.direction, pendingExpansion.expansionLineCountOverride);
        hasLayoutChange = true;
      }
    }
    if (pendingHydratedDiff != null) {
      this.pendingHydratedDiff = void 0;
      if (pendingHydratedDiff.expectedDiff === expectedFileDiff) {
        this.setHydratedState(pendingHydratedDiff.files);
        nextDiff = pendingHydratedDiff.nextDiff;
      }
    }
    if (nextDiff != null) {
      this.forceRenderOverride = true;
      this.resetLayoutCache({ includeEstimatedHeights: true });
    } else if (hasLayoutChange) {
      this.forceRenderOverride = true;
      this.invalidateDerivedLayoutCache(true);
    }
    return nextDiff;
  }
  loadFilesIfNecessary() {
    if (this.pendingHydratedDiff != null) {
      if (this.pendingHydratedDiff.expectedDiff === this.fileDiff) return;
      this.pendingHydratedDiff = void 0;
    }
    super.loadFilesIfNecessary();
  }
  isLineRenderable(lineNumber) {
    if (super.isLineRenderable(lineNumber)) return true;
    const { pendingExpansions } = this;
    const fileDiff = this.fileDiffCache;
    if (pendingExpansions == null || pendingExpansions.length === 0 || fileDiff == null) return false;
    const { expansionLineCount = 100, collapsedContextThreshold = 1 } = this.options;
    const staged = new Map(this.hunksRenderer.getExpandedHunksMap());
    for (const expansion of pendingExpansions) {
      const region = { ...staged.get(expansion.hunkIndex) ?? {
        fromStart: 0,
        fromEnd: 0
      } };
      const count = expansion.expansionLineCountOverride ?? expansionLineCount;
      if (expansion.direction === "up" || expansion.direction === "both") region.fromStart += count;
      if (expansion.direction === "down" || expansion.direction === "both") region.fromEnd += count;
      staged.set(expansion.hunkIndex, region);
    }
    return isAdditionLineRenderable({
      fileDiff,
      lineNumber,
      expandedHunks: staged,
      collapsedContextThreshold
    });
  }
  /**
  * Invalidate layout after an edit session changed the rendered row set
  * without a line-count change (a mid-session region change or the exit
  * recompute): estimated heights bake the hunk shapes in, and nothing else
  * invalidates them now that editing does not flip expandUnchanged. Public
  * so CodeView can run it when reaping a session whose instance was already
  * released.
  */
  invalidateEditSessionLayout() {
    var _a9, _b;
    (_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.markDOMDirty();
    this.resetLayoutCache({
      forceSimpleRecompute: this.isSimpleMode(),
      includeEstimatedHeights: true,
      resetRenderRange: false
    });
    if (!this.isSimpleMode()) this.computeApproximateSize(true);
    (_b = this.getSimpleVirtualizer()) == null ? void 0 : _b.requestHeightReconcile(this);
  }
  escalateEditSessionRender() {
    this.invalidateEditSessionLayout();
    if (!this.enabled || this.fileDiff == null) return;
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, true);
  }
  shouldSelfHealEditSession() {
    return !this.isAdvancedMode() && super.shouldSelfHealEditSession();
  }
  setVisibility(visible) {
    if (this.isAdvancedMode() || this.fileContainer == null) return;
    this.renderRange = void 0;
    if (visible && !this.isVisible) {
      this.top = this.getVirtualizedTop();
      this.isVisible = true;
    } else if (!visible && this.isVisible) {
      this.isVisible = false;
      this.rerender();
    }
  }
  rerender() {
    if (!this.enabled || this.fileDiff == null && this.additionFile == null && this.deletionFile == null) return;
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, false);
  }
  applyDocumentChange(textDocument, newLineAnnotations, shouldUpdateBuffer = false) {
    var _a9, _b;
    const { renderRange: previousRenderRange } = this;
    (_a9 = this.getAdvancedVirtualizer()) == null ? void 0 : _a9.capturePendingLayoutAnchor();
    super.applyDocumentChange(textDocument, newLineAnnotations);
    (_b = this.getSimpleVirtualizer()) == null ? void 0 : _b.markDOMDirty();
    this.resetLayoutCache({
      forceSimpleRecompute: this.isSimpleMode(),
      includeEstimatedHeights: true,
      resetRenderRange: false
    });
    if (!this.isSimpleMode()) this.computeApproximateSize(true);
    else if (shouldUpdateBuffer && previousRenderRange !== void 0 && this.fileDiff !== void 0) {
      const windowSpecs = this.virtualizer.getWindowSpecs();
      const renderRange = this.computeRenderRangeFromWindow(this.fileDiff, this.top ?? 0, windowSpecs);
      if (renderRange.bufferAfter !== previousRenderRange.bufferAfter) this.updateBuffers(renderRange);
    }
    this.forceRenderOverride = true;
    this.virtualizer.instanceChanged(this, true);
  }
  computeApproximateSize(force = false, fileDiff = this.fileDiff) {
    const shouldValidateSize = this.isResizeDebuggingEnabled();
    if (!force && !this.layoutDirty && !shouldValidateSize) return;
    const isFirstCompute = this.height === 0;
    this.height = 0;
    this.cache.checkpoints = [];
    this.cache.totalLines = 0;
    if (fileDiff == null) {
      this.layoutDirty = false;
      return;
    }
    const { disableFileHeader = false, collapsed = false } = this.options;
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    this.height += headerRegion;
    if (collapsed) {
      this.layoutDirty = false;
      return;
    }
    this.height = this.getActiveEstimatedHeight(fileDiff) + this.cache.measuredHeightDeltaTotal;
    if (shouldValidateSize && !isFirstCompute) this.validateComputedHeight(fileDiff);
    this.layoutDirty = false;
  }
  getActiveEstimatedHeight(fileDiff = this.fileDiff) {
    this.ensureEstimatedDiffHeights(fileDiff);
    const estimatedHeight = this.getDiffStyle() === "split" ? this.cache.estimatedSplitHeight : this.cache.estimatedUnifiedHeight;
    if (estimatedHeight == null) throw new Error("VirtualizedFileDiff.getActiveEstimatedHeight: missing estimated height");
    return estimatedHeight;
  }
  ensureEstimatedDiffHeights(fileDiff = this.fileDiff) {
    if (fileDiff == null) {
      this.cache.estimatedSplitHeight = void 0;
      this.cache.estimatedUnifiedHeight = void 0;
      return;
    }
    if (this.cache.estimatedSplitHeight != null && this.cache.estimatedUnifiedHeight != null) return;
    const { disableFileHeader = false, expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    const { splitHeight, unifiedHeight } = computeEstimatedDiffHeights({
      fileDiff,
      metrics: this.metrics,
      disableFileHeader,
      hunkSeparators: this.getHunkSeparatorType(),
      expandUnchanged,
      expandedHunks: this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold,
      canHydratePartialDiff: canHydrateCollapsedContext2(fileDiff, this.options.loadDiffFiles != null)
    });
    this.cache.estimatedSplitHeight = splitHeight;
    this.cache.estimatedUnifiedHeight = unifiedHeight;
  }
  validateComputedHeight(fileDiff = this.fileDiff) {
    if (this.fileContainer == null || fileDiff == null) return;
    const rect = this.fileContainer.getBoundingClientRect();
    if (rect.height !== this.height) console.log("VirtualizedFileDiff.computeApproximateSize: computed height doesnt match", {
      name: fileDiff.name,
      elementHeight: rect.height,
      computedHeight: this.height
    });
    else console.log("VirtualizedFileDiff.computeApproximateSize: computed height IS CORRECT");
  }
  render({ fileContainer, fileDiff, forceRender = false, lineAnnotations, ...fileInputProps } = {}) {
    var _a9, _b;
    const fileInput = getDiffFileInput(fileInputProps, "VirtualizedFileDiff.render");
    const hasFileInput = fileInput != null;
    const oldFile = fileInput == null ? void 0 : fileInput.oldFile;
    const newFile = fileInput == null ? void 0 : fileInput.newFile;
    const filesDidChange = hasFileInput && (!areOptionalFilesEqual2(oldFile, this.deletionFile) || !areOptionalFilesEqual2(newFile, this.additionFile));
    let nextFileDiff = fileDiff ?? this.fileDiff;
    if (fileDiff == null && hasFileInput && (filesDidChange || this.fileDiff == null)) nextFileDiff = parseDiffFromFile(fileInput.oldFile, fileInput.newFile, this.options.parseDiffOptions);
    const { forceRenderOverride, isSetup } = this;
    this.forceRenderOverride = void 0;
    const annotationsChanged = this.syncLineAnnotations(lineAnnotations);
    if (annotationsChanged) this.resetLayoutCache({ includeEstimatedHeights: false });
    const diffInputChanged = fileDiff != null && fileDiff !== this.fileDiff;
    const targetChanged = nextFileDiff != null && !areDiffTargetsEqual(this.fileDiff, nextFileDiff);
    const dataChanged = diffInputChanged || filesDidChange;
    if (targetChanged) this.resetLayoutCache({ includeEstimatedHeights: true });
    fileContainer = this.getOrCreateFileContainer(fileContainer);
    if (nextFileDiff == null) {
      console.error("VirtualizedFileDiff.render: attempting to virtually render when we dont have the correct data");
      return false;
    }
    if (!isSetup) {
      this.computeApproximateSize(false, nextFileDiff);
      const virtualizer = this.getSimpleVirtualizer();
      this.top ?? (this.top = this.getVirtualizedTop());
      if (this.isAdvancedMode()) this.isVisible = true;
      else {
        if (virtualizer == null) throw new Error("VirtualizedFileDiff.render: simple virtualizer is not available");
        virtualizer.connect(fileContainer, this);
        this.isVisible = virtualizer.isInstanceVisible(this.top ?? 0, this.height);
      }
      this.isSetup = true;
    } else {
      this.top ?? (this.top = this.getVirtualizedTop());
      if (targetChanged) {
        (_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.markDOMDirty();
        this.computeApproximateSize(false, nextFileDiff);
      }
    }
    if (!this.isVisible && this.isSimpleMode() && (!dataChanged || !isSetup)) {
      this.fileDiff = nextFileDiff;
      if (fileInput != null) {
        this.deletionFile = oldFile;
        this.additionFile = newFile;
      }
      if (targetChanged) this.cachedHeaderHTML = void 0;
      return this.renderPlaceholder(this.height);
    }
    const windowSpecs = this.virtualizer.getWindowSpecs();
    const fileTop = this.top ?? 0;
    const renderRange = this.computeRenderRangeFromWindow(nextFileDiff, fileTop, windowSpecs);
    const rendered = super.render({
      fileDiff: nextFileDiff,
      fileContainer,
      renderRange,
      lineAnnotations,
      forceRender: (forceRenderOverride ?? forceRender) || annotationsChanged || targetChanged,
      ...fileInput,
      ...fileInputProps
    });
    if (this.isSimpleMode() && rendered) (_b = this.getSimpleVirtualizer()) == null ? void 0 : _b.requestHeightReconcile(this);
    return rendered;
  }
  syncVirtualizedTop() {
    this.top = this.getVirtualizedTop();
  }
  shouldDisableVirtualizationBuffers() {
    return this.isAdvancedMode() || super.shouldDisableVirtualizationBuffers();
  }
  shouldGuardRebuildScroll() {
    return false;
  }
  isSimpleMode() {
    return this.virtualizer.type === "simple";
  }
  isAdvancedMode() {
    return this.virtualizer.type === "advanced";
  }
  getVirtualizedTop() {
    if (this.virtualizer.type === "advanced") return this.virtualizer.getLocalTopForInstance(this);
    return this.fileContainer != null ? this.virtualizer.getOffsetInScrollContainer(this.fileContainer) : 0;
  }
  getSimpleVirtualizer() {
    return this.virtualizer.type === "simple" ? this.virtualizer : void 0;
  }
  getAdvancedVirtualizer() {
    return this.virtualizer.type === "advanced" ? this.virtualizer : void 0;
  }
  isResizeDebuggingEnabled() {
    var _a9;
    return ((_a9 = this.getSimpleVirtualizer()) == null ? void 0 : _a9.config.resizeDebugging) ?? false;
  }
  getDiffStyle() {
    return this.options.diffStyle ?? "split";
  }
  getHunkSeparatorType() {
    return getOptionHunkSeparatorType(this.options.hunkSeparators);
  }
  approximateLayoutCheckpoints(fileDiff = this.fileDiff) {
    var _a9, _b, _c;
    if (!this.layoutDirty && this.cache.checkpoints.length > 0 || fileDiff == null || fileDiff.hunks.length === 0 || this.options.collapsed === true) return;
    const { disableFileHeader = false, expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    const finalHunkIndex = fileDiff.hunks.length - 1;
    const canHydratePartialDiff = canHydrateCollapsedContext2(fileDiff, this.options.loadDiffFiles != null);
    const diffStyle = this.getDiffStyle();
    const hunkSeparators = this.getHunkSeparatorType();
    const expandedHunks = expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap();
    const heightDeltaPrefix = createHeightDeltaPrefix(this.cache.heightDeltas);
    let top = getVirtualFileHeaderRegion(this.metrics, disableFileHeader) + this.cache.fileAnnotationHeight;
    let renderedLineIndex = 0;
    const processRows = ({ rowCount, startLineIndex, preSeparatorHeight = 0, postSeparatorHeight = 0, metadataOffsets = [] }) => {
      if (rowCount <= 0) return;
      const blockStart = renderedLineIndex;
      const blockEnd = renderedLineIndex + rowCount;
      let nextCheckpoint = getNextCheckpointIndex(blockStart);
      while (nextCheckpoint < blockEnd) {
        const offset = nextCheckpoint - blockStart;
        const checkpointTop = top + (offset > 0 ? preSeparatorHeight : 0) + offset * this.metrics.lineHeight + countMetadataOffsetsBefore(metadataOffsets, offset) * this.metrics.lineHeight + sumHeightDeltas(heightDeltaPrefix, startLineIndex, startLineIndex + offset);
        this.cache.checkpoints.push({
          renderedLineIndex: nextCheckpoint,
          lineIndex: startLineIndex + offset,
          top: checkpointTop
        });
        nextCheckpoint += VIRTUALIZED_FILE_DIFF_LAYOUT_CHECKPOINT_INTERVAL;
      }
      top += preSeparatorHeight + rowCount * this.metrics.lineHeight + metadataOffsets.length * this.metrics.lineHeight + sumHeightDeltas(heightDeltaPrefix, startLineIndex, startLineIndex + rowCount) + postSeparatorHeight;
      renderedLineIndex = blockEnd;
    };
    for (let hunkIndex = 0; hunkIndex < fileDiff.hunks.length; hunkIndex++) {
      const hunk = fileDiff.hunks[hunkIndex];
      if (hunk == null) throw new Error("VirtualizedFileDiff.approximateLayoutCheckpoints: invalid hunk index");
      const leadingRegion = getExpandedRegion({
        isPartial: fileDiff.isPartial,
        rangeSize: hunk.collapsedBefore,
        expandedHunks,
        hunkIndex,
        collapsedContextThreshold
      });
      const leadingSeparatorHeight = leadingRegion.collapsedLines > 0 ? ((_a9 = getLeadingHunkSeparatorLayout({
        type: hunkSeparators,
        metrics: this.metrics,
        hunkIndex,
        hunkSpecs: hunk.hunkSpecs
      })) == null ? void 0 : _a9.totalHeight) ?? 0 : 0;
      processRows({
        rowCount: leadingRegion.fromStart,
        startLineIndex: (diffStyle === "split" ? hunk.splitLineStart : hunk.unifiedLineStart) - leadingRegion.rangeSize
      });
      let pendingLeadingSeparatorHeight = leadingSeparatorHeight;
      processRows({
        rowCount: leadingRegion.fromEnd,
        startLineIndex: (diffStyle === "split" ? hunk.splitLineStart : hunk.unifiedLineStart) - leadingRegion.fromEnd,
        preSeparatorHeight: pendingLeadingSeparatorHeight
      });
      if (leadingRegion.fromEnd > 0) pendingLeadingSeparatorHeight = 0;
      const trailingRegion = hunkIndex === finalHunkIndex ? getTrailingExpandedRegion({
        fileDiff,
        hunkIndex,
        expandedHunks,
        collapsedContextThreshold,
        errorPrefix: "VirtualizedFileDiff"
      }) : void 0;
      const trailingSeparatorHeight = trailingRegion != null && trailingRegion.collapsedLines > 0 ? ((_b = getTrailingHunkSeparatorLayout({
        type: hunkSeparators,
        metrics: this.metrics
      })) == null ? void 0 : _b.totalHeight) ?? 0 : hunkIndex === finalHunkIndex && canHydratePartialDiff ? ((_c = getTrailingHunkSeparatorLayout({
        type: hunkSeparators,
        metrics: this.metrics
      })) == null ? void 0 : _c.totalHeight) ?? 0 : 0;
      const trailingExpandedCount = trailingRegion != null ? trailingRegion.fromStart + trailingRegion.fromEnd : 0;
      const hunkBodyRowCount = diffStyle === "split" ? hunk.splitLineCount : hunk.unifiedLineCount;
      const hunkBodyStartLineIndex = diffStyle === "split" ? hunk.splitLineStart : hunk.unifiedLineStart;
      processRows({
        rowCount: hunkBodyRowCount,
        startLineIndex: hunkBodyStartLineIndex,
        preSeparatorHeight: pendingLeadingSeparatorHeight,
        postSeparatorHeight: trailingExpandedCount === 0 ? trailingSeparatorHeight : 0,
        metadataOffsets: getHunkMetadataOffsets({
          diffStyle,
          hunk,
          rowCount: hunkBodyRowCount
        })
      });
      if (trailingRegion != null && trailingExpandedCount > 0) processRows({
        rowCount: trailingExpandedCount,
        startLineIndex: hunkBodyStartLineIndex + hunkBodyRowCount,
        postSeparatorHeight: trailingSeparatorHeight
      });
    }
    this.cache.totalLines = renderedLineIndex;
  }
  getLayoutCheckpointBeforeLineIndex(lineIndex) {
    if (lineIndex <= 0 || this.cache.checkpoints.length === 0) return;
    let low = 0;
    let high = this.cache.checkpoints.length - 1;
    let result;
    while (low <= high) {
      const mid = low + high >> 1;
      const checkpoint = this.cache.checkpoints[mid];
      if (checkpoint == null) throw new Error("VirtualizedFileDiff: invalid checkpoint index");
      if (checkpoint.lineIndex <= lineIndex) {
        result = checkpoint;
        low = mid + 1;
      } else high = mid - 1;
    }
    return result;
  }
  getLayoutCheckpointBeforeTop(top, hunkLineCount) {
    let low = 0;
    let high = this.cache.checkpoints.length - 1;
    let resultIndex = -1;
    while (low <= high) {
      const mid = low + high >> 1;
      const checkpoint = this.cache.checkpoints[mid];
      if (checkpoint == null) throw new Error("VirtualizedFileDiff: invalid checkpoint index");
      if (checkpoint.top <= top) {
        resultIndex = mid;
        low = mid + 1;
      } else high = mid - 1;
    }
    if (hunkLineCount == null) return resultIndex >= 0 ? this.cache.checkpoints[resultIndex] : void 0;
    for (let index = resultIndex; index >= 0; index--) {
      const checkpoint = this.cache.checkpoints[index];
      if (checkpoint == null) throw new Error("VirtualizedFileDiff: invalid checkpoint index");
      if (checkpoint.renderedLineIndex % hunkLineCount === 0) return checkpoint;
    }
  }
  getExpandedLineCount(fileDiff, diffStyle) {
    let count = 0;
    if (fileDiff.isPartial) {
      for (const hunk of fileDiff.hunks) count += diffStyle === "split" ? hunk.splitLineCount : hunk.unifiedLineCount;
      return count;
    }
    const { expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    const expandedHunks = expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap();
    for (const [hunkIndex, hunk] of fileDiff.hunks.entries()) {
      const hunkCount = diffStyle === "split" ? hunk.splitLineCount : hunk.unifiedLineCount;
      count += hunkCount;
      const collapsedBefore = Math.max(hunk.collapsedBefore, 0);
      const { fromStart, fromEnd, renderAll } = getExpandedRegion({
        isPartial: fileDiff.isPartial,
        rangeSize: collapsedBefore,
        expandedHunks,
        hunkIndex,
        collapsedContextThreshold
      });
      if (collapsedBefore > 0) count += renderAll ? collapsedBefore : fromStart + fromEnd;
    }
    const trailingRegion = getTrailingExpandedRegion({
      fileDiff,
      hunkIndex: fileDiff.hunks.length - 1,
      expandedHunks,
      collapsedContextThreshold,
      errorPrefix: "VirtualizedFileDiff"
    });
    if (trailingRegion != null) count += trailingRegion.fromStart + trailingRegion.fromEnd;
    return count;
  }
  getLayoutLineCount(fileDiff, diffStyle) {
    const expandedLineCount = this.getExpandedLineCount(fileDiff, diffStyle);
    const metadataLineCount = diffStyle === "split" ? fileDiff.splitLineCount : fileDiff.unifiedLineCount;
    return Math.max(expandedLineCount, metadataLineCount, fileDiff.additionLines.length, fileDiff.deletionLines.length, this.cache.totalLines);
  }
  computeRenderRangeFromWindow(fileDiff, fileTop, { top, bottom }) {
    const { disableFileHeader = false, expandUnchanged = false, collapsedContextThreshold = 1 } = this.options;
    const { hunkLineCount, lineHeight } = this.metrics;
    const diffStyle = this.getDiffStyle();
    const hunkSeparators = this.getHunkSeparatorType();
    const canHydratePartialDiff = canHydrateCollapsedContext2(fileDiff, this.options.loadDiffFiles != null);
    const fileHeight = this.height;
    let lineCount = this.getLayoutLineCount(fileDiff, diffStyle);
    const headerRegion = getVirtualFileHeaderRegion(this.metrics, disableFileHeader);
    const paddingBottom = fileDiff.hunks.length > 0 ? getVirtualFilePaddingBottom(this.metrics) : 0;
    const { fileAnnotationHeight } = this.cache;
    const codeRegionTop = headerRegion + fileAnnotationHeight;
    const codeHeight = Math.max(0, fileHeight - headerRegion - fileAnnotationHeight - paddingBottom);
    const hasFileAnnotations = this.hasFileAnnotations(fileDiff);
    const fileAnnotationTop = fileTop + headerRegion;
    const measuredFileAnnotationVisible = fileAnnotationHeight > 0 && hasFileAnnotations && fileAnnotationTop < bottom && fileAnnotationTop + fileAnnotationHeight > top;
    if (fileTop < top - fileHeight || fileTop > bottom) return {
      startingLine: 0,
      totalLines: 0,
      bufferBefore: 0,
      bufferAfter: fileHeight - headerRegion - paddingBottom
    };
    if (lineCount <= hunkLineCount || fileDiff.hunks.length === 0) return {
      startingLine: 0,
      totalLines: hunkLineCount,
      bufferBefore: 0,
      bufferAfter: 0
    };
    this.approximateLayoutCheckpoints(fileDiff);
    lineCount = this.getLayoutLineCount(fileDiff, diffStyle);
    const estimatedTargetLines = Math.ceil(Math.max(bottom - top, 0) / lineHeight);
    const totalLines = Math.ceil(estimatedTargetLines / hunkLineCount) * hunkLineCount + hunkLineCount;
    const totalHunks = totalLines / hunkLineCount;
    const overflowHunks = totalHunks;
    const hunkOffsets = [];
    const viewportCenter = (top + bottom) / 2;
    const checkpoint = this.getLayoutCheckpointBeforeTop(Math.max(0, top - fileTop - totalLines * lineHeight * 2), hunkLineCount);
    let absoluteLineTop = fileTop + ((checkpoint == null ? void 0 : checkpoint.top) ?? codeRegionTop);
    let currentLine = (checkpoint == null ? void 0 : checkpoint.renderedLineIndex) ?? 0;
    let firstVisibleHunk;
    let centerHunk;
    let overflowCounter;
    iterateOverDiff({
      diff: fileDiff,
      diffStyle,
      startingLine: (checkpoint == null ? void 0 : checkpoint.renderedLineIndex) ?? 0,
      expandedHunks: expandUnchanged ? true : this.hunksRenderer.getExpandedHunksMap(),
      collapsedContextThreshold,
      callback: ({ hunkIndex, hunk, collapsedBefore, collapsedAfter, deletionLine, additionLine }) => {
        var _a9;
        const splitLineIndex = additionLine != null ? additionLine.splitLineIndex : deletionLine.splitLineIndex;
        const unifiedLineIndex = additionLine != null ? additionLine.unifiedLineIndex : deletionLine.unifiedLineIndex;
        const hasMetadata = ((additionLine == null ? void 0 : additionLine.noEOFCR) ?? false) || ((deletionLine == null ? void 0 : deletionLine.noEOFCR) ?? false);
        const isFinalHunkRow = hunkIndex === fileDiff.hunks.length - 1 && hunk != null && (diffStyle === "split" ? splitLineIndex === hunk.splitLineStart + hunk.splitLineCount - 1 : unifiedLineIndex === hunk.unifiedLineStart + hunk.unifiedLineCount - 1);
        const gapAdjustment = ((_a9 = collapsedBefore > 0 ? getLeadingHunkSeparatorLayout({
          type: hunkSeparators,
          metrics: this.metrics,
          hunkIndex,
          hunkSpecs: hunk == null ? void 0 : hunk.hunkSpecs
        }) : void 0) == null ? void 0 : _a9.totalHeight) ?? 0;
        absoluteLineTop += gapAdjustment;
        const isAtHunkBoundary = currentLine % hunkLineCount === 0;
        const currentHunk = Math.floor(currentLine / hunkLineCount);
        if (isAtHunkBoundary) {
          hunkOffsets[currentHunk] = absoluteLineTop - (fileTop + codeRegionTop + gapAdjustment);
          if (overflowCounter != null) {
            if (overflowCounter <= 0) return true;
            overflowCounter--;
          }
        }
        const lineHeight2 = this.getLineHeight(diffStyle === "split" ? splitLineIndex : unifiedLineIndex, hasMetadata);
        if (absoluteLineTop > top - lineHeight2 && absoluteLineTop < bottom) firstVisibleHunk ?? (firstVisibleHunk = currentHunk);
        if (centerHunk == null && absoluteLineTop + lineHeight2 > viewportCenter) centerHunk = currentHunk;
        if (overflowCounter == null && absoluteLineTop >= bottom && isAtHunkBoundary) overflowCounter = overflowHunks;
        currentLine++;
        absoluteLineTop += lineHeight2;
        if (collapsedAfter > 0 || isFinalHunkRow && canHydratePartialDiff) {
          const trailingSeparator = getTrailingHunkSeparatorLayout({
            type: hunkSeparators,
            metrics: this.metrics
          });
          if (trailingSeparator != null) {
            if (absoluteLineTop < bottom && absoluteLineTop + trailingSeparator.totalHeight > top) firstVisibleHunk ?? (firstVisibleHunk = currentHunk);
            if (centerHunk == null && absoluteLineTop + trailingSeparator.totalHeight > viewportCenter) centerHunk = currentHunk;
            absoluteLineTop += trailingSeparator.totalHeight;
          }
        }
        return false;
      }
    });
    if (firstVisibleHunk == null) if (measuredFileAnnotationVisible) {
      firstVisibleHunk = 0;
      centerHunk = 0;
    } else return {
      startingLine: 0,
      totalLines: 0,
      bufferBefore: 0,
      bufferAfter: fileHeight - headerRegion - paddingBottom
    };
    centerHunk ?? (centerHunk = firstVisibleHunk);
    const idealStartHunk = Math.round(centerHunk - totalHunks / 2);
    const maxStartHunk = Math.max(0, Math.ceil(lineCount / hunkLineCount) - totalHunks);
    const startHunk = Math.max(0, Math.min(idealStartHunk, maxStartHunk));
    const startingLine = startHunk * hunkLineCount;
    const clampedTotalLines = idealStartHunk < 0 ? totalLines + idealStartHunk * hunkLineCount : totalLines;
    const codeBufferBefore = hunkOffsets[startHunk] ?? 0;
    const bufferBefore = startingLine === 0 ? 0 : fileAnnotationHeight + codeBufferBefore;
    const finalHunkIndex = startHunk + clampedTotalLines / hunkLineCount;
    const bufferAfter = finalHunkIndex < hunkOffsets.length ? codeHeight - hunkOffsets[finalHunkIndex] : codeHeight - (absoluteLineTop - fileTop - codeRegionTop);
    return {
      startingLine,
      totalLines: clampedTotalLines,
      bufferBefore,
      bufferAfter: Math.max(0, bufferAfter)
    };
  }
};
function measureFileAnnotationHeight2(codeGroups) {
  let height;
  for (const codeGroup of codeGroups) {
    if (codeGroup == null) continue;
    const content = codeGroup.children[1];
    if (!(content instanceof HTMLElement)) continue;
    for (const child of content.children) {
      if (!(child instanceof HTMLElement)) continue;
      if (child.dataset.lineAnnotation !== FILE_ANNOTATION_DOM_KEY) continue;
      height = Math.max(height ?? 0, child.getBoundingClientRect().height);
    }
  }
  return height;
}
function createHeightDeltaPrefix(heightDeltas) {
  const entries = Array.from(heightDeltas).sort((a2, b3) => a2[0] - b3[0]);
  const lineIndexes = [];
  const prefixTotals = [0];
  let total = 0;
  for (const [lineIndex, delta] of entries) {
    lineIndexes.push(lineIndex);
    total += delta;
    prefixTotals.push(total);
  }
  return {
    lineIndexes,
    prefixTotals
  };
}
function sumHeightDeltas({ lineIndexes, prefixTotals }, startLineIndex, endLineIndex) {
  if (startLineIndex >= endLineIndex || lineIndexes.length === 0) return 0;
  const start = lowerBound(lineIndexes, startLineIndex);
  return (prefixTotals[lowerBound(lineIndexes, endLineIndex)] ?? 0) - (prefixTotals[start] ?? 0);
}
function lowerBound(values, target) {
  let low = 0;
  let high = values.length;
  while (low < high) {
    const mid = low + high >> 1;
    const value = values[mid];
    if (value == null) throw new Error("VirtualizedFileDiff: invalid prefix index");
    if (value < target) low = mid + 1;
    else high = mid;
  }
  return low;
}
function getNextCheckpointIndex(renderedLineIndex) {
  return Math.ceil(renderedLineIndex / VIRTUALIZED_FILE_DIFF_LAYOUT_CHECKPOINT_INTERVAL) * VIRTUALIZED_FILE_DIFF_LAYOUT_CHECKPOINT_INTERVAL;
}
function countMetadataOffsetsBefore(metadataOffsets, offset) {
  let count = 0;
  for (const metadataOffset of metadataOffsets) if (metadataOffset < offset) count++;
  return count;
}
function getHunkMetadataOffsets({ diffStyle, hunk, rowCount }) {
  if (rowCount <= 0 || !hunk.noEOFCRAdditions && !hunk.noEOFCRDeletions) return [];
  const lastContent = hunk.hunkContent.at(-1);
  if (lastContent == null) return [];
  if (lastContent.type === "context") return [rowCount - 1];
  const splitCount = Math.max(lastContent.deletions, lastContent.additions);
  const unifiedCount = lastContent.deletions + lastContent.additions;
  if (diffStyle === "split") return splitCount > 0 && (hunk.noEOFCRAdditions || hunk.noEOFCRDeletions) ? [rowCount - 1] : [];
  const offsets = [];
  const contentStartOffset = rowCount - unifiedCount;
  if (lastContent.deletions > 0 && hunk.noEOFCRDeletions) offsets.push(contentStartOffset + lastContent.deletions - 1);
  if (lastContent.additions > 0 && hunk.noEOFCRAdditions) offsets.push(rowCount - 1);
  return offsets;
}
function hasDiffLayoutOptionChanged(previousOptions, nextOptions) {
  return (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.collapsed ?? false) !== (nextOptions.collapsed ?? false) || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || Boolean(previousOptions.loadDiffFiles) !== Boolean(nextOptions.loadDiffFiles) || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS;
}
function hasDiffEstimateOptionChanged(previousOptions, nextOptions) {
  return (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || Boolean(previousOptions.loadDiffFiles) !== Boolean(nextOptions.loadDiffFiles) || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
}
function canHydrateCollapsedContext2(fileDiff, hasFileLoader) {
  return fileDiff.isPartial && hasFileLoader && (fileDiff.type === "change" || fileDiff.type === "rename-changed");
}
function getOptionHunkSeparatorType(hunkSeparators) {
  return typeof hunkSeparators === "function" ? "custom" : hunkSeparators ?? "line-info";
}
function areOptionalFilesEqual2(fileA, fileB) {
  if (fileA == null || fileB == null) return fileA == null && fileB == null;
  return areFilesEqual(fileA, fileB);
}
function parseLineIndex(lineIndexAttr, diffStyle) {
  const [unifiedIndex, splitIndex] = lineIndexAttr.split(",").map(Number);
  return diffStyle === "split" ? splitIndex : unifiedIndex;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areManagedSnapshotsEqual.js
function areManagedSnapshotsEqual(previous, next) {
  if (previous == null || next == null) return previous === next;
  if (previous.header !== next.header || previous.footer !== next.footer) return false;
  return areRenderedItemsEqual(previous.items, next.items);
}
function areRenderedItemsEqual(previous, next) {
  if (previous == null || next == null) return previous === next;
  if (previous.length !== next.length) return false;
  for (let index = 0; index < previous.length; index++) {
    const previousItem = previous[index];
    const nextItem = next[index];
    if (previousItem == null || nextItem == null || previousItem.id !== nextItem.id || previousItem.type !== nextItem.type || previousItem.element !== nextItem.element || previousItem.instance !== nextItem.instance || previousItem.version !== nextItem.version) return false;
  }
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createCodeViewHeaderFooterHostElement.js
function createCodeViewHeaderFooterHostElement(type, container, resizeObserver) {
  const element2 = document.createElement("div");
  element2.style.display = "flow-root";
  if (type === "header") {
    element2.setAttribute(CODE_VIEW_HEADER_ATTRIBUTE, "");
    container.before(element2);
  } else {
    element2.setAttribute(CODE_VIEW_FOOTER_ATTRIBUTE, "");
    container.after(element2);
  }
  resizeObserver == null ? void 0 : resizeObserver.observe(element2);
  return element2;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/roundToDevicePixel.js
function roundToDevicePixel(value) {
  const dpr = window.devicePixelRatio ?? 1;
  return Math.round(value * dpr) / dpr;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/CodeView.js
var CODE_VIEW_DIFF_OPTION_KEYS = [
  "theme",
  "disableLineNumbers",
  "overflow",
  "themeType",
  "disableFileHeader",
  "disableVirtualizationBuffers",
  "preferredHighlighter",
  "useCSSClasses",
  "useTokenTransformer",
  "tokenizeMaxLineLength",
  "tokenizeMaxLength",
  "unsafeCSS",
  "diffStyle",
  "diffIndicators",
  "disableBackground",
  "expandUnchanged",
  "loadDiffFiles",
  "collapsedContextThreshold",
  "lineDiffType",
  "maxLineDiffLength",
  "expansionLineCount",
  "lineHoverHighlight",
  "enableTokenInteractionsOnWhitespace",
  "enableGutterUtility",
  "__debugPointerEvents",
  "enableLineSelection",
  "controlledSelection",
  "disableErrorHandling"
];
var CODE_VIEW_FILE_OPTION_KEYS = [
  "theme",
  "disableLineNumbers",
  "overflow",
  "themeType",
  "disableFileHeader",
  "disableVirtualizationBuffers",
  "preferredHighlighter",
  "useCSSClasses",
  "useTokenTransformer",
  "tokenizeMaxLineLength",
  "tokenizeMaxLength",
  "unsafeCSS",
  "lineHoverHighlight",
  "enableTokenInteractionsOnWhitespace",
  "enableGutterUtility",
  "__debugPointerEvents",
  "enableLineSelection",
  "controlledSelection",
  "disableErrorHandling"
];
var CODE_VIEW_SHARED_CALLBACK_KEYS = [
  "renderCustomHeader",
  "renderHeaderPrefix",
  "renderHeaderFilenameSuffix",
  "renderHeaderMetadata",
  "renderAnnotation",
  "renderGutterUtility",
  "onPostRender",
  "onGutterUtilityClick",
  "onLineClick",
  "onLineNumberClick",
  "onLineEnter",
  "onLineLeave",
  "onTokenClick",
  "onTokenEnter",
  "onTokenLeave"
];
var CODE_VIEW_SELECTION_CALLBACK_KEYS = [
  "onLineSelected",
  "onLineSelectionStart",
  "onLineSelectionChange",
  "onLineSelectionEnd"
];
var CODE_VIEW_ITEM_OPTIONS_STATE = Symbol("CodeView.itemOptionsState");
function defineOptionsState(options, state) {
  Object.defineProperty(options, CODE_VIEW_ITEM_OPTIONS_STATE, {
    configurable: false,
    enumerable: false,
    value: state
  });
}
function getItemOptionsState(options) {
  return options[CODE_VIEW_ITEM_OPTIONS_STATE];
}
function defineItemOption(target, key2, get) {
  Object.defineProperty(target, key2, {
    configurable: false,
    enumerable: true,
    get() {
      return get(this);
    }
  });
}
var DEFAULT_SCROLL_INTERACTION_RESTORE_DELAY_MS = 120;
var SUB_PIXEL_TOLERANCE = 1;
var SCROLLING_CODE_OVERFLOW_FIX_VARIABLE = "--diffs-overflow-override";
var SCROLL_REBASE_CONTAINER_HEIGHT = 12e6;
var SCROLL_REBASE_TRIGGER_TOP = 1e6;
var SCROLL_REBASE_TARGET_TOP = 2e6;
var SCROLL_REBASE_TARGET_BOTTOM = SCROLL_REBASE_CONTAINER_HEIGHT - SCROLL_REBASE_TARGET_TOP;
var SCROLL_REBASE_THRESHOLD = SCROLL_REBASE_CONTAINER_HEIGHT - SCROLL_REBASE_TRIGGER_TOP;
function assertEditorFactory(createEditor) {
  if (createEditor == null) throw new Error("CodeView: createEditor is required for items with edit: true");
}
var MOBILE_SAFARI = (() => {
  const { navigator: navigator2 } = globalThis;
  const userAgent = navigator2.userAgent;
  const isIOS = /iP(?:hone|ad|od)/.test(userAgent);
  const isIPadOS = navigator2.platform === "MacIntel" && navigator2.maxTouchPoints > 1;
  return (isIOS || isIPadOS) && /AppleWebKit/.test(userAgent) && /Safari/.test(userAgent) && !/(CriOS|FxiOS|EdgiOS|OPiOS)/.test(userAgent);
})();
var _a7;
var CodeView = (_a7 = class {
  constructor(options = { theme: DEFAULT_THEMES }, workerManager, isContainerManaged = false) {
    __publicField(this, "type", "advanced");
    __publicField(this, "config", {
      overscrollSize: 200,
      intersectionObserverMargin: 0,
      resizeDebugging: false
    });
    __publicField(this, "items", []);
    __publicField(this, "idToItem", /* @__PURE__ */ new Map());
    __publicField(this, "selectedLines", null);
    __publicField(this, "itemEditors", /* @__PURE__ */ new Map());
    __publicField(this, "attachedEditors", /* @__PURE__ */ new Set());
    __publicField(this, "instanceToItem", /* @__PURE__ */ new Map());
    __publicField(this, "layoutDirtyIndex");
    __publicField(this, "pendingLayoutReset");
    __publicField(this, "renderOptionsRevision", 0);
    __publicField(this, "slotCoordinator");
    __publicField(this, "slotSnapshot");
    __publicField(this, "scrollListeners", /* @__PURE__ */ new Set());
    __publicField(this, "scrollHeight", 0);
    __publicField(this, "containerHeight", -1);
    __publicField(this, "scrollTop", 0);
    __publicField(this, "scrollPageOffset", 0);
    __publicField(this, "scrollDirty", true);
    __publicField(this, "scrollInteractionFixTimer");
    __publicField(this, "pointerEventsDisabled", false);
    __publicField(this, "codeOverflowFix", false);
    __publicField(this, "height", 0);
    __publicField(this, "heightDirty", true);
    __publicField(this, "windowSpecs", {
      top: 0,
      bottom: 0
    });
    __publicField(this, "renderState", {
      scrollTop: -1,
      firstIndex: -1,
      lastIndex: -1,
      stickyHeight: 0,
      stickyTop: -1,
      stickyBottom: -1
    });
    __publicField(this, "itemMetricsCache", DEFAULT_CODE_VIEW_FILE_METRICS);
    __publicField(this, "fileOptionsPrototype");
    __publicField(this, "diffOptionsPrototype");
    __publicField(this, "pendingScrollTarget");
    __publicField(this, "pendingLayoutAnchor");
    __publicField(this, "shouldFixContainerFocus", false);
    __publicField(this, "scrollAnimation");
    __publicField(this, "root");
    __publicField(this, "resizeObserver");
    __publicField(this, "container", document.createElement("div"));
    __publicField(this, "stickyContainer", document.createElement("div"));
    __publicField(this, "stickyOffset", document.createElement("div"));
    __publicField(this, "header", {
      element: void 0,
      render: void 0,
      height: 0
    });
    __publicField(this, "footer", {
      element: void 0,
      render: void 0,
      height: 0
    });
    __publicField(this, "elementPool", []);
    __publicField(this, "elementPoolVersion", 0);
    __publicField(this, "elementPoolTracker", /* @__PURE__ */ new WeakMap());
    __publicField(this, "pendingElementPool", []);
    __publicField(this, "options");
    __publicField(this, "workerManager");
    __publicField(this, "isReadySubscription");
    __publicField(this, "isContainerManaged");
    __publicField(this, "restoreScrollInteractions", () => {
      this.clearScrollInteractionTimer();
      if (this.pointerEventsDisabled) {
        this.stickyContainer.style.removeProperty("pointer-events");
        this.pointerEventsDisabled = false;
      }
      if (this.codeOverflowFix) {
        this.stickyContainer.style.setProperty(SCROLLING_CODE_OVERFLOW_FIX_VARIABLE, "auto");
        this.codeOverflowFix = false;
      }
    });
    __publicField(this, "computeRenderRangeAndEmit", (timestamp = performance.now()) => {
      if (_a7.__STOP || this.container == null) return;
      if (!this.isReady()) return;
      const viewportHeight = this.getHeight();
      const initialScrollTop = this.getScrollTop();
      let scrollTopAfterLayout = initialScrollTop;
      let computeScrollCorrection = this.pendingLayoutAnchor != null;
      let scrollAnchor = this.getScrollAnchor(scrollTopAfterLayout);
      if (this.layoutDirtyIndex != null) {
        this.recomputeLayout(this.layoutDirtyIndex, this.pendingLayoutReset);
        this.layoutDirtyIndex = void 0;
        this.pendingLayoutReset = void 0;
        computeScrollCorrection = true;
      }
      if (computeScrollCorrection && scrollAnchor != null) {
        const anchoredScrollTopAfterLayout = this.resolveAnchoredScrollTop(scrollAnchor);
        if (anchoredScrollTopAfterLayout != null) {
          const layoutAnchorDelta = anchoredScrollTopAfterLayout - scrollTopAfterLayout;
          scrollTopAfterLayout = anchoredScrollTopAfterLayout;
          if (this.scrollAnimation != null) this.scrollAnimation.position += layoutAnchorDelta;
        }
      }
      if (computeScrollCorrection) {
        scrollTopAfterLayout = this.clampScrollTop(scrollTopAfterLayout);
        this.syncContainerHeight();
      }
      const targetScrollTop = this.computeTargetScrollTopForFrame(scrollTopAfterLayout, timestamp);
      const fitPerfectly = !computeScrollCorrection && (this.renderState.scrollTop === -1 || Math.abs(targetScrollTop - this.renderState.scrollTop) > viewportHeight + this.config.overscrollSize * 2);
      if (fitPerfectly) scrollAnchor = void 0;
      this.windowSpecs = createWindowFromScrollPosition({
        scrollTop: targetScrollTop - this.header.height,
        height: viewportHeight,
        scrollHeight: this.getScrollHeight(),
        fitPerfectly,
        fitPerfectlyOverscroll: this.getFitPerfectlyOverscroll(),
        overscrollSize: this.config.overscrollSize
      });
      let syncedScrollTop = initialScrollTop;
      if (this.pendingScrollTarget != null && targetScrollTop !== syncedScrollTop || this.needsScrollPageUpdate(targetScrollTop)) {
        this.applyScrollFix(targetScrollTop, syncedScrollTop, this.windowSpecs);
        syncedScrollTop = targetScrollTop;
      }
      const { top, bottom } = this.windowSpecs;
      const { firstIndex, lastIndex } = this.renderState;
      if (firstIndex >= 0) for (let index = firstIndex; index <= lastIndex; index++) {
        const item = this.items[index];
        if (item == null) throw new Error(`CodeView.computeRenderRangeAndEmit: No item at index: ${index}`);
        if (!(item.top > top - item.height && item.top <= bottom)) this.releaseRenderedItem(item);
      }
      const hostsChanged = this.reconcileHeaderFooterHosts();
      let prevElement;
      const updatedItems = /* @__PURE__ */ new Set();
      const startingIndex = this.findFirstVisibleIndex(top);
      const lastRenderedIndex = this.findLastVisibleIndex(bottom);
      for (let itemIndex = startingIndex; itemIndex <= lastRenderedIndex; itemIndex++) {
        const item = this.items[itemIndex];
        if (item == null) throw new Error(`CodeView.computeRenderRangeAndEmit: missing item`);
        const { instance: instance2 } = item;
        if (item.element == null) {
          item.element = this.acquireElement();
          syncRenderedItemOrder(this.stickyContainer, item.element, prevElement);
          instance2.virtualizedSetup();
          if (renderItem(item, item.element)) {
            item.renderedOptionsRevision = this.renderOptionsRevision;
            updatedItems.add(item);
          }
          prevElement = item.element;
        } else {
          syncRenderedItemOrder(this.stickyContainer, item.element, prevElement);
          if (renderItem(item, void 0, item.renderedOptionsRevision !== this.renderOptionsRevision)) {
            item.renderedOptionsRevision = this.renderOptionsRevision;
            updatedItems.add(item);
          }
          prevElement = item.element;
        }
        if (item.item.edit === true) this.attachItemEditor(item);
      }
      this.renderState.firstIndex = startingIndex <= lastRenderedIndex ? startingIndex : -1;
      this.renderState.lastIndex = lastRenderedIndex;
      this.flushSlotCoordinator();
      this.flushManagers(updatedItems);
      if (hostsChanged) this.measureMountedHosts();
      this.reconcileRenderedItems(updatedItems);
      this.syncContainerHeight();
      this.updateStickyPositioning();
      const anchoredScrollTopAfterRender = scrollAnchor != null ? this.resolveAnchoredScrollTop(scrollAnchor) : void 0;
      if (scrollAnchor === this.pendingLayoutAnchor) this.pendingLayoutAnchor = void 0;
      const postRenderAnchorDelta = anchoredScrollTopAfterRender != null ? anchoredScrollTopAfterRender - scrollTopAfterLayout : 0;
      let postRenderScrollTop = targetScrollTop;
      let shouldCheckPendingTargetSettled = false;
      if (this.pendingScrollTarget != null) {
        const pendingTargetScrollTop = this.advanceScrollAnimation(timestamp, postRenderAnchorDelta);
        if (pendingTargetScrollTop != null) {
          postRenderScrollTop = pendingTargetScrollTop;
          shouldCheckPendingTargetSettled = true;
        } else postRenderScrollTop = scrollTopAfterLayout;
      } else postRenderScrollTop = anchoredScrollTopAfterRender ?? targetScrollTop;
      if (postRenderScrollTop !== syncedScrollTop) {
        this.applyScrollFix(postRenderScrollTop, syncedScrollTop, this.windowSpecs);
        syncedScrollTop = postRenderScrollTop;
      }
      if (shouldCheckPendingTargetSettled && this.pendingScrollTarget != null && this.isPendingTargetSettled(this.pendingScrollTarget)) {
        this.pendingScrollTarget = void 0;
        this.scrollAnimation = void 0;
      }
      this.renderState.scrollTop = roundToDevicePixel(syncedScrollTop);
      this.updateStickyPositioning();
      this.validateStickyContainerHeight();
      this.fixContainerFocus();
      if (fitPerfectly || this.scrollAnimation != null) this.render();
    });
    __publicField(this, "handleScroll", () => {
      if (_a7.__STOP) return;
      this.suspendScrollInteractions();
      this.scrollDirty = true;
      this.notifyScroll();
      this.render();
    });
    __publicField(this, "clearPendingScroll", () => {
      this.pendingScrollTarget = void 0;
      this.pendingLayoutAnchor = void 0;
      this.scrollAnimation = void 0;
    });
    __publicField(this, "handleResize", (entries) => {
      let shouldRender = false;
      for (const entry of entries) if (entry.target === this.stickyContainer) {
        const blockSize = entry.borderBoxSize[0].blockSize;
        if (Math.abs(blockSize - this.renderState.stickyHeight) >= SUB_PIXEL_TOLERANCE) {
          const currentScrollTop = this.getScrollTop();
          const anchor = this.getScrollAnchor(currentScrollTop);
          this.reconcileRenderedItems();
          this.updateStickyPositioning();
          const anchoredScrollTop = anchor != null ? this.resolveAnchoredScrollTop(anchor) : void 0;
          if (anchoredScrollTop != null) {
            const resizeAnchorDelta = anchoredScrollTop - currentScrollTop;
            this.applyScrollFix(anchoredScrollTop, currentScrollTop, this.windowSpecs);
            if (this.scrollAnimation != null) this.scrollAnimation.position += resizeAnchorDelta;
          }
          if (this.pendingScrollTarget != null && this.isPendingTargetSettled(this.pendingScrollTarget)) {
            this.pendingScrollTarget = void 0;
            this.scrollAnimation = void 0;
          }
          shouldRender = true;
        }
      } else if (entry.target === this.header.element || entry.target === this.footer.element) {
        const host = entry.target === this.header.element ? this.header : this.footer;
        const blockSize = entry.borderBoxSize[0].blockSize;
        if (blockSize !== host.height) {
          const currentScrollTop = this.getScrollTop();
          const anchor = this.getScrollAnchor(currentScrollTop);
          this.setHostHeight(host, blockSize);
          const anchoredScrollTop = anchor != null ? this.resolveAnchoredScrollTop(anchor) : void 0;
          if (anchoredScrollTop != null) {
            const resizeAnchorDelta = anchoredScrollTop - currentScrollTop;
            this.applyScrollFix(anchoredScrollTop, currentScrollTop, this.windowSpecs);
            if (this.scrollAnimation != null) this.scrollAnimation.position += resizeAnchorDelta;
          }
          if (this.pendingScrollTarget != null && this.isPendingTargetSettled(this.pendingScrollTarget)) {
            this.pendingScrollTarget = void 0;
            this.scrollAnimation = void 0;
          }
          shouldRender = true;
        }
      } else {
        this.scrollDirty = true;
        this.heightDirty = true;
        shouldRender = true;
      }
      if (shouldRender) this.render(true);
    });
    this.options = options;
    this.computeMetricsCache(options.itemMetrics);
    this.fileOptionsPrototype = this.createFileOptionsPrototype();
    this.diffOptionsPrototype = this.createDiffOptionsPrototype();
    this.workerManager = workerManager;
    this.isContainerManaged = isContainerManaged;
    this.stickyOffset.style.contain = "layout size";
    this.stickyContainer.style.position = "sticky";
    this.stickyContainer.style.width = "100%";
    this.stickyContainer.style.contain = "layout style inline-size";
    this.stickyContainer.style.isolation = "isolate";
    this.stickyContainer.style.display = "flex";
    this.stickyContainer.style.flexDirection = "column";
  }
  getLayout() {
    return this.options.layout ?? DEFAULT_CODE_VIEW_LAYOUT;
  }
  getItemTopOffset() {
    return this.getLayout().paddingTop + this.header.height;
  }
  computeMetricsCache(itemMetrics) {
    this.itemMetricsCache = {
      hunkLineCount: (itemMetrics == null ? void 0 : itemMetrics.hunkLineCount) ?? DEFAULT_CODE_VIEW_FILE_METRICS.hunkLineCount,
      lineHeight: (itemMetrics == null ? void 0 : itemMetrics.lineHeight) ?? DEFAULT_CODE_VIEW_FILE_METRICS.lineHeight,
      diffHeaderHeight: (itemMetrics == null ? void 0 : itemMetrics.diffHeaderHeight) ?? DEFAULT_CODE_VIEW_FILE_METRICS.diffHeaderHeight,
      hunkSeparatorHeight: itemMetrics == null ? void 0 : itemMetrics.hunkSeparatorHeight,
      spacing: (itemMetrics == null ? void 0 : itemMetrics.spacing) ?? DEFAULT_CODE_VIEW_FILE_METRICS.spacing,
      paddingTop: itemMetrics == null ? void 0 : itemMetrics.paddingTop,
      paddingBottom: itemMetrics == null ? void 0 : itemMetrics.paddingBottom
    };
    return this.itemMetricsCache;
  }
  getSmoothScrollSettings() {
    return this.options.smoothScrollSettings ?? DEFAULT_SMOOTH_SCROLL_SETTINGS;
  }
  shouldDisablePointerEvents() {
    return this.options.pointerEventsOnScroll !== true;
  }
  shouldValidateItemHeights() {
    return DIFFS_DEVELOPMENT_BUILD && this.options.__devOnlyValidateItemHeights === true;
  }
  validateRenderedItemHeight(item) {
    if (!this.shouldValidateItemHeights() || item.element == null) return;
    const stickySpecs = item.instance.getAdvancedStickySpecs();
    if (stickySpecs == null) return;
    const expectedHeight = stickySpecs.height;
    const actualHeight = item.element.getBoundingClientRect().height;
    if (expectedHeight === actualHeight) return;
    console.error("CodeView: reconciled item height does not match DOM height", {
      id: item.item.id,
      type: item.type,
      index: item.index,
      version: item.version,
      expectedHeight,
      actualHeight,
      delta: actualHeight - expectedHeight,
      stickyTopOffset: stickySpecs.topOffset,
      virtualizedHeight: item.instance.getVirtualizedHeight(),
      top: item.top,
      scrollTop: this.getScrollTop(),
      windowSpecs: { ...this.windowSpecs },
      element: item.element,
      instance: item.instance
    });
  }
  validateStickyContainerHeight() {
    var _a9, _b;
    if (!this.shouldValidateItemHeights()) return;
    const { firstIndex, lastIndex, stickyHeight, stickyTop, stickyBottom } = this.renderState;
    if (firstIndex === -1 || lastIndex === -1) return;
    const actualHeight = this.stickyContainer.getBoundingClientRect().height;
    if (Math.abs(actualHeight - stickyHeight) < SUB_PIXEL_TOLERANCE) return;
    console.error("CodeView: sticky container height does not match computed layout", {
      computedStickyHeight: stickyHeight,
      actualStickyHeight: actualHeight,
      delta: actualHeight - stickyHeight,
      stickyTop,
      stickyBottom,
      firstIndex,
      lastIndex,
      firstStickySpecs: (_a9 = this.items[firstIndex]) == null ? void 0 : _a9.instance.getAdvancedStickySpecs(),
      lastStickySpecs: (_b = this.items[lastIndex]) == null ? void 0 : _b.instance.getAdvancedStickySpecs(),
      scrollTop: this.getScrollTop(),
      scrollPageOffset: this.scrollPageOffset,
      windowSpecs: { ...this.windowSpecs },
      stickyContainer: this.stickyContainer
    });
  }
  clearScrollInteractionTimer() {
    if (this.scrollInteractionFixTimer != null) {
      clearTimeout(this.scrollInteractionFixTimer);
      this.scrollInteractionFixTimer = void 0;
    }
  }
  suspendScrollInteractions() {
    this.clearScrollInteractionTimer();
    if (this.shouldDisablePointerEvents() && !this.pointerEventsDisabled) {
      this.stickyContainer.style.pointerEvents = "none";
      this.pointerEventsDisabled = true;
    }
    if (MOBILE_SAFARI && !this.codeOverflowFix) {
      this.stickyContainer.style.setProperty(SCROLLING_CODE_OVERFLOW_FIX_VARIABLE, "hidden");
      this.codeOverflowFix = true;
    }
    this.scrollInteractionFixTimer = setTimeout(this.restoreScrollInteractions, DEFAULT_SCROLL_INTERACTION_RESTORE_DELAY_MS);
  }
  syncLayout() {
    var _a9, _b;
    const { gap, paddingBottom, paddingTop } = this.getLayout();
    this.stickyContainer.style.gap = `${gap}px`;
    (_a9 = this.container) == null ? void 0 : _a9.style.setProperty("margin-top", `${paddingTop}px`);
    (_b = this.container) == null ? void 0 : _b.style.setProperty("margin-bottom", `${paddingBottom}px`);
  }
  reconcileHeaderFooterHosts() {
    const headerChanged = this.reconcileHost("header");
    const footerChanged = this.reconcileHost("footer");
    return headerChanged || footerChanged;
  }
  reconcileHost(type) {
    var _a9;
    const { root: root2, container } = this;
    if (root2 == null || container == null) return false;
    const host = type === "header" ? this.header : this.footer;
    const render2 = type === "header" ? this.options.renderCodeViewHeader : this.options.renderCodeViewFooter;
    if (render2 == null) {
      if (host.element == null) return false;
      (_a9 = this.resizeObserver) == null ? void 0 : _a9.unobserve(host.element);
      host.element.remove();
      host.element = void 0;
      host.render = void 0;
      this.setHostHeight(host, 0);
      return false;
    }
    if (host.element != null && render2 === host.render) return false;
    const element2 = host.element ?? createCodeViewHeaderFooterHostElement(type, container, this.resizeObserver);
    host.element = element2;
    const content = render2();
    if (content != null) element2.replaceChildren(content);
    else if (!this.isContainerManaged && element2.children.length > 0) element2.textContent = "";
    host.render = render2;
    return true;
  }
  setHostHeight(host, height) {
    if (host.height === height) return;
    host.height = height;
    this.scrollDirty = true;
  }
  measureMountedHosts() {
    if (this.header.element != null) this.setHostHeight(this.header, this.header.element.getBoundingClientRect().height);
    if (this.footer.element != null) this.setHostHeight(this.footer, this.footer.element.getBoundingClientRect().height);
  }
  setup(root2) {
    var _a9;
    if (this.root != null) throw new Error("CodeView.setup: already setup");
    (_a9 = this.workerManager) == null ? void 0 : _a9.subscribeToThemeChanges(this);
    this.root = root2;
    this.root.style.overflowAnchor = "none";
    if (!this.root.hasAttribute("tabindex")) this.root.tabIndex = -1;
    this.container ?? (this.container = document.createElement("div"));
    this.container.style.contain = "layout style";
    this.syncLayout();
    this.container.appendChild(this.stickyOffset);
    this.container.appendChild(this.stickyContainer);
    this.root.appendChild(this.container);
    this.scrollDirty = true;
    this.heightDirty = true;
    this.resizeObserver = new ResizeObserver(this.handleResize);
    this.resizeObserver.observe(this.stickyContainer);
    this.root.addEventListener("scroll", this.handleScroll, { passive: true });
    this.root.addEventListener("wheel", this.clearPendingScroll, { passive: true });
    this.root.addEventListener("touchstart", this.clearPendingScroll, { passive: true });
    this.root.addEventListener("pointerdown", this.clearPendingScroll, { passive: true });
    this.root.addEventListener("keydown", this.clearPendingScroll, { passive: true });
    this.resizeObserver.observe(this.root);
    this.render(true);
    window.__INSTANCE = this;
    window.__TOGGLE = () => {
      if (_a7.__STOP) {
        _a7.__STOP = false;
        this.scrollTo({
          type: "position",
          position: _a7.__lastScrollPosition,
          behavior: "instant"
        });
      } else {
        _a7.__lastScrollPosition = this.getScrollTop();
        _a7.__STOP = true;
      }
    };
  }
  reset() {
    var _a9;
    dequeueRender(this.computeRenderRangeAndEmit);
    this.clearReadySubscription();
    this.restoreScrollInteractions();
    this.cleanAllRenderedItems();
    for (const record of this.itemEditors.values()) record.editor.cleanUp();
    this.itemEditors.clear();
    this.attachedEditors.clear();
    this.selectedLines = null;
    this.items.length = 0;
    this.idToItem.clear();
    this.instanceToItem.clear();
    this.layoutDirtyIndex = void 0;
    this.pendingLayoutReset = void 0;
    this.stickyContainer.textContent = "";
    this.stickyOffset.style.height = "";
    (_a9 = this.container) == null ? void 0 : _a9.style.removeProperty("height");
    this.containerHeight = -1;
    this.windowSpecs = {
      top: 0,
      bottom: 0
    };
    this.pendingLayoutAnchor = void 0;
    this.shouldFixContainerFocus = false;
    this.height = 0;
    this.scrollTop = 0;
    this.scrollPageOffset = 0;
    this.scrollHeight = 0;
    this.scrollDirty = true;
    this.heightDirty = true;
    this.resetRenderState();
    if (!this.isContainerManaged) this.flushSlotCoordinator();
  }
  cleanUp() {
    var _a9, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
    this.reset();
    this.clearElementPool();
    this.restoreScrollInteractions();
    (_a9 = this.workerManager) == null ? void 0 : _a9.unsubscribeToThemeChanges(this);
    (_b = this.resizeObserver) == null ? void 0 : _b.disconnect();
    this.resizeObserver = void 0;
    (_c = this.root) == null ? void 0 : _c.removeEventListener("scroll", this.handleScroll);
    (_d = this.root) == null ? void 0 : _d.removeEventListener("wheel", this.clearPendingScroll);
    (_e = this.root) == null ? void 0 : _e.removeEventListener("touchstart", this.clearPendingScroll);
    (_f = this.root) == null ? void 0 : _f.removeEventListener("pointerdown", this.clearPendingScroll);
    (_g = this.root) == null ? void 0 : _g.removeEventListener("keydown", this.clearPendingScroll);
    (_h = this.root) == null ? void 0 : _h.style.removeProperty("overflow-anchor");
    (_i = this.container) == null ? void 0 : _i.remove();
    this.stickyOffset.remove();
    this.stickyContainer.remove();
    this.stickyContainer.textContent = "";
    (_j = this.header.element) == null ? void 0 : _j.remove();
    this.header.element = void 0;
    this.header.render = void 0;
    this.header.height = 0;
    (_k = this.footer.element) == null ? void 0 : _k.remove();
    this.footer.element = void 0;
    this.footer.render = void 0;
    this.footer.height = 0;
    this.root = void 0;
    this.container = void 0;
  }
  cleanAllRenderedItems() {
    if (this.renderState.firstIndex === -1) return;
    for (let index = this.renderState.firstIndex; index <= this.renderState.lastIndex; index++) {
      const item = this.items[index];
      if (item == null) throw new Error(`CodeView.cleanAllRenderedItems: Item does not exist at index: ${index}`);
      this.releaseRenderedItem(item);
    }
  }
  primeScrollTarget(target) {
    if (target.type === "position") return;
    const item = this.idToItem.get(target.id);
    if (item == null) return;
    item.instance.primeHighlightCache();
  }
  getElementPoolLimit() {
    const viewportSize = this.getHeight() + this.config.overscrollSize * 2;
    const { diffHeaderHeight } = this.itemMetricsCache;
    return Math.max(8, Math.ceil(viewportSize / Math.max(diffHeaderHeight, 10)) + 1) * (this.isContainerManaged ? 2 : 1);
  }
  acquireElement() {
    this.promotePendingPooledElements();
    let element2 = this.elementPool.pop();
    while (element2 != null && !this.isElementPoolGenerationCurrent(element2)) element2 = this.elementPool.pop();
    element2 ?? (element2 = document.createElement(DIFFS_TAG_NAME));
    this.markElementPoolGenerationCurrent(element2);
    return element2;
  }
  releaseRenderedItem(item) {
    const { element: element2 } = item;
    if (element2 != null && this.renderedItemOwnsFocus(element2)) this.shouldFixContainerFocus = true;
    item.instance.cleanUp(true);
    this.attachedEditors.delete(item.item.id);
    item.element = void 0;
    if (element2 == null) return;
    element2.remove();
    this.cleanElement(element2);
    this.queueElementForPool(element2);
  }
  renderedItemOwnsFocus(element2) {
    var _a9;
    const { activeElement } = document;
    return activeElement === element2 || element2.contains(activeElement) || ((_a9 = element2.shadowRoot) == null ? void 0 : _a9.activeElement) != null;
  }
  fixContainerFocus() {
    var _a9;
    if (this.shouldFixContainerFocus) {
      this.shouldFixContainerFocus = false;
      (_a9 = this.root) == null ? void 0 : _a9.focus({ preventScroll: true });
    }
  }
  cleanElement(element2) {
    const { shadowRoot } = element2;
    if (shadowRoot != null) {
      for (const child of Array.from(shadowRoot.children)) if (!isPooledShadowChild(child)) child.remove();
    }
    if (!this.isContainerManaged) element2.replaceChildren();
  }
  queueElementForPool(element2) {
    const poolLimit = this.getElementPoolLimit();
    if (!this.isElementPoolGenerationCurrent(element2) || this.getElementPoolSize() >= poolLimit) return;
    if (this.isElementClean(element2)) this.elementPool.push(element2);
    else this.pendingElementPool.push(element2);
  }
  promotePendingPooledElements() {
    if (this.pendingElementPool.length === 0) return;
    const { pendingElementPool: pendingElements } = this;
    this.pendingElementPool = [];
    const poolLimit = this.getElementPoolLimit();
    for (const element2 of pendingElements) if (this.isElementPoolGenerationCurrent(element2) && this.isElementClean(element2) && this.elementPool.length < poolLimit) this.elementPool.push(element2);
    else if (this.isElementPoolGenerationCurrent(element2) && this.getElementPoolSize() < poolLimit) this.pendingElementPool.push(element2);
  }
  isElementClean(element2) {
    return element2.childNodes.length === 0;
  }
  getElementPoolSize() {
    return this.elementPool.length + this.pendingElementPool.length;
  }
  clearElementPool() {
    this.elementPool.length = 0;
    this.pendingElementPool.length = 0;
  }
  invalidateElementPool() {
    this.elementPoolVersion++;
    this.clearElementPool();
  }
  markElementPoolGenerationCurrent(element2) {
    this.elementPoolTracker.set(element2, this.elementPoolVersion);
  }
  isElementPoolGenerationCurrent(element2) {
    return this.elementPoolTracker.get(element2) === this.elementPoolVersion;
  }
  resolveEffectiveScrollBehavior(target, destination) {
    if (prefersReducedMotion()) return "instant";
    if (target.behavior !== "smooth-auto") return target.behavior ?? "instant";
    return Math.abs(destination - this.getScrollTop()) <= this.getHeight() * 10 ? "smooth" : "instant";
  }
  scrollTo(target) {
    if (this.root == null) return;
    const pendingTarget = this.normalizeScrollTarget(target);
    if (pendingTarget == null) return;
    const destination = this.resolveScrollTargetTop(pendingTarget);
    if (destination == null) return;
    this.primeScrollTarget(pendingTarget);
    if (this.resolveEffectiveScrollBehavior(pendingTarget, destination) === "smooth") this.scrollAnimation ?? (this.scrollAnimation = {
      position: this.getScrollTop(),
      velocity: 0,
      lastTimestamp: performance.now()
    });
    else this.scrollAnimation = void 0;
    this.suspendScrollInteractions();
    this.pendingLayoutAnchor = void 0;
    this.pendingScrollTarget = pendingTarget;
    this.render();
  }
  setSelectedLines(selection, options) {
    this.applySelectedLines(selection, options);
  }
  getSelectedLines() {
    return this.selectedLines;
  }
  clearSelectedLines(options) {
    this.applySelectedLines(null, options);
  }
  getItem(itemId) {
    var _a9;
    return (_a9 = this.idToItem.get(itemId)) == null ? void 0 : _a9.item;
  }
  /**
  * Get the live editor for an item currently in edit mode. Use this to drive
  * editor APIs CodeView does not wrap (applyEdits, undo, setMarkers, …).
  * Returns undefined once the item leaves edit mode.
  */
  getEditor(itemId) {
    var _a9;
    return (_a9 = this.itemEditors.get(itemId)) == null ? void 0 : _a9.editor;
  }
  updateItem(input) {
    const item = this.idToItem.get(input.id);
    if (item == null) {
      console.error(`CodeView.updateItem: unknown item id "${input.id}"`);
      return false;
    }
    if (!this.syncItemRecord(item, input)) return false;
    this.markItemLayoutDirty(item);
    this.scrollDirty = true;
    this.render();
    this.syncItemEditors();
    this.syncSelection();
    return true;
  }
  updateItemId(oldId, newId) {
    var _a9, _b, _c;
    if (oldId === newId) return true;
    const item = this.idToItem.get(oldId);
    if (item == null) {
      console.error(`CodeView.updateItemId: unknown item id "${oldId}"`);
      return false;
    }
    if (this.idToItem.has(newId)) {
      console.error(`CodeView.updateItemId: duplicate item id "${newId}"`);
      return false;
    }
    this.idToItem.delete(oldId);
    item.item.id = newId;
    this.idToItem.set(newId, item);
    this.updateItemOptionsId(item.instance.options, newId);
    if (((_a9 = this.selectedLines) == null ? void 0 : _a9.id) === oldId) {
      this.selectedLines = {
        ...this.selectedLines,
        id: newId
      };
      (_c = (_b = this.options).onSelectedLinesChange) == null ? void 0 : _c.call(_b, this.selectedLines);
    }
    const editorRecord = this.itemEditors.get(oldId);
    if (editorRecord != null) {
      editorRecord.state.id = newId;
      this.itemEditors.delete(oldId);
      this.itemEditors.set(newId, editorRecord);
      if (this.attachedEditors.delete(oldId)) this.attachedEditors.add(newId);
    }
    this.renamePendingScrollTarget(oldId, newId);
    this.renamePendingLayoutAnchor(oldId, newId);
    this.render();
    return true;
  }
  addItem(input) {
    this.addItems([input]);
  }
  addItems(inputs) {
    this.appendItemsInternal(inputs);
    this.syncItemEditors();
    this.syncSelection();
  }
  removeItem(itemId) {
    const item = this.idToItem.get(itemId);
    if (item == null) {
      console.error(`CodeView.removeItem: unknown item id "${itemId}"`);
      return false;
    }
    const nextItems = [];
    for (const current of this.items) if (current !== item) nextItems.push(current.item);
    this.setItems(nextItems);
    return true;
  }
  setItems(items) {
    let removedItemsById;
    if (this.items.length === 0) this.appendItemsInternal(items);
    else if (!this.tryAppendItems(items)) removedItemsById = this.reconcileItems(items);
    this.syncItemEditors(removedItemsById);
    this.syncSelection();
  }
  /**
  * Append new records to the viewer while preserving existing layout state.
  * This is the shared path for imperative adds and the append-only reconcile
  * fast path, so it measures new items immediately and only triggers render
  * once at the end.
  */
  appendItemsInternal(inputs, render2 = true) {
    if (inputs.length === 0) return;
    const layout = this.getLayout();
    let nextTop = this.items.length === 0 ? 0 : this.scrollHeight + layout.gap;
    const appendedTop = nextTop;
    for (let index = 0; index < inputs.length; index++) {
      const input = inputs[index];
      if (input == null) throw new Error("CodeView.appendItemsInternal: missing input item");
      if (this.idToItem.has(input.id)) throw new Error(`CodeView.addItem: duplicate id "${input.id}"`);
      const item = this.createItem(input, this.items.length, nextTop);
      this.items.push(item);
      this.idToItem.set(item.item.id, item);
      this.instanceToItem.set(item.instance, item);
      item.height = prepareItemInstance(item);
      nextTop += item.height + layout.gap;
    }
    this.scrollHeight = nextTop - layout.gap;
    this.scrollDirty = true;
    if (render2) if (this.canSkipRenderForAppend(appendedTop)) this.syncContainerHeight();
    else this.render();
  }
  canSkipRenderForAppend(appendedTop) {
    return this.container != null && this.renderState.firstIndex !== -1 && this.pendingScrollTarget == null && this.scrollAnimation == null && this.layoutDirtyIndex == null && appendedTop > this.windowSpecs.bottom;
  }
  onThemeChange() {
    this.invalidateElementPool();
  }
  setOptions(options) {
    if (options == null) return;
    this.capturePendingLayoutAnchor();
    const { options: prevOptions } = this;
    const previousLayout = this.getLayout();
    const { itemMetricsCache: previousItemMetrics } = this;
    if (shouldClearPool(prevOptions, options)) this.invalidateElementPool();
    this.options = options;
    const nextItemMetrics = this.computeMetricsCache(options.itemMetrics);
    const itemMetricsChanged = !areObjectsEqual(previousItemMetrics, nextItemMetrics);
    const layoutChanged = !areObjectsEqual(previousLayout, this.getLayout());
    if (layoutChanged) this.syncLayout();
    const itemLayoutChanged = itemMetricsChanged || hasItemLayoutOptionChanged(prevOptions, options);
    if (itemLayoutChanged) {
      const previousReset = this.pendingLayoutReset;
      this.pendingLayoutReset = {
        metrics: itemMetricsChanged ? nextItemMetrics : previousReset == null ? void 0 : previousReset.metrics,
        resetFileLayoutCache: true,
        resetDiffLayoutCache: true,
        includeEstimatedDiffHeights: (previousReset == null ? void 0 : previousReset.includeEstimatedDiffHeights) === true || itemMetricsChanged || hasCodeViewDiffEstimateOptionChanged(prevOptions, options)
      };
    }
    if (layoutChanged || itemLayoutChanged) {
      this.markLayoutDirtyFromIndex(0);
      this.scrollDirty = true;
    }
    if (!areOptionsEqual(prevOptions, options)) this.renderOptionsRevision++;
    this.syncItemEditors();
    const headerFooterChanged = prevOptions.renderCodeViewHeader !== options.renderCodeViewHeader || prevOptions.renderCodeViewFooter !== options.renderCodeViewFooter;
    if (!this.isContainerManaged && (this.items.length > 0 || headerFooterChanged)) this.render();
  }
  capturePendingLayoutAnchor(nextItems = this.idToItem) {
    if (this.root == null || this.items.length === 0 || this.pendingScrollTarget != null) return;
    this.pendingLayoutAnchor = this.getScrollAnchor(this.getScrollTop(), nextItems);
  }
  render(immediate = false) {
    if (_a7.__STOP) return;
    if (immediate) {
      dequeueRender(this.computeRenderRangeAndEmit);
      this.computeRenderRangeAndEmit();
    } else queueRender(this.computeRenderRangeAndEmit);
  }
  isReady() {
    const { workerManager } = this;
    if (workerManager == null || workerManager.isInitialized() || workerManager.getStats().workersFailed) {
      this.clearReadySubscription();
      return true;
    }
    this.isReadySubscription ?? (this.isReadySubscription = workerManager.subscribeToStatChanges((stats) => {
      if (stats.managerState !== "initialized" && !stats.workersFailed) return;
      this.clearReadySubscription();
      this.render(true);
    }));
    if (workerManager.getStats().managerState === "waiting") workerManager.initialize().catch(() => {
    });
    return false;
  }
  clearReadySubscription() {
    if (this.isReadySubscription == null) return;
    this.isReadySubscription();
    this.isReadySubscription = void 0;
  }
  instanceChanged(instance2, layoutDirty) {
    const item = this.instanceToItem.get(instance2);
    if (item == null) throw new Error("CodeView.instanceChanged: An instance has changed that is not registered");
    if (layoutDirty) this.markItemLayoutDirty(item);
    this.render();
  }
  getWindowSpecs() {
    return this.windowSpecs;
  }
  getContainerElement() {
    return this.root;
  }
  getHeaderElement() {
    return this.header.element;
  }
  getFooterElement() {
    return this.footer.element;
  }
  getRenderedItems() {
    const { firstIndex, lastIndex } = this.renderState;
    if (firstIndex === -1 || lastIndex === -1 || lastIndex < firstIndex) return [];
    const renderedItems = [];
    for (let index = firstIndex; index <= lastIndex; index++) {
      const item = this.items[index];
      if ((item == null ? void 0 : item.element) == null) continue;
      if (item.type === "diff") renderedItems.push({
        id: item.item.id,
        type: "diff",
        item: item.item,
        version: item.version,
        element: item.element,
        instance: item.instance
      });
      else renderedItems.push({
        id: item.item.id,
        type: "file",
        item: item.item,
        version: item.version,
        element: item.element,
        instance: item.instance
      });
    }
    return renderedItems;
  }
  setSlotCoordinator(coordinator) {
    if (coordinator === this.slotCoordinator) return false;
    this.slotCoordinator = coordinator;
    this.slotSnapshot = void 0;
    return true;
  }
  getSlotSnapshot(coordinator) {
    return this.buildSlotSnapshot(coordinator);
  }
  buildSlotSnapshot(coordinator) {
    const items = getSlotItems(this.getRenderedItems(), coordinator);
    const { element: header } = this.header;
    const { element: footer } = this.footer;
    if (items == null && header == null && footer == null) return;
    return {
      items,
      header,
      footer
    };
  }
  subscribeToScroll(listener) {
    this.scrollListeners.add(listener);
    return () => {
      this.scrollListeners.delete(listener);
    };
  }
  getLocalTopForInstance(instance2) {
    const item = this.instanceToItem.get(instance2);
    if (item == null) throw new Error("CodeView.getLocalTopForInstance: unknown virtualized instance");
    return item.top;
  }
  getTopForItem(id) {
    const item = this.idToItem.get(id);
    if (item == null) return;
    return item.top + this.getItemTopOffset();
  }
  createItem(input, index, top) {
    const { itemMetricsCache: itemMetrics } = this;
    if (input.type === "diff") {
      const instance3 = new VirtualizedFileDiff(this.createDiffOptions(input.id), this, itemMetrics, this.workerManager, this.isContainerManaged);
      return {
        type: "diff",
        item: input,
        version: input.version,
        index,
        top,
        height: 0,
        element: void 0,
        renderedOptionsRevision: this.renderOptionsRevision,
        instance: instance3
      };
    }
    const instance2 = new VirtualizedFile(this.createFileOptions(input.id), this, itemMetrics, this.workerManager, this.isContainerManaged);
    return {
      type: "file",
      item: input,
      version: input.version,
      index,
      top,
      height: 0,
      element: void 0,
      renderedOptionsRevision: this.renderOptionsRevision,
      instance: instance2
    };
  }
  applySelectedLines(selection, options) {
    var _a9, _b;
    const { selectedLines: prevSelection } = this;
    if (selection == null && prevSelection == null || selection != null && (prevSelection == null ? void 0 : prevSelection.id) === selection.id && areSelectionsEqual(prevSelection.range, selection.range)) return;
    if (prevSelection != null && prevSelection.id !== (selection == null ? void 0 : selection.id)) (_a9 = this.idToItem.get(prevSelection.id)) == null ? void 0 : _a9.instance.setSelectedLines(null, { notify: false });
    this.selectedLines = selection;
    (_b = this.idToItem.get((selection == null ? void 0 : selection.id) ?? "")) == null ? void 0 : _b.instance.setSelectedLines((selection == null ? void 0 : selection.range) ?? null, options);
  }
  syncSelection() {
    var _a9, _b;
    if (this.selectedLines == null) return;
    const item = this.idToItem.get(this.selectedLines.id);
    if (item == null) {
      this.selectedLines = null;
      (_b = (_a9 = this.options).onSelectedLinesChange) == null ? void 0 : _b.call(_a9, null);
      return;
    }
    item.instance.setSelectedLines(this.selectedLines.range, { notify: false });
  }
  isItemInEditMode(item) {
    return item.item.edit === true && item.item.collapsed !== true;
  }
  /**
  * Attach (or lazily create) the editor for a mounted edit-mode item. Called
  * from the render loop so every mounted item passes through it: fresh
  * mounts, remounts after virtualization released the item, and items whose
  * edit flag was just turned on. Editors persist across unmounts, so a
  * remounted item re-attaches its existing editor and resumes the retained
  * document; the renderers keep the host's file/diff data in sync with the
  * session so the remount paints the edited text.
  */
  attachItemEditor(item) {
    const { id } = item.item;
    const { createEditor } = this.options;
    if (item.element == null || this.attachedEditors.has(id) || !this.isItemInEditMode(item)) return;
    let record = this.itemEditors.get(id);
    let createdEditor = false;
    try {
      if (record == null) {
        assertEditorFactory(createEditor);
        const state = { id };
        const editor = createEditor({ onChange: (file, lineAnnotations) => {
          var _a9, _b;
          const latest = this.idToItem.get(state.id);
          if (latest == null) return;
          state.lastChange = {
            item: latest.item,
            file,
            lineAnnotations
          };
          (_b = (_a9 = this.options).onItemEditChange) == null ? void 0 : _b.call(_a9, latest.item, file, lineAnnotations);
        } });
        if (editor == null) return;
        record = {
          editor,
          state
        };
        this.itemEditors.set(id, record);
        createdEditor = true;
      }
      record.editor.edit(item.instance);
      this.attachedEditors.add(id);
    } catch (error) {
      if (createdEditor && record != null) {
        this.itemEditors.delete(id);
        record.editor.cleanUp();
      }
      this.releaseRenderedItem(item);
      throw error;
    }
  }
  /**
  * Drop editors for items that can no longer be edited: removed, edit turned
  * off, or collapsed. Attachment happens in the render loop via
  * attachItemEditor, so this only reconciles editors CodeView is already
  * holding.
  */
  syncItemEditors(removedItems) {
    var _a9, _b;
    if (this.itemEditors.size === 0) return;
    const completions = [];
    for (const [id, record] of this.itemEditors) {
      const item = this.idToItem.get(id);
      const removedItem = removedItems == null ? void 0 : removedItems.get(id);
      if (removedItem == null && item != null && this.isItemInEditMode(item)) continue;
      record.editor.cleanUp();
      this.itemEditors.delete(id);
      this.attachedEditors.delete(id);
      const { lastChange } = record.state;
      const itemSnapshot = removedItem == null ? (item == null ? void 0 : item.item) ?? (lastChange == null ? void 0 : lastChange.item) : (lastChange == null ? void 0 : lastChange.item) ?? removedItem.item;
      if ((itemSnapshot == null ? void 0 : itemSnapshot.type) === "diff") {
        if (removedItem == null && item != null && item.type === "diff" && item.instance.completeEditSession()) {
          this.markItemLayoutDirty(item);
          this.render();
        }
        finishEditSessionForDiff(itemSnapshot.fileDiff);
      }
      if (lastChange != null) completions.push(removedItem != null || item == null ? lastChange : {
        ...lastChange,
        item: item.item
      });
    }
    for (const { item, file, lineAnnotations } of completions) (_b = (_a9 = this.options).onItemEditComplete) == null ? void 0 : _b.call(_a9, item, file, lineAnnotations);
  }
  renamePendingScrollTarget(oldId, newId) {
    const { pendingScrollTarget } = this;
    if (pendingScrollTarget == null || pendingScrollTarget.type === "position" || pendingScrollTarget.id !== oldId) return;
    this.pendingScrollTarget = {
      ...pendingScrollTarget,
      id: newId
    };
  }
  renamePendingLayoutAnchor(oldId, newId) {
    var _a9;
    if (((_a9 = this.pendingLayoutAnchor) == null ? void 0 : _a9.id) === oldId) this.pendingLayoutAnchor.id = newId;
  }
  createFileOptionsPrototype() {
    const prototype = {};
    for (const key2 of CODE_VIEW_FILE_OPTION_KEYS) defineItemOption(prototype, key2, () => this.options[key2]);
    defineItemOption(prototype, "stickyHeader", () => this.options.stickyHeaders);
    defineItemOption(prototype, "collapsed", (receiver) => {
      var _a9;
      const state = getItemOptionsState(receiver);
      if (state == null) return;
      return (_a9 = this.getItemOptions(state, "file")) == null ? void 0 : _a9.item.collapsed;
    });
    for (const key2 of CODE_VIEW_SHARED_CALLBACK_KEYS) this.defineItemSharedCallback(prototype, "file", key2);
    for (const key2 of CODE_VIEW_SELECTION_CALLBACK_KEYS) this.defineItemSelectionCallback(prototype, "file", key2);
    return prototype;
  }
  createDiffOptionsPrototype() {
    const prototype = {};
    for (const key2 of CODE_VIEW_DIFF_OPTION_KEYS) defineItemOption(prototype, key2, () => this.options[key2]);
    defineItemOption(prototype, "stickyHeader", () => this.options.stickyHeaders);
    defineItemOption(prototype, "hunkSeparators", () => this.options.hunkSeparators);
    defineItemOption(prototype, "collapsed", (receiver) => {
      var _a9;
      const state = getItemOptionsState(receiver);
      if (state == null) return;
      return (_a9 = this.getItemOptions(state, "diff")) == null ? void 0 : _a9.item.collapsed;
    });
    for (const key2 of CODE_VIEW_SHARED_CALLBACK_KEYS) this.defineItemSharedCallback(prototype, "diff", key2);
    for (const key2 of CODE_VIEW_SELECTION_CALLBACK_KEYS) this.defineItemSelectionCallback(prototype, "diff", key2);
    return prototype;
  }
  createFileOptions(id) {
    const options = Object.create(this.fileOptionsPrototype);
    defineOptionsState(options, { id });
    return options;
  }
  createDiffOptions(id) {
    const options = Object.create(this.diffOptionsPrototype);
    defineOptionsState(options, { id });
    return options;
  }
  updateItemOptionsId(options, id) {
    const state = getItemOptionsState(options);
    if (state == null) throw new Error(`CodeView.updateItemOptionsId: No valid state`);
    state.id = id;
  }
  getItemOptions(state, mode) {
    const item = this.idToItem.get(state.id);
    if (item == null || item.type !== mode) return;
    return item;
  }
  defineItemSharedCallback(options, mode, key2) {
    defineItemOption(options, key2, (receiver) => {
      if (this.options[key2] == null) return;
      const state = getItemOptionsState(receiver);
      if (state == null) return;
      const callbackCache = state.callbackCache ?? (state.callbackCache = {});
      let wrapped = callbackCache[key2];
      if (wrapped == null) {
        wrapped = ((...args) => {
          const latest = this.getItemOptions(state, mode);
          if (latest == null) return;
          const callback = this.options[key2];
          return callback == null ? void 0 : callback(...args, latest);
        });
        callbackCache[key2] = wrapped;
      }
      return wrapped;
    });
  }
  defineItemSelectionCallback(options, mode, key2) {
    defineItemOption(options, key2, (receiver) => {
      const state = getItemOptionsState(receiver);
      if (state == null) return;
      const callbackCache = state.callbackCache ?? (state.callbackCache = {});
      let wrapped = callbackCache[key2];
      if (wrapped == null) {
        wrapped = ((range2) => {
          var _a9, _b, _c;
          const latest = this.getItemOptions(state, mode);
          if (latest == null) return;
          const selection = range2 == null ? null : {
            id: latest.item.id,
            range: range2
          };
          if (this.options.controlledSelection !== true) {
            if (range2 != null || ((_a9 = this.selectedLines) == null ? void 0 : _a9.id) === latest.item.id) this.applySelectedLines(selection, { notify: false });
          }
          (_c = (_b = this.options).onSelectedLinesChange) == null ? void 0 : _c.call(_b, selection);
          const callback = this.options[key2];
          return callback == null ? void 0 : callback(range2, latest);
        });
        callbackCache[key2] = wrapped;
      }
      return wrapped;
    });
  }
  /**
  * Track the earliest index whose measured layout may now be stale. Later
  * render passes relayout from this point forward so we do not have to rebuild
  * positions for the whole list after every change.
  */
  markLayoutDirtyFromIndex(index) {
    this.layoutDirtyIndex = Math.min(this.layoutDirtyIndex ?? index, index);
  }
  /**
  * Mark the earliest affected item as layout-dirty after an imperative change.
  * Each record carries its current array index so this stays O(1) even when
  * the viewer holds a very large number of items.
  */
  markItemLayoutDirty(item) {
    if (this.items[item.index] !== item) throw new Error(`CodeView.markItemLayoutDirty: unknown item id "${item.item.id}"`);
    this.markLayoutDirtyFromIndex(item.index);
  }
  /**
  * Detect the common controlled-update case where the new list simply extends
  * the existing ordered prefix. When that happens we can reuse every current
  * record in place, sync any versioned payload changes, and append only the new
  * tail instead of rebuilding the whole list.
  */
  tryAppendItems(items) {
    if (items.length <= this.items.length) return false;
    for (let index = 0; index < this.items.length; index++) {
      const existingItem = this.items[index];
      if (existingItem == null) throw new Error("CodeView.tryAppendItems: missing existing item");
      const nextItem = items[index];
      if (nextItem == null || existingItem.item.id !== nextItem.id || existingItem.type !== nextItem.type) return false;
    }
    for (let index = 0; index < this.items.length; index++) {
      const existingItem = this.items[index];
      if (existingItem == null) throw new Error("CodeView.tryAppendItems: missing existing item");
      const nextItem = items[index];
      if (nextItem == null) throw new Error("CodeView.tryAppendItems: append candidate missing prefix item");
      if (this.syncItemRecord(existingItem, nextItem)) this.markLayoutDirtyFromIndex(index);
    }
    this.appendItemsInternal(items.slice(this.items.length), false);
    this.scrollDirty = true;
    this.render();
    return true;
  }
  /**
  * Reconcile a new controlled item list against the existing records by id.
  * This reuses records and instances when type matches, cleans up removed
  * records, rebuilds the lookup maps, and marks layout dirty whenever order,
  * membership, or versioned item data changes.
  */
  reconcileItems(items) {
    const { items: previousItems, idToItem: previousById } = this;
    const removedItems = new Set(previousItems);
    const nextItems = [];
    const nextIdToItem = /* @__PURE__ */ new Map();
    const nextInstanceToItem = /* @__PURE__ */ new Map();
    const removedItemsById = /* @__PURE__ */ new Map();
    let firstDirtyIndex;
    for (let index = 0; index < items.length; index++) {
      const input = items[index];
      if (input == null) throw new Error("CodeView.reconcileItems: missing input item");
      if (nextIdToItem.has(input.id)) throw new Error(`CodeView.setItems: duplicate id "${input.id}"`);
      const previousItem = previousById.get(input.id);
      const item = previousItem != null && previousItem.type === input.type ? previousItem : this.createItem(input, index, 0);
      item.index = index;
      if (previousItem != null && previousItem.type === input.type) {
        removedItems.delete(previousItem);
        if (this.syncItemRecord(item, input)) firstDirtyIndex = Math.min(firstDirtyIndex ?? index, index);
      } else firstDirtyIndex = Math.min(firstDirtyIndex ?? index, index);
      if (previousItems[index] !== item) firstDirtyIndex = Math.min(firstDirtyIndex ?? index, index);
      nextItems.push(item);
      nextIdToItem.set(input.id, item);
      nextInstanceToItem.set(item.instance, item);
    }
    if (firstDirtyIndex == null) {
      if (removedItems.size === 0) return;
      firstDirtyIndex = Math.max(nextItems.length - 1, 0);
    }
    this.capturePendingLayoutAnchor(nextIdToItem);
    for (let index = 0; index < previousItems.length; index++) {
      const removedItem = previousItems[index];
      if (removedItem == null || !removedItems.has(removedItem)) continue;
      removedItemsById.set(removedItem.item.id, removedItem);
      this.releaseRenderedItem(removedItem);
      const dirtyIndex = Math.max(nextItems.length - 1, 0);
      firstDirtyIndex = Math.min(firstDirtyIndex ?? dirtyIndex, dirtyIndex);
    }
    this.items = nextItems;
    this.idToItem = nextIdToItem;
    this.instanceToItem = nextInstanceToItem;
    if (this.renderState.firstIndex >= nextItems.length) this.resetRenderState();
    else if (this.renderState.lastIndex >= nextItems.length) this.renderState.lastIndex = nextItems.length - 1;
    this.markLayoutDirtyFromIndex(firstDirtyIndex);
    this.scrollDirty = true;
    this.render();
    return removedItemsById.size > 0 ? removedItemsById : void 0;
  }
  /**
  * Update a reused record from the latest controlled item only when its item
  * version changes. Matching versions mean CodeView keeps the current record
  * snapshot, which lets imperative updates remain in place until the caller
  * intentionally publishes a newer version.
  */
  syncItemRecord(item, nextItem) {
    if (item.type !== nextItem.type) throw new Error(`CodeView.syncItemRecord: type mismatch for id "${nextItem.id}"`);
    if (item.version === nextItem.version) return false;
    item.item = nextItem;
    item.version = nextItem.version;
    item.renderedOptionsRevision = -1;
    return true;
  }
  getMaxScrollTopForHeight(scrollHeight) {
    const { paddingBottom, paddingTop } = this.getLayout();
    return Math.max(paddingTop + this.header.height + scrollHeight + this.footer.height + paddingBottom - this.getHeight(), 0);
  }
  getMaxScrollTop() {
    return this.getMaxScrollTopForHeight(this.getScrollHeight());
  }
  shouldRebaseScroll() {
    return this.getMaxScrollTop() > SCROLL_REBASE_THRESHOLD;
  }
  getPagedScrollHeight() {
    return this.shouldRebaseScroll() ? Math.min(this.getScrollHeight(), SCROLL_REBASE_CONTAINER_HEIGHT) : this.getScrollHeight();
  }
  getMaxPagedScrollTop() {
    return this.getMaxScrollTopForHeight(this.getPagedScrollHeight());
  }
  clampPagedScrollTop(value) {
    const maxScroll = this.getMaxPagedScrollTop();
    return Math.max(0, Math.min(value, maxScroll));
  }
  /**
  * Clamps a logical scroll position to the min/max allowable scroll range
  * based on the full computed content height.
  */
  clampScrollTop(value) {
    const maxScroll = this.getMaxScrollTop();
    return Math.max(0, Math.min(value, maxScroll));
  }
  getMaxScrollPageOffset() {
    return Math.max(this.getMaxScrollTop() - this.getMaxPagedScrollTop(), 0);
  }
  clampScrollPageOffset(value) {
    const maxOffset = this.getMaxScrollPageOffset();
    return Math.max(0, Math.min(value, maxOffset));
  }
  resolveScrollPageWindow(scrollTop, preferredPagedScrollTop) {
    let pagedScrollTop = roundToDevicePixel(this.clampPagedScrollTop(preferredPagedScrollTop));
    let scrollPageOffset = this.clampScrollPageOffset(scrollTop - pagedScrollTop);
    pagedScrollTop = roundToDevicePixel(this.clampPagedScrollTop(scrollTop - scrollPageOffset));
    scrollPageOffset = this.clampScrollPageOffset(scrollTop - pagedScrollTop);
    return {
      pagedScrollTop,
      scrollPageOffset
    };
  }
  /**
  * Resolve how a logical scrollTop maps onto the reusable paged scroll window
  * without mutating the current page offset.
  */
  resolvePagedScrollPosition(logicalScrollTop) {
    if (!this.shouldRebaseScroll()) return {
      pagedScrollTop: this.clampPagedScrollTop(logicalScrollTop),
      scrollPageOffset: 0
    };
    const currentPageOffset = this.clampScrollPageOffset(this.scrollPageOffset);
    const pagedScrollTop = logicalScrollTop - currentPageOffset;
    const pagedMaxScrollTop = this.getMaxPagedScrollTop();
    const maxRebaseOffset = this.getMaxScrollPageOffset();
    const shouldMoveDown = pagedScrollTop > SCROLL_REBASE_THRESHOLD && currentPageOffset < maxRebaseOffset;
    const shouldMoveUp = pagedScrollTop < SCROLL_REBASE_TRIGGER_TOP && currentPageOffset > 0;
    if (pagedScrollTop < 0 || pagedScrollTop > pagedMaxScrollTop || shouldMoveDown || shouldMoveUp) return this.resolveScrollPageWindow(logicalScrollTop, shouldMoveUp ? Math.min(SCROLL_REBASE_TARGET_BOTTOM, pagedMaxScrollTop) : SCROLL_REBASE_TARGET_TOP);
    return {
      pagedScrollTop: roundToDevicePixel(this.clampPagedScrollTop(pagedScrollTop)),
      scrollPageOffset: currentPageOffset
    };
  }
  needsScrollPageUpdate(logicalScrollTop) {
    const roundedScrollTop = roundToDevicePixel(this.clampScrollTop(logicalScrollTop));
    const { scrollPageOffset } = this.resolvePagedScrollPosition(roundedScrollTop);
    return scrollPageOffset !== this.scrollPageOffset;
  }
  getPagedLayoutTop(logicalTop) {
    if (!this.shouldRebaseScroll()) return logicalTop;
    return Math.max(logicalTop - this.scrollPageOffset, 0);
  }
  getStickyHeaderOffset() {
    return this.options.stickyHeaders === true && this.options.disableFileHeader !== true ? this.itemMetricsCache.diffHeaderHeight : 0;
  }
  getScrollTargetRect(target) {
    const item = this.idToItem.get(target.id);
    if (item == null) {
      console.warn(`CodeView.scrollTo: unknown item id "${target.id}"`);
      return;
    }
    if (target.type === "item") return {
      top: item.top,
      height: item.height
    };
    if (target.type === "range") {
      const rangePosition = this.getRangeScrollPosition(item, target);
      if (rangePosition == null) {
        console.warn(`CodeView.scrollTo: unable to resolve range ${formatSelectedLineRange(target.range)} for item "${target.id}"`);
        return;
      }
      return {
        top: item.top + rangePosition.top,
        height: rangePosition.height
      };
    }
    const linePosition = this.getLineScrollPosition(item, target);
    if (linePosition == null) {
      console.warn(`CodeView.scrollTo: unable to resolve line ${target.lineNumber} for item "${target.id}"`);
      return;
    }
    return {
      top: item.top + linePosition.top,
      height: linePosition.height
    };
  }
  normalizeScrollTarget(target) {
    if (target.type === "position" || target.align !== "nearest") return target;
    const rect = this.getScrollTargetRect(target);
    if (rect == null) return;
    const offset = target.offset ?? 0;
    const targetTop = this.getItemTopOffset() + rect.top;
    const targetBottom = targetTop + rect.height;
    const currentTop = this.getScrollTop();
    const visibleTop = currentTop + (target.type === "line" || target.type === "range" ? this.getStickyHeaderOffset() : 0);
    const visibleBottom = currentTop + this.getHeight();
    if (targetTop - offset <= visibleTop && targetBottom + offset >= visibleBottom) return;
    if (targetTop - offset < visibleTop) return {
      ...target,
      align: "start"
    };
    if (targetBottom + offset > visibleBottom) return {
      ...target,
      align: "end"
    };
  }
  /**
  * Resolve a target's scroll position
  
  * Returns `undefined` when we can't resolve a target for whatever reason
  */
  resolveScrollTargetTop(target) {
    if (target.type === "position") {
      const clampedPosition = this.clampScrollTop(target.position);
      return clampedPosition !== target.position ? clampedPosition : this.clampScrollTop(target.position - this.getStickyHeaderOffset());
    }
    const item = this.idToItem.get(target.id);
    if (item == null) {
      console.warn(`CodeView.scrollTo: unknown item id "${target.id}"`);
      return;
    }
    if (target.type === "item") return this.clampScrollTop(this.resolveAlignedScrollPosition(item.top, item.height, target.align, target.offset));
    if (target.type === "range") {
      const rangePosition = this.getRangeScrollPosition(item, target);
      if (rangePosition == null) {
        console.warn(`CodeView.scrollTo: unable to resolve range ${formatSelectedLineRange(target.range)} for item "${target.id}"`);
        return;
      }
      return this.clampScrollTop(this.resolveAlignedScrollPosition(item.top + rangePosition.top, rangePosition.height, target.align, target.offset, this.getStickyHeaderOffset()));
    }
    const linePosition = this.getLineScrollPosition(item, target);
    if (linePosition == null) {
      console.warn(`CodeView.scrollTo: unable to resolve line ${target.lineNumber} for item "${target.id}"`);
      return;
    }
    return this.clampScrollTop(this.resolveAlignedScrollPosition(item.top + linePosition.top, linePosition.height, target.align, target.offset, this.getStickyHeaderOffset()));
  }
  /**
  * Given an existing scroll target (scroll top and height), figure out the
  * correct scroll position to target based on the desired alignment, offset
  * and stickyOffset if necessary
  */
  resolveAlignedScrollPosition(targetTop, targetHeight, align, offset = 0, stickyOffset = 0) {
    targetTop += this.getItemTopOffset();
    const viewportHeight = this.getHeight();
    if (align === "center" && targetHeight + offset < viewportHeight) return targetTop - (viewportHeight - targetHeight) / 2 + offset;
    if (align === "end") return targetTop - (viewportHeight - targetHeight) + offset;
    return targetTop - stickyOffset - offset;
  }
  getLineScrollPosition(item, target) {
    if (item.type === "diff") return item.instance.getLinePosition(target.lineNumber, target.side);
    return item.instance.getLinePosition(target.lineNumber);
  }
  getRangeScrollPosition(item, target) {
    const { range: range2 } = target;
    const startPosition = this.getLineScrollPosition(item, {
      type: "line",
      id: target.id,
      lineNumber: range2.start,
      side: range2.side
    });
    const endPosition = this.getLineScrollPosition(item, {
      type: "line",
      id: target.id,
      lineNumber: range2.end,
      side: range2.endSide ?? range2.side
    });
    if (startPosition == null || endPosition == null) return;
    const startTop = startPosition.top;
    const startBottom = startTop + startPosition.height;
    const endTop = endPosition.top;
    const endBottom = endTop + endPosition.height;
    const top = Math.min(startTop, endTop);
    return {
      top,
      height: Math.max(startBottom, endBottom) - top
    };
  }
  /**
  * Determine target scroll position for current frame.
  *
  * If there's no pendingScrollTarget then we just return the current scroll
  * position
  *
  * If there's a pendingScrollTarget then we depend on whether there's a
  * smooth scroll animation or not. If not just return the destination, or
  * compute next position given the smooth scroll spring physics
  */
  computeTargetScrollTopForFrame(scrollTop, frameTimestamp) {
    if (this.pendingScrollTarget == null) return scrollTop;
    const destination = this.resolveScrollTargetTop(this.pendingScrollTarget);
    if (destination == null) return scrollTop;
    const { scrollAnimation } = this;
    if (scrollAnimation == null) return destination;
    return this.computeSpringStep(scrollAnimation, destination, frameTimestamp).position;
  }
  /**
  * Closed-form critical-damped ODE step.
  *
  * Stable at any dt (Euler would blow up once ω·dt ≳ 1), so this survives
  * big RAF gaps (tab-wake, offscreen frames) and resize-driven ticks that
  * fire outside the normal RAF cadence.
  */
  computeSpringStep(animation, destination, frameTimestamp) {
    const dt2 = Math.max(0, frameTimestamp - animation.lastTimestamp);
    const { omega } = this.getSmoothScrollSettings();
    const decay = Math.exp(-omega * dt2);
    const displacement = animation.position - destination;
    const springCoeff = animation.velocity + omega * displacement;
    return {
      position: destination + (displacement + springCoeff * dt2) * decay,
      velocity: (springCoeff * (1 - omega * dt2) - omega * displacement) * decay
    };
  }
  /**
  * For any given pendingScrollTarget, updates any in flight smooth scroll
  * animations and returns the target scrollTop to move towards
  *
  * Resolves the animation based on frame time and adopts any necessary scroll
  * anchoring corrections if necessary
  */
  advanceScrollAnimation(frameTimestamp, anchorDelta) {
    if (this.pendingScrollTarget == null) return;
    const destination = this.resolveScrollTargetTop(this.pendingScrollTarget);
    if (destination == null) {
      this.pendingScrollTarget = void 0;
      this.scrollAnimation = void 0;
      return;
    }
    const animation = this.scrollAnimation;
    if (animation == null) return destination;
    animation.position += anchorDelta;
    const { position, velocity } = this.computeSpringStep(animation, destination, frameTimestamp);
    animation.lastTimestamp = frameTimestamp;
    animation.position = position;
    animation.velocity = velocity;
    const { positionEpsilon, velocityEpsilon } = this.getSmoothScrollSettings();
    if (Math.abs(destination - position) <= positionEpsilon && Math.abs(velocity) <= velocityEpsilon) {
      animation.position = destination;
      animation.velocity = 0;
      this.scrollAnimation = void 0;
      return destination;
    }
    return animation.position;
  }
  flushManagers(updatedItems) {
    for (const item of updatedItems) item.instance.flushManagers();
  }
  syncContainerHeight() {
    const pagedScrollHeight = this.getPagedScrollHeight();
    if (this.container == null || this.containerHeight === pagedScrollHeight) return;
    this.container.style.height = `${pagedScrollHeight}px`;
    this.containerHeight = pagedScrollHeight;
  }
  getStickyBounds(windowSpecs) {
    var _a9, _b;
    const { firstIndex, lastIndex } = windowSpecs != null ? {
      firstIndex: this.findFirstVisibleIndex(windowSpecs.top),
      lastIndex: this.findLastVisibleIndex(windowSpecs.bottom)
    } : this.renderState;
    if (firstIndex === -1 || lastIndex === -1 || firstIndex > lastIndex) return;
    const firstStickySpecs = (_a9 = this.items[firstIndex]) == null ? void 0 : _a9.instance.getAdvancedStickySpecs(windowSpecs);
    const lastStickySpecs = (_b = this.items[lastIndex]) == null ? void 0 : _b.instance.getAdvancedStickySpecs(windowSpecs);
    if (firstStickySpecs == null || lastStickySpecs == null) return;
    return {
      stickyTop: this.getPagedLayoutTop(Math.max(firstStickySpecs.topOffset, 0)),
      stickyBottom: this.getPagedLayoutTop(lastStickySpecs.topOffset + lastStickySpecs.height)
    };
  }
  applyStickyPositioning({ stickyTop, stickyBottom }) {
    const height = this.getHeight();
    const { itemMetricsCache: itemMetrics } = this;
    const stickyContainerHeight = stickyBottom - stickyTop;
    this.renderState.stickyHeight = stickyContainerHeight;
    this.renderState.stickyTop = stickyTop;
    this.renderState.stickyBottom = stickyBottom;
    this.stickyOffset.style.height = `${stickyTop}px`;
    const randomOffset = (Math.random() * itemMetrics.lineHeight >> 0) * -1;
    const stickyJitter = -Math.max(stickyContainerHeight + randomOffset, 0) + height;
    this.stickyContainer.style.top = `${stickyJitter}px`;
    this.stickyContainer.style.bottom = `${stickyJitter + itemMetrics.diffHeaderHeight}px`;
  }
  syncPagedScrollScaffolding(windowSpecs) {
    this.syncContainerHeight();
    const stickyBounds = this.getStickyBounds(windowSpecs);
    if (stickyBounds == null) return;
    this.applyStickyPositioning(stickyBounds);
  }
  reconcileRenderedItems(updatedItems) {
    const { firstIndex, lastIndex } = this.renderState;
    if (firstIndex === -1) return;
    let currentTop = -1;
    let heightChanged = false;
    for (let index = firstIndex; index < this.items.length; index++) {
      if (!heightChanged && index > lastIndex) break;
      const item = this.items[index];
      if (item == null) throw new Error("CodeView.reconcileRenderedItems: Invalid item");
      if (currentTop === -1) currentTop = item.top;
      else if (item.top !== currentTop) {
        item.top = currentTop;
        item.instance.syncVirtualizedTop();
        heightChanged = true;
      }
      if (updatedItems == null ? index <= lastIndex : updatedItems.has(item)) {
        if (item.instance.reconcileHeights()) {
          heightChanged = true;
          item.height = item.instance.getVirtualizedHeight();
        }
        this.validateRenderedItemHeight(item);
      }
      currentTop += item.instance.getVirtualizedHeight();
      if (index < this.items.length - 1) currentTop += this.getLayout().gap;
    }
    if (heightChanged && currentTop != null) {
      this.scrollDirty = true;
      this.scrollHeight = currentTop;
    }
  }
  updateStickyPositioning() {
    const stickyBounds = this.getStickyBounds();
    if (stickyBounds == null) {
      if (this.renderState.firstIndex === -1) this.stickyOffset.style.height = "";
      return;
    }
    const { stickyTop, stickyBottom } = stickyBounds;
    if (stickyBottom - stickyTop === this.renderState.stickyHeight && stickyTop === this.renderState.stickyTop && stickyBottom === this.renderState.stickyBottom) return;
    this.applyStickyPositioning(stickyBounds);
  }
  /**
  * Figure out scrollTop accounting for sticky header if enabled and
  * necessary
  */
  getScrollAnchorViewportTop(absoluteItemTop, scrollTop) {
    return absoluteItemTop < scrollTop ? scrollTop + this.getStickyHeaderOffset() : scrollTop;
  }
  /**
  * Attempt to find a scroll anchor based on build in metrics of the existing
  * rendered files/diff.
  *
  * A scroll anchor represents the first fully visible element (in other
  * words, the first file or first line who's top is fully in the viewport).
  */
  getScrollAnchor(scrollTop, availableItems = this.idToItem) {
    let skippedItem;
    const { pendingLayoutAnchor } = this;
    if (pendingLayoutAnchor != null) {
      const pendingItem = this.idToItem.get(pendingLayoutAnchor.id);
      if (pendingItem != null && availableItems.get(pendingLayoutAnchor.id) === pendingItem) return pendingLayoutAnchor;
      if (pendingItem != null) skippedItem = pendingItem;
    }
    if (scrollTop <= 0) return;
    const { firstIndex, lastIndex, stickyTop, stickyBottom } = this.renderState;
    if (firstIndex === -1 || lastIndex === -1) return;
    const viewportHeight = this.getHeight();
    if (stickyTop === -1 || stickyBottom === -1) return;
    for (let index = firstIndex; index <= lastIndex; index++) {
      const item = this.items[index];
      if (item == null) continue;
      const absoluteItemTop = this.getItemTopOffset() + item.top;
      if (absoluteItemTop + item.height <= scrollTop) continue;
      if (absoluteItemTop >= scrollTop + viewportHeight) break;
      if (!(availableItems.get(item.item.id) === item)) {
        skippedItem ?? (skippedItem = item);
        continue;
      }
      if (absoluteItemTop >= scrollTop) return {
        type: "item",
        id: item.item.id,
        viewportOffset: absoluteItemTop - scrollTop
      };
      const localViewportTop = this.getScrollAnchorViewportTop(absoluteItemTop, scrollTop) - absoluteItemTop;
      const lineAnchor = item.instance.getNumericScrollAnchor(localViewportTop);
      if (lineAnchor != null) {
        const absoluteLineTop = absoluteItemTop + lineAnchor.top;
        return {
          type: "line",
          id: item.item.id,
          lineNumber: lineAnchor.lineNumber,
          side: lineAnchor.side,
          viewportOffset: absoluteLineTop - scrollTop
        };
      }
    }
    if (skippedItem != null) for (let index = skippedItem.index + 1; index < this.items.length; index++) {
      const candidate = this.items[index];
      if (candidate != null && availableItems.get(candidate.item.id) === candidate) return {
        type: "item",
        id: candidate.item.id,
        viewportOffset: 0
      };
    }
  }
  /**
  * Given a scroll anchor, attempt to resolve a newly updated (and clamped)
  * scroll position to keep the anchored element in place.
  *
  * If we can't resolve a position for whatever reason, we'll return
  * undefined.
  */
  resolveAnchoredScrollTop(anchor) {
    const item = this.idToItem.get(anchor.id);
    if (item == null) return;
    const itemTopOffset = this.getItemTopOffset();
    if (anchor.type === "item") {
      const absoluteItemTop = itemTopOffset + item.top;
      return this.clampScrollTop(absoluteItemTop - anchor.viewportOffset);
    }
    const linePosition = item.type === "diff" ? item.instance.getLinePosition(anchor.lineNumber, anchor.side) : item.instance.getLinePosition(anchor.lineNumber);
    if (linePosition == null) return;
    const absoluteLineTop = itemTopOffset + item.top + linePosition.top;
    return this.clampScrollTop(absoluteLineTop - anchor.viewportOffset);
  }
  /**
  * Apply a device-pixel-rounded scroll position if it differs from the last
  * logical scrollTop synchronized into the paged scroll scaffold.
  */
  applyScrollFix(targetScrollTop, syncedScrollTop, windowSpecs) {
    if (this.root == null) return;
    const roundedTargetScrollTop = roundToDevicePixel(this.clampScrollTop(targetScrollTop));
    const roundedSyncedScrollTop = roundToDevicePixel(syncedScrollTop);
    const { scrollPageOffset: previousPageOffset } = this;
    const syncedPagedScrollTop = roundToDevicePixel(this.clampPagedScrollTop(roundedSyncedScrollTop - previousPageOffset));
    const { pagedScrollTop, scrollPageOffset } = this.resolvePagedScrollPosition(roundedTargetScrollTop);
    const targetPagedScrollTop = pagedScrollTop;
    const rebaseChanged = previousPageOffset !== scrollPageOffset;
    if (roundedTargetScrollTop === this.renderState.scrollTop && roundedTargetScrollTop === roundedSyncedScrollTop && targetPagedScrollTop === syncedPagedScrollTop && !rebaseChanged) return;
    this.suspendScrollInteractions();
    if (targetPagedScrollTop !== syncedPagedScrollTop || rebaseChanged) {
      this.scrollPageOffset = scrollPageOffset;
      this.syncPagedScrollScaffolding(windowSpecs);
    }
    if (targetPagedScrollTop !== syncedPagedScrollTop) this.root.scrollTo({
      top: targetPagedScrollTop,
      behavior: "instant"
    });
    this.renderState.scrollTop = roundedTargetScrollTop;
    this.scrollTop = roundedTargetScrollTop;
    this.scrollDirty = false;
  }
  /**
  * Decide whether a pending programmatic scroll has reached its
  * destination and should be cleared.
  */
  isPendingTargetSettled(target) {
    const top = this.resolveScrollTargetTop(target);
    if (top == null) return true;
    return roundToDevicePixel(this.getScrollTop()) === roundToDevicePixel(top);
  }
  getScrollTop() {
    var _a9;
    if (!this.scrollDirty) return this.scrollTop;
    this.scrollDirty = false;
    const rootScrollTop = ((_a9 = this.root) == null ? void 0 : _a9.scrollTop) ?? 0;
    this.scrollTop = this.clampScrollTop(rootScrollTop + this.scrollPageOffset);
    return this.scrollTop;
  }
  getHeight() {
    var _a9;
    if (!this.heightDirty) return this.height;
    this.heightDirty = false;
    this.height = ((_a9 = this.root) == null ? void 0 : _a9.getBoundingClientRect().height) ?? 0;
    return this.height;
  }
  getScrollHeight() {
    return this.scrollHeight;
  }
  flushSlotCoordinator() {
    if (this.slotCoordinator == null) return;
    const slotSnapshot = this.buildSlotSnapshot(this.slotCoordinator);
    if (areManagedSnapshotsEqual(this.slotSnapshot, slotSnapshot)) return;
    this.slotSnapshot = slotSnapshot;
    this.slotCoordinator.onSnapshotChange(slotSnapshot);
  }
  notifyScroll() {
    if (this.scrollListeners.size === 0) return;
    const scrollTop = this.getScrollTop();
    for (const listener of this.scrollListeners) listener(scrollTop, this);
  }
  /**
  * Find the first item whose bottom edge crosses into the viewport window.
  * This lets scroll-time rendering jump directly near the visible range instead
  * of linearly scanning from the start of very large item lists.
  */
  findFirstVisibleIndex(top) {
    let low = 0;
    let high = this.items.length - 1;
    let result = this.items.length;
    while (low <= high) {
      const mid = low + high >> 1;
      const item = this.items[mid];
      if (item == null) throw new Error("CodeView.findFirstVisibleIndex: invalid item index");
      if (item.top + item.height > top) {
        result = mid;
        high = mid - 1;
      } else low = mid + 1;
    }
    return result;
  }
  /**
  * Find the last item whose top edge is still within the viewport window.
  * Paired with findFirstVisibleIndex, this bounds the render loop to only the
  * slice of items that can actually intersect the current scroll range.
  */
  findLastVisibleIndex(bottom) {
    let low = 0;
    let high = this.items.length - 1;
    let result = -1;
    while (low <= high) {
      const mid = low + high >> 1;
      const item = this.items[mid];
      if (item == null) throw new Error("CodeView.findLastVisibleIndex: invalid item index");
      if (item.top <= bottom) {
        result = mid;
        low = mid + 1;
      } else high = mid - 1;
    }
    return result;
  }
  /**
  * Recompute measured tops and heights starting from the earliest dirty item.
  * Earlier items keep their existing layout, while everything from startIndex
  * onward is remeasured so downstream positions and total scroll height stay
  * consistent after inserts, removals, or versioned item updates.
  */
  recomputeLayout(startIndex = 0, reset) {
    if (this.items.length === 0) {
      this.scrollHeight = 0;
      return;
    }
    const layout = this.getLayout();
    let runningTop = 0;
    if (startIndex > 0) {
      const previousItem = this.items[startIndex - 1];
      if (previousItem == null) throw new Error("CodeView.recomputeLayout: invalid dirty index");
      runningTop = previousItem.top + previousItem.height + layout.gap;
    }
    for (let index = startIndex; index < this.items.length; index++) {
      const item = this.items[index];
      if (item == null) throw new Error("CodeView.recomputeLayout: invalid item index");
      item.top = runningTop;
      if (item.type === "diff") {
        const fileDiff = item.instance.consumeCodeViewLayoutChanges(item.item.fileDiff);
        if (fileDiff != null) Object.assign(item.item.fileDiff, fileDiff);
        item.height = item.instance.prepareCodeViewItem(item.item.fileDiff, runningTop, reset, item.item.annotations ?? []);
      } else item.height = item.instance.prepareCodeViewItem(item.item.file, runningTop, reset, item.item.annotations ?? []);
      runningTop += item.height;
      if (index < this.items.length - 1) runningTop += layout.gap;
    }
    if (runningTop !== this.scrollHeight) this.scrollDirty = true;
    this.scrollHeight = runningTop;
  }
  resetRenderState() {
    this.renderState.scrollTop = -1;
    this.renderState.firstIndex = -1;
    this.renderState.lastIndex = -1;
    this.renderState.stickyHeight = 0;
    this.renderState.stickyTop = -1;
    this.renderState.stickyBottom = -1;
  }
  getFitPerfectlyOverscroll() {
    return this.getLayout().gap + this.itemMetricsCache.diffHeaderHeight;
  }
}, __publicField(_a7, "__STOP", false), __publicField(_a7, "__lastScrollPosition", 0), _a7);
function prepareItemInstance(item) {
  item.instance.cleanUp(true);
  if (item.type === "diff") return item.instance.prepareCodeViewItem(item.item.fileDiff, item.top, void 0, item.item.annotations ?? []);
  else return item.instance.prepareCodeViewItem(item.item.file, item.top, void 0, item.item.annotations ?? []);
}
function shouldClearPool(previousOptions, nextOptions) {
  return !areThemesEqual(previousOptions.theme ?? DEFAULT_THEMES, nextOptions.theme ?? DEFAULT_THEMES) || (previousOptions.themeType ?? "system") !== (nextOptions.themeType ?? "system") || previousOptions.unsafeCSS !== nextOptions.unsafeCSS;
}
function hasItemLayoutOptionChanged(previousOptions, nextOptions) {
  return (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS || (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
}
function hasCodeViewDiffEstimateOptionChanged(previousOptions, nextOptions) {
  return (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
}
function isPooledShadowChild(child) {
  if (child instanceof SVGElement) return true;
  return isStyleNode(child) && (child.hasAttribute("data-core-css") || child.hasAttribute("data-theme-css") || child.hasAttribute("data-unsafe-css"));
}
function formatSelectedLineRange(range2) {
  const start = formatSelectedLinePoint(range2.start, range2.side);
  const end = formatSelectedLinePoint(range2.end, range2.endSide ?? range2.side);
  return start === end ? start : `${start}-${end}`;
}
function formatSelectedLinePoint(lineNumber, side) {
  if (side == null) return `${lineNumber}`;
  return `${side === "deletions" ? "D" : "A"}${lineNumber}`;
}
function renderItem(item, fileContainer, forceRender = false) {
  if (item.type === "diff") return item.instance.render({
    deferManagers: true,
    fileContainer,
    fileDiff: item.item.fileDiff,
    forceRender,
    lineAnnotations: item.item.annotations ?? []
  });
  else return item.instance.render({
    deferManagers: true,
    fileContainer,
    file: item.item.file,
    forceRender,
    lineAnnotations: item.item.annotations ?? []
  });
}
function syncRenderedItemOrder(container, element2, prevElement) {
  if (prevElement == null) {
    if (container.firstChild !== element2) container.prepend(element2);
    return;
  }
  if (prevElement.nextSibling !== element2) prevElement.after(element2);
}
function hasAnnotations(item) {
  var _a9;
  return (((_a9 = item.annotations) == null ? void 0 : _a9.length) ?? 0) > 0;
}
function getSlotItems(renderedItems, { hasHeaderRenderers, hasAnnotationRenderer, hasGutterRenderer }) {
  if (renderedItems.length === 0) return;
  if (hasHeaderRenderers || hasGutterRenderer) return renderedItems;
  if (!hasAnnotationRenderer) return;
  const slotSnapshot = [];
  for (const renderedItem of renderedItems) if (hasAnnotations(renderedItem.item)) slotSnapshot.push(renderedItem);
  return slotSnapshot.length > 0 ? slotSnapshot : void 0;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/shiki-stream/tokenizer.js
var ShikiStreamTokenizer = class ShikiStreamTokenizer2 {
  constructor(options) {
    __publicField(this, "options");
    __publicField(this, "tokensStable", []);
    __publicField(this, "tokensUnstable", []);
    __publicField(this, "lastUnstableCodeChunk", "");
    __publicField(this, "lastStableGrammarState");
    this.options = options;
  }
  /**
  * Enqueue a chunk of code to the buffer.
  */
  async enqueue(chunk) {
    const chunkLines = (this.lastUnstableCodeChunk + chunk).split("\n");
    const stable = [];
    let unstable = [];
    const recall = this.tokensUnstable.length;
    chunkLines.forEach((line, i2) => {
      const isLastLine = i2 === chunkLines.length - 1;
      const result = this.options.highlighter.codeToTokens(line, {
        ...this.options,
        grammarState: this.lastStableGrammarState
      });
      const tokens = result.tokens[0];
      if (!isLastLine) tokens.push({
        content: "\n",
        offset: 0
      });
      if (!isLastLine) {
        this.lastStableGrammarState = result.grammarState;
        stable.push(...tokens);
      } else {
        unstable = tokens;
        this.lastUnstableCodeChunk = line;
      }
    });
    this.tokensStable.push(...stable);
    this.tokensUnstable = unstable;
    return {
      recall,
      stable,
      unstable
    };
  }
  close() {
    const stable = this.tokensUnstable;
    this.tokensUnstable = [];
    this.lastUnstableCodeChunk = "";
    this.lastStableGrammarState = void 0;
    return { stable };
  }
  clear() {
    this.tokensStable = [];
    this.tokensUnstable = [];
    this.lastUnstableCodeChunk = "";
    this.lastStableGrammarState = void 0;
  }
  clone() {
    const clone2 = new ShikiStreamTokenizer2(this.options);
    clone2.lastUnstableCodeChunk = this.lastUnstableCodeChunk;
    clone2.tokensUnstable = this.tokensUnstable;
    clone2.tokensStable = this.tokensStable;
    clone2.lastStableGrammarState = this.lastStableGrammarState;
    return clone2;
  }
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/shiki-stream/stream.js
var CodeToTokenTransformStream = class extends TransformStream {
  constructor(options) {
    const tokenizer = new ShikiStreamTokenizer(options);
    const { allowRecalls = false } = options;
    super({
      async transform(chunk, controller) {
        const { stable, unstable: buffer, recall } = await tokenizer.enqueue(chunk);
        if (allowRecalls && recall > 0) controller.enqueue({ recall });
        for (const token2 of stable) controller.enqueue(token2);
        if (allowRecalls) for (const token2 of buffer) controller.enqueue(token2);
      },
      async flush(controller) {
        const { stable } = tokenizer.close();
        if (!allowRecalls) for (const token2 of stable) controller.enqueue(token2);
      }
    });
    __publicField(this, "tokenizer");
    __publicField(this, "options");
    this.tokenizer = tokenizer;
    this.options = options;
  }
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createSpanNodeFromToken.js
function createSpanFromToken(token2) {
  const element2 = document.createElement("span");
  element2.style = stringifyTokenStyle(token2.htmlStyle ?? getTokenStyleObject(token2));
  element2.textContent = token2.content;
  return element2;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/FileStream.js
var instanceId7 = -1;
var FileStream = class {
  constructor(options = { theme: DEFAULT_THEMES }) {
    __publicField(this, "options");
    __publicField(this, "__id", `file-stream:${++instanceId7}`);
    __publicField(this, "highlighter");
    __publicField(this, "stream");
    __publicField(this, "abortController");
    __publicField(this, "fileContainer");
    __publicField(this, "pre");
    __publicField(this, "code");
    __publicField(this, "gutterElement");
    __publicField(this, "contentElement");
    __publicField(this, "themeCSSStyle");
    __publicField(this, "appliedThemeCSS");
    __publicField(this, "currentRowCount", 0);
    __publicField(this, "queuedSetupArgs");
    __publicField(this, "queuedTokens", []);
    __publicField(this, "handleWrite", (token2) => {
      var _a9, _b;
      if ("recall" in token2 && this.queuedTokens.length >= token2.recall) this.queuedTokens.length = this.queuedTokens.length - token2.recall;
      else this.queuedTokens.push(token2);
      queueRender(this.render);
      (_b = (_a9 = this.options).onStreamWrite) == null ? void 0 : _b.call(_a9, token2);
    });
    __publicField(this, "currentLineIndex");
    __publicField(this, "currentLineElement");
    __publicField(this, "render", () => {
      var _a9, _b, _c, _d, _e, _f;
      (_b = (_a9 = this.options).onPreRender) == null ? void 0 : _b.call(_a9, this);
      const { gutter, content } = this.getOrCreateStreamColumns();
      const gutterFragment = document.createDocumentFragment();
      const contentFragment = document.createDocumentFragment();
      for (const token2 of this.queuedTokens) if ("recall" in token2) {
        if (this.currentLineElement == null) throw new Error("FileStream.render: no current line element, shouldnt be possible to get here");
        if (token2.recall > this.currentLineElement.childNodes.length) throw new Error(`FileStream.render: Token recall exceed the current line, there's probably a bug...`);
        for (let i2 = 0; i2 < token2.recall; i2++) (_c = this.currentLineElement.lastChild) == null ? void 0 : _c.remove();
      } else {
        const span = createSpanFromToken(token2);
        if (this.currentLineElement == null) {
          const { gutterLine, contentLine } = this.createLine();
          gutterFragment.appendChild(gutterLine);
          contentFragment.appendChild(contentLine);
        }
        (_d = this.currentLineElement) == null ? void 0 : _d.appendChild(span);
        if (token2.content === "\n") {
          this.currentLineIndex++;
          const { gutterLine, contentLine } = this.createLine();
          gutterFragment.appendChild(gutterLine);
          contentFragment.appendChild(contentLine);
        }
      }
      if (gutterFragment.childNodes.length > 0) gutter.appendChild(gutterFragment);
      if (contentFragment.childNodes.length > 0) content.appendChild(contentFragment);
      this.queuedTokens.length = 0;
      (_f = (_e = this.options).onPostRender) == null ? void 0 : _f.call(_e, this);
    });
    this.options = options;
    this.currentLineIndex = this.options.startingLineIndex ?? 1;
  }
  cleanUp() {
    var _a9;
    dequeueRender(this.render);
    (_a9 = this.abortController) == null ? void 0 : _a9.abort();
    this.abortController = void 0;
  }
  setThemeType(themeType) {
    if ((this.options.themeType ?? "system") === themeType) return;
    this.options = {
      ...this.options,
      themeType
    };
    if (typeof this.options.theme === "string" || this.fileContainer == null || this.appliedThemeCSS == null) return;
    this.applyThemeState(this.fileContainer, this.appliedThemeCSS.themeStyles, themeType, this.appliedThemeCSS.baseThemeType);
  }
  async initializeHighlighter() {
    this.highlighter = await getSharedHighlighter(getHighlighterOptions(this.options.lang, this.options));
    return this.highlighter;
  }
  async setup(_source, _wrapper) {
    const isSettingUp = this.queuedSetupArgs != null;
    this.queuedSetupArgs = [_source, _wrapper];
    if (isSettingUp) return;
    this.highlighter ?? (this.highlighter = await this.initializeHighlighter());
    const [source, wrapper] = this.queuedSetupArgs;
    this.queuedSetupArgs = void 0;
    const stream = source;
    this.setupStream(stream, wrapper, this.highlighter);
  }
  setupStream(stream, wrapper, highlighter2) {
    var _a9, _b, _c;
    const { disableLineNumbers = false, overflow = "scroll", theme = DEFAULT_THEMES, themeType = "system" } = this.options;
    const fileContainer = this.getOrCreateFileContainer();
    if (fileContainer.parentElement == null) wrapper.appendChild(fileContainer);
    this.pre ?? (this.pre = document.createElement("pre"));
    if (this.pre.parentElement == null) (_a9 = fileContainer.shadowRoot) == null ? void 0 : _a9.appendChild(this.pre);
    const baseThemeType = typeof theme === "string" ? highlighter2.getTheme(theme).type : void 0;
    const themeStyles = getHighlighterThemeStyles({
      theme,
      highlighter: highlighter2
    });
    this.applyThemeState(fileContainer, themeStyles, themeType, baseThemeType);
    const pre = setPreNodeProperties(this.pre, {
      type: "file",
      diffIndicators: "none",
      disableBackground: true,
      disableLineNumbers,
      overflow,
      split: false,
      totalLines: 0
    });
    pre.textContent = "";
    this.pre = pre;
    this.code = getOrCreateCodeNode({
      code: this.code,
      pre
    });
    this.gutterElement = void 0;
    this.contentElement = void 0;
    this.currentRowCount = 0;
    this.currentLineElement = void 0;
    this.currentLineIndex = this.options.startingLineIndex ?? 1;
    (_b = this.abortController) == null ? void 0 : _b.abort();
    this.abortController = new AbortController();
    const { onStreamStart, onStreamClose, onStreamAbort } = this.options;
    (_c = this.stream) == null ? void 0 : _c.cancel().catch(() => {
    });
    this.stream = stream;
    this.stream.pipeThrough(typeof theme === "string" ? new CodeToTokenTransformStream({
      ...this.options,
      theme,
      highlighter: highlighter2,
      allowRecalls: true,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeTimeLimit: 0
    }) : new CodeToTokenTransformStream({
      ...this.options,
      themes: theme,
      highlighter: highlighter2,
      allowRecalls: true,
      defaultColor: false,
      cssVariablePrefix: formatCSSVariablePrefix("token"),
      tokenizeTimeLimit: 0
    })).pipeTo(new WritableStream({
      start(controller) {
        onStreamStart == null ? void 0 : onStreamStart(controller);
      },
      close() {
        onStreamClose == null ? void 0 : onStreamClose();
      },
      abort(reason) {
        onStreamAbort == null ? void 0 : onStreamAbort(reason);
      },
      write: this.handleWrite
    }), { signal: this.abortController.signal }).catch((error) => {
      if (error.name !== "AbortError") console.error("FileStream pipe error:", error);
    });
  }
  getOrCreateStreamColumns() {
    if (this.code == null) throw new Error("FileStream: expected code element to exist");
    if (this.gutterElement != null && this.contentElement != null) return {
      gutter: this.gutterElement,
      content: this.contentElement
    };
    const gutter = document.createElement("div");
    gutter.dataset.gutter = "";
    const content = document.createElement("div");
    content.dataset.content = "";
    this.code.appendChild(gutter);
    this.code.appendChild(content);
    this.gutterElement = gutter;
    this.contentElement = content;
    return {
      gutter,
      content
    };
  }
  updateRowSpan() {
    if (this.gutterElement != null) this.gutterElement.style.gridRow = `span ${this.currentRowCount}`;
    if (this.contentElement != null) this.contentElement.style.gridRow = `span ${this.currentRowCount}`;
  }
  createLine() {
    const lineNumber = this.currentLineIndex;
    const lineIndex = `${lineNumber - 1}`;
    const gutterLine = document.createElement("div");
    gutterLine.dataset.columnNumber = `${lineNumber}`;
    gutterLine.dataset.lineType = "context";
    gutterLine.dataset.lineIndex = lineIndex;
    const numberContent = document.createElement("span");
    numberContent.dataset.lineNumberContent = "";
    numberContent.textContent = `${lineNumber}`;
    gutterLine.appendChild(numberContent);
    const contentLine = document.createElement("div");
    contentLine.dataset.line = `${lineNumber}`;
    contentLine.dataset.lineType = "context";
    contentLine.dataset.lineIndex = lineIndex;
    this.currentRowCount += 1;
    this.updateRowSpan();
    this.currentLineElement = contentLine;
    return {
      gutterLine,
      contentLine
    };
  }
  getOrCreateFileContainer(fileContainer) {
    if (fileContainer != null && fileContainer === this.fileContainer || fileContainer == null && this.fileContainer != null) return this.fileContainer;
    if (this.fileContainer != null && fileContainer != null && fileContainer !== this.fileContainer) {
      this.themeCSSStyle = void 0;
      this.appliedThemeCSS = void 0;
    }
    this.fileContainer = fileContainer ?? document.createElement("diffs-container");
    return this.fileContainer;
  }
  applyThemeState(container, themeStyles, themeType, baseThemeType) {
    var _a9, _b;
    const shadowRoot = container.shadowRoot ?? container.attachShadow({ mode: "open" });
    const effectiveThemeType = baseThemeType ?? themeType;
    const currentTheme = this.options.theme ?? DEFAULT_THEMES;
    const theme = typeof currentTheme === "string" ? currentTheme : { ...currentTheme };
    const scrollbarGutter = getMeasuredScrollbarGutter(shadowRoot);
    if (((_a9 = this.themeCSSStyle) == null ? void 0 : _a9.parentNode) === shadowRoot && ((_b = this.appliedThemeCSS) == null ? void 0 : _b.themeStyles) === themeStyles && this.appliedThemeCSS.themeType === effectiveThemeType && this.appliedThemeCSS.scrollbarGutter === scrollbarGutter) {
      this.appliedThemeCSS.theme = theme;
      return;
    }
    this.themeCSSStyle = upsertHostThemeStyle({
      shadowRoot,
      currentNode: this.themeCSSStyle,
      themeCSS: wrapThemeCSS(themeStyles, effectiveThemeType, scrollbarGutter)
    });
    this.appliedThemeCSS = this.themeCSSStyle != null ? {
      theme,
      themeStyles,
      themeType: effectiveThemeType,
      baseThemeType,
      scrollbarGutter
    } : void 0;
  }
};

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/resolveRegion.js
function resolveRegion(diff, target) {
  const { resolution, hunkIndex, startContentIndex, endContentIndex, indexesToDelete = /* @__PURE__ */ new Set() } = target;
  const currentHunk = diff.hunks[hunkIndex];
  if (currentHunk == null) {
    console.error({
      diff,
      hunkIndex
    });
    throw new Error(`resolveRegion: Invalid hunk index: ${hunkIndex}`);
  }
  if (startContentIndex < 0 || endContentIndex >= currentHunk.hunkContent.length || startContentIndex > endContentIndex) throw new Error(`resolveRegion: Invalid content range, ${startContentIndex}, ${endContentIndex}`);
  const { hunks, additionLines, deletionLines } = diff;
  const resolvedDiff = {
    ...diff,
    hunks: [],
    deletionLines: [],
    additionLines: [],
    splitLineCount: 0,
    unifiedLineCount: 0,
    cacheKey: diff.cacheKey != null ? `${diff.cacheKey}:${resolution[0]}-${hunkIndex}:${startContentIndex}-${endContentIndex}` : void 0
  };
  const cursor = {
    nextAdditionLineIndex: 0,
    nextDeletionLineIndex: 0,
    nextAdditionStart: 1,
    nextDeletionStart: 1,
    splitLineCount: 0,
    unifiedLineCount: 0
  };
  const updatesEOFState = hunkIndex === hunks.length - 1 && endContentIndex === currentHunk.hunkContent.length - 1;
  const shouldProcessCollapsedContext = !diff.isPartial;
  for (const [index, hunk] of hunks.entries()) {
    processCollapsedContext(diff, resolvedDiff, cursor, getHunkSideStartBoundary(hunk.deletionStart, hunk.deletionCount) - hunk.collapsedBefore, getHunkSideStartBoundary(hunk.additionStart, hunk.additionCount) - hunk.collapsedBefore, hunk.collapsedBefore, shouldProcessCollapsedContext);
    const newHunk = {
      ...hunk,
      hunkContent: [],
      additionStart: cursor.nextAdditionStart,
      deletionStart: cursor.nextDeletionStart,
      additionLineIndex: cursor.nextAdditionLineIndex,
      deletionLineIndex: cursor.nextDeletionLineIndex,
      additionCount: 0,
      deletionCount: 0,
      deletionLines: 0,
      additionLines: 0,
      splitLineStart: cursor.splitLineCount,
      unifiedLineStart: cursor.unifiedLineCount,
      splitLineCount: 0,
      unifiedLineCount: 0
    };
    for (const [contentIndex, content] of hunk.hunkContent.entries()) if (index !== hunkIndex || contentIndex < startContentIndex || contentIndex > endContentIndex) {
      pushContentLinesToDiff(content, resolvedDiff, deletionLines, additionLines);
      const newContent = {
        ...content,
        additionLineIndex: cursor.nextAdditionLineIndex,
        deletionLineIndex: cursor.nextDeletionLineIndex
      };
      newHunk.hunkContent.push(newContent);
      advanceCursor(newContent, cursor, newHunk);
    } else if (indexesToDelete.has(contentIndex)) newHunk.hunkContent.push({
      type: "context",
      lines: 0,
      deletionLineIndex: cursor.nextDeletionLineIndex,
      additionLineIndex: cursor.nextAdditionLineIndex
    });
    else if (content.type === "context") {
      pushContentLinesToDiff(content, resolvedDiff, deletionLines, additionLines);
      const newContent = {
        ...content,
        deletionLineIndex: cursor.nextDeletionLineIndex,
        additionLineIndex: cursor.nextAdditionLineIndex
      };
      newHunk.hunkContent.push(newContent);
      advanceCursor(newContent, cursor, newHunk);
    } else {
      pushResolveLinesToDiff(resolution, content, resolvedDiff, deletionLines, additionLines);
      const newContent = {
        type: "context",
        lines: resolution === "deletions" ? content.deletions : resolution === "additions" ? content.additions : content.deletions + content.additions,
        deletionLineIndex: cursor.nextDeletionLineIndex,
        additionLineIndex: cursor.nextAdditionLineIndex
      };
      newHunk.hunkContent.push(newContent);
      advanceCursor(newContent, cursor, newHunk);
    }
    if (index === hunkIndex && updatesEOFState) {
      const noEOFCR = resolution === "deletions" ? hunk.noEOFCRDeletions : hunk.noEOFCRAdditions;
      newHunk.noEOFCRAdditions = noEOFCR;
      newHunk.noEOFCRDeletions = noEOFCR;
    }
    if (newHunk.additionCount === 0) {
      newHunk.additionStart--;
      if (!diff.isPartial) newHunk.additionLineIndex--;
    }
    if (newHunk.deletionCount === 0) {
      newHunk.deletionStart--;
      if (!diff.isPartial) newHunk.deletionLineIndex--;
    }
    resolvedDiff.hunks.push(newHunk);
  }
  const finalHunk = hunks.at(-1);
  if (finalHunk != null && !diff.isPartial) {
    const finalDeletionEnd = getHunkSideEndBoundary(finalHunk.deletionStart, finalHunk.deletionCount);
    const finalAdditionEnd = getHunkSideEndBoundary(finalHunk.additionStart, finalHunk.additionCount);
    const trailingContext = Math.min(deletionLines.length - finalDeletionEnd, additionLines.length - finalAdditionEnd);
    pushCollapsedContextLines(resolvedDiff, deletionLines, additionLines, finalDeletionEnd, finalAdditionEnd, trailingContext);
    cursor.splitLineCount += trailingContext;
    cursor.unifiedLineCount += trailingContext;
  }
  resolvedDiff.splitLineCount = cursor.splitLineCount;
  resolvedDiff.unifiedLineCount = cursor.unifiedLineCount;
  return resolvedDiff;
}
function pushCollapsedContextLines(diff, deletionLines, additionLines, deletionLineIndex, additionLineIndex, lineCount) {
  for (let index = 0; index < lineCount; index++) {
    const deletionLine = deletionLines[deletionLineIndex + index];
    const additionLine = additionLines[additionLineIndex + index];
    if (deletionLine == null || additionLine == null) throw new Error("pushCollapsedContextLines: missing collapsed context line");
    diff.deletionLines.push(deletionLine);
    diff.additionLines.push(additionLine);
  }
}
function processCollapsedContext(sourceDiff, resolvedDiff, cursor, deletionLineIndex, additionLineIndex, lineCount, shouldProcessContent) {
  if (lineCount <= 0) return;
  if (shouldProcessContent) {
    pushCollapsedContextLines(resolvedDiff, sourceDiff.deletionLines, sourceDiff.additionLines, deletionLineIndex, additionLineIndex, lineCount);
    cursor.nextAdditionLineIndex += lineCount;
    cursor.nextDeletionLineIndex += lineCount;
  }
  cursor.nextAdditionStart += lineCount;
  cursor.nextDeletionStart += lineCount;
  cursor.splitLineCount += lineCount;
  cursor.unifiedLineCount += lineCount;
}
function pushContentLinesToDiff(content, diff, deletionLines, additionLines) {
  if (content.type === "context") for (let i2 = 0; i2 < content.lines; i2++) {
    const line = additionLines[content.additionLineIndex + i2];
    if (line == null) {
      console.error({
        additionLines,
        content,
        i: i2
      });
      throw new Error("pushContentLinesToDiff: Context line does not exist");
    }
    diff.deletionLines.push(line);
    diff.additionLines.push(line);
  }
  else {
    const len = Math.max(content.deletions, content.additions);
    for (let i2 = 0; i2 < len; i2++) {
      if (i2 < content.deletions) {
        const line = deletionLines[content.deletionLineIndex + i2];
        if (line == null) {
          console.error({
            deletionLines,
            content,
            i: i2
          });
          throw new Error("pushContentLinesToDiff: Deletion line does not exist");
        }
        diff.deletionLines.push(line);
      }
      if (i2 < content.additions) {
        const line = additionLines[content.additionLineIndex + i2];
        if (line == null) {
          console.error({
            additionLines,
            content,
            i: i2
          });
          throw new Error("pushContentLinesToDiff: Addition line does not exist");
        }
        diff.additionLines.push(line);
      }
    }
  }
}
function pushResolveLinesToDiff(resolution, content, diff, deletionLines, additionLines) {
  if (resolution === "deletions" || resolution === "both") for (let i2 = 0; i2 < content.deletions; i2++) {
    const line = deletionLines[content.deletionLineIndex + i2];
    if (line == null) {
      console.error({
        deletionLines,
        content,
        i: i2
      });
      throw new Error("pushResolveLinesToDiff: Deletion line does not exist");
    }
    diff.deletionLines.push(line);
    diff.additionLines.push(line);
  }
  if (resolution === "additions" || resolution === "both") for (let i2 = 0; i2 < content.additions; i2++) {
    const line = additionLines[content.additionLineIndex + i2];
    if (line == null) {
      console.error({
        additionLines,
        content,
        i: i2
      });
      throw new Error("pushResolveLinesToDiff: Addition line does not exist");
    }
    diff.deletionLines.push(line);
    diff.additionLines.push(line);
  }
}
function advanceCursor(content, cursor, hunk) {
  if (content.type === "context") {
    cursor.nextAdditionLineIndex += content.lines;
    cursor.nextDeletionLineIndex += content.lines;
    cursor.nextAdditionStart += content.lines;
    cursor.nextDeletionStart += content.lines;
    cursor.splitLineCount += content.lines;
    cursor.unifiedLineCount += content.lines;
    hunk.additionCount += content.lines;
    hunk.deletionCount += content.lines;
    hunk.splitLineCount += content.lines;
    hunk.unifiedLineCount += content.lines;
  } else {
    cursor.nextAdditionLineIndex += content.additions;
    cursor.nextDeletionLineIndex += content.deletions;
    cursor.nextAdditionStart += content.additions;
    cursor.nextDeletionStart += content.deletions;
    cursor.splitLineCount += Math.max(content.deletions, content.additions);
    cursor.unifiedLineCount += content.deletions + content.additions;
    hunk.deletionCount += content.deletions;
    hunk.deletionLines += content.deletions;
    hunk.additionCount += content.additions;
    hunk.additionLines += content.additions;
    hunk.splitLineCount += Math.max(content.deletions, content.additions);
    hunk.unifiedLineCount += content.deletions + content.additions;
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/normalizeDiffResolution.js
function normalizeDiffResolution(options) {
  const type = (() => {
    return typeof options === "string" ? options : options.type;
  })();
  return type === "accept" || type === "incoming" ? "additions" : type === "reject" || type === "current" ? "deletions" : "both";
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/resolveConflict.js
function resolveConflict(diff, conflict, type) {
  return resolveRegion(diff, {
    resolution: normalizeDiffResolution(type),
    hunkIndex: conflict.hunkIndex,
    startContentIndex: conflict.startContentIndex,
    endContentIndex: conflict.endContentIndex,
    indexesToDelete: getConflictDeleteContentIndexes(conflict)
  });
}
function getConflictDeleteContentIndexes(conflict) {
  const indexes = /* @__PURE__ */ new Set();
  if (conflict.baseContentIndex != null) indexes.add(conflict.baseContentIndex);
  if (conflict.endMarkerContentIndex !== conflict.endContentIndex) indexes.add(conflict.endMarkerContentIndex);
  return indexes;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getMergeConflictActionSlotName.js
function getMergeConflictActionSlotName({ hunkIndex, lineIndex, conflictIndex }) {
  return `merge-conflict-action-${hunkIndex}-${lineIndex}-${conflictIndex}`;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/parseMergeConflictDiffFromFile.js
function getMergeConflictActionAnchor(action, fileDiff) {
  const hunk = fileDiff.hunks[action.hunkIndex];
  if (hunk == null) return;
  return {
    hunkIndex: action.hunkIndex,
    lineIndex: getUnifiedLineStartForContent(hunk, action.startContentIndex)
  };
}
function parseMergeConflictDiffFromFile(file, maxContextLines = 6) {
  maxContextLines = Math.max(maxContextLines, 1);
  const s2 = {
    deletionLines: [],
    additionLines: [],
    conflictStack: [],
    conflictBuilders: [],
    actions: [],
    hunks: [],
    nextConflictIndex: 0,
    splitLineCount: 0,
    unifiedLineCount: 0,
    lastHunkEnd: 0,
    activeHunk: void 0,
    maxContextLines,
    maxContextLines2: maxContextLines * 2
  };
  const contents = file.contents;
  const contentLength = contents.length;
  if (contentLength > 0) {
    let lineStart = 0;
    let lineIndex = 0;
    let newlinePos = contents.indexOf("\n", lineStart);
    while (newlinePos !== -1) {
      processLine2(s2, contents.slice(lineStart, newlinePos + 1), lineIndex);
      lineStart = newlinePos + 1;
      lineIndex++;
      newlinePos = contents.indexOf("\n", lineStart);
    }
    if (lineStart < contentLength) processLine2(s2, contents.slice(lineStart), lineIndex);
  }
  if (s2.conflictStack.length > 0) throw new Error("parseMergeConflictDiffFromFile: unfinished merge conflict marker stack");
  if (s2.activeHunk != null && s2.activeHunk.hunkContent.length > 0) {
    flushBufferedContext(s2, s2.activeHunk, "trailing");
    finalizeActiveHunk(s2);
  }
  for (let conflictIndex = 0; conflictIndex < s2.conflictBuilders.length; conflictIndex++) {
    const builder = s2.conflictBuilders[conflictIndex];
    if (builder == null || !builder.completed) throw new Error(`parseMergeConflictDiffFromFile: failed to build merge conflict action ${conflictIndex}`);
  }
  if (s2.hunks.length > 0 && s2.additionLines.length > 0 && s2.deletionLines.length > 0) {
    const lastHunk = s2.hunks[s2.hunks.length - 1];
    const collapsedAfter = Math.max(s2.additionLines.length - getHunkSideEndBoundary(lastHunk.additionStart, lastHunk.additionCount), 0);
    s2.splitLineCount += collapsedAfter;
    s2.unifiedLineCount += collapsedAfter;
  }
  const currentContents = s2.deletionLines.join("");
  const incomingContents = s2.additionLines.join("");
  const currentFile = createResolvedConflictFile(file, "current", currentContents);
  const incomingFile = createResolvedConflictFile(file, "incoming", incomingContents);
  let type = "change";
  if (incomingContents === "") type = "deleted";
  else if (currentContents === "") type = "new";
  const fileDiff = {
    name: file.name,
    prevName: void 0,
    type,
    hunks: s2.hunks,
    splitLineCount: s2.splitLineCount,
    unifiedLineCount: s2.unifiedLineCount,
    isPartial: false,
    deletionLines: s2.deletionLines,
    additionLines: s2.additionLines,
    cacheKey: file.cacheKey != null ? `${file.cacheKey}:merge-conflict-diff` : void 0
  };
  return {
    fileDiff,
    currentFile,
    incomingFile,
    actions: s2.actions,
    markerRows: buildMergeConflictMarkerRows(fileDiff, s2.actions)
  };
}
function processLine2(s2, line, index) {
  const frame = s2.conflictStack[s2.conflictStack.length - 1];
  if (frame == null) {
    if (line.length >= 7 && line.charCodeAt(0) === 60 && getMergeConflictMarkerType(line) === "start") {
      handleStartMarker(s2, line, index);
      return;
    }
    emitContextLine(s2, line);
    return;
  }
  const markerType = getMergeConflictMarkerType(line);
  if (markerType === "start") {
    handleStartMarker(s2, line, index);
    return;
  }
  if (markerType === "base") {
    frame.stage = "base";
    frame.baseMarkerLineIndex = index;
    frame.markerLines.base = line;
    return;
  }
  if (markerType === "separator") {
    frame.stage = "incoming";
    frame.separatorLineIndex = index;
    frame.markerLines.separator = line;
    return;
  }
  if (markerType === "end") {
    const completedFrame = s2.conflictStack.pop();
    if (completedFrame == null) throw new Error("parseMergeConflictDiffFromFile: encountered end marker before start marker");
    finalizeConflict(s2, completedFrame, index, line);
    return;
  }
  if (frame.stage === "current") emitChangeLine(s2, "deletion", line, frame.conflictIndex, "current");
  else if (frame.stage === "base") emitContextLine(s2, line, frame.conflictIndex);
  else emitChangeLine(s2, "addition", line, frame.conflictIndex, "incoming");
}
function ensureActiveHunk(s2) {
  s2.activeHunk ?? (s2.activeHunk = createHunkBuilder(s2.additionLines.length + 1, s2.deletionLines.length + 1));
  return s2.activeHunk;
}
function assignConflictContent(s2, conflictIndex, role, contentIndex) {
  const builder = s2.conflictBuilders[conflictIndex];
  if (builder == null) throw new Error(`parseMergeConflictDiffFromFile: failed to locate conflict action ${conflictIndex}`);
  const action = builder.action;
  const hunkIndex = s2.hunks.length;
  if (action.hunkIndex < 0) action.hunkIndex = hunkIndex;
  else if (action.hunkIndex !== hunkIndex) throw new Error(`parseMergeConflictDiffFromFile: conflict ${conflictIndex} spans multiple hunks and cannot be anchored`);
  if (action.startContentIndex < 0) action.startContentIndex = contentIndex;
  action.endContentIndex = contentIndex;
  action.endMarkerContentIndex = contentIndex;
  if (role === "current") {
    action.currentContentIndex ?? (action.currentContentIndex = contentIndex);
    return;
  }
  if (role === "base") {
    action.baseContentIndex ?? (action.baseContentIndex = contentIndex);
    return;
  }
  action.incomingContentIndex = contentIndex;
}
function appendChangeLine(hunk, lineType, additionLineIndex, deletionLineIndex) {
  const hunkContent = hunk.hunkContent;
  const lastContent = hunkContent[hunkContent.length - 1];
  if ((lastContent == null ? void 0 : lastContent.type) === "change") {
    if (lineType === "addition") lastContent.additions++;
    else lastContent.deletions++;
    return hunkContent.length - 1;
  }
  hunkContent.push({
    type: "change",
    additions: lineType === "addition" ? 1 : 0,
    deletions: lineType === "deletion" ? 1 : 0,
    additionLineIndex,
    deletionLineIndex
  });
  return hunkContent.length - 1;
}
function flushBufferedContext(s2, hunk, mode) {
  let count = hunk.contextBufferCount;
  let addStart = hunk.contextBufferAdditionStart;
  let delStart = hunk.contextBufferDeletionStart;
  if (mode === "leading" && count > s2.maxContextLines) {
    const difference = count - s2.maxContextLines;
    addStart += difference;
    delStart += difference;
    count = s2.maxContextLines;
    hunk.additionStart += difference;
    hunk.deletionStart += difference;
    hunk.additionLineIndex += difference;
    hunk.deletionLineIndex += difference;
  }
  if (mode === "trailing" && count > s2.maxContextLines) count = s2.maxContextLines;
  if (count === 0) {
    hunk.contextBufferCount = 0;
    hunk.contextBufferBaseConflicts = void 0;
    return;
  }
  const hunkContent = hunk.hunkContent;
  const lastContent = hunkContent[hunkContent.length - 1];
  let contentIndex;
  if ((lastContent == null ? void 0 : lastContent.type) === "context") {
    lastContent.lines += count;
    contentIndex = hunkContent.length - 1;
  } else {
    hunkContent.push({
      type: "context",
      lines: count,
      additionLineIndex: addStart,
      deletionLineIndex: delStart
    });
    contentIndex = hunkContent.length - 1;
  }
  hunk.additionCount += count;
  hunk.deletionCount += count;
  const baseConflicts = hunk.contextBufferBaseConflicts;
  if (baseConflicts != null) {
    const bufferStartOffset = addStart - hunk.contextBufferAdditionStart;
    for (const [offset, conflictIndex] of baseConflicts) if (offset >= bufferStartOffset && offset < bufferStartOffset + count) assignConflictContent(s2, conflictIndex, "base", contentIndex);
  }
  hunk.contextBufferCount = 0;
  hunk.contextBufferBaseConflicts = void 0;
}
function finalizeActiveHunk(s2) {
  if (s2.activeHunk == null) return;
  const hunk = s2.activeHunk;
  s2.activeHunk = void 0;
  if (hunk.hunkContent.length === 0) return;
  let hunkSplitLineCount = 0;
  let hunkUnifiedLineCount = 0;
  for (const content of hunk.hunkContent) if (content.type === "context") {
    hunkSplitLineCount += content.lines;
    hunkUnifiedLineCount += content.lines;
  } else {
    hunkSplitLineCount += Math.max(content.additions, content.deletions);
    hunkUnifiedLineCount += content.additions + content.deletions;
  }
  const additionStart = hunk.additionCount === 0 ? hunk.additionStart - 1 : hunk.additionStart;
  const deletionStart = hunk.deletionCount === 0 ? hunk.deletionStart - 1 : hunk.deletionStart;
  const collapsedBefore = Math.max(getHunkSideStartBoundary(additionStart, hunk.additionCount) - s2.lastHunkEnd, 0);
  const finalizedHunk = {
    collapsedBefore,
    additionStart,
    additionCount: hunk.additionCount,
    additionLines: hunk.additionLines,
    additionLineIndex: hunk.additionLineIndex,
    deletionStart,
    deletionCount: hunk.deletionCount,
    deletionLines: hunk.deletionLines,
    deletionLineIndex: hunk.deletionLineIndex,
    hunkContent: hunk.hunkContent,
    hunkContext: void 0,
    hunkSpecs: `@@ -${formatHunkRange(deletionStart, hunk.deletionCount)} +${formatHunkRange(additionStart, hunk.additionCount)} @@
`,
    splitLineStart: s2.splitLineCount + collapsedBefore,
    splitLineCount: hunkSplitLineCount,
    unifiedLineStart: s2.unifiedLineCount + collapsedBefore,
    unifiedLineCount: hunkUnifiedLineCount,
    noEOFCRAdditions: false,
    noEOFCRDeletions: false
  };
  s2.hunks.push(finalizedHunk);
  s2.splitLineCount += collapsedBefore + hunkSplitLineCount;
  s2.unifiedLineCount += collapsedBefore + hunkUnifiedLineCount;
  s2.lastHunkEnd = getHunkSideEndBoundary(additionStart, hunk.additionCount);
}
function splitHunkWithBufferedContext(s2) {
  if (s2.activeHunk == null) return;
  const hunk = s2.activeHunk;
  const count = hunk.contextBufferCount;
  const omittedContextLineCount = count - s2.maxContextLines2;
  const nextAddStart = hunk.contextBufferAdditionStart + count - s2.maxContextLines;
  const nextDelStart = hunk.contextBufferDeletionStart + count - s2.maxContextLines;
  let nextBaseConflicts;
  if (hunk.contextBufferBaseConflicts != null) {
    const tailOffset = count - s2.maxContextLines;
    for (const [offset, ci] of hunk.contextBufferBaseConflicts) if (offset >= tailOffset) {
      nextBaseConflicts ?? (nextBaseConflicts = /* @__PURE__ */ new Map());
      nextBaseConflicts.set(offset - tailOffset, ci);
    }
  }
  flushBufferedContext(s2, hunk, "trailing");
  const emittedAdditionCount = hunk.additionCount;
  const emittedDeletionCount = hunk.deletionCount;
  finalizeActiveHunk(s2);
  s2.activeHunk = createHunkBuilder(hunk.additionStart + emittedAdditionCount + omittedContextLineCount, hunk.deletionStart + emittedDeletionCount + omittedContextLineCount);
  s2.activeHunk.contextBufferAdditionStart = nextAddStart;
  s2.activeHunk.contextBufferDeletionStart = nextDelStart;
  s2.activeHunk.contextBufferCount = s2.maxContextLines;
  s2.activeHunk.contextBufferBaseConflicts = nextBaseConflicts;
}
function emitContextLine(s2, line, baseConflictIndex = -1) {
  const hunk = ensureActiveHunk(s2);
  if (hunk.contextBufferCount === 0) {
    hunk.contextBufferAdditionStart = s2.additionLines.length;
    hunk.contextBufferDeletionStart = s2.deletionLines.length;
  }
  s2.additionLines.push(line);
  s2.deletionLines.push(line);
  if (baseConflictIndex >= 0) {
    hunk.contextBufferBaseConflicts ?? (hunk.contextBufferBaseConflicts = /* @__PURE__ */ new Map());
    hunk.contextBufferBaseConflicts.set(hunk.contextBufferCount, baseConflictIndex);
  }
  hunk.contextBufferCount++;
}
function emitChangeLine(s2, lineType, line, conflictIndex, role) {
  let hunk = ensureActiveHunk(s2);
  if (hunk.hunkContent.length > 0 && hunk.contextBufferCount > s2.maxContextLines2) {
    splitHunkWithBufferedContext(s2);
    hunk = s2.activeHunk;
  }
  flushBufferedContext(s2, hunk, hunk.hunkContent.length === 0 ? "leading" : "before-change");
  const additionLineIndex = s2.additionLines.length;
  const deletionLineIndex = s2.deletionLines.length;
  if (lineType === "addition") s2.additionLines.push(line);
  else s2.deletionLines.push(line);
  const contentIndex = appendChangeLine(hunk, lineType, additionLineIndex, deletionLineIndex);
  if (lineType === "addition") {
    hunk.additionCount++;
    hunk.additionLines++;
  } else {
    hunk.deletionCount++;
    hunk.deletionLines++;
  }
  assignConflictContent(s2, conflictIndex, role, contentIndex);
}
function finalizeConflict(s2, frame, endLineIndex, endMarkerLine) {
  if (frame.separatorLineIndex == null || frame.markerLines.separator == null) throw new Error(`parseMergeConflictDiffFromFile: conflict ${frame.conflictIndex} is missing a separator marker`);
  const builder = s2.conflictBuilders[frame.conflictIndex];
  if (builder == null) throw new Error(`parseMergeConflictDiffFromFile: failed to finalize conflict ${frame.conflictIndex}`);
  const action = builder.action;
  action.markerLines.separator = frame.markerLines.separator;
  action.markerLines.end = endMarkerLine;
  if (frame.markerLines.base != null) action.markerLines.base = frame.markerLines.base;
  action.conflict = {
    conflictIndex: frame.conflictIndex,
    startLineIndex: frame.startLineIndex,
    startLineNumber: frame.startLineIndex + 1,
    separatorLineIndex: frame.separatorLineIndex,
    separatorLineNumber: frame.separatorLineIndex + 1,
    endLineIndex,
    endLineNumber: endLineIndex + 1,
    baseMarkerLineIndex: frame.baseMarkerLineIndex,
    baseMarkerLineNumber: frame.baseMarkerLineIndex != null ? frame.baseMarkerLineIndex + 1 : void 0
  };
  const fallbackContentIndex = action.currentContentIndex ?? action.incomingContentIndex;
  action.currentContentIndex ?? (action.currentContentIndex = fallbackContentIndex);
  action.incomingContentIndex ?? (action.incomingContentIndex = fallbackContentIndex);
  if (action.startContentIndex < 0 && fallbackContentIndex != null) action.startContentIndex = fallbackContentIndex;
  if (action.endContentIndex < 0 && fallbackContentIndex != null) action.endContentIndex = fallbackContentIndex;
  if (action.endMarkerContentIndex < 0 && fallbackContentIndex != null) action.endMarkerContentIndex = fallbackContentIndex;
  if (action.hunkIndex < 0 || action.startContentIndex < 0 || action.endContentIndex < 0 || action.endMarkerContentIndex < 0) throw new Error(`parseMergeConflictDiffFromFile: failed to anchor merge conflict ${frame.conflictIndex}`);
  s2.actions[action.conflictIndex] = action;
  builder.completed = true;
}
function handleStartMarker(s2, line, lineIndex) {
  const conflictIndex = s2.nextConflictIndex;
  s2.nextConflictIndex++;
  s2.conflictStack.push({
    conflictIndex,
    stage: "current",
    startLineIndex: lineIndex,
    markerLines: { start: line }
  });
  s2.conflictBuilders[conflictIndex] = {
    completed: false,
    action: {
      conflict: {
        conflictIndex,
        startLineIndex: lineIndex,
        startLineNumber: lineIndex + 1,
        separatorLineIndex: lineIndex,
        separatorLineNumber: lineIndex + 1,
        endLineIndex: lineIndex,
        endLineNumber: lineIndex + 1,
        baseMarkerLineIndex: void 0,
        baseMarkerLineNumber: void 0
      },
      conflictIndex,
      hunkIndex: -1,
      startContentIndex: -1,
      endContentIndex: -1,
      endMarkerContentIndex: -1,
      markerLines: {
        start: line,
        separator: "",
        end: ""
      }
    }
  };
}
function createHunkBuilder(additionStart, deletionStart) {
  return {
    additionStart,
    deletionStart,
    additionCount: 0,
    deletionCount: 0,
    additionLines: 0,
    deletionLines: 0,
    additionLineIndex: Math.max(additionStart - 1, 0),
    deletionLineIndex: Math.max(deletionStart - 1, 0),
    hunkContent: [],
    contextBufferAdditionStart: Math.max(additionStart - 1, 0),
    contextBufferDeletionStart: Math.max(deletionStart - 1, 0),
    contextBufferCount: 0,
    contextBufferBaseConflicts: void 0
  };
}
function formatHunkRange(start, count) {
  return count === 1 ? `${start}` : `${start},${count}`;
}
function getMergeConflictMarkerType(line) {
  if (line.length < 7) return;
  const markerCode = line.charCodeAt(0);
  if (markerCode !== 60 && markerCode !== 62 && markerCode !== 61 && markerCode !== 124) return;
  const lineEnd = getLineContentEndIndex(line);
  if (lineEnd < 7) return;
  let markerLength = 1;
  while (markerLength < lineEnd && line.charCodeAt(markerLength) === markerCode) markerLength++;
  if (markerLength < 7) return;
  if (markerCode === 61) return markerLength === lineEnd ? "separator" : void 0;
  if (markerLength !== lineEnd && !isWhitespaceCode(line.charCodeAt(markerLength))) return;
  if (markerCode === 60) return "start";
  if (markerCode === 62) return "end";
  return "base";
}
function getLineContentEndIndex(line) {
  let end = line.length;
  if (end > 0 && line.charCodeAt(end - 1) === 10) end--;
  if (end > 0 && line.charCodeAt(end - 1) === 13) end--;
  return end;
}
function isWhitespaceCode(code) {
  return code === 9 || code === 10 || code === 11 || code === 12 || code === 13 || code === 32;
}
function createResolvedConflictFile(file, side, contents) {
  return {
    ...file,
    contents,
    cacheKey: file.cacheKey != null ? `${file.cacheKey}:merge-conflict-${side}` : void 0
  };
}
function buildMergeConflictMarkerRows(fileDiff, actions) {
  const markerRows = [];
  const hunkLineStartCache = new Array(fileDiff.hunks.length);
  const getLineStart = (hunkIndex, contentIndex) => {
    const hunk = fileDiff.hunks[hunkIndex];
    if (hunk == null) return 0;
    let starts = hunkLineStartCache[hunkIndex];
    if (starts == null) {
      starts = new Array(hunk.hunkContent.length + 1);
      let lineIndex = hunk.unifiedLineStart;
      starts[0] = lineIndex;
      for (let index = 0; index < hunk.hunkContent.length; index++) {
        const content = hunk.hunkContent[index];
        lineIndex += content.type === "context" ? content.lines : content.deletions + content.additions;
        starts[index + 1] = lineIndex;
      }
      hunkLineStartCache[hunkIndex] = starts;
    }
    return starts[Math.max(contentIndex, 0)] ?? hunk.unifiedLineStart;
  };
  const getLineEnd = (hunkIndex, contentIndex) => {
    var _a9;
    const lineStart = getLineStart(hunkIndex, contentIndex);
    const lineEndExclusive = ((_a9 = hunkLineStartCache[hunkIndex]) == null ? void 0 : _a9[Math.max(contentIndex + 1, 0)]) ?? getLineStart(hunkIndex, contentIndex + 1);
    return Math.max(lineStart, lineEndExclusive - 1);
  };
  for (const action of actions) {
    if (action == null) continue;
    const hunk = fileDiff.hunks[action.hunkIndex];
    if (hunk == null) continue;
    const actionLineIndex = getLineStart(action.hunkIndex, action.startContentIndex);
    markerRows.push(createMergeConflictMarkerRow(action, "marker-start", action.startContentIndex, action.markerLines.start, actionLineIndex));
    if (action.baseContentIndex != null) {
      const currentContentIndex2 = action.currentContentIndex;
      const incomingContentIndex = action.incomingContentIndex;
      if (currentContentIndex2 == null || incomingContentIndex == null) continue;
      const baseMarkerLine = action.markerLines.base;
      if (baseMarkerLine == null) continue;
      const currentChange = hunk.hunkContent[currentContentIndex2];
      const baseContext = hunk.hunkContent[action.baseContentIndex];
      const incomingChange = hunk.hunkContent[incomingContentIndex];
      if ((currentChange == null ? void 0 : currentChange.type) !== "change" || (baseContext == null ? void 0 : baseContext.type) !== "context" || (incomingChange == null ? void 0 : incomingChange.type) !== "change") continue;
      const currentStart = getLineStart(action.hunkIndex, currentContentIndex2);
      const incomingStart = getLineStart(action.hunkIndex, incomingContentIndex);
      markerRows.push(createMergeConflictMarkerRow(action, "marker-base", action.baseContentIndex, baseMarkerLine, currentStart + currentChange.deletions));
      markerRows.push(createMergeConflictMarkerRow(action, "marker-separator", action.baseContentIndex, action.markerLines.separator, incomingStart), createMergeConflictMarkerRow(action, "marker-end", action.endMarkerContentIndex, action.markerLines.end, getLineEnd(action.hunkIndex, action.endMarkerContentIndex)));
      continue;
    }
    const currentContentIndex = action.currentContentIndex;
    if (currentContentIndex == null) continue;
    const content = hunk.hunkContent[currentContentIndex];
    if ((content == null ? void 0 : content.type) !== "change") continue;
    const contentStart = getLineStart(action.hunkIndex, currentContentIndex);
    const separatorLineIndex = content.deletions > 0 ? contentStart + content.deletions : actionLineIndex;
    markerRows.push(createMergeConflictMarkerRow(action, "marker-separator", currentContentIndex, action.markerLines.separator, separatorLineIndex), createMergeConflictMarkerRow(action, "marker-end", action.endMarkerContentIndex, action.markerLines.end, getLineEnd(action.hunkIndex, action.endMarkerContentIndex)));
  }
  return markerRows;
}
function createMergeConflictMarkerRow(action, type, contentIndex, lineText, lineIndex) {
  return {
    type,
    hunkIndex: action.hunkIndex,
    contentIndex,
    conflictIndex: action.conflictIndex,
    lineText,
    lineIndex
  };
}
function getUnifiedLineStartForContent(hunk, contentIndex) {
  let lineIndex = hunk.unifiedLineStart;
  for (let index = 0; index < contentIndex; index++) {
    const content = hunk.hunkContent[index];
    lineIndex += content.type === "context" ? content.lines : content.deletions + content.additions;
  }
  return lineIndex;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/renderers/UnresolvedFileHunksRenderer.js
var UnresolvedFileHunksRenderer = class extends DiffHunksRenderer {
  constructor(options = { theme: DEFAULT_THEMES }, onRenderUpdate, workerManager) {
    super(void 0, onRenderUpdate, workerManager);
    __publicField(this, "pendingConflictActions", []);
    __publicField(this, "pendingMarkerRows", []);
    __publicField(this, "injectedRows", /* @__PURE__ */ new Map());
    __publicField(this, "options");
    __publicField(this, "getUnifiedInjectedRowsForLine", (ctx) => {
      const rows = this.injectedRows.get(`${ctx.hunkIndex}:${ctx.lineIndex}`);
      if (rows == null || rows.length === 0) return;
      const { mergeConflictActionsType } = this.getOptionsWithDefaults();
      const before = [];
      const after = [];
      for (const row of rows) {
        if (row.type === "actions") {
          before.push({
            content: createMergeConflictActionsRowElement({
              row,
              includeDefaultActions: mergeConflictActionsType === "default",
              includeSlot: true
            }),
            gutter: createMergeConflictGutterGap("action")
          });
          continue;
        }
        (row.type === "marker-end" ? after : before).push({
          content: createMergeConflictMarkerRowElement(row),
          gutter: createMergeConflictGutterGap("marker", row.type)
        });
      }
      return {
        before: before.length > 0 ? before : void 0,
        after: after.length > 0 ? after : void 0
      };
    });
    this.options = options;
  }
  setConflictState(conflictActions, markerRows, diff) {
    this.pendingConflictActions = conflictActions;
    this.pendingMarkerRows = markerRows;
    this.syncInjectedRows(conflictActions, markerRows, diff);
  }
  syncInjectedRows(conflictActions, markerRows, diff) {
    this.injectedRows.clear();
    for (const action of conflictActions) {
      const anchor = action != null ? getMergeConflictActionAnchor(action, diff) : void 0;
      if (action == null || anchor == null) continue;
      const row = {
        type: "actions",
        hunkIndex: anchor.hunkIndex,
        lineIndex: anchor.lineIndex,
        conflictIndex: action.conflictIndex
      };
      this.addInjectedRow(row);
    }
    for (const row of markerRows) this.addInjectedRow(row);
  }
  addInjectedRow(row) {
    const key2 = `${row.hunkIndex}:${row.lineIndex}`;
    const rows = this.injectedRows.get(key2);
    if (rows == null) this.injectedRows.set(key2, [row]);
    else rows.push(row);
  }
  renderDiff(diff, renderRange = DEFAULT_RENDER_RANGE) {
    if (diff != null) this.syncInjectedRows(this.pendingConflictActions, this.pendingMarkerRows, diff);
    return super.renderDiff(diff, renderRange);
  }
  async asyncRender(diff, renderRange = DEFAULT_RENDER_RANGE) {
    this.syncInjectedRows(this.pendingConflictActions, this.pendingMarkerRows, diff);
    return super.asyncRender(diff, renderRange);
  }
  createPreElement(split, totalLines) {
    return super.createPreElement(split, totalLines, { "data-has-merge-conflict": "" });
  }
  getUnifiedLineDecoration({ type, lineType }) {
    const mergeConflictType = type === "change" ? lineType === "change-deletion" ? "current" : "incoming" : void 0;
    return {
      gutterLineType: type === "change" ? "context" : lineType,
      gutterProperties: getMergeConflictGutterProperties(mergeConflictType),
      contentProperties: getMergeConflictContentProperties(type, mergeConflictType)
    };
  }
  getSplitLineDecoration({ side, type }) {
    const mergeConflictType = type === "change" ? side === "deletions" ? "current" : "incoming" : void 0;
    return {
      gutterLineType: type === "change" ? "context" : type,
      gutterProperties: getMergeConflictGutterProperties(mergeConflictType),
      contentProperties: getMergeConflictContentProperties(type, mergeConflictType)
    };
  }
  getOptionsWithDefaults() {
    const options = super.getOptionsWithDefaults();
    options.diffStyle = "unified";
    options.lineDiffType = "none";
    options.mergeConflictActionsType = this.options.mergeConflictActionsType ?? "default";
    return options;
  }
};
function getMergeConflictGutterProperties(mergeConflictType) {
  return mergeConflictType != null ? { "data-merge-conflict": mergeConflictType } : void 0;
}
function getMergeConflictContentProperties(type, mergeConflictType) {
  if (mergeConflictType == null) return;
  if (type === "change") {
    if (mergeConflictType === "current" || mergeConflictType === "incoming") return {
      "data-line-type": "context",
      "data-merge-conflict": mergeConflictType
    };
    return;
  }
  if (mergeConflictType === "marker-start" || mergeConflictType === "marker-base" || mergeConflictType === "marker-separator" || mergeConflictType === "marker-end") return { "data-merge-conflict": mergeConflictType };
}
function createMergeConflictGutterGap(type, markerType) {
  const gap = createGutterGap(void 0, "annotation", 1);
  gap.properties["data-gutter-buffer"] = type === "action" ? "merge-conflict-action" : `merge-conflict-${markerType ?? "marker"}`;
  return gap;
}
function createMergeConflictActionsRowElement({ row, includeDefaultActions, includeSlot }) {
  const contentChildren = includeDefaultActions ? createMergeConflictActionsContent(row.conflictIndex) : [];
  if (includeSlot) contentChildren.push(createHastElement({
    tagName: "slot",
    properties: {
      name: getMergeConflictActionSlotName({
        hunkIndex: row.hunkIndex,
        lineIndex: row.lineIndex,
        conflictIndex: row.conflictIndex
      }),
      "data-merge-conflict-action-slot": ""
    }
  }));
  return createHastElement({
    tagName: "div",
    properties: { "data-merge-conflict-actions": "" },
    children: [createHastElement({
      tagName: "div",
      properties: { "data-merge-conflict-actions-content": "" },
      children: contentChildren
    })]
  });
}
function createMergeConflictMarkerRowElement(row) {
  return createHastElement({
    tagName: "div",
    properties: {
      "data-merge-conflict": row.type,
      "data-merge-conflict-marker-row": ""
    },
    children: [createTextNodeElement(row.lineText.replace(/(?:\r\n|\n|\r)$/, ""))]
  });
}
function createMergeConflictActionsContent(conflictIndex) {
  return [
    createMergeConflictActionButton({
      resolution: "current",
      label: "Accept current change",
      conflictIndex
    }),
    createMergeConflictActionSeparator(),
    createMergeConflictActionButton({
      resolution: "incoming",
      label: "Accept incoming change",
      conflictIndex
    }),
    createMergeConflictActionSeparator(),
    createMergeConflictActionButton({
      resolution: "both",
      label: "Accept both",
      conflictIndex
    })
  ];
}
function createMergeConflictActionButton({ resolution, label, conflictIndex }) {
  return createHastElement({
    tagName: "button",
    properties: {
      type: "button",
      "data-merge-conflict-action": resolution,
      "data-merge-conflict-conflict-index": `${conflictIndex}`
    },
    children: [createTextNodeElement(label)]
  });
}
function createMergeConflictActionSeparator() {
  return createHastElement({
    tagName: "span",
    properties: { "data-merge-conflict-action-separator": "" },
    children: [createTextNodeElement("|")]
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areMergeConflictActionsEqual.js
function areMergeConflictActionsEqual(a2, b3) {
  return a2.hunkIndex === b3.hunkIndex && a2.startContentIndex === b3.startContentIndex && a2.endContentIndex === b3.endContentIndex && a2.currentContentIndex === b3.currentContentIndex && a2.baseContentIndex === b3.baseContentIndex && a2.incomingContentIndex === b3.incomingContentIndex && a2.endMarkerContentIndex === b3.endMarkerContentIndex && a2.conflictIndex === b3.conflictIndex && areConflictsEqual(a2.conflict, b3.conflict);
}
function areConflictsEqual(a2, b3) {
  return a2.conflictIndex === b3.conflictIndex && a2.startLineIndex === b3.startLineIndex && a2.startLineNumber === b3.startLineNumber && a2.separatorLineIndex === b3.separatorLineIndex && a2.separatorLineNumber === b3.separatorLineNumber && a2.endLineIndex === b3.endLineIndex && a2.endLineNumber === b3.endLineNumber && a2.baseMarkerLineIndex === b3.baseMarkerLineIndex && a2.baseMarkerLineNumber === b3.baseMarkerLineNumber;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/UnresolvedFile.js
var instanceId8 = -1;
var UnresolvedFile = class extends FileDiff {
  constructor(options = { theme: DEFAULT_THEMES }, workerManager, isContainerManaged = false) {
    super(void 0, workerManager, isContainerManaged);
    __publicField(this, "options");
    __publicField(this, "__id", `unresolved-file:${++instanceId8}`);
    __publicField(this, "type", "unresolved-file");
    __publicField(this, "computedCache", {
      file: void 0,
      fileDiff: void 0,
      actions: void 0,
      markerRows: void 0
    });
    __publicField(this, "conflictActions", []);
    __publicField(this, "markerRows", []);
    __publicField(this, "conflictActionCache", /* @__PURE__ */ new Map());
    __publicField(this, "handleMergeConflictActionClick", (target) => {
      const action = this.conflictActions[target.conflictIndex];
      if (action == null) return;
      if (action.conflictIndex !== target.conflictIndex) {
        console.error({
          conflictIndex: target.conflictIndex,
          action
        });
        throw new Error("UnresolvedFile.handleMergeConflictActionClick: conflictIndex and conflictAction don't match");
      }
      const payload = {
        resolution: target.resolution,
        conflict: action.conflict
      };
      if (this.options.onMergeConflictAction != null) {
        this.options.onMergeConflictAction(payload, this);
        return;
      }
      this.resolveConflictAndRender(target.conflictIndex, target.resolution);
    });
    this.options = options;
    this.setOptions(options);
  }
  setOptions(options) {
    if (options == null) return;
    if (options.onMergeConflictAction != null && options.onMergeConflictResolve != null) throw new Error("UnresolvedFile: onMergeConflictAction and onMergeConflictResolve are mutually exclusive. Use only one callback.");
    this.options = options;
    this.hunksRenderer.setOptions(this.getHunksRendererOptions(options));
    this.syncInteractionOptions();
  }
  syncInteractionOptions() {
    this.interactionManager.setOptions(pluckInteractionOptions(this.options, typeof this.options.hunkSeparators === "function" || (this.options.hunkSeparators ?? "line-info") === "line-info" || this.options.hunkSeparators === "line-info-basic" ? this.handleExpandHunk : void 0, this.getLineIndex, this.handleMergeConflictActionClick));
  }
  createHunksRenderer(options) {
    return new UnresolvedFileHunksRenderer(this.getHunksRendererOptions(options), this.handleHighlightRender, this.workerManager);
  }
  getHunksRendererOptions(options) {
    return getUnresolvedDiffHunksRendererOptions(options, this.options);
  }
  applyPreNodeAttributes(pre, result) {
    super.applyPreNodeAttributes(pre, result, { "data-has-merge-conflict": "" });
  }
  cleanUp() {
    this.emitPostRender(true);
    this.clearMergeConflictActionCache();
    this.computedCache = {
      file: void 0,
      fileDiff: void 0,
      actions: void 0,
      markerRows: void 0
    };
    this.conflictActions = [];
    super.cleanUp();
  }
  getOrComputeDiff({ file, fileDiff, actions, markerRows }) {
    var _a9;
    const { maxContextLines, onMergeConflictAction } = this.options;
    wrapper: if (onMergeConflictAction != null) {
      const hasFileDiff = fileDiff != null;
      if (hasFileDiff !== (actions != null) || hasFileDiff !== (markerRows != null)) throw new Error("UnresolvedFile.getOrComputeDiff: fileDiff, actions, and markerRows must be passed together");
      if (fileDiff != null && actions != null && markerRows != null) {
        this.computedCache = {
          file: file ?? this.computedCache.file,
          fileDiff,
          actions,
          markerRows
        };
        break wrapper;
      } else if (file != null || this.computedCache.file != null) {
        if (file != null && this.computedCache.file != null && !areFilesEqual(file, this.computedCache.file) && this.computedCache.fileDiff != null && this.computedCache.actions != null) throw new Error("UnresolvedFile.getOrComputeDiff: file can only be used to initialize unresolved state once. Pass fileDiff and actions for subsequent updates.");
        file ?? (file = this.computedCache.file);
        if (file == null) throw new Error("UnresolvedFile.getOrComputeDiff: file is null, should be impossible");
        if (!areFilesEqual(file, this.computedCache.file) || this.computedCache.fileDiff == null || this.computedCache.actions == null) {
          const computed = parseMergeConflictDiffFromFile(file, maxContextLines);
          this.computedCache = {
            file,
            fileDiff: computed.fileDiff,
            actions: computed.actions,
            markerRows: computed.markerRows
          };
        }
        fileDiff = this.computedCache.fileDiff;
        actions = this.computedCache.actions;
        markerRows = this.computedCache.markerRows;
        break wrapper;
      } else {
        fileDiff = this.computedCache.fileDiff;
        actions = this.computedCache.actions;
        markerRows = this.computedCache.markerRows;
        break wrapper;
      }
    } else {
      if (fileDiff != null || actions != null || markerRows != null) throw new Error("UnresolvedFile.getOrComputeDiff: fileDiff, actions, and markerRows are only usable in controlled mode, you must pass in `onMergeConflictAction`");
      if (file != null && this.computedCache.file != null && !areFilesEqual(file, this.computedCache.file)) throw new Error("UnresolvedFile.getOrComputeDiff: uncontrolled unresolved files parse the file only once. Later updates must come from the cached diff state.");
      (_a9 = this.computedCache).file ?? (_a9.file = file);
      if (this.computedCache.fileDiff == null && this.computedCache.file != null) {
        const computed = parseMergeConflictDiffFromFile(this.computedCache.file, maxContextLines);
        this.computedCache.fileDiff = computed.fileDiff;
        this.computedCache.actions = computed.actions;
        this.computedCache.markerRows = computed.markerRows;
      }
      fileDiff = this.computedCache.fileDiff;
      actions = this.computedCache.actions;
      markerRows = this.computedCache.markerRows;
      break wrapper;
    }
    if (fileDiff == null || actions == null || markerRows == null) return;
    return {
      fileDiff,
      actions,
      markerRows
    };
  }
  hydrate(props) {
    const { file, fileDiff, actions, markerRows, lineAnnotations, fileContainer, prerenderedHTML, preventEmit = false } = props;
    const source = this.getOrComputeDiff({
      file,
      fileDiff,
      actions,
      markerRows
    });
    if (source == null) return;
    this.hydrateElements(fileContainer, prerenderedHTML);
    this.setActiveMergeConflictState(source.actions, source.markerRows);
    if (shouldRenderCode3(this.pre, source.fileDiff, this.options.collapsed) || shouldRenderHeader3(this.headerElement, source.fileDiff, this.options.disableFileHeader)) this.render({
      ...props,
      preventEmit: true
    });
    else {
      this.hydrationSetup({
        fileDiff: source.fileDiff,
        lineAnnotations
      });
      if (this.pre != null) this.renderMergeConflictActionSlots();
    }
    if (!preventEmit) this.emitPostRender();
  }
  rerender() {
    if (!this.enabled || this.fileDiff == null) return;
    this.render({
      forceRender: true,
      renderRange: this.renderRange
    });
  }
  render(props = {}) {
    const { file, fileDiff, actions, markerRows, lineAnnotations, preventEmit = false, ...rest } = props;
    const source = this.getOrComputeDiff({
      file,
      fileDiff,
      actions,
      markerRows
    });
    if (source == null) return false;
    this.setActiveMergeConflictState(source.actions, source.markerRows);
    const didRender = super.render({
      ...rest,
      fileDiff: source.fileDiff,
      lineAnnotations,
      preventEmit: true
    });
    if (didRender) {
      this.renderMergeConflictActionSlots();
      if (!preventEmit) this.emitPostRender();
    }
    return didRender;
  }
  resolveConflict(conflictIndex, resolution, fileDiff = this.computedCache.fileDiff) {
    const action = this.conflictActions[conflictIndex];
    if (fileDiff == null || action == null) return;
    if (action.conflictIndex !== conflictIndex) {
      console.error({
        conflictIndex,
        action
      });
      throw new Error("UnresolvedFile.resolveConflict: conflictIndex and conflictAction don't match");
    }
    const newFileDiff = resolveConflict(fileDiff, action, resolution);
    const previousFile = this.computedCache.file;
    const { file, actions, markerRows } = rebuildFileAndActions({
      fileDiff: newFileDiff,
      previousActions: this.conflictActions,
      resolvedConflictIndex: conflictIndex,
      previousFile,
      resolution
    });
    return {
      file,
      fileDiff: newFileDiff,
      actions,
      markerRows
    };
  }
  resolveConflictAndRender(conflictIndex, resolution) {
    var _a9, _b;
    const action = this.conflictActions[conflictIndex];
    if (action == null) return;
    if (action.conflictIndex !== conflictIndex) {
      console.error({
        conflictIndex,
        action
      });
      throw new Error("UnresolvedFile.resolveConflictAndRender: conflictIndex and conflictAction don't match");
    }
    const payload = {
      resolution,
      conflict: action.conflict
    };
    const { file, fileDiff, actions, markerRows } = this.resolveConflict(conflictIndex, resolution) ?? {};
    if (file == null || fileDiff == null || actions == null || markerRows == null) return;
    this.computedCache = {
      file,
      fileDiff,
      actions,
      markerRows
    };
    this.setActiveMergeConflictState(actions, markerRows);
    if (this.workerManager != null) this.hunksRenderer.renderDiff(fileDiff);
    else this.render({ forceRender: true });
    (_b = (_a9 = this.options).onMergeConflictResolve) == null ? void 0 : _b.call(_a9, file, payload);
  }
  setActiveMergeConflictState(actions = this.conflictActions, markerRows = this.markerRows) {
    this.conflictActions = actions;
    this.markerRows = markerRows;
    if (this.computedCache.fileDiff != null && this.hunksRenderer instanceof UnresolvedFileHunksRenderer) this.hunksRenderer.setConflictState(this.options.mergeConflictActionsType === "none" ? [] : actions, markerRows, this.computedCache.fileDiff);
  }
  renderMergeConflictActionSlots() {
    const { fileDiff } = this.computedCache;
    if (this.isContainerManaged || this.fileContainer == null || typeof this.options.mergeConflictActionsType !== "function" || this.conflictActions.length === 0 || fileDiff == null) {
      this.clearMergeConflictActionCache();
      return;
    }
    const staleActions = new Map(this.conflictActionCache);
    for (let actionIndex = 0; actionIndex < this.conflictActions.length; actionIndex++) {
      const action = this.conflictActions[actionIndex];
      if (action == null) continue;
      if (action.conflictIndex !== actionIndex) {
        console.error({
          conflictIndex: actionIndex,
          action
        });
        throw new Error("UnresolvedFile.renderMergeConflictActionSlots: conflictIndex and conflictAction don't match");
      }
      const anchor = getMergeConflictActionAnchor(action, fileDiff);
      if (anchor == null) continue;
      const conflictIndex = action.conflictIndex;
      const slotName = getMergeConflictActionSlotName({
        hunkIndex: anchor.hunkIndex,
        lineIndex: anchor.lineIndex,
        conflictIndex
      });
      const id = `${actionIndex}-${slotName}`;
      let cache2 = this.conflictActionCache.get(id);
      if (cache2 == null || !areMergeConflictActionsEqual(cache2.action, action)) {
        cache2 == null ? void 0 : cache2.element.remove();
        const rendered = this.renderMergeConflictAction(action);
        if (rendered == null) continue;
        const element2 = createAnnotationWrapperNode(slotName);
        element2.appendChild(rendered);
        this.fileContainer.appendChild(element2);
        cache2 = {
          element: element2,
          action
        };
        this.conflictActionCache.set(id, cache2);
      }
      staleActions.delete(id);
    }
    for (const [id, { element: element2 }] of staleActions.entries()) {
      this.conflictActionCache.delete(id);
      element2.remove();
    }
  }
  renderMergeConflictAction(action) {
    if (typeof this.options.mergeConflictActionsType !== "function") return;
    const rendered = this.options.mergeConflictActionsType(action, this);
    if (rendered == null) return;
    if (rendered instanceof HTMLElement) return rendered;
    if (typeof DocumentFragment !== "undefined" && rendered instanceof DocumentFragment) {
      const wrapper = document.createElement("div");
      wrapper.style.display = "contents";
      wrapper.appendChild(rendered);
      return wrapper;
    }
  }
  clearMergeConflictActionCache() {
    for (const { element: element2 } of this.conflictActionCache.values()) element2.remove();
    this.conflictActionCache.clear();
  }
};
function rebuildFileAndActions({ fileDiff, previousActions, resolvedConflictIndex, previousFile, resolution }) {
  const resolvedAction = previousActions[resolvedConflictIndex];
  if (resolvedAction == null) throw new Error("rebuildFileAndActions: missing resolved action for unresolved file rebuild");
  const actions = updateConflictActionsAfterResolution(previousActions, resolvedConflictIndex, resolvedAction, resolution);
  const markerRows = buildMergeConflictMarkerRows(fileDiff, actions);
  return {
    file: rebuildUnresolvedFile({
      fileDiff,
      resolvedAction,
      resolvedConflictIndex,
      previousFile,
      resolution
    }),
    actions,
    markerRows
  };
}
function rebuildUnresolvedFile({ resolvedAction, resolvedConflictIndex, previousFile, fileDiff, resolution }) {
  const lines = splitFileContents2((previousFile == null ? void 0 : previousFile.contents) ?? "");
  const { conflict } = resolvedAction;
  const replacementLines = getResolvedConflictReplacementLines(lines, conflict, resolution);
  const contents = [
    ...lines.slice(0, conflict.startLineIndex),
    ...replacementLines,
    ...lines.slice(conflict.endLineIndex + 1)
  ].join("");
  return {
    name: (previousFile == null ? void 0 : previousFile.name) ?? fileDiff.name,
    contents,
    cacheKey: (previousFile == null ? void 0 : previousFile.cacheKey) != null ? `${previousFile.cacheKey}:mc-${resolvedConflictIndex}-${resolution}` : void 0
  };
}
function getResolvedConflictReplacementLines(lines, conflict, resolution) {
  const currentLines = lines.slice(conflict.startLineIndex + 1, conflict.baseMarkerLineIndex ?? conflict.separatorLineIndex);
  const incomingLines = lines.slice(conflict.separatorLineIndex + 1, conflict.endLineIndex);
  if (resolution === "current") return currentLines;
  if (resolution === "incoming") return incomingLines;
  return [...currentLines, ...incomingLines];
}
function updateConflictActionsAfterResolution(previousActions, resolvedConflictIndex, resolvedAction, resolution) {
  const lineDelta = getResolvedConflictLineDelta(resolvedAction.conflict, resolution);
  return previousActions.map((action, index) => {
    if (index === resolvedConflictIndex) return;
    if (action == null) return;
    if (action.conflict.startLineIndex > resolvedAction.conflict.endLineIndex) return {
      ...action,
      conflict: shiftMergeConflictRegion(action.conflict, lineDelta)
    };
    return action;
  });
}
function getResolvedConflictLineDelta(conflict, resolution) {
  const currentLineCount = (conflict.baseMarkerLineIndex ?? conflict.separatorLineIndex) - conflict.startLineIndex - 1;
  const incomingLineCount = conflict.endLineIndex - conflict.separatorLineIndex - 1;
  return (resolution === "current" ? currentLineCount : resolution === "incoming" ? incomingLineCount : currentLineCount + incomingLineCount) - (conflict.endLineIndex - conflict.startLineIndex + 1);
}
function shiftMergeConflictRegion(conflict, lineDelta) {
  return {
    ...conflict,
    startLineIndex: conflict.startLineIndex + lineDelta,
    startLineNumber: conflict.startLineNumber + lineDelta,
    separatorLineIndex: conflict.separatorLineIndex + lineDelta,
    separatorLineNumber: conflict.separatorLineNumber + lineDelta,
    endLineIndex: conflict.endLineIndex + lineDelta,
    endLineNumber: conflict.endLineNumber + lineDelta,
    baseMarkerLineIndex: conflict.baseMarkerLineIndex != null ? conflict.baseMarkerLineIndex + lineDelta : void 0,
    baseMarkerLineNumber: conflict.baseMarkerLineNumber != null ? conflict.baseMarkerLineNumber + lineDelta : void 0
  };
}
function shouldRenderCode3(pre, fileDiff, collapsed = false) {
  return !collapsed && pre == null && fileDiff != null;
}
function shouldRenderHeader3(headerElement, fileDiff, disableFileHeader = false) {
  return headerElement == null && fileDiff != null && !disableFileHeader;
}
function getUnresolvedDiffHunksRendererOptions(options, baseOptions) {
  const merged = {
    ...baseOptions,
    ...options
  };
  return {
    ...merged,
    useTokenTransformer: shouldUseTokenTransformer(merged),
    hunkSeparators: typeof (options == null ? void 0 : options.hunkSeparators) === "function" ? "custom" : options == null ? void 0 : options.hunkSeparators,
    mergeConflictActionsType: typeof (options == null ? void 0 : options.mergeConflictActionsType) === "function" ? "custom" : options == null ? void 0 : options.mergeConflictActionsType
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areVirtualWindowSpecsEqual.js
function areVirtualWindowSpecsEqual(windowSpecsA, windowSpecsB) {
  if (windowSpecsA == null || windowSpecsB == null) return windowSpecsA === windowSpecsB;
  return windowSpecsA.top === windowSpecsB.top && windowSpecsA.bottom === windowSpecsB.bottom;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/components/Virtualizer.js
var DEFAULT_OVERSCROLL_SIZE = 1e3;
var INTERSECTION_OBSERVER_MARGIN = DEFAULT_OVERSCROLL_SIZE * 4;
var INTERSECTION_OBSERVER_THRESHOLD = [
  0,
  1e-6,
  0.99999,
  1
];
var DEFAULT_VIRTUALIZER_CONFIG = {
  overscrollSize: DEFAULT_OVERSCROLL_SIZE,
  intersectionObserverMargin: INTERSECTION_OBSERVER_MARGIN,
  resizeDebugging: false
};
var lastSize = 0;
var instance = -1;
var _a8;
var Virtualizer = (_a8 = class {
  constructor(config) {
    __publicField(this, "__id", `virtualizer-${++instance}`);
    __publicField(this, "config");
    __publicField(this, "type", "simple");
    __publicField(this, "intersectionObserver");
    __publicField(this, "scrollTop", 0);
    __publicField(this, "height", 0);
    __publicField(this, "scrollHeight", 0);
    __publicField(this, "windowSpecs", {
      top: 0,
      bottom: 0
    });
    __publicField(this, "root");
    __publicField(this, "contentContainer");
    __publicField(this, "resizeObserver");
    __publicField(this, "observers", /* @__PURE__ */ new Map());
    __publicField(this, "visibleInstances", /* @__PURE__ */ new Map());
    __publicField(this, "visibleInstancesDirty", false);
    __publicField(this, "instancesChanged", /* @__PURE__ */ new Set());
    __publicField(this, "reconcileQueue", /* @__PURE__ */ new Set());
    __publicField(this, "scrollDirty", true);
    __publicField(this, "heightDirty", true);
    __publicField(this, "scrollHeightDirty", true);
    __publicField(this, "renderedObservers", 0);
    __publicField(this, "connectQueue", /* @__PURE__ */ new Map());
    __publicField(this, "handleContainerResize", (entries) => {
      if (this.root == null) return;
      let shouldQueueUpdate = false;
      for (const entry of entries) {
        const blockSize = entry.borderBoxSize[0].blockSize;
        if (this.root instanceof Document) {
          if (blockSize !== this.scrollHeight) {
            this.scrollHeightDirty = true;
            shouldQueueUpdate = true;
            if (this.config.resizeDebugging) {
              console.log("Virtualizer: content size change", this.__id, {
                sizeChange: blockSize - lastSize,
                newSize: blockSize
              });
              lastSize = blockSize;
            }
          }
        } else if (entry.target === this.root) {
          if (blockSize !== this.height) {
            this.heightDirty = true;
            shouldQueueUpdate = true;
          }
        } else if (entry.target === this.contentContainer) {
          this.scrollHeightDirty = true;
          shouldQueueUpdate = true;
          if (this.config.resizeDebugging) {
            console.log("Virtualizer: scroller size change", this.__id, {
              sizeChange: blockSize - lastSize,
              newSize: blockSize
            });
            lastSize = blockSize;
          }
        }
      }
      if (shouldQueueUpdate) queueRender(this.computeRenderRangeAndEmit);
    });
    __publicField(this, "handleWindowResize", () => {
      if (_a8.__STOP || window.innerHeight === this.height) return;
      this.heightDirty = true;
      queueRender(this.computeRenderRangeAndEmit);
    });
    __publicField(this, "handleWindowScroll", () => {
      if (_a8.__STOP || this.root == null || !(this.root instanceof Document)) return;
      this.scrollDirty = true;
      queueRender(this.computeRenderRangeAndEmit);
    });
    __publicField(this, "handleElementScroll", () => {
      if (_a8.__STOP || this.root == null || this.root instanceof Document) return;
      this.scrollDirty = true;
      queueRender(this.computeRenderRangeAndEmit);
    });
    __publicField(this, "computeRenderRangeAndEmit", () => {
      if (_a8.__STOP) return;
      const wrapperDirty = this.heightDirty || this.scrollHeightDirty;
      let instancesHaveChanged = this.instancesChanged.size > 0;
      if (this.instancesChanged.size === 0) {
        const windowSpecs = createWindowFromScrollPosition({
          scrollTop: this.getScrollTop(),
          height: this.getHeight(),
          scrollHeight: this.getScrollHeight(),
          overscrollSize: this.config.overscrollSize
        });
        if (!wrapperDirty && areVirtualWindowSpecsEqual(this.windowSpecs, windowSpecs) && this.renderedObservers === this.observers.size && !this.visibleInstancesDirty && this.reconcileQueue.size === 0) return;
        this.windowSpecs = windowSpecs;
      }
      this.visibleInstancesDirty = false;
      this.renderedObservers = this.observers.size;
      const anchor = this.getScrollAnchor(this.height);
      const updatedInstances = /* @__PURE__ */ new Set();
      for (const instance2 of wrapperDirty ? this.observers.values() : this.visibleInstances.values()) if (instance2.onRender(wrapperDirty)) updatedInstances.add(instance2);
      for (const instance2 of this.instancesChanged) {
        if (updatedInstances.has(instance2)) continue;
        if (instance2.onRender(wrapperDirty)) updatedInstances.add(instance2);
      }
      this.scrollFix(anchor);
      for (const instance2 of updatedInstances) {
        this.reconcileQueue.delete(instance2);
        instance2.reconcileHeights();
      }
      for (const instance2 of this.reconcileQueue) if (instance2.reconcileHeights()) instancesHaveChanged = true;
      this.reconcileQueue.clear();
      instancesHaveChanged || (instancesHaveChanged = this.instancesChanged.size > 0);
      if (instancesHaveChanged) this.markDOMDirty();
      if (instancesHaveChanged || wrapperDirty) queueRender(this.computeRenderRangeAndEmit);
      updatedInstances.clear();
      this.instancesChanged.clear();
    });
    __publicField(this, "handleIntersectionChange", (entries) => {
      this.scrollDirty = true;
      for (const { target, isIntersecting } of entries) {
        if (!(target instanceof HTMLElement)) throw new Error("Virtualizer.handleIntersectionChange: target not an HTMLElement");
        const instance2 = this.observers.get(target);
        if (instance2 == null) continue;
        if (isIntersecting && !this.visibleInstances.has(target)) {
          instance2.setVisibility(true);
          this.visibleInstances.set(target, instance2);
          this.visibleInstancesDirty = true;
        } else if (!isIntersecting && this.visibleInstances.has(target)) {
          instance2.setVisibility(false);
          this.visibleInstances.delete(target);
          this.visibleInstancesDirty = true;
        }
      }
      if (this.visibleInstancesDirty) queueRender(this.computeRenderRangeAndEmit);
    });
    this.config = {
      ...DEFAULT_VIRTUALIZER_CONFIG,
      ...config
    };
  }
  setup(root2, contentContainer) {
    if (this.root != null) return;
    this.root = root2;
    this.resizeObserver = new ResizeObserver(this.handleContainerResize);
    this.intersectionObserver = new IntersectionObserver(this.handleIntersectionChange, {
      root: this.root,
      threshold: INTERSECTION_OBSERVER_THRESHOLD,
      rootMargin: `${this.config.intersectionObserverMargin}px 0px ${this.config.intersectionObserverMargin}px 0px`
    });
    if (root2 instanceof Document) this.setupWindow();
    else this.setupElement(contentContainer);
    window.__INSTANCE = this;
    window.__TOGGLE = () => {
      if (_a8.__STOP) {
        _a8.__STOP = false;
        (this.getScrollContainerElement() ?? window).scrollTo({ top: _a8.__lastScrollPosition });
        queueRender(this.computeRenderRangeAndEmit);
      } else {
        _a8.__lastScrollPosition = this.getScrollTop();
        _a8.__STOP = true;
      }
    };
    for (const [container, instance2] of this.connectQueue.entries()) this.connect(container, instance2);
    this.connectQueue.clear();
    this.markDOMDirty();
    queueRender(this.computeRenderRangeAndEmit);
  }
  instanceChanged(instance2, domDirty) {
    this.instancesChanged.add(instance2);
    if (domDirty) this.markDOMDirty();
    queueRender(this.computeRenderRangeAndEmit);
  }
  requestHeightReconcile(instance2) {
    this.reconcileQueue.add(instance2);
    queueRender(this.computeRenderRangeAndEmit);
  }
  getWindowSpecs() {
    if (this.windowSpecs.top === 0 && this.windowSpecs.bottom === 0) this.windowSpecs = createWindowFromScrollPosition({
      scrollTop: this.getScrollTop(),
      height: this.getHeight(),
      scrollHeight: this.getScrollHeight(),
      overscrollSize: this.config.overscrollSize
    });
    return this.windowSpecs;
  }
  getRoot() {
    return this.root;
  }
  isInstanceVisible(elementTop, elementHeight) {
    const scrollTop = this.getScrollTop();
    const height = this.getHeight();
    const margin = this.config.intersectionObserverMargin;
    const top = scrollTop - margin;
    const bottom = scrollTop + height + margin;
    return !(elementTop < top - elementHeight || elementTop > bottom);
  }
  setupWindow() {
    var _a9;
    if (this.root == null || !(this.root instanceof Document)) throw new Error("Virtualizer.setupWindow: Invalid setup method");
    window.addEventListener("scroll", this.handleWindowScroll, { passive: true });
    window.addEventListener("resize", this.handleWindowResize, { passive: true });
    (_a9 = this.resizeObserver) == null ? void 0 : _a9.observe(this.root.documentElement);
  }
  setupElement(contentContainer) {
    var _a9, _b;
    if (this.root == null || this.root instanceof Document) throw new Error("Virtualizer.setupElement: Invalid setup method");
    this.root.addEventListener("scroll", this.handleElementScroll, { passive: true });
    (_a9 = this.resizeObserver) == null ? void 0 : _a9.observe(this.root);
    contentContainer ?? (contentContainer = this.root.firstElementChild ?? void 0);
    if (contentContainer instanceof HTMLElement) {
      this.contentContainer = contentContainer;
      (_b = this.resizeObserver) == null ? void 0 : _b.observe(contentContainer);
    }
  }
  cleanUp() {
    var _a9, _b, _c;
    dequeueRender(this.computeRenderRangeAndEmit);
    (_a9 = this.resizeObserver) == null ? void 0 : _a9.disconnect();
    this.resizeObserver = void 0;
    (_b = this.intersectionObserver) == null ? void 0 : _b.disconnect();
    this.intersectionObserver = void 0;
    (_c = this.root) == null ? void 0 : _c.removeEventListener("scroll", this.handleElementScroll);
    window.removeEventListener("scroll", this.handleWindowScroll);
    window.removeEventListener("resize", this.handleWindowResize);
    this.root = void 0;
    this.contentContainer = void 0;
    this.observers.clear();
    this.visibleInstances.clear();
    this.instancesChanged.clear();
    this.reconcileQueue.clear();
    this.connectQueue.clear();
    this.visibleInstancesDirty = false;
    this.windowSpecs = {
      top: 0,
      bottom: 0
    };
    this.scrollTop = 0;
    this.height = 0;
    this.scrollHeight = 0;
    this.scrollDirty = true;
    this.heightDirty = true;
    this.scrollHeightDirty = true;
  }
  getOffsetInScrollContainer(element2) {
    return this.getScrollTop() + getRelativeBoundingTop(element2, this.getScrollContainerElement());
  }
  connect(container, instance2) {
    if (this.observers.has(container)) throw new Error("Virtualizer.connect: instance is already connected...");
    if (this.intersectionObserver == null) this.connectQueue.set(container, instance2);
    else {
      this.intersectionObserver.observe(container);
      this.observers.set(container, instance2);
      this.instancesChanged.add(instance2);
      this.markDOMDirty();
      queueRender(this.computeRenderRangeAndEmit);
    }
    return () => this.disconnect(container);
  }
  disconnect(container) {
    var _a9;
    const instance2 = this.observers.get(container);
    this.connectQueue.delete(container);
    if (instance2 == null) return;
    (_a9 = this.intersectionObserver) == null ? void 0 : _a9.unobserve(container);
    this.observers.delete(container);
    this.instancesChanged.delete(instance2);
    this.reconcileQueue.delete(instance2);
    if (this.visibleInstances.delete(container)) this.visibleInstancesDirty = true;
    this.markDOMDirty();
    queueRender(this.computeRenderRangeAndEmit);
  }
  scrollFix(anchor) {
    var _a9;
    if (anchor == null) return;
    const scrollContainer = this.getScrollContainerElement();
    const { lineIndex, lineOffset, fileElement, fileOffset, fileTypeOffset } = anchor;
    if (lineIndex != null && lineOffset != null) {
      const element2 = (_a9 = fileElement.shadowRoot) == null ? void 0 : _a9.querySelector(`[data-line][data-line-index="${lineIndex}"]`);
      if (element2 instanceof HTMLElement) {
        const top2 = getRelativeBoundingTop(element2, scrollContainer);
        if (top2 !== lineOffset) {
          const scrollOffset = top2 - lineOffset;
          this.applyScrollFix(scrollOffset);
        }
        return;
      }
    }
    const top = getRelativeBoundingTop(fileElement, scrollContainer);
    if (fileTypeOffset === "top") {
      if (top !== fileOffset) this.applyScrollFix(top - fileOffset);
    } else {
      const bottom = top + fileElement.getBoundingClientRect().height;
      if (bottom !== fileOffset) this.applyScrollFix(bottom - fileOffset);
    }
  }
  applyScrollFix(scrollOffset) {
    if (this.root == null || this.root instanceof Document) window.scrollTo({
      top: window.scrollY + scrollOffset,
      behavior: "instant"
    });
    else this.root.scrollTo({
      top: this.root.scrollTop + scrollOffset,
      behavior: "instant"
    });
    this.markDOMDirty();
  }
  getScrollAnchor(viewportHeight) {
    var _a9;
    const scrollContainer = this.getScrollContainerElement();
    let bestAnchor;
    for (const [fileElement] of this.visibleInstances.entries()) {
      const fileTop = getRelativeBoundingTop(fileElement, scrollContainer);
      const fileBottom = fileTop + fileElement.offsetHeight;
      let fileOffset;
      let fileTypeOffset;
      if (fileBottom <= 0) {
        fileOffset = fileBottom;
        fileTypeOffset = "bottom";
      } else {
        fileOffset = fileTop;
        fileTypeOffset = "top";
      }
      let bestLineIndex;
      let bestLineOffset;
      if (fileBottom > 0 && fileTop < viewportHeight) for (const line of ((_a9 = fileElement.shadowRoot) == null ? void 0 : _a9.querySelectorAll("[data-line][data-line-index]")) ?? []) {
        if (!(line instanceof HTMLElement)) continue;
        const lineIndex = line.dataset.lineIndex;
        if (lineIndex == null) continue;
        const lineOffset = getRelativeBoundingTop(line, scrollContainer);
        if (lineOffset < 0) continue;
        bestLineIndex = lineIndex;
        bestLineOffset = lineOffset;
        break;
      }
      if ((bestAnchor == null ? void 0 : bestAnchor.lineOffset) != null && bestLineOffset == null) continue;
      let shouldReplace = false;
      if (bestAnchor == null) shouldReplace = true;
      else if (bestLineOffset != null && (bestAnchor.lineOffset == null || bestLineOffset < bestAnchor.lineOffset)) shouldReplace = true;
      else if (bestLineOffset == null && bestAnchor.lineOffset == null) {
        if (fileOffset >= 0 && (bestAnchor.fileOffset < 0 || fileOffset < bestAnchor.fileOffset)) shouldReplace = true;
        else if (fileOffset < 0 && bestAnchor.fileOffset < 0 && fileOffset > bestAnchor.fileOffset) shouldReplace = true;
      }
      if (shouldReplace) bestAnchor = {
        fileElement,
        fileTypeOffset,
        fileOffset,
        lineIndex: bestLineIndex,
        lineOffset: bestLineOffset
      };
    }
    return bestAnchor;
  }
  /** Return the logical vertical position for the scroll container */
  getScrollTop() {
    if (!this.scrollDirty) return this.scrollTop;
    this.scrollDirty = false;
    let scrollTop = (() => {
      if (this.root == null) return 0;
      if (this.root instanceof Document) return window.scrollY;
      return this.root.scrollTop;
    })();
    scrollTop = this.clampScrollTop(scrollTop);
    this.scrollTop = scrollTop;
    return scrollTop;
  }
  /** Scroll to a logical vertical position and queue the normal render pass. */
  scrollTo({ top, behavior = "auto" }) {
    const { root: root2 } = this;
    if (root2 == null) return;
    const scrollTop = this.clampScrollTop(top);
    if (root2 instanceof Document) window.scrollTo({
      top: scrollTop,
      behavior
    });
    else root2.scrollTo({
      top: scrollTop,
      behavior
    });
    this.scrollDirty = true;
    queueRender(this.computeRenderRangeAndEmit);
  }
  clampScrollTop(scrollTop) {
    return Math.max(0, Math.min(scrollTop, this.getScrollHeight() - this.getHeight()));
  }
  getScrollHeight() {
    if (!this.scrollHeightDirty) return this.scrollHeight;
    this.scrollHeightDirty = false;
    this.scrollHeight = (() => {
      if (this.root == null) return 0;
      if (this.root instanceof Document) return this.root.documentElement.scrollHeight;
      return this.root.scrollHeight;
    })();
    return this.scrollHeight;
  }
  getHeight() {
    if (!this.heightDirty) return this.height;
    this.heightDirty = false;
    this.height = (() => {
      if (this.root == null) return 0;
      if (this.root instanceof Document) return globalThis.innerHeight;
      return this.root.getBoundingClientRect().height;
    })();
    return this.height;
  }
  markDOMDirty() {
    this.scrollDirty = true;
    this.scrollHeightDirty = true;
    this.heightDirty = true;
  }
  getScrollContainerElement() {
    return this.root == null || this.root instanceof Document ? void 0 : this.root;
  }
}, __publicField(_a8, "__STOP", false), __publicField(_a8, "__lastScrollPosition", 0), _a8);
function getRelativeBoundingTop(element2, scrollContainer) {
  const rect = element2.getBoundingClientRect();
  const scrollContainerTop = (scrollContainer == null ? void 0 : scrollContainer.getBoundingClientRect().top) ?? 0;
  return rect.top - scrollContainerTop;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/getResolvedLanguages.js
function getResolvedLanguages(languages) {
  const resolvedLanguages = [];
  for (const language of languages) {
    const resolvedLanguage = ResolvedLanguages.get(language);
    if (resolvedLanguage == null) throw new Error(`getResolvedLanguages: ${language} is not resolved. Please resolve languages before calling getResolvedLanguages`);
    resolvedLanguages.push(resolvedLanguage);
  }
  return resolvedLanguages;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/hasResolvedLanguages.js
function hasResolvedLanguages(languages) {
  for (const language of Array.isArray(languages) ? languages : [languages]) if (!ResolvedLanguages.has(language)) return false;
  return true;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/registerCustomLanguage.js
function registerCustomLanguage(lang, loader, extensionsOrFilenames = []) {
  if (lang === "text" || lang === "ansi") throw new Error("registerCustomLanguage: 'text' and 'ansi' are reserved language names");
  if (RegisteredCustomLanguages.has(lang)) {
    console.error(`registerCustomLanguage: lang: ${lang} is already registered`);
    return;
  }
  RegisteredCustomLanguages.set(lang, loader);
  for (const extension of extensionsOrFilenames) setCustomExtension(extension, lang);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/languages/resolveLanguages.js
async function resolveLanguages(languages) {
  const resolvedLanguages = [];
  const languagesToResolve = [];
  for (const language of languages) {
    if (language === "text" || language === "ansi") continue;
    const maybeResolvedLanguage = getResolvedOrResolveLanguage(language) ?? resolveLanguage(language);
    if ("then" in maybeResolvedLanguage) languagesToResolve.push(maybeResolvedLanguage);
    else resolvedLanguages.push(maybeResolvedLanguage);
  }
  if (languagesToResolve.length > 0) await Promise.all(languagesToResolve).then((_resolvedLanguages) => {
    for (const resolvedLanguage of _resolvedLanguages) {
      if (resolvedLanguage == null) throw new Error("resolvedLanguages: unable to resolve language");
      resolvedLanguages.push(resolvedLanguage);
    }
  });
  return resolvedLanguages;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/getResolvedThemes.js
function getResolvedThemes(themeNames) {
  return themeResolver.getResolvedThemes(themeNames);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/registerCustomTheme.js
function registerCustomTheme(themeName, loader) {
  try {
    const descriptor = createTheme({
      name: themeName,
      load: loader
    });
    themeResolver.registerTheme(descriptor.name, descriptor.load);
  } catch (error) {
    if (error instanceof DuplicateThemeError) {
      console.error("SharedHighlight.registerCustomTheme: theme name already registered", themeName);
      return;
    }
    throw error;
  }
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/registerCustomCSSVariableTheme.js
function registerCustomCSSVariableTheme(name, variableDefaults, fontStyle = false) {
  const theme = createCssVariablesTheme({
    name,
    variablePrefix: formatCSSVariablePrefix("global"),
    variableDefaults,
    fontStyle
  });
  registerCustomTheme(name, () => Promise.resolve(theme));
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/highlighter/themes/resolveThemes.js
async function resolveThemes2(themes2) {
  for (const themeName of themes2) prepareThemeResolution(themeName);
  const resolvedThemes = await themeResolver.resolveThemes(themes2);
  for (let i2 = 0; i2 < themes2.length; i2++) validateResolvedThemeName(themes2[i2], resolvedThemes[i2]);
  return resolvedThemes;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/annotationHelpers.js
function isDiffAnnotation(annotation) {
  return "side" in annotation;
}
function isFileAnnotation(annotation) {
  return !isDiffAnnotation(annotation);
}
function isDiffAnnotationCollection(annotations) {
  const first = annotations[0];
  return first == null || isDiffAnnotation(first);
}
function isFileAnnotationCollection(annotations) {
  const first = annotations[0];
  return first == null || isFileAnnotation(first);
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/areWorkerStatsEqual.js
function areWorkerStatsEqual(statsA, statsB) {
  if (statsA == null || statsB == null) return statsA === statsB;
  return statsA.busyWorkers === statsB.busyWorkers && statsA.diffCacheSize === statsB.diffCacheSize && statsA.fileCacheSize === statsB.fileCacheSize && statsA.managerState === statsB.managerState && statsA.activeTasks === statsB.activeTasks && statsA.queuedTasks === statsB.queuedTasks && statsA.themeSubscribers === statsB.themeSubscribers && statsA.totalWorkers === statsB.totalWorkers && statsA.workersFailed === statsB.workersFailed;
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createRowNodes.js
function createRowNodes(line) {
  const row = document.createElement("div");
  row.dataset.line = `${line}`;
  const lineColumn = document.createElement("div");
  lineColumn.dataset.columnNumber = "";
  lineColumn.textContent = `${line}`;
  const content = document.createElement("div");
  content.dataset.columnContent = "";
  row.appendChild(lineColumn);
  row.appendChild(content);
  return {
    row,
    content
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/createStyleElement.js
function createStyleElement(content, isCoreCSS = false) {
  return createHastElement({
    tagName: "style",
    children: [createTextNodeElement(isCoreCSS ? wrapCoreCSS(content) : wrapUnsafeCSS(content))],
    properties: {
      [CORE_CSS_ATTRIBUTE]: isCoreCSS ? "" : void 0,
      [UNSAFE_CSS_ATTRIBUTE]: !isCoreCSS ? "" : void 0
    }
  });
}
function createThemeStyleElement(content) {
  return createHastElement({
    tagName: "style",
    children: [createTextNodeElement(content)],
    properties: { [THEME_CSS_ATTRIBUTE]: "" }
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/diffAcceptRejectHunk.js
function diffAcceptRejectHunk(diff, hunkIndex, options) {
  const hunk = diff.hunks[hunkIndex];
  if (hunk == null) {
    console.error({
      hunkIndex,
      diff
    });
    throw new Error("diffAcceptRejectHunk: Invalid hunk index");
  }
  return resolveRegion(diff, {
    resolution: normalizeDiffResolution(options),
    hunkIndex,
    ...(() => {
      if (typeof options === "object") return {
        startContentIndex: options.changeIndex,
        endContentIndex: options.changeIndex
      };
      return {
        startContentIndex: 0,
        endContentIndex: Math.max(0, (hunk.hunkContent.length ?? 1) - 1)
      };
    })()
  });
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getLineEndingType.js
function getLineEndingType(content) {
  if (content.includes("\r\n")) return "CRLF";
  if (content.includes("\r")) return "CR";
  if (content.includes("\n")) return "LF";
  return "none";
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/getSingularPatch.js
function getSingularPatch(patch) {
  const parsedPatches = parsePatchFiles(patch);
  if (parsedPatches.length !== 1) {
    console.error(parsedPatches);
    throw new Error("PatchDiff: Provided patch must include only 1 patch, with 1 diff");
  }
  const { files } = parsedPatches[0];
  if (files.length !== 1) {
    console.error(files);
    throw new Error("FileDiff: Provided patch must contain exactly 1 file diff");
  }
  return files[0];
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/parseLineType.js
function parseLineType(line) {
  const firstChar = line[0];
  if (firstChar !== "+" && firstChar !== "-" && firstChar !== " " && firstChar !== "\\") {
    console.error(`parseLineType: Invalid firstChar: "${firstChar}", full line: "${line}"`);
    return;
  }
  const processedLine = line.substring(1);
  return {
    line: processedLine === "" ? "\n" : processedLine,
    type: firstChar === " " ? "context" : firstChar === "\\" ? "metadata" : firstChar === "+" ? "addition" : "deletion"
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/setLanguageOverride.js
function setLanguageOverride(fileOrDiff, lang) {
  return {
    ...fileOrDiff,
    lang
  };
}

// ../../node_modules/.pnpm/@pierre+diffs@1.3.5_@shikijs+themes@4.4.3_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@pierre/diffs/dist/utils/trimPatchContext.js
function trimPatchContext(patch, contextSize = 10) {
  const lines = [];
  let currentHunk;
  for (const line of patch.split("\n")) {
    const parsedHunkHeader = line.match(HUNK_HEADER);
    if (parsedHunkHeader != null) {
      if (currentHunk != null) {
        if (currentHunk.hunkLines.length > 0) {
          flushContextLines(currentHunk, contextSize, "trailing");
          flushHunk(currentHunk, lines);
        }
        currentHunk = void 0;
      }
      const additionStart = parseInt(parsedHunkHeader[3]);
      const deletionStart = parseInt(parsedHunkHeader[1]);
      const additionCount = parseInt(parsedHunkHeader[4] ?? "1");
      const deletionCount = parseInt(parsedHunkHeader[2] ?? "1");
      if (isNaN(additionStart) || isNaN(deletionStart) || isNaN(additionCount) || isNaN(deletionCount)) lines.push(line);
      else currentHunk = {
        additionStart,
        deletionStart,
        additionCount: 0,
        deletionCount: 0,
        hunkLines: [],
        contextLines: []
      };
      continue;
    }
    if (currentHunk == null) {
      lines.push(line);
      continue;
    }
    if (line.startsWith(" ")) currentHunk.contextLines.push(line);
    else if (line !== "") {
      if (currentHunk.hunkLines.length > 0 && currentHunk.contextLines.length > contextSize * 2) {
        const omittedContextLineCount = currentHunk.contextLines.length - contextSize * 2;
        const nextContextLines = currentHunk.contextLines.slice(-contextSize);
        flushContextLines(currentHunk, contextSize, "trailing");
        const { additionCount: emittedAdditionCount, deletionCount: emittedDeletionCount } = currentHunk;
        flushHunk(currentHunk, lines);
        currentHunk = {
          additionStart: currentHunk.additionStart + emittedAdditionCount + omittedContextLineCount,
          deletionStart: currentHunk.deletionStart + emittedDeletionCount + omittedContextLineCount,
          deletionCount: 0,
          additionCount: 0,
          contextLines: nextContextLines,
          hunkLines: []
        };
      }
      flushContextLines(currentHunk, contextSize, currentHunk.hunkLines.length === 0 ? "leading" : "before-change");
      currentHunk.hunkLines.push(line);
      if (line.startsWith("+")) currentHunk.additionCount += 1;
      else if (line.startsWith("-")) currentHunk.deletionCount += 1;
    }
  }
  if (currentHunk != null && currentHunk.hunkLines.length > 0) {
    flushContextLines(currentHunk, contextSize, "trailing");
    flushHunk(currentHunk, lines);
  }
  const result = lines.join("\n");
  return patch.endsWith("\n") ? `${result}
` : result;
}
function flushContextLines(hunk, contextSize, mode) {
  if (mode === "leading" && hunk.contextLines.length > contextSize) {
    const difference = hunk.contextLines.length - contextSize;
    hunk.contextLines.splice(0, difference);
    hunk.additionStart += difference;
    hunk.deletionStart += difference;
  }
  if (mode === "trailing" && hunk.contextLines.length > contextSize) hunk.contextLines.length = contextSize;
  if (hunk.contextLines.length > 0) {
    hunk.hunkLines.push(...hunk.contextLines);
    hunk.additionCount += hunk.contextLines.length;
    hunk.deletionCount += hunk.contextLines.length;
    hunk.contextLines.length = 0;
  }
  return hunk;
}
function flushHunk(hunk, lines) {
  lines.push(`@@ -${formatHunkRange2(hunk.deletionStart, hunk.deletionCount)} +${formatHunkRange2(hunk.additionStart, hunk.additionCount)} @@`);
  lines.push(...hunk.hunkLines);
}
function formatHunkRange2(start, count) {
  return count === 1 ? `${start}` : `${start},${count}`;
}
export {
  ALTERNATE_FILE_NAMES_GIT,
  AttachedLanguages,
  AttachedThemes,
  CODE_VIEW_DIFF_OPTION_KEYS,
  CODE_VIEW_FILE_OPTION_KEYS,
  CODE_VIEW_FOOTER_ATTRIBUTE,
  CODE_VIEW_HEADER_ATTRIBUTE,
  COMMIT_METADATA_SPLIT,
  CORE_CSS_ATTRIBUTE,
  CUSTOM_HEADER_SLOT_ID,
  CodeToTokenTransformStream,
  CodeView,
  DEFAULT_CODE_VIEW_FILE_METRICS,
  DEFAULT_CODE_VIEW_LAYOUT,
  DEFAULT_COLLAPSED_CONTEXT_THRESHOLD,
  DEFAULT_EXPANDED_REGION,
  DEFAULT_RENDER_RANGE,
  DEFAULT_SMOOTH_SCROLL_SETTINGS,
  DEFAULT_THEMES,
  DEFAULT_TOKENIZE_MAX_LENGTH,
  DEFAULT_VIRTUAL_FILE_METRICS,
  DIFFS_DEVELOPMENT_BUILD,
  DIFFS_SCROLLBAR_GUTTER_MEASURED_PROPERTY,
  DIFFS_SCROLLBAR_MEASURE_ATTRIBUTE,
  DIFFS_TAG_NAME,
  DiffHunksRenderer,
  EMPTY_RENDER_RANGE,
  EXTENSION_TO_FILE_FORMAT,
  FILENAME_HEADER_REGEX,
  FILENAME_HEADER_REGEX_GIT,
  FILE_CONTEXT_BLOB,
  File,
  FileDiff,
  FileRenderer,
  FileStream,
  GIT_DIFF_FILE_BREAK_REGEX,
  HEADER_FILENAME_SUFFIX_SLOT_ID,
  HEADER_METADATA_SLOT_ID,
  HEADER_PREFIX_SLOT_ID,
  HUNK_HEADER,
  INDEX_LINE_METADATA,
  InteractionManager,
  MERGE_CONFLICT_BASE_MARKER_REGEX,
  MERGE_CONFLICT_END_MARKER_REGEX,
  MERGE_CONFLICT_SEPARATOR_MARKER_REGEX,
  MERGE_CONFLICT_START_MARKER_REGEX,
  RegisteredCustomLanguages,
  ResizeManager,
  ResolvedLanguages,
  ResolvingLanguages,
  SPLIT_WITH_NEWLINES,
  SVGSpriteSheet,
  ScrollSyncManager,
  ShikiStreamTokenizer,
  THEME_CSS_ATTRIBUTE,
  UNIFIED_DIFF_FILE_BREAK_REGEX,
  UNSAFE_CSS_ATTRIBUTE,
  UnresolvedFile,
  VIRTUALIZED_FILE_DIFF_LAYOUT_CHECKPOINT_INTERVAL,
  VirtualizedFile,
  VirtualizedFileDiff,
  Virtualizer,
  areDiffLineAnnotationsEqual,
  areDiffRenderOptionsEqual,
  areDiffTargetsEqual,
  areFileRenderOptionsEqual,
  areFilesEqual,
  areHunkDataEqual,
  areLanguagesAttached,
  areLineAnnotationsEqual,
  areObjectsEqual,
  areOptionsEqual,
  arePrePropertiesEqual,
  areRenderRangesEqual,
  areSelectionsEqual,
  areThemesAttached,
  areThemesEqual,
  areVirtualWindowSpecsEqual,
  areWorkerStatsEqual,
  attachResolvedLanguages,
  attachResolvedThemes,
  cleanLastNewline,
  cleanUpResolvedLanguages,
  cleanUpResolvedThemes,
  clearRenderQueue,
  cloneFileDiffMetadata,
  codeToHtml2 as codeToHtml,
  createAnnotationElement,
  createAnnotationWrapperNode,
  createCssVariablesTheme as createCSSVariablesTheme,
  createDiffSpanDecoration,
  createEmptyRowBuffer,
  createFileHeaderElement,
  createGutterGap,
  createGutterItem,
  createGutterUtilityContentNode,
  createGutterUtilityElement,
  createGutterWrapper,
  createHastElement,
  createIconElement,
  createNoNewlineElement,
  createPreElement,
  createPreWrapperProperties,
  createRowNodes,
  createSeparator,
  createSpanFromToken,
  createStyleElement,
  createTextNodeElement,
  createThemeStyleElement,
  createTransformerWithState,
  createUnsafeCSSStyleNode,
  createWindowFromScrollPosition,
  dequeueRender,
  detachString,
  diffAcceptRejectHunk,
  disposeHighlighter,
  findCodeElement,
  formatCSSVariablePrefix,
  getCustomExtensionsMap,
  getCustomExtensionsVersion,
  getFiletypeFromFileName,
  getHighlighterIfLoaded,
  getHighlighterOptions,
  getHighlighterThemeStyles,
  getHunkSeparatorSlotName,
  getIconForType,
  getLineAnnotationName,
  getLineEndingType,
  getLineNodes,
  getOrCreateCodeNode,
  getResolvedLanguages,
  getResolvedOrResolveLanguage,
  getResolvedOrResolveTheme,
  getResolvedThemes,
  getSharedHighlighter,
  getSingularPatch,
  getThemes,
  getTotalLineCountFromHunks,
  getUnresolvedDiffHunksRendererOptions,
  hasResolvedLanguages,
  hasResolvedThemes,
  hydratePartialDiff,
  isDefaultRenderRange,
  isDiffAnnotation,
  isDiffAnnotationCollection,
  isFileAnnotation,
  isFileAnnotationCollection,
  isHighlighterLoaded,
  isHighlighterLoading,
  isHighlighterNull,
  isWorkerContext,
  parseDiffFromFile,
  parseLineType,
  parsePatchFiles,
  patchScrollbarGutterSize,
  pluckInteractionOptions,
  prefersReducedMotion,
  preloadHighlighter,
  prerenderHTMLIfNecessary,
  processFile,
  processLine,
  processPatch,
  pushOrJoinSpan,
  queueRender,
  registerCustomCSSVariableTheme,
  registerCustomLanguage,
  registerCustomTheme,
  releaseStringDetachBuffer,
  renderDiffWithHighlighter,
  renderFileWithHighlighter,
  replaceCustomExtensions,
  resolveConflict,
  resolveLanguage,
  resolveLanguages,
  resolveRegion,
  resolveTheme,
  resolveThemes2 as resolveThemes,
  setCustomExtension,
  setLanguageOverride,
  setPreNodeProperties,
  trimPatchContext,
  wrapCoreCSS,
  wrapThemeCSS,
  wrapUnsafeCSS
};
//# sourceMappingURL=dist-S2DD3KSM.js.map
