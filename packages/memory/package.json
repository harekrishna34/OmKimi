{
  "name": "@moonshot-ai/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent cross-session memory for Kimi Code (SQLite + FTS5)",
  "license": "MIT",
  "author": "Moonshot AI",
  "type": "module",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12"
  }
}
