{
  "name": "opencode-llm-server",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible proxy exposing the full OpenCode Zen model catalog (models.dev) with a static free-model fallback",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
